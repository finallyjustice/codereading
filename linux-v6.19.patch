From 34d89d3d00630bdc11778400f86e38b208823e54 Mon Sep 17 00:00:00 2001
From: Dongli Zhang <dongli.zhang@oracle.com>
Date: Sun, 15 Mar 2026 10:06:22 -0700
Subject: [PATCH 1/1] linux-v6.19

Signed-off-by: Dongli Zhang <dongli.zhang0129@gmail.com>
---
 arch/arm64/kvm/sys_regs.c          |  34 +++
 arch/x86/events/amd/core.c         |  25 ++
 arch/x86/events/core.c             |  68 +++++
 arch/x86/events/perf_event.h       |   5 +
 arch/x86/include/asm/kvm_host.h    |  14 +
 arch/x86/kvm/pmu.c                 |  70 +++++
 arch/x86/kvm/pmu.h                 |  40 +++
 arch/x86/kvm/svm/avic.c            | 213 ++++++++++++++
 arch/x86/kvm/svm/svm.h             |  38 +++
 arch/x86/kvm/x86.c                 |   5 +
 block/blk-mq.c                     |  10 +
 block/kyber-iosched.c              |  10 +
 drivers/nvme/target/rdma.c         |  10 +
 drivers/pci/hotplug/pciehp_ctrl.c  |  56 ++++
 drivers/pci/hotplug/pciehp_hpc.c   |  28 ++
 drivers/pci/hotplug/pciehp_pci.c   |  28 ++
 drivers/pci/remove.c               |  28 ++
 drivers/scsi/hosts.c               |  77 ++++++
 drivers/scsi/scsi.c                |  29 ++
 drivers/scsi/scsi_lib.c            |  86 ++++++
 drivers/scsi/scsi_proc.c           |   8 +
 drivers/scsi/scsi_scan.c           | 427 +++++++++++++++++++++++++++++
 drivers/scsi/scsi_sysfs.c          | 130 +++++++++
 drivers/scsi/virtio_scsi.c         | 108 ++++++++
 drivers/vhost/scsi.c               |  10 +
 drivers/virtio/virtio.c            |  31 +++
 drivers/virtio/virtio_pci_common.c |  28 ++
 include/scsi/scsi_device.h         |  23 ++
 include/scsi/scsi_host.h           |  19 ++
 lib/sbitmap.c                      |  20 ++
 30 files changed, 1678 insertions(+)

diff --git a/arch/arm64/kvm/sys_regs.c b/arch/arm64/kvm/sys_regs.c
index 88a57ca36..af0408d98 100644
--- a/arch/arm64/kvm/sys_regs.c
+++ b/arch/arm64/kvm/sys_regs.c
@@ -4099,6 +4099,14 @@ static bool handle_tlbi_el1(struct kvm_vcpu *vcpu, struct sys_reg_params *p,
 		.access = (access_fn),					\
 	}
 
+/*
+ * 在以下使用sys_insn_descs[]:
+ *   - arch/arm64/kvm/sys_regs.c|4102| <<global>> static struct sys_reg_desc sys_insn_descs[] = {
+ *   - arch/arm64/kvm/sys_regs.c|5181| <<kvm_handle_sys_reg>> desc = &sys_insn_descs[sr_idx];
+ *   - arch/arm64/kvm/sys_regs.c|5668| <<kvm_sys_reg_table_init>> valid &= check_sysreg_table(sys_insn_descs, ARRAY_SIZE(sys_insn_descs), false);
+ *   - arch/arm64/kvm/sys_regs.c|5685| <<kvm_sys_reg_table_init>> for (i = 0; !ret && i < ARRAY_SIZE(sys_insn_descs); i++)
+ *   - arch/arm64/kvm/sys_regs.c|5686| <<kvm_sys_reg_table_init>> ret = populate_sysreg_config(sys_insn_descs + i, i);
+ */
 static struct sys_reg_desc sys_insn_descs[] = {
 	{ SYS_DESC(SYS_DC_ISW), access_dcsw },
 	{ SYS_DESC(SYS_DC_IGSW), access_dcgsw },
@@ -4651,6 +4659,12 @@ int kvm_handle_cp14_load_store(struct kvm_vcpu *vcpu)
 	return 1;
 }
 
+/*
+ * 在以下使用perform_access():
+ *   - arch/arm64/kvm/sys_regs.c|4704| <<emulate_cp>> perform_access(vcpu, params, r);
+ *   - arch/arm64/kvm/sys_regs.c|4986| <<emulate_sys_reg>> perform_access(vcpu, params, r);
+ *   - arch/arm64/kvm/sys_regs.c|5183| <<kvm_handle_sys_reg>> perform_access(vcpu, &params, desc);
+ */
 static void perform_access(struct kvm_vcpu *vcpu,
 			   struct sys_reg_params *params,
 			   const struct sys_reg_desc *r)
@@ -5158,6 +5172,10 @@ void kvm_reset_sys_regs(struct kvm_vcpu *vcpu)
  *			 trap on a guest execution
  * @vcpu: The VCPU pointer
  */
+/*
+ * 在以下使用kvm_handle_sys_reg():
+ *   - arch/arm64/kvm/handle_exit.c|391| <<global>> [ESR_ELx_EC_SYS64] = kvm_handle_sys_reg,
+ */
 int kvm_handle_sys_reg(struct kvm_vcpu *vcpu)
 {
 	const struct sys_reg_desc *desc = NULL;
@@ -5174,6 +5192,14 @@ int kvm_handle_sys_reg(struct kvm_vcpu *vcpu)
 	params = esr_sys64_to_params(esr);
 	params.regval = vcpu_get_reg(vcpu, Rt);
 
+	/*
+	 * 在以下使用sys_insn_descs[]:
+	 *   - arch/arm64/kvm/sys_regs.c|4102| <<global>> static struct sys_reg_desc sys_insn_descs[] = {
+	 *   - arch/arm64/kvm/sys_regs.c|5181| <<kvm_handle_sys_reg>> desc = &sys_insn_descs[sr_idx];
+	 *   - arch/arm64/kvm/sys_regs.c|5668| <<kvm_sys_reg_table_init>> valid &= check_sysreg_table(sys_insn_descs, ARRAY_SIZE(sys_insn_descs), false);
+	 *   - arch/arm64/kvm/sys_regs.c|5685| <<kvm_sys_reg_table_init>> for (i = 0; !ret && i < ARRAY_SIZE(sys_insn_descs); i++)
+	 *   - arch/arm64/kvm/sys_regs.c|5686| <<kvm_sys_reg_table_init>> ret = populate_sysreg_config(sys_insn_descs + i, i);
+	 */
 	/* System registers have Op0=={2,3}, as per DDI487 J.a C5.1.2 */
 	if (params.Op0 == 2 || params.Op0 == 3)
 		desc = &sys_reg_descs[sr_idx];
@@ -5665,6 +5691,14 @@ int __init kvm_sys_reg_table_init(void)
 	valid &= check_sysreg_table(cp14_64_regs, ARRAY_SIZE(cp14_64_regs), false);
 	valid &= check_sysreg_table(cp15_regs, ARRAY_SIZE(cp15_regs), false);
 	valid &= check_sysreg_table(cp15_64_regs, ARRAY_SIZE(cp15_64_regs), false);
+	/*
+	 * 在以下使用sys_insn_descs[]:
+	 *   - arch/arm64/kvm/sys_regs.c|4102| <<global>> static struct sys_reg_desc sys_insn_descs[] = {
+	 *   - arch/arm64/kvm/sys_regs.c|5181| <<kvm_handle_sys_reg>> desc = &sys_insn_descs[sr_idx];
+	 *   - arch/arm64/kvm/sys_regs.c|5668| <<kvm_sys_reg_table_init>> valid &= check_sysreg_table(sys_insn_descs, ARRAY_SIZE(sys_insn_descs), false);
+	 *   - arch/arm64/kvm/sys_regs.c|5685| <<kvm_sys_reg_table_init>> for (i = 0; !ret && i < ARRAY_SIZE(sys_insn_descs); i++)
+	 *   - arch/arm64/kvm/sys_regs.c|5686| <<kvm_sys_reg_table_init>> ret = populate_sysreg_config(sys_insn_descs + i, i);
+	 */
 	valid &= check_sysreg_table(sys_insn_descs, ARRAY_SIZE(sys_insn_descs), false);
 
 	gicv3_regs = vgic_v3_get_sysreg_table(&sz);
diff --git a/arch/x86/events/amd/core.c b/arch/x86/events/amd/core.c
index 44656d2fb..4cda7836c 100644
--- a/arch/x86/events/amd/core.c
+++ b/arch/x86/events/amd/core.c
@@ -740,6 +740,11 @@ static void amd_pmu_check_overflow(void)
 	 * ARCH_PERFMON_EVENTSEL_INT is always set.
 	 */
 	for_each_set_bit(idx, x86_pmu.cntr_mask, X86_PMC_IDX_MAX) {
+		/*
+		 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+		 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+		 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+		 */
 		if (!test_bit(idx, cpuc->active_mask))
 			continue;
 
@@ -760,6 +765,11 @@ static void amd_pmu_enable_all(int added)
 	amd_brs_enable_all();
 
 	for_each_set_bit(idx, x86_pmu.cntr_mask, X86_PMC_IDX_MAX) {
+		/*
+		 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+		 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+		 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+		 */
 		/* only activate events which are marked as active */
 		if (!test_bit(idx, cpuc->active_mask))
 			continue;
@@ -989,11 +999,26 @@ static int amd_pmu_v2_handle_irq(struct pt_regs *regs)
 	status &= amd_pmu_global_cntr_mask;
 
 	for_each_set_bit(idx, x86_pmu.cntr_mask, X86_PMC_IDX_MAX) {
+		/*
+		 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+		 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+		 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+		 */
 		if (!test_bit(idx, cpuc->active_mask))
 			continue;
 
 		event = cpuc->events[idx];
 		hwc = &event->hw;
+		/*
+		 * 在以下使用x86_perf_event_update():
+		 *   - arch/x86/events/amd/core.c|997| <<amd_pmu_v2_handle_irq>> x86_perf_event_update(event);
+		 *   - arch/x86/events/core.c|2171| <<init_hw_perf_events>> x86_pmu.update = x86_perf_event_update;
+		 *   - arch/x86/events/intel/core.c|2882| <<intel_pmu_read_event>> x86_perf_event_update(event);
+		 *   - arch/x86/events/intel/core.c|3178| <<intel_pmu_update>> return x86_perf_event_update(event);
+		 *   - arch/x86/events/intel/p4.c|1063| <<p4_pmu_handle_irq>> val = x86_perf_event_update(event);
+		 *   - arch/x86/events/perf_event.h|1198| <<intel_pmu_topdown_event_update>> return x86_perf_event_update(event);
+		 *   - arch/x86/events/zhaoxin/core.c|395| <<zhaoxin_pmu_handle_irq>> x86_perf_event_update(event);
+		 */
 		x86_perf_event_update(event);
 		mask = BIT_ULL(idx);
 
diff --git a/arch/x86/events/core.c b/arch/x86/events/core.c
index 576baa9a5..83ad409e2 100644
--- a/arch/x86/events/core.c
+++ b/arch/x86/events/core.c
@@ -122,6 +122,16 @@ u64 __read_mostly hw_cache_extra_regs
  * Can only be executed on the CPU where the event is active.
  * Returns the delta events processed.
  */
+/*
+ * 在以下使用x86_perf_event_update():
+ *   - arch/x86/events/amd/core.c|997| <<amd_pmu_v2_handle_irq>> x86_perf_event_update(event);
+ *   - arch/x86/events/core.c|2171| <<init_hw_perf_events>> x86_pmu.update = x86_perf_event_update;
+ *   - arch/x86/events/intel/core.c|2882| <<intel_pmu_read_event>> x86_perf_event_update(event);
+ *   - arch/x86/events/intel/core.c|3178| <<intel_pmu_update>> return x86_perf_event_update(event);
+ *   - arch/x86/events/intel/p4.c|1063| <<p4_pmu_handle_irq>> val = x86_perf_event_update(event);
+ *   - arch/x86/events/perf_event.h|1198| <<intel_pmu_topdown_event_update>> return x86_perf_event_update(event);
+ *   - arch/x86/events/zhaoxin/core.c|395| <<zhaoxin_pmu_handle_irq>> x86_perf_event_update(event);
+ */
 u64 x86_perf_event_update(struct perf_event *event)
 {
 	struct hw_perf_event *hwc = &event->hw;
@@ -708,6 +718,11 @@ void x86_pmu_disable_all(void)
 		struct hw_perf_event *hwc = &cpuc->events[idx]->hw;
 		u64 val;
 
+		/*
+		 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+		 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+		 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+		 */
 		if (!test_bit(idx, cpuc->active_mask))
 			continue;
 		rdmsrq(x86_pmu_config_addr(idx), val);
@@ -764,6 +779,11 @@ void x86_pmu_enable_all(int added)
 	for_each_set_bit(idx, x86_pmu.cntr_mask, X86_PMC_IDX_MAX) {
 		struct hw_perf_event *hwc = &cpuc->events[idx]->hw;
 
+		/*
+		 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+		 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+		 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+		 */
 		if (!test_bit(idx, cpuc->active_mask))
 			continue;
 
@@ -1543,6 +1563,11 @@ static void x86_pmu_start(struct perf_event *event, int flags)
 
 	event->hw.state = 0;
 
+	/*
+	 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+	 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+	 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+	 */
 	__set_bit(idx, cpuc->active_mask);
 	static_call(x86_pmu_enable)(event);
 	perf_event_update_userpage(event);
@@ -1618,6 +1643,11 @@ void x86_pmu_stop(struct perf_event *event, int flags)
 	struct cpu_hw_events *cpuc = this_cpu_ptr(&cpu_hw_events);
 	struct hw_perf_event *hwc = &event->hw;
 
+	/*
+	 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+	 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+	 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+	 */
 	if (test_bit(hwc->idx, cpuc->active_mask)) {
 		static_call(x86_pmu_disable)(event);
 		__clear_bit(hwc->idx, cpuc->active_mask);
@@ -1718,13 +1748,37 @@ int x86_pmu_handle_irq(struct pt_regs *regs)
 	apic_write(APIC_LVTPC, APIC_DM_NMI);
 
 	for_each_set_bit(idx, x86_pmu.cntr_mask, X86_PMC_IDX_MAX) {
+		/*
+		 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+		 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+		 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+		 */
 		if (!test_bit(idx, cpuc->active_mask))
 			continue;
 
 		event = cpuc->events[idx];
 		last_period = event->hw.last_period;
 
+		/*
+		 * 在以下使用x86_perf_event_update():
+		 *   - arch/x86/events/amd/core.c|997| <<amd_pmu_v2_handle_irq>> x86_perf_event_update(event);
+		 *   - arch/x86/events/core.c|2171| <<init_hw_perf_events>> x86_pmu.update = x86_perf_event_update;
+		 *   - arch/x86/events/intel/core.c|2882| <<intel_pmu_read_event>> x86_perf_event_update(event);
+		 *   - arch/x86/events/intel/core.c|3178| <<intel_pmu_update>> return x86_perf_event_update(event);
+		 *   - arch/x86/events/intel/p4.c|1063| <<p4_pmu_handle_irq>> val = x86_perf_event_update(event);
+		 *   - arch/x86/events/perf_event.h|1198| <<intel_pmu_topdown_event_update>> return x86_perf_event_update(event);
+		 *   - arch/x86/events/zhaoxin/core.c|395| <<zhaoxin_pmu_handle_irq>> x86_perf_event_update(event);
+		 *
+		 *
+		 * x86_perf_event_update()
+		 */
 		val = static_call(x86_pmu_update)(event);
+		/*
+		 * cntval_bits在AMD上是48 (0-47是counter的bit)
+		 * x86_pmu.cntval_bits - 1就是47
+		 * 1往左移动47就是bit=47
+		 * 这个bit如果是1就是overflow了
+		 */
 		if (val & (1ULL << (x86_pmu.cntval_bits - 1)))
 			continue;
 
@@ -1775,6 +1829,10 @@ perf_event_nmi_handler(unsigned int cmd, struct pt_regs *regs)
 		return NMI_DONE;
 
 	start_clock = sched_clock();
+	/*
+	 * amd_pmu_handle_irq()
+	 * amd_pmu_v2_handle_irq()
+	 */
 	ret = static_call(x86_pmu_handle_irq)(regs);
 	finish_clock = sched_clock();
 
@@ -2167,6 +2225,16 @@ static int __init init_hw_perf_events(void)
 	if (!x86_pmu.set_period)
 		x86_pmu.set_period = x86_perf_event_set_period;
 
+	/*
+	 * 在以下使用x86_perf_event_update():
+	 *   - arch/x86/events/amd/core.c|997| <<amd_pmu_v2_handle_irq>> x86_perf_event_update(event);
+	 *   - arch/x86/events/core.c|2171| <<init_hw_perf_events>> x86_pmu.update = x86_perf_event_update;
+	 *   - arch/x86/events/intel/core.c|2882| <<intel_pmu_read_event>> x86_perf_event_update(event);
+	 *   - arch/x86/events/intel/core.c|3178| <<intel_pmu_update>> return x86_perf_event_update(event);
+	 *   - arch/x86/events/intel/p4.c|1063| <<p4_pmu_handle_irq>> val = x86_perf_event_update(event);
+	 *   - arch/x86/events/perf_event.h|1198| <<intel_pmu_topdown_event_update>> return x86_perf_event_update(event);
+	 *   - arch/x86/events/zhaoxin/core.c|395| <<zhaoxin_pmu_handle_irq>> x86_perf_event_update(event);
+	 */
 	if (!x86_pmu.update)
 		x86_pmu.update = x86_perf_event_update;
 
diff --git a/arch/x86/events/perf_event.h b/arch/x86/events/perf_event.h
index ad35c5462..cce5d7616 100644
--- a/arch/x86/events/perf_event.h
+++ b/arch/x86/events/perf_event.h
@@ -256,6 +256,11 @@ struct cpu_hw_events {
 	 * Generic x86 PMC bits
 	 */
 	struct perf_event	*events[X86_PMC_IDX_MAX]; /* in counter order */
+	/*
+	 * 在以下修改cpu_hw_events->active_mask[BITS_TO_LONGS(MAX_HWEVENTS)]:
+	 *   - arch/x86/events/core.c|1546| <<x86_pmu_start>> __set_bit(idx, cpuc->active_mask);
+	 *   - arch/x86/events/core.c|1623| <<x86_pmu_stop>> __clear_bit(hwc->idx, cpuc->active_mask);
+	 */
 	unsigned long		active_mask[BITS_TO_LONGS(X86_PMC_IDX_MAX)];
 	unsigned long		dirty[BITS_TO_LONGS(X86_PMC_IDX_MAX)];
 	int			enabled;
diff --git a/arch/x86/include/asm/kvm_host.h b/arch/x86/include/asm/kvm_host.h
index 5a3bfa293..a5bf5aa1d 100644
--- a/arch/x86/include/asm/kvm_host.h
+++ b/arch/x86/include/asm/kvm_host.h
@@ -574,6 +574,20 @@ struct kvm_pmu {
 	 * filter changes.
 	 */
 	union {
+		/*
+		 * 在以下使用kvm_pmu->reprogram_pmi:
+		 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+		 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+		 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+		 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+		 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+		 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+		 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+		 * 在以下使用kvm_pmu->__reprogram_pmi:
+		 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+		 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+		 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+		 */
 		DECLARE_BITMAP(reprogram_pmi, X86_PMC_IDX_MAX);
 		atomic64_t __reprogram_pmi;
 	};
diff --git a/arch/x86/kvm/pmu.c b/arch/x86/kvm/pmu.c
index 487ad19a2..b67e01bfd 100644
--- a/arch/x86/kvm/pmu.c
+++ b/arch/x86/kvm/pmu.c
@@ -186,6 +186,20 @@ static void kvm_perf_overflow(struct perf_event *perf_event,
 {
 	struct kvm_pmc *pmc = perf_event->overflow_handler_context;
 
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	/*
 	 * Ignore asynchronous overflow events for counters that are scheduled
 	 * to be reprogrammed, e.g. if a PMI for the previous event races with
@@ -592,6 +606,20 @@ void kvm_pmu_handle_event(struct kvm_vcpu *vcpu)
 	struct kvm_pmc *pmc;
 	int bit;
 
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
 
 	/*
@@ -600,6 +628,20 @@ void kvm_pmu_handle_event(struct kvm_vcpu *vcpu)
 	 * the bits that will actually processed.
 	 */
 	BUILD_BUG_ON(sizeof(bitmap) != sizeof(atomic64_t));
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
 
 	kvm_for_each_pmc(pmu, pmc, bit, bitmap) {
@@ -828,6 +870,20 @@ static void kvm_pmu_reset(struct kvm_vcpu *vcpu)
 
 	pmu->need_cleanup = false;
 
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
 
 	kvm_for_each_pmc(pmu, pmc, i, pmu->all_valid_pmc_idx) {
@@ -1138,6 +1194,20 @@ int kvm_vm_ioctl_set_pmu_event_filter(struct kvm *kvm, void __user *argp)
 	BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
 		     sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
 
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	kvm_for_each_vcpu(i, vcpu, kvm)
 		atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
 
diff --git a/arch/x86/kvm/pmu.h b/arch/x86/kvm/pmu.h
index 5c3939e91..b698bc6a9 100644
--- a/arch/x86/kvm/pmu.h
+++ b/arch/x86/kvm/pmu.h
@@ -178,14 +178,40 @@ void kvm_init_pmu_capability(const struct kvm_pmu_ops *pmu_ops);
 
 void kvm_pmu_recalc_pmc_emulation(struct kvm_pmu *pmu, struct kvm_pmc *pmc);
 
+/*
+ * 在以下使用kvm_pmu_request_counter_reprogram():
+ *   - arch/x86/kvm/pmu.c|938| <<kvm_pmu_incr_counter>> kvm_pmu_request_counter_reprogram(pmc);
+ *   - arch/x86/kvm/svm/pmu.c|169| <<amd_pmu_set_msr>> kvm_pmu_request_counter_reprogram(pmc);
+ *   - arch/x86/kvm/vmx/pmu_intel.c|74| <<reprogram_fixed_counters>> kvm_pmu_request_counter_reprogram(pmc);
+ *   - arch/x86/kvm/vmx/pmu_intel.c|446| <<intel_pmu_set_msr>> kvm_pmu_request_counter_reprogram(pmc);
+ */
 static inline void kvm_pmu_request_counter_reprogram(struct kvm_pmc *pmc)
 {
 	kvm_pmu_recalc_pmc_emulation(pmc_to_pmu(pmc), pmc);
 
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
 	kvm_make_request(KVM_REQ_PMU, pmc->vcpu);
 }
 
+/*
+ * 在以下使用reprogram_counters():
+ *   - arch/x86/kvm/pmu.c|796| <<kvm_pmu_set_msr(MSR_CORE_PERF_GLOBAL_CTRL)>> reprogram_counters(pmu, diff);
+ *   - arch/x86/kvm/vmx/pmu_intel.c|406| <<intel_pmu_set_msr(MSR_IA32_PEBS_ENABLE)>> reprogram_counters(pmu, diff);
+ */
 static inline void reprogram_counters(struct kvm_pmu *pmu, u64 diff)
 {
 	int bit;
@@ -193,6 +219,20 @@ static inline void reprogram_counters(struct kvm_pmu *pmu, u64 diff)
 	if (!diff)
 		return;
 
+	/*
+	 * 在以下使用kvm_pmu->reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|194| <<kvm_perf_overflow>> if (test_and_set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi))
+	 *   - arch/x86/kvm/pmu.c|595| <<kvm_pmu_handle_event>> bitmap_copy(bitmap, pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|613| <<kvm_pmu_handle_event>> set_bit(pmc->idx, pmu->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|831| <<kvm_pmu_reset>> bitmap_zero(pmu->reprogram_pmi, X86_PMC_IDX_MAX);
+	 *   - arch/x86/kvm/pmu.c|1138| <<kvm_vm_ioctl_set_pmu_event_filter>> BUILD_BUG_ON(sizeof(((struct kvm_pmu *)0)->reprogram_pmi) >
+	 *   - arch/x86/kvm/pmu.h|185| <<kvm_pmu_request_counter_reprogram>> set_bit(pmc->idx, pmc_to_pmu(pmc)->reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.h|197| <<reprogram_counters>> set_bit(bit, pmu->reprogram_pmi);
+	 * 在以下使用kvm_pmu->__reprogram_pmi:
+	 *   - arch/x86/kvm/pmu.c|603| <<kvm_pmu_handle_event>> atomic64_andnot(*(s64 *)bitmap, &pmu->__reprogram_pmi);
+	 *   - arch/x86/kvm/pmu.c|1139| <<kvm_vm_ioctl_set_pmu_event_filter>> sizeof(((struct kvm_pmu *)0)->__reprogram_pmi));
+	 *   - arch/x86/kvm/pmu.c|1142| <<kvm_vm_ioctl_set_pmu_event_filter>> atomic64_set(&vcpu_to_pmu(vcpu)->__reprogram_pmi, -1ull);
+	 */
 	for_each_set_bit(bit, (unsigned long *)&diff, X86_PMC_IDX_MAX)
 		set_bit(bit, pmu->reprogram_pmi);
 	kvm_make_request(KVM_REQ_PMU, pmu_to_vcpu(pmu));
diff --git a/arch/x86/kvm/svm/avic.c b/arch/x86/kvm/svm/avic.c
index 0f6c85967..9bf0c4301 100644
--- a/arch/x86/kvm/svm/avic.c
+++ b/arch/x86/kvm/svm/avic.c
@@ -255,6 +255,14 @@ static int avic_ga_log_notifier(u32 ga_tag)
 
 	spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
 	hash_for_each_possible(svm_vm_data_hash, kvm_svm, hnode, vm_id) {
+		/*
+		 * 在以下使用kvm_svm->avic_vm_id:
+		 *   - arch/x86/kvm/svm/avic.c|258| <<avic_ga_log_notifier>> if (kvm_svm->avic_vm_id != vm_id)
+		 *   - arch/x86/kvm/svm/avic.c|364| <<avic_vm_init>> if (k2->avic_vm_id == vm_id)
+		 *   - arch/x86/kvm/svm/avic.c|368| <<avic_vm_init>> kvm_svm->avic_vm_id = vm_id;
+		 *   - arch/x86/kvm/svm/avic.c|369| <<avic_vm_init>> hash_add(svm_vm_data_hash, &kvm_svm->hnode, kvm_svm->avic_vm_id);
+		 *   - arch/x86/kvm/svm/avic.c|943| <<avic_pi_update_irte>> .ga_tag = AVIC_GATAG(to_kvm_svm(kvm)->avic_vm_id,
+		 */
 		if (kvm_svm->avic_vm_id != vm_id)
 			continue;
 		vcpu = kvm_get_vcpu(&kvm_svm->kvm, vcpu_idx);
@@ -286,6 +294,17 @@ int avic_alloc_physical_id_table(struct kvm *kvm)
 	if (!irqchip_in_kernel(kvm) || !enable_apicv)
 		return 0;
 
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	if (kvm_svm->avic_physical_id_table)
 		return 0;
 
@@ -306,6 +325,17 @@ void avic_vm_destroy(struct kvm *kvm)
 		return;
 
 	free_page((unsigned long)kvm_svm->avic_logical_id_table);
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	free_pages((unsigned long)kvm_svm->avic_physical_id_table,
 		   avic_get_physical_id_table_order(kvm));
 
@@ -314,6 +344,10 @@ void avic_vm_destroy(struct kvm *kvm)
 	spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
 }
 
+/*
+ * 在以下使用avic_vm_init():
+ *   - arch/x86/kvm/svm/svm.c|5001| <<svm_vm_init>> int ret = avic_vm_init(kvm)
+ */
 int avic_vm_init(struct kvm *kvm)
 {
 	unsigned long flags;
@@ -325,6 +359,15 @@ int avic_vm_init(struct kvm *kvm)
 	if (!enable_apicv)
 		return 0;
 
+	/*
+	 * 在以下使用kvm_svm->avic_logical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|308| <<avic_vm_destroy>> free_page((unsigned long )kvm_svm->avic_logical_id_table);
+	 *   - arch/x86/kvm/svm/avic.c|328| <<avic_vm_init>> kvm_svm->avic_logical_id_table = (void *)get_zeroed_page(GFP_KERNEL_ACCOUNT);
+	 *   - arch/x86/kvm/svm/avic.c|329| <<avic_vm_init>> if (!kvm_svm->avic_logical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|367| <<avic_init_vmcb>> vmcb->control.avic_logical_id = __sme_set(__pa(kvm_svm->avic_logical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|576| <<avic_kick_target_vcpus_fast>> avic_logical_id_table = kvm_svm->avic_logical_id_table;
+	 *   - arch/x86/kvm/svm/avic.c|698| <<avic_get_logical_id_entry>> return &kvm_svm->avic_logical_id_table[index];
+	 */
 	kvm_svm->avic_logical_id_table = (void *)get_zeroed_page(GFP_KERNEL_ACCOUNT);
 	if (!kvm_svm->avic_logical_id_table)
 		goto free_avic;
@@ -339,10 +382,26 @@ int avic_vm_init(struct kvm *kvm)
 	/* Is it still in use? Only possible if wrapped at least once */
 	if (next_vm_id_wrapped) {
 		hash_for_each_possible(svm_vm_data_hash, k2, hnode, vm_id) {
+			/*
+			 * 在以下使用kvm_svm->avic_vm_id:
+			 *   - arch/x86/kvm/svm/avic.c|258| <<avic_ga_log_notifier>> if (kvm_svm->avic_vm_id != vm_id)
+			 *   - arch/x86/kvm/svm/avic.c|364| <<avic_vm_init>> if (k2->avic_vm_id == vm_id)
+			 *   - arch/x86/kvm/svm/avic.c|368| <<avic_vm_init>> kvm_svm->avic_vm_id = vm_id;
+			 *   - arch/x86/kvm/svm/avic.c|369| <<avic_vm_init>> hash_add(svm_vm_data_hash, &kvm_svm->hnode, kvm_svm->avic_vm_id);
+			 *   - arch/x86/kvm/svm/avic.c|943| <<avic_pi_update_irte>> .ga_tag = AVIC_GATAG(to_kvm_svm(kvm)->avic_vm_id,
+			 */
 			if (k2->avic_vm_id == vm_id)
 				goto again;
 		}
 	}
+	/*
+	 * 在以下使用kvm_svm->avic_vm_id:
+	 *   - arch/x86/kvm/svm/avic.c|258| <<avic_ga_log_notifier>> if (kvm_svm->avic_vm_id != vm_id)
+	 *   - arch/x86/kvm/svm/avic.c|364| <<avic_vm_init>> if (k2->avic_vm_id == vm_id)
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_vm_init>> kvm_svm->avic_vm_id = vm_id;
+	 *   - arch/x86/kvm/svm/avic.c|369| <<avic_vm_init>> hash_add(svm_vm_data_hash, &kvm_svm->hnode, kvm_svm->avic_vm_id);
+	 *   - arch/x86/kvm/svm/avic.c|943| <<avic_pi_update_irte>> .ga_tag = AVIC_GATAG(to_kvm_svm(kvm)->avic_vm_id,
+	 */
 	kvm_svm->avic_vm_id = vm_id;
 	hash_add(svm_vm_data_hash, &kvm_svm->hnode, kvm_svm->avic_vm_id);
 	spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
@@ -364,7 +423,27 @@ void avic_init_vmcb(struct vcpu_svm *svm, struct vmcb *vmcb)
 	struct kvm_svm *kvm_svm = to_kvm_svm(svm->vcpu.kvm);
 
 	vmcb->control.avic_backing_page = avic_get_backing_page_address(svm);
+	/*
+	 * 在以下使用kvm_svm->avic_logical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|308| <<avic_vm_destroy>> free_page((unsigned long )kvm_svm->avic_logical_id_table);
+	 *   - arch/x86/kvm/svm/avic.c|328| <<avic_vm_init>> kvm_svm->avic_logical_id_table = (void *)get_zeroed_page(GFP_KERNEL_ACCOUNT);
+	 *   - arch/x86/kvm/svm/avic.c|329| <<avic_vm_init>> if (!kvm_svm->avic_logical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|367| <<avic_init_vmcb>> vmcb->control.avic_logical_id = __sme_set(__pa(kvm_svm->avic_logical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|576| <<avic_kick_target_vcpus_fast>> avic_logical_id_table = kvm_svm->avic_logical_id_table;
+	 *   - arch/x86/kvm/svm/avic.c|698| <<avic_get_logical_id_entry>> return &kvm_svm->avic_logical_id_table[index];
+	 */
 	vmcb->control.avic_logical_id = __sme_set(__pa(kvm_svm->avic_logical_id_table));
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
 	vmcb->control.avic_vapic_bar = APIC_DEFAULT_PHYS_BASE;
 
@@ -422,8 +501,29 @@ static int avic_init_backing_page(struct kvm_vcpu *vcpu)
 	/* Setting AVIC backing page address in the phy APIC ID table */
 	new_entry = avic_get_backing_page_address(svm) |
 		    AVIC_PHYSICAL_ID_ENTRY_VALID_MASK;
+	/*
+	 * 在以下使用avic_physical_id_entry:
+	 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+	 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+	 */
 	svm->avic_physical_id_entry = new_entry;
 
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	/*
 	 * Initialize the real table, as vCPUs must have a valid entry in order
 	 * for broadcast IPIs to function correctly (broadcast IPIs ignore
@@ -560,6 +660,15 @@ static int avic_kick_target_vcpus_fast(struct kvm *kvm, struct kvm_lapic *source
 		if (unlikely(!bitmap))
 			return 0;
 
+		/*
+		 * 在以下使用kvm_svm->avic_logical_id_table:
+		 *   - arch/x86/kvm/svm/avic.c|308| <<avic_vm_destroy>> free_page((unsigned long )kvm_svm->avic_logical_id_table);
+		 *   - arch/x86/kvm/svm/avic.c|328| <<avic_vm_init>> kvm_svm->avic_logical_id_table = (void *)get_zeroed_page(GFP_KERNEL_ACCOUNT);
+		 *   - arch/x86/kvm/svm/avic.c|329| <<avic_vm_init>> if (!kvm_svm->avic_logical_id_table)
+		 *   - arch/x86/kvm/svm/avic.c|367| <<avic_init_vmcb>> vmcb->control.avic_logical_id = __sme_set(__pa(kvm_svm->avic_logical_id_table));
+		 *   - arch/x86/kvm/svm/avic.c|576| <<avic_kick_target_vcpus_fast>> avic_logical_id_table = kvm_svm->avic_logical_id_table;
+		 *   - arch/x86/kvm/svm/avic.c|698| <<avic_get_logical_id_entry>> return &kvm_svm->avic_logical_id_table[index];
+		 */
 		if (apic_x2apic_mode(source))
 			avic_logical_id_table = NULL;
 		else
@@ -685,6 +794,15 @@ static u32 *avic_get_logical_id_entry(struct kvm_vcpu *vcpu, u32 ldr, bool flat)
 		return NULL;
 	index += (cluster << 2);
 
+	/*
+	 * 在以下使用kvm_svm->avic_logical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|308| <<avic_vm_destroy>> free_page((unsigned long )kvm_svm->avic_logical_id_table);
+	 *   - arch/x86/kvm/svm/avic.c|328| <<avic_vm_init>> kvm_svm->avic_logical_id_table = (void *)get_zeroed_page(GFP_KERNEL_ACCOUNT);
+	 *   - arch/x86/kvm/svm/avic.c|329| <<avic_vm_init>> if (!kvm_svm->avic_logical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|367| <<avic_init_vmcb>> vmcb->control.avic_logical_id = __sme_set(__pa(kvm_svm->avic_logical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|576| <<avic_kick_target_vcpus_fast>> avic_logical_id_table = kvm_svm->avic_logical_id_table;
+	 *   - arch/x86/kvm/svm/avic.c|698| <<avic_get_logical_id_entry>> return &kvm_svm->avic_logical_id_table[index];
+	 */
 	return &kvm_svm->avic_logical_id_table[index];
 }
 
@@ -879,6 +997,14 @@ int avic_pi_update_irte(struct kvm_kernel_irqfd *irqfd, struct kvm *kvm,
 	svm_ir_list_del(irqfd);
 
 	if (vcpu) {
+		/*
+		 * 在以下使用kvm_svm->avic_vm_id:
+		 *   - arch/x86/kvm/svm/avic.c|258| <<avic_ga_log_notifier>> if (kvm_svm->avic_vm_id != vm_id)
+		 *   - arch/x86/kvm/svm/avic.c|364| <<avic_vm_init>> if (k2->avic_vm_id == vm_id)
+		 *   - arch/x86/kvm/svm/avic.c|368| <<avic_vm_init>> kvm_svm->avic_vm_id = vm_id;
+		 *   - arch/x86/kvm/svm/avic.c|369| <<avic_vm_init>> hash_add(svm_vm_data_hash, &kvm_svm->hnode, kvm_svm->avic_vm_id);
+		 *   - arch/x86/kvm/svm/avic.c|943| <<avic_pi_update_irte>> .ga_tag = AVIC_GATAG(to_kvm_svm(kvm)->avic_vm_id,
+		 */
 		/*
 		 * Try to enable guest_mode in IRTE, unless AVIC is inhibited,
 		 * in which case configure the IRTE for legacy mode, but track
@@ -910,6 +1036,16 @@ int avic_pi_update_irte(struct kvm_kernel_irqfd *irqfd, struct kvm *kvm,
 		 * scheduled out, KVM will update the pCPU info when the vCPU
 		 * is awakened and/or scheduled in.  See also avic_vcpu_load().
 		 */
+		/*
+		 * 在以下使用avic_physical_id_entry:
+		 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+		 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+		 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+		 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+		 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+		 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+		 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+		 */
 		entry = svm->avic_physical_id_entry;
 		if (entry & AVIC_PHYSICAL_ID_ENTRY_IS_RUNNING_MASK) {
 			pi_data.cpu = entry & AVIC_PHYSICAL_ID_ENTRY_HOST_PHYSICAL_ID_MASK;
@@ -995,6 +1131,11 @@ static void avic_update_iommu_vcpu_affinity(struct kvm_vcpu *vcpu, int cpu,
 	}
 }
 
+/*
+ * 在以下使用__avic_vcpu_load():
+ *   - arch/x86/kvm/svm/avic.c|1220| <<avic_vcpu_load>> __avic_vcpu_load(vcpu, cpu, AVIC_START_RUNNING);
+ *   - arch/x86/kvm/svm/avic.c|1380| <<avic_refresh_apicv_exec_ctrl>> __avic_vcpu_load(vcpu, vcpu->cpu, AVIC_ACTIVATE);
+ */
 static void __avic_vcpu_load(struct kvm_vcpu *vcpu, int cpu,
 			     enum avic_vcpu_action action)
 {
@@ -1022,6 +1163,16 @@ static void __avic_vcpu_load(struct kvm_vcpu *vcpu, int cpu,
 	 */
 	raw_spin_lock_irqsave(&svm->ir_list_lock, flags);
 
+	/*
+	 * 在以下使用avic_physical_id_entry:
+	 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+	 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+	 */
 	entry = svm->avic_physical_id_entry;
 	WARN_ON_ONCE(entry & AVIC_PHYSICAL_ID_ENTRY_IS_RUNNING_MASK);
 
@@ -1041,6 +1192,17 @@ static void __avic_vcpu_load(struct kvm_vcpu *vcpu, int cpu,
 	if (!enable_ipiv)
 		entry &= ~AVIC_PHYSICAL_ID_ENTRY_IS_RUNNING_MASK;
 
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
 
 	avic_update_iommu_vcpu_affinity(vcpu, h_physical_id, action);
@@ -1060,6 +1222,11 @@ void avic_vcpu_load(struct kvm_vcpu *vcpu, int cpu)
 	if (kvm_vcpu_is_blocking(vcpu))
 		return;
 
+	/*
+	 * 在以下使用__avic_vcpu_load():
+	 *   - arch/x86/kvm/svm/avic.c|1220| <<avic_vcpu_load>> __avic_vcpu_load(vcpu, cpu, AVIC_START_RUNNING);
+	 *   - arch/x86/kvm/svm/avic.c|1380| <<avic_refresh_apicv_exec_ctrl>> __avic_vcpu_load(vcpu, vcpu->cpu, AVIC_ACTIVATE);
+	 */
 	__avic_vcpu_load(vcpu, cpu, AVIC_START_RUNNING);
 }
 
@@ -1068,6 +1235,16 @@ static void __avic_vcpu_put(struct kvm_vcpu *vcpu, enum avic_vcpu_action action)
 	struct kvm_svm *kvm_svm = to_kvm_svm(vcpu->kvm);
 	struct vcpu_svm *svm = to_svm(vcpu);
 	unsigned long flags;
+	/*
+	 * 在以下使用avic_physical_id_entry:
+	 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+	 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+	 */
 	u64 entry = svm->avic_physical_id_entry;
 
 	lockdep_assert_preemption_disabled();
@@ -1096,6 +1273,17 @@ static void __avic_vcpu_put(struct kvm_vcpu *vcpu, enum avic_vcpu_action action)
 	 */
 	entry &= ~AVIC_PHYSICAL_ID_ENTRY_IS_RUNNING_MASK;
 
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	if (enable_ipiv)
 		WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
 
@@ -1106,6 +1294,16 @@ static void __avic_vcpu_put(struct kvm_vcpu *vcpu, enum avic_vcpu_action action)
 	if (action & AVIC_START_BLOCKING)
 		entry |= AVIC_PHYSICAL_ID_ENTRY_GA_LOG_INTR;
 
+	/*
+	 * 在以下使用avic_physical_id_entry:
+	 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+	 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+	 */
 	svm->avic_physical_id_entry = entry;
 
 	raw_spin_unlock_irqrestore(&svm->ir_list_lock, flags);
@@ -1120,6 +1318,16 @@ void avic_vcpu_put(struct kvm_vcpu *vcpu)
 	 * can't be scheduled out and thus avic_vcpu_{put,load}() can't run
 	 * recursively.
 	 */
+	/*
+	 * 在以下使用avic_physical_id_entry:
+	 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+	 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+	 */
 	u64 entry = to_svm(vcpu)->avic_physical_id_entry;
 
 	/*
@@ -1178,6 +1386,11 @@ void avic_refresh_apicv_exec_ctrl(struct kvm_vcpu *vcpu)
 
 	avic_refresh_virtual_apic_mode(vcpu);
 
+	/*
+	 * 在以下使用__avic_vcpu_load():
+	 *   - arch/x86/kvm/svm/avic.c|1220| <<avic_vcpu_load>> __avic_vcpu_load(vcpu, cpu, AVIC_START_RUNNING);
+	 *   - arch/x86/kvm/svm/avic.c|1380| <<avic_refresh_apicv_exec_ctrl>> __avic_vcpu_load(vcpu, vcpu->cpu, AVIC_ACTIVATE);
+	 */
 	if (kvm_vcpu_apicv_active(vcpu))
 		__avic_vcpu_load(vcpu, vcpu->cpu, AVIC_ACTIVATE);
 	else
diff --git a/arch/x86/kvm/svm/svm.h b/arch/x86/kvm/svm/svm.h
index 01be93a53..75e4bc8b6 100644
--- a/arch/x86/kvm/svm/svm.h
+++ b/arch/x86/kvm/svm/svm.h
@@ -120,9 +120,37 @@ struct kvm_sev_info {
 struct kvm_svm {
 	struct kvm kvm;
 
+	/*
+	 * 在以下使用kvm_svm->avic_vm_id:
+	 *   - arch/x86/kvm/svm/avic.c|258| <<avic_ga_log_notifier>> if (kvm_svm->avic_vm_id != vm_id)
+	 *   - arch/x86/kvm/svm/avic.c|364| <<avic_vm_init>> if (k2->avic_vm_id == vm_id)
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_vm_init>> kvm_svm->avic_vm_id = vm_id;
+	 *   - arch/x86/kvm/svm/avic.c|369| <<avic_vm_init>> hash_add(svm_vm_data_hash, &kvm_svm->hnode, kvm_svm->avic_vm_id);
+	 *   - arch/x86/kvm/svm/avic.c|943| <<avic_pi_update_irte>> .ga_tag = AVIC_GATAG(to_kvm_svm(kvm)->avic_vm_id,
+	 */
 	/* Struct members for AVIC */
 	u32 avic_vm_id;
+	/*
+	 * 在以下使用kvm_svm->avic_logical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|308| <<avic_vm_destroy>> free_page((unsigned long )kvm_svm->avic_logical_id_table);
+	 *   - arch/x86/kvm/svm/avic.c|328| <<avic_vm_init>> kvm_svm->avic_logical_id_table = (void *)get_zeroed_page(GFP_KERNEL_ACCOUNT);
+	 *   - arch/x86/kvm/svm/avic.c|329| <<avic_vm_init>> if (!kvm_svm->avic_logical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|367| <<avic_init_vmcb>> vmcb->control.avic_logical_id = __sme_set(__pa(kvm_svm->avic_logical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|576| <<avic_kick_target_vcpus_fast>> avic_logical_id_table = kvm_svm->avic_logical_id_table;
+	 *   - arch/x86/kvm/svm/avic.c|698| <<avic_get_logical_id_entry>> return &kvm_svm->avic_logical_id_table[index];
+	 */
 	u32 *avic_logical_id_table;
+	/*
+	 * 在以下使用kvm_svm->avic_physical_id_table:
+	 *   - arch/x86/kvm/svm/avic.c|289| <<avic_alloc_physical_id_table>> if (kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|292| <<avic_alloc_physical_id_table>> kvm_svm->avic_physical_id_table = (void *)__get_free_pages(GFP_KERNEL_ACCOUNT | __GFP_ZERO,
+	 *   - arch/x86/kvm/svm/avic.c|294| <<avic_alloc_physical_id_table>> if (!kvm_svm->avic_physical_id_table)
+	 *   - arch/x86/kvm/svm/avic.c|309| <<avic_vm_destroy>> free_pages((unsigned long )kvm_svm->avic_physical_id_table,
+	 *   - arch/x86/kvm/svm/avic.c|368| <<avic_init_vmcb>> vmcb->control.avic_physical_id = __sme_set(__pa(kvm_svm->avic_physical_id_table));
+	 *   - arch/x86/kvm/svm/avic.c|442| <<avic_init_backing_page>> WRITE_ONCE(kvm_svm->avic_physical_id_table[id], new_entry);
+	 *   - arch/x86/kvm/svm/avic.c|1074| <<__avic_vcpu_load>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 *   - arch/x86/kvm/svm/avic.c|1140| <<__avic_vcpu_put>> WRITE_ONCE(kvm_svm->avic_physical_id_table[vcpu->vcpu_id], entry);
+	 */
 	u64 *avic_physical_id_table;
 	struct hlist_node hnode;
 
@@ -315,6 +343,16 @@ struct vcpu_svm {
 	 * only ever set in the shadow, i.e. is never propagated to the "real"
 	 * table, so that hardware never sees IsRunning=1.
 	 */
+	/*
+	 * 在以下使用avic_physical_id_entry:
+	 *   - arch/x86/kvm/svm/avic.c|425| <<avic_init_backing_page>> svm->avic_physical_id_entry = new_entry;
+	 *   - arch/x86/kvm/svm/avic.c|913| <<avic_pi_update_irte>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1025| <<__avic_vcpu_load>> entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1033| <<__avic_vcpu_load>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1071| <<__avic_vcpu_put>> u64 entry = svm->avic_physical_id_entry;
+	 *   - arch/x86/kvm/svm/avic.c|1109| <<__avic_vcpu_put>> svm->avic_physical_id_entry = entry;
+	 *   - arch/x86/kvm/svm/avic.c|1123| <<avic_vcpu_put>> u64 entry = to_svm(vcpu)->avic_physical_id_entry;
+	 */
 	u64 avic_physical_id_entry;
 
 	/*
diff --git a/arch/x86/kvm/x86.c b/arch/x86/kvm/x86.c
index 72d37c893..d0220eb46 100644
--- a/arch/x86/kvm/x86.c
+++ b/arch/x86/kvm/x86.c
@@ -2545,6 +2545,11 @@ static int set_tsc_khz(struct kvm_vcpu *vcpu, u32 user_tsc_khz, bool scale)
 	return 0;
 }
 
+/*
+ * 在以下使用kvm_set_tsc_khz():
+ *   - arch/x86/kvm/x86.c|6489| <<kvm_arch_vcpu_ioctl(KVM_SET_TSC_KHZ)>> if (!kvm_set_tsc_khz(vcpu, user_tsc_khz))
+ *   - arch/x86/kvm/x86.c|12753| <<kvm_arch_vcpu_create>> kvm_set_tsc_khz(vcpu, vcpu->kvm->arch.default_tsc_khz);
+ */
 static int kvm_set_tsc_khz(struct kvm_vcpu *vcpu, u32 user_tsc_khz)
 {
 	u32 thresh_lo, thresh_hi;
diff --git a/block/blk-mq.c b/block/blk-mq.c
index 968699277..141c261aa 100644
--- a/block/blk-mq.c
+++ b/block/blk-mq.c
@@ -4051,6 +4051,16 @@ blk_mq_alloc_hctx(struct request_queue *q, struct blk_mq_tag_set *set,
 	if (!hctx->ctxs)
 		goto free_cpumask;
 
+	/*
+	 * 在以下使用sbitmap_init_node():
+	 *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+	 *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+	 *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+	 *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+	 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+	 *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+	 *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+	 */
 	if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
 				gfp, node, false, false))
 		goto free_ctxs;
diff --git a/block/kyber-iosched.c b/block/kyber-iosched.c
index c1b36ffd1..c4aff726f 100644
--- a/block/kyber-iosched.c
+++ b/block/kyber-iosched.c
@@ -480,6 +480,16 @@ static int kyber_init_hctx(struct blk_mq_hw_ctx *hctx, unsigned int hctx_idx)
 		kyber_ctx_queue_init(&khd->kcqs[i]);
 
 	for (i = 0; i < KYBER_NUM_DOMAINS; i++) {
+		/*
+		 * 在以下使用sbitmap_init_node():
+		 *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+		 *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+		 *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+		 *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+		 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+		 *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+		 *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+		 */
 		if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
 				      ilog2(8), GFP_KERNEL, hctx->numa_node,
 				      false, false)) {
diff --git a/drivers/nvme/target/rdma.c b/drivers/nvme/target/rdma.c
index 9c12b2361..a4d52ab1b 100644
--- a/drivers/nvme/target/rdma.c
+++ b/drivers/nvme/target/rdma.c
@@ -451,6 +451,16 @@ nvmet_rdma_alloc_rsps(struct nvmet_rdma_queue *queue)
 	int nr_rsps = queue->recv_queue_size * 2;
 	int ret = -ENOMEM, i;
 
+	/*
+	 * 在以下使用sbitmap_init_node():
+	 *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+	 *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+	 *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+	 *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+	 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+	 *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+	 *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+	 */
 	if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
 			NUMA_NO_NODE, false, true))
 		goto out;
diff --git a/drivers/pci/hotplug/pciehp_ctrl.c b/drivers/pci/hotplug/pciehp_ctrl.c
index bcc938d44..6b0c661b2 100644
--- a/drivers/pci/hotplug/pciehp_ctrl.c
+++ b/drivers/pci/hotplug/pciehp_ctrl.c
@@ -213,6 +213,34 @@ void pciehp_handle_button_press(struct controller *ctrl)
 	mutex_unlock(&ctrl->state_lock);
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 void pciehp_handle_disable_request(struct controller *ctrl)
 {
 	mutex_lock(&ctrl->state_lock);
@@ -356,6 +384,34 @@ static int __pciehp_disable_slot(struct controller *ctrl, bool safe_removal)
 	return 0;
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 static int pciehp_disable_slot(struct controller *ctrl, bool safe_removal)
 {
 	int ret;
diff --git a/drivers/pci/hotplug/pciehp_hpc.c b/drivers/pci/hotplug/pciehp_hpc.c
index bcc51b26d..b7bccb5ca 100644
--- a/drivers/pci/hotplug/pciehp_hpc.c
+++ b/drivers/pci/hotplug/pciehp_hpc.c
@@ -724,6 +724,34 @@ static irqreturn_t pciehp_isr(int irq, void *dev_id)
 	return IRQ_WAKE_THREAD;
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 static irqreturn_t pciehp_ist(int irq, void *dev_id)
 {
 	struct controller *ctrl = (struct controller *)dev_id;
diff --git a/drivers/pci/hotplug/pciehp_pci.c b/drivers/pci/hotplug/pciehp_pci.c
index 65e50bee1..8e0e0358e 100644
--- a/drivers/pci/hotplug/pciehp_pci.c
+++ b/drivers/pci/hotplug/pciehp_pci.c
@@ -81,6 +81,34 @@ int pciehp_configure_device(struct controller *ctrl)
 	return ret;
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 /**
  * pciehp_unconfigure_device() - remove PCI devices below a hotplug bridge
  * @ctrl: PCIe hotplug controller
diff --git a/drivers/pci/remove.c b/drivers/pci/remove.c
index 417a9ea59..04dcaf6bf 100644
--- a/drivers/pci/remove.c
+++ b/drivers/pci/remove.c
@@ -133,6 +133,34 @@ static void pci_remove_bus_device(struct pci_dev *dev)
 	pci_destroy_dev(dev);
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 /**
  * pci_stop_and_remove_bus_device - remove a PCI device and any children
  * @dev: the device to remove
diff --git a/drivers/scsi/hosts.c b/drivers/scsi/hosts.c
index 1b3fbd328..a1a6c3524 100644
--- a/drivers/scsi/hosts.c
+++ b/drivers/scsi/hosts.c
@@ -158,6 +158,34 @@ int scsi_host_set_state(struct Scsi_Host *shost, enum scsi_host_state state)
 	return -EINVAL;
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 /**
  * scsi_remove_host - remove a scsi host
  * @shost:	a pointer to a scsi host to remove
@@ -178,6 +206,9 @@ void scsi_remove_host(struct Scsi_Host *shost)
 
 	scsi_autopm_get_host(shost);
 	flush_workqueue(shost->tmf_work_q);
+	/*
+	 * 只在这里调用
+	 */
 	scsi_forget_host(shost);
 	mutex_unlock(&shost->scan_mutex);
 	scsi_proc_host_rm(shost);
@@ -189,7 +220,26 @@ void scsi_remove_host(struct Scsi_Host *shost)
 	 * to zero because .exit_cmd_priv implementations may need the host
 	 * pointer.
 	 */
+	/*
+	 * 在以下使用Scsi_Host->tagset_refcnt:
+	 *   - drivers/scsi/hosts.c|198| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|270| <<scsi_add_host_with_dma>> kref_init(&shost->tagset_refcnt);
+	 *   - drivers/scsi/hosts.c|358| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_scan.c|347| <<scsi_alloc_sdev>> kref_get(&sdev->host->tagset_refcnt);
+	 *   - drivers/scsi/scsi_sysfs.c|1476| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 *
+	 * 在以下使用scsi_mq_free_tags():
+	 *   - drivers/scsi/hosts.c|192| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|346| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_sysfs.c|1458| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 */
 	kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	/*
+	 * 在以下使用Scsi_Host->tagset_freed:
+	 *   - drivers/scsi/hosts.c|206| <<scsi_remove_host>> wait_for_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/hosts.c|286| <<scsi_add_host_with_dma>> init_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/scsi_lib.c|2148| <<scsi_mq_free_tags>> complete(&shost->tagset_freed);
+	 */
 	wait_for_completion(&shost->tagset_freed);
 
 	spin_lock_irqsave(shost->host_lock, flags);
@@ -261,7 +311,21 @@ int scsi_add_host_with_dma(struct Scsi_Host *shost, struct device *dev,
 	if (error)
 		goto fail;
 
+	/*
+	 * 在以下使用Scsi_Host->tagset_refcnt:
+	 *   - drivers/scsi/hosts.c|198| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|270| <<scsi_add_host_with_dma>> kref_init(&shost->tagset_refcnt);
+	 *   - drivers/scsi/hosts.c|358| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_scan.c|347| <<scsi_alloc_sdev>> kref_get(&sdev->host->tagset_refcnt);
+	 *   - drivers/scsi/scsi_sysfs.c|1476| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 */
 	kref_init(&shost->tagset_refcnt);
+	/*
+	 * 在以下使用Scsi_Host->tagset_freed:
+	 *   - drivers/scsi/hosts.c|206| <<scsi_remove_host>> wait_for_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/hosts.c|286| <<scsi_add_host_with_dma>> init_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/scsi_lib.c|2148| <<scsi_mq_free_tags>> complete(&shost->tagset_freed);
+	 */
 	init_completion(&shost->tagset_freed);
 
 	/*
@@ -343,6 +407,19 @@ int scsi_add_host_with_dma(struct Scsi_Host *shost, struct device *dev,
 	pm_runtime_disable(&shost->shost_gendev);
 	pm_runtime_set_suspended(&shost->shost_gendev);
 	pm_runtime_put_noidle(&shost->shost_gendev);
+	/*
+	 * 在以下使用Scsi_Host->tagset_refcnt:
+	 *   - drivers/scsi/hosts.c|198| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|270| <<scsi_add_host_with_dma>> kref_init(&shost->tagset_refcnt);
+	 *   - drivers/scsi/hosts.c|358| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_scan.c|347| <<scsi_alloc_sdev>> kref_get(&sdev->host->tagset_refcnt);
+	 *   - drivers/scsi/scsi_sysfs.c|1476| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 *
+	 * 在以下使用scsi_mq_free_tags():
+	 *   - drivers/scsi/hosts.c|192| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|346| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_sysfs.c|1458| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 */
 	kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
  fail:
 	return error;
diff --git a/drivers/scsi/scsi.c b/drivers/scsi/scsi.c
index 76cdad063..8cd525a40 100644
--- a/drivers/scsi/scsi.c
+++ b/drivers/scsi/scsi.c
@@ -72,6 +72,35 @@
 #define CREATE_TRACE_POINTS
 #include <trace/events/scsi.h>
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host()
+ *    -> async_schedule(do_scan_async, data)
+ *
+ *
+ * do_scan_async()
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
+
 /*
  * Definitions and constants.
  */
diff --git a/drivers/scsi/scsi_lib.c b/drivers/scsi/scsi_lib.c
index 4a902c9df..bcd710f72 100644
--- a/drivers/scsi/scsi_lib.c
+++ b/drivers/scsi/scsi_lib.c
@@ -41,6 +41,35 @@
 #include "scsi_priv.h"
 #include "scsi_logging.h"
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host()
+ *    -> async_schedule(do_scan_async, data)
+ *
+ *
+ * do_scan_async()
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
+
 /*
  * Size of integrity metadata is usually small, 1 inline sg should
  * cover normal cases.
@@ -1364,6 +1393,10 @@ scsi_device_state_check(struct scsi_device *sdev, struct request *req)
  * scsi_dev_queue_ready: if we can send requests to sdev, assign one token
  * and return the token else return -1.
  */
+/*
+ * 在以下使用scsi_dev_queue_ready():
+ *   - drivers/scsi/scsi_lib.c|1786| <<scsi_mq_get_budget>> int token = scsi_dev_queue_ready(q, sdev);
+ */
 static inline int scsi_dev_queue_ready(struct request_queue *q,
 				  struct scsi_device *sdev)
 {
@@ -1765,6 +1798,12 @@ void scsi_done_direct(struct scsi_cmnd *cmd)
 }
 EXPORT_SYMBOL(scsi_done_direct);
 
+/*
+ * 在以下使用scsi_mq_put_budget():
+ *   - drivers/scsi/scsi_lib.c|2055| <<global>> struct blk_mq_ops scsi_mq_ops_no_commit.put_budget = scsi_mq_put_budget,
+ *   - drivers/scsi/scsi_lib.c|2083| <<global>> struct blk_mq_ops scsi_mq_ops.put_budget = scsi_mq_put_budget,
+ *   - drivers/scsi/scsi_lib.c|1918| <<scsi_queue_rq>> scsi_mq_put_budget(q, cmd->budget_token);
+ */
 static void scsi_mq_put_budget(struct request_queue *q, int budget_token)
 {
 	struct scsi_device *sdev = q->queuedata;
@@ -1780,9 +1819,17 @@ static void scsi_mq_put_budget(struct request_queue *q, int budget_token)
  */
 #define SCSI_QUEUE_DELAY 3
 
+/*
+ * 在以下使用scsi_mq_get_budget():
+ *   - drivers/scsi/scsi_lib.c|2054| <<global>> struct blk_mq_ops scsi_mq_ops_no_commit.get_budget = scsi_mq_get_budget,
+ *   - drivers/scsi/scsi_lib.c|2082| <<global>> struct blk_mq_ops scsi_mq_ops.get_budget = scsi_mq_get_budget,
+ */
 static int scsi_mq_get_budget(struct request_queue *q)
 {
 	struct scsi_device *sdev = q->queuedata;
+	/*
+	 * 只在这里调用
+	 */
 	int token = scsi_dev_queue_ready(q, sdev);
 
 	if (token >= 0)
@@ -2133,12 +2180,51 @@ int scsi_mq_setup_tags(struct Scsi_Host *shost)
 	return blk_mq_alloc_tag_set(tag_set);
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ *
+ * 在以下使用scsi_mq_free_tags():
+ *   - drivers/scsi/hosts.c|192| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+ *   - drivers/scsi/hosts.c|346| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+ *   - drivers/scsi/scsi_sysfs.c|1458| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+ */
 void scsi_mq_free_tags(struct kref *kref)
 {
 	struct Scsi_Host *shost = container_of(kref, typeof(*shost),
 					       tagset_refcnt);
 
 	blk_mq_free_tag_set(&shost->tag_set);
+	/*
+	 * 在以下使用Scsi_Host->tagset_freed:
+	 *   - drivers/scsi/hosts.c|206| <<scsi_remove_host>> wait_for_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/hosts.c|286| <<scsi_add_host_with_dma>> init_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/scsi_lib.c|2148| <<scsi_mq_free_tags>> complete(&shost->tagset_freed);
+	 */
 	complete(&shost->tagset_freed);
 }
 
diff --git a/drivers/scsi/scsi_proc.c b/drivers/scsi/scsi_proc.c
index 41f23cd0b..18e759251 100644
--- a/drivers/scsi/scsi_proc.c
+++ b/drivers/scsi/scsi_proc.c
@@ -344,6 +344,14 @@ static int scsi_add_single_device(uint host, uint channel, uint id, uint lun)
 	if (!shost)
 		return error;
 
+	/*
+	 * 在以下使用scsi_scan_host_selected():
+	 *   - drivers/scsi/scsi_proc.c|350| <<scsi_add_single_device>> error = scsi_scan_host_selected(shost, channel, id, lun,
+	 *   - drivers/scsi/scsi_scan.c|2122| <<do_scsi_scan_host>> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD,
+	 *   - drivers/scsi/scsi_sysfs.c|151| <<scsi_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+	 *   - drivers/scsi/scsi_transport_sas.c|1729| <<sas_user_scan>> res = scsi_scan_host_selected(shost, i, id, lun,
+	 *   - drivers/scsi/scsi_transport_sas.c|1738| <<sas_user_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+	 */
 	if (shost->transportt->user_scan)
 		error = shost->transportt->user_scan(shost, channel, id, lun);
 	else
diff --git a/drivers/scsi/scsi_scan.c b/drivers/scsi/scsi_scan.c
index 7acbfcfc2..d8977c4ca 100644
--- a/drivers/scsi/scsi_scan.c
+++ b/drivers/scsi/scsi_scan.c
@@ -53,6 +53,35 @@
 #define ALLOC_FAILURE_MSG	KERN_ERR "%s: Allocation failure during" \
 	" SCSI scanning, some SCSI devices might not be configured\n"
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host()
+ *    -> async_schedule(do_scan_async, data)
+ *
+ *
+ * do_scan_async()
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
+
 /*
  * Default timeout
  */
@@ -114,6 +143,19 @@ MODULE_PARM_DESC(inq_timeout,
 
 /* This lock protects only this list */
 static DEFINE_SPINLOCK(async_scan_lock);
+/*
+ * 在以下使用scanning_hosts:
+ *   - drivers/scsi/scsi_scan.c|117| <<global>> static LIST_HEAD(scanning_hosts);
+ *   - drivers/scsi/scsi_scan.c|155| <<scsi_complete_async_scans>> if (list_empty(&scanning_hosts))
+ *   - drivers/scsi/scsi_scan.c|171| <<scsi_complete_async_scans>> if (list_empty(&scanning_hosts))
+ *   - drivers/scsi/scsi_scan.c|173| <<scsi_complete_async_scans>> list_add_tail(&data->list, &scanning_hosts);
+ *   - drivers/scsi/scsi_scan.c|181| <<scsi_complete_async_scans>> if (!list_empty(&scanning_hosts)) {
+ *   - drivers/scsi/scsi_scan.c|182| <<scsi_complete_async_scans>> struct async_scan_data *next = list_entry(scanning_hosts.next,
+ *   - drivers/scsi/scsi_scan.c|2048| <<scsi_prep_async_scan>> if (list_empty(&scanning_hosts))
+ *   - drivers/scsi/scsi_scan.c|2050| <<scsi_prep_async_scan>> list_add_tail(&data->list, &scanning_hosts);
+ *   - drivers/scsi/scsi_scan.c|2100| <<scsi_finish_async_scan>> if (!list_empty(&scanning_hosts)) {
+ *   - drivers/scsi/scsi_scan.c|2101| <<scsi_finish_async_scan>> struct async_scan_data *next = list_entry(scanning_hosts.next,
+ */
 static LIST_HEAD(scanning_hosts);
 
 struct async_scan_data {
@@ -215,6 +257,11 @@ static void scsi_unlock_floptical(struct scsi_device *sdev,
 			 SCSI_TIMEOUT, 3, NULL);
 }
 
+/*
+ * 在以下使用scsi_realloc_sdev_budget_map():
+ *   - drivers/scsi/scsi_scan.c|371| <<scsi_alloc_sdev>> if (scsi_realloc_sdev_budget_map(sdev, depth)) {
+ *   - drivers/scsi/scsi_scan.c|1123| <<scsi_add_lun>> scsi_realloc_sdev_budget_map(sdev, sdev->queue_depth);
+ */
 static int scsi_realloc_sdev_budget_map(struct scsi_device *sdev,
 					unsigned int depth)
 {
@@ -245,6 +292,16 @@ static int scsi_realloc_sdev_budget_map(struct scsi_device *sdev,
 		memflags = blk_mq_freeze_queue(sdev->request_queue);
 		sb_backup = sdev->budget_map;
 	}
+	/*
+	 * 在以下使用sbitmap_init_node():
+	 *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+	 *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+	 *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+	 *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+	 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+	 *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+	 *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+	 */
 	ret = sbitmap_init_node(&sdev->budget_map,
 				scsi_device_max_queue_depth(sdev),
 				new_shift, GFP_NOIO,
@@ -277,6 +334,12 @@ static int scsi_realloc_sdev_budget_map(struct scsi_device *sdev,
  * Return value:
  *     scsi_Device pointer, or NULL on failure.
  **/
+/*
+ * 在以下使用scsi_alloc_sdev():
+ *   - drivers/scsi/scsi_scan.c|1251| <<scsi_probe_and_add_lun>> sdev = scsi_alloc_sdev(starget, lun, hostdata);
+ *   - drivers/scsi/scsi_scan.c|1524| <<scsi_report_lun_scan>> sdev = scsi_alloc_sdev(starget, 0, NULL);
+ *   - drivers/scsi/scsi_scan.c|2210| <<scsi_get_pseudo_sdev>> sdev = scsi_alloc_sdev(starget, U64_MAX, NULL);
+ */
 static struct scsi_device *scsi_alloc_sdev(struct scsi_target *starget,
 					   u64 lun, void *hostdata)
 {
@@ -344,6 +407,14 @@ static struct scsi_device *scsi_alloc_sdev(struct scsi_target *starget,
 		kfree(sdev);
 		goto out;
 	}
+	/*
+	 * 在以下使用Scsi_Host->tagset_refcnt:
+	 *   - drivers/scsi/hosts.c|198| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|270| <<scsi_add_host_with_dma>> kref_init(&shost->tagset_refcnt);
+	 *   - drivers/scsi/hosts.c|358| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_scan.c|347| <<scsi_alloc_sdev>> kref_get(&sdev->host->tagset_refcnt);
+	 *   - drivers/scsi/scsi_sysfs.c|1476| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 */
 	kref_get(&sdev->host->tagset_refcnt);
 	sdev->request_queue = q;
 
@@ -360,6 +431,11 @@ static struct scsi_device *scsi_alloc_sdev(struct scsi_target *starget,
 	 * default device queue depth to figure out sbitmap shift
 	 * since we use this queue depth most of times.
 	 */
+	/*
+	 * 在以下使用scsi_realloc_sdev_budget_map():
+	 *   - drivers/scsi/scsi_scan.c|371| <<scsi_alloc_sdev>> if (scsi_realloc_sdev_budget_map(sdev, depth)) {
+	 *   - drivers/scsi/scsi_scan.c|1123| <<scsi_add_lun>> scsi_realloc_sdev_budget_map(sdev, sdev->queue_depth);
+	 */
 	if (scsi_realloc_sdev_budget_map(sdev, depth)) {
 		put_device(&starget->dev);
 		kfree(sdev);
@@ -493,6 +569,12 @@ static void scsi_target_reap_ref_put(struct scsi_target *starget)
  * The target is returned with an incremented reference, so the caller
  * is responsible for both reaping and doing a last put
  */
+/*
+ * 在以下使用scsi_alloc_target():
+ *   - drivers/scsi/scsi_scan.c|1706| <<__scsi_add_device>> starget = scsi_alloc_target(parent, channel, id);
+ *   - drivers/scsi/scsi_scan.c|1858| <<__scsi_scan_target>> starget = scsi_alloc_target(parent, channel, id);
+ *   - drivers/scsi/scsi_scan.c|2367| <<scsi_get_pseudo_sdev>> starget = scsi_alloc_target(&shost->shost_gendev, 0, shost->max_id);
+ */
 static struct scsi_target *scsi_alloc_target(struct device *parent,
 					     int channel, uint id)
 {
@@ -592,6 +674,14 @@ static struct scsi_target *scsi_alloc_target(struct device *parent,
  * it checks atomically that nothing is using the target and removes
  * it if so.
  */
+/*
+ * 在以下使用scsi_target_reap():
+ *   - drivers/scsi/scsi_scan.c|1798| <<__scsi_add_device>> scsi_target_reap(starget);
+ *   - drivers/scsi/scsi_scan.c|1995| <<__scsi_scan_target>> scsi_target_reap(starget);
+ *   - drivers/scsi/scsi_scan.c|2482| <<scsi_get_pseudo_sdev>> scsi_target_reap(starget);
+ *   - drivers/scsi/scsi_sysfs.c|1537| <<__scsi_remove_device>> scsi_target_reap(scsi_target(sdev));
+ *   - drivers/scsi/scsi_sysfs.c|1679| <<scsi_remove_target>> scsi_target_reap(starget);
+ */
 void scsi_target_reap(struct scsi_target *starget)
 {
 	/*
@@ -646,6 +736,10 @@ EXPORT_SYMBOL(scsi_sanitize_inquiry_string);
  *     INQUIRY data is in @inq_result; the scsi_level and INQUIRY length
  *     are copied to the scsi_device any flags value is stored in *@bflags.
  **/
+/*
+ * 在以下使用scsi_probe_lun():
+ *   - drivers/scsi/scsi_scan.c|1308| <<scsi_probe_and_add_lun>> if (scsi_probe_lun(sdev, result, result_len, &bflags))
+ */
 static int scsi_probe_lun(struct scsi_device *sdev, unsigned char *inq_result,
 			  int result_len, blist_flags_t *bflags)
 {
@@ -875,6 +969,10 @@ static int scsi_probe_lun(struct scsi_device *sdev, unsigned char *inq_result,
  *     SCSI_SCAN_NO_RESPONSE: could not allocate or setup a scsi_device
  *     SCSI_SCAN_LUN_PRESENT: a new scsi_device was allocated and initialized
  **/
+/*
+ * 在以下使用scsi_add_lun():
+ *   - drivers/scsi/scsi_scan.c|1377| <<scsi_probe_and_add_lun>> res = scsi_add_lun(sdev, result, &bflags, shost->async_scan);
+ */
 static int scsi_add_lun(struct scsi_device *sdev, unsigned char *inq_result,
 		blist_flags_t *bflags, int async)
 {
@@ -1111,6 +1209,11 @@ static int scsi_add_lun(struct scsi_device *sdev, unsigned char *inq_result,
 	 * Set up budget map again since memory consumption of the map depends
 	 * on actual queue depth.
 	 */
+	/*
+	 * 在以下使用scsi_realloc_sdev_budget_map():
+	 *   - drivers/scsi/scsi_scan.c|371| <<scsi_alloc_sdev>> if (scsi_realloc_sdev_budget_map(sdev, depth)) {
+	 *   - drivers/scsi/scsi_scan.c|1123| <<scsi_add_lun>> scsi_realloc_sdev_budget_map(sdev, sdev->queue_depth);
+	 */
 	if (hostt->sdev_configure)
 		scsi_realloc_sdev_budget_map(sdev, sdev->queue_depth);
 
@@ -1159,6 +1262,34 @@ static unsigned char *scsi_inq_str(unsigned char *buf, unsigned char *inq,
 }
 #endif
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host()
+ *    -> async_schedule(do_scan_async, data)
+ *
+ *
+ * do_scan_async()
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
 /**
  * scsi_probe_and_add_lun - probe a LUN, if a LUN is found add it
  * @starget:	pointer to target device structure
@@ -1180,6 +1311,27 @@ static unsigned char *scsi_inq_str(unsigned char *buf, unsigned char *inq,
  *         attached at the LUN
  *   - SCSI_SCAN_LUN_PRESENT: a new scsi_device was allocated and initialized
  **/
+/*
+ * [0] scsi_alloc_sdev
+ * [0] scsi_probe_and_add_lun
+ * [0] __scsi_scan_target
+ * [0] scsi_scan_channel
+ * [0] scsi_scan_host_selected
+ * [0] do_scsi_scan_host
+ * [0] do_scan_async
+ * [0] async_run_entry_fn
+ * [0] process_one_work
+ * [0] worker_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ *
+ * 在以下使用scsi_probe_and_add_lun():
+ *   - drivers/scsi/scsi_scan.c|1440| <<scsi_sequential_lun_scan>> if ((scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan,
+ *   - drivers/scsi/scsi_scan.c|1622| <<scsi_report_lun_scan>> res = scsi_probe_and_add_lun(starget,
+ *   - drivers/scsi/scsi_scan.c|1669| <<__scsi_add_device>> scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
+ *   - drivers/scsi/scsi_scan.c|1814| <<__scsi_scan_target>> scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan, NULL);
+ *   - drivers/scsi/scsi_scan.c|1822| <<__scsi_scan_target>> res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL)
+ */
 static int scsi_probe_and_add_lun(struct scsi_target *starget,
 				  u64 lun, blist_flags_t *bflagsp,
 				  struct scsi_device **sdevp,
@@ -1216,6 +1368,12 @@ static int scsi_probe_and_add_lun(struct scsi_target *starget,
 		scsi_device_put(sdev);
 	} else
 		sdev = scsi_alloc_sdev(starget, lun, hostdata);
+	/*
+	 * 在以下使用scsi_alloc_sdev():
+	 *   - drivers/scsi/scsi_scan.c|1251| <<scsi_probe_and_add_lun>> sdev = scsi_alloc_sdev(starget, lun, hostdata);
+	 *   - drivers/scsi/scsi_scan.c|1524| <<scsi_report_lun_scan>> sdev = scsi_alloc_sdev(starget, 0, NULL);
+	 *   - drivers/scsi/scsi_scan.c|2210| <<scsi_get_pseudo_sdev>> sdev = scsi_alloc_sdev(starget, U64_MAX, NULL);
+	 */
 	if (!sdev)
 		goto out;
 
@@ -1225,10 +1383,17 @@ static int scsi_probe_and_add_lun(struct scsi_target *starget,
 		return SCSI_SCAN_LUN_PRESENT;
 	}
 
+	/*
+	 * 上面:
+	 * unsigned char *result;
+	 */
 	result = kmalloc(result_len, GFP_KERNEL);
 	if (!result)
 		goto out_free_sdev;
 
+	/*
+	 * 只在这里调用
+	 */
 	if (scsi_probe_lun(sdev, result, result_len, &bflags))
 		goto out_free_result;
 
@@ -1298,6 +1463,9 @@ static int scsi_probe_and_add_lun(struct scsi_target *starget,
 		goto out_free_result;
 	}
 
+	/*
+	 * 只在这里调用
+	 */
 	res = scsi_add_lun(sdev, result, &bflags, shost->async_scan);
 	if (res == SCSI_SCAN_LUN_PRESENT) {
 		if (bflags & BLIST_KEY) {
@@ -1338,6 +1506,10 @@ static int scsi_probe_and_add_lun(struct scsi_target *starget,
  *
  *     Modifies sdevscan->lun.
  **/
+/*
+ * 在以下使用scsi_sequential_lun_scan():
+ *   - drivers/scsi/scsi_scan.c|1861| <<__scsi_scan_target>> scsi_sequential_lun_scan(starget, bflags,
+ */
 static void scsi_sequential_lun_scan(struct scsi_target *starget,
 				     blist_flags_t bflags, int scsi_level,
 				     enum scsi_scan_mode rescan)
@@ -1398,6 +1570,14 @@ static void scsi_sequential_lun_scan(struct scsi_target *starget,
 	else
 		max_dev_lun = min(256U, max_dev_lun);
 
+	/*
+	 * 在以下使用scsi_probe_and_add_lun():
+	 *   - drivers/scsi/scsi_scan.c|1440| <<scsi_sequential_lun_scan>> if ((scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan,
+	 *   - drivers/scsi/scsi_scan.c|1622| <<scsi_report_lun_scan>> res = scsi_probe_and_add_lun(starget,
+	 *   - drivers/scsi/scsi_scan.c|1669| <<__scsi_add_device>> scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
+	 *   - drivers/scsi/scsi_scan.c|1814| <<__scsi_scan_target>> scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan, NULL);
+	 *   - drivers/scsi/scsi_scan.c|1822| <<__scsi_scan_target>> res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL)
+	 */
 	/*
 	 * We have already scanned LUN 0, so start at LUN 1. Keep scanning
 	 * until we reach the max, or no LUN is found and we are not
@@ -1430,6 +1610,10 @@ static void scsi_sequential_lun_scan(struct scsi_target *starget,
  *     0: scan completed (or no memory, so further scanning is futile)
  *     1: could not scan with REPORT LUN
  **/
+/*
+ * 在以下使用scsi_report_lun_scan():
+ *   - drivers/scsi/scsi_scan.c|1856| <<__scsi_scan_target>> if (scsi_report_lun_scan(starget, bflags, rescan) != 0)
+ */
 static int scsi_report_lun_scan(struct scsi_target *starget, blist_flags_t bflags,
 				enum scsi_scan_mode rescan)
 {
@@ -1488,6 +1672,12 @@ static int scsi_report_lun_scan(struct scsi_target *starget, blist_flags_t bflag
 		return 1;
 
 	if (!(sdev = scsi_device_lookup_by_target(starget, 0))) {
+		/*
+		 * 在以下使用scsi_alloc_sdev():
+		 *   - drivers/scsi/scsi_scan.c|1251| <<scsi_probe_and_add_lun>> sdev = scsi_alloc_sdev(starget, lun, hostdata);
+		 *   - drivers/scsi/scsi_scan.c|1524| <<scsi_report_lun_scan>> sdev = scsi_alloc_sdev(starget, 0, NULL);
+		 *   - drivers/scsi/scsi_scan.c|2210| <<scsi_get_pseudo_sdev>> sdev = scsi_alloc_sdev(starget, U64_MAX, NULL);
+		 */
 		sdev = scsi_alloc_sdev(starget, 0, NULL);
 		if (!sdev)
 			return 0;
@@ -1504,6 +1694,11 @@ static int scsi_report_lun_scan(struct scsi_target *starget, blist_flags_t bflag
 	 */
 	length = (511 + 1) * sizeof(struct scsi_lun);
 retry:
+	/*
+	 * struct scsi_lun {
+	 *     __u8 scsi_lun[8];
+	 * };
+	 */
 	lun_data = kmalloc(length, GFP_KERNEL);
 	if (!lun_data) {
 		printk(ALLOC_FAILURE_MSG, __func__);
@@ -1586,6 +1781,14 @@ static int scsi_report_lun_scan(struct scsi_target *starget, blist_flags_t bflag
 		} else {
 			int res;
 
+			/*
+			 * 在以下使用scsi_probe_and_add_lun():
+			 *   - drivers/scsi/scsi_scan.c|1440| <<scsi_sequential_lun_scan>> if ((scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan,
+			 *   - drivers/scsi/scsi_scan.c|1622| <<scsi_report_lun_scan>> res = scsi_probe_and_add_lun(starget,
+			 *   - drivers/scsi/scsi_scan.c|1669| <<__scsi_add_device>> scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
+			 *   - drivers/scsi/scsi_scan.c|1814| <<__scsi_scan_target>> scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan, NULL);
+			 *   - drivers/scsi/scsi_scan.c|1822| <<__scsi_scan_target>> res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL)
+			 */
 			res = scsi_probe_and_add_lun(starget,
 				lun, NULL, NULL, rescan, NULL);
 			if (res == SCSI_SCAN_NO_RESPONSE) {
@@ -1623,6 +1826,12 @@ struct scsi_device *__scsi_add_device(struct Scsi_Host *shost, uint channel,
 	if (strncmp(scsi_scan_type, "none", 4) == 0)
 		return ERR_PTR(-ENODEV);
 
+	/*
+	 * 在以下使用scsi_alloc_target():
+	 *   - drivers/scsi/scsi_scan.c|1706| <<__scsi_add_device>> starget = scsi_alloc_target(parent, channel, id);
+	 *   - drivers/scsi/scsi_scan.c|1858| <<__scsi_scan_target>> starget = scsi_alloc_target(parent, channel, id);
+	 *   - drivers/scsi/scsi_scan.c|2367| <<scsi_get_pseudo_sdev>> starget = scsi_alloc_target(&shost->shost_gendev, 0, shost->max_id);
+	 */
 	starget = scsi_alloc_target(parent, channel, id);
 	if (!starget)
 		return ERR_PTR(-ENOMEM);
@@ -1633,6 +1842,14 @@ struct scsi_device *__scsi_add_device(struct Scsi_Host *shost, uint channel,
 		scsi_complete_async_scans();
 
 	if (scsi_host_scan_allowed(shost) && scsi_autopm_get_host(shost) == 0) {
+		/*
+		 * 在以下使用scsi_probe_and_add_lun():
+		 *   - drivers/scsi/scsi_scan.c|1440| <<scsi_sequential_lun_scan>> if ((scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan,
+		 *   - drivers/scsi/scsi_scan.c|1622| <<scsi_report_lun_scan>> res = scsi_probe_and_add_lun(starget,
+		 *   - drivers/scsi/scsi_scan.c|1669| <<__scsi_add_device>> scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
+		 *   - drivers/scsi/scsi_scan.c|1814| <<__scsi_scan_target>> scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan, NULL);
+		 *   - drivers/scsi/scsi_scan.c|1822| <<__scsi_scan_target>> res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL)
+		 */
 		scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
 				       SCSI_SCAN_RESCAN, hostdata);
 		scsi_autopm_put_host(shost);
@@ -1755,6 +1972,40 @@ int scsi_rescan_device(struct scsi_device *sdev)
 }
 EXPORT_SYMBOL(scsi_rescan_device);
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host()
+ *    -> async_schedule(do_scan_async, data)
+ *
+ *
+ * do_scan_async()
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
+/*
+ * 在以下使用__scsi_scan_target():
+ *   - drivers/scsi/scsi_scan.c|1879| <<scsi_scan_target>> __scsi_scan_target(parent, channel, id, lun, rescan);
+ *   - drivers/scsi/scsi_scan.c|1910| <<scsi_scan_channel>> __scsi_scan_target(&shost->shost_gendev, channel,
+ *   - drivers/scsi/scsi_scan.c|1914| <<scsi_scan_channel>> __scsi_scan_target(&shost->shost_gendev, channel,
+ */
 static void __scsi_scan_target(struct device *parent, unsigned int channel,
 		unsigned int id, u64 lun, enum scsi_scan_mode rescan)
 {
@@ -1769,12 +2020,26 @@ static void __scsi_scan_target(struct device *parent, unsigned int channel,
 		 */
 		return;
 
+	/*
+	 * 在以下使用scsi_alloc_target():
+	 *   - drivers/scsi/scsi_scan.c|1706| <<__scsi_add_device>> starget = scsi_alloc_target(parent, channel, id);
+	 *   - drivers/scsi/scsi_scan.c|1858| <<__scsi_scan_target>> starget = scsi_alloc_target(parent, channel, id);
+	 *   - drivers/scsi/scsi_scan.c|2367| <<scsi_get_pseudo_sdev>> starget = scsi_alloc_target(&shost->shost_gendev, 0, shost->max_id);
+	 */
 	starget = scsi_alloc_target(parent, channel, id);
 	if (!starget)
 		return;
 	scsi_autopm_get_target(starget);
 
 	if (lun != SCAN_WILD_CARD) {
+		/*
+		 * 在以下使用scsi_probe_and_add_lun():
+		 *   - drivers/scsi/scsi_scan.c|1440| <<scsi_sequential_lun_scan>> if ((scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan,
+		 *   - drivers/scsi/scsi_scan.c|1622| <<scsi_report_lun_scan>> res = scsi_probe_and_add_lun(starget,
+		 *   - drivers/scsi/scsi_scan.c|1669| <<__scsi_add_device>> scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
+		 *   - drivers/scsi/scsi_scan.c|1814| <<__scsi_scan_target>> scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan, NULL);
+		 *   - drivers/scsi/scsi_scan.c|1822| <<__scsi_scan_target>> res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL)
+		 */
 		/*
 		 * Scan for a specific host/chan/id/lun.
 		 */
@@ -1782,13 +2047,30 @@ static void __scsi_scan_target(struct device *parent, unsigned int channel,
 		goto out_reap;
 	}
 
+	/*
+	 * 在以下使用scsi_probe_and_add_lun():
+	 *   - drivers/scsi/scsi_scan.c|1440| <<scsi_sequential_lun_scan>> if ((scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan,
+	 *   - drivers/scsi/scsi_scan.c|1622| <<scsi_report_lun_scan>> res = scsi_probe_and_add_lun(starget,
+	 *   - drivers/scsi/scsi_scan.c|1669| <<__scsi_add_device>> scsi_probe_and_add_lun(starget, lun, NULL, &sdev,
+	 *   - drivers/scsi/scsi_scan.c|1814| <<__scsi_scan_target>> scsi_probe_and_add_lun(starget, lun, NULL, NULL, rescan, NULL);
+	 *   - drivers/scsi/scsi_scan.c|1822| <<__scsi_scan_target>> res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL)
+	 */
 	/*
 	 * Scan LUN 0, if there is some response, scan further. Ideally, we
 	 * would not configure LUN 0 until all LUNs are scanned.
 	 */
 	res = scsi_probe_and_add_lun(starget, 0, &bflags, NULL, rescan, NULL);
 	if (res == SCSI_SCAN_LUN_PRESENT || res == SCSI_SCAN_TARGET_PRESENT) {
+		/*
+		 * 只在这里调用scsi_report_lun_scan()
+		 */
 		if (scsi_report_lun_scan(starget, bflags, rescan) != 0)
+			/*
+			 * 注释:
+			 * Generally, scan from LUN 1 (LUN 0 is assumed to already have been
+			 *     scanned) to some maximum lun until a LUN is found with no device
+			 *     attached. Use the bflags to figure out any oddities.
+			 */
 			/*
 			 * The REPORT LUN did not scan the target,
 			 * do a sequential scan.
@@ -1826,6 +2108,20 @@ static void __scsi_scan_target(struct device *parent, unsigned int channel,
  *     First try a REPORT LUN scan, if that does not scan the target, do a
  *     sequential scan of LUNs on the target id.
  **/
+/*
+ * 在以下使用scsi_scan_target():
+ *   - drivers/infiniband/ulp/srp/ib_srp.c|3137| <<srp_add_target>> scsi_scan_target(&target->scsi_host->shost_gendev,
+ *   - drivers/message/fusion/mptspi.c|1149| <<mpt_work_wrapper>> scsi_scan_target(&ioc->sh->shost_gendev, 1, disk, 0, SCSI_SCAN_RESCAN);
+ *   - drivers/s390/scsi/zfcp_unit.c|30| <<zfcp_unit_scsi_scan>> scsi_scan_target(&rport->dev, 0, rport->scsi_target_id, lun,
+ *   - drivers/scsi/mpi3mr/mpi3mr_os.c|983| <<mpi3mr_report_tgtdev_to_host>> scsi_scan_target(&mrioc->shost->shost_gendev,
+ *   - drivers/scsi/scsi_transport_fc.c|2560| <<fc_user_scan_tgt>> scsi_scan_target(&rport->dev, channel, id, lun,
+ *   - drivers/scsi/scsi_transport_fc.c|3728| <<fc_scsi_scan_rport>> scsi_scan_target(&rport->dev, rport->channel,
+ *   - drivers/scsi/scsi_transport_iscsi.c|1798| <<iscsi_user_scan_session>> scsi_scan_target(&session->dev, 0, id,
+ *   - drivers/scsi/scsi_transport_sas.c|1581| <<sas_rphy_add>> scsi_scan_target(&rphy->dev, 0, rphy->scsi_target_id, lun,
+ *   - drivers/scsi/scsi_transport_sas.c|1699| <<scan_channel_zero>> scsi_scan_target(&rphy->dev, 0, rphy->scsi_target_id,
+ *   - drivers/scsi/snic/snic_disc.c|157| <<snic_scsi_scan_tgt>> scsi_scan_target(&tgt->dev,
+ *   - drivers/scsi/virtio_scsi.c|290| <<virtscsi_handle_transport_reset>> scsi_scan_target(&shost->shost_gendev, 0, target,
+ */
 void scsi_scan_target(struct device *parent, unsigned int channel,
 		      unsigned int id, u64 lun, enum scsi_scan_mode rescan)
 {
@@ -1843,6 +2139,12 @@ void scsi_scan_target(struct device *parent, unsigned int channel,
 		scsi_complete_async_scans();
 
 	if (scsi_host_scan_allowed(shost) && scsi_autopm_get_host(shost) == 0) {
+		/*
+		 * 在以下使用__scsi_scan_target():
+		 *   - drivers/scsi/scsi_scan.c|1879| <<scsi_scan_target>> __scsi_scan_target(parent, channel, id, lun, rescan);
+		 *   - drivers/scsi/scsi_scan.c|1910| <<scsi_scan_channel>> __scsi_scan_target(&shost->shost_gendev, channel,
+		 *   - drivers/scsi/scsi_scan.c|1914| <<scsi_scan_channel>> __scsi_scan_target(&shost->shost_gendev, channel,
+		 */
 		__scsi_scan_target(parent, channel, id, lun, rescan);
 		scsi_autopm_put_host(shost);
 	}
@@ -1850,6 +2152,11 @@ void scsi_scan_target(struct device *parent, unsigned int channel,
 }
 EXPORT_SYMBOL(scsi_scan_target);
 
+/*
+ * 在以下使用scsi_scan_channel():
+ *   - drivers/scsi/scsi_scan.c|1983| <<scsi_scan_host_selected>> scsi_scan_channel(shost, channel, id, lun,
+ *   - drivers/scsi/scsi_scan.c|1986| <<scsi_scan_host_selected>> scsi_scan_channel(shost, channel, id, lun, rescan);
+ */
 static void scsi_scan_channel(struct Scsi_Host *shost, unsigned int channel,
 			      unsigned int id, u64 lun,
 			      enum scsi_scan_mode rescan)
@@ -1867,6 +2174,11 @@ static void scsi_scan_channel(struct Scsi_Host *shost, unsigned int channel,
 			 * the FC ID can be the same as a target id
 			 * without a huge overhead of sparse id's.
 			 */
+			/*
+			 * 在以下使用Scsi_Host->reverse_ordering:
+			 *   - drivers/scsi/scsi_scan.c|1992| <<scsi_scan_channel>> if (shost->reverse_ordering)
+			 *   - drivers/scsi/sym53c8xx_2/sym_nvram.c|51| <<sym_nvram_setup_host>> shost->reverse_ordering = 1;
+			 */
 			if (shost->reverse_ordering)
 				/*
 				 * Scan from high to low id.
@@ -1874,6 +2186,12 @@ static void scsi_scan_channel(struct Scsi_Host *shost, unsigned int channel,
 				order_id = shost->max_id - id - 1;
 			else
 				order_id = id;
+			/*
+			 * 在以下使用__scsi_scan_target():
+			 *   - drivers/scsi/scsi_scan.c|1879| <<scsi_scan_target>> __scsi_scan_target(parent, channel, id, lun, rescan);
+			 *   - drivers/scsi/scsi_scan.c|1910| <<scsi_scan_channel>> __scsi_scan_target(&shost->shost_gendev, channel,
+			 *   - drivers/scsi/scsi_scan.c|1914| <<scsi_scan_channel>> __scsi_scan_target(&shost->shost_gendev, channel,
+			 */
 			__scsi_scan_target(&shost->shost_gendev, channel,
 					order_id, lun, rescan);
 		}
@@ -1882,6 +2200,18 @@ static void scsi_scan_channel(struct Scsi_Host *shost, unsigned int channel,
 				id, lun, rescan);
 }
 
+/*
+ * 在以下使用scsi_scan_host_selected():
+ *   - drivers/scsi/scsi_proc.c|350| <<scsi_add_single_device>> error = scsi_scan_host_selected(shost, channel, id, lun,
+ *   - drivers/scsi/scsi_scan.c|2122| <<do_scsi_scan_host>> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD,
+ *   - drivers/scsi/scsi_sysfs.c|151| <<scsi_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+ *   - drivers/scsi/scsi_transport_sas.c|1729| <<sas_user_scan>> res = scsi_scan_host_selected(shost, i, id, lun,
+ *   - drivers/scsi/scsi_transport_sas.c|1738| <<sas_user_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+ *
+ * 从do_scsi_scan_host过来.
+ * scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD,
+ *			   SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ */
 int scsi_scan_host_selected(struct Scsi_Host *shost, unsigned int channel,
 			    unsigned int id, u64 lun,
 			    enum scsi_scan_mode rescan)
@@ -1970,6 +2300,19 @@ static struct async_scan_data *scsi_prep_async_scan(struct Scsi_Host *shost)
 	spin_lock(&async_scan_lock);
 	if (list_empty(&scanning_hosts))
 		complete(&data->prev_finished);
+	/*
+	 * 在以下使用scanning_hosts:
+	 *   - drivers/scsi/scsi_scan.c|117| <<global>> static LIST_HEAD(scanning_hosts);
+	 *   - drivers/scsi/scsi_scan.c|155| <<scsi_complete_async_scans>> if (list_empty(&scanning_hosts))
+	 *   - drivers/scsi/scsi_scan.c|171| <<scsi_complete_async_scans>> if (list_empty(&scanning_hosts))
+	 *   - drivers/scsi/scsi_scan.c|173| <<scsi_complete_async_scans>> list_add_tail(&data->list, &scanning_hosts);
+	 *   - drivers/scsi/scsi_scan.c|181| <<scsi_complete_async_scans>> if (!list_empty(&scanning_hosts)) {
+	 *   - drivers/scsi/scsi_scan.c|182| <<scsi_complete_async_scans>> struct async_scan_data *next = list_entry(scanning_hosts.next,
+	 *   - drivers/scsi/scsi_scan.c|2048| <<scsi_prep_async_scan>> if (list_empty(&scanning_hosts))
+	 *   - drivers/scsi/scsi_scan.c|2050| <<scsi_prep_async_scan>> list_add_tail(&data->list, &scanning_hosts);
+	 *   - drivers/scsi/scsi_scan.c|2100| <<scsi_finish_async_scan>> if (!list_empty(&scanning_hosts)) {
+	 *   - drivers/scsi/scsi_scan.c|2101| <<scsi_finish_async_scan>> struct async_scan_data *next = list_entry(scanning_hosts.next,
+	 */
 	list_add_tail(&data->list, &scanning_hosts);
 	spin_unlock(&async_scan_lock);
 
@@ -1989,6 +2332,10 @@ static struct async_scan_data *scsi_prep_async_scan(struct Scsi_Host *shost)
  * This function announces all the devices it has found to the rest
  * of the system.
  */
+/*
+ * 在以下使用scsi_finish_async_scan():
+ *   - drivers/scsi/scsi_scan.c|2133| <<do_scan_async>> scsi_finish_async_scan(data);
+ */
 static void scsi_finish_async_scan(struct async_scan_data *data)
 {
 	struct Scsi_Host *shost;
@@ -2032,6 +2379,11 @@ static void scsi_finish_async_scan(struct async_scan_data *data)
 	kfree(data);
 }
 
+/*
+ * 在以下使用do_scsi_scan_host():
+ *   - drivers/scsi/scsi_scan.c|2132| <<do_scan_async>> do_scsi_scan_host(shost);
+ *   - drivers/scsi/scsi_scan.c|2154| <<scsi_scan_host>> do_scsi_scan_host(shost);
+ */
 static void do_scsi_scan_host(struct Scsi_Host *shost)
 {
 	if (shost->hostt->scan_finished) {
@@ -2042,17 +2394,37 @@ static void do_scsi_scan_host(struct Scsi_Host *shost)
 		while (!shost->hostt->scan_finished(shost, jiffies - start))
 			msleep(10);
 	} else {
+		/*
+		 * 在以下使用scsi_scan_host_selected():
+		 *   - drivers/scsi/scsi_proc.c|350| <<scsi_add_single_device>> error = scsi_scan_host_selected(shost, channel, id, lun,
+		 *   - drivers/scsi/scsi_scan.c|2122| <<do_scsi_scan_host>> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD,
+		 *   - drivers/scsi/scsi_sysfs.c|151| <<scsi_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+		 *   - drivers/scsi/scsi_transport_sas.c|1729| <<sas_user_scan>> res = scsi_scan_host_selected(shost, i, id, lun,
+		 *   - drivers/scsi/scsi_transport_sas.c|1738| <<sas_user_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+		 */
 		scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD,
 				SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
 	}
 }
 
+/*
+ * 在以下使用do_scan_async():
+ *   - drivers/scsi/scsi_scan.c|2162| <<scsi_scan_host>> async_schedule(do_scan_async, data);
+ */
 static void do_scan_async(void *_data, async_cookie_t c)
 {
 	struct async_scan_data *data = _data;
 	struct Scsi_Host *shost = data->shost;
 
+	/*
+	 * 在以下使用do_scsi_scan_host():
+	 *   - drivers/scsi/scsi_scan.c|2132| <<do_scan_async>> do_scsi_scan_host(shost);
+	 *   - drivers/scsi/scsi_scan.c|2154| <<scsi_scan_host>> do_scsi_scan_host(shost);
+	 */
 	do_scsi_scan_host(shost);
+	/*
+	 * 只在这里调用
+	 */
 	scsi_finish_async_scan(data);
 }
 
@@ -2072,8 +2444,20 @@ void scsi_scan_host(struct Scsi_Host *shost)
 	if (scsi_autopm_get_host(shost) < 0)
 		return;
 
+	/*
+	 * struct async_scan_data {
+	 *     struct list_head list;
+	 *     struct Scsi_Host *shost;
+	 *     struct completion prev_finished;
+	 * };
+	 */
 	data = scsi_prep_async_scan(shost);
 	if (!data) {
+		/*
+		 * 在以下使用do_scsi_scan_host():
+		 *   - drivers/scsi/scsi_scan.c|2132| <<do_scan_async>> do_scsi_scan_host(shost);
+		 *   - drivers/scsi/scsi_scan.c|2154| <<scsi_scan_host>> do_scsi_scan_host(shost);
+		 */
 		do_scsi_scan_host(shost);
 		scsi_autopm_put_host(shost);
 		return;
@@ -2088,6 +2472,37 @@ void scsi_scan_host(struct Scsi_Host *shost)
 }
 EXPORT_SYMBOL(scsi_scan_host);
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ *
+ * 在以下使用scsi_forget_host():
+ *   - drivers/scsi/hosts.c|181| <<scsi_remove_host>> scsi_forget_host(shost);
+ */
 void scsi_forget_host(struct Scsi_Host *shost)
 {
 	struct scsi_device *sdev;
@@ -2139,10 +2554,22 @@ struct scsi_device *scsi_get_pseudo_sdev(struct Scsi_Host *shost)
 	if (!scsi_host_scan_allowed(shost))
 		goto out;
 
+	/*
+	 * 在以下使用scsi_alloc_target():
+	 *   - drivers/scsi/scsi_scan.c|1706| <<__scsi_add_device>> starget = scsi_alloc_target(parent, channel, id);
+	 *   - drivers/scsi/scsi_scan.c|1858| <<__scsi_scan_target>> starget = scsi_alloc_target(parent, channel, id);
+	 *   - drivers/scsi/scsi_scan.c|2367| <<scsi_get_pseudo_sdev>> starget = scsi_alloc_target(&shost->shost_gendev, 0, shost->max_id);
+	 */
 	starget = scsi_alloc_target(&shost->shost_gendev, 0, shost->max_id);
 	if (!starget)
 		goto out;
 
+	/*
+	 * 在以下使用scsi_alloc_sdev():
+	 *   - drivers/scsi/scsi_scan.c|1251| <<scsi_probe_and_add_lun>> sdev = scsi_alloc_sdev(starget, lun, hostdata);
+	 *   - drivers/scsi/scsi_scan.c|1524| <<scsi_report_lun_scan>> sdev = scsi_alloc_sdev(starget, 0, NULL);
+	 *   - drivers/scsi/scsi_scan.c|2210| <<scsi_get_pseudo_sdev>> sdev = scsi_alloc_sdev(starget, U64_MAX, NULL);
+	 */
 	sdev = scsi_alloc_sdev(starget, U64_MAX, NULL);
 	if (!sdev) {
 		scsi_target_reap(starget);
diff --git a/drivers/scsi/scsi_sysfs.c b/drivers/scsi/scsi_sysfs.c
index 99eb0a30d..adbb671f1 100644
--- a/drivers/scsi/scsi_sysfs.c
+++ b/drivers/scsi/scsi_sysfs.c
@@ -145,6 +145,14 @@ static int scsi_scan(struct Scsi_Host *shost, const char *str)
 		return -EINVAL;
 	if (check_set(&lun, s3))
 		return -EINVAL;
+	/*
+	 * 在以下使用scsi_scan_host_selected():
+	 *   - drivers/scsi/scsi_proc.c|350| <<scsi_add_single_device>> error = scsi_scan_host_selected(shost, channel, id, lun,
+	 *   - drivers/scsi/scsi_scan.c|2122| <<do_scsi_scan_host>> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD,
+	 *   - drivers/scsi/scsi_sysfs.c|151| <<scsi_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+	 *   - drivers/scsi/scsi_transport_sas.c|1729| <<sas_user_scan>> res = scsi_scan_host_selected(shost, i, id, lun,
+	 *   - drivers/scsi/scsi_transport_sas.c|1738| <<sas_user_scan>> res = scsi_scan_host_selected(shost, channel, id, lun,
+	 */
 	if (shost->transportt->user_scan)
 		res = shost->transportt->user_scan(shost, channel, id, lun);
 	else
@@ -1402,6 +1410,47 @@ int scsi_sysfs_add_sdev(struct scsi_device *sdev)
 	return error;
 }
 
+/*
+ * [0] __scsi_remove_device
+ * [0] scsi_forget_host
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ *
+ *
+ * 在以下使用__scsi_remove_device():
+ *   - drivers/scsi/scsi_scan.c|387| <<scsi_alloc_sdev>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|1317| <<scsi_probe_and_add_lun>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|1322| <<scsi_probe_and_add_lun>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|1495| <<scsi_report_lun_scan>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|1611| <<scsi_report_lun_scan>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|1929| <<scsi_sysfs_add_devices>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|2103| <<scsi_forget_host>> __scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_scan.c|2113| <<scsi_forget_host>> __scsi_remove_device(shost->pseudo_sdev);
+ *   - drivers/scsi/scsi_sysfs.c|1484| <<scsi_remove_device>> __scsi_remove_device(sdev);
+ */
 void __scsi_remove_device(struct scsi_device *sdev)
 {
 	struct device *dev = &sdev->sdev_gendev;
@@ -1412,6 +1461,11 @@ void __scsi_remove_device(struct scsi_device *sdev)
 	 * to get a new reference with scsi_device_get() someone can still
 	 * hold a previously acquired one.
 	 */
+	/*
+	 * 只在这个大函数设置SDEV_DEL
+	 *
+	 * 简单6.19的测试这里是SDEV_RUNNING
+	 */
 	if (sdev->sdev_state == SDEV_DEL)
 		return;
 
@@ -1455,6 +1509,19 @@ void __scsi_remove_device(struct scsi_device *sdev)
 	mutex_unlock(&sdev->state_mutex);
 
 	blk_mq_destroy_queue(sdev->request_queue);
+	/*
+	 * 在以下使用Scsi_Host->tagset_refcnt:
+	 *   - drivers/scsi/hosts.c|198| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|270| <<scsi_add_host_with_dma>> kref_init(&shost->tagset_refcnt);
+	 *   - drivers/scsi/hosts.c|358| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_scan.c|347| <<scsi_alloc_sdev>> kref_get(&sdev->host->tagset_refcnt);
+	 *   - drivers/scsi/scsi_sysfs.c|1476| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 *
+	 * 在以下使用scsi_mq_free_tags():
+	 *   - drivers/scsi/hosts.c|192| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|346| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_sysfs.c|1458| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 */
 	kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
 	cancel_work_sync(&sdev->requeue_work);
 
@@ -1472,6 +1539,42 @@ void __scsi_remove_device(struct scsi_device *sdev)
 	put_device(dev);
 }
 
+/*
+ * 在以下使用scsi_remove_device():
+ *   - drivers/ata/libata-core.c|6175| <<ata_port_detach>> scsi_remove_device(dev->sdev);
+ *   - drivers/ata/libata-scsi.c|4713| <<ata_scsi_remove_dev>> scsi_remove_device(sdev);
+ *   - drivers/firewire/sbp2.c|881| <<sbp2_login>> scsi_remove_device(sdev);
+ *   - drivers/firewire/sbp2.c|1228| <<sbp2_remove>> scsi_remove_device(sdev);
+ *   - drivers/message/fusion/mptsas.c|4570| <<mptsas_hotplug_work>> scsi_remove_device(hot_plug_info->sdev);
+ *   - drivers/message/fusion/mptsas.c|5144| <<mptsas_volume_delete>> scsi_remove_device(sdev);
+ *   - drivers/s390/scsi/zfcp_unit.c|254| <<zfcp_unit_remove>> scsi_remove_device(sdev);
+ *   - drivers/scsi/aacraid/commsup.c|1235| <<aac_handle_aif>> scsi_remove_device(device);
+ *   - drivers/scsi/aacraid/commsup.c|1405| <<aac_handle_aif>> scsi_remove_device(device);
+ *   - drivers/scsi/aacraid/commsup.c|1431| <<aac_handle_aif>> scsi_remove_device(device);
+ *   - drivers/scsi/aacraid/commsup.c|1727| <<aac_remove_safw_device>> scsi_remove_device(sdev);
+ *   - drivers/scsi/arcmsr/arcmsr_hba.c|975| <<arcmsr_message_isr_bh_fn>> scsi_remove_device(psdev);
+ *   - drivers/scsi/arcmsr/arcmsr_hba.c|1667| <<arcmsr_remove_scsi_devices>> scsi_remove_device(psdev);
+ *   - drivers/scsi/esas2r/esas2r_main.c|1685| <<esas2r_remove_device>> scsi_remove_device(scsi_dev);
+ *   - drivers/scsi/hpsa.c|1896| <<hpsa_remove_device>> scsi_remove_device(sdev);
+ *   - drivers/scsi/ipr.c|3267| <<ipr_add_remove_thread>> scsi_remove_device(sdev);
+ *   - drivers/scsi/megaraid/megaraid_sas_base.c|8750| <<megasas_remove_scsi_device>> scsi_remove_device(sdev);
+ *   - drivers/scsi/mvumi.c|1389| <<mvumi_handle_hotplug>> scsi_remove_device(sdev);
+ *   - drivers/scsi/mvumi.c|1490| <<mvumi_detach_devices>> scsi_remove_device(sdev);
+ *   - drivers/scsi/mvumi.c|2441| <<mvumi_io_attach>> scsi_remove_device(sdev);
+ *   - drivers/scsi/pmcraid.c|3875| <<pmcraid_worker_function>> scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_proc.c|377| <<scsi_remove_single_device>> scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_sysfs.c|729| <<sdev_store_delete>> scsi_remove_device(sdev);
+ *   - drivers/scsi/scsi_sysfs.c|1512| <<__scsi_remove_target>> scsi_remove_device(sdev);
+ *   - drivers/scsi/smartpqi/smartpqi_init.c|1928| <<pqi_remove_device>> scsi_remove_device(device->sdev);
+ *   - drivers/scsi/storvsc_drv.c|535| <<storvsc_remove_lun>> scsi_remove_device(sdev);
+ *   - drivers/scsi/virtio_scsi.c|299| <<virtscsi_handle_transport_reset>> scsi_remove_device(sdev);
+ *   - drivers/scsi/virtio_scsi.c|362| <<virtscsi_rescan_hotunplug>> scsi_remove_device(sdev);
+ *   - drivers/scsi/virtio_scsi.c|370| <<virtscsi_rescan_hotunplug>> scsi_remove_device(sdev);
+ *   - drivers/scsi/vmw_pvscsi.c|1080| <<pvscsi_process_msg>> scsi_remove_device(sdev);
+ *   - drivers/scsi/xen-scsifront.c|1106| <<scsifront_do_lun_hotplug>> scsi_remove_device(sdev);
+ *   - drivers/target/loopback/tcm_loop.c|621| <<tcm_loop_port_unlink>> scsi_remove_device(sd);
+ *   - drivers/ufs/core/ufshcd.c|8267| <<ufshcd_scsi_add_wlus>> scsi_remove_device(hba->ufs_device_wlun);
+ */
 /**
  * scsi_remove_device - unregister a device from the scsi bus
  * @sdev:	scsi_device to unregister
@@ -1481,11 +1584,27 @@ void scsi_remove_device(struct scsi_device *sdev)
 	struct Scsi_Host *shost = sdev->host;
 
 	mutex_lock(&shost->scan_mutex);
+	/*
+	 * 在以下使用__scsi_remove_device():
+	 *   - drivers/scsi/scsi_scan.c|387| <<scsi_alloc_sdev>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|1317| <<scsi_probe_and_add_lun>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|1322| <<scsi_probe_and_add_lun>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|1495| <<scsi_report_lun_scan>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|1611| <<scsi_report_lun_scan>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|1929| <<scsi_sysfs_add_devices>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|2103| <<scsi_forget_host>> __scsi_remove_device(sdev);
+	 *   - drivers/scsi/scsi_scan.c|2113| <<scsi_forget_host>> __scsi_remove_device(shost->pseudo_sdev);
+	 *   - drivers/scsi/scsi_sysfs.c|1484| <<scsi_remove_device>> __scsi_remove_device(sdev);
+	 */
 	__scsi_remove_device(sdev);
 	mutex_unlock(&shost->scan_mutex);
 }
 EXPORT_SYMBOL(scsi_remove_device);
 
+/*
+ * 在以下使用__scsi_remove_target():
+ *   - drivers/scsi/scsi_sysfs.c|1621| <<scsi_remove_target>> __scsi_remove_target(starget);
+ */
 static void __scsi_remove_target(struct scsi_target *starget)
 {
 	struct Scsi_Host *shost = dev_to_shost(starget->dev.parent);
@@ -1525,6 +1644,17 @@ static void __scsi_remove_target(struct scsi_target *starget)
  * requests the addition of another device then the target won't be
  * removed.
  */
+/*
+ * 在以下使用scsi_remove_target():
+ *   - drivers/scsi/mpi3mr/mpi3mr_os.c|929| <<mpi3mr_remove_tgtdev_from_host>> scsi_remove_target(&tgtdev->starget->dev);
+ *   - drivers/scsi/mpt3sas/mpt3sas_scsih.c|7976| <<_scsih_pcie_device_remove_from_sml>> scsi_remove_target(&pcie_device->starget->dev);
+ *   - drivers/scsi/mpt3sas/mpt3sas_scsih.c|9098| <<_scsih_sas_volume_delete>> scsi_remove_target(&starget->dev);
+ *   - drivers/scsi/mpt3sas/mpt3sas_scsih.c|11299| <<scsih_remove>> scsi_remove_target(&raid_device->starget->dev);
+ *   - drivers/scsi/scsi_transport_fc.c|2943| <<fc_starget_delete>> scsi_remove_target(&rport->dev);
+ *   - drivers/scsi/scsi_transport_iscsi.c|2001| <<__iscsi_unbind_session>> scsi_remove_target(&session->dev);
+ *   - drivers/scsi/scsi_transport_sas.c|1657| <<sas_rphy_remove>> scsi_remove_target(dev);
+ *   - drivers/scsi/snic/snic_disc.c|226| <<snic_tgt_del>> scsi_remove_target(&tgt->dev);
+ */
 void scsi_remove_target(struct device *dev)
 {
 	struct Scsi_Host *shost = dev_to_shost(dev->parent);
diff --git a/drivers/scsi/virtio_scsi.c b/drivers/scsi/virtio_scsi.c
index 96a69eddd..e1a82f05c 100644
--- a/drivers/scsi/virtio_scsi.c
+++ b/drivers/scsi/virtio_scsi.c
@@ -32,6 +32,35 @@
 
 #include "sd.h"
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host()
+ *    -> async_schedule(do_scan_async, data)
+ *
+ *
+ * do_scan_async()
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
+
 #define VIRTIO_SCSI_MEMPOOL_SZ 64
 #define VIRTIO_SCSI_EVENT_LEN 8
 #define VIRTIO_SCSI_VQ_BASE 2
@@ -287,6 +316,20 @@ static void virtscsi_handle_transport_reset(struct virtio_scsi *vscsi,
 	switch (virtio32_to_cpu(vscsi->vdev, event->reason)) {
 	case VIRTIO_SCSI_EVT_RESET_RESCAN:
 		if (lun == 0) {
+			/*
+			 * 在以下使用scsi_scan_target():
+			 *   - drivers/infiniband/ulp/srp/ib_srp.c|3137| <<srp_add_target>> scsi_scan_target(&target->scsi_host->shost_gendev,
+			 *   - drivers/message/fusion/mptspi.c|1149| <<mpt_work_wrapper>> scsi_scan_target(&ioc->sh->shost_gendev, 1, disk, 0, SCSI_SCAN_RESCAN);
+			 *   - drivers/s390/scsi/zfcp_unit.c|30| <<zfcp_unit_scsi_scan>> scsi_scan_target(&rport->dev, 0, rport->scsi_target_id, lun,
+			 *   - drivers/scsi/mpi3mr/mpi3mr_os.c|983| <<mpi3mr_report_tgtdev_to_host>> scsi_scan_target(&mrioc->shost->shost_gendev,
+			 *   - drivers/scsi/scsi_transport_fc.c|2560| <<fc_user_scan_tgt>> scsi_scan_target(&rport->dev, channel, id, lun,
+			 *   - drivers/scsi/scsi_transport_fc.c|3728| <<fc_scsi_scan_rport>> scsi_scan_target(&rport->dev, rport->channel,
+			 *   - drivers/scsi/scsi_transport_iscsi.c|1798| <<iscsi_user_scan_session>> scsi_scan_target(&session->dev, 0, id,
+			 *   - drivers/scsi/scsi_transport_sas.c|1581| <<sas_rphy_add>> scsi_scan_target(&rphy->dev, 0, rphy->scsi_target_id, lun,
+			 *   - drivers/scsi/scsi_transport_sas.c|1699| <<scan_channel_zero>> scsi_scan_target(&rphy->dev, 0, rphy->scsi_target_id,
+			 *   - drivers/scsi/snic/snic_disc.c|157| <<snic_scsi_scan_tgt>> scsi_scan_target(&tgt->dev,
+			 *   - drivers/scsi/virtio_scsi.c|290| <<virtscsi_handle_transport_reset>> scsi_scan_target(&shost->shost_gendev, 0, target,
+			 */
 			scsi_scan_target(&shost->shost_gendev, 0, target,
 					 SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
 		} else {
@@ -333,6 +376,10 @@ static void virtscsi_handle_param_change(struct virtio_scsi *vscsi,
 	scsi_device_put(sdev);
 }
 
+/*
+ * 在以下使用virtscsi_rescan_hotunplug():
+ *   - drivers/scsi/virtio_scsi.c|391| <<virtscsi_handle_event>> ret = virtscsi_rescan_hotunplug(vscsi);
+ */
 static int virtscsi_rescan_hotunplug(struct virtio_scsi *vscsi)
 {
 	struct scsi_device *sdev;
@@ -375,6 +422,10 @@ static int virtscsi_rescan_hotunplug(struct virtio_scsi *vscsi)
 	return 0;
 }
 
+/*
+ * 在以下使用virtscsi_handle_event():
+ *   - drivers/scsi/virtio_scsi.c|239| <<virtscsi_kick_event>> INIT_WORK(&event_node->work, virtscsi_handle_event);
+ */
 static void virtscsi_handle_event(struct work_struct *work)
 {
 	struct virtio_scsi_event_node *event_node =
@@ -901,6 +952,35 @@ static int virtscsi_init(struct virtio_device *vdev,
 	return err;
 }
 
+/*
+ * virtio scsi probe的过程.
+ * 假设hotplug一个vhost-scsi的HBA, 上面只有一个lun.
+ *
+ * virtscsi_probe()
+ * -> scsi_scan_host() 
+ *    -> async_schedule(do_scan_async, data)
+ *                      
+ *              
+ * do_scan_async()      
+ * -> do_scsi_scan_host()
+ *    -> scsi_scan_host_selected(shost, SCAN_WILD_CARD, SCAN_WILD_CARD, SCAN_WILD_CARD, SCSI_SCAN_INITIAL);
+ *       -> 针对每一个channel: scsi_scan_channel() 一般一个channel
+ *          -> 针对256个target: __scsi_scan_target()
+ *      
+ *
+ * __scsi_scan_target() -->调用256次
+ * -> scsi_alloc_target()
+ *    -> scsi_probe_and_add_lun() -->针对LUN0, 返回SCSI_SCAN_LUN_PRESENT或者SCSI_SCAN_TARGET_PRESENT说明有效
+ *       -> scsi_alloc_sdev()
+ *       -> __scsi_remove_device()
+ *    -> scsi_report_lun_scan()
+ *       if (!scsi_device_lookup_by_target(starget, 0))
+ *         scsi_alloc_sdev(LUN0) ===> 注意, 就算LUN0不存在这里重新分配!
+ *       -> 针对每一个lun调用scsi_probe_and_add_lun()
+ *          -> scsi_alloc_sdev()
+ *          -> __scsi_remove_device()
+ */
+
 static int virtscsi_probe(struct virtio_device *vdev)
 {
 	struct Scsi_Host *shost;
@@ -990,6 +1070,34 @@ static int virtscsi_probe(struct virtio_device *vdev)
 	return err;
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 static void virtscsi_remove(struct virtio_device *vdev)
 {
 	struct Scsi_Host *shost = virtio_scsi_host(vdev);
diff --git a/drivers/vhost/scsi.c b/drivers/vhost/scsi.c
index f43c1fe9f..92f1b9b03 100644
--- a/drivers/vhost/scsi.c
+++ b/drivers/vhost/scsi.c
@@ -1931,6 +1931,16 @@ static int vhost_scsi_setup_vq_cmds(struct vhost_virtqueue *vq, int max_cmds)
 	if (svq->scsi_cmds)
 		return 0;
 
+	/*
+	 * 在以下使用sbitmap_init_node():
+	 *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+	 *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+	 *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+	 *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+	 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+	 *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+	 *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+	 */
 	if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
 			      NUMA_NO_NODE, false, true))
 		return -ENOMEM;
diff --git a/drivers/virtio/virtio.c b/drivers/virtio/virtio.c
index 5bdc6b82b..d3e6de921 100644
--- a/drivers/virtio/virtio.c
+++ b/drivers/virtio/virtio.c
@@ -365,6 +365,34 @@ static int virtio_dev_probe(struct device *_d)
 
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 static void virtio_dev_remove(struct device *_d)
 {
 	struct virtio_device *dev = dev_to_virtio(_d);
@@ -372,6 +400,9 @@ static void virtio_dev_remove(struct device *_d)
 
 	virtio_config_core_disable(dev);
 
+	/*
+	 * 例子: virtscsi_remove
+	 */
 	drv->remove(dev);
 
 	/* Driver should have reset device. */
diff --git a/drivers/virtio/virtio_pci_common.c b/drivers/virtio/virtio_pci_common.c
index d6d79af44..fe67478d9 100644
--- a/drivers/virtio/virtio_pci_common.c
+++ b/drivers/virtio/virtio_pci_common.c
@@ -742,6 +742,34 @@ static int virtio_pci_probe(struct pci_dev *pci_dev,
 	return rc;
 }
 
+/*
+ * [0] scsi_mq_free_tags
+ * [0] scsi_remove_host
+ * [0] virtscsi_remove
+ * [0] virtio_dev_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] bus_remove_device
+ * [0] device_del
+ * [0] device_unregister
+ * [0] unregister_virtio_device
+ * [0] virtio_pci_remove
+ * [0] pci_device_remove
+ * [0] device_remove
+ * [0] device_release_driver_internal
+ * [0] device_release_driver
+ * [0] pci_stop_bus_device
+ * [0] pci_stop_and_remove_bus_device
+ * [0] pciehp_unconfigure_device
+ * [0] pciehp_disable_slot
+ * [0] pciehp_handle_disable_request
+ * [0] pciehp_ist
+ * [0] irq_thread_fn
+ * [0] irq_thread
+ * [0] kthread
+ * [0] ret_from_fork
+ */
 static void virtio_pci_remove(struct pci_dev *pci_dev)
 {
 	struct virtio_pci_device *vp_dev = pci_get_drvdata(pci_dev);
diff --git a/include/scsi/scsi_device.h b/include/scsi/scsi_device.h
index d32f5841f..36fd94ea9 100644
--- a/include/scsi/scsi_device.h
+++ b/include/scsi/scsi_device.h
@@ -108,6 +108,29 @@ struct scsi_device {
 	struct list_head    siblings;   /* list of all devices on this host */
 	struct list_head    same_target_siblings; /* just the devices sharing same target id */
 
+	/*
+	 * 在以下使用scsi->budget_map:
+	 *   - drivers/scsi/scsi.c|219| <<scsi_change_queue_depth>> if (!sdev->budget_map.map)
+	 *   - drivers/scsi/scsi.c|232| <<scsi_change_queue_depth>> sbitmap_resize(&sdev->budget_map, sdev->queue_depth);
+	 *   - drivers/scsi/scsi.c|261| <<scsi_track_queue_full>> if (!sdev->budget_map.map)
+	 *   - drivers/scsi/scsi_error.c|761| <<scsi_handle_queue_ramp_up>> if (!sdev->budget_map.map)
+	 *   - drivers/scsi/scsi_lib.c|407| <<scsi_device_unbusy>> if (sdev->budget_map.map)
+	 *   - drivers/scsi/scsi_lib.c|408| <<scsi_device_unbusy>> sbitmap_put(&sdev->budget_map, cmd->budget_token);
+	 *   - drivers/scsi/scsi_lib.c|1376| <<scsi_dev_queue_ready>> if (!sdev->budget_map.map)
+	 *   - drivers/scsi/scsi_lib.c|1379| <<scsi_dev_queue_ready>> token = sbitmap_get(&sdev->budget_map);
+	 *   - drivers/scsi/scsi_lib.c|1392| <<scsi_dev_queue_ready>> sbitmap_put(&sdev->budget_map, token);
+	 *   - drivers/scsi/scsi_lib.c|1782| <<scsi_mq_put_budget>> if (sdev->budget_map.map)
+	 *   - drivers/scsi/scsi_lib.c|1783| <<scsi_mq_put_budget>> sbitmap_put(&sdev->budget_map, budget_token);
+	 *   - drivers/scsi/scsi_scan.c|227| <<scsi_realloc_sdev_budget_map>> bool need_alloc = !sdev->budget_map.map;
+	 *   - drivers/scsi/scsi_scan.c|239| <<scsi_realloc_sdev_budget_map>> if (!need_alloc && new_shift != sdev->budget_map.shift)
+	 *   - drivers/scsi/scsi_scan.c|251| <<scsi_realloc_sdev_budget_map>> sb_backup = sdev->budget_map;
+	 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+	 *   - drivers/scsi/scsi_scan.c|258| <<scsi_realloc_sdev_budget_map>> sbitmap_resize(&sdev->budget_map, depth);
+	 *   - drivers/scsi/scsi_scan.c|262| <<scsi_realloc_sdev_budget_map>> sdev->budget_map = sb_backup;
+	 *   - drivers/scsi/scsi_scan.c|1146| <<scsi_add_lun>> WARN_ON_ONCE(sdev->max_queue_depth > sdev->budget_map.depth);
+	 *   - drivers/scsi/scsi_sysfs.c|481| <<scsi_device_dev_release>> sbitmap_free(&sdev->budget_map);
+	 *   - include/scsi/scsi_device.h|718| <<scsi_device_busy>> return sbitmap_weight(&sdev->budget_map);
+	 */
 	struct sbitmap budget_map;
 	atomic_t device_blocked;	/* Device returned QUEUE_FULL. */
 
diff --git a/include/scsi/scsi_host.h b/include/scsi/scsi_host.h
index e87cf7ead..7baeb47cb 100644
--- a/include/scsi/scsi_host.h
+++ b/include/scsi/scsi_host.h
@@ -579,7 +579,21 @@ struct Scsi_Host {
 	const struct scsi_host_template *hostt;
 	struct scsi_transport_template *transportt;
 
+	/*
+	 * 在以下使用Scsi_Host->tagset_refcnt:
+	 *   - drivers/scsi/hosts.c|198| <<scsi_remove_host>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/hosts.c|270| <<scsi_add_host_with_dma>> kref_init(&shost->tagset_refcnt);
+	 *   - drivers/scsi/hosts.c|358| <<scsi_add_host_with_dma>> kref_put(&shost->tagset_refcnt, scsi_mq_free_tags);
+	 *   - drivers/scsi/scsi_scan.c|347| <<scsi_alloc_sdev>> kref_get(&sdev->host->tagset_refcnt);
+	 *   - drivers/scsi/scsi_sysfs.c|1476| <<__scsi_remove_device>> kref_put(&sdev->host->tagset_refcnt, scsi_mq_free_tags);
+	 */
 	struct kref		tagset_refcnt;
+	/*
+	 * 在以下使用Scsi_Host->tagset_freed:
+	 *   - drivers/scsi/hosts.c|206| <<scsi_remove_host>> wait_for_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/hosts.c|286| <<scsi_add_host_with_dma>> init_completion(&shost->tagset_freed);
+	 *   - drivers/scsi/scsi_lib.c|2148| <<scsi_mq_free_tags>> complete(&shost->tagset_freed);
+	 */
 	struct completion	tagset_freed;
 	/* Area to keep a shared tag map */
 	struct blk_mq_tag_set	tag_set;
@@ -669,6 +683,11 @@ struct Scsi_Host {
 	 * set for the minority of drivers whose authors actually read
 	 * the spec ;).
 	 */
+	/*
+	 * 在以下使用Scsi_Host->reverse_ordering:
+	 *   - drivers/scsi/scsi_scan.c|1992| <<scsi_scan_channel>> if (shost->reverse_ordering)
+	 *   - drivers/scsi/sym53c8xx_2/sym_nvram.c|51| <<sym_nvram_setup_host>> shost->reverse_ordering = 1;
+	 */
 	unsigned reverse_ordering:1;
 
 	/* Task mgmt function in progress */
diff --git a/lib/sbitmap.c b/lib/sbitmap.c
index 4d188d05d..026bdf660 100644
--- a/lib/sbitmap.c
+++ b/lib/sbitmap.c
@@ -98,6 +98,16 @@ static inline bool sbitmap_deferred_clear(struct sbitmap_word *map,
 	return true;
 }
 
+/*
+ * 在以下使用sbitmap_init_node():
+ *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+ *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+ *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+ *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+ *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+ *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+ *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+ */
 int sbitmap_init_node(struct sbitmap *sb, unsigned int depth, int shift,
 		      gfp_t flags, int node, bool round_robin,
 		      bool alloc_hint)
@@ -451,6 +461,16 @@ int sbitmap_queue_init_node(struct sbitmap_queue *sbq, unsigned int depth,
 	int ret;
 	int i;
 
+	/*
+	 * 在以下使用sbitmap_init_node():
+	 *   - lib/sbitmap.c|143| <<global>> EXPORT_SYMBOL_GPL(sbitmap_init_node);
+	 *   - block/blk-mq.c|4054| <<blk_mq_alloc_hctx>> if (sbitmap_init_node(&hctx->ctx_map, nr_cpu_ids, ilog2(8),
+	 *   - block/kyber-iosched.c|483| <<kyber_init_hctx>> if (sbitmap_init_node(&khd->kcq_map[i], hctx->nr_ctx,
+	 *   - drivers/nvme/target/rdma.c|454| <<nvmet_rdma_alloc_rsps>> if (sbitmap_init_node(&queue->rsp_tags, nr_rsps, -1, GFP_KERNEL,
+	 *   - drivers/scsi/scsi_scan.c|253| <<scsi_realloc_sdev_budget_map>> ret = sbitmap_init_node(&sdev->budget_map,
+	 *   - drivers/vhost/scsi.c|1934| <<vhost_scsi_setup_vq_cmds>> if (sbitmap_init_node(&svq->scsi_tags, max_cmds, -1, GFP_KERNEL,
+	 *   - lib/sbitmap.c|454| <<sbitmap_queue_init_node>> ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
+	 */
 	ret = sbitmap_init_node(&sbq->sb, depth, shift, flags, node,
 				round_robin, true);
 	if (ret)
-- 
2.50.1 (Apple Git-155)

