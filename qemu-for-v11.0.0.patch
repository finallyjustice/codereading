From 2b46cb19d2cba6fd743b564a5663eb2dee812cd4 Mon Sep 17 00:00:00 2001
From: Dongli Zhang <dongli.zhang0129@gmail.com>
Date: Mon, 25 May 2026 11:23:44 -0700
Subject: [PATCH 1/1] qemu for v11.0.0

Signed-off-by: Dongli Zhang <dongli.zhang0129@gmail.com>
---
 accel/kvm/kvm-all.c                |   9 +
 backends/hostmem-memfd.c           |  13 +
 backends/hostmem-shm.c             |  13 +
 backends/iommufd.c                 |  13 +
 block/accounting.c                 |  34 ++
 block/block-backend.c              |   7 +
 hw/acpi/aml-build.c                | 158 +++++++
 hw/acpi/bios-linker-loader.c       |  58 +++
 hw/acpi/core.c                     |   7 +
 hw/acpi/erst.c                     |   5 +
 hw/acpi/generic_event_device.c     |   9 +
 hw/acpi/ich9.c                     |  13 +
 hw/acpi/pci.c                      |   6 +
 hw/acpi/pcihp.c                    | 104 +++++
 hw/acpi/piix4.c                    | 156 +++++++
 hw/acpi/utils.c                    |  18 +
 hw/arm/virt-acpi-build.c           |  14 +
 hw/block/virtio-blk.c              | 149 ++++++
 hw/char/serial.c                   |  62 +++
 hw/char/virtio-serial-bus.c        |  20 +
 hw/core/irq.c                      |  48 ++
 hw/core/loader.c                   |   9 +
 hw/core/machine.c                  |   9 +
 hw/core/vm-change-state-handler.c  |  32 ++
 hw/i386/acpi-build.c               | 497 ++++++++++++++++++++
 hw/i386/acpi-common.c              |   8 +
 hw/i386/acpi-microvm.c             |  25 +
 hw/i386/amd_iommu.c                |  41 ++
 hw/i386/intel_iommu.c              |  15 +
 hw/i386/kvm/i8259.c                |   4 +
 hw/i386/pc.c                       |  60 +++
 hw/i386/pc_piix.c                  |  30 ++
 hw/i386/pc_q35.c                   |  30 ++
 hw/i386/x86-common.c               |  69 +++
 hw/i386/x86-cpu.c                  |  28 ++
 hw/ide/ahci.c                      |   7 +
 hw/ide/core.c                      |   7 +
 hw/intc/ioapic_common.c            |   4 +
 hw/isa/lpc_ich9.c                  |   7 +
 hw/net/igb.c                       |   7 +
 hw/net/igb_core.c                  |  16 +
 hw/net/net_tx_pkt.c                |   4 +
 hw/net/virtio-net.c                | 236 ++++++++++
 hw/nvme/ctrl.c                     |  23 +
 hw/nvram/fw_cfg.c                  |  13 +
 hw/pci-host/gpex-acpi.c            |   6 +
 hw/pci/pcie.c                      |   4 +
 hw/pci/pcie_sriov.c                |  34 ++
 hw/scsi/scsi-bus.c                 |  50 ++
 hw/scsi/scsi-disk.c                | 165 +++++++
 hw/scsi/virtio-scsi.c              |  13 +
 hw/vfio/container-legacy.c         |  28 ++
 hw/vfio/container.c                |  12 +
 hw/vfio/cpr-iommufd.c              |  64 +++
 hw/vfio/cpr-legacy.c               |  28 ++
 hw/vfio/cpr.c                      |  13 +
 hw/vfio/device.c                   |  15 +
 hw/vfio/iommufd.c                  |   6 +
 hw/vfio/migration.c                |  81 ++++
 hw/virtio/vhost-shadow-virtqueue.c |  27 ++
 hw/virtio/vhost-user.c             |  24 +
 hw/virtio/virtio-crypto.c          |  13 +
 hw/virtio/virtio-rng.c             |  20 +
 hw/virtio/virtio.c                 | 701 +++++++++++++++++++++++++++++
 include/hw/acpi/acpi-defs.h        |   7 +
 include/hw/acpi/acpi.h             |   6 +
 include/hw/acpi/pcihp.h            |  16 +
 include/hw/core/irq.h              |  48 ++
 include/hw/i386/pc.h               |  30 ++
 include/hw/vfio/vfio-container.h   |   6 +
 include/hw/virtio/virtio.h         |  18 +
 migration/channel.c                |   3 +
 migration/colo.c                   |   8 +
 migration/cpr.c                    | 205 +++++++++
 migration/migration-stats.c        |  10 +
 migration/migration-stats.h        |  10 +
 migration/migration.c              | 326 ++++++++++++++
 migration/multifd-device-state.c   |  10 +
 migration/multifd-nocomp.c         |  70 +++
 migration/multifd-qatzip.c         |  10 +
 migration/multifd-qpl.c            |  10 +
 migration/multifd-uadk.c           |  10 +
 migration/multifd-zlib.c           |  10 +
 migration/multifd-zstd.c           |  10 +
 migration/multifd.c                | 207 +++++++++
 migration/multifd.h                |   7 +
 migration/options.c                |  28 ++
 migration/postcopy-ram.c           |   8 +
 migration/ram.c                    |  33 ++
 migration/savevm.c                 |  63 +++
 net/filter.c                       |   5 +
 net/net.c                          |  12 +
 net/queue.c                        |   5 +
 net/vhost-vdpa.c                   |   5 +
 system/cpus.c                      |   9 +
 system/physmem.c                   |  43 ++
 system/runstate.c                  |  23 +
 util/fifo8.c                       |  20 +
 98 files changed, 4692 insertions(+)

diff --git a/accel/kvm/kvm-all.c b/accel/kvm/kvm-all.c
index 92af42503..97f6f5378 100644
--- a/accel/kvm/kvm-all.c
+++ b/accel/kvm/kvm-all.c
@@ -2885,6 +2885,15 @@ static int kvm_reset_vmfd(MachineState *ms)
      * can call synchronize_all_post_init() to sync all vcpu states to
      * kvm.
      */
+    /*
+     * 在以下使用cpu_synchronize_all_post_init():
+     *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+     *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+     *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+     */
     if (ms->cgs) {
         cpu_synchronize_all_post_init();
     }
diff --git a/backends/hostmem-memfd.c b/backends/hostmem-memfd.c
index 923239f9c..cdfaf84f0 100644
--- a/backends/hostmem-memfd.c
+++ b/backends/hostmem-memfd.c
@@ -35,6 +35,19 @@ memfd_backend_memory_alloc(HostMemoryBackend *backend, Error **errp)
 {
     HostMemoryBackendMemfd *m = MEMORY_BACKEND_MEMFD(backend);
     g_autofree char *name = host_memory_backend_get_name(backend);
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     int fd = cpr_find_fd(name, 0);
     uint32_t ram_flags;
 
diff --git a/backends/hostmem-shm.c b/backends/hostmem-shm.c
index 806e2670e..83f8069be 100644
--- a/backends/hostmem-shm.c
+++ b/backends/hostmem-shm.c
@@ -28,6 +28,19 @@ shm_backend_memory_alloc(HostMemoryBackend *backend, Error **errp)
 {
     g_autofree char *backend_name = host_memory_backend_get_name(backend);
     uint32_t ram_flags;
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     int fd = cpr_find_fd(backend_name, 0);
 
     if (!backend->size) {
diff --git a/backends/iommufd.c b/backends/iommufd.c
index e1fee16ac..305a978dd 100644
--- a/backends/iommufd.c
+++ b/backends/iommufd.c
@@ -78,6 +78,19 @@ static void iommufd_backend_complete(UserCreatable *uc, Error **errp)
     if (!be->owned) {
         /* fd came from the command line. Fetch updated value from cpr state. */
         if (cpr_is_incoming()) {
+            /*
+	     * 在以下使用cpr_find_fd():
+             *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+             *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+             *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+             *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+             *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+             *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+             *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+             *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+             *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+             *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+	     */
             be->fd = cpr_find_fd(name, 0);
         } else {
             cpr_save_fd(name, 0, be->fd);
diff --git a/block/accounting.c b/block/accounting.c
index f00fe9974..f1f22eb85 100644
--- a/block/accounting.c
+++ b/block/accounting.c
@@ -262,6 +262,40 @@ static void block_account_one_io(BlockAcctStats *stats, BlockAcctCookie *cookie,
     cookie->type = BLOCK_ACCT_NONE;
 }
 
+/*
+ * 在以下使用block_acct_done():
+ *   - hw/block/dataplane/xen-block.c|304| <<xen_block_complete_aio>> block_acct_done(blk_get_stats(dataplane->blk), &request->acct);
+ *   - hw/block/virtio-blk.c|145| <<virtio_blk_rw_complete>> block_acct_done(blk_get_stats(s->blk), &req->acct);
+ *   - hw/block/virtio-blk.c|160| <<virtio_blk_flush_complete>> block_acct_done(blk_get_stats(s->blk), &req->acct);
+ *   - hw/block/virtio-blk.c|177| <<virtio_blk_discard_write_zeroes_complete>> block_acct_done(blk_get_stats(s->blk), &req->acct);
+ *   - hw/ide/ahci.c|1007| <<ncq_finish>> block_acct_done(blk_get_stats(ncq_tfs->drive->port.ifs[0].blk),
+ *   - hw/ide/atapi.c|120| <<cd_read_sector_sync>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/atapi.c|140| <<cd_read_sector_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/atapi.c|417| <<ide_atapi_cmd_read_dma_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/core.c|470| <<ide_issue_trim_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/core.c|779| <<ide_sector_read_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/core.c|990| <<ide_dma_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/core.c|1048| <<ide_sector_write_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/core.c|1126| <<ide_flush_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/macio.c|125| <<pmac_ide_atapi_transfer_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/ide/macio.c|205| <<pmac_ide_transfer_cb>> block_acct_done(blk_get_stats(s->blk), &s->acct);
+ *   - hw/nvme/ctrl.c|2177| <<nvme_rw_complete_cb>> block_acct_done(stats, acct);
+ *   - hw/nvme/ctrl.c|2267| <<nvme_verify_cb>> block_acct_done(stats, acct);
+ *   - hw/nvme/ctrl.c|2416| <<nvme_compare_mdata_cb>> block_acct_done(stats, acct);
+ *   - hw/nvme/ctrl.c|2485| <<nvme_compare_data_cb>> block_acct_done(stats, acct);
+ *   - hw/nvme/ctrl.c|2788| <<nvme_copy_done>> block_acct_done(stats, &iocb->acct.read);
+ *   - hw/nvme/ctrl.c|2789| <<nvme_copy_done>> block_acct_done(stats, &iocb->acct.write);
+ *   - hw/scsi/scsi-disk.c|392| <<scsi_aio_complete>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - hw/scsi/scsi-disk.c|462| <<scsi_dma_complete>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - hw/scsi/scsi-disk.c|510| <<scsi_read_complete>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - hw/scsi/scsi-disk.c|579| <<scsi_do_read_cb>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - hw/scsi/scsi-disk.c|686| <<scsi_write_complete>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - hw/scsi/scsi-disk.c|1930| <<scsi_unmap_complete>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - hw/scsi/scsi-disk.c|2017| <<scsi_write_same_complete>> block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
+ *   - qemu-io-cmds.c|1375| <<aio_write_done>> block_acct_done(blk_get_stats(ctx->blk), &ctx->acct);
+ *   - qemu-io-cmds.c|1418| <<aio_read_done>> block_acct_done(blk_get_stats(ctx->blk), &ctx->acct);
+ *   - qemu-io-cmds.c|1711| <<aio_flush_f>> block_acct_done(blk_get_stats(blk), &cookie);
+ */
 void block_acct_done(BlockAcctStats *stats, BlockAcctCookie *cookie)
 {
     block_account_one_io(stats, cookie, false);
diff --git a/block/block-backend.c b/block/block-backend.c
index 994465712..279fd2129 100644
--- a/block/block-backend.c
+++ b/block/block-backend.c
@@ -2161,6 +2161,13 @@ static void send_qmp_error_event(BlockBackend *blk,
  * about the error, it does not know whether an operation comes from
  * the device or the block layer (from a job, for example).
  */
+/*
+ * 在以下使用blk_error_action():
+ *  - hw/block/virtio-blk.c|94| <<virtio_blk_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+ *  - hw/ide/ahci.c|1030| <<ncq_cb>> blk_error_action(ide_state->blk, action, is_read, -ret);
+ *  - hw/ide/core.c|876| <<ide_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+ *  - hw/scsi/scsi-disk.c|300| <<scsi_handle_rw_error>> blk_error_action(s->qdev.conf.blk, action, is_read, error);
+ */
 void blk_error_action(BlockBackend *blk, BlockErrorAction action,
                       bool is_read, int error)
 {
diff --git a/hw/acpi/aml-build.c b/hw/acpi/aml-build.c
index 4b3740508..9d5f1919d 100644
--- a/hw/acpi/aml-build.c
+++ b/hw/acpi/aml-build.c
@@ -1781,6 +1781,18 @@ void acpi_add_table(GArray *table_offsets, GArray *table_data)
     g_array_append_val(table_offsets, offset);
 }
 
+/*
+ * 在以下使用acpi_build_tables_init():
+ *   - hw/arm/virt-acpi-build.c|1431| <<virt_acpi_build_update>> acpi_build_tables_init(&tables);
+ *   - hw/arm/virt-acpi-build.c|1476| <<virt_acpi_setup>> acpi_build_tables_init(&tables);
+ *   - hw/i386/acpi-build.c|2336| <<acpi_build_update>> acpi_build_tables_init(&tables);
+ *   - hw/i386/acpi-build.c|2430| <<acpi_setup>> acpi_build_tables_init(&tables);
+ *   - hw/i386/acpi-microvm.c|256| <<acpi_setup_microvm>> acpi_build_tables_init(&tables);
+ *   - hw/loongarch/virt-acpi-build.c|650| <<acpi_build_update>> acpi_build_tables_init(&tables);
+ *   - hw/loongarch/virt-acpi-build.c|702| <<virt_acpi_setup>> acpi_build_tables_init(&tables);
+ *   - hw/riscv/virt-acpi-build.c|976| <<virt_acpi_build_update>> acpi_build_tables_init(&tables);
+ *   - hw/riscv/virt-acpi-build.c|1010| <<virt_acpi_setup>> acpi_build_tables_init(&tables);
+ */
 void acpi_build_tables_init(AcpiBuildTables *tables)
 {
     tables->rsdp = g_array_new(false, true /* clear */, 1);
@@ -1791,6 +1803,18 @@ void acpi_build_tables_init(AcpiBuildTables *tables)
     tables->linker = bios_linker_loader_init();
 }
 
+/*
+ * 在以下使用acpi_build_tables_cleanup():
+ *   - hw/arm/virt-acpi-build.c|1453| <<virt_acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+ *   - hw/arm/virt-acpi-build.c|1525| <<virt_acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+ *   - hw/i386/acpi-build.c|2589| <<acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+ *   - hw/i386/acpi-build.c|2710| <<acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+ *   - hw/i386/acpi-microvm.c|292| <<acpi_setup_microvm>> acpi_build_tables_cleanup(&tables, false);
+ *   - hw/loongarch/virt-acpi-build.c|658| <<acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+ *   - hw/loongarch/virt-acpi-build.c|730| <<virt_acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+ *   - hw/riscv/virt-acpi-build.c|984| <<virt_acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+ *   - hw/riscv/virt-acpi-build.c|1036| <<virt_acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+ */
 void acpi_build_tables_cleanup(AcpiBuildTables *tables, bool mfre)
 {
     bios_linker_loader_cleanup(tables->linker);
@@ -1824,6 +1848,20 @@ build_rsdp(GArray *tbl, BIOSLinker *linker, AcpiRsdpData *rsdp_data)
         g_assert_not_reached();
     }
 
+    /*
+     * 在以下使用bios_linker_loader_alloc():
+     *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+     *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+     *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+     *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+     *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     */
     bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16,
                              true /* fseg memory */);
 
@@ -1872,6 +1910,10 @@ build_rsdp(GArray *tbl, BIOSLinker *linker, AcpiRsdpData *rsdp_data)
 /*
  * ACPI 1.0 Root System Description Table (RSDT)
  */
+/*
+ * 在以下使用build_rsdt():
+ *   - hw/i386/acpi-build.c|2283| <<acpi_build>> build_rsdt(tables_blob, tables->linker, table_offsets,
+ */
 void
 build_rsdt(GArray *table_data, BIOSLinker *linker, GArray *table_offsets,
            const char *oem_id, const char *oem_table_id)
@@ -2242,11 +2284,89 @@ void build_pptt(GArray *table_data, BIOSLinker *linker, MachineState *ms,
     acpi_table_end(linker, &table);
 }
 
+/*
+ * 创建的时候:
+ * (gdb) bt
+ * #0  build_fadt (tbl=0x555557570670, linker=0x555557575290, f=0x7fffffffd598, oem_id=0x5555577f7390 "BOCHS ", oem_table_id=0x5555577f73b0 "BXPC    ") at ../hw/acpi/aml-build.c:2248
+ * #1  0x0000555555e4ca42 in acpi_build (tables=0x7fffffffd6b0, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1984
+ * #2  0x0000555555e4d625 in acpi_setup () at ../hw/i386/acpi-build.c:2204
+ * #3  0x0000555555e41329 in pc_machine_done (notifier=0x5555577f0e70, data=0x0) at ../hw/i386/pc.c:539
+ * #4  0x00005555561583b5 in notifier_list_notify (list=0x5555574a8cd0 <machine_init_done_notifiers>, data=0x0) at ../util/notify.c:39
+ * #5  0x00005555559bafbd in qdev_machine_creation_done () at ../hw/core/machine.c:1742
+ * #6  0x0000555555c6404c in qemu_machine_creation_done (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2781
+ * #7  0x0000555555c6418b in qmp_x_exit_preconfig (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2809
+ * #8  0x0000555555c66ca3 in qemu_init (argc=26, argv=0x7fffffffda88) at ../system/vl.c:3845
+ * #9  0x000055555608087d in main (argc=26, argv=0x7fffffffda88) at ../system/main.c:71
+ *
+ * (gdb) bt
+ * #0  build_fadt (tbl=0x5555578cc2d0, linker=0x7ffde034b480, f=0x7fffeff7c368, oem_id=0x5555577f7390 "BOCHS ", oem_table_id=0x5555577f73b0 "BXPC    ") at ../hw/acpi/aml-build.c:2248
+ * #1  0x0000555555e4ca42 in acpi_build (tables=0x7fffeff7c470, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1984
+ * #2  0x0000555555e4d486 in acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2148
+ * #3  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #4  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #5  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0, mask=65535, attrs=...) at ../system/memory.c:491
+ * #6  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1, access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, 
+ *     mr=0x555557d33c80, attrs=...) at ../system/memory.c:567
+ * #7  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...) at ../system/memory.c:1547
+ * #8  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0, l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #9  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde0302ac0, addr=1296, attrs=..., ptr=0x7ffff7ff2000, len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #10 0x0000555555c898f3 in flatview_write (fv=0x7ffde0302ac0, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3330
+ * #11 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #12 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #13 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1) at ../accel/kvm/kvm-all.c:3136
+ * #14 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #15 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #16 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #17 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #18 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * 重启的时候和system_reset的时候
+ * (gdb) bt
+ * #0  build_fadt (tbl=0x7ffde039e720, linker=0x7ffde0007d30, f=0x7fffeff7c368, oem_id=0x5555577f7390 "BOCHS ", oem_table_id=0x5555577f73b0 "BXPC    ") at ../hw/acpi/aml-build.c:2248
+ * #1  0x0000555555e4ca42 in acpi_build (tables=0x7fffeff7c470, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1984
+ * #2  0x0000555555e4d486 in acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2148
+ * #3  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #4  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #5  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0, mask=65535, attrs=...) at ../system/memory.c:491
+ * #6  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1, access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>,
+ *     mr=0x555557d33c80, attrs=...) at ../system/memory.c:567
+ * #7  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...) at ../system/memory.c:1547
+ * #8  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0, l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #9  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde034eb80, addr=1296, attrs=..., ptr=0x7ffff7ff2000, len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #10 0x0000555555c898f3 in flatview_write (fv=0x7ffde034eb80, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3330
+ * #11 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #12 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #13 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1) at ../accel/kvm/kvm-all.c:3136
+ * #14 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #15 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #16 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #17 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #18 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ *
+ * 在以下使用build_fadt():
+ *   - hw/arm/virt-acpi-build.c|1119| <<build_fadt_rev6>> build_fadt(table_data, linker, &fadt, vms->oem_id, vms->oem_table_id);
+ *   - hw/i386/acpi-build.c|1984| <<acpi_build>> build_fadt(tables_blob, tables->linker, &pm.fadt, oem_id, oem_table_id);
+ *   - hw/i386/acpi-microvm.c|201| <<acpi_build_microvm>> build_fadt(tables_blob, tables->linker, &pmfadt, x86ms->oem_id,
+ *   - hw/loongarch/virt-acpi-build.c|534| <<acpi_build>> build_fadt(tables_blob, tables->linker, &fadt_data,
+ *   - hw/riscv/virt-acpi-build.c|437| <<build_fadt_rev6>> build_fadt(table_data, linker, &fadt, s->oem_id, s->oem_table_id);
+ */
 /* build rev1/rev3/rev5.1/rev6.0 FADT */
 void build_fadt(GArray *tbl, BIOSLinker *linker, const AcpiFadtData *f,
                 const char *oem_id, const char *oem_table_id)
 {
     int off;
+    /*
+     * typedef struct AcpiTable {
+     *     const char *sig;
+     *     const uint8_t rev;
+     *     const char *oem_id;
+     *     const char *oem_table_id;
+     *     // private vars tracking table state
+     *     GArray *array;
+     *     unsigned table_offset;
+     * } AcpiTable;
+     */
     AcpiTable table = { .sig = "FACP", .rev = f->rev,
                         .oem_id = oem_id, .oem_table_id = oem_table_id };
 
@@ -2281,6 +2401,17 @@ void build_fadt(GArray *tbl, BIOSLinker *linker, const AcpiFadtData *f,
     build_append_int_noprefix(tbl, 0 /* not supported */, 1); /* S4BIOS_REQ */
     /* ACPI1.0: Reserved, ACPI2.0+: PSTATE_CNT */
     build_append_int_noprefix(tbl, 0, 1);
+    /*
+     * 在以下使用AcpiFadtData->pm1a_evt:
+     *   - hw/acpi/aml-build.c|2367| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.address, 4);
+     *   - hw/acpi/aml-build.c|2376| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.bit_width / 8, 1);
+     *   - hw/acpi/aml-build.c|2430| <<build_fadt>> build_append_gas_from_struct(tbl, &f->pm1a_evt);
+     *   - hw/i386/acpi-build.c|177| <<init_common_fadt_data>> .pm1a_evt = { .space_id = as, .bit_width = 4 * 8, .address = io },
+     *
+     * const AcpiFadtData *f:
+     * -> struct AcpiGenericAddress pm1a_cnt;   // PM1a_CNT_BLK
+     * -> struct AcpiGenericAddress pm1a_evt;   // PM1a_EVT_BLK
+     */
     build_append_int_noprefix(tbl, f->pm1a_evt.address, 4); /* PM1a_EVT_BLK */
     build_append_int_noprefix(tbl, 0, 4); /* PM1b_EVT_BLK */
     build_append_int_noprefix(tbl, f->pm1a_cnt.address, 4); /* PM1a_CNT_BLK */
@@ -2344,6 +2475,13 @@ void build_fadt(GArray *tbl, BIOSLinker *linker, const AcpiFadtData *f,
             ACPI_BUILD_TABLE_FILE, *f->xdsdt_tbl_offset);
     }
 
+    /*
+     * 在以下使用AcpiFadtData->pm1a_evt:
+     *   - hw/acpi/aml-build.c|2367| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.address, 4);
+     *   - hw/acpi/aml-build.c|2376| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.bit_width / 8, 1);
+     *   - hw/acpi/aml-build.c|2430| <<build_fadt>> build_append_gas_from_struct(tbl, &f->pm1a_evt);
+     *   - hw/i386/acpi-build.c|177| <<init_common_fadt_data>> .pm1a_evt = { .space_id = as, .bit_width = 4 * 8, .address = io },
+     */
     build_append_gas_from_struct(tbl, &f->pm1a_evt); /* X_PM1a_EVT_BLK */
     /* X_PM1b_EVT_BLK */
     build_append_gas(tbl, AML_AS_SYSTEM_MEMORY, 0 , 0, 0, 0);
@@ -2385,6 +2523,12 @@ done:
  * table 7: TCG Hardware Interface Description Table Format for TPM 2.0
  * of TCG ACPI Specification, Family “1.2” and “2.0”, Version 1.2, Rev 8
  */
+/*
+ * 在以下使用build_tpm2():
+ *   - hw/arm/virt-acpi-build.c|1363| <<virt_acpi_build>> build_tpm2(tables_blob, tables->linker, tables->tcpalog, vms->oem_id,
+ *   - hw/i386/acpi-build.c|2221| <<acpi_build>> build_tpm2(tables_blob, tables->linker, tables->tcpalog,
+ *   - hw/loongarch/virt-acpi-build.c|578| <<acpi_build>> build_tpm2(tables_blob, tables->linker,
+ */
 void build_tpm2(GArray *table_data, BIOSLinker *linker, GArray *tcpalog,
                 const char *oem_id, const char *oem_table_id)
 {
@@ -2424,6 +2568,20 @@ void build_tpm2(GArray *table_data, BIOSLinker *linker, GArray *tcpalog,
     build_append_int_noprefix(table_data, TPM_LOG_AREA_MINIMUM_SIZE, 4);
 
     acpi_data_push(tcpalog, TPM_LOG_AREA_MINIMUM_SIZE);
+    /*
+     * 在以下使用bios_linker_loader_alloc():
+     *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+     *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+     *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+     *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+     *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     */
     bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1,
                              false);
 
diff --git a/hw/acpi/bios-linker-loader.c b/hw/acpi/bios-linker-loader.c
index c9ffe449a..225bdbf02 100644
--- a/hw/acpi/bios-linker-loader.c
+++ b/hw/acpi/bios-linker-loader.c
@@ -124,8 +124,18 @@ typedef struct BiosLinkerFileEntry {
  * After initialization, linker commands can be added, and will
  * be stored in the linker.cmd_blob array.
  */
+/*
+ * 在以下使用bios_linker_loader_init():
+ *   - hw/acpi/aml-build.c|1803| <<acpi_build_tables_init>> tables->linker = bios_linker_loader_init();
+ */
 BIOSLinker *bios_linker_loader_init(void)
 {
+    /*
+     * typedef struct BIOSLinker {
+     *     GArray *cmd_blob;
+     *     GArray *file_list;
+     * } BIOSLinker;
+     */
     BIOSLinker *linker = g_new(BIOSLinker, 1);
 
     linker->cmd_blob = g_array_new(false, true /* clear */, 1);
@@ -134,6 +144,10 @@ BIOSLinker *bios_linker_loader_init(void)
     return linker;
 }
 
+/*
+ * 在以下使用bios_linker_loader_cleanup():
+ *   - hw/acpi/aml-build.c|1808| <<acpi_build_tables_cleanup>> bios_linker_loader_cleanup(tables->linker);
+ */
 /* Free linker wrapper */
 void bios_linker_loader_cleanup(BIOSLinker *linker)
 {
@@ -150,6 +164,14 @@ void bios_linker_loader_cleanup(BIOSLinker *linker)
     g_free(linker);
 }
 
+/*
+ * 在以下使用bios_linker_find_file():
+ *   - hw/acpi/bios-linker-loader.c|214| <<bios_linker_loader_alloc>> assert(!bios_linker_find_file(linker, file_name));
+ *   - hw/acpi/bios-linker-loader.c|248| <<bios_linker_loader_add_checksum>> const BiosLinkerFileEntry *file = bios_linker_find_file(linker, file_name);
+ *   - hw/acpi/bios-linker-loader.c|293| <<bios_linker_loader_add_pointer>> bios_linker_find_file(linker, dest_file);
+ *   - hw/acpi/bios-linker-loader.c|295| <<bios_linker_loader_add_pointer>> bios_linker_find_file(linker, src_file);
+ *   - hw/acpi/bios-linker-loader.c|346| <<bios_linker_loader_write_pointer>> bios_linker_find_file(linker, src_file);
+ */
 static const BiosLinkerFileEntry *
 bios_linker_find_file(const BIOSLinker *linker, const char *name)
 {
@@ -186,6 +208,25 @@ bool bios_linker_loader_can_write_pointer(void)
  *
  * Note: this command must precede any other linker command using this file.
  */
+/*
+ * 在以下使用bios_linker_loader_alloc():
+ *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+ *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+ *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+ *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+ *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+ *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+ *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+ *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+ *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+ *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+ *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+ *
+ * typedef struct BIOSLinker {
+ *     GArray *cmd_blob;
+ *     GArray *file_list;
+ * } BIOSLinker;
+ */
 void bios_linker_loader_alloc(BIOSLinker *linker,
                               const char *file_name,
                               GArray *file_blob,
@@ -266,6 +307,23 @@ void bios_linker_loader_add_checksum(BIOSLinker *linker, const char *file_name,
  *              @dest_file+@dst_patched_offset will point to after
  *              firmware's executed ADD_POINTER command
  */
+/*
+ * 在以下使用bios_linker_loader_add_pointer():
+ *   - hw/acpi/aml-build.c|1863| <<build_rsdp>> bios_linker_loader_add_pointer(linker, ACPI_BUILD_RSDP_FILE,
+ *   - hw/acpi/aml-build.c|1884| <<build_rsdp>> bios_linker_loader_add_pointer(linker, ACPI_BUILD_RSDP_FILE,
+ *   - hw/acpi/aml-build.c|1922| <<build_rsdt>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/aml-build.c|1951| <<build_xsdt>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/aml-build.c|2367| <<build_fadt>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/aml-build.c|2376| <<build_fadt>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/aml-build.c|2461| <<build_fadt>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/aml-build.c|2580| <<build_tpm2>> bios_linker_loader_add_pointer(linker, ACPI_BUILD_TABLE_FILE,
+ *   - hw/acpi/ghes.c|278| <<build_ghes_error_table>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/ghes.c|333| <<build_ghes_v2_entry>> bios_linker_loader_add_pointer(linker, ACPI_BUILD_TABLE_FILE,
+ *   - hw/acpi/ghes.c|353| <<build_ghes_v2_entry>> bios_linker_loader_add_pointer(linker, ACPI_BUILD_TABLE_FILE,
+ *   - hw/acpi/nvdimm.c|1419| <<nvdimm_build_ssdt>> bios_linker_loader_add_pointer(linker,
+ *   - hw/acpi/vmgenid.c|115| <<vmgenid_build_acpi>> bios_linker_loader_add_pointer(linker,
+ *   - hw/i386/acpi-build.c|1486| <<build_tpm_tcpa>> bios_linker_loader_add_pointer(linker, ACPI_BUILD_TABLE_FILE,
+ */
 void bios_linker_loader_add_pointer(BIOSLinker *linker,
                                     const char *dest_file,
                                     uint32_t dst_patched_offset,
diff --git a/hw/acpi/core.c b/hw/acpi/core.c
index a6a62a742..d22d35f7c 100644
--- a/hw/acpi/core.c
+++ b/hw/acpi/core.c
@@ -560,6 +560,13 @@ void acpi_pm_tmr_reset(ACPIREGS *ar)
     timer_del(ar->tmr.timer);
 }
 
+/*
+ * 在以下使用acpi_pm1_cnt_update():
+ *   - hw/acpi/piix4.c|75| <<apm_ctrl_changed>> acpi_pm1_cnt_update(&s->ar,
+ *            val == ACPI_ENABLE, val == ACPI_DISABLE);
+ *   - hw/isa/lpc_ich9.c|468| <<ich9_apm_ctrl_changed>> acpi_pm1_cnt_update(&lpc->pm.acpi_regs,
+ *            val == ICH9_APM_ACPI_ENABLE, val == ICH9_APM_ACPI_DISABLE);
+ */
 /* ACPI PM1aCNT */
 void acpi_pm1_cnt_update(ACPIREGS *ar,
                          bool sci_enable, bool sci_disable)
diff --git a/hw/acpi/erst.c b/hw/acpi/erst.c
index b6c1942e3..4077308f5 100644
--- a/hw/acpi/erst.c
+++ b/hw/acpi/erst.c
@@ -225,6 +225,11 @@ static void build_serialization_instruction(
     build_append_int_noprefix(e->table_data, mask, 8);
 }
 
+/*
+ * 在以下使用build_erst():
+ *   - hw/i386/acpi-build.c|2188| <<acpi_build>> build_erst(tables_blob, tables->linker, erst_dev,
+ *   - hw/i386/acpi-microvm.c|214| <<acpi_build_microvm>> build_erst(tables_blob, tables->linker, erst_dev,
+ */
 /* ACPI 4.0: 17.4.1 Serialization Action Table */
 void build_erst(GArray *table_data, BIOSLinker *linker, Object *erst_dev,
     const char *oem_id, const char *oem_table_id)
diff --git a/hw/acpi/generic_event_device.c b/hw/acpi/generic_event_device.c
index 9e9416d40..aa47b4c50 100644
--- a/hw/acpi/generic_event_device.c
+++ b/hw/acpi/generic_event_device.c
@@ -289,6 +289,15 @@ static void acpi_ged_unplug_request_cb(HotplugHandler *hotplug_dev,
     } else if (object_dynamic_cast(OBJECT(dev), TYPE_CPU)) {
         acpi_cpu_unplug_request_cb(hotplug_dev, &s->cpuhp_state, dev, errp);
     } else if (object_dynamic_cast(OBJECT(dev), TYPE_PCI_DEVICE)) {
+        /*
+	 * 在以下使用acpi_pcihp_device_unplug_request_cb():
+	 *   - hw/acpi/generic_event_device.c|292| <<acpi_ged_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &s->pcihp_state, dev, errp);
+         *   - hw/acpi/ich9.c|500| <<ich9_pm_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &lpc->pm.acpi_pci_hotplug, dev, errp);
+         *   - hw/acpi/piix4.c|504| <<piix4_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &s->acpi_pci_hotplug, dev, errp);
+	 */
         acpi_pcihp_device_unplug_request_cb(hotplug_dev, &s->pcihp_state,
                                             dev, errp);
     } else {
diff --git a/hw/acpi/ich9.c b/hw/acpi/ich9.c
index 5c7dfb2c6..6c871acbf 100644
--- a/hw/acpi/ich9.c
+++ b/hw/acpi/ich9.c
@@ -472,6 +472,10 @@ void ich9_pm_device_plug_cb(HotplugHandler *hotplug_dev, DeviceState *dev,
     }
 }
 
+/*
+ * 在以下使用ich9_pm_device_unplug_request_cb():
+ *   - hw/isa/lpc_ich9.c|915| <<ich9_lpc_class_init>> hc->unplug_request = ich9_pm_device_unplug_request_cb;
+ */
 void ich9_pm_device_unplug_request_cb(HotplugHandler *hotplug_dev,
                                       DeviceState *dev, Error **errp)
 {
@@ -497,6 +501,15 @@ void ich9_pm_device_unplug_request_cb(HotplugHandler *hotplug_dev,
         acpi_cpu_unplug_request_cb(hotplug_dev, &lpc->pm.cpuhp_state,
                                    dev, errp);
     } else if (object_dynamic_cast(OBJECT(dev), TYPE_PCI_DEVICE)) {
+        /*
+	 * 在以下使用acpi_pcihp_device_unplug_request_cb():
+         *   - hw/acpi/generic_event_device.c|292| <<acpi_ged_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &s->pcihp_state, dev, errp);
+         *   - hw/acpi/ich9.c|500| <<ich9_pm_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &lpc->pm.acpi_pci_hotplug, dev, errp);
+         *   - hw/acpi/piix4.c|504| <<piix4_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &s->acpi_pci_hotplug, dev, errp);
+	 */
         acpi_pcihp_device_unplug_request_cb(hotplug_dev,
                                             &lpc->pm.acpi_pci_hotplug,
                                             dev, errp);
diff --git a/hw/acpi/pci.c b/hw/acpi/pci.c
index 8c7ed1047..23b4c19bf 100644
--- a/hw/acpi/pci.c
+++ b/hw/acpi/pci.c
@@ -302,6 +302,12 @@ void build_srat_generic_affinity_structures(GArray *table_data)
                                    table_data);
 }
 
+/*
+ * 在以下使用build_pci_host_bridge_osc_method():
+ *   - hw/i386/acpi-build.c|994| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(!pm->pcihp_bridge_en));
+ *   - hw/i386/acpi-build.c|1114| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(true));
+ *   - hw/pci-host/gpex-acpi.c|99| <<acpi_dsdt_add_host_bridge_methods>> build_pci_host_bridge_osc_method(enable_native_pcie_hotplug));
+ */
 Aml *build_pci_host_bridge_osc_method(bool enable_native_pcie_hotplug)
 {
     Aml *if_ctx;
diff --git a/hw/acpi/pcihp.c b/hw/acpi/pcihp.c
index 87162ff2c..1ba79a0e3 100644
--- a/hw/acpi/pcihp.c
+++ b/hw/acpi/pcihp.c
@@ -175,6 +175,11 @@ static bool acpi_pcihp_pc_no_hotplug(AcpiPciHpState *s, PCIDevice *dev)
            pci_is_vf(dev);
 }
 
+/*
+ * 在以下使用acpi_pcihp_eject_slot():
+ *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+ *   - hw/acpi/pcihp.c|481| <<pci_write(PCI_EJ_BASE)>> acpi_pcihp_eject_slot(s, s->hotplug_select, data);
+ */
 static void acpi_pcihp_eject_slot(AcpiPciHpState *s, unsigned bsel, unsigned slots)
 {
     HotplugHandler *hotplug_ctrl;
@@ -188,6 +193,22 @@ static void acpi_pcihp_eject_slot(AcpiPciHpState *s, unsigned bsel, unsigned slo
         return;
     }
 
+    /*
+     * 在以下使用AcpiPciHpPciStatus->acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS]:
+     *   - hw/acpi/pcihp.c|192| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|193| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|232| <<acpi_pcihp_update_hotplug_bus>> while (s->acpi_pcihp_pci_status[bsel].down) {
+     *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+     *   - hw/acpi/pcihp.c|236| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable = ~0;
+     *   - hw/acpi/pcihp.c|247| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|319| <<acpi_pcihp_device_plug_cb>> s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
+     *   - hw/acpi/pcihp.c|375| <<acpi_pcihp_device_unplug_request_cb>> s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
+     *   - hw/acpi/pcihp.c|409| <<pci_read(PCI_UP_BASE)>> val = s->acpi_pcihp_pci_status[bsel].up;
+     *   - hw/acpi/pcihp.c|411| <<pci_read(PCI_UP_BASE)>> s->acpi_pcihp_pci_status[bsel].up = 0;
+     *   - hw/acpi/pcihp.c|416| <<pci_read(PCI_DOWN_BASE)>> val = s->acpi_pcihp_pci_status[bsel].down;
+     *   - hw/acpi/pcihp.c|423| <<pci_read(PCI_RMV_BASE)>> val = s->acpi_pcihp_pci_status[bsel].hotplug_enable;
+     *   - include/hw/acpi/pcihp.h|97| <<VMSTATE_PCI_HOTPLUG>> VMSTATE_STRUCT_ARRAY_TEST(pcihp.acpi_pcihp_pci_status, state, \
+     */
     /* Mark request as complete */
     s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
     s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
@@ -228,8 +249,29 @@ static void acpi_pcihp_update_hotplug_bus(AcpiPciHpState *s, int bsel)
     BusChild *kid, *next;
     PCIBus *bus = acpi_pcihp_find_hotplug_bus(s, bsel);
 
+    /*
+     * 在以下使用AcpiPciHpPciStatus->acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS]:
+     *   - hw/acpi/pcihp.c|192| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|193| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|232| <<acpi_pcihp_update_hotplug_bus>> while (s->acpi_pcihp_pci_status[bsel].down) {
+     *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+     *   - hw/acpi/pcihp.c|236| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable = ~0;
+     *   - hw/acpi/pcihp.c|247| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|319| <<acpi_pcihp_device_plug_cb>> s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
+     *   - hw/acpi/pcihp.c|375| <<acpi_pcihp_device_unplug_request_cb>> s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
+     *   - hw/acpi/pcihp.c|409| <<pci_read(PCI_UP_BASE)>> val = s->acpi_pcihp_pci_status[bsel].up;
+     *   - hw/acpi/pcihp.c|411| <<pci_read(PCI_UP_BASE)>> s->acpi_pcihp_pci_status[bsel].up = 0;
+     *   - hw/acpi/pcihp.c|416| <<pci_read(PCI_DOWN_BASE)>> val = s->acpi_pcihp_pci_status[bsel].down;
+     *   - hw/acpi/pcihp.c|423| <<pci_read(PCI_RMV_BASE)>> val = s->acpi_pcihp_pci_status[bsel].hotplug_enable;
+     *   - include/hw/acpi/pcihp.h|97| <<VMSTATE_PCI_HOTPLUG>> VMSTATE_STRUCT_ARRAY_TEST(pcihp.acpi_pcihp_pci_status, state, \
+     */
     /* Execute any pending removes during reset */
     while (s->acpi_pcihp_pci_status[bsel].down) {
+        /*
+	 * 在以下使用acpi_pcihp_eject_slot():
+         *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+         *   - hw/acpi/pcihp.c|481| <<pci_write(PCI_EJ_BASE)>> acpi_pcihp_eject_slot(s, s->hotplug_select, data);
+	 */
         acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
     }
 
@@ -316,6 +358,22 @@ void acpi_pcihp_device_plug_cb(HotplugHandler *hotplug_dev, AcpiPciHpState *s,
 
     bsel = acpi_pcihp_get_bsel(bus);
     g_assert(bsel >= 0);
+    /*
+     * 在以下使用AcpiPciHpPciStatus->acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS]:
+     *   - hw/acpi/pcihp.c|192| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|193| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|232| <<acpi_pcihp_update_hotplug_bus>> while (s->acpi_pcihp_pci_status[bsel].down) {
+     *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+     *   - hw/acpi/pcihp.c|236| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable = ~0;
+     *   - hw/acpi/pcihp.c|247| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|319| <<acpi_pcihp_device_plug_cb>> s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
+     *   - hw/acpi/pcihp.c|375| <<acpi_pcihp_device_unplug_request_cb>> s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
+     *   - hw/acpi/pcihp.c|409| <<pci_read(PCI_UP_BASE)>> val = s->acpi_pcihp_pci_status[bsel].up;
+     *   - hw/acpi/pcihp.c|411| <<pci_read(PCI_UP_BASE)>> s->acpi_pcihp_pci_status[bsel].up = 0;
+     *   - hw/acpi/pcihp.c|416| <<pci_read(PCI_DOWN_BASE)>> val = s->acpi_pcihp_pci_status[bsel].down;
+     *   - hw/acpi/pcihp.c|423| <<pci_read(PCI_RMV_BASE)>> val = s->acpi_pcihp_pci_status[bsel].hotplug_enable;
+     *   - include/hw/acpi/pcihp.h|97| <<VMSTATE_PCI_HOTPLUG>> VMSTATE_STRUCT_ARRAY_TEST(pcihp.acpi_pcihp_pci_status, state, \
+     */
     s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
     acpi_send_event(DEVICE(hotplug_dev), ACPI_PCI_HOTPLUG_STATUS);
 }
@@ -331,6 +389,15 @@ void acpi_pcihp_device_unplug_cb(HotplugHandler *hotplug_dev, AcpiPciHpState *s,
     qdev_unrealize(dev);
 }
 
+/*
+ * 在以下使用acpi_pcihp_device_unplug_request_cb():
+ *   - hw/acpi/generic_event_device.c|292| <<acpi_ged_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+ *                                                 &s->pcihp_state, dev, errp);
+ *   - hw/acpi/ich9.c|500| <<ich9_pm_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+ *                                                 &lpc->pm.acpi_pci_hotplug, dev, errp);
+ *   - hw/acpi/piix4.c|504| <<piix4_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+ *                                                 &s->acpi_pci_hotplug, dev, errp);
+ */
 void acpi_pcihp_device_unplug_request_cb(HotplugHandler *hotplug_dev,
                                          AcpiPciHpState *s, DeviceState *dev,
                                          Error **errp)
@@ -363,6 +430,22 @@ void acpi_pcihp_device_unplug_request_cb(HotplugHandler *hotplug_dev,
     pdev->qdev.pending_deleted_expires_ms =
         qemu_clock_get_ms(QEMU_CLOCK_VIRTUAL); /* 1 msec */
 
+    /*
+     * 在以下使用AcpiPciHpPciStatus->acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS]:
+     *   - hw/acpi/pcihp.c|192| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|193| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|232| <<acpi_pcihp_update_hotplug_bus>> while (s->acpi_pcihp_pci_status[bsel].down) {
+     *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+     *   - hw/acpi/pcihp.c|236| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable = ~0;
+     *   - hw/acpi/pcihp.c|247| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|319| <<acpi_pcihp_device_plug_cb>> s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
+     *   - hw/acpi/pcihp.c|375| <<acpi_pcihp_device_unplug_request_cb>> s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
+     *   - hw/acpi/pcihp.c|409| <<pci_read(PCI_UP_BASE)>> val = s->acpi_pcihp_pci_status[bsel].up;
+     *   - hw/acpi/pcihp.c|411| <<pci_read(PCI_UP_BASE)>> s->acpi_pcihp_pci_status[bsel].up = 0;
+     *   - hw/acpi/pcihp.c|416| <<pci_read(PCI_DOWN_BASE)>> val = s->acpi_pcihp_pci_status[bsel].down;
+     *   - hw/acpi/pcihp.c|423| <<pci_read(PCI_RMV_BASE)>> val = s->acpi_pcihp_pci_status[bsel].hotplug_enable;
+     *   - include/hw/acpi/pcihp.h|97| <<VMSTATE_PCI_HOTPLUG>> VMSTATE_STRUCT_ARRAY_TEST(pcihp.acpi_pcihp_pci_status, state, \
+     */
     s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
     acpi_send_event(DEVICE(hotplug_dev), ACPI_PCI_HOTPLUG_STATUS);
 }
@@ -395,6 +478,22 @@ static uint64_t pci_read(void *opaque, hwaddr addr, unsigned int size)
         return 0;
     }
 
+    /*
+     * 在以下使用AcpiPciHpPciStatus->acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS]:
+     *   - hw/acpi/pcihp.c|192| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|193| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|232| <<acpi_pcihp_update_hotplug_bus>> while (s->acpi_pcihp_pci_status[bsel].down) {
+     *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+     *   - hw/acpi/pcihp.c|236| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable = ~0;
+     *   - hw/acpi/pcihp.c|247| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|319| <<acpi_pcihp_device_plug_cb>> s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
+     *   - hw/acpi/pcihp.c|375| <<acpi_pcihp_device_unplug_request_cb>> s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
+     *   - hw/acpi/pcihp.c|409| <<pci_read(PCI_UP_BASE)>> val = s->acpi_pcihp_pci_status[bsel].up;
+     *   - hw/acpi/pcihp.c|411| <<pci_read(PCI_UP_BASE)>> s->acpi_pcihp_pci_status[bsel].up = 0;
+     *   - hw/acpi/pcihp.c|416| <<pci_read(PCI_DOWN_BASE)>> val = s->acpi_pcihp_pci_status[bsel].down;
+     *   - hw/acpi/pcihp.c|423| <<pci_read(PCI_RMV_BASE)>> val = s->acpi_pcihp_pci_status[bsel].hotplug_enable;
+     *   - include/hw/acpi/pcihp.h|97| <<VMSTATE_PCI_HOTPLUG>> VMSTATE_STRUCT_ARRAY_TEST(pcihp.acpi_pcihp_pci_status, state, \
+     */
     switch (addr) {
     case PCI_UP_BASE:
         val = s->acpi_pcihp_pci_status[bsel].up;
@@ -469,6 +568,11 @@ static void pci_write(void *opaque, hwaddr addr, uint64_t data,
         if (s->hotplug_select >= ACPI_PCIHP_MAX_HOTPLUG_BUS) {
             break;
         }
+        /*
+	 * 在以下使用acpi_pcihp_eject_slot():
+         *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+         *   - hw/acpi/pcihp.c|481| <<pci_write(PCI_EJ_BASE)>> acpi_pcihp_eject_slot(s, s->hotplug_select, data);
+	 */
         acpi_pcihp_eject_slot(s, s->hotplug_select, data);
         trace_acpi_pci_ej_write(addr, data);
         break;
diff --git a/hw/acpi/piix4.c b/hw/acpi/piix4.c
index 9b7f50c7a..3e70bc05f 100644
--- a/hw/acpi/piix4.c
+++ b/hw/acpi/piix4.c
@@ -66,11 +66,22 @@ static void pm_tmr_timer(ACPIREGS *ar)
     acpi_update_sci(&s->ar, s->irq);
 }
 
+/*
+ * 在以下使用apm_ctrl_changed():
+ *   - hw/acpi/piix4.c|442| <<piix4_pm_realize>> apm_init(dev, &s->apm, apm_ctrl_changed, s);
+ */
 static void apm_ctrl_changed(uint32_t val, void *arg)
 {
     PIIX4PMState *s = arg;
     PCIDevice *d = PCI_DEVICE(s);
 
+    /*
+     * 在以下使用acpi_pm1_cnt_update():
+     *   - hw/acpi/piix4.c|75| <<apm_ctrl_changed>> acpi_pm1_cnt_update(&s->ar,
+     *            val == ACPI_ENABLE, val == ACPI_DISABLE);
+     *   - hw/isa/lpc_ich9.c|468| <<ich9_apm_ctrl_changed>> acpi_pm1_cnt_update(&lpc->pm.acpi_regs,
+     *            val == ICH9_APM_ACPI_ENABLE, val == ICH9_APM_ACPI_DISABLE);
+     */
     /* ACPI specs 3.0, 4.7.2.5 */
     acpi_pm1_cnt_update(&s->ar, val == ACPI_ENABLE, val == ACPI_DISABLE);
     if (val == ACPI_ENABLE || val == ACPI_DISABLE) {
@@ -84,10 +95,142 @@ static void apm_ctrl_changed(uint32_t val, void *arg)
     }
 }
 
+/*
+ * (gdb) bt
+ * #0  pm_io_space_update (s=0x555557d7c720) at ../hw/acpi/piix4.c:89
+ * #1  0x000055555595f922 in piix4_pm_reset (dev=0x555557d7c720) at ../hw/acpi/piix4.c:291
+ * #2  0x0000555555f2dc0e in do_legacy_reset (obj=0x555557d7c720, type=RESET_TYPE_COLD)
+ *     at ../hw/core/qdev.c:805
+ * #3  0x0000555555f2ed9a in resettable_phase_hold (obj=0x555557d7c720, opaque=0x0, type=RESET_TYPE_COLD)
+ *     at ../hw/core/resettable.c:162
+ * #4  0x0000555555f276a9 in bus_reset_child_foreach (obj=0x5555578ff7f0, cb=0x555555f2ec82 <resettable_phase_hold>,
+ *     opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/bus.c:97
+ * #5  0x0000555555f2eae9 in resettable_child_foreach (rc=0x555557669e40, obj=0x5555578ff7f0,
+ *     cb=0x555555f2ec82 <resettable_phase_hold>, opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/resettable.c:92
+ * #6  0x0000555555f2ed3f in resettable_phase_hold (obj=0x5555578ff7f0, opaque=0x0, type=RESET_TYPE_COLD)
+ *     at ../hw/core/resettable.c:155
+ * #7  0x0000555555f2c712 in device_reset_child_foreach (obj=0x5555578c8950, cb=0x555555f2ec82 <resettable_phase_hold>,
+ *     opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/qdev.c:260
+ * #8  0x0000555555f2eae9 in resettable_child_foreach (rc=0x55555761e6c0, obj=0x5555578c8950,
+ *     cb=0x555555f2ec82 <resettable_phase_hold>, opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/resettable.c:92
+ * #9  0x0000555555f2ed3f in resettable_phase_hold (obj=0x5555578c8950, opaque=0x0, type=RESET_TYPE_COLD)
+ *     at ../hw/core/resettable.c:155
+ * #10 0x0000555555f276a9 in bus_reset_child_foreach (obj=0x5555577fa670, cb=0x555555f2ec82 <resettable_phase_hold>,
+ *     opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/bus.c:97
+ * #11 0x0000555555f2eae9 in resettable_child_foreach (rc=0x555557737650, obj=0x5555577fa670,
+ *     cb=0x555555f2ec82 <resettable_phase_hold>, opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/resettable.c:92
+ * #12 0x0000555555f2ed3f in resettable_phase_hold (obj=0x5555577fa670, opaque=0x0, type=RESET_TYPE_COLD)
+ *     at ../hw/core/resettable.c:155
+ * #13 0x0000555555f2e0ee in resettable_container_child_foreach (obj=0x555557824930,
+ *     cb=0x555555f2ec82 <resettable_phase_hold>, opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/resetcontainer.c:54
+ * #14 0x0000555555f2eae9 in resettable_child_foreach (rc=0x5555577c3350, obj=0x555557824930,
+ *     cb=0x555555f2ec82 <resettable_phase_hold>, opaque=0x0, type=RESET_TYPE_COLD) at ../hw/core/resettable.c:92
+ * #15 0x0000555555f2ed3f in resettable_phase_hold (obj=0x555557824930, opaque=0x0, type=RESET_TYPE_COLD)
+ *     at ../hw/core/resettable.c:155
+ * #16 0x0000555555f2e99e in resettable_assert_reset (obj=0x555557824930, type=RESET_TYPE_COLD)
+ *     at ../hw/core/resettable.c:58
+ * #17 0x0000555555f2e8f6 in resettable_reset (obj=0x555557824930, type=RESET_TYPE_COLD) at ../hw/core/resettable.c:45
+ * #18 0x00005555559c2590 in qemu_devices_reset (type=RESET_TYPE_COLD) at ../hw/core/reset.c:176
+ * #19 0x0000555555e44313 in pc_machine_reset (machine=0x5555577f0a20, type=RESET_TYPE_COLD) at ../hw/i386/pc.c:1615
+ * #20 0x0000555555c96ab2 in qemu_system_reset (reason=SHUTDOWN_CAUSE_NONE) at ../system/runstate.c:554
+ * #21 0x00005555559baff8 in qdev_machine_creation_done () at ../hw/core/machine.c:1760
+ * #22 0x0000555555c6404c in qemu_machine_creation_done (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2781
+ * #23 0x0000555555c6418b in qmp_x_exit_preconfig (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2809
+ * #24 0x0000555555c66ca3 in qemu_init (argc=26, argv=0x7fffffffdb18) at ../system/vl.c:3845
+ * #25 0x000055555608087d in main (argc=26, argv=0x7fffffffdb18) at ../system/main.c:71
+ *
+ * (gdb) bt
+ * #0  pm_io_space_update (s=0x555557d7c720) at ../hw/acpi/piix4.c:89
+ * #1  0x000055555595f68b in pm_write_config (d=0x555557d7c720, address=64, val=45057, len=4) at ../hw/acpi/piix4.c:119
+ * #2  0x0000555555b127da in pci_host_config_write_common (pci_dev=0x555557d7c720, addr=64, limit=256, val=45057, len=4)
+ *     at ../hw/pci/pci_host.c:96
+ * #3  0x0000555555b12a25 in pci_data_write (s=0x5555578ff7f0, addr=2147486528, val=45057, len=4)
+ *     at ../hw/pci/pci_host.c:138
+ * #4  x0000555555b12bea in pci_host_data_write (opaque=0x5555578c8950, addr=0, val=45057, len=4)
+ *     at ../hw/pci/pci_host.c:188
+ * #5  0x0000555555c73beb in memory_region_write_accessor (mr=0x5555578c8d90, addr=0, value=0x7fffeff7c638,
+ *     size=4, shift=0, mask=4294967295, attrs=...) at ../system/memory.c:491
+ * #6  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=4, access_size_min=1,
+ *     access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, mr=0x5555578c8d90, attrs=...)
+ *     at ../system/memory.c:567
+ * #7  0x0000555555c77700 in memory_region_dispatch_write (mr=0x5555578c8d90, addr=0, data=45057, op=MO_32, attrs=...)
+ *     at ../system/memory.c:1547
+ * #8  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 "\001\260", len=4, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x5555578c8d90) at ../system/physmem.c:3269
+ * #9  0x0000555555c897c6 in flatview_write_continue (fv=0x5555583a91f0, addr=3324, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=4, mr_addr=0, l=4, mr=0x5555578c8d90) at ../system/physmem.c:3299
+ * #10 0x0000555555c898f3 in flatview_write (fv=0x5555583a91f0, addr=3324, attrs=..., buf=0x7ffff7ff2000, len=4)
+ *     at ../system/physmem.c:3330
+ * #11 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=3324, attrs=...,
+ *     buf=0x7ffff7ff2000, len=4) at ../system/physmem.c:3450
+ * #12 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=3324, attrs=...,
+ *     buf=0x7ffff7ff2000, len=4, is_write=true) at ../system/physmem.c:3460
+ * #13 0x0000555555ef7b0e in kvm_handle_io (port=3324, attrs=..., data=0x7ffff7ff2000, direction=1, size=4, count=1)
+*      at ../accel/kvm/kvm-all.c:3136
+ * #14 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #15 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #16 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #17 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #18 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * (gdb) bt
+ * #0  pm_io_space_update (s=0x555557d7c720) at ../hw/acpi/piix4.c:89
+ * #1  0x000055555595f68b in pm_write_config (d=0x555557d7c720, address=128, val=1, len=1) at ../hw/acpi/piix4.c:119
+ * #2  0x0000555555b127da in pci_host_config_write_common (pci_dev=0x555557d7c720, addr=128, limit=256, val=1, len=1)
+ * at ../hw/pci/pci_host.c:96
+ * #3  0x0000555555b12a25 in pci_data_write (s=0x5555578ff7f0, addr=2147486592, val=1, len=1) at ../hw/pci/pci_host.c:138
+ * #4  0x0000555555b12bea in pci_host_data_write (opaque=0x5555578c8950, addr=0, val=1, len=1) at ../hw/pci/pci_host.c:188
+ * #5  0x0000555555c73beb in memory_region_write_accessor (mr=0x5555578c8d90, addr=0, value=0x7fffeff7c638, size=1, shift=0,
+ *     mask=255, attrs=...) at ../system/memory.c:491
+ * #6  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=1, access_size_min=1, access_size_max=4,
+ *     access_fn=0x555555c73af5 <memory_region_write_accessor>, mr=0x5555578c8d90, attrs=...) at ../system/memory.c:567
+ * #7  0x0000555555c77700 in memory_region_dispatch_write (mr=0x5555578c8d90, addr=0, data=1, op=MO_8, attrs=...)
+ *     at ../system/memory.c:1547
+ * #8  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 "\001\v", len=1, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x5555578c8d90) at ../system/physmem.c:3269
+ * #9  0x0000555555c897c6 in flatview_write_continue (fv=0x5555583a91f0, addr=3324, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=1, mr_addr=0, l=1, mr=0x5555578c8d90) at ../system/physmem.c:3299
+ * #10 0x0000555555c898f3 in flatview_write (fv=0x5555583a91f0, addr=3324, attrs=..., buf=0x7ffff7ff2000, len=1)
+ *     at ../system/physmem.c:3330
+ * #11 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=3324, attrs=...,
+ *     buf=0x7ffff7ff2000, len=1) at ../system/physmem.c:3450
+ * #12 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=3324, attrs=...,
+ *     buf=0x7ffff7ff2000, len=1, is_write=true) at ../system/physmem.c:3460
+ * #13 0x0000555555ef7b0e in kvm_handle_io (port=3324, attrs=..., data=0x7ffff7ff2000, direction=1, size=1, count=1)
+ *     at ../accel/kvm/kvm-all.c:3136
+ * #14 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #15 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #16 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #17 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #18 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ *
+ * [root@ol810-uefi ~]# lspci -s 00:01.3 -vv
+ * 00:01.3 Bridge: Intel Corporation 82371AB/EB/MB PIIX4 ACPI (rev 03)
+ * 	Subsystem: Red Hat, Inc. Qemu virtual machine
+ * 	Control: I/O+ Mem+ BusMaster+ SpecCycle- MemWINV- VGASnoop- ParErr- Stepping- SERR- FastB2B- DisINTx-
+ * 	Status: Cap- 66MHz- UDF- FastB2B+ ParErr- DEVSEL=medium >TAbort- <TAbort- <MAbort- >SERR- <PERR- INTx-
+ * 	Latency: 0
+ * 	Interrupt: pin A routed to IRQ 9
+ * 	Kernel driver in use: piix4_smbus
+ * 	Kernel modules: i2c_piix4
+ * 
+ * [root@ol810-uefi ~]#   setpci -s 00:01.3 40.l
+ * 0000b001
+ * [root@ol810-uefi ~]#   setpci -s 00:01.3 80.b
+ * 01
+ *
+ */
 static void pm_io_space_update(PIIX4PMState *s)
 {
     PCIDevice *d = PCI_DEVICE(s);
 
+    /*
+     * 查看pm_write_config()
+     * 1. guest 往 PCI config offset 0x40 写值.
+     * 2. QEMU 的 pci_default_write_config() 把值写进 d->config.
+     * 3. 然后 pm_io_space_update() 重新取 d->config + 0x40, 更新 PM I/O 映射.
+     */
     s->io_base = le32_to_cpu(*(uint32_t *)(d->config + 0x40));
     s->io_base &= 0xffc0;
 
@@ -110,6 +253,10 @@ static void smbus_io_space_update(PIIX4PMState *s)
     memory_region_transaction_commit();
 }
 
+/*
+ * 在以下使用pm_write_config():
+ *   - hw/acpi/piix4.c|731| <<piix4_pm_class_init>> k->config_write = pm_write_config;
+ */
 static void pm_write_config(PCIDevice *d,
                             uint32_t address, uint32_t val, int len)
 {
@@ -354,6 +501,15 @@ static void piix4_device_unplug_request_cb(HotplugHandler *hotplug_dev,
         acpi_memory_unplug_request_cb(hotplug_dev, &s->acpi_memory_hotplug,
                                       dev, errp);
     } else if (object_dynamic_cast(OBJECT(dev), TYPE_PCI_DEVICE)) {
+        /*
+	 * 在以下使用acpi_pcihp_device_unplug_request_cb():
+         *   - hw/acpi/generic_event_device.c|292| <<acpi_ged_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &s->pcihp_state, dev, errp);
+         *   - hw/acpi/ich9.c|500| <<ich9_pm_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &lpc->pm.acpi_pci_hotplug, dev, errp);
+         *   - hw/acpi/piix4.c|504| <<piix4_device_unplug_request_cb>> acpi_pcihp_device_unplug_request_cb(hotplug_dev,
+         *                                                 &s->acpi_pci_hotplug, dev, errp);
+	 */
         acpi_pcihp_device_unplug_request_cb(hotplug_dev, &s->acpi_pci_hotplug,
                                             dev, errp);
     } else if (object_dynamic_cast(OBJECT(dev), TYPE_CPU)) {
diff --git a/hw/acpi/utils.c b/hw/acpi/utils.c
index b7cab6f5c..2ced91485 100644
--- a/hw/acpi/utils.c
+++ b/hw/acpi/utils.c
@@ -26,6 +26,24 @@
 #include "hw/acpi/utils.h"
 #include "hw/core/loader.h"
 
+/*
+ * 在以下使用acpi_add_rom_blob():
+ *   - hw/arm/virt-acpi-build.c|1480| <<virt_acpi_setup>> build_state->table_mr = acpi_add_rom_blob(virt_acpi_build_update,
+ *   - hw/arm/virt-acpi-build.c|1485| <<virt_acpi_setup>> build_state->linker_mr = acpi_add_rom_blob(virt_acpi_build_update,
+ *   - hw/arm/virt-acpi-build.c|1500| <<virt_acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(virt_acpi_build_update,
+ *   - hw/i386/acpi-build.c|2315| <<acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+ *   - hw/i386/acpi-build.c|2327| <<acpi_setup>> acpi_add_rom_blob(acpi_build_update, build_state,
+ *   - hw/i386/acpi-build.c|2358| <<acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
+ *   - hw/i386/acpi-microvm.c|260| <<acpi_setup_microvm>> acpi_add_rom_blob(acpi_build_no_update, NULL, tables.table_data,
+ *   - hw/i386/acpi-microvm.c|262| <<acpi_setup_microvm>> acpi_add_rom_blob(acpi_build_no_update, NULL, tables.linker->cmd_blob,
+ *   - hw/i386/acpi-microvm.c|264| <<acpi_setup_microvm>> acpi_add_rom_blob(acpi_build_no_update, NULL, tables.rsdp,
+ *   - hw/loongarch/virt-acpi-build.c|706| <<virt_acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+ *   - hw/loongarch/virt-acpi-build.c|712| <<virt_acpi_setup>> acpi_add_rom_blob(acpi_build_update, build_state,
+ *   - hw/loongarch/virt-acpi-build.c|715| <<virt_acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
+ *   - hw/riscv/virt-acpi-build.c|1014| <<virt_acpi_setup>> build_state->table_mr = acpi_add_rom_blob(virt_acpi_build_update,
+ *   - hw/riscv/virt-acpi-build.c|1019| <<virt_acpi_setup>> build_state->linker_mr = acpi_add_rom_blob(virt_acpi_build_update,
+ *   - hw/riscv/virt-acpi-build.c|1024| <<virt_acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(virt_acpi_build_update,
+ */
 MemoryRegion *acpi_add_rom_blob(FWCfgCallback update, void *opaque,
                                 GArray *blob, const char *name)
 {
diff --git a/hw/arm/virt-acpi-build.c b/hw/arm/virt-acpi-build.c
index 591cfc993..208f6068f 100644
--- a/hw/arm/virt-acpi-build.c
+++ b/hw/arm/virt-acpi-build.c
@@ -1259,6 +1259,20 @@ void virt_acpi_build(VirtMachineState *vms, AcpiBuildTables *tables)
     table_offsets = g_array_new(false, true /* clear */,
                                         sizeof(uint32_t));
 
+    /*
+     * 在以下使用bios_linker_loader_alloc():
+     *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+     *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+     *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+     *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+     *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     */
     bios_linker_loader_alloc(tables->linker,
                              ACPI_BUILD_TABLE_FILE, tables_blob,
                              64, false /* high memory */);
diff --git a/hw/block/virtio-blk.c b/hw/block/virtio-blk.c
index 9cb9f1fb2..6e4f7eb12 100644
--- a/hw/block/virtio-blk.c
+++ b/hw/block/virtio-blk.c
@@ -43,6 +43,11 @@
 
 static void virtio_blk_ioeventfd_attach(VirtIOBlock *s);
 
+/*
+ * 在以下使用virtio_blk_init_request():
+ *   - hw/block/virtio-blk.c|182| <<virtio_blk_get_request>> virtio_blk_init_request(s, vq, req);
+ *   - hw/block/virtio-blk.c|1388| <<virtio_blk_load_device>> virtio_blk_init_request(s, virtio_get_queue(vdev, vq_idx), req);
+ */
 static void virtio_blk_init_request(VirtIOBlock *s, VirtQueue *vq,
                                     VirtIOBlockReq *req)
 {
@@ -54,6 +59,26 @@ static void virtio_blk_init_request(VirtIOBlock *s, VirtQueue *vq,
     req->mr_next = NULL;
 }
 
+/*
+ * 在以下使用virtio_blk_req_complete():
+ *   - hw/block/virtio-blk.c|99| <<virtio_blk_handle_rw_error>> virtio_blk_req_complete(req, VIRTIO_BLK_S_IOERR);
+ *   - hw/block/virtio-blk.c|165| <<virtio_blk_rw_complete>> virtio_blk_req_complete(req, VIRTIO_BLK_S_OK);
+ *   - hw/block/virtio-blk.c|187| <<virtio_blk_flush_complete>> virtio_blk_req_complete(req, VIRTIO_BLK_S_OK);
+ *   - hw/block/virtio-blk.c|210| <<virtio_blk_discard_write_zeroes_complete>> virtio_blk_req_complete(req, VIRTIO_BLK_S_OK);
+ *   - hw/block/virtio-blk.c|284| <<virtio_blk_handle_scsi>> virtio_blk_req_complete(req, status);
+ *   - hw/block/virtio-blk.c|721| <<virtio_blk_zone_report_complete>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|776| <<virtio_blk_handle_zone_report>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|792| <<virtio_blk_zone_mgmt_complete>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|834| <<virtio_blk_handle_zone_mgmt>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|865| <<virtio_blk_zone_append_complete>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|903| <<virtio_blk_handle_zone_append>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|973| <<virtio_blk_handle_request>> virtio_blk_req_complete(req, VIRTIO_BLK_S_IOERR);
+ *   - hw/block/virtio-blk.c|1031| <<virtio_blk_handle_request>> virtio_blk_req_complete(req, VIRTIO_BLK_S_OK);
+ *   - hw/block/virtio-blk.c|1063| <<virtio_blk_handle_request>> virtio_blk_req_complete(req, VIRTIO_BLK_S_UNSUPP);
+ *   - hw/block/virtio-blk.c|1080| <<virtio_blk_handle_request>> virtio_blk_req_complete(req, err_status);
+ *   - hw/block/virtio-blk.c|1095| <<virtio_blk_handle_request>> virtio_blk_req_complete(req, VIRTIO_BLK_S_UNSUPP);
+ *   - hw/vmapple/virtio-blk.c|56| <<vmapple_virtio_blk_handle_unknown_request>> virtio_blk_req_complete(req, VIRTIO_BLK_S_OK);
+ */
 void virtio_blk_req_complete(VirtIOBlockReq *req, unsigned char status)
 {
     VirtIOBlock *s = req->dev;
@@ -68,6 +93,13 @@ void virtio_blk_req_complete(VirtIOBlockReq *req, unsigned char status)
     virtio_notify(vdev, req->vq);
 }
 
+/*
+ * 在以下使用virtio_blk_handle_rw_error():
+ *   - hw/block/virtio-blk.c|139| <<virtio_blk_rw_complete>> if (virtio_blk_handle_rw_error(req, -ret, is_read, true)) {
+ *   - hw/block/virtio-blk.c|155| <<virtio_blk_flush_complete>> if (ret && virtio_blk_handle_rw_error(req, -ret, 0, true)) {
+ *   - hw/block/virtio-blk.c|171| <<virtio_blk_discard_write_zeroes_complete>> if (ret && virtio_blk_handle_rw_error(req,
+ *                                                                  -ret, false, is_write_zeroes)) {
+ */
 static int virtio_blk_handle_rw_error(VirtIOBlockReq *req, int error,
     bool is_read, bool acct_failed)
 {
@@ -91,10 +123,24 @@ static int virtio_blk_handle_rw_error(VirtIOBlockReq *req, int error,
         g_free(req);
     }
 
+    /*
+     * 在以下使用blk_error_action():
+     *  - hw/block/virtio-blk.c|94| <<virtio_blk_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+     *  - hw/ide/ahci.c|1030| <<ncq_cb>> blk_error_action(ide_state->blk, action, is_read, -ret);
+     *  - hw/ide/core.c|876| <<ide_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+     *  - hw/scsi/scsi-disk.c|300| <<scsi_handle_rw_error>> blk_error_action(s->qdev.conf.blk, action, is_read, error);
+     */
     blk_error_action(s->blk, action, is_read, error);
     return action != BLOCK_ERROR_ACTION_IGNORE;
 }
 
+/*
+ * 在以下使用virtio_blk_rw_complete():
+ *   - hw/block/virtio-blk.c|297| <<submit_requests>> blk_aio_pwritev(blk, sector_num << BDRV_SECTOR_BITS, qiov,
+ *                       flags, virtio_blk_rw_complete, mrb->reqs[start]);
+ *   - hw/block/virtio-blk.c|301| <<submit_requests>> blk_aio_preadv(blk, sector_num << BDRV_SECTOR_BITS, qiov,
+ *                       flags, virtio_blk_rw_complete, mrb->reqs[start]);
+ */
 static void virtio_blk_rw_complete(void *opaque, int ret)
 {
     VirtIOBlockReq *next = opaque;
@@ -124,6 +170,13 @@ static void virtio_blk_rw_complete(void *opaque, int ret)
              * the memory until the request is completed (which will
              * happen on the other side of the migration).
              */
+            /*
+	     * 在以下使用virtio_blk_handle_rw_error():
+             *   - hw/block/virtio-blk.c|139| <<virtio_blk_rw_complete>> if (virtio_blk_handle_rw_error(req, -ret, is_read, true)) {
+             *   - hw/block/virtio-blk.c|155| <<virtio_blk_flush_complete>> if (ret && virtio_blk_handle_rw_error(req, -ret, 0, true)) {
+             *   - hw/block/virtio-blk.c|171| <<virtio_blk_discard_write_zeroes_complete>> if (ret && virtio_blk_handle_rw_error(req,
+             *                                                                  -ret, false, is_write_zeroes)) {
+	     */
             if (virtio_blk_handle_rw_error(req, -ret, is_read, true)) {
                 continue;
             }
@@ -140,6 +193,13 @@ static void virtio_blk_flush_complete(void *opaque, int ret)
     VirtIOBlockReq *req = opaque;
     VirtIOBlock *s = req->dev;
 
+    /*
+     * 在以下使用virtio_blk_handle_rw_error():
+     *   - hw/block/virtio-blk.c|139| <<virtio_blk_rw_complete>> if (virtio_blk_handle_rw_error(req, -ret, is_read, true)) {
+     *   - hw/block/virtio-blk.c|155| <<virtio_blk_flush_complete>> if (ret && virtio_blk_handle_rw_error(req, -ret, 0, true)) {
+     *   - hw/block/virtio-blk.c|171| <<virtio_blk_discard_write_zeroes_complete>> if (ret && virtio_blk_handle_rw_error(req,
+     *                                                                  -ret, false, is_write_zeroes)) {
+     */
     if (ret && virtio_blk_handle_rw_error(req, -ret, 0, true)) {
         return;
     }
@@ -156,6 +216,13 @@ static void virtio_blk_discard_write_zeroes_complete(void *opaque, int ret)
     bool is_write_zeroes = (virtio_ldl_p(VIRTIO_DEVICE(s), &req->out.type) &
                             ~VIRTIO_BLK_T_BARRIER) == VIRTIO_BLK_T_WRITE_ZEROES;
 
+    /*
+     * 在以下使用virtio_blk_handle_rw_error():
+     *   - hw/block/virtio-blk.c|139| <<virtio_blk_rw_complete>> if (virtio_blk_handle_rw_error(req, -ret, is_read, true)) {
+     *   - hw/block/virtio-blk.c|155| <<virtio_blk_flush_complete>> if (ret && virtio_blk_handle_rw_error(req, -ret, 0, true)) {
+     *   - hw/block/virtio-blk.c|171| <<virtio_blk_discard_write_zeroes_complete>> if (ret && virtio_blk_handle_rw_error(req,
+     *                                                                  -ret, false, is_write_zeroes)) {
+     */
     if (ret && virtio_blk_handle_rw_error(req, -ret, false, is_write_zeroes)) {
         return;
     }
@@ -167,11 +234,37 @@ static void virtio_blk_discard_write_zeroes_complete(void *opaque, int ret)
     g_free(req);
 }
 
+/*
+ * 在以下使用virtio_blk_get_request():
+ *   - hw/block/virtio-blk.c|1031| <<virtio_blk_handle_vq>> while ((req = virtio_blk_get_request(s, vq))) {
+ */
 static VirtIOBlockReq *virtio_blk_get_request(VirtIOBlock *s, VirtQueue *vq)
 {
+    /*
+     * typedef struct VirtIOBlockReq {
+     *     VirtQueueElement elem;
+     *     int64_t sector_num;
+     *     VirtIOBlock *dev;
+     *     VirtQueue *vq;
+     *     IOVDiscardUndo inhdr_undo;
+     *     IOVDiscardUndo outhdr_undo;
+     *     struct virtio_blk_inhdr *in;
+     *     struct virtio_blk_outhdr out;
+     *     QEMUIOVector qiov;
+     *     size_t in_len;
+     *     struct VirtIOBlockReq *next;
+     *     struct VirtIOBlockReq *mr_next;
+     *     BlockAcctCookie acct;
+     * } VirtIOBlockReq;
+     */
     VirtIOBlockReq *req = virtqueue_pop(vq, sizeof(VirtIOBlockReq));
 
     if (req) {
+        /*
+	 * 在以下使用virtio_blk_init_request():
+         *   - hw/block/virtio-blk.c|182| <<virtio_blk_get_request>> virtio_blk_init_request(s, vq, req);
+         *   - hw/block/virtio-blk.c|1388| <<virtio_blk_load_device>> virtio_blk_init_request(s, virtio_get_queue(vdev, vq_idx), req);
+	 */
         virtio_blk_init_request(s, vq, req);
     }
     return req;
@@ -255,10 +348,24 @@ static inline void submit_requests(VirtIOBlock *s, MultiReqBuffer *mrb,
     }
 
     if (is_write) {
+        /*
+	 * 在以下使用virtio_blk_rw_complete():
+         *   - hw/block/virtio-blk.c|297| <<submit_requests>> blk_aio_pwritev(blk, sector_num << BDRV_SECTOR_BITS, qiov,
+         *                       flags, virtio_blk_rw_complete, mrb->reqs[start]);
+         *   - hw/block/virtio-blk.c|301| <<submit_requests>> blk_aio_preadv(blk, sector_num << BDRV_SECTOR_BITS, qiov,
+         *                       flags, virtio_blk_rw_complete, mrb->reqs[start]);
+	 */
         blk_aio_pwritev(blk, sector_num << BDRV_SECTOR_BITS, qiov,
                         flags, virtio_blk_rw_complete,
                         mrb->reqs[start]);
     } else {
+        /*
+	 * 在以下使用virtio_blk_rw_complete():
+         *   - hw/block/virtio-blk.c|297| <<submit_requests>> blk_aio_pwritev(blk, sector_num << BDRV_SECTOR_BITS, qiov,
+         *                       flags, virtio_blk_rw_complete, mrb->reqs[start]);
+         *   - hw/block/virtio-blk.c|301| <<submit_requests>> blk_aio_preadv(blk, sector_num << BDRV_SECTOR_BITS, qiov,
+         *                       flags, virtio_blk_rw_complete, mrb->reqs[start]);
+	 */
         blk_aio_preadv(blk, sector_num << BDRV_SECTOR_BITS, qiov,
                        flags, virtio_blk_rw_complete,
                        mrb->reqs[start]);
@@ -818,6 +925,11 @@ out:
     return err_status;
 }
 
+/*
+ * 在以下使用virtio_blk_handle_request():
+ *   - hw/block/virtio-blk.c|1032| <<virtio_blk_handle_vq>> if (virtio_blk_handle_request(req, &mrb)) {
+ *   - hw/block/virtio-blk.c|1077| <<virtio_blk_dma_restart_bh>> if (virtio_blk_handle_request(req, &mrb)) {
+ */
 static int virtio_blk_handle_request(VirtIOBlockReq *req, MultiReqBuffer *mrb)
 {
     uint32_t type;
@@ -1022,6 +1134,11 @@ void virtio_blk_handle_vq(VirtIOBlock *s, VirtQueue *vq)
         }
 
         while ((req = virtio_blk_get_request(s, vq))) {
+            /*
+	     * 在以下使用virtio_blk_handle_request():
+             *   - hw/block/virtio-blk.c|1032| <<virtio_blk_handle_vq>> if (virtio_blk_handle_request(req, &mrb)) {
+             *   - hw/block/virtio-blk.c|1077| <<virtio_blk_dma_restart_bh>> if (virtio_blk_handle_request(req, &mrb)) {
+	     */
             if (virtio_blk_handle_request(req, &mrb)) {
                 virtqueue_detach_element(req->vq, &req->elem, 0);
                 g_free(req);
@@ -1033,6 +1150,19 @@ void virtio_blk_handle_vq(VirtIOBlock *s, VirtQueue *vq)
             virtio_queue_set_notification(vq, 1);
         }
     } while (!virtio_queue_empty(vq));
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
 
     if (mrb.num_reqs) {
         virtio_blk_submit_multireq(s, &mrb);
@@ -1067,6 +1197,11 @@ static void virtio_blk_dma_restart_bh(void *opaque)
 
     while (req) {
         VirtIOBlockReq *next = req->next;
+        /*
+	 * 在以下使用virtio_blk_handle_request():
+	 *   - hw/block/virtio-blk.c|1032| <<virtio_blk_handle_vq>> if (virtio_blk_handle_request(req, &mrb)) {
+	 *   - hw/block/virtio-blk.c|1077| <<virtio_blk_dma_restart_bh>> if (virtio_blk_handle_request(req, &mrb)) {
+	 */
         if (virtio_blk_handle_request(req, &mrb)) {
             /* Device is now broken and won't do any processing until it gets
              * reset. Already queued requests will be lost: let's purge them.
@@ -1378,6 +1513,11 @@ static int virtio_blk_load_device(VirtIODevice *vdev, QEMUFile *f,
         }
 
         req = qemu_get_virtqueue_element(vdev, f, sizeof(VirtIOBlockReq));
+        /*
+	 * 在以下使用virtio_blk_init_request():
+         *   - hw/block/virtio-blk.c|182| <<virtio_blk_get_request>> virtio_blk_init_request(s, vq, req);
+         *   - hw/block/virtio-blk.c|1388| <<virtio_blk_load_device>> virtio_blk_init_request(s, virtio_get_queue(vdev, vq_idx), req);
+	 */
         virtio_blk_init_request(s, virtio_get_queue(vdev, vq_idx), req);
 
         WITH_QEMU_LOCK_GUARD(&s->rq_lock) {
@@ -1832,6 +1972,15 @@ static void virtio_blk_device_realize(DeviceState *dev, Error **errp)
      * This must be after virtio_init() so virtio_blk_dma_restart_cb() gets
      * called after ->start_ioeventfd() has already set blk's AioContext.
      */
+    /*
+     * 在以下使用qdev_add_vm_change_state_handler():
+     *   - hw/block/virtio-blk.c|1976| <<virtio_blk_device_realize>>
+     *          qdev_add_vm_change_state_handler(dev, virtio_blk_dma_restart_cb, NULL, s);
+     *   - hw/scsi/scsi-bus.c|412| <<scsi_qdev_realize>>
+     *          dev->vmsentry = qdev_add_vm_change_state_handler(DEVICE(dev), scsi_dma_restart_cb, NULL, dev);
+     *   - hw/virtio/virtio.c|4252| <<virtio_init>>
+     *          vdev->vmstate = qdev_add_vm_change_state_handler(DEVICE(vdev), NULL, virtio_vmstate_change, vdev);
+     */
     s->change =
         qdev_add_vm_change_state_handler(dev, virtio_blk_dma_restart_cb, NULL, s);
 
diff --git a/hw/char/serial.c b/hw/char/serial.c
index 0f3469a1e..cdef8890d 100644
--- a/hw/char/serial.c
+++ b/hw/char/serial.c
@@ -120,6 +120,42 @@ static inline void recv_fifo_put(SerialState *s, uint8_t chr)
     }
 }
 
+/*
+ * (gdb) bt
+ * #0  qemu_set_irq
+ * #1  0x00005555559abedf in qemu_irq_raise
+ * #2  0x00005555559ac24a in serial_update_irq
+ * #3  0x00005555559acf23 in serial_ioport_write
+ * #4  0x0000555555c806e4 in memory_region_write_accessor
+ * #5  0x0000555555c80a36 in access_with_adjusted_size
+ * #6  0x0000555555c841f9 in memory_region_dispatch_write
+ * #7  0x0000555555c961ea in flatview_write_continue_step
+ * #8  0x0000555555c962bf in flatview_write_continue
+ * #9  0x0000555555c963ec in flatview_write
+ * #10 0x0000555555c96879 in address_space_write
+ * #11 0x0000555555c968f3 in address_space_rw
+ * #12 0x0000555555f07101 in kvm_handle_io
+ * #13 0x0000555555f08089 in kvm_cpu_exec
+ * #14 0x0000555555f0b825 in kvm_vcpu_thread_fn
+ * #15 0x000055555616bcab in qemu_thread_start
+ * #16 0x00007ffff6c9a1da in start_thread
+ * #17 0x00007ffff4fcb8d3 in clone
+ *
+ *
+ * 在以下使用serial_update_irq():
+ *   - hw/char/serial.c|216| <<serial_update_msl>> serial_update_irq(s);
+ *   - hw/char/serial.c|256| <<serial_xmit>> serial_update_irq(s);
+ *   - hw/char/serial.c|359| <<serial_ioport_write>> serial_update_irq(s);
+ *   - hw/char/serial.c|405| <<serial_ioport_write>> serial_update_irq(s);
+ *   - hw/char/serial.c|431| <<serial_ioport_write>> serial_update_irq(s);
+ *   - hw/char/serial.c|496| <<serial_ioport_read>> serial_update_irq(s);
+ *   - hw/char/serial.c|514| <<serial_ioport_read>> serial_update_irq(s);
+ *   - hw/char/serial.c|528| <<serial_ioport_read>> serial_update_irq(s);
+ *   - hw/char/serial.c|545| <<serial_ioport_read>> serial_update_irq(s);
+ *   - hw/char/serial.c|584| <<serial_receive_break>> serial_update_irq(s);
+ *   - hw/char/serial.c|592| <<fifo_timeout_int>> serial_update_irq(s);
+ *   - hw/char/serial.c|623| <<serial_receive1>> serial_update_irq(s);
+ */
 static void serial_update_irq(SerialState *s)
 {
     uint8_t tmp_iir = UART_IIR_NO_INT;
@@ -230,10 +266,20 @@ static gboolean serial_watch_cb(void *do_not_use, GIOCondition cond,
 {
     SerialState *s = opaque;
     s->watch_tag = 0;
+    /*
+     * 在以下使用serial_xmit():
+     *   - hw/char/serial.c|269| <<serial_watch_cb>> serial_xmit(s);
+     *   - hw/char/serial.c|397| <<serial_ioport_write>> serial_xmit(s);
+     */
     serial_xmit(s);
     return G_SOURCE_REMOVE;
 }
 
+/*
+ * 在以下使用serial_xmit():
+ *   - hw/char/serial.c|269| <<serial_watch_cb>> serial_xmit(s);
+ *   - hw/char/serial.c|397| <<serial_ioport_write>> serial_xmit(s);
+ */
 static void serial_xmit(SerialState *s)
 {
     do {
@@ -242,6 +288,12 @@ static void serial_xmit(SerialState *s)
             assert(!(s->lsr & UART_LSR_THRE));
 
             if (s->fcr & UART_FCR_FE) {
+                /*
+		 * SerialState *s:
+		 * -> uint64_t last_xmit_ts;
+		 * -> Fifo8 recv_fifo;
+		 * -> Fifo8 xmit_fifo;
+		 */
                 assert(!fifo8_is_empty(&s->xmit_fifo));
                 s->tsr = fifo8_pop(&s->xmit_fifo);
                 if (fifo8_is_empty(&s->xmit_fifo)) {
@@ -261,6 +313,11 @@ static void serial_xmit(SerialState *s)
             /* in loopback mode, say that we just received a char */
             serial_receive1(s, &s->tsr, 1);
         } else {
+            /*
+	     * SerialState *s:
+	     * -> uint8_t tsr;
+	     * -> CharFrontend chr;
+	     */
             int rc = qemu_chr_fe_write(&s->chr, &s->tsr, 1);
 
             if ((rc == 0 ||
@@ -357,6 +414,11 @@ static void serial_ioport_write(void *opaque, hwaddr addr, uint64_t val,
             s->lsr &= ~UART_LSR_THRE;
             s->lsr &= ~UART_LSR_TEMT;
             serial_update_irq(s);
+            /*
+	     * 在以下使用serial_xmit():
+             *   - hw/char/serial.c|269| <<serial_watch_cb>> serial_xmit(s);
+             *   - hw/char/serial.c|397| <<serial_ioport_write>> serial_xmit(s);
+	     */
             if (s->tsr_retry == 0) {
                 serial_xmit(s);
             }
diff --git a/hw/char/virtio-serial-bus.c b/hw/char/virtio-serial-bus.c
index cd234dc6d..c893747d3 100644
--- a/hw/char/virtio-serial-bus.c
+++ b/hw/char/virtio-serial-bus.c
@@ -310,6 +310,19 @@ size_t virtio_serial_guest_ready(VirtIOSerialPort *port)
     VirtQueue *vq = port->ivq;
     unsigned int bytes;
 
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
     if (!virtio_queue_ready(vq) ||
         !(vdev->status & VIRTIO_CONFIG_S_DRIVER_OK) ||
         virtio_queue_empty(vq)) {
@@ -318,6 +331,13 @@ size_t virtio_serial_guest_ready(VirtIOSerialPort *port)
     if (use_multiport(port->vser) && !port->guest_connected) {
         return 0;
     }
+    /*
+     * 在以下使用virtqueue_get_avail_bytes():
+     *   - hw/char/virtio-serial-bus.c|321| <<virtio_serial_guest_ready>> virtqueue_get_avail_bytes(vq, &bytes, NULL, 4096, 0);
+     *   - hw/net/virtio-net.c|1657| <<virtio_net_has_buffers>> opaque = virtqueue_get_avail_bytes(q->rx_vq, &in_bytes, NULL,
+     *   - hw/virtio/virtio-rng.c|40| <<get_request_size>> virtqueue_get_avail_bytes(vq, &in, &out, quota, 0);
+     *   - hw/virtio/virtio.c|1610| <<virtqueue_avail_bytes>> virtqueue_get_avail_bytes(vq, &in_total, &out_total, in_bytes, out_bytes);
+     */
     virtqueue_get_avail_bytes(vq, &bytes, NULL, 4096, 0);
     return bytes;
 }
diff --git a/hw/core/irq.c b/hw/core/irq.c
index 106805e24..aabcfc905 100644
--- a/hw/core/irq.c
+++ b/hw/core/irq.c
@@ -26,14 +26,44 @@
 #include "hw/core/irq.h"
 #include "qom/object.h"
 
+/*
+ * (gdb) bt
+ * #0  qemu_set_irq
+ * #1  0x00005555559abedf in qemu_irq_raise
+ * #2  0x00005555559ac24a in serial_update_irq
+ * #3  0x00005555559acf23 in serial_ioport_write
+ * #4  0x0000555555c806e4 in memory_region_write_accessor
+ * #5  0x0000555555c80a36 in access_with_adjusted_size
+ * #6  0x0000555555c841f9 in memory_region_dispatch_write
+ * #7  0x0000555555c961ea in flatview_write_continue_step
+ * #8  0x0000555555c962bf in flatview_write_continue
+ * #9  0x0000555555c963ec in flatview_write
+ * #10 0x0000555555c96879 in address_space_write
+ * #11 0x0000555555c968f3 in address_space_rw
+ * #12 0x0000555555f07101 in kvm_handle_io
+ * #13 0x0000555555f08089 in kvm_cpu_exec
+ * #14 0x0000555555f0b825 in kvm_vcpu_thread_fn
+ * #15 0x000055555616bcab in qemu_thread_start
+ * #16 0x00007ffff6c9a1da in start_thread
+ * #17 0x00007ffff4fcb8d3 in clone
+ */
 void qemu_set_irq(qemu_irq irq, int level)
 {
     if (!irq)
         return;
 
+    /*
+     * isa-serial是gsi_handler()?
+     */
     irq->handler(irq->opaque, irq->n, level);
 }
 
+/*
+ * 在以下使用init_irq_fields():
+ *   - hw/core/irq.c|49| <<qemu_init_irq>> init_irq_fields(irq, handler, opaque, n);
+ *   - hw/core/irq.c|57| <<qemu_init_irq_child>> init_irq_fields(irq, handler, opaque, n);
+ *   - hw/core/irq.c|92| <<qemu_allocate_irq>> init_irq_fields(irq, handler, opaque, n);
+ */
 static void init_irq_fields(IRQState *irq, qemu_irq_handler handler,
                             void *opaque, int n)
 {
@@ -46,6 +76,12 @@ void qemu_init_irq(IRQState *irq, qemu_irq_handler handler, void *opaque,
                    int n)
 {
     object_initialize(irq, sizeof(*irq), TYPE_IRQ);
+    /*
+     * 在以下使用init_irq_fields():
+     *   - hw/core/irq.c|49| <<qemu_init_irq>> init_irq_fields(irq, handler, opaque, n);
+     *   - hw/core/irq.c|57| <<qemu_init_irq_child>> init_irq_fields(irq, handler, opaque, n);
+     *   - hw/core/irq.c|92| <<qemu_allocate_irq>> init_irq_fields(irq, handler, opaque, n);
+     */
     init_irq_fields(irq, handler, opaque, n);
 }
 
@@ -54,6 +90,12 @@ void qemu_init_irq_child(Object *parent, const char *propname,
                          void *opaque, int n)
 {
     object_initialize_child(parent, propname, irq, TYPE_IRQ);
+    /*
+     * 在以下使用init_irq_fields():
+     *   - hw/core/irq.c|49| <<qemu_init_irq>> init_irq_fields(irq, handler, opaque, n);
+     *   - hw/core/irq.c|57| <<qemu_init_irq_child>> init_irq_fields(irq, handler, opaque, n);
+     *   - hw/core/irq.c|92| <<qemu_allocate_irq>> init_irq_fields(irq, handler, opaque, n);
+     */
     init_irq_fields(irq, handler, opaque, n);
 }
 
@@ -89,6 +131,12 @@ qemu_irq *qemu_allocate_irqs(qemu_irq_handler handler, void *opaque, int n)
 qemu_irq qemu_allocate_irq(qemu_irq_handler handler, void *opaque, int n)
 {
     IRQState *irq = IRQ(object_new(TYPE_IRQ));
+    /*
+     * 在以下使用init_irq_fields():
+     *   - hw/core/irq.c|49| <<qemu_init_irq>> init_irq_fields(irq, handler, opaque, n);
+     *   - hw/core/irq.c|57| <<qemu_init_irq_child>> init_irq_fields(irq, handler, opaque, n);
+     *   - hw/core/irq.c|92| <<qemu_allocate_irq>> init_irq_fields(irq, handler, opaque, n);
+     */
     init_irq_fields(irq, handler, opaque, n);
     return irq;
 }
diff --git a/hw/core/loader.c b/hw/core/loader.c
index 5cbfba0a8..8b2dd58b3 100644
--- a/hw/core/loader.c
+++ b/hw/core/loader.c
@@ -1158,6 +1158,15 @@ err:
     return -1;
 }
 
+/*
+ * 在以下使用rom_add_blob():
+ *   - hw/acpi/utils.c|64| <<acpi_add_rom_blob>> return rom_add_blob(name, blob->data, acpi_data_len(blob), max_size, -1,
+ *   - hw/arm/allwinner-a10.c|58| <<allwinner_a10_bootrom_setup>> rom_add_blob("allwinner-a10.bootrom", buffer, rom_size,
+ *   - hw/arm/allwinner-h3.c|189| <<allwinner_h3_bootrom_setup>> rom_add_blob("allwinner-h3.bootrom", buffer, rom_size,
+ *   - hw/arm/allwinner-r40.c|255| <<allwinner_r40_bootrom_setup>> rom_add_blob("allwinner-r40.bootrom", buffer, rom_size,
+ *   - include/hw/core/loader.h|332| <<rom_add_blob_fixed>> rom_add_blob(_f, _b, _l, _l, _a, NULL, NULL, NULL, NULL, true)
+ *   - include/hw/core/loader.h|340| <<rom_add_blob_fixed_as>> rom_add_blob(_f, _b, _l, _l, _a, NULL, NULL, NULL, _as, true)
+ */
 MemoryRegion *rom_add_blob(const char *name, const void *blob, size_t len,
                    size_t max_len, hwaddr addr, const char *fw_file_name,
                    FWCfgCallback fw_callback, void *callback_opaque,
diff --git a/hw/core/machine.c b/hw/core/machine.c
index 0aa77a57e..7a9356508 100644
--- a/hw/core/machine.c
+++ b/hw/core/machine.c
@@ -1714,6 +1714,15 @@ static void handle_machine_dumpdtb(MachineState *ms)
 
 void qdev_machine_creation_done(void)
 {
+    /*
+     * 在以下使用cpu_synchronize_all_post_init():
+     *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+     *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+     *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+     */
     cpu_synchronize_all_post_init();
 
     if (current_machine->boot_config.once) {
diff --git a/hw/core/vm-change-state-handler.c b/hw/core/vm-change-state-handler.c
index 2c1113502..63c8c49bd 100644
--- a/hw/core/vm-change-state-handler.c
+++ b/hw/core/vm-change-state-handler.c
@@ -55,12 +55,29 @@ static int qdev_get_dev_tree_depth(DeviceState *dev)
  *
  * Returns: an entry to be freed with qemu_del_vm_change_state_handler()
  */
+/*
+ * 在以下使用qdev_add_vm_change_state_handler():
+ *   - hw/block/virtio-blk.c|1976| <<virtio_blk_device_realize>>
+ *          qdev_add_vm_change_state_handler(dev, virtio_blk_dma_restart_cb, NULL, s);
+ *   - hw/scsi/scsi-bus.c|412| <<scsi_qdev_realize>>
+ *          dev->vmsentry = qdev_add_vm_change_state_handler(DEVICE(dev), scsi_dma_restart_cb, NULL, dev);
+ *   - hw/virtio/virtio.c|4252| <<virtio_init>>
+ *          vdev->vmstate = qdev_add_vm_change_state_handler(DEVICE(vdev), NULL, virtio_vmstate_change, vdev);
+ */
 VMChangeStateEntry *qdev_add_vm_change_state_handler(DeviceState *dev,
                                                      VMChangeStateHandler *cb,
                                                      VMChangeStateHandlerWithRet *cb_ret,
                                                      void *opaque)
 {
     assert(!cb || !cb_ret);
+    /*
+     * 在以下使用qdev_add_vm_change_state_handler_full():
+     *   - hw/core/vm-change-state-handler.c|64| <<qdev_add_vm_change_state_handler>>
+     *          return qdev_add_vm_change_state_handler_full(dev, cb, NULL, cb_ret, opaque);
+     *   - hw/vfio/migration.c|1106| <<vfio_migration_init>>
+     *          migration->vm_state = qdev_add_vm_change_state_handler_full(
+     *           vbasedev->dev, vfio_vmstate_change, prepare_cb, NULL, vbasedev);
+     */
     return qdev_add_vm_change_state_handler_full(dev, cb, NULL, cb_ret, opaque);
 }
 
@@ -68,6 +85,14 @@ VMChangeStateEntry *qdev_add_vm_change_state_handler(DeviceState *dev,
  * Exactly like qdev_add_vm_change_state_handler() but passes a prepare_cb
  * and the cb_ret arguments too.
  */
+/*
+ * 在以下使用qdev_add_vm_change_state_handler_full():
+ *   - hw/core/vm-change-state-handler.c|64| <<qdev_add_vm_change_state_handler>>
+ *          return qdev_add_vm_change_state_handler_full(dev, cb, NULL, cb_ret, opaque);
+ *   - hw/vfio/migration.c|1106| <<vfio_migration_init>>
+ *          migration->vm_state = qdev_add_vm_change_state_handler_full(
+ *           vbasedev->dev, vfio_vmstate_change, prepare_cb, NULL, vbasedev);
+ */
 VMChangeStateEntry *qdev_add_vm_change_state_handler_full(
     DeviceState *dev, VMChangeStateHandler *cb, VMChangeStateHandler *prepare_cb,
     VMChangeStateHandlerWithRet *cb_ret, void *opaque)
@@ -75,6 +100,13 @@ VMChangeStateEntry *qdev_add_vm_change_state_handler_full(
     int depth = qdev_get_dev_tree_depth(dev);
 
     assert(!cb || !cb_ret);
+    /*
+     * 在以下使用qemu_add_vm_change_state_handler_prio_full():
+     *   - hw/core/vm-change-state-handler.c|78| <<qdev_add_vm_change_state_handler_full>>
+     *          return qemu_add_vm_change_state_handler_prio_full(cb, prepare_cb, cb_ret, opaque, depth);
+     *   - system/runstate.c|312| <<qemu_add_vm_change_state_handler_prio>>
+     *          return qemu_add_vm_change_state_handler_prio_full(cb, NULL, NULL, opaque, priority);
+     */
     return qemu_add_vm_change_state_handler_prio_full(cb, prepare_cb, cb_ret,
                                                       opaque, depth);
 }
diff --git a/hw/i386/acpi-build.c b/hw/i386/acpi-build.c
index f622b91b7..5db588904 100644
--- a/hw/i386/acpi-build.c
+++ b/hw/i386/acpi-build.c
@@ -133,6 +133,91 @@ const struct AcpiGenericAddress x86_nvdimm_acpi_dsmio = {
     .bit_width = NVDIMM_ACPI_IO_LEN << 3
 };
 
+/*
+ * 创建QEMU
+ * 
+ * (gdb) bt
+ * #0  init_common_fadt_data (ms=0x5555577f0a20, o=0x555557d7c720, data=0x7fffffffd628) at ../hw/i386/acpi-build.c:139
+ * #1  0x0000555555e46463 in acpi_get_pm_info (machine=0x5555577f0a20, pm=0x7fffffffd620) at ../hw/i386/acpi-build.c:209
+ * #2  0x0000555555e4c873 in acpi_build (tables=0x7fffffffd740, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1940
+ * #3  0x0000555555e4d625 in acpi_setup () at ../hw/i386/acpi-build.c:2204
+ * #4  0x0000555555e41329 in pc_machine_done (notifier=0x5555577f0e70, data=0x0) at ../hw/i386/pc.c:539
+ * #5  0x00005555561583b5 in notifier_list_notify (list=0x5555574a8cd0 <machine_init_done_notifiers>, data=0x0) at ../util/notify.c:39
+ * #6  0x00005555559bafbd in qdev_machine_creation_done () at ../hw/core/machine.c:1742
+ * #7  0x0000555555c6404c in qemu_machine_creation_done (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2781
+ * #8  0x0000555555c6418b in qmp_x_exit_preconfig (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2809
+ * #9  0x0000555555c66ca3 in qemu_init (argc=26, argv=0x7fffffffdb18) at ../system/vl.c:3845
+ * #10 0x000055555608087d in main (argc=26, argv=0x7fffffffdb18) at ../system/main.c:71
+ *
+ * (gdb) bt
+ * #0  init_common_fadt_data (ms=0x5555577f0a20, o=0x555557d7c720, data=0x7fffeff7c368) at ../hw/i386/acpi-build.c:139
+ * #1  0x0000555555e46463 in acpi_get_pm_info (machine=0x5555577f0a20, pm=0x7fffeff7c360) at ../hw/i386/acpi-build.c:209
+ * #2  0x0000555555e4c873 in acpi_build (tables=0x7fffeff7c470, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1940
+ * #3  0x0000555555e4d486 in acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2148
+ * #4  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #5  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #6  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2,
+ *     shift=0, mask=65535, attrs=...) at ../system/memory.c:491
+ * #7  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1,
+ *     access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, mr=0x555557d33c80, attrs=...)
+ *     at ../system/memory.c:567
+ * #8  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...)
+ *     at ../system/memory.c:1547
+ * #9  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #10 0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde0302a80, addr=1296, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #11 0x0000555555c898f3 in flatview_write (fv=0x7ffde0302a80, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2)
+ *     at ../system/physmem.c:3330
+ * #12 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #13 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #14 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1)
+ *     at ../accel/kvm/kvm-all.c:3136
+ * #15 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #16 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #17 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #18 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #19 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * Reboot的时候
+ *
+ * (gdb) bt
+ * #0  init_common_fadt_data (ms=0x5555577f0a20, o=0x555557d7c720, data=0x7fffeff7c368) at ../hw/i386/acpi-build.c:139
+ * #1  0x0000555555e46463 in acpi_get_pm_info (machine=0x5555577f0a20, pm=0x7fffeff7c360) at ../hw/i386/acpi-build.c:209
+ * #2  0x0000555555e4c873 in acpi_build (tables=0x7fffeff7c470, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1940
+ * #3  0x0000555555e4d486 in acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2148
+ * #4  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #5  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #6  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2,
+ *     shift=0, mask=65535, attrs=...) at ../system/memory.c:491
+ * #7  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1,
+ *     access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, mr=0x555557d33c80, attrs=...)
+ *     at ../system/memory.c:567
+ * #8  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...)
+ *     at ../system/memory.c:1547
+ * #9  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #10 0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde039bbe0, addr=1296, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #11 0x0000555555c898f3 in flatview_write (fv=0x7ffde039bbe0, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2)
+ *     at ../system/physmem.c:3330
+ * #12 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #13 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #14 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1)
+ *     at ../accel/kvm/kvm-all.c:3136
+ * #15 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #16 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #17 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #18 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #19 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * 在以下使用init_common_fadt_data():
+ *   - hw/i386/acpi-build.c|209| <<acpi_get_pm_info>> init_common_fadt_data(machine, obj, &pm->fadt);
+ */
 static void init_common_fadt_data(MachineState *ms, Object *o,
                                   AcpiFadtData *data)
 {
@@ -144,6 +229,12 @@ static void init_common_fadt_data(MachineState *ms, Object *o,
      */
     bool smm_enabled = object_property_get_bool(o, "smm-compat", NULL) ?
         true : x86_machine_is_smm_enabled(x86ms);
+    /*
+     * 在以下使用ACPI_PM_PROP_PM_IO_BASE:
+     *   - hw/acpi/ich9.c|402| <<ich9_pm_add_properties>> object_property_add_uint32_ptr(obj, ACPI_PM_PROP_PM_IO_BASE,
+     *   - hw/acpi/piix4.c|437| <<piix4_pm_add_properties>> object_property_add_uint32_ptr(OBJECT(s), ACPI_PM_PROP_PM_IO_BASE,
+     *   - hw/i386/acpi-build.c|147| <<init_common_fadt_data>> uint32_t io = object_property_get_uint(o, ACPI_PM_PROP_PM_IO_BASE, NULL);
+     */
     uint32_t io = object_property_get_uint(o, ACPI_PM_PROP_PM_IO_BASE, NULL);
     AmlAddressSpace as = AML_AS_SYSTEM_IO;
     AcpiFadtData fadt = {
@@ -174,6 +265,13 @@ static void init_common_fadt_data(MachineState *ms, Object *o,
             smm_enabled ?
             object_property_get_uint(o, ACPI_PM_PROP_ACPI_DISABLE_CMD, NULL) :
             0,
+	/*
+	 * 在以下使用AcpiFadtData->pm1a_evt:
+	 *   - hw/acpi/aml-build.c|2367| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.address, 4);
+	 *   - hw/acpi/aml-build.c|2376| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.bit_width / 8, 1);
+	 *   - hw/acpi/aml-build.c|2430| <<build_fadt>> build_append_gas_from_struct(tbl, &f->pm1a_evt);
+	 *   - hw/i386/acpi-build.c|177| <<init_common_fadt_data>> .pm1a_evt = { .space_id = as, .bit_width = 4 * 8, .address = io },
+	 */
         .pm1a_evt = { .space_id = as, .bit_width = 4 * 8, .address = io },
         .pm1a_cnt = { .space_id = as, .bit_width = 2 * 8,
                       .address = io + 0x04 },
@@ -320,6 +418,24 @@ static void acpi_align_size(GArray *blob, unsigned align)
     g_array_set_size(blob, ROUND_UP(acpi_data_len(blob), align));
 }
 
+/*
+ * [000h 0000   4]                    Signature : "FACS"
+ * [004h 0004   4]                       Length : 00000040
+ * [008h 0008   4]           Hardware Signature : 00000000
+ * [00Ch 0012   4]    32 Firmware Waking Vector : 00000000
+ * [010h 0016   4]                  Global Lock : 00000000
+ * [014h 0020   4]        Flags (decoded below) : 00000000
+ *                       S4BIOS Support Present : 0
+ *                   64-bit Wake Supported (V2) : 0
+ * [018h 0024   8]    64 Firmware Waking Vector : 0000000000000000
+ * [020h 0032   1]                      Version : 00
+ * [021h 0033   3]                     Reserved : 000000
+ * [024h 0036   4]    OspmFlags (decoded below) : 00000000
+ *                64-bit Wake Env Required (V2) : 0
+ *
+ * 只在以下调用build_facs():
+ *   - hw/i386/acpi-build.c|2156| <<acpi_build>> build_facs(tables_blob);
+ */
 /*
  * ACPI spec 1.0b,
  * 5.2.6 Firmware ACPI Control Structure
@@ -846,6 +962,10 @@ static void build_acpi0017(Aml *table)
     aml_append(table, scope);
 }
 
+/*
+ * 在以下使用build_dsdt():
+ *   - hw/i386/acpi-build.c|2160| <<acpi_build>> build_dsdt(tables_blob, tables->linker, &pm, &misc,
+ */
 static void
 build_dsdt(GArray *table_data, BIOSLinker *linker,
            AcpiPmInfo *pm, AcpiMiscInfo *misc,
@@ -899,6 +1019,12 @@ build_dsdt(GArray *table_data, BIOSLinker *linker,
         aml_append(dev, aml_name_decl("_HID", aml_eisaid("PNP0A08")));
         aml_append(dev, aml_name_decl("_CID", aml_eisaid("PNP0A03")));
         aml_append(dev, aml_name_decl("_UID", aml_int(pcmc->pci_root_uid)));
+	/*
+	 * 在以下使用build_pci_host_bridge_osc_method():
+         *   - hw/i386/acpi-build.c|994| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(!pm->pcihp_bridge_en));
+         *   - hw/i386/acpi-build.c|1114| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(true));
+         *   - hw/pci-host/gpex-acpi.c|99| <<acpi_dsdt_add_host_bridge_methods>> build_pci_host_bridge_osc_method(enable_native_pcie_hotplug));
+	 */
         aml_append(dev, build_pci_host_bridge_osc_method(!pm->pcihp_bridge_en));
         aml_append(dev, build_pci_bridge_edsm());
         aml_append(sb_scope, dev);
@@ -1018,6 +1144,12 @@ build_dsdt(GArray *table_data, BIOSLinker *linker,
                 aml_append(dev, aml_name_decl("_HID", aml_eisaid("PNP0A08")));
                 aml_append(dev, aml_name_decl("_CID", aml_eisaid("PNP0A03")));
 
+                /*
+		 * 在以下使用build_pci_host_bridge_osc_method():
+                 *   - hw/i386/acpi-build.c|994| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(!pm->pcihp_bridge_en));
+                 *   - hw/i386/acpi-build.c|1114| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(true));
+                 *   - hw/pci-host/gpex-acpi.c|99| <<acpi_dsdt_add_host_bridge_methods>> build_pci_host_bridge_osc_method(enable_native_pcie_hotplug));
+		 */
                 /* Expander bridges do not have ACPI PCI Hot-plug enabled */
                 aml_append(dev, build_pci_host_bridge_osc_method(true));
             } else {
@@ -1285,6 +1417,10 @@ build_dsdt(GArray *table_data, BIOSLinker *linker,
  * IA-PC HPET (High Precision Event Timers) Specification (Revision: 1.0a)
  * 3.2.4The ACPI 2.0 HPET Description Table (HPET)
  */
+/*
+ * 在以下使用build_hpet():
+ *   - hw/i386/acpi-build.c|2210| <<acpi_build>> build_hpet(tables_blob, tables->linker, x86ms->oem_id,
+ */
 static void
 build_hpet(GArray *table_data, BIOSLinker *linker, const char *oem_id,
            const char *oem_table_id)
@@ -1336,6 +1472,20 @@ build_tpm_tcpa(GArray *table_data, BIOSLinker *linker, GArray *tcpalog,
 
     /* allocate/reserve space for TPM log area */
     acpi_data_push(tcpalog, TPM_LOG_AREA_MINIMUM_SIZE);
+    /*
+     * 在以下使用bios_linker_loader_alloc():
+     *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+     *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+     *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+     *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+     *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     */
     bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1,
                              false /* high memory */);
     /* log area start address to be filled by Guest linker */
@@ -1545,6 +1695,10 @@ dmar_host_bridges(Object *obj, void *opaque)
  * Architecture Specification. Revision 3.3
  * 8.1 DMA Remapping Reporting Structure
  */
+/*
+ * 在以下使用build_dmar_q35():
+ *   - hw/i386/acpi-build.c|2251| <<acpi_build>> build_dmar_q35(tables_blob, tables->linker, x86ms->oem_id, 
+ */
 static void
 build_dmar_q35(GArray *table_data, BIOSLinker *linker, const char *oem_id,
                const char *oem_table_id)
@@ -1622,6 +1776,21 @@ build_dmar_q35(GArray *table_data, BIOSLinker *linker, const char *oem_id,
     acpi_table_end(linker, &table);
 }
 
+/*
+ * [000h 0000   4]                    Signature : "WAET"    [Windows ACPI Emulated Devices Table]
+ * [004h 0004   4]                 Table Length : 00000028
+ * [008h 0008   1]                     Revision : 01
+ * [009h 0009   1]                     Checksum : 39
+ * [00Ah 0010   6]                       Oem ID : "BOCHS "
+ * [010h 0016   8]                 Oem Table ID : "BXPC    "
+ * [018h 0024   4]                 Oem Revision : 00000001
+ * [01Ch 0028   4]              Asl Compiler ID : "BXPC"
+ * [020h 0032   4]        Asl Compiler Revision : 00000001
+ *
+ * [024h 0036   4]        Flags (decoded below) : 00000002
+ *                         RTC needs no INT ack : 0
+ *                      PM timer, one read only : 1
+ */
 /*
  * Windows ACPI Emulated Devices Table
  * (Version 1.0 - April 6, 2009)
@@ -1746,6 +1915,10 @@ ivrs_host_bridges(Object *obj, void *opaque)
     return 0;
 }
 
+/*
+ * 在以下使用build_amd_iommu():
+ *   - hw/i386/acpi-build.c|2247| <<acpi_build>> build_amd_iommu(tables_blob, tables->linker, x86ms->oem_id,
+ */
 static void
 build_amd_iommu(GArray *table_data, BIOSLinker *linker, const char *oem_id,
                 const char *oem_table_id)
@@ -1881,6 +2054,12 @@ build_amd_iommu(GArray *table_data, BIOSLinker *linker, const char *oem_id,
     acpi_table_end(linker, &table);
 }
 
+/*
+ * 在以下使用AcpiBuildState->table_mr:
+ *   - hw/i386/acpi-build.c|2243| <<acpi_build_update>> acpi_ram_update(build_state->table_mr, tables.table_data);
+ *   - hw/i386/acpi-build.c|2315| <<acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+ *   - hw/i386/acpi-build.c|2318| <<acpi_setup>> assert(build_state->table_mr != NULL);
+ */
 typedef
 struct AcpiBuildState {
     /* Copy of table in RAM (for patching). */
@@ -1918,6 +2097,85 @@ static bool acpi_get_mcfg(AcpiMcfgInfo *mcfg)
     return true;
 }
 
+/*
+ * 创建的时候:
+ * (gdb) bt
+ * #0  build_fadt (tbl=0x555557570670, linker=0x555557575290, f=0x7fffffffd598, oem_id=0x5555577f7390 "BOCHS ", oem_table_id=0x5555577f73b0 "BXPC    ") at ../hw/acpi/aml-build.c:2248
+ * #1  0x0000555555e4ca42 in acpi_build (tables=0x7fffffffd6b0, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1984
+ * #2  0x0000555555e4d625 in acpi_setup () at ../hw/i386/acpi-build.c:2204
+ * #3  0x0000555555e41329 in pc_machine_done (notifier=0x5555577f0e70, data=0x0) at ../hw/i386/pc.c:539
+ * #4  0x00005555561583b5 in notifier_list_notify (list=0x5555574a8cd0 <machine_init_done_notifiers>, data=0x0) at ../util/notify.c:39
+ * #5  0x00005555559bafbd in qdev_machine_creation_done () at ../hw/core/machine.c:1742
+ * #6  0x0000555555c6404c in qemu_machine_creation_done (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2781
+ * #7  0x0000555555c6418b in qmp_x_exit_preconfig (errp=0x5555574e8e60 <error_fatal>) at ../system/vl.c:2809
+ * #8  0x0000555555c66ca3 in qemu_init (argc=26, argv=0x7fffffffda88) at ../system/vl.c:3845
+ * #9  0x000055555608087d in main (argc=26, argv=0x7fffffffda88) at ../system/main.c:71
+ *
+ * (gdb) bt
+ * #0  build_fadt (tbl=0x5555578cc2d0, linker=0x7ffde034b480, f=0x7fffeff7c368, oem_id=0x5555577f7390 "BOCHS ", oem_table_id=0x5555577f73b0 "BXPC    ") at ../hw/acpi/aml-build.c:2248
+ * #1  0x0000555555e4ca42 in acpi_build (tables=0x7fffeff7c470, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1984
+ * #2  0x0000555555e4d486 in acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2148
+ * #3  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #4  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #5  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0, mask=65535, attrs=...) at ../system/memory.c:491
+ * #6  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1, access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, 
+ *     mr=0x555557d33c80, attrs=...) at ../system/memory.c:567
+ * #7  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...) at ../system/memory.c:1547
+ * #8  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0, l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #9  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde0302ac0, addr=1296, attrs=..., ptr=0x7ffff7ff2000, len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #10 0x0000555555c898f3 in flatview_write (fv=0x7ffde0302ac0, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3330
+ * #11 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #12 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #13 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1) at ../accel/kvm/kvm-all.c:3136
+ * #14 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #15 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #16 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #17 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #18 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *             
+ * 重启的时候和system_reset的时候
+ * (gdb) bt
+ * #0  build_fadt (tbl=0x7ffde039e720, linker=0x7ffde0007d30, f=0x7fffeff7c368, oem_id=0x5555577f7390 "BOCHS ", oem_table_id=0x5555577f73b0 "BXPC    ") at ../hw/acpi/aml-build.c:2248
+ * #1  0x0000555555e4ca42 in acpi_build (tables=0x7fffeff7c470, machine=0x5555577f0a20) at ../hw/i386/acpi-build.c:1984
+ * #2  0x0000555555e4d486 in acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2148
+ * #3  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #4  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #5  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0, mask=65535, attrs=...) at ../system/memory.c:491
+ * #6  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1, access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>,
+ *     mr=0x555557d33c80, attrs=...) at ../system/memory.c:567
+ * #7  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...) at ../system/memory.c:1547
+ * #8  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0, l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #9  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde034eb80, addr=1296, attrs=..., ptr=0x7ffff7ff2000, len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #10 0x0000555555c898f3 in flatview_write (fv=0x7ffde034eb80, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3330
+ * #11 0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #12 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #13 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1) at ../accel/kvm/kvm-all.c:3136
+ * #14 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #15 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #16 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #17 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #18 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ *
+ * typedef struct BIOSLinker {
+ *     GArray *cmd_blob;
+ *     GArray *file_list;
+ * } BIOSLinker;
+ *
+ * typedef
+ * struct AcpiBuildTables {
+ *     GArray *table_data;
+ *     GArray *rsdp;
+ *     GArray *tcpalog;
+ *     GArray *vmgenid;
+ *     GArray *hardware_errors;
+ *     BIOSLinker *linker;
+ * } AcpiBuildTables;
+ *
+ * 在以下使用acpi_build():
+ *   - hw/i386/acpi-build.c|2148| <<acpi_build_update>> acpi_build(&tables, MACHINE(qdev_get_machine()));
+ *   - hw/i386/acpi-build.c|2204| <<acpi_setup>> acpi_build(&tables, MACHINE(pcms));
+ */
 static
 void acpi_build(AcpiBuildTables *tables, MachineState *machine)
 {
@@ -1937,6 +2195,9 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
     char *oem_id;
     char *oem_table_id;
 
+    /*
+     * AcpiPmInfo pm;
+     */
     acpi_get_pm_info(machine, &pm);
     acpi_get_misc_info(&misc);
     acpi_get_pci_holes(&pci_hole, &pci_hole64);
@@ -1958,6 +2219,20 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
                                         sizeof(uint32_t));
     ACPI_BUILD_DPRINTF("init ACPI tables\n");
 
+    /*
+     * 在以下使用bios_linker_loader_alloc():
+     *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+     *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+     *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+     *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+     *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     */
     bios_linker_loader_alloc(tables->linker,
                              ACPI_BUILD_TABLE_FILE, tables_blob,
                              64 /* Ensure FACS is aligned */,
@@ -1968,6 +2243,9 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
      * We place it first since it's the only table that has alignment
      * requirements.
      */
+    /*
+     * unsigned facs, dsdt, rsdt;
+     */
     facs = tables_blob->len;
     build_facs(tables_blob);
 
@@ -1981,9 +2259,23 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
     pm.fadt.facs_tbl_offset = &facs;
     pm.fadt.dsdt_tbl_offset = &dsdt;
     pm.fadt.xdsdt_tbl_offset = &dsdt;
+    /*
+     * 在以下使用build_fadt():
+     *   - hw/arm/virt-acpi-build.c|1119| <<build_fadt_rev6>> build_fadt(table_data, linker, &fadt, vms->oem_id, vms->oem_table_id);
+     *   - hw/i386/acpi-build.c|1984| <<acpi_build>> build_fadt(tables_blob, tables->linker, &pm.fadt, oem_id, oem_table_id);
+     *   - hw/i386/acpi-microvm.c|201| <<acpi_build_microvm>> build_fadt(tables_blob, tables->linker, &pmfadt, x86ms->oem_id,
+     *   - hw/loongarch/virt-acpi-build.c|534| <<acpi_build>> build_fadt(tables_blob, tables->linker, &fadt_data,
+     *   - hw/riscv/virt-acpi-build.c|437| <<build_fadt_rev6>> build_fadt(table_data, linker, &fadt, s->oem_id, s->oem_table_id);
+     */
     build_fadt(tables_blob, tables->linker, &pm.fadt, oem_id, oem_table_id);
 
     acpi_add_table(table_offsets, tables_blob);
+    /*
+     * 在以下使用acpi_build_madt():
+     *   - hw/i386/acpi-build.c|2179| <<acpi_build>> acpi_build_madt(tables_blob, tables->linker, x86ms,
+     *   - hw/i386/acpi-build.c|2609| <<acpi_build_madt_standalone>> acpi_build_madt(table, NULL, x86ms, x86ms->oem_id,
+     *   - hw/i386/acpi-microvm.c|205| <<acpi_build_microvm>> acpi_build_madt(tables_blob, tables->linker, X86_MACHINE(machine),
+     */
     acpi_build_madt(tables_blob, tables->linker, x86ms,
                     x86ms->oem_id, x86ms->oem_table_id);
 
@@ -1993,6 +2285,11 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
         erst_dev = find_erst_dev();
         if (erst_dev) {
             acpi_add_table(table_offsets, tables_blob);
+            /*
+	     * 在以下使用build_erst():
+             *   - hw/i386/acpi-build.c|2188| <<acpi_build>> build_erst(tables_blob, tables->linker, erst_dev,
+             *   - hw/i386/acpi-microvm.c|214| <<acpi_build_microvm>> build_erst(tables_blob, tables->linker, erst_dev,
+	     */
             build_erst(tables_blob, tables->linker, erst_dev,
                        x86ms->oem_id, x86ms->oem_table_id);
         }
@@ -2026,6 +2323,12 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
                            x86ms->oem_id, x86ms->oem_table_id);
         } else { /* TPM_VERSION_2_0 */
             acpi_add_table(table_offsets, tables_blob);
+            /*
+	     * 在以下使用build_tpm2():
+             *   - hw/arm/virt-acpi-build.c|1363| <<virt_acpi_build>> build_tpm2(tables_blob, tables->linker, tables->tcpalog, vms->oem_id,
+             *   - hw/i386/acpi-build.c|2221| <<acpi_build>> build_tpm2(tables_blob, tables->linker, tables->tcpalog,
+             *   - hw/loongarch/virt-acpi-build.c|578| <<acpi_build>> build_tpm2(tables_blob, tables->linker,
+	     */
             build_tpm2(tables_blob, tables->linker, tables->tcpalog,
                        x86ms->oem_id, x86ms->oem_table_id);
         }
@@ -2088,6 +2391,9 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
 
     /* RSDT is pointed to by RSDP */
     rsdt = tables_blob->len;
+    /*
+     * 只在这里调用
+     */
     build_rsdt(tables_blob, tables->linker, table_offsets,
                oem_id, oem_table_id);
 
@@ -2121,6 +2427,12 @@ void acpi_build(AcpiBuildTables *tables, MachineState *machine)
     g_free(slic_oem.table_id);
 }
 
+/*
+ * 在以下使用acpi_ram_update():
+ *   - hw/i386/acpi-build.c|2243| <<acpi_build_update>> acpi_ram_update(build_state->table_mr, tables.table_data);
+ *   - hw/i386/acpi-build.c|2245| <<acpi_build_update>> acpi_ram_update(build_state->rsdp_mr, tables.rsdp);
+ *   - hw/i386/acpi-build.c|2247| <<acpi_build_update>> acpi_ram_update(build_state->linker_mr, tables.linker->cmd_blob);
+ */
 static void acpi_ram_update(MemoryRegion *mr, GArray *data)
 {
     uint32_t size = acpi_data_len(data);
@@ -2132,6 +2444,101 @@ static void acpi_ram_update(MemoryRegion *mr, GArray *data)
     memory_region_set_dirty(mr, 0, size);
 }
 
+/*
+ * 创建QEMU的时候和uefi guest reboot的时候, 都调用三遍
+ *
+ * (gdb) bt
+ * #0  acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2136
+ * #1  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=41) at ../hw/nvram/fw_cfg.c:285
+ * #2  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=41, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #3  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0,
+ *     mask=65535, attrs=...) at ../system/memory.c:491
+ * #4  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1,
+ *     access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, mr=0x555557d33c80, attrs=...)
+ *     at ../system/memory.c:567
+ * #5  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=41, op=MO_16, attrs=...)
+ *     at ../system/memory.c:1547
+ * #6  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 ")", len=2, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #7  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde0302a80, addr=1296, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #8  0x0000555555c898f3 in flatview_write (fv=0x7ffde0302a80, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2)
+ *     at ../system/physmem.c:3330
+ * #9  0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #10 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #11 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1)
+ *     at ../accel/kvm/kvm-all.c:3136
+ * #12 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #13 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #14 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #15 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #16 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * (gdb) bt
+ * #0  acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2136
+ * #1  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=34) at ../hw/nvram/fw_cfg.c:285
+ * #2  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=34, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #3  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0,
+ *     mask=65535, attrs=...) at ../system/memory.c:491
+ * #4  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1,
+ *     access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>,  mr=0x555557d33c80, attrs=...)
+ *     at ../system/memory.c:567
+ * #5  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=34, op=MO_16, attrs=...)
+ *     at ../system/memory.c:1547
+ * #6  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 "\"", len=2, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #7  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde0395140, addr=1296, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #8  0x0000555555c898f3 in flatview_write (fv=0x7ffde0395140, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2)
+ *     at ../system/physmem.c:3330
+ * #9  0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #10 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #11 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1)
+ *     at ../accel/kvm/kvm-all.c:3136
+ * #12 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #13 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #14 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #15 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #16 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * (gdb) bt
+ * #0  acpi_build_update (build_opaque=0x555557814e40) at ../hw/i386/acpi-build.c:2136
+ * #1  0x0000555555afecac in fw_cfg_select (s=0x555557d337c0, key=35) at ../hw/nvram/fw_cfg.c:285
+ * #2  0x0000555555aff624 in fw_cfg_comb_write (opaque=0x555557d337c0, addr=0, value=35, size=2) at ../hw/nvram/fw_cfg.c:509
+ * #3  0x0000555555c73beb in memory_region_write_accessor (mr=0x555557d33c80, addr=0, value=0x7fffeff7c638, size=2, shift=0,
+ *     mask=65535, attrs=...) at ../system/memory.c:491
+ * #4  0x0000555555c73f3d in access_with_adjusted_size (addr=0, value=0x7fffeff7c638, size=2, access_size_min=1,
+ *     access_size_max=4, access_fn=0x555555c73af5 <memory_region_write_accessor>, mr=0x555557d33c80, attrs=...)
+ *     at ../system/memory.c:567
+ * #5  0x0000555555c77700 in memory_region_dispatch_write (mr=0x555557d33c80, addr=0, data=35, op=MO_16, attrs=...)
+ *      at ../system/memory.c:1547
+ * #6  0x0000555555c896f1 in flatview_write_continue_step (attrs=..., buf=0x7ffff7ff2000 "#", len=2, mr_addr=0,
+ *     l=0x7fffeff7c720, mr=0x555557d33c80) at ../system/physmem.c:3269
+ * #7  0x0000555555c897c6 in flatview_write_continue (fv=0x7ffde0395140, addr=1296, attrs=..., ptr=0x7ffff7ff2000,
+ *     len=2, mr_addr=0, l=2, mr=0x555557d33c80) at ../system/physmem.c:3299
+ * #8  0x0000555555c898f3 in flatview_write (fv=0x7ffde0395140, addr=1296, attrs=..., buf=0x7ffff7ff2000, len=2)
+ *     at ../system/physmem.c:3330
+ * #9  0x0000555555c89d80 in address_space_write (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2) at ../system/physmem.c:3450
+ * #10 0x0000555555c89dfa in address_space_rw (as=0x5555574cae40 <address_space_io>, addr=1296, attrs=...,
+ *     buf=0x7ffff7ff2000, len=2, is_write=true) at ../system/physmem.c:3460
+ * #11 0x0000555555ef7b0e in kvm_handle_io (port=1296, attrs=..., data=0x7ffff7ff2000, direction=1, size=2, count=1)
+ *     at ../accel/kvm/kvm-all.c:3136
+ * #12 0x0000555555ef8a96 in kvm_cpu_exec (cpu=0x5555578863a0) at ../accel/kvm/kvm-all.c:3524
+ * #13 0x0000555555efc232 in kvm_vcpu_thread_fn (arg=0x5555578863a0) at ../accel/kvm/kvm-accel-ops.c:54
+ * #14 0x000055555614eb89 in qemu_thread_start (args=0x555557892a10) at ../util/qemu-thread-posix.c:414
+ * #15 0x00007ffff67141da in start_thread () from /lib64/libpthread.so.0
+ * #16 0x00007ffff636e8d3 in clone () from /lib64/libc.so.6
+ *
+ * 在以下使用acpi_build_update():
+ *   - hw/i386/acpi-build.c|2222| <<acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+ *   - hw/i386/acpi-build.c|2228| <<acpi_setup>> acpi_add_rom_blob(acpi_build_update, build_state,
+ *   - hw/i386/acpi-build.c|2253| <<acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
+ */
 static void acpi_build_update(void *build_opaque)
 {
     AcpiBuildState *build_state = build_opaque;
@@ -2143,15 +2550,72 @@ static void acpi_build_update(void *build_opaque)
     }
     build_state->patched = 1;
 
+    /*
+     * 在以下使用acpi_build_tables_init():
+     *   - hw/arm/virt-acpi-build.c|1431| <<virt_acpi_build_update>> acpi_build_tables_init(&tables);
+     *   - hw/arm/virt-acpi-build.c|1476| <<virt_acpi_setup>> acpi_build_tables_init(&tables);
+     *   - hw/i386/acpi-build.c|2336| <<acpi_build_update>> acpi_build_tables_init(&tables);
+     *   - hw/i386/acpi-build.c|2430| <<acpi_setup>> acpi_build_tables_init(&tables);
+     *   - hw/i386/acpi-microvm.c|256| <<acpi_setup_microvm>> acpi_build_tables_init(&tables);
+     *   - hw/loongarch/virt-acpi-build.c|650| <<acpi_build_update>> acpi_build_tables_init(&tables);
+     *   - hw/loongarch/virt-acpi-build.c|702| <<virt_acpi_setup>> acpi_build_tables_init(&tables);
+     *   - hw/riscv/virt-acpi-build.c|976| <<virt_acpi_build_update>> acpi_build_tables_init(&tables);
+     *   - hw/riscv/virt-acpi-build.c|1010| <<virt_acpi_setup>> acpi_build_tables_init(&tables);
+     */
     acpi_build_tables_init(&tables);
 
+    /*
+     * 在以下使用acpi_build():
+     *   - hw/i386/acpi-build.c|2148| <<acpi_build_update>> acpi_build(&tables, MACHINE(qdev_get_machine()));
+     *   - hw/i386/acpi-build.c|2204| <<acpi_setup>> acpi_build(&tables, MACHINE(pcms));
+     */
     acpi_build(&tables, MACHINE(qdev_get_machine()));
 
+    /*
+     * 这三个对应三份不同的 ACPI 相关 blob, 用途不一样.
+     *
+     * - tables.table_data
+     * 这是 ACPI 表本体, 也就是 FACS, DSDT, FADT, MADT, RSDT/XSDT
+     * 等表拼在一起的总二进制数据.
+     * 它对应 etc/acpi/tables, 是在 hw/i386/acpi-build.c:2207 挂给 guest 的.
+     * - tables.rsdp
+     * 这是 RSDP 单独那一小块数据.
+     * RSDP 是 ACPI 的入口指针, OS 或固件先靠它找到 RSDT/XSDT.
+     * 它单独放, 是因为它通常有专门的放置要求. 代码里注释直接写了, RSDP is in
+     * FSEG memory, so allocate it separately, 见 hw/acpi/aml-build.c:1827 和
+     * hw/i386/acpi-build.c:2238.
+     * - tables.linker->cmd_blob
+     * 这是 linker/loader 命令流, 不是 ACPI 表本身.
+     * 它告诉 guest 侧固件或 loader:
+     * - 把 etc/acpi/tables 放到 guest 内存哪里,
+     * - 把 FADT 里的 DSDT/FACS 指针补成真实地址,
+     * - 把 checksum 算好.
+     *
+     * 这份数据对应 etc/table-loader, 定义和注释在 hw/acpi/bios-linker-loader.c:18.
+     *
+     * 所以这三行的区别可以直接记成:
+     *
+     * - table_mr: 真正的 ACPI tables 内容.
+     * - rsdp_mr: ACPI 入口指针 RSDP.
+     * - linker_mr: 指导固件如何摆放和修补这些表的命令.
+     */
     acpi_ram_update(build_state->table_mr, tables.table_data);
 
     acpi_ram_update(build_state->rsdp_mr, tables.rsdp);
 
     acpi_ram_update(build_state->linker_mr, tables.linker->cmd_blob);
+    /*
+     * 在以下使用acpi_build_tables_cleanup():
+     *   - hw/arm/virt-acpi-build.c|1453| <<virt_acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+     *   - hw/arm/virt-acpi-build.c|1525| <<virt_acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+     *   - hw/i386/acpi-build.c|2589| <<acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+     *   - hw/i386/acpi-build.c|2710| <<acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+     *   - hw/i386/acpi-microvm.c|292| <<acpi_setup_microvm>> acpi_build_tables_cleanup(&tables, false);
+     *   - hw/loongarch/virt-acpi-build.c|658| <<acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+     *   - hw/loongarch/virt-acpi-build.c|730| <<virt_acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+     *   - hw/riscv/virt-acpi-build.c|984| <<virt_acpi_build_update>> acpi_build_tables_cleanup(&tables, true);
+     *   - hw/riscv/virt-acpi-build.c|1036| <<virt_acpi_setup>> acpi_build_tables_cleanup(&tables, false);
+     */
     acpi_build_tables_cleanup(&tables, true);
 }
 
@@ -2171,6 +2635,10 @@ static const VMStateDescription vmstate_acpi_build = {
     },
 };
 
+/*
+ * 在以下使用acpi_setup():
+ *   - hw/i386/pc.c|539| <<pc_machine_done>> acpi_setup();
+ */
 void acpi_setup(void)
 {
     PCMachineState *pcms = PC_MACHINE(qdev_get_machine());
@@ -2201,14 +2669,31 @@ void acpi_setup(void)
     build_state = g_malloc0(sizeof *build_state);
 
     acpi_build_tables_init(&tables);
+    /*
+     * 在以下使用acpi_build():
+     *   - hw/i386/acpi-build.c|2148| <<acpi_build_update>> acpi_build(&tables, MACHINE(qdev_get_machine()));
+     *   - hw/i386/acpi-build.c|2204| <<acpi_setup>> acpi_build(&tables, MACHINE(pcms));
+     */
     acpi_build(&tables, MACHINE(pcms));
 
+    /*
+     * 在以下使用acpi_build_update():
+     *   - hw/i386/acpi-build.c|2222| <<acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+     *   - hw/i386/acpi-build.c|2228| <<acpi_setup>> acpi_add_rom_blob(acpi_build_update, build_state,
+     **   - hw/i386/acpi-build.c|2253| <<acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
+     */
     /* Now expose it all to Guest */
     build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
                                               build_state, tables.table_data,
                                               ACPI_BUILD_TABLE_FILE);
     assert(build_state->table_mr != NULL);
 
+    /*
+     * 在以下使用acpi_build_update():
+     *   - hw/i386/acpi-build.c|2222| <<acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+     *   - hw/i386/acpi-build.c|2228| <<acpi_setup>> acpi_add_rom_blob(acpi_build_update, build_state,
+     *   - hw/i386/acpi-build.c|2253| <<acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
+     */
     build_state->linker_mr =
         acpi_add_rom_blob(acpi_build_update, build_state,
                           tables.linker->cmd_blob, ACPI_BUILD_LOADER_FILE);
@@ -2235,6 +2720,12 @@ void acpi_setup(void)
                            tables.vmgenid);
     }
 
+    /*
+     * 在以下使用acpi_build_update():
+     *   - hw/i386/acpi-build.c|2222| <<acpi_setup>> build_state->table_mr = acpi_add_rom_blob(acpi_build_update,
+     *   - hw/i386/acpi-build.c|2228| <<acpi_setup>> acpi_add_rom_blob(acpi_build_update, build_state,
+     **   - hw/i386/acpi-build.c|2253| <<acpi_setup>> build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
+     */
     build_state->rsdp_mr = acpi_add_rom_blob(acpi_build_update,
                                              build_state, tables.rsdp,
                                              ACPI_BUILD_RSDP_FILE);
@@ -2253,6 +2744,12 @@ GArray *acpi_build_madt_standalone(MachineState *machine)
 {
   X86MachineState *x86ms = X86_MACHINE(machine);
   GArray *table = g_array_new(false, true, 1);
+  /*
+   * 在以下使用acpi_build_madt():
+   *   - hw/i386/acpi-build.c|2179| <<acpi_build>> acpi_build_madt(tables_blob, tables->linker, x86ms,
+   *   - hw/i386/acpi-build.c|2609| <<acpi_build_madt_standalone>> acpi_build_madt(table, NULL, x86ms, x86ms->oem_id,
+   *   - hw/i386/acpi-microvm.c|205| <<acpi_build_microvm>> acpi_build_madt(tables_blob, tables->linker, X86_MACHINE(machine),
+   */
   acpi_build_madt(table, NULL, x86ms, x86ms->oem_id,
                   x86ms->oem_table_id);
   return table;
diff --git a/hw/i386/acpi-common.c b/hw/i386/acpi-common.c
index 7bd08067a..d88e3daa4 100644
--- a/hw/i386/acpi-common.c
+++ b/hw/i386/acpi-common.c
@@ -92,6 +92,14 @@ build_xrupt_override(GArray *entry, uint8_t src, uint32_t gsi, uint16_t flags)
  * ACPI spec, Revision 1.0b
  * 5.2.8 Multiple APIC Description Table
  */
+/*
+ * Signature : "APIC"    [Multiple APIC Description Table (MADT)]
+ *
+ * 在以下使用acpi_build_madt():
+ *   - hw/i386/acpi-build.c|2179| <<acpi_build>> acpi_build_madt(tables_blob, tables->linker, x86ms,
+ *   - hw/i386/acpi-build.c|2609| <<acpi_build_madt_standalone>> acpi_build_madt(table, NULL, x86ms, x86ms->oem_id,
+ *   - hw/i386/acpi-microvm.c|205| <<acpi_build_microvm>> acpi_build_madt(tables_blob, tables->linker, X86_MACHINE(machine),
+ */
 void acpi_build_madt(GArray *table_data, BIOSLinker *linker,
                      X86MachineState *x86ms,
                      const char *oem_id, const char *oem_table_id)
diff --git a/hw/i386/acpi-microvm.c b/hw/i386/acpi-microvm.c
index bc6571778..8e199e65d 100644
--- a/hw/i386/acpi-microvm.c
+++ b/hw/i386/acpi-microvm.c
@@ -187,6 +187,20 @@ static void acpi_build_microvm(AcpiBuildTables *tables,
 
     table_offsets = g_array_new(false, true /* clear */,
                                         sizeof(uint32_t));
+    /*
+     * 在以下使用bios_linker_loader_alloc():
+     *   - hw/acpi/aml-build.c|1839| <<build_rsdp>> bios_linker_loader_alloc(linker, ACPI_BUILD_RSDP_FILE, tbl, 16, true);
+     *   - hw/acpi/aml-build.c|2535| <<build_tpm2>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/acpi/ghes.c|270| <<build_ghes_error_table>> bios_linker_loader_alloc(linker, ACPI_HW_ERROR_FW_CFG_FILE, hardware_errors, sizeof(uint64_t), false);
+     *   - hw/acpi/nvdimm.c|1416| <<nvdimm_build_ssdt>> bios_linker_loader_alloc(linker, NVDIMM_DSM_MEM_FILE, nvdimm_state->dsm_mem, sizeof(NvdimmDsmIn), false);
+     *   - hw/acpi/vmgenid.c|97| <<vmgenid_build_acpi>> bios_linker_loader_alloc(linker, VMGENID_GUID_FW_CFG_FILE, guid, 4096, false);
+     *   - hw/arm/virt-acpi-build.c|1262| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-build.c|1443| <<build_tpm_tcpa>> bios_linker_loader_alloc(linker, ACPI_BUILD_TPMLOG_FILE, tcpalog, 1, false);
+     *   - hw/i386/acpi-build.c|2145| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/i386/acpi-microvm.c|190| <<acpi_build_microvm>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/loongarch/virt-acpi-build.c|513| <<acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     *   - hw/riscv/virt-acpi-build.c|868| <<virt_acpi_build>> bios_linker_loader_alloc(tables->linker, ACPI_BUILD_TABLE_FILE, tables_blob, 64, false);
+     */
     bios_linker_loader_alloc(tables->linker,
                              ACPI_BUILD_TABLE_FILE, tables_blob,
                              64 /* Ensure FACS is aligned */,
@@ -202,6 +216,12 @@ static void acpi_build_microvm(AcpiBuildTables *tables,
                x86ms->oem_table_id);
 
     acpi_add_table(table_offsets, tables_blob);
+    /*
+     * 在以下使用acpi_build_madt():
+     *   - hw/i386/acpi-build.c|2179| <<acpi_build>> acpi_build_madt(tables_blob, tables->linker, x86ms,
+     *   - hw/i386/acpi-build.c|2609| <<acpi_build_madt_standalone>> acpi_build_madt(table, NULL, x86ms, x86ms->oem_id,
+     *   - hw/i386/acpi-microvm.c|205| <<acpi_build_microvm>> acpi_build_madt(tables_blob, tables->linker, X86_MACHINE(machine),
+     */
     acpi_build_madt(tables_blob, tables->linker, X86_MACHINE(machine),
                     x86ms->oem_id, x86ms->oem_table_id);
 
@@ -211,6 +231,11 @@ static void acpi_build_microvm(AcpiBuildTables *tables,
         erst_dev = find_erst_dev();
         if (erst_dev) {
             acpi_add_table(table_offsets, tables_blob);
+            /*
+	     * 在以下使用build_erst():
+             *   - hw/i386/acpi-build.c|2188| <<acpi_build>> build_erst(tables_blob, tables->linker, erst_dev,
+             *   - hw/i386/acpi-microvm.c|214| <<acpi_build_microvm>> build_erst(tables_blob, tables->linker, erst_dev,
+	     */
             build_erst(tables_blob, tables->linker, erst_dev,
                        x86ms->oem_id, x86ms->oem_table_id);
         }
diff --git a/hw/i386/amd_iommu.c b/hw/i386/amd_iommu.c
index 789e09d6f..43a78ea39 100644
--- a/hw/i386/amd_iommu.c
+++ b/hw/i386/amd_iommu.c
@@ -1463,6 +1463,12 @@ static void amdvi_cmdbuf_exec(AMDVIState *s)
     }
 }
 
+/*
+ * 在以下使用amdvi_cmdbuf_run():
+ *   - hw/i386/amd_iommu.c|1563| <<amdvi_handle_control_write>> amdvi_cmdbuf_run(s);
+ *   - hw/i386/amd_iommu.c|1582| <<amdvi_handle_cmdhead_write>> amdvi_cmdbuf_run(s);
+ *   - hw/i386/amd_iommu.c|1598| <<amdvi_handle_cmdtail_write>> amdvi_cmdbuf_run(s);
+ */
 static void amdvi_cmdbuf_run(AMDVIState *s)
 {
     if (!s->cmdbuf_enabled) {
@@ -1560,6 +1566,12 @@ static void amdvi_handle_control_write(AMDVIState *s)
     }
 
     trace_amdvi_control_status(control);
+    /*
+     * 在以下使用amdvi_cmdbuf_run():
+     *   - hw/i386/amd_iommu.c|1563| <<amdvi_handle_control_write>> amdvi_cmdbuf_run(s);
+     *   - hw/i386/amd_iommu.c|1582| <<amdvi_handle_cmdhead_write>> amdvi_cmdbuf_run(s);
+     *   - hw/i386/amd_iommu.c|1598| <<amdvi_handle_cmdtail_write>> amdvi_cmdbuf_run(s);
+     */
     amdvi_cmdbuf_run(s);
 }
 
@@ -1575,10 +1587,20 @@ static inline void amdvi_handle_devtab_write(AMDVIState *s)
                      AMDVI_MMIO_DEVTAB_ENTRY_SIZE));
 }
 
+/*
+ * 在以下使用amdvi_handle_cmdhead_write():
+ *   - hw/i386/amd_iommu.c|1705| <<amdvi_mmio_write(AMDVI_MMIO_COMMAND_HEAD)>> amdvi_handle_cmdhead_write(s);
+ */
 static inline void amdvi_handle_cmdhead_write(AMDVIState *s)
 {
     s->cmdbuf_head = amdvi_readq(s, AMDVI_MMIO_COMMAND_HEAD)
                      & AMDVI_MMIO_CMDBUF_HEAD_MASK;
+    /*
+     * 在以下使用amdvi_cmdbuf_run():
+     *   - hw/i386/amd_iommu.c|1563| <<amdvi_handle_control_write>> amdvi_cmdbuf_run(s);
+     *   - hw/i386/amd_iommu.c|1582| <<amdvi_handle_cmdhead_write>> amdvi_cmdbuf_run(s);
+     *   - hw/i386/amd_iommu.c|1598| <<amdvi_handle_cmdtail_write>> amdvi_cmdbuf_run(s);
+     */
     amdvi_cmdbuf_run(s);
 }
 
@@ -1591,10 +1613,29 @@ static inline void amdvi_handle_cmdbase_write(AMDVIState *s)
     s->cmdbuf_head = s->cmdbuf_tail = 0;
 }
 
+/*
+ * 在以下使用amdvi_handle_cmdtail_write():
+ *   - hw/i386/amd_iommu.c|1719| <<amdvi_mmio_write(AMDVI_MMIO_COMMAND_TAIL)>> amdvi_handle_cmdtail_write(s);
+ */
 static inline void amdvi_handle_cmdtail_write(AMDVIState *s)
 {
+    /*
+     * ring 的大小是 cmdbuf_len * 16 字节, head 每次加
+     * AMDVI_COMMAND_SIZE，到末尾就回绕到 0.
+     * 但以前 guest 往 COMMAND_TAIL 或 COMMAND_HEAD MMIO 寄存器里写值时,
+     * 只做了寄存器位掩码, 没有再限制到 ring buffer 实际大小范围内.
+     * 如果 guest 写了一个超出 ring 范围的 tail, head 只会在合法范围内循环回绕,
+     * 而 tail 永远是非法大值, 于是 while (head != tail) 永远退出不了,
+     * 形成死循环.
+     */
     s->cmdbuf_tail = amdvi_readq(s, AMDVI_MMIO_COMMAND_TAIL)
                      & AMDVI_MMIO_CMDBUF_TAIL_MASK;
+    /*
+     * 在以下使用amdvi_cmdbuf_run():
+     *   - hw/i386/amd_iommu.c|1563| <<amdvi_handle_control_write>> amdvi_cmdbuf_run(s);
+     *   - hw/i386/amd_iommu.c|1582| <<amdvi_handle_cmdhead_write>> amdvi_cmdbuf_run(s);
+     *   - hw/i386/amd_iommu.c|1598| <<amdvi_handle_cmdtail_write>> amdvi_cmdbuf_run(s);
+     */
     amdvi_cmdbuf_run(s);
 }
 
diff --git a/hw/i386/intel_iommu.c b/hw/i386/intel_iommu.c
index f395fa248..455e72c2d 100644
--- a/hw/i386/intel_iommu.c
+++ b/hw/i386/intel_iommu.c
@@ -4355,6 +4355,11 @@ static bool vtd_remap_irq_get(IntelIOMMUState *iommu, uint16_t index,
     return true;
 }
 
+/*
+ * 在以下使用vtd_interrupt_remap_msi():
+ *   - hw/i386/intel_iommu.c|4465| <<vtd_int_remap>> return vtd_interrupt_remap_msi(INTEL_IOMMU_DEVICE(iommu),
+ *   - hw/i386/intel_iommu.c|4532| <<vtd_mem_ir_write>> ret = vtd_interrupt_remap_msi(s, &from, &to, sid, true);
+ */
 /* Interrupt remapping for MSI/MSI-X entry */
 static int vtd_interrupt_remap_msi(IntelIOMMUState *iommu,
                                    MSIMessage *origin,
@@ -4462,6 +4467,11 @@ out:
 static int vtd_int_remap(X86IOMMUState *iommu, MSIMessage *src,
                          MSIMessage *dst, uint16_t sid)
 {
+    /*
+     * 在以下使用vtd_interrupt_remap_msi():
+     *   - hw/i386/intel_iommu.c|4465| <<vtd_int_remap>> return vtd_interrupt_remap_msi(INTEL_IOMMU_DEVICE(iommu),
+     *   - hw/i386/intel_iommu.c|4532| <<vtd_mem_ir_write>> ret = vtd_interrupt_remap_msi(s, &from, &to, sid, true);
+     */
     return vtd_interrupt_remap_msi(INTEL_IOMMU_DEVICE(iommu),
                                    src, dst, sid, false);
 }
@@ -4529,6 +4539,11 @@ static MemTxResult vtd_mem_ir_write(void *opaque, hwaddr addr,
         }
     }
 
+    /*
+     * 在以下使用vtd_interrupt_remap_msi():
+     *   - hw/i386/intel_iommu.c|4465| <<vtd_int_remap>> return vtd_interrupt_remap_msi(INTEL_IOMMU_DEVICE(iommu),
+     *   - hw/i386/intel_iommu.c|4532| <<vtd_mem_ir_write>> ret = vtd_interrupt_remap_msi(s, &from, &to, sid, true);
+     */
     ret = vtd_interrupt_remap_msi(s, &from, &to, sid, true);
     if (ret) {
         /* Drop this interrupt */
diff --git a/hw/i386/kvm/i8259.c b/hw/i386/kvm/i8259.c
index 66f37e130..928168230 100644
--- a/hw/i386/kvm/i8259.c
+++ b/hw/i386/kvm/i8259.c
@@ -111,6 +111,10 @@ static void kvm_pic_reset(DeviceState *dev)
     kvm_pic_put(s);
 }
 
+/*
+ * 在以下使用kvm_pic_set_irq():
+ *   - hw/i386/kvm/i8259.c|139| <<kvm_i8259_init>> return qemu_allocate_irqs(kvm_pic_set_irq, NULL, ISA_NUM_IRQS);
+ */
 static void kvm_pic_set_irq(void *opaque, int irq, int level)
 {
     int delivered;
diff --git a/hw/i386/pc.c b/hw/i386/pc.c
index 4b53b5be4..1154f22bc 100644
--- a/hw/i386/pc.c
+++ b/hw/i386/pc.c
@@ -753,6 +753,36 @@ void pc_memory_init(PCMachineState *pcms,
 
     linux_boot = (machine->kernel_filename != NULL);
 
+    /*
+     * 在以下使用PCMachineClass->enforce_amd_1tb_hole:
+     *   - hw/i386/pc.c|762| <<pc_memory_init>> if (IS_AMD_CPU(&cpu->env) && pcmc->enforce_amd_1tb_hole) {
+     *   - hw/i386/pc.c|1646| <<pc_machine_class_init>> pcmc->enforce_amd_1tb_hole = true;
+     *   - hw/i386/pc_piix.c|561| <<pc_i440fx_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *   - hw/i386/pc_q35.c|493| <<pc_q35_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *
+     * enforce_amd_1tb_hole 是 QEMU x86 PC 机器类型里的一个兼容开关, 用来处理
+     * AMD IOMMU 在接近 1TiB 地址处保留的 HyperTransport GPA 区间.
+     *
+     * - AMD IOMMU 有一段不能用于 DMA map 的 guest physical address:
+     *   0xfd00000000 - 0xffffffffff，也就是 1TiB 前面的 12GiB.
+     * - 如果 vCPU 是 AMD, 并且 enforce_amd_1tb_hole = true, QEMU 会:
+     * 1. 如果可能使用到这段 HT 保留区, 就把 ram-above-4g 的起始地址挪到
+     * 0x10000000000, 也就是 1TiB 之后.
+     * 2. 如果 CPU 物理地址宽度达到 40 bit, 就通过 E820 把这段区域报告给 guest
+     * 为 E820_RESERVED.
+     *
+     * 这里的 40 bit 指 vCPU 暴露给 guest 的物理地址宽度, 不是随便的阈值.
+     * 2^40 = 0x10000000000 = 1 TiB
+     * AMD 这段 HyperTransport reserved GPA 是:
+     * 0xfd00000000 - 0xffffffffff
+     *
+     * 所以关系是:
+     *
+     * - 如果 phys_bits < 40, guest 物理地址空间根本到不了 1TiB 附近,
+     * 这段地址对 guest 不存在, 没必要写进 E820.
+     * - 如果 phys_bits >= 40, guest 能表达/访问到这段 GPA, 那么 firmware/BIOS
+     * 就应该通过 E820 告诉 guest OS: 这段不是 RAM, 是 reserved.
+     */
     /*
      * The HyperTransport range close to the 1T boundary is unique to AMD
      * hosts with IOMMUs enabled. Restrict the ram-above-4g relocation
@@ -1643,6 +1673,36 @@ static void pc_machine_class_init(ObjectClass *oc, const void *data)
     pcmc->smbios_defaults = true;
     pcmc->gigabyte_align = true;
     pcmc->has_reserved_memory = true;
+    /*
+     * 在以下使用PCMachineClass->enforce_amd_1tb_hole:
+     *   - hw/i386/pc.c|762| <<pc_memory_init>> if (IS_AMD_CPU(&cpu->env) && pcmc->enforce_amd_1tb_hole) {
+     *   - hw/i386/pc.c|1646| <<pc_machine_class_init>> pcmc->enforce_amd_1tb_hole = true;
+     *   - hw/i386/pc_piix.c|561| <<pc_i440fx_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *   - hw/i386/pc_q35.c|493| <<pc_q35_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *
+     * enforce_amd_1tb_hole 是 QEMU x86 PC 机器类型里的一个兼容开关, 用来处理
+     * AMD IOMMU 在接近 1TiB 地址处保留的 HyperTransport GPA 区间.
+     *
+     * - AMD IOMMU 有一段不能用于 DMA map 的 guest physical address:
+     *   0xfd00000000 - 0xffffffffff，也就是 1TiB 前面的 12GiB.
+     * - 如果 vCPU 是 AMD, 并且 enforce_amd_1tb_hole = true, QEMU 会:
+     * 1. 如果可能使用到这段 HT 保留区, 就把 ram-above-4g 的起始地址挪到
+     * 0x10000000000, 也就是 1TiB 之后.
+     * 2. 如果 CPU 物理地址宽度达到 40 bit, 就通过 E820 把这段区域报告给 guest
+     * 为 E820_RESERVED.
+     *
+     * 这里的 40 bit 指 vCPU 暴露给 guest 的物理地址宽度, 不是随便的阈值.
+     * 2^40 = 0x10000000000 = 1 TiB
+     * AMD 这段 HyperTransport reserved GPA 是:
+     * 0xfd00000000 - 0xffffffffff
+     *
+     * 所以关系是:
+     *
+     * - 如果 phys_bits < 40, guest 物理地址空间根本到不了 1TiB 附近,
+     * 这段地址对 guest 不存在, 没必要写进 E820.
+     * - 如果 phys_bits >= 40, guest 能表达/访问到这段 GPA, 那么 firmware/BIOS
+     * 就应该通过 E820 告诉 guest OS: 这段不是 RAM, 是 reserved.
+     */
     pcmc->enforce_amd_1tb_hole = true;
     pcmc->isa_bios_alias = true;
     pcmc->kvmclock_create_always = true;
diff --git a/hw/i386/pc_piix.c b/hw/i386/pc_piix.c
index 4d71e0d51..ff830916d 100644
--- a/hw/i386/pc_piix.c
+++ b/hw/i386/pc_piix.c
@@ -558,6 +558,36 @@ static void pc_i440fx_machine_7_0_options(MachineClass *m)
 {
     PCMachineClass *pcmc = PC_MACHINE_CLASS(m);
     pc_i440fx_machine_7_1_options(m);
+    /*
+     * 在以下使用PCMachineClass->enforce_amd_1tb_hole:
+     *   - hw/i386/pc.c|762| <<pc_memory_init>> if (IS_AMD_CPU(&cpu->env) && pcmc->enforce_amd_1tb_hole) {
+     *   - hw/i386/pc.c|1646| <<pc_machine_class_init>> pcmc->enforce_amd_1tb_hole = true;
+     *   - hw/i386/pc_piix.c|561| <<pc_i440fx_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *   - hw/i386/pc_q35.c|493| <<pc_q35_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *
+     * enforce_amd_1tb_hole 是 QEMU x86 PC 机器类型里的一个兼容开关, 用来处理
+     * AMD IOMMU 在接近 1TiB 地址处保留的 HyperTransport GPA 区间.
+     *
+     * - AMD IOMMU 有一段不能用于 DMA map 的 guest physical address:
+     *   0xfd00000000 - 0xffffffffff，也就是 1TiB 前面的 12GiB.
+     * - 如果 vCPU 是 AMD, 并且 enforce_amd_1tb_hole = true, QEMU 会:
+     * 1. 如果可能使用到这段 HT 保留区, 就把 ram-above-4g 的起始地址挪到
+     * 0x10000000000, 也就是 1TiB 之后.
+     * 2. 如果 CPU 物理地址宽度达到 40 bit, 就通过 E820 把这段区域报告给 guest
+     * 为 E820_RESERVED.
+     *
+     * 这里的 40 bit 指 vCPU 暴露给 guest 的物理地址宽度, 不是随便的阈值.
+     * 2^40 = 0x10000000000 = 1 TiB
+     * AMD 这段 HyperTransport reserved GPA 是:
+     * 0xfd00000000 - 0xffffffffff
+     *
+     * 所以关系是:
+     *
+     * - 如果 phys_bits < 40, guest 物理地址空间根本到不了 1TiB 附近,
+     * 这段地址对 guest 不存在, 没必要写进 E820.
+     * - 如果 phys_bits >= 40, guest 能表达/访问到这段 GPA, 那么 firmware/BIOS
+     * 就应该通过 E820 告诉 guest OS: 这段不是 RAM, 是 reserved.
+     */
     pcmc->enforce_amd_1tb_hole = false;
     compat_props_add(m->compat_props, hw_compat_7_0, hw_compat_7_0_len);
     compat_props_add(m->compat_props, pc_compat_7_0, pc_compat_7_0_len);
diff --git a/hw/i386/pc_q35.c b/hw/i386/pc_q35.c
index cb23322f5..fd1f7cb52 100644
--- a/hw/i386/pc_q35.c
+++ b/hw/i386/pc_q35.c
@@ -490,6 +490,36 @@ static void pc_q35_machine_7_0_options(MachineClass *m)
 {
     PCMachineClass *pcmc = PC_MACHINE_CLASS(m);
     pc_q35_machine_7_1_options(m);
+    /*
+     * 在以下使用PCMachineClass->enforce_amd_1tb_hole:
+     *   - hw/i386/pc.c|762| <<pc_memory_init>> if (IS_AMD_CPU(&cpu->env) && pcmc->enforce_amd_1tb_hole) {
+     *   - hw/i386/pc.c|1646| <<pc_machine_class_init>> pcmc->enforce_amd_1tb_hole = true;
+     *   - hw/i386/pc_piix.c|561| <<pc_i440fx_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *   - hw/i386/pc_q35.c|493| <<pc_q35_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     * 
+     * enforce_amd_1tb_hole 是 QEMU x86 PC 机器类型里的一个兼容开关, 用来处理
+     * AMD IOMMU 在接近 1TiB 地址处保留的 HyperTransport GPA 区间.
+     *
+     * - AMD IOMMU 有一段不能用于 DMA map 的 guest physical address:
+     *   0xfd00000000 - 0xffffffffff，也就是 1TiB 前面的 12GiB.
+     * - 如果 vCPU 是 AMD, 并且 enforce_amd_1tb_hole = true, QEMU 会:
+     * 1. 如果可能使用到这段 HT 保留区, 就把 ram-above-4g 的起始地址挪到
+     * 0x10000000000, 也就是 1TiB 之后.
+     * 2. 如果 CPU 物理地址宽度达到 40 bit, 就通过 E820 把这段区域报告给 guest
+     * 为 E820_RESERVED.
+     *
+     * 这里的 40 bit 指 vCPU 暴露给 guest 的物理地址宽度, 不是随便的阈值.
+     * 2^40 = 0x10000000000 = 1 TiB
+     * AMD 这段 HyperTransport reserved GPA 是:
+     * 0xfd00000000 - 0xffffffffff
+     *
+     * 所以关系是:
+     *
+     * - 如果 phys_bits < 40, guest 物理地址空间根本到不了 1TiB 附近,
+     * 这段地址对 guest 不存在, 没必要写进 E820.
+     * - 如果 phys_bits >= 40, guest 能表达/访问到这段 GPA, 那么 firmware/BIOS
+     * 就应该通过 E820 告诉 guest OS: 这段不是 RAM, 是 reserved.
+     */
     pcmc->enforce_amd_1tb_hole = false;
     compat_props_add(m->compat_props, hw_compat_7_0, hw_compat_7_0_len);
     compat_props_add(m->compat_props, pc_compat_7_0, pc_compat_7_0_len);
diff --git a/hw/i386/x86-common.c b/hw/i386/x86-common.c
index fde05fa7d..060f912c7 100644
--- a/hw/i386/x86-common.c
+++ b/hw/i386/x86-common.c
@@ -454,8 +454,70 @@ static long get_file_size(FILE *f)
     return size;
 }
 
+/*
+ * (gdb) bt  
+ * #0  pic_irq_request
+ * #1  0x0000555555f33a35 in qemu_set_irq
+ * #2  0x0000555555a503a3 in qemu_irq_lower
+ * #3  0x0000555555a50930 in pic_update_irq
+ * #4  0x0000555555a50aea in pic_set_irq
+ * #5  0x0000555555f33a35 in qemu_set_irq
+ * #6  0x0000555555e25c2a in gsi_handler
+ * #7  0x0000555555f33a35 in qemu_set_irq
+ * #8  0x00005555559a1691 in qemu_irq_lower
+ * #9  0x00005555559a19eb in serial_update_irq
+ * #10 0x00005555559a26af in serial_ioport_write
+ * #11 0x0000555555c75e3d in memory_region_write_accessor
+ * #12 0x0000555555c7618f in access_with_adjusted_size
+ * #13 0x0000555555c79952 in memory_region_dispatch_write
+ * #14 0x0000555555c8b943 in flatview_write_continue_step
+ * #15 0x0000555555c8ba18 in flatview_write_continue
+ * #16 0x0000555555c8bb45 in flatview_write
+ * #17 0x0000555555c8bfd2 in address_space_write
+ * #18 0x0000555555c8c04c in address_space_rw
+ * #19 0x0000555555efc2c1 in kvm_handle_io
+ * #20 0x0000555555efd249 in kvm_cpu_exec
+ * #21 0x0000555555f009e5 in kvm_vcpu_thread_fn
+ * #22 0x0000555556153363 in qemu_thread_start
+ * #23 0x00007ffff62a91da in start_thread
+ * #24 0x00007ffff5f038d3 in clone
+ *
+ * (gdb) bt
+ * #0  qemu_set_irq
+ * #1  0x00005555559abedf in qemu_irq_raise
+ * #2  0x00005555559ac24a in serial_update_irq
+ * #3  0x00005555559acf23 in serial_ioport_write
+ * #4  0x0000555555c806e4 in memory_region_write_accessor
+ * #5  0x0000555555c80a36 in access_with_adjusted_size
+ * #6  0x0000555555c841f9 in memory_region_dispatch_write
+ * #7  0x0000555555c961ea in flatview_write_continue_step
+ * #8  0x0000555555c962bf in flatview_write_continue
+ * #9  0x0000555555c963ec in flatview_write
+ * #10 0x0000555555c96879 in address_space_write 
+ * #11 0x0000555555c968f3 in address_space_rw
+ * #12 0x0000555555f07101 in kvm_handle_io
+ * #13 0x0000555555f08089 in kvm_cpu_exec
+ * #14 0x0000555555f0b825 in kvm_vcpu_thread_fn
+ * #15 0x000055555616bcab in qemu_thread_start
+ * #16 0x00007ffff6c9a1da in start_thread
+ * #17 0x00007ffff4fcb8d3 in clone
+ *
+ * 在以下使用gsi_handler():
+ *   - hw/i386/microvm.c|172| <<microvm_devices_init>> x86ms->gsi = qemu_allocate_irqs(gsi_handler, gsi_state,
+ *   - hw/i386/pc.c|199| <<pc_gsi_create>> *irqs = qemu_allocate_irqs(gsi_handler, s, IOAPIC_NUM_PINS);
+ *
+ * ttyS0 isa-serial的例子:
+ * gsi_handler (opaque=0x555557ec9d80, n=4, level=1)
+ */
 void gsi_handler(void *opaque, int n, int level)
 {
+    /*
+     * typedef struct GSIState {
+     *     qemu_irq i8259_irq[ISA_NUM_IRQS];
+     *     qemu_irq ioapic_irq[IOAPIC_NUM_PINS];
+     *     qemu_irq ioapic2_irq[IOAPIC_NUM_PINS];
+     * } GSIState;
+     */
     GSIState *s = opaque;
     bool bypass_ioapic = false;
 
@@ -481,6 +543,13 @@ void gsi_handler(void *opaque, int n, int level)
     switch (n) {
     case 0 ... ISA_NUM_IRQS - 1:
         if (s->i8259_irq[n]) {
+            /*
+	     * serial的例子:
+	     * (gdb) p *s->i8259_irq[4]
+	     * $5 = {parent_obj = {class = 0x555557716740, free = 0x555555897a90 <g_free@plt>,
+	     *   properties = 0x55555810e9e0, ref = 1, parent = 0x0},
+	     *   handler = 0x555555e6176a <kvm_pic_set_irq>, opaque = 0x0, n = 4}
+	     */
             /* Under KVM, Kernel will forward to both PIC and IOAPIC */
             qemu_set_irq(s->i8259_irq[n], level);
         }
diff --git a/hw/i386/x86-cpu.c b/hw/i386/x86-cpu.c
index 95e08e3c2..8e810be7f 100644
--- a/hw/i386/x86-cpu.c
+++ b/hw/i386/x86-cpu.c
@@ -37,6 +37,34 @@ uint64_t cpu_get_tsc(CPUX86State *env)
     return cpus_get_elapsed_ticks();
 }
 
+/*
+ * (gdb) bt
+ * #0  pic_irq_request
+ * #1  0x0000555555f33a35 in qemu_set_irq
+ * #2  0x0000555555a503a3 in qemu_irq_lower
+ * #3  0x0000555555a50930 in pic_update_irq
+ * #4  0x0000555555a50aea in pic_set_irq
+ * #5  0x0000555555f33a35 in qemu_set_irq
+ * #6  0x0000555555e25c2a in gsi_handler
+ * #7  0x0000555555f33a35 in qemu_set_irq
+ * #8  0x00005555559a1691 in qemu_irq_lower
+ * #9  0x00005555559a19eb in serial_update_irq
+ * #10 0x00005555559a26af in serial_ioport_write
+ * #11 0x0000555555c75e3d in memory_region_write_accessor
+ * #12 0x0000555555c7618f in access_with_adjusted_size
+ * #13 0x0000555555c79952 in memory_region_dispatch_write
+ * #14 0x0000555555c8b943 in flatview_write_continue_step
+ * #15 0x0000555555c8ba18 in flatview_write_continue
+ * #16 0x0000555555c8bb45 in flatview_write
+ * #17 0x0000555555c8bfd2 in address_space_write
+ * #18 0x0000555555c8c04c in address_space_rw
+ * #19 0x0000555555efc2c1 in kvm_handle_io
+ * #20 0x0000555555efd249 in kvm_cpu_exec
+ * #21 0x0000555555f009e5 in kvm_vcpu_thread_fn
+ * #22 0x0000555556153363 in qemu_thread_start
+ * #23 0x00007ffff62a91da in start_thread
+ * #24 0x00007ffff5f038d3 in clone
+ */
 /* IRQ handling */
 static void pic_irq_request(void *opaque, int irq, int level)
 {
diff --git a/hw/ide/ahci.c b/hw/ide/ahci.c
index 08f51c8e3..a92b3df7d 100644
--- a/hw/ide/ahci.c
+++ b/hw/ide/ahci.c
@@ -1027,6 +1027,13 @@ static void ncq_cb(void *opaque, int ret)
         } else if (action == BLOCK_ERROR_ACTION_REPORT) {
             ncq_err(ncq_tfs);
         }
+	/*
+	 * 在以下使用blk_error_action():
+         *  - hw/block/virtio-blk.c|94| <<virtio_blk_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+         *  - hw/ide/ahci.c|1030| <<ncq_cb>> blk_error_action(ide_state->blk, action, is_read, -ret);
+         *  - hw/ide/core.c|876| <<ide_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+         *  - hw/scsi/scsi-disk.c|300| <<scsi_handle_rw_error>> blk_error_action(s->qdev.conf.blk, action, is_read, error);
+	 */
         blk_error_action(ide_state->blk, action, is_read, -ret);
     } else {
         ide_state->status = READY_STAT | SEEK_STAT;
diff --git a/hw/ide/core.c b/hw/ide/core.c
index 7a15d6cac..c1dd0efec 100644
--- a/hw/ide/core.c
+++ b/hw/ide/core.c
@@ -873,6 +873,13 @@ int ide_handle_rw_error(IDEState *s, int error, int op)
             ide_rw_error(s);
         }
     }
+    /*
+     * 在以下使用blk_error_action():
+     *  - hw/block/virtio-blk.c|94| <<virtio_blk_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+     *  - hw/ide/ahci.c|1030| <<ncq_cb>> blk_error_action(ide_state->blk, action, is_read, -ret);
+     *  - hw/ide/core.c|876| <<ide_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+     *  - hw/scsi/scsi-disk.c|300| <<scsi_handle_rw_error>> blk_error_action(s->qdev.conf.blk, action, is_read, error);
+     */
     blk_error_action(s->blk, action, is_read, error);
     return action != BLOCK_ERROR_ACTION_IGNORE;
 }
diff --git a/hw/intc/ioapic_common.c b/hw/intc/ioapic_common.c
index d3bcd5699..ee2857c85 100644
--- a/hw/intc/ioapic_common.c
+++ b/hw/intc/ioapic_common.c
@@ -91,6 +91,10 @@ static void ioapic_print_redtbl(GString *buf, IOAPICCommonState *s)
         g_string_append_c(buf, '\n');
     }
     for (i = 0; i < IOAPIC_NUM_PINS; i++) {
+        /*
+	 * IOAPICCommonState *s:
+	 * -> uint64_t ioredtbl[IOAPIC_NUM_PINS];
+	 */
         uint64_t entry = s->ioredtbl[i];
         uint32_t delm = (uint32_t)((entry & IOAPIC_LVT_DELIV_MODE) >>
                                    IOAPIC_LVT_DELIV_MODE_SHIFT);
diff --git a/hw/isa/lpc_ich9.c b/hw/isa/lpc_ich9.c
index 51dc68002..ed51c8f23 100644
--- a/hw/isa/lpc_ich9.c
+++ b/hw/isa/lpc_ich9.c
@@ -464,6 +464,13 @@ static void ich9_apm_ctrl_changed(uint32_t val, void *arg)
 {
     ICH9LPCState *lpc = arg;
 
+    /*
+     * 在以下使用acpi_pm1_cnt_update():
+     *   - hw/acpi/piix4.c|75| <<apm_ctrl_changed>> acpi_pm1_cnt_update(&s->ar,
+     *            val == ACPI_ENABLE, val == ACPI_DISABLE);
+     *   - hw/isa/lpc_ich9.c|468| <<ich9_apm_ctrl_changed>> acpi_pm1_cnt_update(&lpc->pm.acpi_regs,
+     *            val == ICH9_APM_ACPI_ENABLE, val == ICH9_APM_ACPI_DISABLE);
+     */
     /* ACPI specs 3.0, 4.7.2.5 */
     acpi_pm1_cnt_update(&lpc->pm.acpi_regs,
                         val == ICH9_APM_ACPI_ENABLE,
diff --git a/hw/net/igb.c b/hw/net/igb.c
index c076807e7..10d4db3bc 100644
--- a/hw/net/igb.c
+++ b/hw/net/igb.c
@@ -453,6 +453,13 @@ static void igb_pci_realize(PCIDevice *pci_dev, Error **errp)
         return;
     }
 
+    /*
+     * 在以下使用pcie_sriov_pf_init_vf_bar():
+     *   - hw/net/igb.c|456| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MMIO_BAR_IDX,
+     *   - hw/net/igb.c|459| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MSIX_BAR_IDX,
+     *   - hw/nvme/ctrl.c|8894| <<nvme_init_sriov>> pcie_sriov_pf_init_vf_bar(pci_dev, 0, PCI_BASE_ADDRESS_SPACE_MEMORY |
+     *   - hw/pci/pcie_sriov.c|351| <<pcie_sriov_pf_init_from_user_created_vfs>> pcie_sriov_pf_init_vf_bar(dev, i, region->type, region->size);
+     */
     pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MMIO_BAR_IDX,
         PCI_BASE_ADDRESS_MEM_TYPE_64 | PCI_BASE_ADDRESS_MEM_PREFETCH,
         IGBVF_MMIO_SIZE);
diff --git a/hw/net/igb_core.c b/hw/net/igb_core.c
index 45d8fd795..dcb9745ef 100644
--- a/hw/net/igb_core.c
+++ b/hw/net/igb_core.c
@@ -120,6 +120,14 @@ static void igb_msix_notify(IGBCore *core, unsigned int cause)
     unsigned int vector;
 
     vfn = 8 - (cause + 2) / IGBVF_MSIX_VEC_NUM;
+    /*
+     * 在以下使用pcie_sriov_num_vfs():
+     *   - hw/net/igb_core.c|123| <<igb_msix_notify>> if (vfn < pcie_sriov_num_vfs(core->owner)) {
+     *   - hw/net/igb_core.c|2956| <<igb_get_status>> uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
+     *   - hw/nvme/ctrl.c|9473| <<nvme_sriov_post_write_config>> for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
+     *   - hw/nvme/ctrl.c|9482| <<nvme_pci_write_config>> uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
+     *   - hw/pci/pcie_sriov.c|505| <<pcie_sriov_get_vf_at_index>> if (n < pcie_sriov_num_vfs(dev)) {
+     */
     if (vfn < pcie_sriov_num_vfs(core->owner)) {
         dev = pcie_sriov_get_vf_at_index(core->owner, vfn);
         assert(dev);
@@ -2953,6 +2961,14 @@ igb_get_ctrl(IGBCore *core, int index)
 static uint32_t igb_get_status(IGBCore *core, int index)
 {
     uint32_t res = core->mac[STATUS];
+    /*
+     * 在以下使用pcie_sriov_num_vfs():
+     *   - hw/net/igb_core.c|123| <<igb_msix_notify>> if (vfn < pcie_sriov_num_vfs(core->owner)) {
+     *   - hw/net/igb_core.c|2956| <<igb_get_status>> uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
+     *   - hw/nvme/ctrl.c|9473| <<nvme_sriov_post_write_config>> for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
+     *   - hw/nvme/ctrl.c|9482| <<nvme_pci_write_config>> uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
+     *   - hw/pci/pcie_sriov.c|505| <<pcie_sriov_get_vf_at_index>> if (n < pcie_sriov_num_vfs(dev)) {
+     */
     uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
 
     if (core->mac[CTRL] & E1000_CTRL_FRCDPX) {
diff --git a/hw/net/net_tx_pkt.c b/hw/net/net_tx_pkt.c
index 903238dca..3b517e220 100644
--- a/hw/net/net_tx_pkt.c
+++ b/hw/net/net_tx_pkt.c
@@ -715,6 +715,10 @@ static void net_tx_pkt_udp_fragment_fix(struct NetTxPkt *pkt,
     eth_fix_ip4_checksum(l3hdr->iov_base, l3hdr->iov_len);
 }
 
+/*
+ * 在以下使用net_tx_pkt_do_sw_fragmentation():
+ *   - hw/net/net_tx_pkt.c|850| <<net_tx_pkt_send_custom>> return net_tx_pkt_do_sw_fragmentation(pkt, callback, context);
+ */
 static bool net_tx_pkt_do_sw_fragmentation(struct NetTxPkt *pkt,
                                            NetTxPktSend callback,
                                            void *context)
diff --git a/hw/net/virtio-net.c b/hw/net/virtio-net.c
index 2a5d642a6..2785baea7 100644
--- a/hw/net/virtio-net.c
+++ b/hw/net/virtio-net.c
@@ -1270,6 +1270,12 @@ static void virtio_net_detach_ebpf_rss(VirtIONet *n)
     virtio_net_attach_ebpf_to_backend(n->nic, -1);
 }
 
+/*
+ * 在以下使用virtio_net_commit_rss_config():
+ *   - hw/net/virtio-net.c|1309| <<virtio_net_disable_rss>> virtio_net_commit_rss_config(n);
+ *   - hw/net/virtio-net.c|1507| <<virtio_net_handle_rss>> virtio_net_commit_rss_config(n);
+ *   - hw/net/virtio-net.c|3338| <<virtio_net_post_load_device>> virtio_net_commit_rss_config(n);
+ */
 static void virtio_net_commit_rss_config(VirtIONet *n)
 {
     if (n->rss_data.peer_hash_available) {
@@ -1299,6 +1305,13 @@ static void virtio_net_commit_rss_config(VirtIONet *n)
     }
 }
 
+/*
+ * 在以下使用virtio_net_disable_rss():
+ *   - hw/net/virtio-net.c|1511| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+ *   - hw/net/virtio-net.c|1533| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+ *   - hw/net/virtio-net.c|1548| <<virtio_net_handle_mq>> virtio_net_disable_rss(n);
+ *   - hw/net/virtio-net.c|4249| <<virtio_net_reset>> virtio_net_disable_rss(n);
+ */
 static void virtio_net_disable_rss(VirtIONet *n)
 {
     if (!n->rss_data.enabled) {
@@ -1306,6 +1319,12 @@ static void virtio_net_disable_rss(VirtIONet *n)
     }
 
     n->rss_data.enabled = false;
+    /*
+     * 在以下使用virtio_net_commit_rss_config():
+     *   - hw/net/virtio-net.c|1309| <<virtio_net_disable_rss>> virtio_net_commit_rss_config(n);
+     *   - hw/net/virtio-net.c|1507| <<virtio_net_handle_rss>> virtio_net_commit_rss_config(n);
+     *   - hw/net/virtio-net.c|3338| <<virtio_net_post_load_device>> virtio_net_commit_rss_config(n);
+     */
     virtio_net_commit_rss_config(n);
 }
 
@@ -1374,6 +1393,11 @@ static void virtio_net_unload_ebpf(VirtIONet *n)
     ebpf_rss_unload(&n->ebpf_rss);
 }
 
+/*
+ * 在以下使用virtio_net_handle_rss():
+ *   - hw/net/virtio-net.c|1506| <<virtio_net_handle_mq>> queue_pairs = virtio_net_handle_rss(n, iov, iov_cnt, false);
+ *   - hw/net/virtio-net.c|1510| <<virtio_net_handle_mq>> queue_pairs = virtio_net_handle_rss(n, iov, iov_cnt, true);
+ */
 static uint16_t virtio_net_handle_rss(VirtIONet *n,
                                       struct iovec *iov,
                                       unsigned int iov_cnt,
@@ -1399,12 +1423,29 @@ static uint16_t virtio_net_handle_rss(VirtIONet *n,
         goto error;
     }
     size_get = offsetof(struct virtio_net_rss_config, indirection_table);
+    /*
+     * struct virtio_net_rss_config {
+     *     uint32_t hash_types;
+     *     uint16_t indirection_table_mask;
+     *     uint16_t unclassified_queue;
+     *     uint16_t indirection_table[1// + indirection_table_mask];
+     *     uint16_t max_tx_vq;
+     *     uint8_t hash_key_length;
+     *     uint8_t hash_key_data[// hash_key_length];
+     * };
+     *
+     * struct virtio_net_rss_config cfg;
+     */
     s = iov_to_buf(iov, iov_cnt, offset, &cfg, size_get);
     if (s != size_get) {
         err_msg = "Short command buffer";
         err_value = (uint32_t)s;
         goto error;
     }
+    /*
+     * VirtIONet *n:
+     * -> VirtioNetRssData rss_data;
+     */
     n->rss_data.runtime_hash_types = virtio_ldl_p(vdev, &cfg.hash_types);
     n->rss_data.indirections_len =
         virtio_lduw_p(vdev, &cfg.indirection_table_mask);
@@ -1474,6 +1515,13 @@ static uint16_t virtio_net_handle_rss(VirtIONet *n,
         goto error;
     }
     if (!temp.b && !n->rss_data.runtime_hash_types) {
+        /*
+	 * 在以下使用virtio_net_disable_rss():
+         *   - hw/net/virtio-net.c|1511| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+         *   - hw/net/virtio-net.c|1533| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+         *   - hw/net/virtio-net.c|1548| <<virtio_net_handle_mq>> virtio_net_disable_rss(n);
+         *   - hw/net/virtio-net.c|4249| <<virtio_net_reset>> virtio_net_disable_rss(n);
+	 */
         virtio_net_disable_rss(n);
         return queue_pairs;
     }
@@ -1486,14 +1534,31 @@ static uint16_t virtio_net_handle_rss(VirtIONet *n,
         goto error;
     }
     n->rss_data.enabled = true;
+    /*
+     * 在以下使用virtio_net_commit_rss_config():
+     *   - hw/net/virtio-net.c|1309| <<virtio_net_disable_rss>> virtio_net_commit_rss_config(n);
+     *   - hw/net/virtio-net.c|1507| <<virtio_net_handle_rss>> virtio_net_commit_rss_config(n);
+     *   - hw/net/virtio-net.c|3338| <<virtio_net_post_load_device>> virtio_net_commit_rss_config(n);
+     */
     virtio_net_commit_rss_config(n);
     return queue_pairs;
 error:
     trace_virtio_net_rss_error(n, err_msg, err_value);
+    /*
+     * 在以下使用virtio_net_disable_rss():
+     *   - hw/net/virtio-net.c|1511| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|1533| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|1548| <<virtio_net_handle_mq>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|4249| <<virtio_net_reset>> virtio_net_disable_rss(n);
+     */
     virtio_net_disable_rss(n);
     return 0;
 }
 
+/*
+ * 在以下使用virtio_net_handle_mq():
+ *   - hw/net/virtio-net.c|1581| <<virtio_net_handle_ctrl_iov>> status = virtio_net_handle_mq(n, ctrl.cmd, iov, out_num);
+ */
 static int virtio_net_handle_mq(VirtIONet *n, uint8_t cmd,
                                 struct iovec *iov, unsigned int iov_cnt)
 {
@@ -1501,6 +1566,13 @@ static int virtio_net_handle_mq(VirtIONet *n, uint8_t cmd,
     uint16_t queue_pairs;
     NetClientState *nc = qemu_get_queue(n->nic);
 
+    /*
+     * 在以下使用virtio_net_disable_rss():
+     *   - hw/net/virtio-net.c|1511| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|1533| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|1548| <<virtio_net_handle_mq>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|4249| <<virtio_net_reset>> virtio_net_disable_rss(n);
+     */
     virtio_net_disable_rss(n);
     if (cmd == VIRTIO_NET_CTRL_MQ_HASH_CONFIG) {
         queue_pairs = virtio_net_handle_rss(n, iov, iov_cnt, false);
@@ -1509,6 +1581,11 @@ static int virtio_net_handle_mq(VirtIONet *n, uint8_t cmd,
     if (cmd == VIRTIO_NET_CTRL_MQ_RSS_CONFIG) {
         queue_pairs = virtio_net_handle_rss(n, iov, iov_cnt, true);
     } else if (cmd == VIRTIO_NET_CTRL_MQ_VQ_PAIRS_SET) {
+        /*
+	 * struct virtio_net_ctrl_mq {
+	 *     __virtio16 virtqueue_pairs;
+	 * };
+	 */
         struct virtio_net_ctrl_mq mq;
         size_t s;
         if (!virtio_vdev_has_feature(vdev, VIRTIO_NET_F_MQ)) {
@@ -1547,6 +1624,11 @@ static int virtio_net_handle_mq(VirtIONet *n, uint8_t cmd,
     return VIRTIO_NET_OK;
 }
 
+/*
+ * 在以下使用virtio_net_handle_ctrl_iov():
+ *   - hw/net/virtio-net.c|1604| <<virtio_net_handle_ctrl>> written = virtio_net_handle_ctrl_iov(vdev, elem->in_sg, elem->in_num,
+ *   - net/vhost-vdpa.c|1564| <<vhost_vdpa_net_handle_ctrl_avail>> virtio_net_handle_ctrl_iov(svq->vdev, &model_in, 1, &out, 1);
+ */
 size_t virtio_net_handle_ctrl_iov(VirtIODevice *vdev,
                                   const struct iovec *in_sg, unsigned in_num,
                                   const struct iovec *out_sg,
@@ -1590,6 +1672,11 @@ size_t virtio_net_handle_ctrl_iov(VirtIODevice *vdev,
     return sizeof(status);
 }
 
+/*
+ * 在以下使用virtio_net_handle_ctrl():
+ *   - hw/net/virtio-net.c|3087| <<virtio_net_change_num_queues>> n->ctrl_vq = virtio_add_queue(vdev, 64, virtio_net_handle_ctrl);
+ *   - hw/net/virtio-net.c|4012| <<virtio_net_device_realize>> n->ctrl_vq = virtio_add_queue(vdev, 64, virtio_net_handle_ctrl);
+ */
 static void virtio_net_handle_ctrl(VirtIODevice *vdev, VirtQueue *vq)
 {
     VirtQueueElement *elem;
@@ -1601,6 +1688,22 @@ static void virtio_net_handle_ctrl(VirtIODevice *vdev, VirtQueue *vq)
             break;
         }
 
+        /*
+	 * - written > 0
+	 * 正常路径. QEMU 已经给 guest 的 in buffer 写了一个 status byte, 于是
+	 * virtqueue_push() 把这个请求作为"已完成"放回 used ring, 并通知 guest.
+	 * - written == 0
+	 * 异常路径. QEMU 没有成功产生一个有效回复, 所以不能 push. 这时只能
+	 * detach, 把 host 侧占着的资源释放掉.
+         *
+	 * virtqueue_push() = "这个请求处理完了, 回给 guest"
+	 * virtqueue_detach_element() = "这个请求我不再持有了, 但也不宣告完成"
+	 *
+	 *
+	 * 在以下使用virtio_net_handle_ctrl_iov():
+         *   - hw/net/virtio-net.c|1604| <<virtio_net_handle_ctrl>> written = virtio_net_handle_ctrl_iov(vdev, elem->in_sg, elem->in_num,
+         *   - net/vhost-vdpa.c|1564| <<vhost_vdpa_net_handle_ctrl_avail>> virtio_net_handle_ctrl_iov(svq->vdev, &model_in, 1, &out, 1);
+	 */
         written = virtio_net_handle_ctrl_iov(vdev, elem->in_sg, elem->in_num,
                                              elem->out_sg, elem->out_num);
         if (written > 0) {
@@ -1608,6 +1711,13 @@ static void virtio_net_handle_ctrl(VirtIODevice *vdev, VirtQueue *vq)
             virtio_notify(vdev, vq);
             g_free(elem);
         } else {
+            /*
+	     * detach_element() 本身不会唤醒前端; 如果没有后续 completion 或
+	     * broken 传播, 前端确实可能 hang.
+	     *
+	     * 如果这是可恢复错误, 应该用 unpop();如果这是不可恢复错误,
+	     * detach() 不回退 last_avail_idx 是对的.
+	     */
             virtqueue_detach_element(vq, elem, 0);
             g_free(elem);
             break;
@@ -1653,7 +1763,27 @@ static int virtio_net_has_buffers(VirtIONetQueue *q, int bufsize)
     unsigned int in_bytes;
     VirtIONet *n = q->n;
 
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
     while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+        /*
+	 * 在以下使用virtqueue_get_avail_bytes():
+         *   - hw/char/virtio-serial-bus.c|321| <<virtio_serial_guest_ready>> virtqueue_get_avail_bytes(vq, &bytes, NULL, 4096, 0);
+         *   - hw/net/virtio-net.c|1657| <<virtio_net_has_buffers>> opaque = virtqueue_get_avail_bytes(q->rx_vq, &in_bytes, NULL,
+         *   - hw/virtio/virtio-rng.c|40| <<get_request_size>> virtqueue_get_avail_bytes(vq, &in, &out, quota, 0);
+         *   - hw/virtio/virtio.c|1610| <<virtqueue_avail_bytes>> virtqueue_get_avail_bytes(vq, &in_total, &out_total, in_bytes, out_bytes);
+	 */
         opaque = virtqueue_get_avail_bytes(q->rx_vq, &in_bytes, NULL,
                                            bufsize, 0);
         /* Buffer is enough, disable notifiaction */
@@ -1901,6 +2031,27 @@ static int virtio_net_process_rss(NetClientState *nc, const uint8_t *buf,
     return (index == new_index) ? -1 : new_index;
 }
 
+/*
+ * virtio_net_receive_rcu() 是 QEMU virtio-net 的 RX 收包核心函数.
+ * 路径大概是:
+ *
+ * host backend 收到包
+ * -> net_virtio_info.receive = virtio_net_receive
+ *    -> virtio_net_do_receive()
+ *       -> virtio_net_receive_rcu()
+ *
+ * 1. 选 RX queue, RSS 开的话按 hash 选 subqueue.
+ * 2. 检查 guest 有没有可用 RX buffer.
+ * 3. 从 guest 的 RX virtqueue 里 virtqueue_pop() buffer.
+ * 4. 把 host 收到的包 buf 拷贝进 guest buffer.
+ * 5. virtqueue_fill() 标记这些 buffer 已经用完.
+ * 6. virtqueue_flush() 更新 used ring.
+ * 7. virtio_notify() 通知 guest: 你有包可以收了.
+ *
+ *
+ * 在以下使用virtio_net_receive_rcu():
+ *   - hw/net/virtio-net.c|2205| <<virtio_net_do_receive>> return virtio_net_receive_rcu(nc, buf, size);
+ */
 static ssize_t virtio_net_receive_rcu(NetClientState *nc, const uint8_t *buf,
                                       size_t size)
 {
@@ -2034,11 +2185,25 @@ static ssize_t virtio_net_receive_rcu(NetClientState *nc, const uint8_t *buf,
     }
 
     for (j = 0; j < i; j++) {
+        /*
+	 * 在以下使用virtqueue_fill():
+         *   - hw/net/virtio-net.c|2058| <<virtio_net_receive_rcu>> virtqueue_fill(q->rx_vq, elems[j], lens[j], j);
+         *   - hw/virtio/vhost-shadow-virtqueue.c|534| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i);
+         *   - hw/virtio/vhost-shadow-virtqueue.c|538| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i++);
+         *   - hw/virtio/virtio.c|1478| <<virtqueue_push>> virtqueue_fill(vq, elem, len, 0);
+	 */
         /* signal other side */
         virtqueue_fill(q->rx_vq, elems[j], lens[j], j);
         g_free(elems[j]);
     }
 
+    /*
+     * 在以下使用virtqueue_flush():
+     *   - hw/net/virtio-net.c|2062| <<virtio_net_receive_rcu>> virtqueue_flush(q->rx_vq, i);
+     *   - hw/virtio/vhost-shadow-virtqueue.c|535| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+     *   - hw/virtio/vhost-shadow-virtqueue.c|541| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+     *   - hw/virtio/virtio.c|1479| <<virtqueue_push>> virtqueue_flush(vq, 1);
+     */
     virtqueue_flush(q->rx_vq, i);
     virtio_notify(vdev, q->rx_vq);
 
@@ -2053,6 +2218,38 @@ err:
     return err;
 }
 
+/*
+ * virtio_net_receive_rcu() 是 QEMU virtio-net 的 RX 收包核心函数.
+ * 路径大概是:
+ *
+ * host backend 收到包
+ * -> net_virtio_info.receive = virtio_net_receive
+ *    -> virtio_net_do_receive()
+ *       -> virtio_net_receive_rcu()
+ *
+ * 1. 选 RX queue, RSS 开的话按 hash 选 subqueue.
+ * 2. 检查 guest 有没有可用 RX buffer.
+ * 3. 从 guest 的 RX virtqueue 里 virtqueue_pop() buffer.
+ * 4. 把 host 收到的包 buf 拷贝进 guest buffer.
+ * 5. virtqueue_fill() 标记这些 buffer 已经用完.
+ * 6. virtqueue_flush() 更新 used ring.
+ * 7. virtio_notify() 通知 guest: 你有包可以收了.
+ *
+ *
+ * 在以下使用virtio_net_do_receive():
+ *   - hw/net/virtio-net.c|2280| <<virtio_net_rsc_drain_seg>> ret = virtio_net_do_receive(seg->nc, seg->buf, seg->size);
+ *   - hw/net/virtio-net.c|2558| <<virtio_net_rsc_do_coalesce>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2597| <<virtio_net_rsc_drain_flow>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2659| <<virtio_net_rsc_receive4>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2665| <<virtio_net_rsc_receive4>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2670| <<virtio_net_rsc_receive4>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2728| <<virtio_net_rsc_receive6>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2734| <<virtio_net_rsc_receive6>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2739| <<virtio_net_rsc_receive6>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2798| <<virtio_net_rsc_receive>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2813| <<virtio_net_rsc_receive>> return virtio_net_do_receive(nc, buf, size);
+ *   - hw/net/virtio-net.c|2823| <<virtio_net_receive>> return virtio_net_do_receive(nc, buf, size);
+ */
 static ssize_t virtio_net_do_receive(NetClientState *nc, const uint8_t *buf,
                                   size_t size)
 {
@@ -2669,6 +2866,17 @@ static ssize_t virtio_net_rsc_receive(NetClientState *nc,
     return virtio_net_do_receive(nc, buf, size);
 }
 
+/*
+ * 3847 static NetClientInfo net_virtio_info = {
+ * 3848     .type = NET_CLIENT_DRIVER_NIC,
+ * 3849     .size = sizeof(NICState),
+ * 3850     .can_receive = virtio_net_can_receive,
+ * 3851     .receive = virtio_net_receive,
+ * 3852     .link_status_changed = virtio_net_set_link_status,
+ * 3853     .query_rx_filter = virtio_net_query_rxfilter,
+ * 3854     .announce = virtio_net_announce,
+ * 3855 };
+ */
 static ssize_t virtio_net_receive(NetClientState *nc, const uint8_t *buf,
                                   size_t size)
 {
@@ -2676,6 +2884,21 @@ static ssize_t virtio_net_receive(NetClientState *nc, const uint8_t *buf,
     if ((n->rsc4_enabled || n->rsc6_enabled)) {
         return virtio_net_rsc_receive(nc, buf, size);
     } else {
+        /*
+	 * 在以下使用virtio_net_do_receive():
+	 *   - hw/net/virtio-net.c|2280| <<virtio_net_rsc_drain_seg>> ret = virtio_net_do_receive(seg->nc, seg->buf, seg->size);
+	 *   - hw/net/virtio-net.c|2558| <<virtio_net_rsc_do_coalesce>> return virtio_net_do_receive(nc, buf, size);
+	 *   - hw/net/virtio-net.c|2597| <<virtio_net_rsc_drain_flow>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2659| <<virtio_net_rsc_receive4>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2665| <<virtio_net_rsc_receive4>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2670| <<virtio_net_rsc_receive4>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2728| <<virtio_net_rsc_receive6>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2734| <<virtio_net_rsc_receive6>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2739| <<virtio_net_rsc_receive6>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2798| <<virtio_net_rsc_receive>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2813| <<virtio_net_rsc_receive>> return virtio_net_do_receive(nc, buf, size);
+         *   - hw/net/virtio-net.c|2823| <<virtio_net_receive>> return virtio_net_do_receive(nc, buf, size);
+	 */
         return virtio_net_do_receive(nc, buf, size);
     }
 }
@@ -3241,6 +3464,12 @@ static int virtio_net_post_load_device(void *opaque, int version_id)
         }
     }
 
+    /*
+     * 在以下使用virtio_net_commit_rss_config():
+     *   - hw/net/virtio-net.c|1309| <<virtio_net_disable_rss>> virtio_net_commit_rss_config(n);
+     *   - hw/net/virtio-net.c|1507| <<virtio_net_handle_rss>> virtio_net_commit_rss_config(n);
+     *   - hw/net/virtio-net.c|3338| <<virtio_net_post_load_device>> virtio_net_commit_rss_config(n);
+     */
     virtio_net_commit_rss_config(n);
     return 0;
 }
@@ -4124,6 +4353,13 @@ static void virtio_net_reset(VirtIODevice *vdev)
         flush_or_purge_queued_packets(qemu_get_subqueue(n->nic, i));
     }
 
+    /*
+     * 在以下使用virtio_net_disable_rss():
+     *   - hw/net/virtio-net.c|1511| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|1533| <<virtio_net_handle_rss>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|1548| <<virtio_net_handle_mq>> virtio_net_disable_rss(n);
+     *   - hw/net/virtio-net.c|4249| <<virtio_net_reset>> virtio_net_disable_rss(n);
+     */
     virtio_net_disable_rss(n);
 }
 
diff --git a/hw/nvme/ctrl.c b/hw/nvme/ctrl.c
index be6c7028c..2cfe00faf 100644
--- a/hw/nvme/ctrl.c
+++ b/hw/nvme/ctrl.c
@@ -8891,6 +8891,13 @@ static bool nvme_init_sriov(NvmeCtrl *n, PCIDevice *pci_dev, uint16_t offset,
         return false;
     }
 
+    /*
+     * 在以下使用pcie_sriov_pf_init_vf_bar():
+     *   - hw/net/igb.c|456| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MMIO_BAR_IDX,
+     *   - hw/net/igb.c|459| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MSIX_BAR_IDX,
+     *   - hw/nvme/ctrl.c|8894| <<nvme_init_sriov>> pcie_sriov_pf_init_vf_bar(pci_dev, 0, PCI_BASE_ADDRESS_SPACE_MEMORY |
+     *   - hw/pci/pcie_sriov.c|351| <<pcie_sriov_pf_init_from_user_created_vfs>> pcie_sriov_pf_init_vf_bar(dev, i, region->type, region->size);
+     */
     pcie_sriov_pf_init_vf_bar(pci_dev, 0, PCI_BASE_ADDRESS_SPACE_MEMORY |
                               PCI_BASE_ADDRESS_MEM_TYPE_64, bar_size);
 
@@ -9470,6 +9477,14 @@ static void nvme_sriov_post_write_config(PCIDevice *dev, uint16_t old_num_vfs)
     NvmeSecCtrlEntry *sctrl;
     int i;
 
+    /*
+     * 在以下使用pcie_sriov_num_vfs():
+     *   - hw/net/igb_core.c|123| <<igb_msix_notify>> if (vfn < pcie_sriov_num_vfs(core->owner)) {
+     *   - hw/net/igb_core.c|2956| <<igb_get_status>> uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
+     *   - hw/nvme/ctrl.c|9473| <<nvme_sriov_post_write_config>> for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
+     *   - hw/nvme/ctrl.c|9482| <<nvme_pci_write_config>> uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
+     *   - hw/pci/pcie_sriov.c|505| <<pcie_sriov_get_vf_at_index>> if (n < pcie_sriov_num_vfs(dev)) {
+     */
     for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
         sctrl = &n->sec_ctrl_list[i];
         nvme_virt_set_state(n, le16_to_cpu(sctrl->scid), false);
@@ -9479,6 +9494,14 @@ static void nvme_sriov_post_write_config(PCIDevice *dev, uint16_t old_num_vfs)
 static void nvme_pci_write_config(PCIDevice *dev, uint32_t address,
                                   uint32_t val, int len)
 {
+    /*
+     * 在以下使用pcie_sriov_num_vfs():
+     *   - hw/net/igb_core.c|123| <<igb_msix_notify>> if (vfn < pcie_sriov_num_vfs(core->owner)) {
+     *   - hw/net/igb_core.c|2956| <<igb_get_status>> uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
+     *   - hw/nvme/ctrl.c|9473| <<nvme_sriov_post_write_config>> for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
+     *   - hw/nvme/ctrl.c|9482| <<nvme_pci_write_config>> uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
+     *   - hw/pci/pcie_sriov.c|505| <<pcie_sriov_get_vf_at_index>> if (n < pcie_sriov_num_vfs(dev)) {
+     */
     uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
 
     /* DOE is only initialised if SPDM over DOE is used */
diff --git a/hw/nvram/fw_cfg.c b/hw/nvram/fw_cfg.c
index 1d7d83542..43840ae96 100644
--- a/hw/nvram/fw_cfg.c
+++ b/hw/nvram/fw_cfg.c
@@ -834,6 +834,19 @@ static void fw_cfg_acpi_mr_save(FWCfgState *s, const char *filename, size_t len)
     }
 }
 
+/*
+ * 在以下使用fw_cfg_add_file_callback():
+ *   - hw/acpi/ghes.c|416| <<acpi_ghes_add_fw_cfg>> fw_cfg_add_file_callback(s, ACPI_HEST_ADDR_FW_CFG_FILE, NULL, NULL,
+ *   - hw/acpi/ghes.c|420| <<acpi_ghes_add_fw_cfg>> fw_cfg_add_file_callback(s, ACPI_HW_ERROR_ADDR_FW_CFG_FILE, NULL, NULL,
+ *   - hw/acpi/vmgenid.c|130| <<vmgenid_add_fw_cfg>> fw_cfg_add_file_callback(s, VMGENID_ADDR_FW_CFG_FILE, NULL, NULL, NULL,
+ *   - hw/core/loader.c|1197| <<rom_add_blob>> fw_cfg_add_file_callback(fw_cfg, fw_file_name,
+ *   - hw/display/ramfb.c|154| <<ramfb_setup>> fw_cfg_add_file_callback(fw_cfg, "etc/ramfb",
+ *   - hw/isa/lpc_ich9.c|448| <<ich9_lpc_pm_init>> fw_cfg_add_file_callback(fw_cfg, "etc/smi/requested-features",
+ *   - hw/isa/lpc_ich9.c|453| <<ich9_lpc_pm_init>> fw_cfg_add_file_callback(fw_cfg, "etc/smi/features-ok",
+ *   - hw/misc/vmcoreinfo.c|60| <<vmcoreinfo_realize>> fw_cfg_add_file_callback(fw_cfg, FW_CFG_VMCOREINFO_FILENAME,
+ *   - hw/nvram/fw_cfg.c|907| <<fw_cfg_add_file>> fw_cfg_add_file_callback(s, filename, NULL, NULL, NULL, data, len, true);
+ *   - hw/nvram/fw_cfg.c|933| <<fw_cfg_modify_file>> fw_cfg_add_file_callback(s, filename, NULL, NULL, NULL, data, len, true);
+ */
 void fw_cfg_add_file_callback(FWCfgState *s,  const char *filename,
                               FWCfgCallback select_cb,
                               FWCfgWriteCallback write_cb,
diff --git a/hw/pci-host/gpex-acpi.c b/hw/pci-host/gpex-acpi.c
index d9820f9b4..3234bc390 100644
--- a/hw/pci-host/gpex-acpi.c
+++ b/hw/pci-host/gpex-acpi.c
@@ -94,6 +94,12 @@ static void acpi_dsdt_add_host_bridge_methods(Aml *dev,
                                               bool enable_native_pcie_hotplug,
                                               bool preserve_config)
 {
+    /*
+     * 在以下使用build_pci_host_bridge_osc_method():
+     *   - hw/i386/acpi-build.c|994| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(!pm->pcihp_bridge_en));
+     *   - hw/i386/acpi-build.c|1114| <<build_dsdt>> aml_append(dev, build_pci_host_bridge_osc_method(true));
+     *   - hw/pci-host/gpex-acpi.c|99| <<acpi_dsdt_add_host_bridge_methods>> build_pci_host_bridge_osc_method(enable_native_pcie_hotplug));
+     */
     /* Declare an _OSC (OS Control Handoff) method */
     aml_append(dev,
                build_pci_host_bridge_osc_method(enable_native_pcie_hotplug));
diff --git a/hw/pci/pcie.c b/hw/pci/pcie.c
index 4622c75e4..76ba967bb 100644
--- a/hw/pci/pcie.c
+++ b/hw/pci/pcie.c
@@ -608,6 +608,10 @@ static void pcie_cap_slot_do_unplug(PCIDevice *dev)
                                PCI_EXP_SLTSTA_PDC);
 }
 
+/*
+ * 在以下使用pcie_cap_slot_unplug_request_cb():
+ *   - hw/pci/pcie_port.c|223| <<pcie_slot_class_init>> hc->unplug_request = pcie_cap_slot_unplug_request_cb;
+ */
 void pcie_cap_slot_unplug_request_cb(HotplugHandler *hotplug_dev,
                                      DeviceState *dev, Error **errp)
 {
diff --git a/hw/pci/pcie_sriov.c b/hw/pci/pcie_sriov.c
index c41ac95be..31539e03e 100644
--- a/hw/pci/pcie_sriov.c
+++ b/hw/pci/pcie_sriov.c
@@ -220,6 +220,13 @@ void pcie_sriov_pf_exit(PCIDevice *dev)
     }
 }
 
+/*
+ * 在以下使用pcie_sriov_pf_init_vf_bar():
+ *   - hw/net/igb.c|456| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MMIO_BAR_IDX,
+ *   - hw/net/igb.c|459| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MSIX_BAR_IDX,
+ *   - hw/nvme/ctrl.c|8894| <<nvme_init_sriov>> pcie_sriov_pf_init_vf_bar(pci_dev, 0, PCI_BASE_ADDRESS_SPACE_MEMORY |
+ *   - hw/pci/pcie_sriov.c|351| <<pcie_sriov_pf_init_from_user_created_vfs>> pcie_sriov_pf_init_vf_bar(dev, i, region->type, region->size);
+ */
 void pcie_sriov_pf_init_vf_bar(PCIDevice *dev, int region_num,
                                uint8_t type, dma_addr_t size)
 {
@@ -348,6 +355,13 @@ int16_t pcie_sriov_pf_init_from_user_created_vfs(PCIDevice *dev,
         PCIIORegion *region = &vfs[0]->io_regions[i];
 
         if (region->size) {
+            /*
+	     * 在以下使用pcie_sriov_pf_init_vf_bar():
+	     *   - hw/net/igb.c|456| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MMIO_BAR_IDX,
+	     *   - hw/net/igb.c|459| <<igb_pci_realize>> pcie_sriov_pf_init_vf_bar(pci_dev, IGBVF_MSIX_BAR_IDX,
+	     *   - hw/nvme/ctrl.c|8894| <<nvme_init_sriov>> pcie_sriov_pf_init_vf_bar(pci_dev, 0, PCI_BASE_ADDRESS_SPACE_MEMORY |
+	     *   - hw/pci/pcie_sriov.c|351| <<pcie_sriov_pf_init_from_user_created_vfs>> pcie_sriov_pf_init_vf_bar(dev, i, region->type, region->size);
+	     */
             pcie_sriov_pf_init_vf_bar(dev, i, region->type, region->size);
         }
     }
@@ -355,6 +369,10 @@ int16_t pcie_sriov_pf_init_from_user_created_vfs(PCIDevice *dev,
     return size;
 }
 
+/*
+ * 在以下使用pcie_sriov_register_device():
+ *   - hw/pci/pci.c|2317| <<pci_qdev_realize>> if (!pcie_sriov_register_device(pci_dev, errp)) {
+ */
 bool pcie_sriov_register_device(PCIDevice *dev, Error **errp)
 {
     if (!dev->exp.sriov_pf.vf && dev->qdev.id &&
@@ -502,12 +520,28 @@ PCIDevice *pcie_sriov_get_pf(PCIDevice *dev)
 PCIDevice *pcie_sriov_get_vf_at_index(PCIDevice *dev, int n)
 {
     assert(!pci_is_vf(dev));
+    /*
+     * 在以下使用pcie_sriov_num_vfs():
+     *   - hw/net/igb_core.c|123| <<igb_msix_notify>> if (vfn < pcie_sriov_num_vfs(core->owner)) {
+     *   - hw/net/igb_core.c|2956| <<igb_get_status>> uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
+     *   - hw/nvme/ctrl.c|9473| <<nvme_sriov_post_write_config>> for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
+     *   - hw/nvme/ctrl.c|9482| <<nvme_pci_write_config>> uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
+     *   - hw/pci/pcie_sriov.c|505| <<pcie_sriov_get_vf_at_index>> if (n < pcie_sriov_num_vfs(dev)) {
+     */
     if (n < pcie_sriov_num_vfs(dev)) {
         return dev->exp.sriov_pf.vf[n];
     }
     return NULL;
 }
 
+/*
+ * 在以下使用pcie_sriov_num_vfs():
+ *   - hw/net/igb_core.c|123| <<igb_msix_notify>> if (vfn < pcie_sriov_num_vfs(core->owner)) {
+ *   - hw/net/igb_core.c|2956| <<igb_get_status>> uint16_t num_vfs = pcie_sriov_num_vfs(core->owner);
+ *   - hw/nvme/ctrl.c|9473| <<nvme_sriov_post_write_config>> for (i = pcie_sriov_num_vfs(dev); i < old_num_vfs; i++) {
+ *   - hw/nvme/ctrl.c|9482| <<nvme_pci_write_config>> uint16_t old_num_vfs = pcie_sriov_num_vfs(dev);
+ *   - hw/pci/pcie_sriov.c|505| <<pcie_sriov_get_vf_at_index>> if (n < pcie_sriov_num_vfs(dev)) {
+ */
 uint16_t pcie_sriov_num_vfs(PCIDevice *dev)
 {
     uint16_t sriov_cap = dev->exp.sriov_cap;
diff --git a/hw/scsi/scsi-bus.c b/hw/scsi/scsi-bus.c
index 1a6b181b9..6222539b0 100644
--- a/hw/scsi/scsi-bus.c
+++ b/hw/scsi/scsi-bus.c
@@ -278,6 +278,10 @@ void scsi_req_retry(SCSIRequest *req)
     req->retry = true;
 }
 
+/*
+ * 在以下使用scsi_dma_restart_req():
+ *   - hw/scsi/scsi-bus.c|311| <<scsi_dma_restart_cb>> scsi_device_for_each_req_async(s, scsi_dma_restart_req, NULL);
+ */
 /* Called in the AioContext that is executing the request */
 static void scsi_dma_restart_req(SCSIRequest *req, void *opaque)
 {
@@ -298,6 +302,11 @@ static void scsi_dma_restart_req(SCSIRequest *req, void *opaque)
     scsi_req_unref(req);
 }
 
+/*
+ * 在以下使用scsi_dma_restart_cb():
+ *   - hw/scsi/scsi-bus.c|404| <<scsi_qdev_realize>> dev->vmsentry = qdev_add_vm_change_state_handler(DEVICE(dev),
+ *                                                     scsi_dma_restart_cb, NULL, dev);
+ */
 static void scsi_dma_restart_cb(void *opaque, bool running, RunState state)
 {
     SCSIDevice *s = opaque;
@@ -400,6 +409,15 @@ static void scsi_qdev_realize(DeviceState *qdev, Error **errp)
         error_propagate(errp, local_err);
         return;
     }
+    /*
+     * 在以下使用qdev_add_vm_change_state_handler():
+     *   - hw/block/virtio-blk.c|1976| <<virtio_blk_device_realize>>
+     *          qdev_add_vm_change_state_handler(dev, virtio_blk_dma_restart_cb, NULL, s);
+     *   - hw/scsi/scsi-bus.c|412| <<scsi_qdev_realize>>
+     *          dev->vmsentry = qdev_add_vm_change_state_handler(DEVICE(dev), scsi_dma_restart_cb, NULL, dev);
+     *   - hw/virtio/virtio.c|4252| <<virtio_init>>
+     *          vdev->vmstate = qdev_add_vm_change_state_handler(DEVICE(vdev), NULL, virtio_vmstate_change, vdev);
+     */
     dev->vmsentry = qdev_add_vm_change_state_handler(DEVICE(dev),
             scsi_dma_restart_cb, NULL, dev);
 }
@@ -1518,6 +1536,38 @@ void scsi_req_unref(SCSIRequest *req)
     }
 }
 
+/*
+ * 在以下使用scsi_req_continue():
+ *   - hw/scsi/esp.c|335| <<do_command_phase>> scsi_req_continue(s->current_req);
+ *   - hw/scsi/esp.c|617| <<esp_do_dma>> scsi_req_continue(s->current_req);
+ *   - hw/scsi/esp.c|669| <<esp_do_dma>> scsi_req_continue(s->current_req);
+ *   - hw/scsi/esp.c|675| <<esp_do_dma>> scsi_req_continue(s->current_req);
+ *   - hw/scsi/esp.c|763| <<esp_nodma_ti_dataout>> scsi_req_continue(s->current_req);
+ *   - hw/scsi/esp.c|908| <<esp_do_nodma>> scsi_req_continue(s->current_req);
+ *   - hw/scsi/lsi53c895a.c|679| <<lsi_do_dma>> scsi_req_continue(req);
+ *   - hw/scsi/lsi53c895a.c|901| <<lsi_do_command>> scsi_req_continue(s->current->req);
+ *   - hw/scsi/megasas.c|1077| <<megasas_pd_get_info_submit>> scsi_req_continue(cmd->req);
+ *   - hw/scsi/megasas.c|1093| <<megasas_pd_get_info_submit>> scsi_req_continue(cmd->req);
+ *   - hw/scsi/megasas.c|1283| <<megasas_ld_get_info_submit>> scsi_req_continue(cmd->req);
+ *   - hw/scsi/megasas.c|1694| <<megasas_enqueue_req>> scsi_req_continue(cmd->req);
+ *   - hw/scsi/megasas.c|1853| <<megasas_xfer_complete>> scsi_req_continue(req);
+ *   - hw/scsi/megasas.c|1868| <<megasas_xfer_complete>> scsi_req_continue(req);
+ *   - hw/scsi/megasas.c|1874| <<megasas_xfer_complete>> scsi_req_continue(req);
+ *   - hw/scsi/mptsas.c|354| <<mptsas_process_scsi_io_request>> scsi_req_continue(req->sreq);
+ *   - hw/scsi/ncr53c710.c|722| <<ncr710_do_dma>> scsi_req_continue(s->current->req);
+ *   - hw/scsi/ncr53c710.c|956| <<ncr710_do_command>> scsi_req_continue(s->current->req);
+ *   - hw/scsi/scsi-bus.c|290| <<scsi_dma_restart_req>> scsi_req_continue(req);
+ *   - hw/scsi/scsi-bus.c|1569| <<scsi_req_data>> scsi_req_continue(req);
+ *   - hw/scsi/spapr_vscsi.c|549| <<vscsi_transfer_data>> scsi_req_continue(sreq);
+ *   - hw/scsi/spapr_vscsi.c|834| <<vscsi_queue_cmd>> scsi_req_continue(req->sreq);
+ *   - hw/scsi/virtio-scsi.c|902| <<virtio_scsi_handle_cmd_req_submit>> scsi_req_continue(sreq);
+ *   - hw/scsi/vmw_pvscsi.c|741| <<pvscsi_process_request_descriptor>> scsi_req_continue(r->sreq);
+ *   - hw/ufs/lu.c|270| <<ufs_process_scsi_cmd>> scsi_req_continue(scsi_req);
+ *   - hw/usb/dev-storage.c|225| <<usb_msd_copy_data>> scsi_req_continue(s->req);
+ *   - hw/usb/dev-storage.c|453| <<usb_msd_handle_data>> scsi_req_continue(s->req);
+ *   - hw/usb/dev-uas.c|515| <<usb_uas_copy_data>> scsi_req_continue(req->req);
+ *   - hw/usb/dev-uas.c|739| <<usb_uas_command>> scsi_req_continue(req->req);
+ */
 /* Tell the device that we finished processing this chunk of I/O.  It
    will start the next chunk or complete the command.  */
 void scsi_req_continue(SCSIRequest *req)
diff --git a/hw/scsi/scsi-disk.c b/hw/scsi/scsi-disk.c
index 5ba5b46c4..090ec852c 100644
--- a/hw/scsi/scsi-disk.c
+++ b/hw/scsi/scsi-disk.c
@@ -220,6 +220,10 @@ static void scsi_disk_emulate_load_request(QEMUFile *f, SCSIRequest *req)
  * scsi_handle_rw_error always manages its reference counts, independent
  * of the return value.
  */
+/*
+ * 在以下使用scsi_handle_rw_error():
+ *   - hw/scsi/scsi-disk.c|340| <<scsi_disk_req_check_error>> return scsi_handle_rw_error(r, ret, acct_failed);
+ */
 static bool scsi_handle_rw_error(SCSIDiskReq *r, int ret, bool acct_failed)
 {
     bool is_read = (r->req.cmd.mode == SCSI_XFER_FROM_DEV);
@@ -297,6 +301,13 @@ static bool scsi_handle_rw_error(SCSIDiskReq *r, int ret, bool acct_failed)
         acct_failed = false;
     } else {
         action = blk_get_error_action(s->qdev.conf.blk, is_read, error);
+	/*
+	 * 在以下使用blk_error_action():
+	 *  - hw/block/virtio-blk.c|94| <<virtio_blk_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+	 *  - hw/ide/ahci.c|1030| <<ncq_cb>> blk_error_action(ide_state->blk, action, is_read, -ret);
+	 *  - hw/ide/core.c|876| <<ide_handle_rw_error>> blk_error_action(s->blk, action, is_read, error);
+	 *  - hw/scsi/scsi-disk.c|300| <<scsi_handle_rw_error>> blk_error_action(s->qdev.conf.blk, action, is_read, error);
+	 */
         blk_error_action(s->qdev.conf.blk, action, is_read, error);
     }
 
@@ -329,6 +340,16 @@ static bool scsi_handle_rw_error(SCSIDiskReq *r, int ret, bool acct_failed)
     }
 }
 
+/*
+ * 在以下使用scsi_disk_req_check_error():
+ *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+ *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+ *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+ *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+ *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+ *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+ *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+ */
 static bool scsi_disk_req_check_error(SCSIDiskReq *r, int ret, bool acct_failed)
 {
     if (r->req.io_canceled) {
@@ -354,6 +375,16 @@ static void scsi_aio_complete(void *opaque, int ret)
     assert(r->req.aiocb != NULL);
     r->req.aiocb = NULL;
 
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, true)) {
         goto done;
     }
@@ -394,6 +425,16 @@ static bool scsi_is_cmd_fua(SCSICommand *cmd)
 static void scsi_dma_complete_noio(SCSIDiskReq *r, int ret)
 {
     assert(r->req.aiocb == NULL);
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, ret > 0)) {
         goto done;
     }
@@ -431,6 +472,16 @@ static void scsi_read_complete_noio(SCSIDiskReq *r, int ret)
     assert(r->req.ctx == qemu_get_current_aio_context());
 
     assert(r->req.aiocb == NULL);
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, ret > 0)) {
         goto done;
     }
@@ -462,6 +513,11 @@ static void scsi_read_complete(void *opaque, int ret)
     scsi_read_complete_noio(r, ret);
 }
 
+/*
+ * 在以下使用scsi_do_read():
+ *   - hw/scsi/scsi-disk.c|572| <<scsi_do_read_cb>> scsi_do_read(opaque, ret);
+ *   - hw/scsi/scsi-disk.c|612| <<scsi_read_data>> scsi_do_read(r, 0);
+ */
 /* Actually issue a read to the block device.  */
 static void scsi_do_read(SCSIDiskReq *r, int ret)
 {
@@ -469,6 +525,16 @@ static void scsi_do_read(SCSIDiskReq *r, int ret)
     SCSIDiskClass *sdc = (SCSIDiskClass *) object_get_class(OBJECT(s));
 
     assert (r->req.aiocb == NULL);
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, false)) {
         goto done;
     }
@@ -495,6 +561,10 @@ done:
     scsi_req_unref(&r->req);
 }
 
+/*
+ * 在以下使用scsi_do_read_cb():
+ *   - hw/scsi/scsi-disk.c|610| <<scsi_read_data>> r->req.aiocb = blk_aio_flush(s->qdev.conf.blk, scsi_do_read_cb, r);
+ */
 static void scsi_do_read_cb(void *opaque, int ret)
 {
     SCSIDiskReq *r = (SCSIDiskReq *)opaque;
@@ -511,6 +581,11 @@ static void scsi_do_read_cb(void *opaque, int ret)
     scsi_do_read(opaque, ret);
 }
 
+/*
+ * 在以下使用scsi_read_data():
+ *   - hw/scsi/scsi-disk.c|2787| <<global>> SCSIReqOps scsi_disk_dma_reqops.read_data = scsi_read_data,
+ *   - hw/scsi/scsi-disk.c|3222| <<global>> SCSIReqOps scsi_block_dma_reqops.read_data = scsi_read_data,
+ */
 /* Read more data from scsi device into buffer.  */
 static void scsi_read_data(SCSIRequest *req)
 {
@@ -552,6 +627,13 @@ static void scsi_read_data(SCSIRequest *req)
     }
 }
 
+/*
+ * 在以下使用scsi_write_complete_noio():
+ *   - hw/scsi/scsi-disk.c|596| <<scsi_write_complete>> scsi_write_complete_noio(r, ret);
+ *   - hw/scsi/scsi-disk.c|613| <<scsi_write_data>> scsi_write_complete_noio(r, -EINVAL);
+ *   - hw/scsi/scsi-disk.c|620| <<scsi_write_data>> scsi_write_complete_noio(r, 0);
+ *   - hw/scsi/scsi-disk.c|624| <<scsi_write_data>> scsi_write_complete_noio(r, -ENOMEDIUM);
+ */
 static void scsi_write_complete_noio(SCSIDiskReq *r, int ret)
 {
     uint32_t n;
@@ -560,6 +642,16 @@ static void scsi_write_complete_noio(SCSIDiskReq *r, int ret)
     assert(r->req.ctx == qemu_get_current_aio_context());
 
     assert (r->req.aiocb == NULL);
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, ret > 0)) {
         goto done;
     }
@@ -593,6 +685,13 @@ static void scsi_write_complete(void * opaque, int ret)
     } else if (ret == 0) {
         block_acct_done(blk_get_stats(s->qdev.conf.blk), &r->acct);
     }
+    /*
+     * 在以下使用scsi_write_complete_noio():
+     *   - hw/scsi/scsi-disk.c|596| <<scsi_write_complete>> scsi_write_complete_noio(r, ret);
+     *   - hw/scsi/scsi-disk.c|613| <<scsi_write_data>> scsi_write_complete_noio(r, -EINVAL);
+     *   - hw/scsi/scsi-disk.c|620| <<scsi_write_data>> scsi_write_complete_noio(r, 0);
+     *   - hw/scsi/scsi-disk.c|624| <<scsi_write_data>> scsi_write_complete_noio(r, -ENOMEDIUM);
+     */
     scsi_write_complete_noio(r, ret);
 }
 
@@ -610,6 +709,13 @@ static void scsi_write_data(SCSIRequest *req)
     scsi_req_ref(&r->req);
     if (r->req.cmd.mode != SCSI_XFER_TO_DEV) {
         trace_scsi_disk_write_data_invalid();
+        /*
+	 * 在以下使用scsi_write_complete_noio():
+         *   - hw/scsi/scsi-disk.c|596| <<scsi_write_complete>> scsi_write_complete_noio(r, ret);
+         *   - hw/scsi/scsi-disk.c|613| <<scsi_write_data>> scsi_write_complete_noio(r, -EINVAL);
+         *   - hw/scsi/scsi-disk.c|620| <<scsi_write_data>> scsi_write_complete_noio(r, 0);
+         *   - hw/scsi/scsi-disk.c|624| <<scsi_write_data>> scsi_write_complete_noio(r, -ENOMEDIUM);
+	 */
         scsi_write_complete_noio(r, -EINVAL);
         return;
     }
@@ -617,10 +723,24 @@ static void scsi_write_data(SCSIRequest *req)
     if (!r->req.sg && !r->qiov.size) {
         /* Called for the first time.  Ask the driver to send us more data.  */
         r->started = true;
+        /*
+	 * 在以下使用scsi_write_complete_noio():
+         *   - hw/scsi/scsi-disk.c|596| <<scsi_write_complete>> scsi_write_complete_noio(r, ret);
+         *   - hw/scsi/scsi-disk.c|613| <<scsi_write_data>> scsi_write_complete_noio(r, -EINVAL);
+         *   - hw/scsi/scsi-disk.c|620| <<scsi_write_data>> scsi_write_complete_noio(r, 0);
+         *   - hw/scsi/scsi-disk.c|624| <<scsi_write_data>> scsi_write_complete_noio(r, -ENOMEDIUM);
+	 */
         scsi_write_complete_noio(r, 0);
         return;
     }
     if (!blk_is_available(req->dev->conf.blk)) {
+        /*
+	 * 在以下使用scsi_write_complete_noio():
+         *   - hw/scsi/scsi-disk.c|596| <<scsi_write_complete>> scsi_write_complete_noio(r, ret);
+         *   - hw/scsi/scsi-disk.c|613| <<scsi_write_data>> scsi_write_complete_noio(r, -EINVAL);
+         *   - hw/scsi/scsi-disk.c|620| <<scsi_write_data>> scsi_write_complete_noio(r, 0);
+         *   - hw/scsi/scsi-disk.c|624| <<scsi_write_data>> scsi_write_complete_noio(r, -ENOMEDIUM);
+	 */
         scsi_write_complete_noio(r, -ENOMEDIUM);
         return;
     }
@@ -1793,6 +1913,16 @@ static void scsi_unmap_complete(void *opaque, int ret)
     assert(r->req.aiocb != NULL);
     r->req.aiocb = NULL;
 
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, true)) {
         scsi_req_unref(&r->req);
         g_free(data);
@@ -1870,6 +2000,16 @@ static void scsi_write_same_complete(void *opaque, int ret)
     assert(r->req.aiocb != NULL);
     r->req.aiocb = NULL;
 
+    /*
+     * 在以下使用scsi_disk_req_check_error():
+     *   - hw/scsi/scsi-disk.c|357| <<scsi_aio_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|397| <<scsi_dma_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|434| <<scsi_read_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|472| <<scsi_do_read>> if (scsi_disk_req_check_error(r, ret, false)) {
+     *   - hw/scsi/scsi-disk.c|563| <<scsi_write_complete_noio>> if (scsi_disk_req_check_error(r, ret, ret > 0)) {
+     *   - hw/scsi/scsi-disk.c|1796| <<scsi_unmap_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     *   - hw/scsi/scsi-disk.c|1873| <<scsi_write_same_complete>> if (scsi_disk_req_check_error(r, ret, true)) {
+     */
     if (scsi_disk_req_check_error(r, ret, true)) {
         goto done;
     }
@@ -2885,6 +3025,13 @@ static void scsi_block_sgio_complete(void *opaque, int ret)
     req->cb(req->cb_opaque, ret);
 }
 
+/*
+ * 在以下使用scsi_block_do_sgio():
+ *   - hw/scsi/scsi-disk.c|2986| <<scsi_block_dma_readv>> return scsi_block_do_sgio(r, offset, iov,
+ *                                                  SG_DXFER_FROM_DEV, cb, cb_opaque);
+ *   - hw/scsi/scsi-disk.c|2996| <<scsi_block_dma_writev>> return scsi_block_do_sgio(r, offset, iov,
+ *                                                  SG_DXFER_TO_DEV, cb, cb_opaque);
+ */
 static BlockAIOCB *scsi_block_do_sgio(SCSIBlockReq *req,
                                       int64_t offset, QEMUIOVector *iov,
                                       int direction,
@@ -2977,12 +3124,23 @@ static bool scsi_block_no_fua(SCSICommand *cmd)
     return false;
 }
 
+/*
+ * 在以下使用scsi_block_dma_readv():
+ *   - hw/scsi/scsi-disk.c|3530| <<scsi_block_class_initfn>> sdc->dma_readv = scsi_block_dma_readv;
+ */
 static BlockAIOCB *scsi_block_dma_readv(int64_t offset,
                                         QEMUIOVector *iov,
                                         BlockCompletionFunc *cb, void *cb_opaque,
                                         void *opaque)
 {
     SCSIBlockReq *r = opaque;
+    /*
+     * 在以下使用scsi_block_do_sgio():
+     *   - hw/scsi/scsi-disk.c|2986| <<scsi_block_dma_readv>> return scsi_block_do_sgio(r, offset, iov,
+     *                                                  SG_DXFER_FROM_DEV, cb, cb_opaque);
+     *   - hw/scsi/scsi-disk.c|2996| <<scsi_block_dma_writev>> return scsi_block_do_sgio(r, offset, iov,
+     *                                                  SG_DXFER_TO_DEV, cb, cb_opaque);
+     */
     return scsi_block_do_sgio(r, offset, iov,
                               SG_DXFER_FROM_DEV, cb, cb_opaque);
 }
@@ -2993,6 +3151,13 @@ static BlockAIOCB *scsi_block_dma_writev(int64_t offset,
                                          void *opaque)
 {
     SCSIBlockReq *r = opaque;
+    /*
+     * 在以下使用scsi_block_do_sgio():
+     *   - hw/scsi/scsi-disk.c|2986| <<scsi_block_dma_readv>> return scsi_block_do_sgio(r, offset, iov,
+     *                                                  SG_DXFER_FROM_DEV, cb, cb_opaque);
+     *   - hw/scsi/scsi-disk.c|2996| <<scsi_block_dma_writev>> return scsi_block_do_sgio(r, offset, iov,
+     *                                                  SG_DXFER_TO_DEV, cb, cb_opaque);
+     */
     return scsi_block_do_sgio(r, offset, iov,
                               SG_DXFER_TO_DEV, cb, cb_opaque);
 }
diff --git a/hw/scsi/virtio-scsi.c b/hw/scsi/virtio-scsi.c
index 6c7376801..6f8698d5d 100644
--- a/hw/scsi/virtio-scsi.c
+++ b/hw/scsi/virtio-scsi.c
@@ -942,6 +942,19 @@ static void virtio_scsi_handle_cmd_vq(VirtIOSCSI *s, VirtQueue *vq)
             virtio_queue_set_notification(vq, 1);
         }
     } while (ret != -EINVAL && !virtio_queue_empty(vq));
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
 
     QTAILQ_FOREACH_SAFE(req, &reqs, next, next) {
         virtio_scsi_handle_cmd_req_submit(s, req);
diff --git a/hw/vfio/container-legacy.c b/hw/vfio/container-legacy.c
index 625f15136..6603753fa 100644
--- a/hw/vfio/container-legacy.c
+++ b/hw/vfio/container-legacy.c
@@ -618,6 +618,19 @@ static bool vfio_container_connect(VFIOGroup *group, AddressSpace *as,
     bool group_was_added = false;
 
     space = vfio_address_space_get(as);
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     fd = cpr_find_fd("vfio_container_for_group", group->groupid);
 
     if (!cpr_is_incoming()) {
@@ -980,6 +993,21 @@ static bool vfio_legacy_attach_device(const char *name, VFIODevice *vbasedev,
     if (vbasedev->mdev) {
         error_setg(&vbasedev->cpr.mdev_blocker,
                    "CPR does not support vfio mdev %s", vbasedev->name);
+        /*
+	 * 在以下使用migrate_add_blocker_modes():
+         *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+         *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+         *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+         *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+         *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+         *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+         *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+         *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+	 */
         if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
                     BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC),
                     errp) < 0) {
diff --git a/hw/vfio/container.c b/hw/vfio/container.c
index 4c2816b57..2c902d663 100644
--- a/hw/vfio/container.c
+++ b/hw/vfio/container.c
@@ -82,10 +82,22 @@ int vfio_container_dma_map(VFIOContainer *bcontainer,
     RAMBlock *rb = mr->ram_block;
     int mfd = rb ? qemu_ram_get_fd(rb) : -1;
 
+    /*
+     * 在以下使用VFIOIOMMUClass->dma_map_file():
+     *   - hw/vfio/container.c|85| <<vfio_container_dma_map>> if (mfd >= 0 && vioc->dma_map_file) {
+     *   - hw/vfio/container.c|89| <<vfio_container_dma_map>> return vioc->dma_map_file(bcontainer, iova, size, mfd, start + offset,
+     *   - hw/vfio/iommufd.c|910| <<vfio_iommu_iommufd_class_init>> vioc->dma_map_file = iommufd_cdev_map_file;
+     */
     if (mfd >= 0 && vioc->dma_map_file) {
         unsigned long start = vaddr - qemu_ram_get_host_addr(rb);
         unsigned long offset = qemu_ram_get_fd_offset(rb);
 
+        /*
+	 * 在以下使用VFIOIOMMUClass->dma_map_file():
+         *   - hw/vfio/container.c|85| <<vfio_container_dma_map>> if (mfd >= 0 && vioc->dma_map_file) {
+         *   - hw/vfio/container.c|89| <<vfio_container_dma_map>> return vioc->dma_map_file(bcontainer, iova, size, mfd, start + offset,
+         *   - hw/vfio/iommufd.c|910| <<vfio_iommu_iommufd_class_init>> vioc->dma_map_file = iommufd_cdev_map_file;
+	 */
         return vioc->dma_map_file(bcontainer, iova, size, mfd, start + offset,
                                   readonly);
     }
diff --git a/hw/vfio/cpr-iommufd.c b/hw/vfio/cpr-iommufd.c
index 6758c73b6..f42b09806 100644
--- a/hw/vfio/cpr-iommufd.c
+++ b/hw/vfio/cpr-iommufd.c
@@ -60,11 +60,47 @@ static void vfio_cpr_save_device(VFIODevice *vbasedev)
     elem->ioas_id = vbasedev->cpr.ioas_id;
     elem->devid = vbasedev->devid;
     elem->hwpt_id = vbasedev->cpr.hwpt_id;
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     */
     QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
 }
 
 static CprVFIODevice *find_device(const char *name)
 {
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     */
     CprVFIODeviceList *head = &cpr_state.vfio_devices;
     CprVFIODevice *elem;
 
@@ -158,6 +194,21 @@ bool vfio_iommufd_cpr_register_iommufd(IOMMUFDBackend *be, Error **errp)
     Error **cpr_blocker = &be->cpr_blocker;
 
     if (!vfio_cpr_supported(be, cpr_blocker)) {
+        /*
+	 * 在以下使用migrate_add_blocker_modes():
+         *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+         *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+         *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+         *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+         *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+         *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+         *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+         *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+	 */
         return migrate_add_blocker_modes(cpr_blocker,
                     BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC),
                     errp) == 0;
@@ -220,6 +271,19 @@ void vfio_cpr_load_device(VFIODevice *vbasedev)
         g_assert(ret);
 
         if (vbasedev->fd < 0) {
+            /*
+	     * 在以下使用cpr_find_fd():
+             *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+             *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+             *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+             *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+             *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+             *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+             *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+             *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+             *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+             *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+	     */
             vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
         }
     }
diff --git a/hw/vfio/cpr-legacy.c b/hw/vfio/cpr-legacy.c
index 033a546c3..f9658d178 100644
--- a/hw/vfio/cpr-legacy.c
+++ b/hw/vfio/cpr-legacy.c
@@ -176,6 +176,21 @@ bool vfio_legacy_cpr_register_container(VFIOLegacyContainer *container,
                                 MIG_MODE_CPR_REBOOT);
 
     if (!vfio_cpr_supported(container, cpr_blocker)) {
+        /*
+	 * 在以下使用migrate_add_blocker_modes():
+         *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+         *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+         *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+         *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+         *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+         *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+         *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+         *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+         *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+	 */
         return migrate_add_blocker_modes(cpr_blocker,
                         BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC),
                         errp) == 0;
@@ -257,6 +272,19 @@ bool vfio_cpr_ram_discard_replay_populated(VFIOContainer *bcontainer,
 int vfio_cpr_group_get_device_fd(int d, const char *name)
 {
     const int id = 0;
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     int fd = cpr_find_fd(name, id);
 
     if (fd < 0) {
diff --git a/hw/vfio/cpr.c b/hw/vfio/cpr.c
index ffa4f8e09..a06d6f360 100644
--- a/hw/vfio/cpr.c
+++ b/hw/vfio/cpr.c
@@ -42,6 +42,19 @@ void vfio_cpr_save_vector_fd(VFIOPCIDevice *vdev, const char *name, int nr,
 int vfio_cpr_load_vector_fd(VFIOPCIDevice *vdev, const char *name, int nr)
 {
     g_autofree char *fdname = STRDUP_VECTOR_FD_NAME(vdev, name);
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     return cpr_find_fd(fdname, nr);
 }
 
diff --git a/hw/vfio/device.c b/hw/vfio/device.c
index 973fc35b5..1616fbb82 100644
--- a/hw/vfio/device.c
+++ b/hw/vfio/device.c
@@ -345,6 +345,21 @@ bool vfio_device_get_name(VFIODevice *vbasedev, Error **errp)
                 error_setg(&vbasedev->cpr.id_blocker,
                            "vfio device with fd=%d needs an id property",
                            vbasedev->fd);
+                /*
+		 * 在以下使用migrate_add_blocker_modes():
+		 *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+                 *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+                 *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+                 *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+                 *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+                 *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+                 *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+                 *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+                 *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+                 *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+                 *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+                 *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+		 */
                 return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
                                                  BIT(MIG_MODE_CPR_TRANSFER),
                                                  errp) == 0;
diff --git a/hw/vfio/iommufd.c b/hw/vfio/iommufd.c
index 3e33dfbb3..44401e116 100644
--- a/hw/vfio/iommufd.c
+++ b/hw/vfio/iommufd.c
@@ -907,6 +907,12 @@ static void vfio_iommu_iommufd_class_init(ObjectClass *klass, const void *data)
     VFIOIOMMUClass *vioc = VFIO_IOMMU_CLASS(klass);
 
     vioc->dma_map = iommufd_cdev_map;
+    /*
+     * 在以下使用VFIOIOMMUClass->dma_map_file():
+     *   - hw/vfio/container.c|85| <<vfio_container_dma_map>> if (mfd >= 0 && vioc->dma_map_file) {
+     *   - hw/vfio/container.c|89| <<vfio_container_dma_map>> return vioc->dma_map_file(bcontainer, iova, size, mfd, start + offset,
+     *   - hw/vfio/iommufd.c|910| <<vfio_iommu_iommufd_class_init>> vioc->dma_map_file = iommufd_cdev_map_file;
+     */
     vioc->dma_map_file = iommufd_cdev_map_file;
     vioc->dma_unmap = iommufd_cdev_unmap;
     vioc->attach_device = iommufd_cdev_attach;
diff --git a/hw/vfio/migration.c b/hw/vfio/migration.c
index 83327b657..deb5f4028 100644
--- a/hw/vfio/migration.c
+++ b/hw/vfio/migration.c
@@ -314,6 +314,11 @@ static void vfio_migration_cleanup(VFIODevice *vbasedev)
     migration->data_fd = -1;
 }
 
+/*
+ * 在以下使用vfio_query_stop_copy_size():
+ *   - hw/vfio/migration.c|475| <<vfio_save_setup>> vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
+ *   - hw/vfio/migration.c|590| <<vfio_state_pending_exact>> vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
+ */
 static int vfio_query_stop_copy_size(VFIODevice *vbasedev,
                                      uint64_t *stop_copy_size)
 {
@@ -337,6 +342,11 @@ static int vfio_query_stop_copy_size(VFIODevice *vbasedev,
     return 0;
 }
 
+/*
+ * 在以下使用vfio_query_precopy_size():
+ *   - hw/vfio/migration.c|497| <<vfio_save_setup>> vfio_query_precopy_size(migration);
+ *   - hw/vfio/migration.c|594| <<vfio_state_pending_exact>> vfio_query_precopy_size(migration);
+ */
 static int vfio_query_precopy_size(VFIOMigration *migration)
 {
     struct vfio_precopy_info precopy = {
@@ -346,6 +356,20 @@ static int vfio_query_precopy_size(VFIOMigration *migration)
     migration->precopy_init_size = 0;
     migration->precopy_dirty_size = 0;
 
+    /*
+     * 原来有 10 份状态
+     * - 你传了 4 份到目标端
+     * - 但源端设备继续跑, 这 4 份里面有 2 份后来又变了
+     *
+     * 那这时候就可以近似理解成:
+     * - init = 6
+     * - dirty = 2
+     *
+     * 意思是:
+     * - 还有 6 份"从来还没传过"的老数据
+     * - 另外有 2 份"虽然传过了, 但后来又变了, 所以之前传过去的版本过时了,
+     * 要补传"
+     */
     if (ioctl(migration->data_fd, VFIO_MIG_GET_PRECOPY_INFO, &precopy)) {
         return -errno;
     }
@@ -472,6 +496,11 @@ static int vfio_save_setup(QEMUFile *f, void *opaque, Error **errp)
 
     qemu_put_be64(f, VFIO_MIG_FLAG_DEV_SETUP_STATE);
 
+    /*
+     * 在以下使用vfio_query_stop_copy_size():
+     *   - hw/vfio/migration.c|475| <<vfio_save_setup>> vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
+     *   - hw/vfio/migration.c|590| <<vfio_state_pending_exact>> vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
+     */
     vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
     migration->data_buffer_size = MIN(VFIO_MIG_DEFAULT_DATA_BUFFER_SIZE,
                                       stop_copy_size);
@@ -494,6 +523,11 @@ static int vfio_save_setup(QEMUFile *f, void *opaque, Error **errp)
                 return ret;
             }
 
+            /*
+	     * 在以下使用vfio_query_precopy_size():
+             *   - hw/vfio/migration.c|497| <<vfio_save_setup>> vfio_query_precopy_size(migration);
+             *   - hw/vfio/migration.c|594| <<vfio_state_pending_exact>> vfio_query_precopy_size(migration);
+	     */
             vfio_query_precopy_size(migration);
 
             break;
@@ -576,6 +610,29 @@ static void vfio_state_pending_estimate(void *opaque, uint64_t *must_precopy,
  */
 #define VFIO_MIG_STOP_COPY_SIZE (100 * GiB)
 
+/*
+ * 884 static const SaveVMHandlers savevm_vfio_handlers = {
+ * 885     .save_prepare = vfio_save_prepare,
+ * 886     .save_setup = vfio_save_setup,
+ * 887     .save_cleanup = vfio_save_cleanup,
+ * 888     .state_pending_estimate = vfio_state_pending_estimate,
+ * 889     .state_pending_exact = vfio_state_pending_exact,
+ * 890     .is_active_iterate = vfio_is_active_iterate,
+ * 891     .save_live_iterate = vfio_save_iterate,
+ * 892     .save_complete = vfio_save_complete_precopy,
+ * 893     .save_state = vfio_save_state,
+ * 894     .load_setup = vfio_load_setup,
+ * 895     .load_cleanup = vfio_load_cleanup,
+ * 896     .load_state = vfio_load_state,
+ * 897     .switchover_ack_needed = vfio_switchover_ack_needed,
+ * 898     //
+ * 899      // Multifd support
+ * 900      //
+ * 901     .load_state_buffer = vfio_multifd_load_state_buffer,
+ * 902     .switchover_start = vfio_switchover_start,
+ * 903     .save_complete_precopy_thread = vfio_multifd_save_complete_precopy_thread,
+ * 904 };
+ */
 static void vfio_state_pending_exact(void *opaque, uint64_t *must_precopy,
                                      uint64_t *can_postcopy)
 {
@@ -587,10 +644,20 @@ static void vfio_state_pending_exact(void *opaque, uint64_t *must_precopy,
      * If getting pending migration size fails, VFIO_MIG_STOP_COPY_SIZE is
      * reported so downtime limit won't be violated.
      */
+    /*
+     * 在以下使用vfio_query_stop_copy_size():
+     *   - hw/vfio/migration.c|475| <<vfio_save_setup>> vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
+     *   - hw/vfio/migration.c|590| <<vfio_state_pending_exact>> vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
+     */
     vfio_query_stop_copy_size(vbasedev, &stop_copy_size);
     *must_precopy += stop_copy_size;
 
     if (vfio_device_state_is_precopy(vbasedev)) {
+        /*
+	 * 在以下使用vfio_query_precopy_size():
+         *   - hw/vfio/migration.c|497| <<vfio_save_setup>> vfio_query_precopy_size(migration);
+         *   - hw/vfio/migration.c|594| <<vfio_state_pending_exact>> vfio_query_precopy_size(migration);
+	 */
         vfio_query_precopy_size(migration);
     }
 
@@ -1036,6 +1103,14 @@ static int vfio_migration_init(VFIODevice *vbasedev)
     prepare_cb = migration->mig_flags & VFIO_MIGRATION_P2P ?
                      vfio_vmstate_change_prepare :
                      NULL;
+    /*
+     * 在以下使用qdev_add_vm_change_state_handler_full():
+     *   - hw/core/vm-change-state-handler.c|64| <<qdev_add_vm_change_state_handler>>
+     *          return qdev_add_vm_change_state_handler_full(dev, cb, NULL, cb_ret, opaque);
+     *   - hw/vfio/migration.c|1106| <<vfio_migration_init>>
+     *          migration->vm_state = qdev_add_vm_change_state_handler_full(
+     *           vbasedev->dev, vfio_vmstate_change, prepare_cb, NULL, vbasedev);
+     */
     migration->vm_state = qdev_add_vm_change_state_handler_full(
         vbasedev->dev, vfio_vmstate_change, prepare_cb, NULL, vbasedev);
     migration_add_notifier(&migration->migration_state,
@@ -1139,6 +1214,12 @@ void vfio_migration_reset_bytes_transferred(void)
     qatomic_set(&bytes_transferred, 0);
 }
 
+/*
+ * 在以下使用vfio_migration_add_bytes_transferred():
+ *   - hw/vfio/migration-multifd.c|684| <<vfio_save_complete_precopy_thread_config_state>> vfio_migration_add_bytes_transferred(packet_len);
+ *   - hw/vfio/migration-multifd.c|755| <<vfio_multifd_save_complete_precopy_thread>> vfio_migration_add_bytes_transferred(packet_size);
+ *   - hw/vfio/migration.c|391| <<vfio_save_block>> vfio_migration_add_bytes_transferred(data_size);
+ */
 void vfio_migration_add_bytes_transferred(unsigned long val)
 {
     qatomic_add(&bytes_transferred, val);
diff --git a/hw/virtio/vhost-shadow-virtqueue.c b/hw/virtio/vhost-shadow-virtqueue.c
index 6242aeb69..0db9a8fd5 100644
--- a/hw/virtio/vhost-shadow-virtqueue.c
+++ b/hw/virtio/vhost-shadow-virtqueue.c
@@ -368,6 +368,19 @@ static void vhost_handle_guest_kick(VhostShadowVirtqueue *svq)
 
         virtio_queue_set_notification(svq->vq, true);
     } while (!virtio_queue_empty(svq->vq));
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
 }
 
 /**
@@ -522,9 +535,23 @@ static void vhost_svq_flush(VhostShadowVirtqueue *svq,
                 virtqueue_flush(vq, i);
                 return;
             }
+            /*
+	     * 在以下使用virtqueue_fill():
+             *   - hw/net/virtio-net.c|2058| <<virtio_net_receive_rcu>> virtqueue_fill(q->rx_vq, elems[j], lens[j], j);
+             *   - hw/virtio/vhost-shadow-virtqueue.c|534| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i);
+             *   - hw/virtio/vhost-shadow-virtqueue.c|538| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i++);
+             *   - hw/virtio/virtio.c|1478| <<virtqueue_push>> virtqueue_fill(vq, elem, len, 0);
+	     */
             virtqueue_fill(vq, elem, len, i++);
         }
 
+	/*
+	 * 在以下使用virtqueue_flush():
+         *   - hw/net/virtio-net.c|2062| <<virtio_net_receive_rcu>> virtqueue_flush(q->rx_vq, i);
+         *   - hw/virtio/vhost-shadow-virtqueue.c|535| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+         *   - hw/virtio/vhost-shadow-virtqueue.c|541| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+         *   - hw/virtio/virtio.c|1479| <<virtqueue_push>> virtqueue_flush(vq, 1);
+	 */
         virtqueue_flush(vq, i);
         event_notifier_set(&svq->svq_call);
 
diff --git a/hw/virtio/vhost-user.c b/hw/virtio/vhost-user.c
index a8907cca7..0e4bf15eb 100644
--- a/hw/virtio/vhost-user.c
+++ b/hw/virtio/vhost-user.c
@@ -1318,6 +1318,12 @@ static int vhost_user_get_vring_base(struct vhost_dev *dev,
     return 0;
 }
 
+/*
+ * 在以下使用vhost_set_vring_file():
+ *   - hw/virtio/vhost-user.c|1368| <<vhost_user_set_vring_kick>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_KICK, file);
+ *   - hw/virtio/vhost-user.c|1374| <<vhost_user_set_vring_call>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_CALL, file);
+ *   - hw/virtio/vhost-user.c|1380| <<vhost_user_set_vring_err>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_ERR, file);
+ */
 static int vhost_set_vring_file(struct vhost_dev *dev,
                                 VhostUserRequest request,
                                 struct vhost_vring_file *file)
@@ -1365,18 +1371,36 @@ static int vhost_set_vring_file(struct vhost_dev *dev,
 static int vhost_user_set_vring_kick(struct vhost_dev *dev,
                                      struct vhost_vring_file *file)
 {
+    /*
+     * 在以下使用vhost_set_vring_file():
+     *   - hw/virtio/vhost-user.c|1368| <<vhost_user_set_vring_kick>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_KICK, file);
+     *   - hw/virtio/vhost-user.c|1374| <<vhost_user_set_vring_call>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_CALL, file);
+     *   - hw/virtio/vhost-user.c|1380| <<vhost_user_set_vring_err>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_ERR, file);
+     */
     return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_KICK, file);
 }
 
 static int vhost_user_set_vring_call(struct vhost_dev *dev,
                                      struct vhost_vring_file *file)
 {
+    /*
+     * 在以下使用vhost_set_vring_file():
+     *   - hw/virtio/vhost-user.c|1368| <<vhost_user_set_vring_kick>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_KICK, file);
+     *   - hw/virtio/vhost-user.c|1374| <<vhost_user_set_vring_call>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_CALL, file);
+     *   - hw/virtio/vhost-user.c|1380| <<vhost_user_set_vring_err>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_ERR, file);
+     */
     return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_CALL, file);
 }
 
 static int vhost_user_set_vring_err(struct vhost_dev *dev,
                                     struct vhost_vring_file *file)
 {
+    /*
+     * 在以下使用vhost_set_vring_file():
+     *   - hw/virtio/vhost-user.c|1368| <<vhost_user_set_vring_kick>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_KICK, file);
+     *   - hw/virtio/vhost-user.c|1374| <<vhost_user_set_vring_call>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_CALL, file);
+     *   - hw/virtio/vhost-user.c|1380| <<vhost_user_set_vring_err>> return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_ERR, file);
+     */
     return vhost_set_vring_file(dev, VHOST_USER_SET_VRING_ERR, file);
 }
 
diff --git a/hw/virtio/virtio-crypto.c b/hw/virtio/virtio-crypto.c
index 6fceb3968..5fd7959f0 100644
--- a/hw/virtio/virtio-crypto.c
+++ b/hw/virtio/virtio-crypto.c
@@ -968,6 +968,19 @@ static void virtio_crypto_dataq_bh(void *opaque)
         virtio_crypto_handle_dataq(vdev, q->dataq);
         virtio_queue_set_notification(q->dataq, 1);
 
+        /*
+	 * 在以下使用virtio_queue_empty():
+         *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+         *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+         *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+         *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+         *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+         *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+         *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+         *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+         *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+         *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+	 */
         /* Are we done or did the guest add more buffers? */
         if (virtio_queue_empty(q->dataq)) {
             break;
diff --git a/hw/virtio/virtio-rng.c b/hw/virtio/virtio-rng.c
index 66690a34d..91606430e 100644
--- a/hw/virtio/virtio-rng.c
+++ b/hw/virtio/virtio-rng.c
@@ -37,6 +37,13 @@ static size_t get_request_size(VirtQueue *vq, unsigned quota)
 {
     unsigned int in, out;
 
+    /*
+     * 在以下使用virtqueue_get_avail_bytes():
+     *   - hw/char/virtio-serial-bus.c|321| <<virtio_serial_guest_ready>> virtqueue_get_avail_bytes(vq, &bytes, NULL, 4096, 0);
+     *   - hw/net/virtio-net.c|1657| <<virtio_net_has_buffers>> opaque = virtqueue_get_avail_bytes(q->rx_vq, &in_bytes, NULL,
+     *   - hw/virtio/virtio-rng.c|40| <<get_request_size>> virtqueue_get_avail_bytes(vq, &in, &out, quota, 0);
+     *   - hw/virtio/virtio.c|1610| <<virtqueue_avail_bytes>> virtqueue_get_avail_bytes(vq, &in_total, &out_total, in_bytes, out_bytes);
+     */
     virtqueue_get_avail_bytes(vq, &in, &out, quota, 0);
     return in;
 }
@@ -84,6 +91,19 @@ static void chr_read(void *opaque, const void *buf, size_t size)
     }
     virtio_notify(vdev, vrng->vq);
 
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
     if (!virtio_queue_empty(vrng->vq)) {
         /* If we didn't drain the queue, call virtio_rng_process
          * to take care of asking for more data as appropriate.
diff --git a/hw/virtio/virtio.c b/hw/virtio/virtio.c
index 8fcf6cfd0..c9f6cb641 100644
--- a/hw/virtio/virtio.c
+++ b/hw/virtio/virtio.c
@@ -127,6 +127,22 @@ struct VirtQueue
 
     /* Next head to pop */
     uint16_t last_avail_idx;
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     */
     bool last_avail_wrap_counter;
 
     /* Last avail_idx read from VQ. */
@@ -361,6 +377,11 @@ static void vring_split_desc_read(VirtIODevice *vdev, VRingDesc *desc,
     virtio_tswap16s(vdev, &desc->next);
 }
 
+/*
+ * 在以下使用vring_packed_event_read():
+ *   - hw/virtio/virtio.c|603| <<virtio_queue_packed_set_notification>> vring_packed_event_read(vq->vdev, &caches->used, &e);
+ *   - hw/virtio/virtio.c|3117| <<virtio_packed_should_notify>> vring_packed_event_read(vdev, &caches->avail, &e);
+ */
 static void vring_packed_event_read(VirtIODevice *vdev,
                                     MemoryRegionCache *cache,
                                     VRingPackedDescEvent *e)
@@ -374,6 +395,10 @@ static void vring_packed_event_read(VirtIODevice *vdev,
     e->off_wrap = virtio_lduw_phys_cached(vdev, cache, off_off);
 }
 
+/*
+ * 在以下使用vring_packed_off_wrap_write():
+ *   - hw/virtio/virtio.c|609| <<virtio_queue_packed_set_notification>> vring_packed_off_wrap_write(vq->vdev, &caches->used, off_wrap);
+ */
 static void vring_packed_off_wrap_write(VirtIODevice *vdev,
                                         MemoryRegionCache *cache,
                                         uint16_t off_wrap)
@@ -384,6 +409,10 @@ static void vring_packed_off_wrap_write(VirtIODevice *vdev,
     address_space_cache_invalidate(cache, off, sizeof(off_wrap));
 }
 
+/*
+ * 在以下使用vring_packed_flags_write():
+ *   - hw/virtio/virtio.c|617| <<virtio_queue_packed_set_notification>> vring_packed_flags_write(vq->vdev, &caches->used, e.flags);
+ */
 static void vring_packed_flags_write(VirtIODevice *vdev,
                                      MemoryRegionCache *cache, uint16_t flags)
 {
@@ -572,6 +601,10 @@ static void virtio_queue_split_set_notification(VirtQueue *vq, int enable)
     }
 }
 
+/*
+ * 在以下使用virtio_queue_packed_set_notification():
+ *   - hw/virtio/virtio.c|638| <<virtio_queue_set_notification>> virtio_queue_packed_set_notification(vq, enable);
+ */
 static void virtio_queue_packed_set_notification(VirtQueue *vq, int enable)
 {
     uint16_t off_wrap;
@@ -584,6 +617,11 @@ static void virtio_queue_packed_set_notification(VirtQueue *vq, int enable)
         return;
     }
 
+    /*
+     * 在以下使用vring_packed_event_read():
+     *   - hw/virtio/virtio.c|603| <<virtio_queue_packed_set_notification>> vring_packed_event_read(vq->vdev, &caches->used, &e);
+     *   - hw/virtio/virtio.c|3117| <<virtio_packed_should_notify>> vring_packed_event_read(vdev, &caches->avail, &e);
+     */
     vring_packed_event_read(vq->vdev, &caches->used, &e);
 
     if (!enable) {
@@ -630,6 +668,11 @@ int virtio_queue_ready(VirtQueue *vq)
     return vq->vring.avail != 0;
 }
 
+/*
+ * 在以下使用vring_packed_desc_read_flags():
+ *   - hw/virtio/virtio.c|676| <<vring_packed_desc_read>> vring_packed_desc_read_flags(vdev, &desc->flags, cache, i);
+ *   - hw/virtio/virtio.c|857| <<virtio_queue_packed_empty_rcu>> vring_packed_desc_read_flags(vq->vdev, &desc.flags, &cache->desc, vq->last_avail_idx);
+ */
 static void vring_packed_desc_read_flags(VirtIODevice *vdev,
                                          uint16_t *flags,
                                          MemoryRegionCache *cache,
@@ -640,6 +683,16 @@ static void vring_packed_desc_read_flags(VirtIODevice *vdev,
     *flags = virtio_lduw_phys_cached(vdev, cache, off);
 }
 
+/*
+ * 在以下使用vring_packed_desc_read():
+ *   - hw/virtio/virtio.c|939| <<virtio_queue_packed_poll>> vring_packed_desc_read(vq->vdev, &desc, &caches->desc, shadow_idx, true);
+ *   - hw/virtio/virtio.c|1652| <<virtqueue_packed_read_next_desc>> vring_packed_desc_read(vq->vdev, desc, desc_cache, *next, false);
+ *   - hw/virtio/virtio.c|1707| <<virtqueue_packed_get_avail_bytes>> vring_packed_desc_read(vdev, &desc, desc_cache, idx, true);
+ *   - hw/virtio/virtio.c|1743| <<virtqueue_packed_get_avail_bytes>> vring_packed_desc_read(vdev, &desc, desc_cache, i, false);
+ *   - hw/virtio/virtio.c|2199| <<virtqueue_packed_pop>> vring_packed_desc_read(vdev, &desc, desc_cache, i, true);
+ *   - hw/virtio/virtio.c|2219| <<virtqueue_packed_pop>> vring_packed_desc_read(vdev, &desc, desc_cache, i, false);
+ *   - hw/virtio/virtio.c|2390| <<virtqueue_packed_drop_all>> vring_packed_desc_read(vdev, &desc, desc_cache, vq->last_avail_idx , true);
+ */
 static void vring_packed_desc_read(VirtIODevice *vdev,
                                    VRingPackedDesc *desc,
                                    MemoryRegionCache *cache,
@@ -694,6 +747,10 @@ static void vring_packed_desc_write_flags(VirtIODevice *vdev,
     address_space_cache_invalidate(cache, off, sizeof(desc->flags));
 }
 
+/*
+ * 爱以下使用vring_packed_desc_write():
+ *   - hw/virtio/virtio.c|1063| <<virtqueue_packed_fill_desc>> vring_packed_desc_write(vq->vdev, &desc, &caches->desc, head, strict_order);
+ */
 static void vring_packed_desc_write(VirtIODevice *vdev,
                                     VRingPackedDesc *desc,
                                     MemoryRegionCache *cache,
@@ -707,6 +764,23 @@ static void vring_packed_desc_write(VirtIODevice *vdev,
     vring_packed_desc_write_flags(vdev, desc, cache, i);
 }
 
+/*
+ * 注释:
+ * If the current used_wrap_counter = 1, then the used form in the current wrap is:
+ * - AVAIL = 1
+ * - USED = 1
+ * If the current used_wrap_counter = 0, then the used form in the current wrap is:
+ * - AVAIL = 0
+ * - USED = 0
+ * When the current used_wrap_counter = 1, only 1,1 means used.
+ * When the current used_wrap_counter = 0, only 0,0 means used.
+ *
+ * 在以下使用is_desc_avail():
+ *   - hw/virtio/virtio.c|778| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+ *   - hw/virtio/virtio.c|822| <<virtio_queue_packed_poll>> return is_desc_avail(desc.flags, vq->shadow_avail_wrap_counter);
+ *   - hw/virtio/virtio.c|1473| <<virtqueue_packed_get_avail_bytes>> if (!is_desc_avail(desc.flags, wrap_counter)) {
+ *   - hw/virtio/virtio.c|2071| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+ */
 static inline bool is_desc_avail(uint16_t flags, bool wrap_counter)
 {
     bool avail, used;
@@ -719,8 +793,23 @@ static inline bool is_desc_avail(uint16_t flags, bool wrap_counter)
 /* Fetch avail_idx from VQ memory only when we really need to know if
  * guest has added some buffers.
  * Called within rcu_read_lock().  */
+/*
+ * 在以下使用virtio_queue_empty_rcu():
+ *   - hw/virtio/virtio.c|1844| <<virtqueue_split_pop>> if (virtio_queue_empty_rcu(vq)) {
+ */
 static int virtio_queue_empty_rcu(VirtQueue *vq)
 {
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         return 1;
     }
@@ -736,10 +825,25 @@ static int virtio_queue_empty_rcu(VirtQueue *vq)
     return vring_avail_idx(vq) == vq->last_avail_idx;
 }
 
+/*
+ * 在以下使用virtio_queue_split_empty():
+ *   - hw/virtio/virtio.c|810| <<virtio_queue_empty>> return virtio_queue_split_empty(vq);
+ */
 static int virtio_queue_split_empty(VirtQueue *vq)
 {
     bool empty;
 
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         return 1;
     }
@@ -757,6 +861,11 @@ static int virtio_queue_split_empty(VirtQueue *vq)
     return empty;
 }
 
+/*
+ * 在以下使用virtio_queue_packed_empty_rcu():
+ *   - hw/virtio/virtio.c|802| <<virtio_queue_packed_empty>> return virtio_queue_packed_empty_rcu(vq);
+ *   - hw/virtio/virtio.c|1992| <<virtqueue_packed_pop>> if (virtio_queue_packed_empty_rcu(vq)) {
+ */
 /* Called within rcu_read_lock().  */
 static int virtio_queue_packed_empty_rcu(VirtQueue *vq)
 {
@@ -775,15 +884,59 @@ static int virtio_queue_packed_empty_rcu(VirtQueue *vq)
     vring_packed_desc_read_flags(vq->vdev, &desc.flags, &cache->desc,
                                  vq->last_avail_idx);
 
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     *
+     * 在以下使用is_desc_avail():
+     *   - hw/virtio/virtio.c|778| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|822| <<virtio_queue_packed_poll>> return is_desc_avail(desc.flags, vq->shadow_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1473| <<virtqueue_packed_get_avail_bytes>> if (!is_desc_avail(desc.flags, wrap_counter)) {
+     *   - hw/virtio/virtio.c|2071| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     */
     return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
 }
 
+/*
+ * 在以下使用virtio_queue_packed_empty():
+ *   - hw/virtio/virtio.c|848| <<virtio_queue_empty>> return virtio_queue_packed_empty(vq);
+ */
 static int virtio_queue_packed_empty(VirtQueue *vq)
 {
     RCU_READ_LOCK_GUARD();
+    /*
+     * 在以下使用virtio_queue_packed_empty_rcu():
+     *   - hw/virtio/virtio.c|802| <<virtio_queue_packed_empty>> return virtio_queue_packed_empty_rcu(vq);
+     *   - hw/virtio/virtio.c|1992| <<virtqueue_packed_pop>> if (virtio_queue_packed_empty_rcu(vq)) {
+     */
     return virtio_queue_packed_empty_rcu(vq);
 }
 
+/*
+ * 在以下使用virtio_queue_empty():
+ *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+ *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+ *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+ *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+ *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+ *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+ *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+ *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+ *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+ *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+ */
 int virtio_queue_empty(VirtQueue *vq)
 {
     if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_RING_PACKED)) {
@@ -802,6 +955,10 @@ static bool virtio_queue_split_poll(VirtQueue *vq, unsigned shadow_idx)
     return (uint16_t)shadow_idx != vring_avail_idx(vq);
 }
 
+/*
+ * 在以下使用virtio_queue_packed_poll():
+ *   - hw/virtio/virtio.c|832| <<virtio_queue_poll>> return virtio_queue_packed_poll(vq, shadow_idx);
+ */
 static bool virtio_queue_packed_poll(VirtQueue *vq, unsigned shadow_idx)
 {
     VRingPackedDesc desc;
@@ -819,11 +976,33 @@ static bool virtio_queue_packed_poll(VirtQueue *vq, unsigned shadow_idx)
     vring_packed_desc_read(vq->vdev, &desc, &caches->desc,
                            shadow_idx, true);
 
+    /*
+     * 在以下使用is_desc_avail():
+     *   - hw/virtio/virtio.c|778| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|822| <<virtio_queue_packed_poll>> return is_desc_avail(desc.flags, vq->shadow_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1473| <<virtqueue_packed_get_avail_bytes>> if (!is_desc_avail(desc.flags, wrap_counter)) {
+     *   - hw/virtio/virtio.c|2071| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     */
     return is_desc_avail(desc.flags, vq->shadow_avail_wrap_counter);
 }
 
+/*
+ * 在以下使用virtio_queue_poll():
+ *   - hw/virtio/virtio.c|844| <<virtio_queue_enable_notification_and_check>> return virtio_queue_poll(vq, (unsigned )opaque)
+ */
 static bool virtio_queue_poll(VirtQueue *vq, unsigned shadow_idx)
 {
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         return false;
     }
@@ -897,6 +1076,22 @@ static void virtqueue_packed_rewind(VirtQueue *vq, unsigned int num)
 {
     if (vq->last_avail_idx < num) {
         vq->last_avail_idx = vq->vring.num + vq->last_avail_idx - num;
+        /*
+	 * 在以下使用VirtQueue->last_avail_wrap_counter:
+         *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+         *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+         *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+         *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+         *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+         *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+         *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+	 */
         vq->last_avail_wrap_counter ^= 1;
     } else {
         vq->last_avail_idx -= num;
@@ -936,6 +1131,10 @@ void virtqueue_unpop(VirtQueue *vq, const VirtQueueElement *elem,
  * Returns: true on success, false if @num is greater than the number of in use
  * elements.
  */
+/*
+ * 在以下使用virtqueue_rewind():
+ *   - hw/virtio/virtio-balloon.c|964| <<virtio_balloon_set_status>> (status & VIRTIO_CONFIG_S_DRIVER_OK) && virtqueue_rewind(s->svq, 1)) {
+ */
 bool virtqueue_rewind(VirtQueue *vq, unsigned int num)
 {
     if (num > vq->inuse) {
@@ -967,6 +1166,10 @@ static void virtqueue_split_fill(VirtQueue *vq, const VirtQueueElement *elem,
     vring_used_write(vq, &uelem, idx);
 }
 
+/*
+ * 在以下使用virtqueue_packed_fill():
+ *   - hw/virtio/virtio.c|1294| <<virtqueue_fill>> virtqueue_packed_fill(vq, elem, len, idx);
+ */
 static void virtqueue_packed_fill(VirtQueue *vq, const VirtQueueElement *elem,
                                   unsigned int len, unsigned int idx)
 {
@@ -975,6 +1178,15 @@ static void virtqueue_packed_fill(VirtQueue *vq, const VirtQueueElement *elem,
     vq->used_elems[idx].ndescs = elem->ndescs;
 }
 
+/*
+ * 在 VIRTIO_F_IN_ORDER 模式下, 它不是立刻把请求写回 used ring,
+ * 而是先把"这个请求已经做完了"记到账上, 等前面的请求也都齐了, 再由
+ * virtqueue_ordered_flush() 按顺序一口气往guest 那边提交.
+ *
+ *
+ * 在以下使用virtqueue_ordered_fill():
+ *   - hw/virtio/virtio.c|1310| <<virtqueue_fill>> virtqueue_ordered_fill(vq, elem, len);
+ */
 static void virtqueue_ordered_fill(VirtQueue *vq, const VirtQueueElement *elem,
                                    unsigned int len)
 {
@@ -1025,6 +1237,13 @@ static void virtqueue_ordered_fill(VirtQueue *vq, const VirtQueueElement *elem,
     }
 }
 
+/*
+ * 在以下使用virtqueue_packed_fill_desc():
+ *   - hw/virtio/virtio.c|1125| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+ *   - hw/virtio/virtio.c|1128| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[0], 0, true);
+ *   - hw/virtio/virtio.c|1170| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+ *   - hw/virtio/virtio.c|1186| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[vq->used_idx], 0, true);
+ */
 static void virtqueue_packed_fill_desc(VirtQueue *vq,
                                        const VirtQueueElement *elem,
                                        unsigned int idx,
@@ -1060,9 +1279,19 @@ static void virtqueue_packed_fill_desc(VirtQueue *vq,
         return;
     }
 
+    /*
+     * 只在这里调用
+     */
     vring_packed_desc_write(vq->vdev, &desc, &caches->desc, head, strict_order);
 }
 
+/*
+ * 在以下使用virtqueue_fill():
+ *   - hw/net/virtio-net.c|2058| <<virtio_net_receive_rcu>> virtqueue_fill(q->rx_vq, elems[j], lens[j], j);
+ *   - hw/virtio/vhost-shadow-virtqueue.c|534| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i);
+ *   - hw/virtio/vhost-shadow-virtqueue.c|538| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i++);
+ *   - hw/virtio/virtio.c|1478| <<virtqueue_push>> virtqueue_fill(vq, elem, len, 0);
+ */
 /* Called within rcu_read_lock().  */
 void virtqueue_fill(VirtQueue *vq, const VirtQueueElement *elem,
                     unsigned int len, unsigned int idx)
@@ -1071,10 +1300,29 @@ void virtqueue_fill(VirtQueue *vq, const VirtQueueElement *elem,
 
     virtqueue_unmap_sg(vq, elem, len);
 
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         return;
     }
 
+    /*
+     * 主要使用VIRTIO_F_IN_ORDER的地方:
+     *   - hw/virtio/virtio.c|1309| <<virtqueue_fill>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|1498| <<virtqueue_flush>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2215| <<virtqueue_split_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2389| <<virtqueue_packed_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - include/hw/virtio/virtio.h|404| <<DEFINE_VIRTIO_COMMON_FEATURES>> VIRTIO_F_IN_ORDER, false)
+     */
     if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
         virtqueue_ordered_fill(vq, elem, len);
     } else if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_RING_PACKED)) {
@@ -1084,6 +1332,10 @@ void virtqueue_fill(VirtQueue *vq, const VirtQueueElement *elem,
     }
 }
 
+/*
+ * 在以下使用virtqueue_split_flush():
+ *   - hw/virtio/virtio.c|1495| <<virtqueue_flush>> virtqueue_split_flush(vq, count);
+ */
 /* Called within rcu_read_lock().  */
 static void virtqueue_split_flush(VirtQueue *vq, unsigned int count)
 {
@@ -1104,6 +1356,10 @@ static void virtqueue_split_flush(VirtQueue *vq, unsigned int count)
         vq->signalled_used_valid = false;
 }
 
+/*
+ * 在以下使用virtqueue_packed_flush():
+ *   - hw/virtio/virtio.c|1489| <<virtqueue_flush>> virtqueue_packed_flush(vq, count);
+ */
 static void virtqueue_packed_flush(VirtQueue *vq, unsigned int count)
 {
     unsigned int i, ndescs = 0;
@@ -1122,9 +1378,23 @@ static void virtqueue_packed_flush(VirtQueue *vq, unsigned int count)
      */
     ndescs += vq->used_elems[0].ndescs;
     for (i = 1; i < count; i++) {
+        /*
+	 * 在以下使用virtqueue_packed_fill_desc():
+         *   - hw/virtio/virtio.c|1125| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+         *   - hw/virtio/virtio.c|1128| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[0], 0, true);
+         *   - hw/virtio/virtio.c|1170| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+         *   - hw/virtio/virtio.c|1186| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[vq->used_idx], 0, true);
+	 */
         virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
         ndescs += vq->used_elems[i].ndescs;
     }
+    /*
+     * 在以下使用virtqueue_packed_fill_desc():
+     *   - hw/virtio/virtio.c|1125| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+     *   - hw/virtio/virtio.c|1128| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[0], 0, true);
+     *   - hw/virtio/virtio.c|1170| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+     *   - hw/virtio/virtio.c|1186| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[vq->used_idx], 0, true);
+     */
     virtqueue_packed_fill_desc(vq, &vq->used_elems[0], 0, true);
 
     vq->inuse -= ndescs;
@@ -1136,6 +1406,10 @@ static void virtqueue_packed_flush(VirtQueue *vq, unsigned int count)
     }
 }
 
+/*
+ * 在以下使用virtqueue_ordered_flush():
+ *   - hw/virtio/virtio.c|1499| <<virtqueue_flush>> virtqueue_ordered_flush(vq);
+ */
 static void virtqueue_ordered_flush(VirtQueue *vq)
 {
     unsigned int i = vq->used_idx % vq->vring.num;
@@ -1155,6 +1429,13 @@ static void virtqueue_ordered_flush(VirtQueue *vq)
         return;
     }
 
+    /*
+     * 在以下使用VirtQueueElement->in_order_filled:
+     *   - hw/virtio/virtio.c|1199| <<virtqueue_ordered_fill>> vq->used_elems[i].in_order_filled = true;
+     *   - hw/virtio/virtio.c|1412| <<virtqueue_ordered_flush>> if (!vq->used_elems[i].in_order_filled) {
+     *   - hw/virtio/virtio.c|1417| <<virtqueue_ordered_flush>> while (vq->used_elems[i].in_order_filled) {
+     *   - hw/virtio/virtio.c|1437| <<virtqueue_ordered_flush>> vq->used_elems[i].in_order_filled = false;
+     */
     /* First expected in-order element isn't ready, nothing to do */
     if (!vq->used_elems[i].in_order_filled) {
         return;
@@ -1167,6 +1448,13 @@ static void virtqueue_ordered_flush(VirtQueue *vq)
          * doesn't see invalid descriptors.
          */
         if (packed && i != vq->used_idx) {
+            /*
+	     * 在以下使用virtqueue_packed_fill_desc():
+             *   - hw/virtio/virtio.c|1125| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+             *   - hw/virtio/virtio.c|1128| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[0], 0, true);
+             *   - hw/virtio/virtio.c|1170| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+             *   - hw/virtio/virtio.c|1186| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[vq->used_idx], 0, true);
+	     */
             virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
         } else if (!packed) {
             uelem.id = vq->used_elems[i].index;
@@ -1183,6 +1471,13 @@ static void virtqueue_ordered_flush(VirtQueue *vq)
     }
 
     if (packed) {
+        /*
+	 * 在以下使用virtqueue_packed_fill_desc():
+         *   - hw/virtio/virtio.c|1125| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+         *   - hw/virtio/virtio.c|1128| <<virtqueue_packed_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[0], 0, true);
+         *   - hw/virtio/virtio.c|1170| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[i], ndescs, false);
+         *   - hw/virtio/virtio.c|1186| <<virtqueue_ordered_flush>> virtqueue_packed_fill_desc(vq, &vq->used_elems[vq->used_idx], 0, true);
+	 */
         virtqueue_packed_fill_desc(vq, &vq->used_elems[vq->used_idx], 0, true);
         vq->used_idx += ndescs;
         if (vq->used_idx >= vq->vring.num) {
@@ -1203,13 +1498,39 @@ static void virtqueue_ordered_flush(VirtQueue *vq)
     vq->inuse -= ndescs;
 }
 
+/*
+ * 在以下使用virtqueue_flush():
+ *   - hw/net/virtio-net.c|2062| <<virtio_net_receive_rcu>> virtqueue_flush(q->rx_vq, i);
+ *   - hw/virtio/vhost-shadow-virtqueue.c|535| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+ *   - hw/virtio/vhost-shadow-virtqueue.c|541| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+ *   - hw/virtio/virtio.c|1479| <<virtqueue_push>> virtqueue_flush(vq, 1);
+ */
 void virtqueue_flush(VirtQueue *vq, unsigned int count)
 {
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         vq->inuse -= count;
         return;
     }
 
+    /*
+     * 主要使用VIRTIO_F_IN_ORDER的地方:
+     *   - hw/virtio/virtio.c|1309| <<virtqueue_fill>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|1498| <<virtqueue_flush>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2215| <<virtqueue_split_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2389| <<virtqueue_packed_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - include/hw/virtio/virtio.h|404| <<DEFINE_VIRTIO_COMMON_FEATURES>> VIRTIO_F_IN_ORDER, false)
+     */
     if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
         virtqueue_ordered_flush(vq);
     } else if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_RING_PACKED)) {
@@ -1219,11 +1540,33 @@ void virtqueue_flush(VirtQueue *vq, unsigned int count)
     }
 }
 
+/*
+ * 很多调用, 这里举几个例子:
+ *   - hw/block/virtio-blk.c|72| <<virtio_blk_req_complete>> virtqueue_push(req->vq, &req->elem, req->in_len);
+ *   - hw/net/virtio-net.c|1607| <<virtio_net_handle_ctrl>> virtqueue_push(vq, elem, written);
+ *   - hw/net/virtio-net.c|2726| <<virtio_net_tx_complete>> virtqueue_push(q->tx_vq, q->async_tx.elem, 0);
+ *   - hw/net/virtio-net.c|2838| <<virtio_net_flush_tx>> virtqueue_push(q->tx_vq, elem, 0);
+ *   - hw/scsi/virtio-scsi.c|118| <<virtio_scsi_complete_req>> virtqueue_push(vq, &req->elem, req->qsgl.size + req->resp_iov.size);
+ */
 void virtqueue_push(VirtQueue *vq, const VirtQueueElement *elem,
                     unsigned int len)
 {
     RCU_READ_LOCK_GUARD();
+    /*
+     * 在以下使用virtqueue_fill():
+     *   - hw/net/virtio-net.c|2058| <<virtio_net_receive_rcu>> virtqueue_fill(q->rx_vq, elems[j], lens[j], j);
+     *   - hw/virtio/vhost-shadow-virtqueue.c|534| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i);
+     *   - hw/virtio/vhost-shadow-virtqueue.c|538| <<vhost_svq_flush>> virtqueue_fill(vq, elem, len, i++);
+     *   - hw/virtio/virtio.c|1478| <<virtqueue_push>> virtqueue_fill(vq, elem, len, 0);
+     */
     virtqueue_fill(vq, elem, len, 0);
+    /*
+     * 在以下使用virtqueue_flush():
+     *   - hw/net/virtio-net.c|2062| <<virtio_net_receive_rcu>> virtqueue_flush(q->rx_vq, i);
+     *   - hw/virtio/vhost-shadow-virtqueue.c|535| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+     *   - hw/virtio/vhost-shadow-virtqueue.c|541| <<vhost_svq_flush>> virtqueue_flush(vq, i);
+     *   - hw/virtio/virtio.c|1479| <<virtqueue_push>> virtqueue_flush(vq, 1);
+     */
     virtqueue_flush(vq, 1);
 }
 
@@ -1438,6 +1781,10 @@ static int virtqueue_packed_read_next_desc(VirtQueue *vq,
     return VIRTQUEUE_READ_DESC_MORE;
 }
 
+/*
+ * 在以下使用virtqueue_packed_get_avail_bytes():
+ *   - hw/virtio/virtio.c|1584| <<virtqueue_get_avail_bytes>> virtqueue_packed_get_avail_bytes(vq, in_bytes, out_bytes,
+ */
 /* Called within rcu_read_lock().  */
 static void virtqueue_packed_get_avail_bytes(VirtQueue *vq,
                                              unsigned int *in_bytes,
@@ -1458,6 +1805,22 @@ static void virtqueue_packed_get_avail_bytes(VirtQueue *vq,
     address_space_cache_init_empty(&indirect_desc_cache);
 
     idx = vq->last_avail_idx;
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     */
     wrap_counter = vq->last_avail_wrap_counter;
     total_bufs = in_total = out_total = 0;
 
@@ -1470,6 +1833,13 @@ static void virtqueue_packed_get_avail_bytes(VirtQueue *vq,
         desc_cache = &caches->desc;
 
         vring_packed_desc_read(vdev, &desc, desc_cache, idx, true);
+        /*
+	 * 在以下使用is_desc_avail():
+         *   - hw/virtio/virtio.c|778| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+	 *   - hw/virtio/virtio.c|822| <<virtio_queue_packed_poll>> return is_desc_avail(desc.flags, vq->shadow_avail_wrap_counter);
+	 *   - hw/virtio/virtio.c|1473| <<virtqueue_packed_get_avail_bytes>> if (!is_desc_avail(desc.flags, wrap_counter)) {
+	 *   - hw/virtio/virtio.c|2071| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+	 */
         if (!is_desc_avail(desc.flags, wrap_counter)) {
             break;
         }
@@ -1555,6 +1925,13 @@ err:
     goto done;
 }
 
+/*
+ * 在以下使用virtqueue_get_avail_bytes():
+ *   - hw/char/virtio-serial-bus.c|321| <<virtio_serial_guest_ready>> virtqueue_get_avail_bytes(vq, &bytes, NULL, 4096, 0);
+ *   - hw/net/virtio-net.c|1657| <<virtio_net_has_buffers>> opaque = virtqueue_get_avail_bytes(q->rx_vq, &in_bytes, NULL,
+ *   - hw/virtio/virtio-rng.c|40| <<get_request_size>> virtqueue_get_avail_bytes(vq, &in, &out, quota, 0);
+ *   - hw/virtio/virtio.c|1610| <<virtqueue_avail_bytes>> virtqueue_get_avail_bytes(vq, &in_total, &out_total, in_bytes, out_bytes);
+ */
 int virtqueue_get_avail_bytes(VirtQueue *vq, unsigned int *in_bytes,
                               unsigned int *out_bytes, unsigned max_in_bytes,
                               unsigned max_out_bytes)
@@ -1607,6 +1984,13 @@ int virtqueue_avail_bytes(VirtQueue *vq, unsigned int in_bytes,
 {
     unsigned int in_total, out_total;
 
+    /*
+     * 在以下使用virtqueue_get_avail_bytes():
+     *   - hw/char/virtio-serial-bus.c|321| <<virtio_serial_guest_ready>> virtqueue_get_avail_bytes(vq, &bytes, NULL, 4096, 0);
+     *   - hw/net/virtio-net.c|1657| <<virtio_net_has_buffers>> opaque = virtqueue_get_avail_bytes(q->rx_vq, &in_bytes, NULL,
+     *   - hw/virtio/virtio-rng.c|40| <<get_request_size>> virtqueue_get_avail_bytes(vq, &in, &out, quota, 0);
+     *   - hw/virtio/virtio.c|1610| <<virtqueue_avail_bytes>> virtqueue_get_avail_bytes(vq, &in_total, &out_total, in_bytes, out_bytes);
+     */
     virtqueue_get_avail_bytes(vq, &in_total, &out_total, in_bytes, out_bytes);
     return in_bytes <= in_total && out_bytes <= out_total;
 }
@@ -1730,6 +2114,10 @@ static void *virtqueue_alloc_element(size_t sz, unsigned out_num, unsigned in_nu
     return elem;
 }
 
+/*
+ * 在以下使用virtqueue_split_pop():
+ *   - hw/virtio/virtio.c|2146| <<virtqueue_pop>> return virtqueue_split_pop(vq, sz);
+ */
 static void *virtqueue_split_pop(VirtQueue *vq, size_t sz)
 {
     unsigned int i, head, max, idx;
@@ -1748,6 +2136,9 @@ static void *virtqueue_split_pop(VirtQueue *vq, size_t sz)
     address_space_cache_init_empty(&indirect_desc_cache);
 
     RCU_READ_LOCK_GUARD();
+    /*
+     * 只在这里使用virtio_queue_empty_rcu()
+     */
     if (virtio_queue_empty_rcu(vq)) {
         goto done;
     }
@@ -1857,6 +2248,14 @@ static void *virtqueue_split_pop(VirtQueue *vq, size_t sz)
         elem->in_sg[i] = iov[out_num + i];
     }
 
+    /*
+     * 主要使用VIRTIO_F_IN_ORDER的地方:
+     *   - hw/virtio/virtio.c|1309| <<virtqueue_fill>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|1498| <<virtqueue_flush>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2215| <<virtqueue_split_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2389| <<virtqueue_packed_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - include/hw/virtio/virtio.h|404| <<DEFINE_VIRTIO_COMMON_FEATURES>> VIRTIO_F_IN_ORDER, false)
+     */
     if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
         idx = (vq->last_avail_idx - 1) % vq->vring.num;
         vq->used_elems[idx].index = elem->index;
@@ -1877,6 +2276,46 @@ err_undo_map:
     goto done;
 }
 
+/*
+ * 主要好处是设备和实现都可以更简单, 开销更低.
+ *
+ * 放在 virtqueue 里看, VIRTIO_F_IN_ORDER 的价值主要有这几条:
+ *
+ * 1. 设备不用维护复杂的乱序完成状态
+ * 如果允许乱序, 设备处理完一个请求后, 还得想办法把它填回正确的 used 位置,
+ * 或者额外记录"这个请求对应哪个槽位".
+ * in_order 下只要按取出的顺序完成, used_idx 就能线性往前推, 逻辑简单很多.
+ *
+ * 2. 减少额外查找和缓存 bookkeeping
+ * 在 QEMU 这里, 没 in_order 时 packed ring 允许乱序完成, 所以完成阶段需要靠
+ * used_elems[] 记录和 flush.
+ * 有 in_order 时, 语义上保证"先拿先还", 很多实现可以少做一层重排管理,
+ * host/device 侧状态机会更轻.
+ *
+ * 3. 更适合流水线型后端
+ * 比如请求天然就是 FIFO 消费的, 像某些简单块设备, 软件后端, 单线程处理路径.
+ * 这类场景本来就不会从乱序完成里获益, 声明 in_order 可以让实现直接走顺序推进.
+ *
+ * 4. guest 侧也可以做更激进的简化
+ * 规范给了这个保证后, driver 或 device
+ * 两边都可能减少对"完成顺序不可预测"的防御性处理.
+ *
+ * 但代价也很直接:
+ *
+ * - 可能损失并行性
+ * 如果后面的请求先完成了, 但前面的还没完成, in_order
+ * 语义下就不能先回报后面的完成.
+ * 所以对高并发, 不同请求耗时差异大的设备, 乱序完成通常吞吐/尾延迟更好.
+ *
+ * 一句话总结:
+ *
+ * in_order 是用"少一点调度自由"”换"更简单的实现和更低的状态管理成本"
+ */
+
+/*
+ * 在以下使用virtqueue_packed_pop():
+ *   - hw/virtio/virtio.c|2144| <<virtqueue_pop>> return virtqueue_packed_pop(vq, sz);
+ */
 static void *virtqueue_packed_pop(VirtQueue *vq, size_t sz)
 {
     unsigned int i, max;
@@ -1896,6 +2335,11 @@ static void *virtqueue_packed_pop(VirtQueue *vq, size_t sz)
     address_space_cache_init_empty(&indirect_desc_cache);
 
     RCU_READ_LOCK_GUARD();
+    /*
+     * 在以下使用virtio_queue_packed_empty_rcu():
+     *   - hw/virtio/virtio.c|802| <<virtio_queue_packed_empty>> return virtio_queue_packed_empty_rcu(vq);
+     *   - hw/virtio/virtio.c|1992| <<virtqueue_packed_pop>> if (virtio_queue_packed_empty_rcu(vq)) {
+     */
     if (virtio_queue_packed_empty_rcu(vq)) {
         goto done;
     }
@@ -1912,6 +2356,16 @@ static void *virtqueue_packed_pop(VirtQueue *vq, size_t sz)
 
     i = vq->last_avail_idx;
 
+    /*
+     * typedef struct VRingMemoryRegionCaches {
+     *     struct rcu_head rcu;
+     *     MemoryRegionCache desc;
+     *     MemoryRegionCache avail;
+     *     MemoryRegionCache used;
+     * } VRingMemoryRegionCaches;
+     *
+     * VRingMemoryRegionCaches *caches;
+     */
     caches = vring_get_region_caches(vq);
     if (!caches) {
         virtio_error(vdev, "Region caches not initialized");
@@ -1923,7 +2377,20 @@ static void *virtqueue_packed_pop(VirtQueue *vq, size_t sz)
         goto done;
     }
 
+    /*
+     * struct MemoryRegionCache {
+     *     uint8_t *ptr;
+     *     hwaddr xlat;
+     *     hwaddr len;
+     *     FlatView *fv;
+     *     MemoryRegionSection mrs;
+     *     bool is_write;
+     * };
+     */
     desc_cache = &caches->desc;
+    /*
+     * VRingPackedDesc desc;
+     */
     vring_packed_desc_read(vdev, &desc, desc_cache, i, true);
     id = desc.id;
     if (desc.flags & VRING_DESC_F_INDIRECT) {
@@ -1999,6 +2466,14 @@ static void *virtqueue_packed_pop(VirtQueue *vq, size_t sz)
     elem->index = id;
     elem->ndescs = (desc_cache == &indirect_desc_cache) ? 1 : elem_entries;
 
+    /*
+     * 主要使用VIRTIO_F_IN_ORDER的地方:
+     *   - hw/virtio/virtio.c|1309| <<virtqueue_fill>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|1498| <<virtqueue_flush>> if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2215| <<virtqueue_split_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - hw/virtio/virtio.c|2389| <<virtqueue_packed_pop>> if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
+     *   - include/hw/virtio/virtio.h|404| <<DEFINE_VIRTIO_COMMON_FEATURES>> VIRTIO_F_IN_ORDER, false)
+     */
     if (virtio_vdev_has_feature(vdev, VIRTIO_F_IN_ORDER)) {
         vq->used_elems[vq->last_avail_idx].index = elem->index;
         vq->used_elems[vq->last_avail_idx].len = elem->len;
@@ -2010,10 +2485,42 @@ static void *virtqueue_packed_pop(VirtQueue *vq, size_t sz)
 
     if (vq->last_avail_idx >= vq->vring.num) {
         vq->last_avail_idx -= vq->vring.num;
+	/*
+	 * 在以下使用VirtQueue->last_avail_wrap_counter:
+         *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+         *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+         *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+         *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+         *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+         *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+         *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+	 */
         vq->last_avail_wrap_counter ^= 1;
     }
 
     vq->shadow_avail_idx = vq->last_avail_idx;
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     */
     vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
 
     trace_virtqueue_pop(vq, elem, elem->in_num, elem->out_num);
@@ -2027,12 +2534,29 @@ err_undo_map:
     goto done;
 }
 
+/*
+ * 被特别多调用
+ */
 void *virtqueue_pop(VirtQueue *vq, size_t sz)
 {
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         return NULL;
     }
 
+    /*
+     * 只在这里使用virtqueue_packed_pop()和virtqueue_split_pop()
+     */
     if (virtio_vdev_has_feature(vq->vdev, VIRTIO_F_RING_PACKED)) {
         return virtqueue_packed_pop(vq, sz);
     } else {
@@ -2068,6 +2592,28 @@ static unsigned int virtqueue_packed_drop_all(VirtQueue *vq)
          */
         vring_packed_desc_read(vdev, &desc, desc_cache,
                                vq->last_avail_idx , true);
+        /*
+	 * 在以下使用VirtQueue->last_avail_wrap_counter:
+         *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+         *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+         *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+         *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+         *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+         *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+         *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+         *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+         *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+	 *
+         * 在以下使用is_desc_avail():
+         *   - hw/virtio/virtio.c|778| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+         *   - hw/virtio/virtio.c|822| <<virtio_queue_packed_poll>> return is_desc_avail(desc.flags, vq->shadow_avail_wrap_counter);
+         *   - hw/virtio/virtio.c|1473| <<virtqueue_packed_get_avail_bytes>> if (!is_desc_avail(desc.flags, wrap_counter)) {
+         *   - hw/virtio/virtio.c|2071| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+	 */
         if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
             break;
         }
@@ -2086,6 +2632,22 @@ static unsigned int virtqueue_packed_drop_all(VirtQueue *vq)
         vq->last_avail_idx += elem.ndescs;
         if (vq->last_avail_idx >= vq->vring.num) {
             vq->last_avail_idx -= vq->vring.num;
+	    /*
+	     * 在以下使用VirtQueue->last_avail_wrap_counter:
+             *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+             *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+             *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+             *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+             *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+             *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+             *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+             *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+             *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+             *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+             *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+             *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+             *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+	     */
             vq->last_avail_wrap_counter ^= 1;
         }
     }
@@ -2100,6 +2662,19 @@ static unsigned int virtqueue_split_drop_all(VirtQueue *vq)
     VirtIODevice *vdev = vq->vdev;
     bool fEventIdx = virtio_vdev_has_feature(vdev, VIRTIO_RING_F_EVENT_IDX);
 
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
     while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
         /* works similar to virtqueue_pop but does not map buffers
         * and does not allocate any memory */
@@ -2131,6 +2706,17 @@ unsigned int virtqueue_drop_all(VirtQueue *vq)
 {
     struct VirtIODevice *vdev = vq->vdev;
 
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vq->vdev)) {
         return 0;
     }
@@ -2249,6 +2835,17 @@ static void virtio_notify_vector(VirtIODevice *vdev, uint16_t vector)
     BusState *qbus = qdev_get_parent_bus(DEVICE(vdev));
     VirtioBusClass *k = VIRTIO_BUS_GET_CLASS(qbus);
 
+    /*
+     * 在以下使用virtio_device_disabled():
+     *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+     *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+     */
     if (virtio_device_disabled(vdev)) {
         return;
     }
@@ -2338,6 +2935,22 @@ static void __virtio_queue_reset(VirtIODevice *vdev, uint32_t i)
     vdev->vq[i].last_avail_idx = 0;
     vdev->vq[i].shadow_avail_idx = 0;
     vdev->vq[i].used_idx = 0;
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     */
     vdev->vq[i].last_avail_wrap_counter = true;
     vdev->vq[i].shadow_avail_wrap_counter = true;
     vdev->vq[i].used_wrap_counter = true;
@@ -2615,6 +3228,19 @@ static bool virtio_split_should_notify(VirtIODevice *vdev, VirtQueue *vq)
     bool v;
     /* We need to expose used array entries before checking used event. */
     smp_mb();
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
     /* Always notify when queue is empty (when feature acknowledge) */
     if (virtio_vdev_has_feature(vdev, VIRTIO_F_NOTIFY_ON_EMPTY) &&
         !vq->inuse && virtio_queue_empty(vq)) {
@@ -2658,6 +3284,11 @@ static bool virtio_packed_should_notify(VirtIODevice *vdev, VirtQueue *vq)
         return false;
     }
 
+    /*
+     * 在以下使用vring_packed_event_read():
+     *   - hw/virtio/virtio.c|603| <<virtio_queue_packed_set_notification>> vring_packed_event_read(vq->vdev, &caches->used, &e);
+     *   - hw/virtio/virtio.c|3117| <<virtio_packed_should_notify>> vring_packed_event_read(vdev, &caches->avail, &e);
+     */
     vring_packed_event_read(vdev, &caches->avail, &e);
 
     old = vq->signalled_used;
@@ -3474,6 +4105,22 @@ virtio_load(VirtIODevice *vdev, QEMUFile *f, int version_id)
 
             if (virtio_vdev_has_feature(vdev, VIRTIO_F_RING_PACKED)) {
                 vdev->vq[i].shadow_avail_idx = vdev->vq[i].last_avail_idx;
+                /*
+		 * 在以下使用VirtQueue->last_avail_wrap_counter:
+                 *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+                 *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+                 *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+                 *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+                 *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+                 *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+                 *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+                 *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+                 *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+                 *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+                 *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+                 *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+                 *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+		 */
                 vdev->vq[i].shadow_avail_wrap_counter =
                                         vdev->vq[i].last_avail_wrap_counter;
                 continue;
@@ -3602,6 +4249,15 @@ void virtio_init(VirtIODevice *vdev, uint16_t device_id, size_t config_size)
     } else {
         vdev->config = NULL;
     }
+    /*
+     * 在以下使用qdev_add_vm_change_state_handler():
+     *   - hw/block/virtio-blk.c|1976| <<virtio_blk_device_realize>>
+     *          qdev_add_vm_change_state_handler(dev, virtio_blk_dma_restart_cb, NULL, s);
+     *   - hw/scsi/scsi-bus.c|412| <<scsi_qdev_realize>>
+     *          dev->vmsentry = qdev_add_vm_change_state_handler(DEVICE(dev), scsi_dma_restart_cb, NULL, dev);
+     *   - hw/virtio/virtio.c|4252| <<virtio_init>>
+     *          vdev->vmstate = qdev_add_vm_change_state_handler(DEVICE(vdev), NULL, virtio_vmstate_change, vdev);
+     */
     vdev->vmstate = qdev_add_vm_change_state_handler(DEVICE(vdev),
             NULL, virtio_vmstate_change, vdev);
     vdev->device_endian = virtio_default_endian();
@@ -3705,6 +4361,22 @@ static unsigned int virtio_queue_packed_get_last_avail_idx(VirtIODevice *vdev,
     unsigned int avail, used;
 
     avail = vdev->vq[n].last_avail_idx;
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     */
     avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
 
     used = vdev->vq[n].used_idx;
@@ -3734,6 +4406,22 @@ static void virtio_queue_packed_set_last_avail_idx(VirtIODevice *vdev,
     struct VirtQueue *vq = &vdev->vq[n];
 
     vq->last_avail_idx = vq->shadow_avail_idx = idx & 0x7fff;
+    /*
+     * 在以下使用VirtQueue->last_avail_wrap_counter:
+     *   - hw/virtio/virtio.c|3132| <<global>> VMSTATE_BOOL(last_avail_wrap_counter, struct VirtQueue),
+     *   - hw/virtio/virtio.c|841| <<virtio_queue_packed_empty_rcu>> return !is_desc_avail(desc.flags, vq->last_avail_wrap_counter);
+     *   - hw/virtio/virtio.c|1011| <<virtqueue_packed_rewind>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|1633| <<virtqueue_packed_get_avail_bytes>> wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2222| <<virtqueue_packed_pop>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2226| <<virtqueue_packed_pop>> vq->shadow_avail_wrap_counter = vq->last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|2304| <<virtqueue_packed_drop_all>> if (!is_desc_avail(desc.flags, vq->last_avail_wrap_counter)) {
+     *   - hw/virtio/virtio.c|2322| <<virtqueue_packed_drop_all>> vq->last_avail_wrap_counter ^= 1;
+     *   - hw/virtio/virtio.c|2609| <<__virtio_queue_reset>> vdev->vq[i].last_avail_wrap_counter = true;
+     *   - hw/virtio/virtio.c|3759| <<virtio_load>> vdev->vq[i].last_avail_wrap_counter;
+     *   - hw/virtio/virtio.c|3989| <<virtio_queue_packed_get_last_avail_idx>> avail |= ((uint16_t)vdev->vq[n].last_avail_wrap_counter) << 15;
+     *   - hw/virtio/virtio.c|4018| <<virtio_queue_packed_set_last_avail_idx>> vq->last_avail_wrap_counter =
+     *                                                      vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
+     */
     vq->last_avail_wrap_counter =
         vq->shadow_avail_wrap_counter = !!(idx & 0x8000);
     idx >>= 16;
@@ -3885,6 +4573,19 @@ static bool virtio_queue_host_notifier_aio_poll(void *opaque)
     EventNotifier *n = opaque;
     VirtQueue *vq = container_of(n, VirtQueue, host_notifier);
 
+    /*
+     * 在以下使用virtio_queue_empty():
+     *   - hw/block/virtio-blk.c|1083| <<virtio_blk_handle_vq>> } while (!virtio_queue_empty(vq));
+     *   - hw/char/virtio-serial-bus.c|315| <<virtio_serial_guest_ready>> if (... virtio_queue_empty(vq)) {
+     *   - hw/net/virtio-net.c|1656| <<virtio_net_has_buffers>> while (virtio_queue_empty(q->rx_vq) || n->mergeable_rx_bufs) {
+     *   - hw/scsi/virtio-scsi.c|944| <<virtio_scsi_handle_cmd_vq>> } while (ret != -EINVAL && !virtio_queue_empty(vq));
+     *   - hw/virtio/vhost-shadow-virtqueue.c|370| <<vhost_handle_guest_kick>> } while (!virtio_queue_empty(svq->vq));
+     *   - hw/virtio/virtio-crypto.c|972| <<virtio_crypto_dataq_bh>> if (virtio_queue_empty(q->dataq)) {
+     *   - hw/virtio/virtio-rng.c|94| <<chr_read>> if (!virtio_queue_empty(vrng->vq)) {
+     *   - hw/virtio/virtio.c|2306| <<virtqueue_split_drop_all>> while (!virtio_queue_empty(vq) && vq->inuse < vq->vring.num) {
+     *   - hw/virtio/virtio.c|2845| <<virtio_split_should_notify>> if (... !vq->inuse && virtio_queue_empty(vq)) {
+     *   - hw/virtio/virtio.c|4113| <<virtio_queue_host_notifier_aio_poll>> return vq->vring.desc && !virtio_queue_empty(vq);
+     */
     return vq->vring.desc && !virtio_queue_empty(vq);
 }
 
diff --git a/include/hw/acpi/acpi-defs.h b/include/hw/acpi/acpi-defs.h
index 2e6e34199..3b6d50db7 100644
--- a/include/hw/acpi/acpi-defs.h
+++ b/include/hw/acpi/acpi-defs.h
@@ -59,6 +59,13 @@ struct AcpiGenericAddress {
 
 typedef struct AcpiFadtData {
     struct AcpiGenericAddress pm1a_cnt;   /* PM1a_CNT_BLK */
+    /*
+     * 在以下使用AcpiFadtData->pm1a_evt:
+     *   - hw/acpi/aml-build.c|2367| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.address, 4);
+     *   - hw/acpi/aml-build.c|2376| <<build_fadt>> build_append_int_noprefix(tbl, f->pm1a_evt.bit_width / 8, 1);
+     *   - hw/acpi/aml-build.c|2430| <<build_fadt>> build_append_gas_from_struct(tbl, &f->pm1a_evt);
+     *   - hw/i386/acpi-build.c|177| <<init_common_fadt_data>> .pm1a_evt = { .space_id = as, .bit_width = 4 * 8, .address = io },
+     */
     struct AcpiGenericAddress pm1a_evt;   /* PM1a_EVT_BLK */
     struct AcpiGenericAddress pm_tmr;    /* PM_TMR_BLK */
     struct AcpiGenericAddress gpe0_blk;  /* GPE0_BLK */
diff --git a/include/hw/acpi/acpi.h b/include/hw/acpi/acpi.h
index dc3672db5..a8a40099d 100644
--- a/include/hw/acpi/acpi.h
+++ b/include/hw/acpi/acpi.h
@@ -44,6 +44,12 @@
 #define ACPI_PM_PROP_SCI_INT "sci_int"
 #define ACPI_PM_PROP_ACPI_ENABLE_CMD "acpi_enable_cmd"
 #define ACPI_PM_PROP_ACPI_DISABLE_CMD "acpi_disable_cmd"
+/*
+ * 在以下使用ACPI_PM_PROP_PM_IO_BASE:
+ *   - hw/acpi/ich9.c|402| <<ich9_pm_add_properties>> object_property_add_uint32_ptr(obj, ACPI_PM_PROP_PM_IO_BASE,
+ *   - hw/acpi/piix4.c|437| <<piix4_pm_add_properties>> object_property_add_uint32_ptr(OBJECT(s), ACPI_PM_PROP_PM_IO_BASE,
+ *   - hw/i386/acpi-build.c|147| <<init_common_fadt_data>> uint32_t io = object_property_get_uint(o, ACPI_PM_PROP_PM_IO_BASE, NULL);
+ */
 #define ACPI_PM_PROP_PM_IO_BASE "pm_io_base"
 #define ACPI_PM_PROP_GPE0_BLK "gpe0_blk"
 #define ACPI_PM_PROP_GPE0_BLK_LEN "gpe0_blk_len"
diff --git a/include/hw/acpi/pcihp.h b/include/hw/acpi/pcihp.h
index efce5fd2e..ad0a374cd 100644
--- a/include/hw/acpi/pcihp.h
+++ b/include/hw/acpi/pcihp.h
@@ -51,6 +51,22 @@ typedef struct AcpiPciHpPciStatus {
 #define ACPI_PCIHP_BSEL_DEFAULT 0x0
 
 typedef struct AcpiPciHpState {
+    /*
+     * 在以下使用AcpiPciHpPciStatus->acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS]:
+     *   - hw/acpi/pcihp.c|192| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|193| <<acpi_pcihp_eject_slot>> s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|232| <<acpi_pcihp_update_hotplug_bus>> while (s->acpi_pcihp_pci_status[bsel].down) {
+     *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+     *   - hw/acpi/pcihp.c|236| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable = ~0;
+     *   - hw/acpi/pcihp.c|247| <<acpi_pcihp_update_hotplug_bus>> s->acpi_pcihp_pci_status[bsel].hotplug_enable &= ~(1U << slot);
+     *   - hw/acpi/pcihp.c|319| <<acpi_pcihp_device_plug_cb>> s->acpi_pcihp_pci_status[bsel].up |= (1U << slot);
+     *   - hw/acpi/pcihp.c|375| <<acpi_pcihp_device_unplug_request_cb>> s->acpi_pcihp_pci_status[bsel].down |= (1U << slot);
+     *   - hw/acpi/pcihp.c|409| <<pci_read(PCI_UP_BASE)>> val = s->acpi_pcihp_pci_status[bsel].up;
+     *   - hw/acpi/pcihp.c|411| <<pci_read(PCI_UP_BASE)>> s->acpi_pcihp_pci_status[bsel].up = 0;
+     *   - hw/acpi/pcihp.c|416| <<pci_read(PCI_DOWN_BASE)>> val = s->acpi_pcihp_pci_status[bsel].down;
+     *   - hw/acpi/pcihp.c|423| <<pci_read(PCI_RMV_BASE)>> val = s->acpi_pcihp_pci_status[bsel].hotplug_enable;
+     *   - include/hw/acpi/pcihp.h|97| <<VMSTATE_PCI_HOTPLUG>> VMSTATE_STRUCT_ARRAY_TEST(pcihp.acpi_pcihp_pci_status, state, \
+     */
     AcpiPciHpPciStatus acpi_pcihp_pci_status[ACPI_PCIHP_MAX_HOTPLUG_BUS];
     uint32_t hotplug_select;
     uint32_t acpi_index;
diff --git a/include/hw/core/irq.h b/include/hw/core/irq.h
index 291fdd67d..f21fb5124 100644
--- a/include/hw/core/irq.h
+++ b/include/hw/core/irq.h
@@ -18,6 +18,54 @@ struct IRQState {
 
 void qemu_set_irq(qemu_irq irq, int level);
 
+/*
+ * (gdb) bt  
+ * #0  pic_irq_request
+ * #1  0x0000555555f33a35 in qemu_set_irq
+ * #2  0x0000555555a503a3 in qemu_irq_lower
+ * #3  0x0000555555a50930 in pic_update_irq
+ * #4  0x0000555555a50aea in pic_set_irq
+ * #5  0x0000555555f33a35 in qemu_set_irq
+ * #6  0x0000555555e25c2a in gsi_handler
+ * #7  0x0000555555f33a35 in qemu_set_irq
+ * #8  0x00005555559a1691 in qemu_irq_lower
+ * #9  0x00005555559a19eb in serial_update_irq
+ * #10 0x00005555559a26af in serial_ioport_write
+ * #11 0x0000555555c75e3d in memory_region_write_accessor
+ * #12 0x0000555555c7618f in access_with_adjusted_size
+ * #13 0x0000555555c79952 in memory_region_dispatch_write
+ * #14 0x0000555555c8b943 in flatview_write_continue_step
+ * #15 0x0000555555c8ba18 in flatview_write_continue
+ * #16 0x0000555555c8bb45 in flatview_write
+ * #17 0x0000555555c8bfd2 in address_space_write
+ * #18 0x0000555555c8c04c in address_space_rw
+ * #19 0x0000555555efc2c1 in kvm_handle_io
+ * #20 0x0000555555efd249 in kvm_cpu_exec
+ * #21 0x0000555555f009e5 in kvm_vcpu_thread_fn
+ * #22 0x0000555556153363 in qemu_thread_start
+ * #23 0x00007ffff62a91da in start_thread
+ * #24 0x00007ffff5f038d3 in clone
+ *
+ * (gdb) bt
+ * #0  qemu_set_irq
+ * #1  0x00005555559abedf in qemu_irq_raise
+ * #2  0x00005555559ac24a in serial_update_irq
+ * #3  0x00005555559acf23 in serial_ioport_write
+ * #4  0x0000555555c806e4 in memory_region_write_accessor
+ * #5  0x0000555555c80a36 in access_with_adjusted_size
+ * #6  0x0000555555c841f9 in memory_region_dispatch_write
+ * #7  0x0000555555c961ea in flatview_write_continue_step
+ * #8  0x0000555555c962bf in flatview_write_continue
+ * #9  0x0000555555c963ec in flatview_write
+ * #10 0x0000555555c96879 in address_space_write
+ * #11 0x0000555555c968f3 in address_space_rw
+ * #12 0x0000555555f07101 in kvm_handle_io
+ * #13 0x0000555555f08089 in kvm_cpu_exec
+ * #14 0x0000555555f0b825 in kvm_vcpu_thread_fn
+ * #15 0x000055555616bcab in qemu_thread_start
+ * #16 0x00007ffff6c9a1da in start_thread
+ * #17 0x00007ffff4fcb8d3 in clone
+ */
 static inline void qemu_irq_raise(qemu_irq irq)
 {
     qemu_set_irq(irq, 1);
diff --git a/include/hw/i386/pc.h b/include/hw/i386/pc.h
index 22325324d..964a3964d 100644
--- a/include/hw/i386/pc.h
+++ b/include/hw/i386/pc.h
@@ -107,6 +107,36 @@ struct PCMachineClass {
     /* RAM / address space compat: */
     bool gigabyte_align;
     bool has_reserved_memory;
+    /*
+     * 在以下使用PCMachineClass->enforce_amd_1tb_hole:
+     *   - hw/i386/pc.c|762| <<pc_memory_init>> if (IS_AMD_CPU(&cpu->env) && pcmc->enforce_amd_1tb_hole) {
+     *   - hw/i386/pc.c|1646| <<pc_machine_class_init>> pcmc->enforce_amd_1tb_hole = true;
+     *   - hw/i386/pc_piix.c|561| <<pc_i440fx_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *   - hw/i386/pc_q35.c|493| <<pc_q35_machine_7_0_options>> pcmc->enforce_amd_1tb_hole = false;
+     *
+     * enforce_amd_1tb_hole 是 QEMU x86 PC 机器类型里的一个兼容开关, 用来处理
+     * AMD IOMMU 在接近 1TiB 地址处保留的 HyperTransport GPA 区间.
+     *
+     * - AMD IOMMU 有一段不能用于 DMA map 的 guest physical address:
+     *   0xfd00000000 - 0xffffffffff，也就是 1TiB 前面的 12GiB.
+     * - 如果 vCPU 是 AMD, 并且 enforce_amd_1tb_hole = true, QEMU 会:
+     * 1. 如果可能使用到这段 HT 保留区, 就把 ram-above-4g 的起始地址挪到
+     * 0x10000000000, 也就是 1TiB 之后.
+     * 2. 如果 CPU 物理地址宽度达到 40 bit, 就通过 E820 把这段区域报告给 guest
+     * 为 E820_RESERVED.
+     *
+     * 这里的 40 bit 指 vCPU 暴露给 guest 的物理地址宽度, 不是随便的阈值.
+     * 2^40 = 0x10000000000 = 1 TiB
+     * AMD 这段 HyperTransport reserved GPA 是:
+     * 0xfd00000000 - 0xffffffffff
+     *
+     * 所以关系是:
+     *
+     * - 如果 phys_bits < 40, guest 物理地址空间根本到不了 1TiB 附近,
+     * 这段地址对 guest 不存在, 没必要写进 E820.
+     * - 如果 phys_bits >= 40, guest 能表达/访问到这段 GPA, 那么 firmware/BIOS
+     * 就应该通过 E820 告诉 guest OS: 这段不是 RAM, 是 reserved.
+     */
     bool enforce_amd_1tb_hole;
     bool isa_bios_alias;
 
diff --git a/include/hw/vfio/vfio-container.h b/include/hw/vfio/vfio-container.h
index a7d5c5ed6..894d24326 100644
--- a/include/hw/vfio/vfio-container.h
+++ b/include/hw/vfio/vfio-container.h
@@ -184,6 +184,12 @@ struct VFIOIOMMUClass {
      * @start: starting file offset of the range to map
      * @readonly: map read only if true
      */
+    /*
+     * 在以下使用VFIOIOMMUClass->dma_map_file():
+     *   - hw/vfio/container.c|85| <<vfio_container_dma_map>> if (mfd >= 0 && vioc->dma_map_file) {
+     *   - hw/vfio/container.c|89| <<vfio_container_dma_map>> return vioc->dma_map_file(bcontainer, iova, size, mfd, start + offset,
+     *   - hw/vfio/iommufd.c|910| <<vfio_iommu_iommufd_class_init>> vioc->dma_map_file = iommufd_cdev_map_file;
+     */
     int (*dma_map_file)(const VFIOContainer *bcontainer,
                         hwaddr iova, uint64_t size,
                         int fd, unsigned long start, bool readonly);
diff --git a/include/hw/virtio/virtio.h b/include/hw/virtio/virtio.h
index 6344bd7b6..1826fe6e9 100644
--- a/include/hw/virtio/virtio.h
+++ b/include/hw/virtio/virtio.h
@@ -70,6 +70,13 @@ typedef struct VirtQueueElement
     unsigned int ndescs;
     unsigned int out_num;
     unsigned int in_num;
+    /*
+     * 在以下使用VirtQueueElement->in_order_filled:
+     *   - hw/virtio/virtio.c|1199| <<virtqueue_ordered_fill>> vq->used_elems[i].in_order_filled = true;
+     *   - hw/virtio/virtio.c|1412| <<virtqueue_ordered_flush>> if (!vq->used_elems[i].in_order_filled) {
+     *   - hw/virtio/virtio.c|1417| <<virtqueue_ordered_flush>> while (vq->used_elems[i].in_order_filled) {
+     *   - hw/virtio/virtio.c|1437| <<virtqueue_ordered_flush>> vq->used_elems[i].in_order_filled = false;
+     */
     /* Element has been processed (VIRTIO_F_IN_ORDER) */
     bool in_order_filled;
     hwaddr *in_addr;
@@ -538,6 +545,17 @@ static inline void virtio_set_disabled(VirtIODevice *vdev, bool disable)
     }
 }
 
+/*
+ * 在以下使用virtio_device_disabled():
+ *   - hw/virtio/virtio.c|724| <<virtio_queue_empty_rcu>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|743| <<virtio_queue_split_empty>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|827| <<virtio_queue_poll>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|1074| <<virtqueue_fill>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|1208| <<virtqueue_flush>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|2032| <<virtqueue_pop>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|2134| <<virtqueue_drop_all>> if (virtio_device_disabled(vq->vdev)) {
+ *   - hw/virtio/virtio.c|2252| <<virtio_notify_vector>> if (virtio_device_disabled(vdev)) {
+ */
 static inline bool virtio_device_disabled(VirtIODevice *vdev)
 {
     return unlikely(vdev->disabled || vdev->broken);
diff --git a/migration/channel.c b/migration/channel.c
index 1e2935f92..04b270f7a 100644
--- a/migration/channel.c
+++ b/migration/channel.c
@@ -258,6 +258,9 @@ void migration_channel_connect_outgoing(MigrationState *s, QIOChannel *ioc)
 
     migration_ioc_register_yank(ioc);
     migration_outgoing_setup(ioc);
+    /*
+     * 只在这里调用
+     */
     migration_start_outgoing(s);
 }
 
diff --git a/migration/colo.c b/migration/colo.c
index 2d36f933c..58171c749 100644
--- a/migration/colo.c
+++ b/migration/colo.c
@@ -674,6 +674,14 @@ static void colo_incoming_process_checkpoint(MigrationIncomingState *mis,
         return;
     }
 
+    /*
+     * 在以下使用qemu_loadvm_state_main():
+     *   - migration/colo.c|677| <<colo_incoming_process_checkpoint>> ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
+     *   - migration/postcopy-ram.c|2133| <<postcopy_listen_thread>> load_res = qemu_loadvm_state_main(f, mis, &local_err);
+     *   - migration/savevm.c|2366| <<loadvm_handle_cmd_packaged>> ret = qemu_loadvm_state_main(packf, mis, errp);
+     *   - migration/savevm.c|3049| <<qemu_loadvm_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     *   - migration/savevm.c|3130| <<qemu_load_device_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     */
     ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
     if (ret < 0) {
         return;
diff --git a/migration/cpr.c b/migration/cpr.c
index a0b37007f..f7282311c 100644
--- a/migration/cpr.c
+++ b/migration/cpr.c
@@ -23,6 +23,24 @@
 /*************************************************************************/
 /* cpr state container for all information to be saved. */
 
+/*
+ * 在以下使用cpr_state:
+ *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+ *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+ *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+ *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+ *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+ *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+ *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+ *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+ *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+ *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+ *
+ * typedef struct CprState {
+ *     CprFdList fds;
+ *     CprVFIODeviceList vfio_devices;
+ * } CprState;
+ */
 CprState cpr_state;
 
 /****************************************************************************/
@@ -57,9 +75,33 @@ void cpr_save_fd(const char *name, int id, int fd)
     elem->namelen = strlen(name) + 1;
     elem->id = id;
     elem->fd = fd;
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     */
     QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
 }
 
+/*
+ * 在以下使用find_fd():
+ *   - migration/cpr.c|77| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+ *   - migration/cpr.c|90| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+ *   - migration/cpr.c|99| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+ */
 static CprFd *find_fd(CprFdList *head, const char *name, int id)
 {
     CprFd *elem;
@@ -74,6 +116,29 @@ static CprFd *find_fd(CprFdList *head, const char *name, int id)
 
 void cpr_delete_fd(const char *name, int id)
 {
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     *
+     * 在以下使用find_fd():
+     *   - migration/cpr.c|77| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|90| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|99| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     */
     CprFd *elem = find_fd(&cpr_state.fds, name, id);
 
     if (elem) {
@@ -85,8 +150,44 @@ void cpr_delete_fd(const char *name, int id)
     trace_cpr_delete_fd(name, id);
 }
 
+/*
+ * 在以下使用cpr_find_fd():
+ *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+ *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+ *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+ *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+ *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+ *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+ *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+ *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+ *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+ *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+ */
 int cpr_find_fd(const char *name, int id)
 {
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     *
+     * 在以下使用find_fd():
+     *   - migration/cpr.c|77| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|90| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|99| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     */
     CprFd *elem = find_fd(&cpr_state.fds, name, id);
     int fd = elem ? elem->fd : -1;
 
@@ -96,6 +197,30 @@ int cpr_find_fd(const char *name, int id)
 
 void cpr_resave_fd(const char *name, int id, int fd)
 {
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     *
+     *
+     * 在以下使用find_fd():
+     *   - migration/cpr.c|77| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|90| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|99| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     */
     CprFd *elem = find_fd(&cpr_state.fds, name, id);
     int old_fd = elem ? elem->fd : -1;
 
@@ -112,6 +237,19 @@ void cpr_resave_fd(const char *name, int id, int fd)
 int cpr_open_fd(const char *path, int flags, const char *name, int id,
                 Error **errp)
 {
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     int fd = cpr_find_fd(name, id);
 
     if (fd < 0) {
@@ -127,6 +265,24 @@ bool cpr_walk_fd(cpr_walk_fd_cb cb)
 {
     CprFd *elem;
 
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     */
     QLIST_FOREACH(elem, &cpr_state.fds, next) {
         g_assert(elem->fd >= 0);
         if (!cb(elem->fd)) {
@@ -199,6 +355,24 @@ bool cpr_state_save(MigrationChannel *channel, Error **errp)
     qemu_put_be32(f, QEMU_CPR_FILE_MAGIC);
     qemu_put_be32(f, QEMU_CPR_FILE_VERSION);
 
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     */
     ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
     if (ret) {
         qemu_fclose(f);
@@ -263,6 +437,24 @@ int cpr_state_load(MigrationChannel *channel, Error **errp)
         return -ENOTSUP;
     }
 
+    /*
+     * 在以下使用cpr_state:
+     *   - migration/cpr.c|26| <<global>> CprState cpr_state;
+     *   - hw/vfio/cpr-iommufd.c|63| <<vfio_cpr_save_device>> QLIST_INSERT_HEAD(&cpr_state.vfio_devices, elem, next);
+     *   - hw/vfio/cpr-iommufd.c|68| <<find_device>> CprVFIODeviceList *head = &cpr_state.vfio_devices;
+     *   - migration/cpr.c|60| <<cpr_save_fd>> QLIST_INSERT_HEAD(&cpr_state.fds, elem, next);
+     *   - migration/cpr.c|89| <<cpr_delete_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|121| <<cpr_find_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|136| <<cpr_resave_fd>> CprFd *elem = find_fd(&cpr_state.fds, name, id);
+     *   - migration/cpr.c|167| <<cpr_walk_fd>> QLIST_FOREACH(elem, &cpr_state.fds, next) {
+     *   - migration/cpr.c|239| <<cpr_state_save>> ret = vmstate_save_state(f, &vmstate_cpr_state, &cpr_state, 0, errp);
+     *   - migration/cpr.c|303| <<cpr_state_load>> ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
+     *
+     * typedef struct CprState {
+     *     CprFdList fds;
+     *     CprVFIODeviceList vfio_devices;
+     * } CprState;
+     */
     ret = vmstate_load_state(f, &vmstate_cpr_state, &cpr_state, 1, errp);
     if (ret) {
         qemu_fclose(f);
@@ -318,6 +510,19 @@ int cpr_get_fd_param(const char *name, const char *fdname, int index,
     int fd;
 
     if (cpr_is_incoming()) {
+        /*
+	 * 在以下使用cpr_find_fd():
+         *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+         *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+         *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+         *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+         *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+         *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+         *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+         *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+         *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+         *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+	 */
         fd = cpr_find_fd(name, index);
         if (fd < 0) {
             error_setg(errp, "cannot find saved value for fd %s", fdname);
diff --git a/migration/migration-stats.c b/migration/migration-stats.c
index 3f17b6ac5..efade2ecb 100644
--- a/migration/migration-stats.c
+++ b/migration/migration-stats.c
@@ -61,6 +61,16 @@ void migration_rate_reset(void)
 
 uint64_t migration_transferred_bytes(void)
 {
+    /*
+     * 在以下使用MigrationAtomicStats->multifd_bytes:
+     *   - migration/migration-hmp-cmds.c|200| <<hmp_info_migrate>> g_autofree char *str_multifd = size_to_str(info->ram->multifd_bytes);
+     *   - migration/migration-stats.c|64| <<migration_transferred_bytes>> uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
+     *   - migration/migration.c|1214| <<populate_ram_info>> info->ram->multifd_bytes = qatomic_read(&mig_stats.multifd_bytes);
+     *   - migration/multifd-nocomp.c|184| <<multifd_nocomp_send_prepare>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+     *   - migration/multifd.c|183| <<multifd_send_initial_packet>> qatomic_add(&mig_stats.multifd_bytes, size);
+     *   - migration/multifd.c|864| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, total_size);
+     *   - migration/multifd.c|912| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+     */
     uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
     uint64_t rdma = qatomic_read(&mig_stats.rdma_bytes);
     uint64_t qemu_file = qatomic_read(&mig_stats.qemu_file_transferred);
diff --git a/migration/migration-stats.h b/migration/migration-stats.h
index c0f50144c..86f91ceef 100644
--- a/migration/migration-stats.h
+++ b/migration/migration-stats.h
@@ -59,6 +59,16 @@ typedef struct {
     /*
      * Number of bytes sent through multifd channels.
      */
+    /*
+     * 在以下使用MigrationAtomicStats->multifd_bytes:
+     *   - migration/migration-hmp-cmds.c|200| <<hmp_info_migrate>> g_autofree char *str_multifd = size_to_str(info->ram->multifd_bytes);
+     *   - migration/migration-stats.c|64| <<migration_transferred_bytes>> uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
+     *   - migration/migration.c|1214| <<populate_ram_info>> info->ram->multifd_bytes = qatomic_read(&mig_stats.multifd_bytes);
+     *   - migration/multifd-nocomp.c|184| <<multifd_nocomp_send_prepare>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+     *   - migration/multifd.c|183| <<multifd_send_initial_packet>> qatomic_add(&mig_stats.multifd_bytes, size);
+     *   - migration/multifd.c|864| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, total_size);
+     *   - migration/multifd.c|912| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+     */
     uint64_t multifd_bytes;
     /*
      * Number of pages transferred that were not full of zeros.
diff --git a/migration/migration.c b/migration/migration.c
index 5c9aaa6e5..257fdf275 100644
--- a/migration/migration.c
+++ b/migration/migration.c
@@ -64,11 +64,28 @@
 #include "qemu/sockets.h"
 #include "system/kvm.h"
 
+/*
+ * 没人用NOTIFIER_ELEM_INIT
+ */
 #define NOTIFIER_ELEM_INIT(array, elem)    \
     [elem] = NOTIFIER_WITH_RETURN_LIST_INITIALIZER((array)[elem])
 
+/*
+ * 在以下使用INMIGRATE_DEFAULT_EXIT_ON_ERROR:
+ *   - migration/migration.c|331| <<migration_object_init>> current_incoming->exit_on_error = INMIGRATE_DEFAULT_EXIT_ON_ERROR;
+ *   - migration/migration.c|1816| <<qmp_migrate_incoming>> has_exit_on_error ? exit_on_error : INMIGRATE_DEFAULT_EXIT_ON_ERROR;
+ */
 #define INMIGRATE_DEFAULT_EXIT_ON_ERROR true
 
+/*
+ * 在以下使用migration_state_notifiers[MIG_MODE__MAX]:
+ *   - migration/migration.c|72| <<global>> static GSList *migration_state_notifiers[MIG_MODE__MAX];
+ *   - migration/migration.c|1487| <<add_notifiers>> migration_state_notifiers[mode] =
+ *   - migration/migration.c|1488| <<add_notifiers>> g_slist_prepend(migration_state_notifiers[mode], notify);
+ *   - migration/migration.c|1516| <<migration_remove_notifier>> migration_state_notifiers[mode] =
+ *   - migration/migration.c|1517| <<migration_remove_notifier>> g_slist_remove(migration_state_notifiers[mode], notify);
+ *   - migration/migration.c|1535| <<migration_call_notifiers>> for (elem = migration_state_notifiers[mode]; elem; elem = next) {
+ */
 static GSList *migration_state_notifiers[MIG_MODE__MAX];
 
 /* Messages sent on the return path from destination to source */
@@ -90,9 +107,42 @@ enum mig_rp_message_type {
    migrations at once.  For now we don't need to add
    dynamic creation of migration */
 
+/*
+ * 在以下使用current_migration:
+ *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+ *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+ *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+ *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+ *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+ *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+ *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+ *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+ *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+ *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+ *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+ */
 static MigrationState *current_migration;
+/*
+ * 只在两个函数使用current_incoming:
+ *   - migration/migration.c|103| <<global>> static MigrationIncomingState *current_incoming;
+ *   - migration_object_init()
+ *   - migration/migration.c|423| <<migration_incoming_get_current>> assert(current_incoming);
+ *   - migration/migration.c|424| <<migration_incoming_get_current>> return current_incoming;
+ */
 static MigrationIncomingState *current_incoming;
 
+/*
+ * 在以下使用migration_blockers[MIG_MODE__MAX]:
+ *   - migration/migration.c|105| <<global>> static GSList *migration_blockers[MIG_MODE__MAX];
+ *   - migration/migration.c|1122| <<fill_source_migration_info>> GSList *cur_blocker = migration_blockers[migrate_mode()];
+ *   - migration/migration.c|1742| <<add_blockers>> assert(g_slist_index(migration_blockers[mode],
+ *   - migration/migration.c|1744| <<add_blockers>> migration_blockers[mode] = g_slist_prepend(migration_blockers[mode],
+ *   - migration/migration.c|1785| <<migrate_del_blocker>> migration_blockers[mode] = g_slist_remove(migration_blockers[mode],
+ *   - migration/migration.c|1914| <<migration_is_blocked>> GSList *blockers = migration_blockers[migrate_mode()];
+ *
+ *   { 'enum': 'MigMode',
+ *     'data': [ 'normal', 'cpr-reboot', 'cpr-transfer', 'cpr-exec' ] }
+ */
 static GSList *migration_blockers[MIG_MODE__MAX];
 
 static bool migration_object_check(MigrationState *ms, Error **errp);
@@ -101,6 +151,10 @@ static bool stop_return_path_thread_on_source(MigrationState *s);
 static void migration_release_dst_files(MigrationState *ms);
 static void migration_completion_end(MigrationState *s);
 
+/*
+ * 在以下使用migration_downtime_start():
+ *   - migration/migration.c|332| <<migration_stop_vm>> migration_downtime_start(s);
+ */
 static void migration_downtime_start(MigrationState *s)
 {
     trace_vmstate_downtime_checkpoint("src-downtime-start");
@@ -123,6 +177,10 @@ static void migrate_incoming_ref_outgoing_state(void)
 {
     object_ref(migrate_get_current());
 }
+/*
+ * 在以下使用migrate_incoming_unref_outgoing_state():
+ *   - migration/migration.c|910| <<process_incoming_migration_co>> migrate_incoming_unref_outgoing_state();
+ */
 static void migrate_incoming_unref_outgoing_state(void)
 {
     object_unref(migrate_get_current());
@@ -153,6 +211,10 @@ static void precopy_notify_complete(void)
     trace_migration_precopy_complete();
 }
 
+/*
+ * 在以下使用migration_needs_multiple_sockets():
+ *   - migration/migration.c|281| <<migration_channels_and_transport_compatible>> if (migration_needs_multiple_sockets() &&
+ */
 static bool migration_needs_multiple_sockets(void)
 {
     return migrate_multifd() || migrate_postcopy_preempt();
@@ -292,8 +354,26 @@ static int migration_stop_vm(MigrationState *s, RunState state)
     return ret;
 }
 
+/*
+ * 在以下使用migration_object_init():
+ *   - system/vl.c|3825| <<qemu_init>> migration_object_init();
+ */
 void migration_object_init(void)
 {
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     /* This can only be called once. */
     assert(!current_migration);
     current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
@@ -385,6 +465,20 @@ void migration_shutdown(void)
      * stop the migration using this structure
      */
     migration_cancel();
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     object_unref(OBJECT(current_migration));
 
     /*
@@ -404,6 +498,20 @@ void migration_shutdown(void)
 /* For outgoing */
 MigrationState *migrate_get_current(void)
 {
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     /* This can only be called after the object created. */
     assert(current_migration);
     return current_migration;
@@ -411,6 +519,13 @@ MigrationState *migrate_get_current(void)
 
 MigrationIncomingState *migration_incoming_get_current(void)
 {
+    /*
+     * 只在两个函数使用current_incoming:
+     *   - migration/migration.c|103| <<global>> static MigrationIncomingState *current_incoming;
+     *   - migration_object_init()
+     *   - migration/migration.c|423| <<migration_incoming_get_current>> assert(current_incoming);
+     *   - migration/migration.c|424| <<migration_incoming_get_current>> return current_incoming;
+     */
     assert(current_incoming);
     return current_incoming;
 }
@@ -986,6 +1101,20 @@ void migrate_send_rp_resume_ack(MigrationIncomingState *mis, uint32_t value)
 
 bool migration_is_running(void)
 {
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     MigrationState *s = current_migration;
 
     if (!s) {
@@ -1014,6 +1143,20 @@ bool migration_is_running(void)
 
 static bool migration_is_active(void)
 {
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     MigrationState *s = current_migration;
 
     return (s->state == MIGRATION_STATUS_ACTIVE ||
@@ -1110,6 +1253,18 @@ static void fill_source_migration_info(MigrationInfo *info)
 {
     MigrationState *s = migrate_get_current();
     int state = qatomic_read(&s->state);
+    /*
+     * 在以下使用migration_blockers[MIG_MODE__MAX]:
+     *   - migration/migration.c|105| <<global>> static GSList *migration_blockers[MIG_MODE__MAX];
+     *   - migration/migration.c|1122| <<fill_source_migration_info>> GSList *cur_blocker = migration_blockers[migrate_mode()];
+     *   - migration/migration.c|1742| <<add_blockers>> assert(g_slist_index(migration_blockers[mode],
+     *   - migration/migration.c|1744| <<add_blockers>> migration_blockers[mode] = g_slist_prepend(migration_blockers[mode],
+     *   - migration/migration.c|1785| <<migrate_del_blocker>> migration_blockers[mode] = g_slist_remove(migration_blockers[mode],
+     *   - migration/migration.c|1914| <<migration_is_blocked>> GSList *blockers = migration_blockers[migrate_mode()];
+     *
+     *   { 'enum': 'MigMode',
+     *     'data': [ 'normal', 'cpr-reboot', 'cpr-transfer', 'cpr-exec' ] }
+     */
     GSList *cur_blocker = migration_blockers[migrate_mode()];
 
     info->blocked_reasons = NULL;
@@ -1480,10 +1635,23 @@ void migration_cancel(void)
     }
 }
 
+/*
+ * 在以下使用add_notifiers():
+ *   - migration/migration.c|1497| <<migration_add_notifier_modes>> add_notifiers(notify, modes);
+ */
 static void add_notifiers(NotifierWithReturn *notify, unsigned modes)
 {
     for (MigMode mode = 0; mode < MIG_MODE__MAX; mode++) {
         if (modes & BIT(mode)) {
+            /*
+	     * 在以下使用migration_state_notifiers[MIG_MODE__MAX]:
+             *   - migration/migration.c|72| <<global>> static GSList *migration_state_notifiers[MIG_MODE__MAX];
+             *   - migration/migration.c|1487| <<add_notifiers>> migration_state_notifiers[mode] =
+             *   - migration/migration.c|1488| <<add_notifiers>> g_slist_prepend(migration_state_notifiers[mode], notify);
+             *   - migration/migration.c|1516| <<migration_remove_notifier>> migration_state_notifiers[mode] =
+             *   - migration/migration.c|1517| <<migration_remove_notifier>> g_slist_remove(migration_state_notifiers[mode], notify);
+             *   - migration/migration.c|1535| <<migration_call_notifiers>> for (elem = migration_state_notifiers[mode]; elem; elem = next) {
+	     */
             migration_state_notifiers[mode] =
                 g_slist_prepend(migration_state_notifiers[mode], notify);
         }
@@ -1513,6 +1681,15 @@ void migration_remove_notifier(NotifierWithReturn *notify)
 {
     if (notify->notify) {
         for (MigMode mode = 0; mode < MIG_MODE__MAX; mode++) {
+            /*
+	     * 在以下使用migration_state_notifiers[MIG_MODE__MAX]:
+             *   - migration/migration.c|72| <<global>> static GSList *migration_state_notifiers[MIG_MODE__MAX];
+             *   - migration/migration.c|1487| <<add_notifiers>> migration_state_notifiers[mode] =
+             *   - migration/migration.c|1488| <<add_notifiers>> g_slist_prepend(migration_state_notifiers[mode], notify);
+             *   - migration/migration.c|1516| <<migration_remove_notifier>> migration_state_notifiers[mode] =
+             *   - migration/migration.c|1517| <<migration_remove_notifier>> g_slist_remove(migration_state_notifiers[mode], notify);
+             *   - migration/migration.c|1535| <<migration_call_notifiers>> for (elem = migration_state_notifiers[mode]; elem; elem = next) {
+	     */
             migration_state_notifiers[mode] =
                 g_slist_remove(migration_state_notifiers[mode], notify);
         }
@@ -1532,6 +1709,15 @@ int migration_call_notifiers(MigrationEventType type, Error **errp)
 
     e.type = type;
 
+    /*
+     * 在以下使用migration_state_notifiers[MIG_MODE__MAX]:
+     *   - migration/migration.c|72| <<global>> static GSList *migration_state_notifiers[MIG_MODE__MAX];
+     *   - migration/migration.c|1487| <<add_notifiers>> migration_state_notifiers[mode] =
+     *   - migration/migration.c|1488| <<add_notifiers>> g_slist_prepend(migration_state_notifiers[mode], notify);
+     *   - migration/migration.c|1516| <<migration_remove_notifier>> migration_state_notifiers[mode] =
+     *   - migration/migration.c|1517| <<migration_remove_notifier>> g_slist_remove(migration_state_notifiers[mode], notify);
+     *   - migration/migration.c|1535| <<migration_call_notifiers>> for (elem = migration_state_notifiers[mode]; elem; elem = next) {
+     */
     for (elem = migration_state_notifiers[mode]; elem; elem = next) {
         next = elem->next;
         notifier = (NotifierWithReturn *)elem->data;
@@ -1601,6 +1787,20 @@ bool migration_in_bg_snapshot(void)
 
 bool migration_thread_is_self(void)
 {
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     MigrationState *s = current_migration;
 
     return qemu_thread_is_self(&s->thread);
@@ -1695,10 +1895,27 @@ static bool is_only_migratable(Error **reasonp, unsigned modes, Error **errp)
     return false;
 }
 
+/*
+ * 在以下使用add_blockers():
+ *   - migration/migration.c|1768| <<migrate_add_blocker_modes>> return add_blockers(reasonp, modes, errp);
+ *   - migration/migration.c|1778| <<migrate_add_blocker_internal>> return add_blockers(reasonp, modes, errp);
+ */
 static int add_blockers(Error **reasonp, unsigned modes, Error **errp)
 {
     for (MigMode mode = 0; mode < MIG_MODE__MAX; mode++) {
         if (modes & BIT(mode)) {
+            /*
+	     * 在以下使用migration_blockers[MIG_MODE__MAX]:
+             *   - migration/migration.c|105| <<global>> static GSList *migration_blockers[MIG_MODE__MAX];
+             *   - migration/migration.c|1122| <<fill_source_migration_info>> GSList *cur_blocker = migration_blockers[migrate_mode()];
+             *   - migration/migration.c|1742| <<add_blockers>> assert(g_slist_index(migration_blockers[mode],
+             *   - migration/migration.c|1744| <<add_blockers>> migration_blockers[mode] = g_slist_prepend(migration_blockers[mode],
+             *   - migration/migration.c|1785| <<migrate_del_blocker>> migration_blockers[mode] = g_slist_remove(migration_blockers[mode],
+             *   - migration/migration.c|1914| <<migration_is_blocked>> GSList *blockers = migration_blockers[migrate_mode()];
+             *
+             *   { 'enum': 'MigMode',
+             *     'data': [ 'normal', 'cpr-reboot', 'cpr-transfer', 'cpr-exec' ] }
+	     */
             assert(g_slist_index(migration_blockers[mode],
                                  *reasonp) == -1);
             migration_blockers[mode] = g_slist_prepend(migration_blockers[mode],
@@ -1710,14 +1927,59 @@ static int add_blockers(Error **reasonp, unsigned modes, Error **errp)
 
 int migrate_add_blocker(Error **reasonp, Error **errp)
 {
+    /*
+     * 在以下使用migrate_add_blocker_modes():
+     *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+     *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+     *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+     *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+     *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+     *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+     *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+     *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+     */
     return migrate_add_blocker_modes(reasonp, -1u, errp);
 }
 
 int migrate_add_blocker_normal(Error **reasonp, Error **errp)
 {
+    /*
+     * 在以下使用migrate_add_blocker_modes():
+     *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+     *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+     *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+     *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+     *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+     *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+     *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+     *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+     */
     return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
 }
 
+/*
+ * 在以下使用migrate_add_blocker_modes():
+ *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+ *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+ *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+ *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+ *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+ *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+ *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+ *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+ *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+ *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+ *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+ *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+ */
 int migrate_add_blocker_modes(Error **reasonp, unsigned modes, Error **errp)
 {
     if (is_only_migratable(reasonp, modes, errp)) {
@@ -1725,9 +1987,18 @@ int migrate_add_blocker_modes(Error **reasonp, unsigned modes, Error **errp)
     } else if (is_busy(reasonp, errp)) {
         return -EBUSY;
     }
+    /*
+     * 在以下使用add_blockers():
+     *   - migration/migration.c|1768| <<migrate_add_blocker_modes>> return add_blockers(reasonp, modes, errp);
+     *   - migration/migration.c|1778| <<migrate_add_blocker_internal>> return add_blockers(reasonp, modes, errp);
+     */
     return add_blockers(reasonp, modes, errp);
 }
 
+/*
+ * 在以下使用migrate_add_blocker_internal():
+ *   - dump/dump.c|2189| <<qmp_dump_guest_memory>> if (migrate_add_blocker_internal(&dump_migration_blocker, errp)) {
+ */
 int migrate_add_blocker_internal(Error **reasonp, Error **errp)
 {
     unsigned modes = BIT(MIG_MODE__MAX) - 1;
@@ -1735,6 +2006,11 @@ int migrate_add_blocker_internal(Error **reasonp, Error **errp)
     if (is_busy(reasonp, errp)) {
         return -EBUSY;
     }
+    /*
+     * 在以下使用add_blockers():
+     *   - migration/migration.c|1768| <<migrate_add_blocker_modes>> return add_blockers(reasonp, modes, errp);
+     *   - migration/migration.c|1778| <<migrate_add_blocker_internal>> return add_blockers(reasonp, modes, errp);
+     */
     return add_blockers(reasonp, modes, errp);
 }
 
@@ -1742,6 +2018,18 @@ void migrate_del_blocker(Error **reasonp)
 {
     if (*reasonp) {
         for (MigMode mode = 0; mode < MIG_MODE__MAX; mode++) {
+            /*
+	     * 在以下使用migration_blockers[MIG_MODE__MAX]:
+             *   - migration/migration.c|105| <<global>> static GSList *migration_blockers[MIG_MODE__MAX];
+             *   - migration/migration.c|1122| <<fill_source_migration_info>> GSList *cur_blocker = migration_blockers[migrate_mode()];
+             *   - migration/migration.c|1742| <<add_blockers>> assert(g_slist_index(migration_blockers[mode],
+             *   - migration/migration.c|1744| <<add_blockers>> migration_blockers[mode] = g_slist_prepend(migration_blockers[mode],
+             *   - migration/migration.c|1785| <<migrate_del_blocker>> migration_blockers[mode] = g_slist_remove(migration_blockers[mode],
+             *   - migration/migration.c|1914| <<migration_is_blocked>> GSList *blockers = migration_blockers[migrate_mode()];
+             *
+             *   { 'enum': 'MigMode',
+             *     'data': [ 'normal', 'cpr-reboot', 'cpr-transfer', 'cpr-exec' ] }
+	     */
             migration_blockers[mode] = g_slist_remove(migration_blockers[mode],
                                                       *reasonp);
         }
@@ -1871,6 +2159,18 @@ void qmp_migrate_pause(Error **errp)
 
 bool migration_is_blocked(Error **errp)
 {
+    /*
+     * 在以下使用migration_blockers[MIG_MODE__MAX]:
+     *   - migration/migration.c|105| <<global>> static GSList *migration_blockers[MIG_MODE__MAX];
+     *   - migration/migration.c|1122| <<fill_source_migration_info>> GSList *cur_blocker = migration_blockers[migrate_mode()];
+     *   - migration/migration.c|1742| <<add_blockers>> assert(g_slist_index(migration_blockers[mode],
+     *   - migration/migration.c|1744| <<add_blockers>> migration_blockers[mode] = g_slist_prepend(migration_blockers[mode],
+     *   - migration/migration.c|1785| <<migrate_del_blocker>> migration_blockers[mode] = g_slist_remove(migration_blockers[mode],
+     *   - migration/migration.c|1914| <<migration_is_blocked>> GSList *blockers = migration_blockers[migrate_mode()];
+     *
+     *   { 'enum': 'MigMode',
+     *     'data': [ 'normal', 'cpr-reboot', 'cpr-transfer', 'cpr-exec' ] }
+     */
     GSList *blockers = migration_blockers[migrate_mode()];
 
     if (qemu_savevm_state_blocked(errp)) {
@@ -3021,6 +3321,20 @@ static MigThrError postcopy_pause(MigrationState *s)
 
 void migration_file_set_error(int ret, Error *err)
 {
+    /*
+     * 在以下使用current_migration:
+     *   - migration/migration.c|102| <<global>> static MigrationState *current_migration;
+     *   - migration/migration.c|307| <<migration_object_init>> assert(!current_migration);
+     *   - migration/migration.c|308| <<migration_object_init>> current_migration = MIGRATION_OBJ(object_new(TYPE_MIGRATION));
+     *   - migration/migration.c|333| <<migration_object_init>> migration_object_check(current_migration, &error_fatal);
+     *   - migration/migration.c|397| <<migration_shutdown>> object_unref(OBJECT(current_migration));
+     *   - migration/migration.c|417| <<migrate_get_current>> assert(current_migration);
+     *   - migration/migration.c|418| <<migrate_get_current>> return current_migration;
+     *   - migration/migration.c|998| <<migration_is_running>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1026| <<migration_is_active>> MigrationState *s = current_migration;
+     *   - migration/migration.c|1644| <<migration_thread_is_self>> MigrationState *s = current_migration;
+     *   - migration/migration.c|3064| <<migration_file_set_error>> MigrationState *s = current_migration;
+     */
     MigrationState *s = current_migration;
 
     WITH_QEMU_LOCK_GUARD(&s->qemu_file_lock) {
@@ -3489,6 +3803,11 @@ static void qemu_savevm_wait_unplug(MigrationState *s, int old_state,
  * Master migration thread on the source VM.
  * It drives the migration and pumps the data down the outgoing channel.
  */
+/*
+ * 在以下使用migration_thread():
+ *   - migration/migration.c|4131| <<migration_start_outgoing>> qemu_thread_create(&s->thread,
+ *            MIGRATION_THREAD_SRC_MAIN, migration_thread, s, QEMU_THREAD_JOINABLE);
+ */
 static void *migration_thread(void *opaque)
 {
     MigrationState *s = opaque;
@@ -3502,6 +3821,9 @@ static void *migration_thread(void *opaque)
 
     update_iteration_initial_status(s);
 
+    /*
+     * 只在这里调用!!!
+     */
     if (!multifd_send_setup()) {
         goto out;
     }
@@ -3752,6 +4074,10 @@ done:
     return NULL;
 }
 
+/*
+ * 在以下使用migration_start_outgoing():
+ *   - migration/channel.c|261| <<migration_channel_connect_outgoing>> migration_start_outgoing(s);
+ */
 void migration_start_outgoing(MigrationState *s)
 {
     Error *local_err = NULL;
diff --git a/migration/multifd-device-state.c b/migration/multifd-device-state.c
index 91d5d8155..badcbd426 100644
--- a/migration/multifd-device-state.c
+++ b/migration/multifd-device-state.c
@@ -76,6 +76,10 @@ static void multifd_prepare_header_device_state(MultiFDSendParams *p)
     p->iovs_num++;
 }
 
+/*
+ * 在以下使用multifd_device_state_send_prepare():
+ *   - migration/multifd.c|718| <<multifd_send_thread>> multifd_device_state_send_prepare(p);
+ */
 void multifd_device_state_send_prepare(MultiFDSendParams *p)
 {
     MultiFDDeviceState_t *device_state = &p->data->u.device_state;
@@ -115,6 +119,12 @@ bool multifd_queue_device_state(char *idstr, uint32_t instance_id,
     device_state->buf = g_memdup2(data, len);
     device_state->buf_len = len;
 
+    /*
+     * 在以下使用multifd_send():
+     *   - migration/multifd-device-state.c|118| <<multifd_queue_device_state>> if (!multifd_send(&multifd_send_device_state->send_data)) {
+     *   - migration/multifd-nocomp.c|352| <<multifd_queue_page>> if (!multifd_send(&multifd_ram_send)) {
+     *   - migration/multifd-nocomp.c|415| <<multifd_ram_flush_and_sync>> if (!multifd_send(&multifd_ram_send)) {
+     */
     if (!multifd_send(&multifd_send_device_state->send_data)) {
         multifd_send_data_clear(multifd_send_device_state->send_data);
         return false;
diff --git a/migration/multifd-nocomp.c b/migration/multifd-nocomp.c
index 9f7a792fa..297902537 100644
--- a/migration/multifd-nocomp.c
+++ b/migration/multifd-nocomp.c
@@ -86,6 +86,11 @@ static void multifd_nocomp_send_cleanup(MultiFDSendParams *p, Error **errp)
     p->iov = NULL;
 }
 
+/*
+ * 在以下使用multifd_ram_prepare_header():
+ *   - migration/multifd-nocomp.c|129| <<multifd_nocomp_send_prepare>> multifd_ram_prepare_header(p);
+ *   - migration/multifd-nocomp.c|467| <<multifd_send_prepare_common>> multifd_ram_prepare_header(p);
+ */
 static void multifd_ram_prepare_header(MultiFDSendParams *p)
 {
     p->iov[0].iov_len = p->packet_len;
@@ -93,6 +98,11 @@ static void multifd_ram_prepare_header(MultiFDSendParams *p)
     p->iovs_num++;
 }
 
+/*
+ * 在以下使用multifd_send_prepare_iovs():
+ *   - migration/multifd-nocomp.c|118| <<multifd_nocomp_send_prepare>> multifd_send_prepare_iovs(p);
+ *   - migration/multifd-nocomp.c|132| <<multifd_nocomp_send_prepare>> multifd_send_prepare_iovs(p);
+ */
 static void multifd_send_prepare_iovs(MultiFDSendParams *p)
 {
     MultiFDPages_t *pages = &p->data->u.ram;
@@ -107,6 +117,10 @@ static void multifd_send_prepare_iovs(MultiFDSendParams *p)
     p->next_packet_size = pages->normal_num * page_size;
 }
 
+/*
+ * 只在这里使用multifd_nocomp_send_prepare():
+ *   - migration/multifd-nocomp.c|481| <<global>> .send_prepare = multifd_nocomp_send_prepare,
+ */
 static int multifd_nocomp_send_prepare(MultiFDSendParams *p, Error **errp)
 {
     bool use_zero_copy_send = migrate_zero_copy_send();
@@ -115,6 +129,11 @@ static int multifd_nocomp_send_prepare(MultiFDSendParams *p, Error **errp)
     multifd_send_zero_page_detect(p);
 
     if (migrate_mapped_ram()) {
+        /*
+	 * 在以下使用multifd_send_prepare_iovs():
+         *   - migration/multifd-nocomp.c|118| <<multifd_nocomp_send_prepare>> multifd_send_prepare_iovs(p);
+         *   - migration/multifd-nocomp.c|132| <<multifd_nocomp_send_prepare>> multifd_send_prepare_iovs(p);
+	 */
         multifd_send_prepare_iovs(p);
         multifd_set_file_bitmap(p);
 
@@ -126,12 +145,32 @@ static int multifd_nocomp_send_prepare(MultiFDSendParams *p, Error **errp)
          * Only !zerocopy needs the header in IOV; zerocopy will
          * send it separately.
          */
+        /*
+	 * 在以下使用multifd_ram_prepare_header():
+         *   - migration/multifd-nocomp.c|129| <<multifd_nocomp_send_prepare>> multifd_ram_prepare_header(p);
+         *   - migration/multifd-nocomp.c|467| <<multifd_send_prepare_common>> multifd_ram_prepare_header(p);
+	 */
         multifd_ram_prepare_header(p);
     }
 
+    /*
+     * 在以下使用multifd_send_prepare_iovs():
+     *   - migration/multifd-nocomp.c|118| <<multifd_nocomp_send_prepare>> multifd_send_prepare_iovs(p);
+     *   - migration/multifd-nocomp.c|132| <<multifd_nocomp_send_prepare>> multifd_send_prepare_iovs(p);
+     */
     multifd_send_prepare_iovs(p);
     p->flags |= MULTIFD_FLAG_NOCOMP;
 
+    /*
+     * 在以下使用multifd_send_fill_packet():
+     *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+     */
     multifd_send_fill_packet(p);
 
     if (use_zero_copy_send) {
@@ -142,6 +181,16 @@ static int multifd_nocomp_send_prepare(MultiFDSendParams *p, Error **errp)
             return -1;
         }
 
+        /*
+	 * 在以下使用MigrationAtomicStats->multifd_bytes:
+	 *   - migration/migration-hmp-cmds.c|200| <<hmp_info_migrate>> g_autofree char *str_multifd = size_to_str(info->ram->multifd_bytes);
+	 *   - migration/migration-stats.c|64| <<migration_transferred_bytes>> uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
+	 *   - migration/migration.c|1214| <<populate_ram_info>> info->ram->multifd_bytes = qatomic_read(&mig_stats.multifd_bytes);
+	 *   - migration/multifd-nocomp.c|184| <<multifd_nocomp_send_prepare>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+         *   - migration/multifd.c|183| <<multifd_send_initial_packet>> qatomic_add(&mig_stats.multifd_bytes, size);
+         *   - migration/multifd.c|864| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, total_size);
+         *   - migration/multifd.c|912| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+	 */
         qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
     }
 
@@ -320,6 +369,10 @@ static inline void multifd_enqueue(MultiFDPages_t *pages, ram_addr_t offset)
     pages->offset[pages->num++] = offset;
 }
 
+/*
+ * 在以下使用multifd_queue_page():
+ *   - migration/ram.c|1346| <<ram_save_multifd_page>> if (!multifd_queue_page(block, offset)) {
+ */
 /* Returns true if enqueue successful, false otherwise */
 bool multifd_queue_page(RAMBlock *block, ram_addr_t offset)
 {
@@ -349,6 +402,12 @@ retry:
      * After flush, always retry.
      */
     if (pages->block != block || multifd_queue_full(pages)) {
+        /*
+	 * 在以下使用multifd_send():
+	 *   - migration/multifd-device-state.c|118| <<multifd_queue_device_state>> if (!multifd_send(&multifd_send_device_state->send_data)) {
+	 *   - migration/multifd-nocomp.c|352| <<multifd_queue_page>> if (!multifd_send(&multifd_ram_send)) {
+	 *   - migration/multifd-nocomp.c|415| <<multifd_ram_flush_and_sync>> if (!multifd_send(&multifd_ram_send)) {
+	 */
         if (!multifd_send(&multifd_ram_send)) {
             return false;
         }
@@ -412,6 +471,12 @@ int multifd_ram_flush_and_sync(QEMUFile *f)
     }
 
     if (!multifd_payload_empty(multifd_ram_send)) {
+        /*
+	 * 在以下使用multifd_send():
+	 *   - migration/multifd-device-state.c|118| <<multifd_queue_device_state>> if (!multifd_send(&multifd_send_device_state->send_data)) {
+	 *   - migration/multifd-nocomp.c|352| <<multifd_queue_page>> if (!multifd_send(&multifd_ram_send)) {
+	 *   - migration/multifd-nocomp.c|415| <<multifd_ram_flush_and_sync>> if (!multifd_send(&multifd_ram_send)) {
+	 */
         if (!multifd_send(&multifd_ram_send)) {
             error_report("%s: multifd_send fail", __func__);
             return -1;
@@ -448,6 +513,11 @@ int multifd_ram_flush_and_sync(QEMUFile *f)
 bool multifd_send_prepare_common(MultiFDSendParams *p)
 {
     MultiFDPages_t *pages = &p->data->u.ram;
+    /*
+     * 在以下使用multifd_ram_prepare_header():
+     *   - migration/multifd-nocomp.c|129| <<multifd_nocomp_send_prepare>> multifd_ram_prepare_header(p);
+     *   - migration/multifd-nocomp.c|467| <<multifd_send_prepare_common>> multifd_ram_prepare_header(p);
+     */
     multifd_ram_prepare_header(p);
     multifd_send_zero_page_detect(p);
 
diff --git a/migration/multifd-qatzip.c b/migration/multifd-qatzip.c
index 7419e5dc0..92d8df865 100644
--- a/migration/multifd-qatzip.c
+++ b/migration/multifd-qatzip.c
@@ -211,6 +211,16 @@ static int qatzip_send_prepare(MultiFDSendParams *p, Error **errp)
 
 out:
     p->flags |= MULTIFD_FLAG_QATZIP;
+    /*
+     * 在以下使用multifd_send_fill_packet():
+     *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+     */
     multifd_send_fill_packet(p);
     return 0;
 }
diff --git a/migration/multifd-qpl.c b/migration/multifd-qpl.c
index 52902eb00..5f71ebc5b 100644
--- a/migration/multifd-qpl.c
+++ b/migration/multifd-qpl.c
@@ -490,6 +490,16 @@ static int multifd_qpl_send_prepare(MultiFDSendParams *p, Error **errp)
 
 out:
     p->flags |= MULTIFD_FLAG_QPL;
+    /*
+     * 在以下使用multifd_send_fill_packet():
+     *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+     */
     multifd_send_fill_packet(p);
     return 0;
 }
diff --git a/migration/multifd-uadk.c b/migration/multifd-uadk.c
index fd7cd9b5e..0f152223c 100644
--- a/migration/multifd-uadk.c
+++ b/migration/multifd-uadk.c
@@ -200,6 +200,16 @@ static int multifd_uadk_send_prepare(MultiFDSendParams *p, Error **errp)
     }
 out:
     p->flags |= MULTIFD_FLAG_UADK;
+    /*
+     * 在以下使用multifd_send_fill_packet():
+     *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+     */
     multifd_send_fill_packet(p);
     return 0;
 }
diff --git a/migration/multifd-zlib.c b/migration/multifd-zlib.c
index 8820b2a78..ed9ffeb2b 100644
--- a/migration/multifd-zlib.c
+++ b/migration/multifd-zlib.c
@@ -156,6 +156,16 @@ static int multifd_zlib_send_prepare(MultiFDSendParams *p, Error **errp)
 
 out:
     p->flags |= MULTIFD_FLAG_ZLIB;
+    /*
+     * 在以下使用multifd_send_fill_packet():
+     *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+     */
     multifd_send_fill_packet(p);
     return 0;
 }
diff --git a/migration/multifd-zstd.c b/migration/multifd-zstd.c
index 3c2dcf76b..6769c40a4 100644
--- a/migration/multifd-zstd.c
+++ b/migration/multifd-zstd.c
@@ -143,6 +143,16 @@ static int multifd_zstd_send_prepare(MultiFDSendParams *p, Error **errp)
 
 out:
     p->flags |= MULTIFD_FLAG_ZSTD;
+    /*
+     * 在以下使用multifd_send_fill_packet():
+     *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+     *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+     */
     multifd_send_fill_packet(p);
     return 0;
 }
diff --git a/migration/multifd.c b/migration/multifd.c
index 035cb70f7..51fd413ad 100644
--- a/migration/multifd.c
+++ b/migration/multifd.c
@@ -48,6 +48,12 @@ typedef struct {
 struct {
     MultiFDSendParams *params;
 
+    /*
+     * 在以下使用multifd_send_mutex:
+     *   - migration/multifd.c|360| <<multifd_send>> QEMU_LOCK_GUARD(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|563| <<multifd_send_cleanup_state>> qemu_mutex_destroy(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|929| <<multifd_send_setup>> qemu_mutex_init(&multifd_send_state->multifd_send_mutex);
+     */
     /* multifd_send() body is not thread safe, needs serialization */
     QemuMutex multifd_send_mutex;
 
@@ -155,8 +161,22 @@ void multifd_register_ops(int method, const MultiFDMethods *ops)
     multifd_ops[method] = ops;
 }
 
+/*
+ * 在以下使用multifd_send_initial_packet():
+ *   - migration/multifd.c|690| <<multifd_send_thread>> if (multifd_send_initial_packet(p, &local_err) < 0) {
+ */
 static int multifd_send_initial_packet(MultiFDSendParams *p, Error **errp)
 {
+    /*
+     * typedef struct {
+     *     uint32_t magic;
+     *     uint32_t version;
+     *     unsigned char uuid[16]; // QemuUUID
+     *     uint8_t id;
+     *     uint8_t unused1[7];     // Reserved for future use
+     *     uint64_t unused2[4];    // Reserved for future use
+     * } __attribute__((packed)) MultiFDInit_t;
+     */
     MultiFDInit_t msg = {};
     size_t size = sizeof(msg);
     int ret;
@@ -166,10 +186,24 @@ static int multifd_send_initial_packet(MultiFDSendParams *p, Error **errp)
     msg.id = p->id;
     memcpy(msg.uuid, &qemu_uuid.data, sizeof(msg.uuid));
 
+    /*
+     * MultiFDSendParams *p:
+     * -> QIOChannel *c;
+     */
     ret = qio_channel_write_all(p->c, (char *)&msg, size, errp);
     if (ret != 0) {
         return -1;
     }
+    /*
+     * 在以下使用MigrationAtomicStats->multifd_bytes:
+     *   - migration/migration-hmp-cmds.c|200| <<hmp_info_migrate>> g_autofree char *str_multifd = size_to_str(info->ram->multifd_bytes);
+     *   - migration/migration-stats.c|64| <<migration_transferred_bytes>> uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
+     *   - migration/migration.c|1214| <<populate_ram_info>> info->ram->multifd_bytes = qatomic_read(&mig_stats.multifd_bytes);
+     *   - migration/multifd-nocomp.c|184| <<multifd_nocomp_send_prepare>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+     *   - migration/multifd.c|183| <<multifd_send_initial_packet>> qatomic_add(&mig_stats.multifd_bytes, size);
+     *   - migration/multifd.c|864| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, total_size);
+     *   - migration/multifd.c|912| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+     */
     qatomic_add(&mig_stats.multifd_bytes, size);
     return 0;
 }
@@ -219,6 +253,16 @@ static int multifd_recv_initial_packet(QIOChannel *c, Error **errp)
     return msg.id;
 }
 
+/*
+ * 在以下使用multifd_send_fill_packet():
+ *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+ *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+ *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+ *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+ *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+ *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+ *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+ */
 /* Fills a RAM multifd packet */
 void multifd_send_fill_packet(MultiFDSendParams *p)
 {
@@ -310,6 +354,14 @@ static int multifd_recv_unfill_packet(MultiFDRecvParams *p, Error **errp)
     return multifd_recv_unfill_packet_ram(p, errp);
 }
 
+/*
+ * 在以下使用multifd_send_should_exit():
+ *   - migration/multifd.c|368| <<multifd_send>> if (multifd_send_should_exit()) {
+ *   - migration/multifd.c|390| <<multifd_send>> if (multifd_send_should_exit()) {
+ *   - migration/multifd.c|645| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+ *   - migration/multifd.c|662| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+ *   - migration/multifd.c|700| <<multifd_send_thread>> if (multifd_send_should_exit()) {
+ */
 static bool multifd_send_should_exit(void)
 {
     return qatomic_read(&multifd_send_state->exiting);
@@ -346,17 +398,48 @@ static void multifd_send_kick_main(MultiFDSendParams *p)
  *
  * Returns true if succeed, false otherwise.
  */
+/*
+ * 在以下使用multifd_send():
+ *   - migration/multifd-device-state.c|118| <<multifd_queue_device_state>> if (!multifd_send(&multifd_send_device_state->send_data)) {
+ *   - migration/multifd-nocomp.c|352| <<multifd_queue_page>> if (!multifd_send(&multifd_ram_send)) {
+ *   - migration/multifd-nocomp.c|415| <<multifd_ram_flush_and_sync>> if (!multifd_send(&multifd_ram_send)) {
+ */
 bool multifd_send(MultiFDSendData **send_data)
 {
     int i;
     static int next_channel;
     MultiFDSendParams *p = NULL; /* make happy gcc */
+    /*
+     * typedef struct MultiFDPayload {
+     *     MultiFDPages_t ram;
+     *     MultiFDDeviceState_t device_state;
+     * } MultiFDPayload;
+     *
+     * struct MultiFDSendData {
+     *     MultiFDPayloadType type;
+     *     MultiFDPayload u;
+     * };
+     */
     MultiFDSendData *tmp;
 
+    /*
+     * 在以下使用multifd_send_should_exit():
+     *   - migration/multifd.c|368| <<multifd_send>> if (multifd_send_should_exit()) {
+     *   - migration/multifd.c|390| <<multifd_send>> if (multifd_send_should_exit()) {
+     *   - migration/multifd.c|645| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+     *   - migration/multifd.c|662| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+     *   - migration/multifd.c|700| <<multifd_send_thread>> if (multifd_send_should_exit()) {
+     */
     if (multifd_send_should_exit()) {
         return false;
     }
 
+    /*
+     * 在以下使用multifd_send_mutex:
+     *   - migration/multifd.c|360| <<multifd_send>> QEMU_LOCK_GUARD(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|563| <<multifd_send_cleanup_state>> qemu_mutex_destroy(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|929| <<multifd_send_setup>> qemu_mutex_init(&multifd_send_state->multifd_send_mutex);
+     */
     QEMU_LOCK_GUARD(&multifd_send_state->multifd_send_mutex);
 
     /* We wait here, until at least one channel is ready */
@@ -369,14 +452,32 @@ bool multifd_send(MultiFDSendData **send_data)
      */
     next_channel %= migrate_multifd_channels();
     for (i = next_channel;; i = (i + 1) % migrate_multifd_channels()) {
+        /*
+	 * 在以下使用multifd_send_should_exit():
+         *   - migration/multifd.c|368| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|390| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|645| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|662| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|700| <<multifd_send_thread>> if (multifd_send_should_exit()) {
+	 */
         if (multifd_send_should_exit()) {
             return false;
         }
+        /*
+	 * MultiFDSendParams *p = NULL;
+	 */
         p = &multifd_send_state->params[i];
         /*
          * Lockless read to p->pending_job is safe, because only multifd
          * sender thread can clear it.
          */
+        /*
+	 * 在以下使用MultiFDSendParams->pending_job:
+         *   - migration/multifd.c|398| <<multifd_send>> if (qatomic_read(&p->pending_job) == false) {
+         *   - migration/multifd.c|424| <<multifd_send>> qatomic_store_release(&p->pending_job, true);
+         *   - migration/multifd.c|708| <<multifd_send_thread>> if (qatomic_load_acquire(&p->pending_job)) {
+         *   - migration/multifd.c|762| <<multifd_send_thread>> qatomic_store_release(&p->pending_job, false);
+	 */
         if (qatomic_read(&p->pending_job) == false) {
             next_channel = (i + 1) % migrate_multifd_channels();
             break;
@@ -403,6 +504,13 @@ bool multifd_send(MultiFDSendData **send_data)
      * Making sure p->data is setup before marking pending_job=true. Pairs
      * with the qatomic_load_acquire() in multifd_send_thread().
      */
+    /*
+     * 在以下使用MultiFDSendParams->pending_job:
+     *   - migration/multifd.c|398| <<multifd_send>> if (qatomic_read(&p->pending_job) == false) {
+     *   - migration/multifd.c|424| <<multifd_send>> qatomic_store_release(&p->pending_job, true);
+     *   - migration/multifd.c|708| <<multifd_send_thread>> if (qatomic_load_acquire(&p->pending_job)) {
+     *   - migration/multifd.c|762| <<multifd_send_thread>> qatomic_store_release(&p->pending_job, false);
+     */
     qatomic_store_release(&p->pending_job, true);
     qemu_sem_post(&p->sem);
 
@@ -560,6 +668,12 @@ static void multifd_send_cleanup_state(void)
     multifd_device_state_send_cleanup();
     qemu_sem_destroy(&multifd_send_state->channels_created);
     qemu_sem_destroy(&multifd_send_state->channels_ready);
+    /*
+     * 在以下使用multifd_send_mutex:
+     *   - migration/multifd.c|360| <<multifd_send>> QEMU_LOCK_GUARD(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|563| <<multifd_send_cleanup_state>> qemu_mutex_destroy(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|929| <<multifd_send_setup>> qemu_mutex_init(&multifd_send_state->multifd_send_mutex);
+     */
     qemu_mutex_destroy(&multifd_send_state->multifd_send_mutex);
     g_free(multifd_send_state->params);
     multifd_send_state->params = NULL;
@@ -618,6 +732,14 @@ int multifd_send_sync_main(MultiFDSyncReq req)
     for (i = 0; i < migrate_multifd_channels(); i++) {
         MultiFDSendParams *p = &multifd_send_state->params[i];
 
+        /*
+	 * 在以下使用multifd_send_should_exit():
+         *   - migration/multifd.c|368| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|390| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|645| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|662| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|700| <<multifd_send_thread>> if (multifd_send_should_exit()) {
+	 */
         if (multifd_send_should_exit()) {
             return -1;
         }
@@ -635,6 +757,14 @@ int multifd_send_sync_main(MultiFDSyncReq req)
     for (i = 0; i < migrate_multifd_channels(); i++) {
         MultiFDSendParams *p = &multifd_send_state->params[i];
 
+        /*
+	 * 在以下使用multifd_send_should_exit():
+         *   - migration/multifd.c|368| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|390| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|645| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|662| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|700| <<multifd_send_thread>> if (multifd_send_should_exit()) {
+	 */
         if (multifd_send_should_exit()) {
             return -1;
         }
@@ -663,6 +793,9 @@ static void *multifd_send_thread(void *opaque)
     rcu_register_thread();
 
     if (use_packets) {
+        /*
+	 * 只在这里调用
+	 */
         if (multifd_send_initial_packet(p, &local_err) < 0) {
             ret = -1;
             goto out;
@@ -673,6 +806,14 @@ static void *multifd_send_thread(void *opaque)
         qemu_sem_post(&multifd_send_state->channels_ready);
         qemu_sem_wait(&p->sem);
 
+        /*
+	 * 在以下使用multifd_send_should_exit():
+         *   - migration/multifd.c|368| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|390| <<multifd_send>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|645| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|662| <<multifd_send_sync_main>> if (multifd_send_should_exit()) {
+         *   - migration/multifd.c|700| <<multifd_send_thread>> if (multifd_send_should_exit()) {
+	 */
         if (multifd_send_should_exit()) {
             break;
         }
@@ -681,6 +822,13 @@ static void *multifd_send_thread(void *opaque)
          * Read pending_job flag before p->data.  Pairs with the
          * qatomic_store_release() in multifd_send().
          */
+	/*
+	 * 在以下使用MultiFDSendParams->pending_job:
+         *   - migration/multifd.c|398| <<multifd_send>> if (qatomic_read(&p->pending_job) == false) {
+         *   - migration/multifd.c|424| <<multifd_send>> qatomic_store_release(&p->pending_job, true);
+         *   - migration/multifd.c|708| <<multifd_send_thread>> if (qatomic_load_acquire(&p->pending_job)) {
+         *   - migration/multifd.c|762| <<multifd_send_thread>> qatomic_store_release(&p->pending_job, false);
+	 */
         if (qatomic_load_acquire(&p->pending_job)) {
             bool is_device_state = multifd_payload_device_state(p->data);
             size_t total_size;
@@ -691,11 +839,23 @@ static void *multifd_send_thread(void *opaque)
             assert(!multifd_payload_empty(p->data));
 
             if (is_device_state) {
+                /*
+		 * 只在这里调用
+		 */
                 multifd_device_state_send_prepare(p);
 
                 /* Device state packets cannot be sent via zerocopy */
                 write_flags_masked |= QIO_CHANNEL_WRITE_FLAG_ZERO_COPY;
             } else {
+                /*
+		 * 所有的可能:
+		 *   - migration/multifd-nocomp.c|481| <<global>> .send_prepare = multifd_nocomp_send_prepare,
+		 *   - migration/multifd-qatzip.c|384| <<global>> .send_prepare = qatzip_send_prepare,
+		 *   - migration/multifd-qpl.c|701| <<global>> .send_prepare = multifd_qpl_send_prepare,
+		 *   - migration/multifd-uadk.c|313| <<global>> .send_prepare = multifd_uadk_send_prepare,
+		 *   - migration/multifd-zlib.c|283| <<global>> .send_prepare = multifd_zlib_send_prepare,
+		 *   - migration/multifd-zstd.c|271| <<global>> .send_prepare = multifd_zstd_send_prepare
+		 */
                 ret = multifd_send_state->ops->send_prepare(p, &local_err);
                 if (ret != 0) {
                     break;
@@ -725,6 +885,16 @@ static void *multifd_send_thread(void *opaque)
                 break;
             }
 
+            /*
+	     * 在以下使用MigrationAtomicStats->multifd_bytes:
+	     *   - migration/migration-hmp-cmds.c|200| <<hmp_info_migrate>> g_autofree char *str_multifd = size_to_str(info->ram->multifd_bytes);
+	     *   - migration/migration-stats.c|64| <<migration_transferred_bytes>> uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
+	     *   - migration/migration.c|1214| <<populate_ram_info>> info->ram->multifd_bytes = qatomic_read(&mig_stats.multifd_bytes);
+	     *   - migration/multifd-nocomp.c|184| <<multifd_nocomp_send_prepare>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+	     *   - migration/multifd.c|183| <<multifd_send_initial_packet>> qatomic_add(&mig_stats.multifd_bytes, size);
+	     *   - migration/multifd.c|864| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, total_size);
+	     *   - migration/multifd.c|912| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+	     */
             qatomic_add(&mig_stats.multifd_bytes, total_size);
 
             p->next_packet_size = 0;
@@ -735,6 +905,13 @@ static void *multifd_send_thread(void *opaque)
              * free".  Pairs with the smp_mb_acquire() in
              * multifd_send().
              */
+            /*
+	     * 在以下使用MultiFDSendParams->pending_job:
+	     *   - migration/multifd.c|398| <<multifd_send>> if (qatomic_read(&p->pending_job) == false) {
+	     *   - migration/multifd.c|424| <<multifd_send>> qatomic_store_release(&p->pending_job, true);
+	     *   - migration/multifd.c|708| <<multifd_send_thread>> if (qatomic_load_acquire(&p->pending_job)) {
+	     *   - migration/multifd.c|762| <<multifd_send_thread>> qatomic_store_release(&p->pending_job, false);
+	     */
             qatomic_store_release(&p->pending_job, false);
         } else {
             MultiFDSyncReq req = qatomic_read(&p->pending_sync);
@@ -749,12 +926,32 @@ static void *multifd_send_thread(void *opaque)
             /* Only push the SYNC message if it involves a remote sync */
             if (req == MULTIFD_SYNC_ALL) {
                 p->flags = MULTIFD_FLAG_SYNC;
+                /*
+		 * 在以下使用multifd_send_fill_packet():
+                 *   - migration/multifd-nocomp.c|135| <<multifd_nocomp_send_prepare>> multifd_send_fill_packet(p);
+                 *   - migration/multifd-qatzip.c|214| <<qatzip_send_prepare>> multifd_send_fill_packet(p);
+                 *   - migration/multifd-qpl.c|493| <<multifd_qpl_send_prepare>> multifd_send_fill_packet(p);
+                 *   - migration/multifd-uadk.c|203| <<multifd_uadk_send_prepare>> multifd_send_fill_packet(p);
+                 *   - migration/multifd-zlib.c|159| <<multifd_zlib_send_prepare>> multifd_send_fill_packet(p);
+                 *   - migration/multifd-zstd.c|146| <<multifd_zstd_send_prepare>> multifd_send_fill_packet(p);
+                 *   - migration/multifd.c|876| <<multifd_send_thread>> multifd_send_fill_packet(p);
+		 */
                 multifd_send_fill_packet(p);
                 ret = qio_channel_write_all(p->c, (void *)p->packet,
                                             p->packet_len, &local_err);
                 if (ret != 0) {
                     break;
                 }
+                /*
+		 * 在以下使用MigrationAtomicStats->multifd_bytes:
+		 *   - migration/migration-hmp-cmds.c|200| <<hmp_info_migrate>> g_autofree char *str_multifd = size_to_str(info->ram->multifd_bytes);
+		 *   - migration/migration-stats.c|64| <<migration_transferred_bytes>> uint64_t multifd = qatomic_read(&mig_stats.multifd_bytes);
+		 *   - migration/migration.c|1214| <<populate_ram_info>> info->ram->multifd_bytes = qatomic_read(&mig_stats.multifd_bytes);
+		 *   - migration/multifd-nocomp.c|184| <<multifd_nocomp_send_prepare>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+		 *   - migration/multifd.c|183| <<multifd_send_initial_packet>> qatomic_add(&mig_stats.multifd_bytes, size);
+		 *   - migration/multifd.c|864| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, total_size);
+		 *   - migration/multifd.c|912| <<multifd_send_thread>> qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
+		 */
                 /* p->next_packet_size will always be zero for a SYNC packet */
                 qatomic_add(&mig_stats.multifd_bytes, p->packet_len);
             }
@@ -911,6 +1108,10 @@ static bool multifd_new_send_channel_create(gpointer opaque, Error **errp)
     return true;
 }
 
+/*
+ * 在以下使用multifd_send_setup():
+ *   migration/migration.c|3807| <<migration_thread>> if (!multifd_send_setup()) {
+ */
 bool multifd_send_setup(void)
 {
     MigrationState *s = migrate_get_current();
@@ -926,6 +1127,12 @@ bool multifd_send_setup(void)
     thread_count = migrate_multifd_channels();
     multifd_send_state = g_malloc0(sizeof(*multifd_send_state));
     multifd_send_state->params = g_new0(MultiFDSendParams, thread_count);
+    /*
+     * 在以下使用multifd_send_mutex:
+     *   - migration/multifd.c|360| <<multifd_send>> QEMU_LOCK_GUARD(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|563| <<multifd_send_cleanup_state>> qemu_mutex_destroy(&multifd_send_state->multifd_send_mutex);
+     *   - migration/multifd.c|929| <<multifd_send_setup>> qemu_mutex_init(&multifd_send_state->multifd_send_mutex);
+     */
     qemu_mutex_init(&multifd_send_state->multifd_send_mutex);
     qemu_sem_init(&multifd_send_state->channels_created, 0);
     qemu_sem_init(&multifd_send_state->channels_ready, 0);
diff --git a/migration/multifd.h b/migration/multifd.h
index fbc35702b..1d0531b99 100644
--- a/migration/multifd.h
+++ b/migration/multifd.h
@@ -214,6 +214,13 @@ typedef struct {
      * For both of these fields, they're only set by the requesters, and
      * cleared by the multifd sender threads.
      */
+    /*
+     * 在以下使用MultiFDSendParams->pending_job:
+     *   - migration/multifd.c|398| <<multifd_send>> if (qatomic_read(&p->pending_job) == false) {
+     *   - migration/multifd.c|424| <<multifd_send>> qatomic_store_release(&p->pending_job, true);
+     *   - migration/multifd.c|708| <<multifd_send_thread>> if (qatomic_load_acquire(&p->pending_job)) {
+     *   - migration/multifd.c|762| <<multifd_send_thread>> qatomic_store_release(&p->pending_job, false);
+     */
     bool pending_job;
     MultiFDSyncReq pending_sync;
 
diff --git a/migration/options.c b/migration/options.c
index 7556fbc06..77dcaf7a4 100644
--- a/migration/options.c
+++ b/migration/options.c
@@ -357,6 +357,34 @@ bool migrate_late_block_activate(void)
     return s->capabilities[MIGRATION_CAPABILITY_LATE_BLOCK_ACTIVATE];
 }
 
+/*
+ * 在以下使用migrate_multifd():
+ *   - migration/channel.c|114| <<migration_has_main_and_multifd_channels>> if (migrate_multifd() && !multifd_recv_all_channels_created()) {
+ *   - migration/channel.c|171| <<migration_channel_identify>> assert(migrate_multifd());
+ *   - migration/channel.c|180| <<migration_channel_identify>> } else if (mis->from_src_file && migrate_multifd()) {
+ *   - migration/file.c|146| <<file_create_incoming_channels>> if (migrate_multifd()) {
+ *   - migration/migration.c|208| <<migration_needs_multiple_sockets>> return migrate_multifd() || migrate_postcopy_preempt();
+ *   - migration/migration.c|253| <<migration_needs_extra_fds>> return migrate_multifd() && migrate_direct_io();
+ *   - migration/multifd-device-state.c|128| <<multifd_device_state_supported>> return migrate_multifd() && !migrate_mapped_ram() &&
+ *   - migration/multifd-nocomp.c|380| <<multifd_ram_sync_per_section>> if (!migrate_multifd()) {
+ *   - migration/multifd-nocomp.c|394| <<multifd_ram_sync_per_round>> if (!migrate_multifd()) {
+ *   - migration/multifd-nocomp.c|410| <<multifd_ram_flush_and_sync>> if (!migrate_multifd() || migration_in_postcopy()) {
+ *   - migration/multifd.c|574| <<multifd_send_shutdown>> if (!migrate_multifd()) {
+ *   - migration/multifd.c|922| <<multifd_send_setup>> if (!migrate_multifd()) {
+ *   - migration/multifd.c|1109| <<multifd_recv_shutdown>> if (migrate_multifd()) {
+ *   - migration/multifd.c|1152| <<multifd_recv_cleanup>> if (!migrate_multifd()) {
+ *   - migration/multifd.c|1175| <<multifd_recv_sync_main>> if (!migrate_multifd()) {
+ *   - migration/multifd.c|1463| <<multifd_recv_setup>> if (multifd_recv_state || !migrate_multifd()) {
+ *   - migration/multifd.c|1518| <<multifd_recv_all_channels_created>> if (!migrate_multifd()) {
+ *   - migration/options.c|685| <<migrate_caps_check>> if (!migrate_multifd() && migrate_incoming_started()) {
+ *   - migration/ram.c|2068| <<ram_save_target_page>> if (!migrate_multifd()
+ *   - migration/ram.c|2075| <<ram_save_target_page>> if (migrate_multifd() && !migration_in_postcopy()) {
+ *   - migration/ram.c|3168| <<ram_save_setup>> if (migrate_multifd()) {
+ *   - migration/ram.c|4124| <<read_ramblock_mapped_ram>> if (migrate_multifd()) {
+ *   - migration/ram.c|4439| <<ram_load_precopy>> if (migrate_multifd() &&
+ *   - migration/ram.c|4663| <<ram_save_postcopy_prepare>> if (migrate_multifd()) {
+ *   - migration/socket.c|151| <<socket_connect_incoming>> if (migrate_multifd()) 
+ */
 bool migrate_multifd(void)
 {
     MigrationState *s = migrate_get_current();
diff --git a/migration/postcopy-ram.c b/migration/postcopy-ram.c
index f5ef93f19..68566293a 100644
--- a/migration/postcopy-ram.c
+++ b/migration/postcopy-ram.c
@@ -2129,6 +2129,14 @@ static void *postcopy_listen_thread(void *opaque)
      */
     qemu_file_set_blocking(f, true, &error_fatal);
 
+    /*
+     * 在以下使用qemu_loadvm_state_main():
+     *   - migration/colo.c|677| <<colo_incoming_process_checkpoint>> ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
+     *   - migration/postcopy-ram.c|2133| <<postcopy_listen_thread>> load_res = qemu_loadvm_state_main(f, mis, &local_err);
+     *   - migration/savevm.c|2366| <<loadvm_handle_cmd_packaged>> ret = qemu_loadvm_state_main(packf, mis, errp);
+     *   - migration/savevm.c|3049| <<qemu_loadvm_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     *   - migration/savevm.c|3130| <<qemu_load_device_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     */
     /* TODO: sanity check that only postcopiable data will be loaded here */
     load_res = qemu_loadvm_state_main(f, mis, &local_err);
 
diff --git a/migration/ram.c b/migration/ram.c
index 979751f61..061b9bd39 100644
--- a/migration/ram.c
+++ b/migration/ram.c
@@ -1217,6 +1217,10 @@ void ram_release_page(const char *rbname, uint64_t offset)
  * @pss: current PSS channel
  * @offset: offset inside the block for the page
  */
+/*
+ * 在以下使用save_zero_page():
+ *   - migration/ram.c|2070| <<ram_save_target_page>> if (save_zero_page(rs, pss, offset)) {
+ */
 static int save_zero_page(RAMState *rs, PageSearchStatus *pss,
                           ram_addr_t offset)
 {
@@ -1307,6 +1311,10 @@ static int save_normal_page(PageSearchStatus *pss, RAMBlock *block,
  * @block: block that contains the page we want to send
  * @offset: offset inside the block for the page
  */
+/*
+ * 在以下使用ram_save_page():
+ *   - migration/ram.c|2079| <<ram_save_target_page>> return ram_save_page(rs, pss);
+ */
 static int ram_save_page(RAMState *rs, PageSearchStatus *pss)
 {
     int pages = -1;
@@ -1341,6 +1349,10 @@ static int ram_save_page(RAMState *rs, PageSearchStatus *pss)
     return pages;
 }
 
+/*
+ * 在以下使用ram_save_multifd_page():
+ *   - migration/ram.c|2076| <<ram_save_target_page>> return ram_save_multifd_page(pss->block, offset);
+ */
 static int ram_save_multifd_page(RAMBlock *block, ram_addr_t offset)
 {
     if (!multifd_queue_page(block, offset)) {
@@ -2049,6 +2061,11 @@ int ram_save_queue_pages(const char *rbname, ram_addr_t start, ram_addr_t len,
  * @rs: current RAM state
  * @pss: data about the page we want to send
  */
+/*
+ * 在以下使用ram_save_target_page():
+ *   - migration/ram.c|2183| <<ram_save_host_page_urgent>> if (ram_save_target_page(rs, pss) != 1) {
+ *   - migration/ram.c|2253| <<ram_save_host_page>> tmppages = ram_save_target_page(rs, pss);
+ */
 static int ram_save_target_page(RAMState *rs, PageSearchStatus *pss)
 {
     ram_addr_t offset = ((ram_addr_t)pss->page) << TARGET_PAGE_BITS;
@@ -2073,9 +2090,15 @@ static int ram_save_target_page(RAMState *rs, PageSearchStatus *pss)
     }
 
     if (migrate_multifd() && !migration_in_postcopy()) {
+        /*
+	 * 只在这里使用
+	 */
         return ram_save_multifd_page(pss->block, offset);
     }
 
+    /*
+     * 只在以下使用
+     */
     return ram_save_page(rs, pss);
 }
 
@@ -2179,6 +2202,11 @@ static int ram_save_host_page_urgent(PageSearchStatus *pss)
         page_dirty = migration_bitmap_clear_dirty(rs, pss->block, pss->page);
 
         if (page_dirty) {
+            /*
+	     * 在以下使用ram_save_target_page():
+             *   - migration/ram.c|2183| <<ram_save_host_page_urgent>> if (ram_save_target_page(rs, pss) != 1) {
+             *   - migration/ram.c|2253| <<ram_save_host_page>> tmppages = ram_save_target_page(rs, pss);
+	     */
             /* Be strict to return code; it must be 1, or what else? */
             if (ram_save_target_page(rs, pss) != 1) {
                 error_report_once("%s: ram_save_target_page failed", __func__);
@@ -2250,6 +2278,11 @@ static int ram_save_host_page(RAMState *rs, PageSearchStatus *pss)
             if (preempt_active) {
                 qemu_mutex_unlock(&rs->bitmap_mutex);
             }
+            /*
+	     * 在以下使用ram_save_target_page():
+             *   - migration/ram.c|2183| <<ram_save_host_page_urgent>> if (ram_save_target_page(rs, pss) != 1) {
+             *   - migration/ram.c|2253| <<ram_save_host_page>> tmppages = ram_save_target_page(rs, pss);
+	     */
             tmppages = ram_save_target_page(rs, pss);
             if (tmppages >= 0) {
                 pages += tmppages;
diff --git a/migration/savevm.c b/migration/savevm.c
index dd58f2a70..5f63a2b8c 100644
--- a/migration/savevm.c
+++ b/migration/savevm.c
@@ -2143,6 +2143,15 @@ static void loadvm_postcopy_handle_run_bh(void *opaque)
     /* TODO we should move all of this lot into postcopy_ram.c or a shared code
      * in migration.c
      */
+    /*
+     * 在以下使用cpu_synchronize_all_post_init():
+     *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+     *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+     *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+     */
     cpu_synchronize_all_post_init();
 
     trace_vmstate_downtime_checkpoint("dst-postcopy-bh-cpu-synced");
@@ -2363,6 +2372,14 @@ static int loadvm_handle_cmd_packaged(MigrationIncomingState *mis, Error **errp)
         qemu_coroutine_yield();
     } while (1);
 
+    /*
+     * 在以下使用qemu_loadvm_state_main():
+     *   - migration/colo.c|677| <<colo_incoming_process_checkpoint>> ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
+     *   - migration/postcopy-ram.c|2133| <<postcopy_listen_thread>> load_res = qemu_loadvm_state_main(f, mis, &local_err);
+     *   - migration/savevm.c|2366| <<loadvm_handle_cmd_packaged>> ret = qemu_loadvm_state_main(packf, mis, errp);
+     *   - migration/savevm.c|3049| <<qemu_loadvm_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     *   - migration/savevm.c|3130| <<qemu_load_device_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     */
     ret = qemu_loadvm_state_main(packf, mis, errp);
     trace_loadvm_handle_cmd_packaged_main(ret);
     qemu_fclose(packf);
@@ -2444,6 +2461,10 @@ static int loadvm_postcopy_handle_switchover_start(Error **errp)
  * LOADVM_QUIT All good, but exit the loop
  * <0          Error
  */
+/*
+ * 在以下使用loadvm_process_command():
+ *   - migration/savevm.c|2977| <<qemu_loadvm_state_main>> ret = loadvm_process_command(f, errp);
+ */
 static int loadvm_process_command(QEMUFile *f, Error **errp)
 {
     MigrationIncomingState *mis = migration_incoming_get_current();
@@ -2938,6 +2959,14 @@ static bool postcopy_pause_incoming(MigrationIncomingState *mis)
     return true;
 }
 
+/*
+ * 在以下使用qemu_loadvm_state_main():
+ *   - migration/colo.c|677| <<colo_incoming_process_checkpoint>> ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
+ *   - migration/postcopy-ram.c|2133| <<postcopy_listen_thread>> load_res = qemu_loadvm_state_main(f, mis, &local_err);
+ *   - migration/savevm.c|2366| <<loadvm_handle_cmd_packaged>> ret = qemu_loadvm_state_main(packf, mis, errp);
+ *   - migration/savevm.c|3049| <<qemu_loadvm_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+ *   - migration/savevm.c|3130| <<qemu_load_device_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+ */
 int qemu_loadvm_state_main(QEMUFile *f, MigrationIncomingState *mis,
                            Error **errp)
 {
@@ -3046,6 +3075,14 @@ int qemu_loadvm_state(QEMUFile *f, Error **errp)
 
     cpu_synchronize_all_pre_loadvm();
 
+    /*
+     * 在以下使用qemu_loadvm_state_main():
+     *   - migration/colo.c|677| <<colo_incoming_process_checkpoint>> ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
+     *   - migration/postcopy-ram.c|2133| <<postcopy_listen_thread>> load_res = qemu_loadvm_state_main(f, mis, &local_err);
+     *   - migration/savevm.c|2366| <<loadvm_handle_cmd_packaged>> ret = qemu_loadvm_state_main(packf, mis, errp);
+     *   - migration/savevm.c|3049| <<qemu_loadvm_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     *   - migration/savevm.c|3130| <<qemu_load_device_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     */
     ret = qemu_loadvm_state_main(f, mis, errp);
     qemu_event_set(&mis->main_thread_load_event);
 
@@ -3116,6 +3153,15 @@ int qemu_loadvm_state(QEMUFile *f, Error **errp)
         }
     }
 
+    /*
+     * 在以下使用cpu_synchronize_all_post_init():
+     *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+     *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+     *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+     */
     cpu_synchronize_all_post_init();
 
     return ret;
@@ -3126,12 +3172,29 @@ int qemu_load_device_state(QEMUFile *f, Error **errp)
     MigrationIncomingState *mis = migration_incoming_get_current();
     int ret;
 
+    /*
+     * 在以下使用qemu_loadvm_state_main():
+     *   - migration/colo.c|677| <<colo_incoming_process_checkpoint>> ret = qemu_loadvm_state_main(mis->from_src_file, mis, errp);
+     *   - migration/postcopy-ram.c|2133| <<postcopy_listen_thread>> load_res = qemu_loadvm_state_main(f, mis, &local_err);
+     *   - migration/savevm.c|2366| <<loadvm_handle_cmd_packaged>> ret = qemu_loadvm_state_main(packf, mis, errp);
+     *   - migration/savevm.c|3049| <<qemu_loadvm_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     *   - migration/savevm.c|3130| <<qemu_load_device_state>> ret = qemu_loadvm_state_main(f, mis, errp);
+     */
     /* Load QEMU_VM_SECTION_FULL section */
     ret = qemu_loadvm_state_main(f, mis, errp);
     if (ret < 0) {
         return ret;
     }
 
+    /*
+     * 在以下使用cpu_synchronize_all_post_init():
+     *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+     *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+     *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+     *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+     */
     cpu_synchronize_all_post_init();
     return 0;
 }
diff --git a/net/filter.c b/net/filter.c
index c7cc6615d..3048ea310 100644
--- a/net/filter.c
+++ b/net/filter.c
@@ -110,6 +110,11 @@ ssize_t qemu_netfilter_pass_to_next(NetClientState *sender,
      * deleted while we go through filters.
      */
     if (sender && sender->peer) {
+        /*
+	 * 在以下使用qemu_net_queue_send_iov():
+         *   - net/filter.c|113| <<qemu_netfilter_pass_to_next>> qemu_net_queue_send_iov(sender->peer->incoming_queue,
+         *   - net/net.c|915| <<qemu_sendv_packet_async>> return qemu_net_queue_send_iov(queue, sender,
+	 */
         qemu_net_queue_send_iov(sender->peer->incoming_queue,
                                 sender, flags, iov, iovcnt, NULL);
     }
diff --git a/net/net.c b/net/net.c
index a176936f9..8b8fa36c8 100644
--- a/net/net.c
+++ b/net/net.c
@@ -881,6 +881,13 @@ static ssize_t qemu_deliver_packet_iov(NetClientState *sender,
     return ret;
 }
 
+/*
+ * 在以下使用qemu_sendv_packet_async():
+ *   - hw/net/virtio-net.c|2829| <<virtio_net_flush_tx>> ret = qemu_sendv_packet_async(qemu_get_subqueue(n->nic, queue_index),
+ *   - net/af-xdp.c|243| <<af_xdp_send>> if (!qemu_sendv_packet_async(&s->nc, &iov, 1,
+ *   - net/net.c|923| <<qemu_sendv_packet>> return qemu_sendv_packet_async(nc, iov, iovcnt, NULL);
+ *   - net/netmap.c|283| <<netmap_send>> iovsize = qemu_sendv_packet_async(&s->nc, s->iov, iovcnt,
+ */
 ssize_t qemu_sendv_packet_async(NetClientState *sender,
                                 const struct iovec *iov, int iovcnt,
                                 NetPacketSent *sent_cb)
@@ -912,6 +919,11 @@ ssize_t qemu_sendv_packet_async(NetClientState *sender,
 
     queue = sender->peer->incoming_queue;
 
+    /*
+     * 在以下使用qemu_net_queue_send_iov():
+     *   - net/filter.c|113| <<qemu_netfilter_pass_to_next>> qemu_net_queue_send_iov(sender->peer->incoming_queue,
+     *   - net/net.c|915| <<qemu_sendv_packet_async>> return qemu_net_queue_send_iov(queue, sender,
+     */
     return qemu_net_queue_send_iov(queue, sender,
                                    QEMU_NET_PACKET_FLAG_NONE,
                                    iov, iovcnt, sent_cb);
diff --git a/net/queue.c b/net/queue.c
index fb33856c1..cb4902bc2 100644
--- a/net/queue.c
+++ b/net/queue.c
@@ -218,6 +218,11 @@ ssize_t qemu_net_queue_send(NetQueue *queue,
     return ret;
 }
 
+/*
+ * 在以下使用qemu_net_queue_send_iov():
+ *   - net/filter.c|113| <<qemu_netfilter_pass_to_next>> qemu_net_queue_send_iov(sender->peer->incoming_queue,
+ *   - net/net.c|915| <<qemu_sendv_packet_async>> return qemu_net_queue_send_iov(queue, sender,
+ */
 ssize_t qemu_net_queue_send_iov(NetQueue *queue,
                                 NetClientState *sender,
                                 unsigned flags,
diff --git a/net/vhost-vdpa.c b/net/vhost-vdpa.c
index 3df609127..4f7055f34 100644
--- a/net/vhost-vdpa.c
+++ b/net/vhost-vdpa.c
@@ -1561,6 +1561,11 @@ static int vhost_vdpa_net_handle_ctrl_avail(VhostShadowVirtqueue *svq,
     }
 
     status = VIRTIO_NET_ERR;
+    /*
+     * 在以下使用virtio_net_handle_ctrl_iov():
+     *   - hw/net/virtio-net.c|1604| <<virtio_net_handle_ctrl>> written = virtio_net_handle_ctrl_iov(vdev, elem->in_sg, elem->in_num,
+     *   - net/vhost-vdpa.c|1564| <<vhost_vdpa_net_handle_ctrl_avail>> virtio_net_handle_ctrl_iov(svq->vdev, &model_in, 1, &out, 1);
+     */
     virtio_net_handle_ctrl_iov(svq->vdev, &model_in, 1, &out, 1);
     if (status != VIRTIO_NET_OK) {
         error_report("Bad CVQ processing in model");
diff --git a/system/cpus.c b/system/cpus.c
index bded87feb..af1c4e324 100644
--- a/system/cpus.c
+++ b/system/cpus.c
@@ -149,6 +149,15 @@ void cpu_synchronize_all_post_reset(void)
     }
 }
 
+/*
+ * 在以下使用cpu_synchronize_all_post_init():
+ *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+ *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+ *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+ *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+ *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+ *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+ */
 void cpu_synchronize_all_post_init(void)
 {
     CPUState *cpu;
diff --git a/system/physmem.c b/system/physmem.c
index 4e26f1a1d..5639ee829 100644
--- a/system/physmem.c
+++ b/system/physmem.c
@@ -2242,6 +2242,21 @@ static void ram_block_add(RAMBlock *new_block, Error **errp)
                        "Memory region %s uses guest_memfd, "
                        "which is not supported with CPR.",
                        memory_region_name(new_block->mr));
+            /*
+	     * 在以下使用migrate_add_blocker_modes():
+             *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+             *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+             *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+             *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+             *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+             *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+             *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+             *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+             *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+             *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+             *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+             *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+	     */
             migrate_add_blocker_modes(&new_block->cpr_blocker,
                                       BIT(MIG_MODE_CPR_TRANSFER), errp);
         }
@@ -2441,6 +2456,19 @@ RAMBlock *qemu_ram_alloc_from_file(ram_addr_t size, MemoryRegion *mr,
  */
 static int qemu_ram_get_shared_fd(const char *name, bool *reused, Error **errp)
 {
+    /*
+     * 在以下使用cpr_find_fd():
+     *   - backends/hostmem-memfd.c|38| <<memfd_backend_memory_alloc>> int fd = cpr_find_fd(name, 0);
+     *   - backends/hostmem-shm.c|31| <<shm_backend_memory_alloc>> int fd = cpr_find_fd(backend_name, 0);
+     *   - backends/iommufd.c|81| <<iommufd_backend_complete>> be->fd = cpr_find_fd(name, 0);
+     *   - hw/vfio/container-legacy.c|621| <<vfio_container_connect>> fd = cpr_find_fd("vfio_container_for_group", group->groupid);
+     *   - hw/vfio/cpr-iommufd.c|238| <<vfio_cpr_load_device>> vbasedev->fd = cpr_find_fd(vbasedev->name, 0);
+     *   - hw/vfio/cpr-legacy.c|275| <<vfio_cpr_group_get_device_fd>> int fd = cpr_find_fd(name, id);
+     *   - hw/vfio/cpr.c|45| <<vfio_cpr_load_vector_fd>> return cpr_find_fd(fdname, nr);
+     *   - migration/cpr.c|115| <<cpr_open_fd>> int fd = cpr_find_fd(name, id);
+     *   - migration/cpr.c|321| <<cpr_get_fd_param>> fd = cpr_find_fd(name, index);
+     *   - system/physmem.c|2459| <<qemu_ram_get_shared_fd>> int fd = cpr_find_fd(name, 0);
+     */
     int fd = cpr_find_fd(name, 0);
 
     if (fd >= 0) {
@@ -4465,6 +4493,21 @@ void ram_block_add_cpr_blocker(RAMBlock *rb, Error **errp)
                "Memory region %s is not compatible with CPR. share=on is "
                "required for memory-backend objects, and aux-ram-share=on is "
                "required.", memory_region_name(rb->mr));
+    /*
+     * 在以下使用migrate_add_blocker_modes():
+     *   - hw/vfio/container-legacy.c|983| <<vfio_legacy_attach_device>> if (migrate_add_blocker_modes(&vbasedev->cpr.mdev_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) < 0) {
+     *   - hw/vfio/cpr-iommufd.c|161| <<vfio_iommufd_cpr_register_iommufd>> return migrate_add_blocker_modes(cpr_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+     *   - hw/vfio/cpr-legacy.c|179| <<vfio_legacy_cpr_register_container>> return migrate_add_blocker_modes(cpr_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER) | BIT(MIG_MODE_CPR_EXEC), errp) == 0;
+     *   - hw/vfio/device.c|348| <<vfio_device_get_name>> return migrate_add_blocker_modes(&vbasedev->cpr.id_blocker,
+     *            BIT(MIG_MODE_CPR_TRANSFER), errp) == 0;
+     *   - migration/migration.c|1753| <<migrate_add_blocker>> return migrate_add_blocker_modes(reasonp, -1u, errp);
+     *   - migration/migration.c|1758| <<migrate_add_blocker_normal>> return migrate_add_blocker_modes(reasonp, BIT(MIG_MODE_NORMAL), errp);
+     *   - system/physmem.c|2245| <<ram_block_add>> migrate_add_blocker_modes(&new_block->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER), errp);
+     *   - system/physmem.c|4468| <<ram_block_add_cpr_blocker>> migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
+     */
     migrate_add_blocker_modes(&rb->cpr_blocker, BIT(MIG_MODE_CPR_TRANSFER),
                               errp);
 }
diff --git a/system/runstate.c b/system/runstate.c
index 2d4e95a21..168d2bf80 100644
--- a/system/runstate.c
+++ b/system/runstate.c
@@ -309,10 +309,24 @@ static QTAILQ_HEAD(, VMChangeStateEntry) vm_change_state_head =
 VMChangeStateEntry *qemu_add_vm_change_state_handler_prio(
         VMChangeStateHandler *cb, void *opaque, int priority)
 {
+    /*
+     * 在以下使用qemu_add_vm_change_state_handler_prio_full():
+     *   - hw/core/vm-change-state-handler.c|78| <<qdev_add_vm_change_state_handler_full>>
+     *          return qemu_add_vm_change_state_handler_prio_full(cb, prepare_cb, cb_ret, opaque, depth);
+     *   - system/runstate.c|312| <<qemu_add_vm_change_state_handler_prio>>
+     *          return qemu_add_vm_change_state_handler_prio_full(cb, NULL, NULL, opaque, priority);
+     */
     return qemu_add_vm_change_state_handler_prio_full(cb, NULL, NULL,
                                                       opaque, priority);
 }
 
+/*
+ * 在以下使用qemu_add_vm_change_state_handler_prio_full():
+ *   - hw/core/vm-change-state-handler.c|78| <<qdev_add_vm_change_state_handler_full>>
+ *          return qemu_add_vm_change_state_handler_prio_full(cb, prepare_cb, cb_ret, opaque, depth);
+ *   - system/runstate.c|312| <<qemu_add_vm_change_state_handler_prio>>
+ *          return qemu_add_vm_change_state_handler_prio_full(cb, NULL, NULL, opaque, priority);
+ */
 VMChangeStateEntry *
 qemu_add_vm_change_state_handler_prio_full(VMChangeStateHandler *cb,
                                            VMChangeStateHandler *prepare_cb,
@@ -578,6 +592,15 @@ void qemu_system_reset(ShutdownCause reason)
              * need to sync full cpu state for non confidential guests post
              * reset.
              */
+            /*
+	     * 在以下使用cpu_synchronize_all_post_init():
+             *   - accel/kvm/kvm-all.c|2889| <<kvm_reset_vmfd>> cpu_synchronize_all_post_init();
+             *   - hw/core/machine.c|1717| <<qdev_machine_creation_done>> cpu_synchronize_all_post_init();
+             *   - migration/savevm.c|2146| <<loadvm_postcopy_handle_run_bh>> cpu_synchronize_all_post_init();
+             *   - migration/savevm.c|3147| <<qemu_loadvm_state>> cpu_synchronize_all_post_init();
+             *   - migration/savevm.c|3171| <<qemu_load_device_state>> cpu_synchronize_all_post_init();
+             *   - system/runstate.c|581| <<qemu_system_reset>> cpu_synchronize_all_post_init();
+	     */
             cpu_synchronize_all_post_init();
         } else {
             cpu_synchronize_all_post_reset();
diff --git a/util/fifo8.c b/util/fifo8.c
index cc4f590b7..af24fb424 100644
--- a/util/fifo8.c
+++ b/util/fifo8.c
@@ -22,6 +22,18 @@ void fifo8_reset(Fifo8 *fifo)
     fifo->head = 0;
 }
 
+/*
+ * typedef struct {
+ *     uint8_t *data;
+ *     uint32_t capacity;
+ *     uint32_t head;
+ *     uint32_t num;
+ * } Fifo8;
+ *
+ * 两个例子:
+ *   - hw/char/serial.c|971| <<serial_realize>> fifo8_create(&s->recv_fifo, UART_FIFO_LENGTH);
+ *   - hw/char/serial.c|972| <<serial_realize>> fifo8_create(&s->xmit_fifo, UART_FIFO_LENGTH);
+ */
 void fifo8_create(Fifo8 *fifo, uint32_t capacity)
 {
     fifo->data = g_new(uint8_t, capacity);
@@ -60,6 +72,14 @@ void fifo8_push_all(Fifo8 *fifo, const uint8_t *data, uint32_t num)
     fifo->num += num;
 }
 
+/*
+ * typedef struct {
+ *     uint8_t *data;
+ *     uint32_t capacity;
+ *     uint32_t head;
+ *     uint32_t num;
+ * } Fifo8;
+ */
 uint8_t fifo8_pop(Fifo8 *fifo)
 {
     uint8_t ret;
-- 
2.50.1 (Apple Git-155)

