From d7aee06ef8f0962d721a4e08dfd277f5144d082d Mon Sep 17 00:00:00 2001
From: Dongli Zhang <dongli.zhang@oracle.com>
Date: Sun, 16 Aug 2026 23:32:48 -0700
Subject: [PATCH 1/1] qemu for v11.1.0

Signed-off-by: Dongli Zhang <dongli.zhang0129@gmail.com>
---
 backends/hostmem-memfd.c       |  13 +++
 backends/hostmem-shm.c         |  13 +++
 backends/iommufd.c             |  13 +++
 block.c                        |  42 ++++++++++
 block/block-backend.c          |  18 +++++
 block/nbd.c                    |  25 ++++++
 chardev/char-socket.c          |  25 ++++++
 hw/acpi/generic_event_device.c |   9 +++
 hw/acpi/ich9.c                 |   9 +++
 hw/acpi/pcihp.c                |  51 ++++++++++++
 hw/acpi/piix4.c                |   9 +++
 hw/core/qdev-hotplug.c         |  31 +++++++
 hw/core/qdev.c                 |  57 +++++++++++++
 hw/core/sysbus-fdt.c           |   9 +++
 hw/i386/amd_iommu.c            |   8 ++
 hw/net/igb.c                   |   5 ++
 hw/net/virtio-net.c            |  38 +++++++++
 hw/nvme/ctrl.c                 |   5 ++
 hw/pci/pci-hmp-cmds.c          |   8 ++
 hw/pci/pci.c                   | 144 +++++++++++++++++++++++++++++++++
 hw/pci/pci_bridge.c            |   4 +
 hw/pci/pci_host.c              |  19 +++++
 hw/pci/pcie.c                  |  95 ++++++++++++++++++++++
 hw/pci/pcie_sriov.c            |  32 ++++++++
 hw/remote/remote-obj.c         |   9 +++
 hw/remote/vfio-user-obj.c      |   9 +++
 hw/smbios/smbios.c             |  16 ++++
 hw/vfio/cpr-legacy.c           |  31 +++++++
 hw/vfio/cpr.c                  |  72 +++++++++++++++++
 hw/vfio/migration.c            |  90 +++++++++++++++++++++
 hw/vfio/pci.c                  |  12 +++
 include/hw/core/boards.h       |  20 +++++
 include/hw/pci/pci_bus.h       |  22 +++++
 include/hw/pci/pci_device.h    |  19 +++++
 include/qemu/rcu.h             |  21 +++++
 io/channel.c                   |  26 ++++++
 migration/block-active.c       |  14 ++++
 migration/block-dirty-bitmap.c |  45 +++++++++++
 migration/channel-block.c      |  27 +++++++
 migration/channel.c            |  32 ++++++++
 migration/colo.c               |  15 ++++
 migration/cpr-transfer.c       |  10 +++
 migration/cpr.c                |  52 ++++++++++++
 migration/exec.c               |   5 ++
 migration/fd.c                 |   4 +
 migration/file.c               |  12 +++
 migration/global_state.c       |  26 ++++++
 migration/migration.c          |  84 +++++++++++++++++++
 migration/savevm.c             |  13 +++
 migration/socket.c             |  13 +++
 migration/tls.c                |  15 ++++
 migration/vfio.c               |   9 +++
 migration/yank_functions.c     |  36 +++++++++
 system/physmem.c               |  13 +++
 util/yank.c                    |  90 +++++++++++++++++++++
 55 files changed, 1544 insertions(+)

diff --git a/backends/hostmem-memfd.c b/backends/hostmem-memfd.c
index 923239f9c..c7e6fa873 100644
--- a/backends/hostmem-memfd.c
+++ b/backends/hostmem-memfd.c
@@ -54,6 +54,19 @@ memfd_backend_memory_alloc(HostMemoryBackend *backend, Error **errp)
     if (fd == -1) {
         return false;
     }
+    /*
+     * 在以下使用cpr_save_fd():
+     *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+     *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+     *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+     *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+     *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+     *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+     *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+     *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+     */
     cpr_save_fd(name, 0, fd);
 
 have_fd:
diff --git a/backends/hostmem-shm.c b/backends/hostmem-shm.c
index 806e2670e..fd0af85d7 100644
--- a/backends/hostmem-shm.c
+++ b/backends/hostmem-shm.c
@@ -48,6 +48,19 @@ shm_backend_memory_alloc(HostMemoryBackend *backend, Error **errp)
     if (fd < 0) {
         return false;
     }
+    /*
+     * 在以下使用cpr_save_fd():
+     *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+     *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+     *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+     *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+     *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+     *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+     *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+     *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+     */
     cpr_save_fd(backend_name, 0, fd);
 
 have_fd:
diff --git a/backends/iommufd.c b/backends/iommufd.c
index 15f2a5135..e27382e04 100644
--- a/backends/iommufd.c
+++ b/backends/iommufd.c
@@ -85,6 +85,19 @@ static void iommufd_backend_complete(UserCreatable *uc, Error **errp)
         if (cpr_is_incoming()) {
             be->fd = cpr_find_fd(name, 0);
         } else {
+            /*
+	     * 在以下使用cpr_save_fd():
+             *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+             *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+             *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+             *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+             *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+             *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+             *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+             *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+	     */
             cpr_save_fd(name, 0, be->fd);
         }
     } else if (!g_file_test("/dev/iommu", G_FILE_TEST_EXISTS)) {
diff --git a/block.c b/block.c
index f0a6042e6..ea7963db2 100644
--- a/block.c
+++ b/block.c
@@ -2717,6 +2717,15 @@ bdrv_refresh_perms(BlockDriverState *bs, Transaction *tran, Error **errp)
     return ret;
 }
 
+/*
+ * 在以下使用bdrv_child_try_set_perm():
+ *   - block.c|2765| <<bdrv_child_refresh_perms>> return bdrv_child_try_set_perm(c, perms, shared, errp);
+ *   - block/block-backend.c|278| <<blk_root_inactivate>> bdrv_child_try_set_perm(blk->root, 0, BLK_PERM_ALL, &error_abort);
+ *   - block/block-backend.c|956| <<blk_set_perm_locked>> ret = bdrv_child_try_set_perm(blk->root, perm, shared_perm, errp);
+ *
+ * perm: 我需要什么权限
+ * shared_perm: 我允许别人同时拿什么权限
+ */
 int bdrv_child_try_set_perm(BdrvChild *c, uint64_t perm, uint64_t shared,
                             Error **errp)
 {
@@ -2762,6 +2771,15 @@ int bdrv_child_refresh_perms(BlockDriverState *bs, BdrvChild *c, Error **errp)
     bdrv_child_perm(bs, c->bs, c, c->role, NULL,
                     parent_perms, parent_shared, &perms, &shared);
 
+    /*
+     * 在以下使用bdrv_child_try_set_perm():
+     *   - block.c|2765| <<bdrv_child_refresh_perms>> return bdrv_child_try_set_perm(c, perms, shared, errp);
+     *   - block/block-backend.c|278| <<blk_root_inactivate>> bdrv_child_try_set_perm(blk->root, 0, BLK_PERM_ALL, &error_abort);
+     *   - block/block-backend.c|956| <<blk_set_perm_locked>> ret = bdrv_child_try_set_perm(blk->root, perm, shared_perm, errp);
+     *
+     * perm: 我需要什么权限
+     * shared_perm: 我允许别人同时拿什么权限
+     */
     return bdrv_child_try_set_perm(c, perms, shared, errp);
 }
 
@@ -6966,6 +6984,12 @@ bdrv_has_bds_parent(BlockDriverState *bs, bool only_active)
     return false;
 }
 
+/*
+ * 在以下使用bdrv_inactivate_recurse():
+ *   - block.c|7034| <<bdrv_inactivate_recurse>> ret = bdrv_inactivate_recurse(child->bs, false);
+ *   - block.c|7055| <<bdrv_inactivate>> ret = bdrv_inactivate_recurse(bs, true);
+ *   - block.c|7082| <<bdrv_inactivate_all>> ret = bdrv_inactivate_recurse(bs, true);
+ */
 static int GRAPH_RDLOCK
 bdrv_inactivate_recurse(BlockDriverState *bs, bool top_level)
 {
@@ -7031,6 +7055,12 @@ bdrv_inactivate_recurse(BlockDriverState *bs, bool top_level)
 
     /* Recursively inactivate children */
     QLIST_FOREACH(child, &bs->children, next) {
+        /*
+	 * 在以下使用bdrv_inactivate_recurse():
+         *   - block.c|7034| <<bdrv_inactivate_recurse>> ret = bdrv_inactivate_recurse(child->bs, false);
+         *   - block.c|7055| <<bdrv_inactivate>> ret = bdrv_inactivate_recurse(bs, true);
+         *   - block.c|7082| <<bdrv_inactivate_all>> ret = bdrv_inactivate_recurse(bs, true);
+	 */
         ret = bdrv_inactivate_recurse(child->bs, false);
         if (ret < 0) {
             return ret;
@@ -7052,6 +7082,12 @@ int bdrv_inactivate(BlockDriverState *bs, Error **errp)
         return -EPERM;
     }
 
+    /*
+     * 在以下使用bdrv_inactivate_recurse():
+     *   - block.c|7034| <<bdrv_inactivate_recurse>> ret = bdrv_inactivate_recurse(child->bs, false);
+     *   - block.c|7055| <<bdrv_inactivate>> ret = bdrv_inactivate_recurse(bs, true);
+     *   - block.c|7082| <<bdrv_inactivate_all>> ret = bdrv_inactivate_recurse(bs, true);
+     */
     ret = bdrv_inactivate_recurse(bs, true);
     if (ret < 0) {
         error_setg_errno(errp, -ret, "Failed to inactivate node");
@@ -7079,6 +7115,12 @@ int bdrv_inactivate_all(void)
         if (bdrv_has_bds_parent(bs, false)) {
             continue;
         }
+        /*
+	 * 在以下使用bdrv_inactivate_recurse():
+         *   - block.c|7034| <<bdrv_inactivate_recurse>> ret = bdrv_inactivate_recurse(child->bs, false);
+         *   - block.c|7055| <<bdrv_inactivate>> ret = bdrv_inactivate_recurse(bs, true);
+         *   - block.c|7082| <<bdrv_inactivate_all>> ret = bdrv_inactivate_recurse(bs, true);
+	 */
         ret = bdrv_inactivate_recurse(bs, true);
         if (ret < 0) {
             bdrv_next_cleanup(&it);
diff --git a/block/block-backend.c b/block/block-backend.c
index 37ba7e9fc..631db52c1 100644
--- a/block/block-backend.c
+++ b/block/block-backend.c
@@ -275,6 +275,15 @@ static int GRAPH_RDLOCK blk_root_inactivate(BdrvChild *child)
 
     blk->disable_perm = true;
     if (blk->root) {
+        /*
+	 * 在以下使用bdrv_child_try_set_perm():
+         *   - block.c|2765| <<bdrv_child_refresh_perms>> return bdrv_child_try_set_perm(c, perms, shared, errp);
+         *   - block/block-backend.c|278| <<blk_root_inactivate>> bdrv_child_try_set_perm(blk->root, 0, BLK_PERM_ALL, &error_abort);
+         *   - block/block-backend.c|956| <<blk_set_perm_locked>> ret = bdrv_child_try_set_perm(blk->root, perm, shared_perm, errp);
+         *
+         * perm: 我需要什么权限
+         * shared_perm: 我允许别人同时拿什么权限
+	 */
         bdrv_child_try_set_perm(blk->root, 0, BLK_PERM_ALL, &error_abort);
     }
 
@@ -953,6 +962,15 @@ blk_set_perm_locked(BlockBackend *blk, uint64_t perm, uint64_t shared_perm,
     GLOBAL_STATE_CODE();
 
     if (blk->root && !blk->disable_perm) {
+        /*
+	 * 在以下使用bdrv_child_try_set_perm():
+         *   - block.c|2765| <<bdrv_child_refresh_perms>> return bdrv_child_try_set_perm(c, perms, shared, errp);
+         *   - block/block-backend.c|278| <<blk_root_inactivate>> bdrv_child_try_set_perm(blk->root, 0, BLK_PERM_ALL, &error_abort);
+         *   - block/block-backend.c|956| <<blk_set_perm_locked>> ret = bdrv_child_try_set_perm(blk->root, perm, shared_perm, errp);
+         *
+         * perm: 我需要什么权限
+         * shared_perm: 我允许别人同时拿什么权限
+	 */
         ret = bdrv_child_try_set_perm(blk->root, perm, shared_perm, errp);
         if (ret < 0) {
             return ret;
diff --git a/block/nbd.c b/block/nbd.c
index 5d231d5c4..11f7bb795 100644
--- a/block/nbd.c
+++ b/block/nbd.c
@@ -117,6 +117,14 @@ static void nbd_clear_bdrvstate(BlockDriverState *bs)
     nbd_client_connection_release(s->conn);
     s->conn = NULL;
 
+    /*
+     * 在以下使用yank_unregister_instance():
+     *   - block/nbd.c|120| <<nbd_clear_bdrvstate>> yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
+     *   - chardev/char-socket.c|1113| <<char_socket_finalize>> yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
+     *   - migration/migration.c|496| <<migration_incoming_state_destroy>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     *   - migration/migration.c|1418| <<migration_cleanup>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     *   - migration/migration.c|1877| <<qmp_migrate_incoming>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     */
     yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
 
     /* Must not leave timers behind that would access freed data */
@@ -330,6 +338,15 @@ int coroutine_fn nbd_co_do_establish_connection(BlockDriverState *bs,
         return -ECONNREFUSED;
     }
 
+    /*
+     * 在以下使用yank_register_function():
+     *   - block/nbd.c|341| <<nbd_co_do_establish_connection>> yank_register_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+     *               nbd_yank, bs);
+     *   - chardev/char-socket.c|909| <<tcp_chr_new_client>> yank_register_function(CHARDEV_YANK_INSTANCE(chr->label),
+     *               char_socket_yank_iochannel, QIO_CHANNEL(sioc));
+     *   - migration/yank_functions.c|44| <<migration_ioc_register_yank>> yank_register_function(MIGRATION_YANK_INSTANCE,
+     *               migration_yank_iochannel, ioc);
+     */
     yank_register_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name), nbd_yank,
                            bs);
 
@@ -1917,6 +1934,14 @@ static int nbd_open(BlockDriverState *bs, QDict *options, int flags,
     qemu_co_mutex_init(&s->send_mutex);
     qemu_co_mutex_init(&s->receive_mutex);
 
+    /*
+     * 在以下使用yank_register_instance():
+     *   - block/nbd.c|1920| <<nbd_open>> if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
+     *   - chardev/char-socket.c|1409| <<tcp_chr_open>> if (!yank_register_instance(CHARDEV_YANK_INSTANCE(chr->label), errp)) {
+     *   - migration/migration.c|1867| <<qmp_migrate_incoming>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     *   - migration/migration.c|2097| <<migrate_prepare>> yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
+     *   - migration/savevm.c|3527| <<load_snapshot>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     */
     if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
         return -EEXIST;
     }
diff --git a/chardev/char-socket.c b/chardev/char-socket.c
index b629575fc..70d271437 100644
--- a/chardev/char-socket.c
+++ b/chardev/char-socket.c
@@ -906,6 +906,15 @@ static int tcp_chr_new_client(Chardev *chr, QIOChannelSocket *sioc)
     object_ref(OBJECT(sioc));
 
     if (s->registered_yank) {
+        /*
+	 * 在以下使用yank_register_function():
+         *   - block/nbd.c|341| <<nbd_co_do_establish_connection>> yank_register_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+         *               nbd_yank, bs);
+         *   - chardev/char-socket.c|909| <<tcp_chr_new_client>> yank_register_function(CHARDEV_YANK_INSTANCE(chr->label),
+         *               char_socket_yank_iochannel, QIO_CHANNEL(sioc));
+         *   - migration/yank_functions.c|44| <<migration_ioc_register_yank>> yank_register_function(MIGRATION_YANK_INSTANCE,
+         *               migration_yank_iochannel, ioc);
+	 */
         yank_register_function(CHARDEV_YANK_INSTANCE(chr->label),
                                char_socket_yank_iochannel,
                                QIO_CHANNEL(sioc));
@@ -1110,6 +1119,14 @@ static void char_socket_finalize(Object *obj)
          * instance, as it still may be needed.
          */
         if (!chr->handover_yank_instance) {
+            /*
+	     * 在以下使用yank_unregister_instance():
+             *   - block/nbd.c|120| <<nbd_clear_bdrvstate>> yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
+             *   - chardev/char-socket.c|1113| <<char_socket_finalize>> yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
+             *   - migration/migration.c|496| <<migration_incoming_state_destroy>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+             *   - migration/migration.c|1418| <<migration_cleanup>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+             *   - migration/migration.c|1877| <<qmp_migrate_incoming>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+	     */
             yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
         }
     }
@@ -1401,6 +1418,14 @@ static bool tcp_chr_open(Chardev *chr, ChardevBackend *backend, Error **errp)
     }
 #endif
 
+    /*
+     * 在以下使用yank_register_instance():
+     *   - block/nbd.c|1920| <<nbd_open>> if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
+     *   - chardev/char-socket.c|1409| <<tcp_chr_open>> if (!yank_register_instance(CHARDEV_YANK_INSTANCE(chr->label), errp)) {
+     *   - migration/migration.c|1867| <<qmp_migrate_incoming>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     *   - migration/migration.c|2097| <<migrate_prepare>> yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
+     *   - migration/savevm.c|3527| <<load_snapshot>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     */
     /*
      * In the chardev-change special-case, we shouldn't register a new yank
      * instance, as there already may be one.
diff --git a/hw/acpi/generic_event_device.c b/hw/acpi/generic_event_device.c
index 9e9416d40..58cad48e6 100644
--- a/hw/acpi/generic_event_device.c
+++ b/hw/acpi/generic_event_device.c
@@ -535,6 +535,15 @@ static void acpi_ged_realize(DeviceState *dev, Error **errp)
             memory_region_init(&s->container_pcihp, OBJECT(dev),
                                ACPI_PCIHP_REGION_NAME, ACPI_PCIHP_SIZE);
             sysbus_init_mmio(sbd, &s->container_pcihp);
+            /*
+	     * 在以下使用acpi_pcihp_init():
+             *   - hw/acpi/generic_event_device.c|538| <<acpi_ged_realize>> acpi_pcihp_init(OBJECT(s),
+             *         &s->pcihp_state, &s->container_pcihp, 0);
+             *   - hw/acpi/ich9.c|320| <<ich9_pm_init>> acpi_pcihp_init(OBJECT(lpc_pci),
+             *         &pm->acpi_pci_hotplug, pci_address_space_io(lpc_pci), ACPI_PCIHP_ADDR_ICH9);
+             *   - hw/acpi/piix4.c|532| <<piix4_acpi_system_hot_add_init>> acpi_pcihp_init(OBJECT(s),
+             *         &s->acpi_pci_hotplug, parent, ACPI_PCIHP_ADDR_PIIX4);
+	     */
             acpi_pcihp_init(OBJECT(s), &s->pcihp_state,
                             &s->container_pcihp, 0);
             qbus_set_hotplug_handler(BUS(s->pcihp_state.root), OBJECT(dev));
diff --git a/hw/acpi/ich9.c b/hw/acpi/ich9.c
index 5e8f8a7ea..e5cb3ccb2 100644
--- a/hw/acpi/ich9.c
+++ b/hw/acpi/ich9.c
@@ -317,6 +317,15 @@ void ich9_pm_init(PCIDevice *lpc_pci, ICH9LPCPMRegs *pm, qemu_irq sci_irq)
     if (pm->acpi_pci_hotplug.use_acpi_hotplug_bridge) {
         object_property_set_link(OBJECT(lpc_pci), "bus",
                                  OBJECT(pci_get_bus(lpc_pci)), &error_abort);
+        /*
+	 * 在以下使用acpi_pcihp_init():
+         *   - hw/acpi/generic_event_device.c|538| <<acpi_ged_realize>> acpi_pcihp_init(OBJECT(s),
+         *         &s->pcihp_state, &s->container_pcihp, 0);
+         *   - hw/acpi/ich9.c|320| <<ich9_pm_init>> acpi_pcihp_init(OBJECT(lpc_pci),
+         *         &pm->acpi_pci_hotplug, pci_address_space_io(lpc_pci), ACPI_PCIHP_ADDR_ICH9);
+         *   - hw/acpi/piix4.c|532| <<piix4_acpi_system_hot_add_init>> acpi_pcihp_init(OBJECT(s),
+         *         &s->acpi_pci_hotplug, parent, ACPI_PCIHP_ADDR_PIIX4);
+	 */
         acpi_pcihp_init(OBJECT(lpc_pci),
                         &pm->acpi_pci_hotplug,
                         pci_address_space_io(lpc_pci),
diff --git a/hw/acpi/pcihp.c b/hw/acpi/pcihp.c
index 87162ff2c..5262193de 100644
--- a/hw/acpi/pcihp.c
+++ b/hw/acpi/pcihp.c
@@ -160,6 +160,9 @@ static PCIBus *acpi_pcihp_find_hotplug_bus(AcpiPciHpState *s, int bsel)
     return find.bus;
 }
 
+/*
+ * 判断这个 PCI 设备在 ACPI PCI hotplug 机制下是否"不允许热插拔/热拔"
+ */
 static bool acpi_pcihp_pc_no_hotplug(AcpiPciHpState *s, PCIDevice *dev)
 {
     DeviceClass *dc = DEVICE_GET_CLASS(dev);
@@ -175,6 +178,11 @@ static bool acpi_pcihp_pc_no_hotplug(AcpiPciHpState *s, PCIDevice *dev)
            pci_is_vf(dev);
 }
 
+/*
+ * 在以下使用acpi_pcihp_eject_slot():
+ *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+ *   - hw/acpi/pcihp.c|472| <<pci_write(PCI_EJ_BASE)>> acpi_pcihp_eject_slot(s, s->hotplug_select, data);
+ */
 static void acpi_pcihp_eject_slot(AcpiPciHpState *s, unsigned bsel, unsigned slots)
 {
     HotplugHandler *hotplug_ctrl;
@@ -192,11 +200,35 @@ static void acpi_pcihp_eject_slot(AcpiPciHpState *s, unsigned bsel, unsigned slo
     s->acpi_pcihp_pci_status[bsel].down &= ~(1U << slot);
     s->acpi_pcihp_pci_status[bsel].up &= ~(1U << slot);
 
+    /*
+     * PCIBus *bus:
+     * -> BusState qbus;
+     *    -> BusChildHead children;
+     */
     QTAILQ_FOREACH_SAFE(kid, &bus->qbus.children, sibling, next) {
         DeviceState *qdev = kid->child;
         PCIDevice *dev = PCI_DEVICE(qdev);
         if (PCI_SLOT(dev->devfn) == slot) {
             if (!acpi_pcihp_pc_no_hotplug(s, dev)) {
+                /*
+		 * 在以下使用PCIDevice->partially_hotplugged:
+                 *   - hw/acpi/pcihp.c|207| <<acpi_pcihp_eject_slot>> if (dev->partially_hotplugged) {
+                 *   - hw/net/virtio-net.c|3759| <<failover_unplug_primary>> pci_dev->partially_hotplugged = true;
+                 *   - hw/net/virtio-net.c|3779| <<failover_replug_primary>> if (!pdev->partially_hotplugged) {
+                 *   - hw/net/virtio-net.c|3797| <<failover_replug_primary>> pdev->partially_hotplugged = false;
+                 *   - hw/pci/pci_host.c|72| <<is_pci_dev_ejected>> return pci_dev && pci_dev->partially_hotplugged &&
+                 *   - hw/pci/pcie.c|584| <<pcie_unplug_device>> if (dev->partially_hotplugged) {
+                 *
+                 * 不是"普通热插拔了一半"的通用状态, 它主要是给 virtio-net failover
+                 * 用的特殊标志.
+                 * 这个 PCI 设备已经被 guest 通过 hot-unplug 流程"看起来拔掉了", 但 QEMU
+                 * 还没有真正把对象删掉, 因为后面可能还要把它重新插回去.
+                 * virtio-net failover 做迁移时, 会把 primary 网卡先从 guest 里隐藏/拔掉,
+                 * 让 guest 使用 standby virtio-net. 这里不能真的 object_unparent() 删除
+                 * primary 设备, 因为如果迁移失败, 还要 replug 回来.
+                 * 一句话总结: partially_hotplugged 表示 guest 侧已经完成热拔, 但 QEMU
+                 * 故意保留设备对象, 等待 failover 流程决定是彻底消失还是重新插回.
+		 */
                 /*
                  * partially_hotplugged is used by virtio-net failover:
                  * failover has asked the guest OS to unplug the device
@@ -230,6 +262,11 @@ static void acpi_pcihp_update_hotplug_bus(AcpiPciHpState *s, int bsel)
 
     /* Execute any pending removes during reset */
     while (s->acpi_pcihp_pci_status[bsel].down) {
+        /*
+	 * 在以下使用acpi_pcihp_eject_slot():
+         *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+         *   - hw/acpi/pcihp.c|472| <<pci_write(PCI_EJ_BASE)>> acpi_pcihp_eject_slot(s, s->hotplug_select, data);
+	 */
         acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
     }
 
@@ -469,6 +506,11 @@ static void pci_write(void *opaque, hwaddr addr, uint64_t data,
         if (s->hotplug_select >= ACPI_PCIHP_MAX_HOTPLUG_BUS) {
             break;
         }
+        /*
+	 * 在以下使用acpi_pcihp_eject_slot():
+         *   - hw/acpi/pcihp.c|233| <<acpi_pcihp_update_hotplug_bus>> acpi_pcihp_eject_slot(s, bsel, s->acpi_pcihp_pci_status[bsel].down);
+         *   - hw/acpi/pcihp.c|472| <<pci_write(PCI_EJ_BASE)>> acpi_pcihp_eject_slot(s, s->hotplug_select, data);
+	 */
         acpi_pcihp_eject_slot(s, s->hotplug_select, data);
         trace_acpi_pci_ej_write(addr, data);
         break;
@@ -491,6 +533,15 @@ static const MemoryRegionOps acpi_pcihp_io_ops = {
     },
 };
 
+/*
+ * 在以下使用acpi_pcihp_init():
+ *   - hw/acpi/generic_event_device.c|538| <<acpi_ged_realize>> acpi_pcihp_init(OBJECT(s),
+ *         &s->pcihp_state, &s->container_pcihp, 0);
+ *   - hw/acpi/ich9.c|320| <<ich9_pm_init>> acpi_pcihp_init(OBJECT(lpc_pci),
+ *         &pm->acpi_pci_hotplug, pci_address_space_io(lpc_pci), ACPI_PCIHP_ADDR_ICH9);
+ *   - hw/acpi/piix4.c|532| <<piix4_acpi_system_hot_add_init>> acpi_pcihp_init(OBJECT(s),
+ *         &s->acpi_pci_hotplug, parent, ACPI_PCIHP_ADDR_PIIX4);
+ */
 void acpi_pcihp_init(Object *owner, AcpiPciHpState *s,
                      MemoryRegion *io, uint16_t io_base)
 {
diff --git a/hw/acpi/piix4.c b/hw/acpi/piix4.c
index 9b7f50c7a..d03c1716f 100644
--- a/hw/acpi/piix4.c
+++ b/hw/acpi/piix4.c
@@ -529,6 +529,15 @@ static void piix4_acpi_system_hot_add_init(MemoryRegion *parent,
     if (s->acpi_pci_hotplug.use_acpi_hotplug_bridge ||
         s->acpi_pci_hotplug.use_acpi_root_pci_hotplug) {
         object_property_set_link(OBJECT(s), "bus", OBJECT(bus), &error_abort);
+        /*
+	 * 在以下使用acpi_pcihp_init():
+         *   - hw/acpi/generic_event_device.c|538| <<acpi_ged_realize>> acpi_pcihp_init(OBJECT(s),
+	 *         &s->pcihp_state, &s->container_pcihp, 0);
+         *   - hw/acpi/ich9.c|320| <<ich9_pm_init>> acpi_pcihp_init(OBJECT(lpc_pci),
+         *         &pm->acpi_pci_hotplug, pci_address_space_io(lpc_pci), ACPI_PCIHP_ADDR_ICH9);
+         *   - hw/acpi/piix4.c|532| <<piix4_acpi_system_hot_add_init>> acpi_pcihp_init(OBJECT(s),
+         *         &s->acpi_pci_hotplug, parent, ACPI_PCIHP_ADDR_PIIX4);
+	 */
         acpi_pcihp_init(OBJECT(s), &s->acpi_pci_hotplug, parent,
                         ACPI_PCIHP_ADDR_PIIX4);
         qbus_set_hotplug_handler(BUS(pci_get_bus(PCI_DEVICE(s))), OBJECT(s));
diff --git a/hw/core/qdev-hotplug.c b/hw/core/qdev-hotplug.c
index 1d547e0db..7a0f7799d 100644
--- a/hw/core/qdev-hotplug.c
+++ b/hw/core/qdev-hotplug.c
@@ -23,6 +23,26 @@ HotplugHandler *qdev_get_machine_hotplug_handler(DeviceState *dev)
     if (object_dynamic_cast(m_obj, TYPE_MACHINE)) {
         machine = MACHINE(m_obj);
         mc = MACHINE_GET_CLASS(machine);
+        /*
+	 * 在以下设置MachineClass->get_hotplug_handler:
+         *   - hw/arm/virt.c|4191| <<virt_machine_class_init>> mc->get_hotplug_handler = virt_machine_get_hotplug_handler;
+         *   - hw/i386/microvm.c|670| <<microvm_class_init>> mc->get_hotplug_handler = microvm_get_hotplug_handler;
+         *   - hw/i386/pc.c|1700| <<pc_machine_class_init>> mc->get_hotplug_handler = pc_get_hotplug_handler;
+         *   - hw/loongarch/virt.c|1480| <<virt_class_init>> mc->get_hotplug_handler = virt_get_hotplug_handler;
+         *   - hw/ppc/e500plat.c|78| <<e500plat_machine_class_init>> mc->get_hotplug_handler = e500plat_machine_get_hotpug_handler;
+	 *   - hw/ppc/spapr.c|4641| <<spapr_machine_class_init>> mc->get_hotplug_handler = spapr_get_hotplug_handler;
+         *   - hw/riscv/virt.c|1737| <<virt_machine_class_init>> mc->get_hotplug_handler = virt_machine_get_hotplug_handler;
+         *   - hw/s390x/s390-virtio-ccw.c|826| <<ccw_machine_class_init>> mc->get_hotplug_handler = s390_get_hotplug_handler;
+         * 在以下使用MachineClass->get_hotplug_handler:
+         *   - hw/arm/virt.c|4190| <<virt_machine_class_init>> assert(!mc->get_hotplug_handler);
+         *   - hw/core/qdev-hotplug.c|26| <<qdev_get_machine_hotplug_handler>> if (mc->get_hotplug_handler) {
+         *   - hw/core/qdev-hotplug.c|27| <<qdev_get_machine_hotplug_handler>> return mc->get_hotplug_handler(machine, dev);
+         *   - hw/i386/pc.c|1699| <<pc_machine_class_init>> assert(!mc->get_hotplug_handler);
+         *   - hw/ppc/e500plat.c|77| <<e500plat_machine_class_init>> assert(!mc->get_hotplug_handler);
+         *   - hw/ppc/spapr.c|4640| <<spapr_machine_class_init>> assert(!mc->get_hotplug_handler);
+         *   - hw/riscv/virt.c|1736| <<virt_machine_class_init>> assert(!mc->get_hotplug_handler);
+         *   - hw/s390x/s390-virtio-ccw.c|825| <<ccw_machine_class_init>> assert(!mc->get_hotplug_handler);
+	 */
         if (mc->get_hotplug_handler) {
             return mc->get_hotplug_handler(machine, dev);
         }
@@ -108,6 +128,17 @@ HotplugHandler *qdev_get_hotplug_handler(DeviceState *dev)
     return hotplug_ctrl;
 }
 
+/*
+ * 在以下使用qdev_simple_device_unplug_cb():
+ *   - hw/char/virtio-serial-bus.c|1157| <<virtio_serial_class_init>> hc->unplug = qdev_simple_device_unplug_cb;
+ *   - hw/s390x/ap-bridge.c|69| <<ap_bridge_class_init>> hc->unplug = qdev_simple_device_unplug_cb;
+ *   - hw/scsi/scsi-bus.c|2041| <<scsi_bus_class_init>> hc->unplug = qdev_simple_device_unplug_cb;
+ *   - hw/scsi/virtio-scsi.c|1197| <<virtio_scsi_hotunplug>> qdev_simple_device_unplug_cb(hotplug_dev, dev, errp);
+ *   - hw/scsi/vmw_pvscsi.c|628| <<pvscsi_hot_unplug>> qdev_simple_device_unplug_cb(hotplug_dev, dev, errp);
+ *   - hw/usb/bus.c|37| <<usb_bus_class_init>> hc->unplug = qdev_simple_device_unplug_cb;
+ *   - hw/usb/dev-smartcard-reader.c|1453| <<ccid_class_initfn>> hc->unplug = qdev_simple_device_unplug_cb;
+ *   - hw/xen/xen-legacy-backend.c|657| <<xen_sysbus_class_init>> hc->unplug = qdev_simple_device_unplug_cb;
+ */
 /* can be used as ->unplug() callback for the simple cases */
 void qdev_simple_device_unplug_cb(HotplugHandler *hotplug_dev,
                                   DeviceState *dev, Error **errp)
diff --git a/hw/core/qdev.c b/hw/core/qdev.c
index e2aab3d1f..adb3e7f9c 100644
--- a/hw/core/qdev.c
+++ b/hw/core/qdev.c
@@ -371,12 +371,60 @@ int qdev_walk_children(DeviceState *dev,
     return 0;
 }
 
+/*
+ * pci_host_bridges上是PCIHostState.
+ *
+ * PCIHostState:
+ * -> PCIBus *bus;
+ *    -> BusState qbus;
+ *       -> BusChildHead children; (BusChild)
+ *
+ * typedef struct BusChild {
+ *     struct rcu_head rcu;
+ *     DeviceState *child;
+ *     int index;
+ *     QTAILQ_ENTRY(BusChild) sibling;
+ * } BusChild;
+ *
+ * DeviceState *child:
+ * -> BusStateHead child_bus; (BusState)
+ *
+ *
+ * 在以下使用qdev_find_recursive():
+ *   - hw/core/qdev.c|389| <<qdev_find_recursive>> ret = qdev_find_recursive(child, id);
+ *   - hw/core/sysbus-fdt.c|205| <<platform_bus_add_all_fdt_nodes>> dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
+ *   - hw/pci/pci.c|2822| <<pci_qdev_find_recursive>> DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
+ *   - hw/remote/remote-obj.c|99| <<remote_object_machine_done>> dev = qdev_find_recursive(sysbus_get_default(), o->devid);
+ *   - hw/remote/vfio-user-obj.c|760| <<vfu_object_init_ctx>> dev = qdev_find_recursive(sysbus_get_default(), o->device);
+ *   - ui/console.c|1241| <<qemu_console_lookup_by_device_name>> dev = qdev_find_recursive(sysbus_get_default(), device_id);
+ */
 DeviceState *qdev_find_recursive(BusState *bus, const char *id)
 {
     BusChild *kid;
     DeviceState *ret;
     BusState *child;
 
+    /*
+     * ITH_RCU_READ_LOCK_GUARD()核心技巧是用 for 循环制造一个作用域:
+     *
+     * 1. 初始化时调用 rcu_read_auto_lock(), 里面执行 rcu_read_lock()
+     * 2. 循环体执行你的代码
+     * 3. 离开循环时调用 rcu_read_auto_unlock(), 里面执行 rcu_read_unlock()
+     *
+     * QTAILQ_FOREACH_RCU 本身不是"加锁", 也不是完整的 RCU 机制; 它主要是 用
+     * RCU/atomic 读法遍历链表指针.
+     * 也就是说, 它和普通 QTAILQ_FOREACH 的区别是读链表指针时用:
+     * qatomic_rcu_read(...)
+     *
+     * qatomic_rcu_read() 的作用主要是:
+     * - 用 atomic load 读指针;
+     * - 带合适的内存顺序/屏障语义;
+     * - 保证 reader 看到的节点内容和 writer 发布节点时的初始化顺序一致.
+     *
+     * 两者分工是:
+     * - WITH_RCU_READ_LOCK_GUARD(): 告诉 RCU 系统"我正在读, 旧节点别 free"
+     * - QTAILQ_FOREACH_RCU(): 用 RCU 安全的 atomic load 读链表 next 指针
+     */
     WITH_RCU_READ_LOCK_GUARD() {
         QTAILQ_FOREACH_RCU(kid, &bus->children, sibling) {
             DeviceState *dev = kid->child;
@@ -386,6 +434,15 @@ DeviceState *qdev_find_recursive(BusState *bus, const char *id)
             }
 
             QLIST_FOREACH(child, &dev->child_bus, sibling) {
+	        /*
+		 * 在以下使用qdev_find_recursive():
+		 *   - hw/core/qdev.c|389| <<qdev_find_recursive>> ret = qdev_find_recursive(child, id);
+                 *   - hw/core/sysbus-fdt.c|205| <<platform_bus_add_all_fdt_nodes>> dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
+		 *   - hw/pci/pci.c|2822| <<pci_qdev_find_recursive>> DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
+                 *   - hw/remote/remote-obj.c|99| <<remote_object_machine_done>> dev = qdev_find_recursive(sysbus_get_default(), o->devid);
+                 *   - hw/remote/vfio-user-obj.c|760| <<vfu_object_init_ctx>> dev = qdev_find_recursive(sysbus_get_default(), o->device);
+                 *   - ui/console.c|1241| <<qemu_console_lookup_by_device_name>> dev = qdev_find_recursive(sysbus_get_default(), device_id);
+		 */
                 ret = qdev_find_recursive(child, id);
                 if (ret) {
                     return ret;
diff --git a/hw/core/sysbus-fdt.c b/hw/core/sysbus-fdt.c
index 12238570b..c496a321c 100644
--- a/hw/core/sysbus-fdt.c
+++ b/hw/core/sysbus-fdt.c
@@ -202,6 +202,15 @@ void platform_bus_add_all_fdt_nodes(void *fdt, const char *intc, hwaddr addr,
 
     qemu_fdt_setprop_phandle(fdt, node, "interrupt-parent", intc);
 
+    /*
+     * 在以下使用qdev_find_recursive():
+     *   - hw/core/qdev.c|389| <<qdev_find_recursive>> ret = qdev_find_recursive(child, id);
+     *   - hw/core/sysbus-fdt.c|205| <<platform_bus_add_all_fdt_nodes>> dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
+     *   - hw/pci/pci.c|2822| <<pci_qdev_find_recursive>> DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
+     *   - hw/remote/remote-obj.c|99| <<remote_object_machine_done>> dev = qdev_find_recursive(sysbus_get_default(), o->devid);
+     *   - hw/remote/vfio-user-obj.c|760| <<vfu_object_init_ctx>> dev = qdev_find_recursive(sysbus_get_default(), o->device);
+     *   - ui/console.c|1241| <<qemu_console_lookup_by_device_name>> dev = qdev_find_recursive(sysbus_get_default(), device_id);
+     */
     dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
     pbus = PLATFORM_BUS_DEVICE(dev);
 
diff --git a/hw/i386/amd_iommu.c b/hw/i386/amd_iommu.c
index 90252c52a..66e9f1230 100644
--- a/hw/i386/amd_iommu.c
+++ b/hw/i386/amd_iommu.c
@@ -2683,6 +2683,14 @@ static void amdvi_sysbus_realize(DeviceState *dev, Error **errp)
 
     if (s->pci_id) {
         PCIDevice *pdev = NULL;
+        /*
+	 * 在以下使用pci_qdev_find_device():
+         *   - hw/i386/amd_iommu.c|2686| <<amdvi_sysbus_realize>> int ret = pci_qdev_find_device(s->pci_id, &pdev);
+         *   - hw/pci/pci-hmp-cmds.c|185| <<hmp_pcie_aer_inject_error>> ret = pci_qdev_find_device(id, &dev);
+         *   - hw/pci/pcie_sriov.c|404| <<pcie_sriov_register_device>> if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
+         *   - hw/smbios/smbios.c|784| <<smbios_build_type_9_table>> int rc = pci_qdev_find_device(t9->pcidev, &pdev);
+         *   - hw/smbios/smbios.c|971| <<smbios_build_type_41_table>> int rc = pci_qdev_find_device(t41->pcidev, &pdev);
+	 */
         int ret = pci_qdev_find_device(s->pci_id, &pdev);
 
         if (ret) {
diff --git a/hw/net/igb.c b/hw/net/igb.c
index c076807e7..d598fe388 100644
--- a/hw/net/igb.c
+++ b/hw/net/igb.c
@@ -445,6 +445,11 @@ static void igb_pci_realize(PCIDevice *pci_dev, Error **errp)
 
     pcie_ari_init(pci_dev, 0x150);
 
+    /*
+     * 在以下使用pcie_sriov_pf_init():
+     *   - hw/net/igb.c|448| <<igb_pci_realize>> if (!pcie_sriov_pf_init(pci_dev, IGB_CAP_SRIOV_OFFSET, TYPE_IGBVF,
+     *   - hw/nvme/ctrl.c|9059| <<nvme_init_sriov>> if (!pcie_sriov_pf_init(pci_dev, offset, "nvme", vf_dev_id,
+     */
     if (!pcie_sriov_pf_init(pci_dev, IGB_CAP_SRIOV_OFFSET, TYPE_IGBVF,
                             IGB_82576_VF_DEV_ID, IGB_MAX_VF_FUNCTIONS,
                             IGB_MAX_VF_FUNCTIONS, IGB_VF_OFFSET, IGB_VF_STRIDE,
diff --git a/hw/net/virtio-net.c b/hw/net/virtio-net.c
index 814b99a43..86165ce74 100644
--- a/hw/net/virtio-net.c
+++ b/hw/net/virtio-net.c
@@ -3756,6 +3756,25 @@ static bool failover_unplug_primary(VirtIONet *n, DeviceState *dev)
     hotplug_ctrl = qdev_get_hotplug_handler(dev);
     if (hotplug_ctrl) {
         pci_dev = PCI_DEVICE(dev);
+        /*
+	 * 在以下使用PCIDevice->partially_hotplugged:
+         *   - hw/acpi/pcihp.c|207| <<acpi_pcihp_eject_slot>> if (dev->partially_hotplugged) {
+         *   - hw/net/virtio-net.c|3759| <<failover_unplug_primary>> pci_dev->partially_hotplugged = true;
+         *   - hw/net/virtio-net.c|3779| <<failover_replug_primary>> if (!pdev->partially_hotplugged) {
+         *   - hw/net/virtio-net.c|3797| <<failover_replug_primary>> pdev->partially_hotplugged = false;
+         *   - hw/pci/pci_host.c|72| <<is_pci_dev_ejected>> return pci_dev && pci_dev->partially_hotplugged &&
+         *   - hw/pci/pcie.c|584| <<pcie_unplug_device>> if (dev->partially_hotplugged) {
+         *
+         * 不是"普通热插拔了一半"的通用状态, 它主要是给 virtio-net failover
+         * 用的特殊标志.
+         * 这个 PCI 设备已经被 guest 通过 hot-unplug 流程"看起来拔掉了", 但 QEMU
+         * 还没有真正把对象删掉, 因为后面可能还要把它重新插回去.
+         * virtio-net failover 做迁移时, 会把 primary 网卡先从 guest 里隐藏/拔掉,
+         * 让 guest 使用 standby virtio-net. 这里不能真的 object_unparent() 删除
+         * primary 设备, 因为如果迁移失败, 还要 replug 回来.
+         * 一句话总结: partially_hotplugged 表示 guest 侧已经完成热拔, 但 QEMU
+         * 故意保留设备对象, 等待 failover 流程决定是彻底消失还是重新插回.
+	 */
         pci_dev->partially_hotplugged = true;
         hotplug_handler_unplug_request(hotplug_ctrl, dev, &err);
         if (err) {
@@ -3776,6 +3795,25 @@ static bool failover_replug_primary(VirtIONet *n, DeviceState *dev,
     PCIDevice *pdev = PCI_DEVICE(dev);
     BusState *primary_bus;
 
+    /*
+     * 在以下使用PCIDevice->partially_hotplugged:
+     *   - hw/acpi/pcihp.c|207| <<acpi_pcihp_eject_slot>> if (dev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3759| <<failover_unplug_primary>> pci_dev->partially_hotplugged = true;
+     *   - hw/net/virtio-net.c|3779| <<failover_replug_primary>> if (!pdev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3797| <<failover_replug_primary>> pdev->partially_hotplugged = false;
+     *   - hw/pci/pci_host.c|72| <<is_pci_dev_ejected>> return pci_dev && pci_dev->partially_hotplugged &&
+     *   - hw/pci/pcie.c|584| <<pcie_unplug_device>> if (dev->partially_hotplugged) {
+     *
+     * 不是"普通热插拔了一半"的通用状态, 它主要是给 virtio-net failover
+     * 用的特殊标志.
+     * 这个 PCI 设备已经被 guest 通过 hot-unplug 流程"看起来拔掉了", 但 QEMU
+     * 还没有真正把对象删掉, 因为后面可能还要把它重新插回去.
+     * virtio-net failover 做迁移时, 会把 primary 网卡先从 guest 里隐藏/拔掉,
+     * 让 guest 使用 standby virtio-net. 这里不能真的 object_unparent() 删除
+     * primary 设备, 因为如果迁移失败, 还要 replug 回来.
+     * 一句话总结: partially_hotplugged 表示 guest 侧已经完成热拔, 但 QEMU
+     * 故意保留设备对象, 等待 failover 流程决定是彻底消失还是重新插回.
+     */
     if (!pdev->partially_hotplugged) {
         return true;
     }
diff --git a/hw/nvme/ctrl.c b/hw/nvme/ctrl.c
index 4893cf7e7..298f3cbbe 100644
--- a/hw/nvme/ctrl.c
+++ b/hw/nvme/ctrl.c
@@ -9056,6 +9056,11 @@ static bool nvme_init_sriov(NvmeCtrl *n, PCIDevice *pci_dev, uint16_t offset,
                                       le16_to_cpu(cap->vifrsm),
                                       NULL, NULL);
 
+    /*
+     * 在以下使用pcie_sriov_pf_init():
+     *   - hw/net/igb.c|448| <<igb_pci_realize>> if (!pcie_sriov_pf_init(pci_dev, IGB_CAP_SRIOV_OFFSET, TYPE_IGBVF,
+     *   - hw/nvme/ctrl.c|9059| <<nvme_init_sriov>> if (!pcie_sriov_pf_init(pci_dev, offset, "nvme", vf_dev_id,
+     */
     if (!pcie_sriov_pf_init(pci_dev, offset, "nvme", vf_dev_id,
                             n->params.sriov_max_vfs, n->params.sriov_max_vfs,
                             NVME_VF_OFFSET, NVME_VF_STRIDE, errp)) {
diff --git a/hw/pci/pci-hmp-cmds.c b/hw/pci/pci-hmp-cmds.c
index a5f6483cc..82c88ebe6 100644
--- a/hw/pci/pci-hmp-cmds.c
+++ b/hw/pci/pci-hmp-cmds.c
@@ -182,6 +182,14 @@ void hmp_pcie_aer_inject_error(Monitor *mon, const QDict *qdict)
     PCIEAERErr aer_err;
     int ret;
 
+    /*
+     * 在以下使用pci_qdev_find_device():
+     *   - hw/i386/amd_iommu.c|2686| <<amdvi_sysbus_realize>> int ret = pci_qdev_find_device(s->pci_id, &pdev);
+     *   - hw/pci/pci-hmp-cmds.c|185| <<hmp_pcie_aer_inject_error>> ret = pci_qdev_find_device(id, &dev);
+     *   - hw/pci/pcie_sriov.c|404| <<pcie_sriov_register_device>> if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
+     *   - hw/smbios/smbios.c|784| <<smbios_build_type_9_table>> int rc = pci_qdev_find_device(t9->pcidev, &pdev);
+     *   - hw/smbios/smbios.c|971| <<smbios_build_type_41_table>> int rc = pci_qdev_find_device(t41->pcidev, &pdev);
+     */
     ret = pci_qdev_find_device(id, &dev);
     if (ret == -ENODEV) {
         error_setg(&err, "device '%s' not found", id);
diff --git a/hw/pci/pci.c b/hw/pci/pci.c
index d3191609e..f56afcc48 100644
--- a/hw/pci/pci.c
+++ b/hw/pci/pci.c
@@ -56,6 +56,63 @@
 #include "hw/xen/xen.h"
 #include "hw/i386/kvm/xen_evtchn.h"
 
+/*
+ * pci_host_bridges上是PCIHostState.
+ *
+ * PCIHostState:
+ * -> PCIBus *bus;
+ *    -> BusState qbus;
+ *       -> BusChildHead children; (BusChild)
+ *
+ * typedef struct BusChild {
+ *     struct rcu_head rcu;
+ *     DeviceState *child;
+ *     int index;
+ *     QTAILQ_ENTRY(BusChild) sibling;
+ * } BusChild;
+ *
+ * DeviceState *child:
+ * -> BusStateHead child_bus; (BusState)
+ */
+/*
+ * 给一个 PCIDevice *dev, 要看它是不是 PCI bridge. 只有 bridge, root-port,
+ * downstream-port 这类设备后面才会有 secondary bus. 普通 endpoint 后面没有
+ * bus.
+ *
+ * 常见做法:
+ *
+ * if (object_dynamic_cast(OBJECT(dev), TYPE_PCI_BRIDGE)) {
+ *     PCIBus *sec_bus = pci_bridge_get_sec_bus(PCI_BRIDGE(dev));
+ * }
+ *
+ * 或者如果代码里已经确认它是 bridge:
+ *
+ * PCIBus *sec_bus = pci_bridge_get_sec_bus(PCI_BRIDGE(dev));
+ *
+ * 比如 ACPI PCI hotplug 里 cold-plug bridge 时就是这么取 secondary bus:
+ *
+ * if (object_dynamic_cast(OBJECT(dev), TYPE_PCI_BRIDGE)) {
+ *     PCIBus *sec = pci_bridge_get_sec_bus(PCI_BRIDGE(pdev));
+ *     qbus_set_hotplug_handler(BUS(sec), OBJECT(hotplug_dev));
+ * }
+ *
+ * 判断逻辑:
+ * - PCIDevice 是普通网卡, 磁盘, GPU endpoint: 没有后面的 bus.
+ * - PCIDevice 是 TYPE_PCI_BRIDGE: 有 secondary PCIBus.
+ * - PCIe root port / downstream port 本质上也是 bridge 类设备, 也可以走
+ * pci_bridge_get_sec_bus().
+ *
+ * 如果你只是想从一个设备找它所在的 bus, 用:
+ *
+ * PCIBus *bus = pci_get_bus(dev);
+ *
+ * 这拿到的是设备挂在哪条 bus 上, 不是它后面的 secondary bus.
+ *
+ * 如果你想拿 bridge 后面的 bus, 那不是 pci_get_bus(), 而是:
+ *
+ * pci_bridge_get_sec_bus(PCI_BRIDGE(dev))
+ */
+
 bool pci_available = true;
 
 static char *pcibus_get_dev_path(DeviceState *dev);
@@ -358,6 +415,13 @@ static void pci_del_option_rom(PCIDevice *pdev);
 static uint16_t pci_default_sub_vendor_id = PCI_SUBVENDOR_ID_REDHAT_QUMRANET;
 static uint16_t pci_default_sub_device_id = PCI_SUBDEVICE_ID_QEMU;
 
+/*
+ * 在以下使用pci_host_bridges:
+ *   - hw/pci/pci.c|361| <<global>> PCIHostStateList pci_host_bridges;
+ *   - hw/pci/pci-qmp-cmds.c|192| <<qmp_query_pci>> QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
+ *   - hw/pci/pci.c|629| <<pci_host_bus_register>> QLIST_INSERT_HEAD(&pci_host_bridges, host_bridge, next);
+ *   - hw/pci/pci.c|2840| <<pci_qdev_find_device>> QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
+ */
 PCIHostStateList pci_host_bridges;
 
 int pci_bar(PCIDevice *d, int reg)
@@ -622,10 +686,21 @@ static void pcibus_reset_hold(Object *obj, ResetType type)
     }
 }
 
+/*
+ * 在以下使用pci_host_bus_register():
+ *   - hw/pci/pci.c|698| <<pci_root_bus_internal_init>> pci_host_bus_register(parent);
+ */
 static void pci_host_bus_register(DeviceState *host)
 {
     PCIHostState *host_bridge = PCI_HOST_BRIDGE(host);
 
+    /*
+     * 在以下使用pci_host_bridges:
+     *   - hw/pci/pci.c|361| <<global>> PCIHostStateList pci_host_bridges;
+     *   - hw/pci/pci-qmp-cmds.c|192| <<qmp_query_pci>> QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
+     *   - hw/pci/pci.c|629| <<pci_host_bus_register>> QLIST_INSERT_HEAD(&pci_host_bridges, host_bridge, next);
+     *   - hw/pci/pci.c|2840| <<pci_qdev_find_device>> QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
+     */
     QLIST_INSERT_HEAD(&pci_host_bridges, host_bridge, next);
 }
 
@@ -681,6 +756,11 @@ bool pci_bus_bypass_iommu(PCIBus *bus)
     return host_bridge->bypass_iommu;
 }
 
+/*
+ * 在以下使用pci_root_bus_internal_init():
+ *   - hw/pci/pci.c|717| <<pci_root_bus_init>> pci_root_bus_internal_init(bus, parent, mem, io, devfn_min);
+ *   - hw/pci/pci.c|727| <<pci_root_bus_new>> pci_root_bus_internal_init(bus, parent, mem, io, devfn_min);
+ */
 static void pci_root_bus_internal_init(PCIBus *bus, DeviceState *parent,
                                        MemoryRegion *mem, MemoryRegion *io,
                                        uint8_t devfn_min)
@@ -708,6 +788,10 @@ bool pci_bus_is_express(const PCIBus *bus)
     return object_dynamic_cast(OBJECT(bus), TYPE_PCIE_BUS);
 }
 
+/*
+ * 在以下使用pci_root_bus_init():
+ *   - hw/pci-host/versatile.c|408| <<pci_vpb_realize>> pci_root_bus_init(&s->pci_bus, sizeof(s->pci_bus), dev, "pci",
+ */
 void pci_root_bus_init(PCIBus *bus, size_t bus_size, DeviceState *parent,
                        const char *name,
                        MemoryRegion *mem, MemoryRegion *io,
@@ -717,6 +801,17 @@ void pci_root_bus_init(PCIBus *bus, size_t bus_size, DeviceState *parent,
     pci_root_bus_internal_init(bus, parent, mem, io, devfn_min);
 }
 
+/*
+ * 在以下使用pci_root_bus_new():
+ *   - hw/pci-bridge/pci_expander_bridge.c|364| <<pxb_dev_realize_common>> bus = pci_root_bus_new(ds, dev_name, NULL, NULL, 0, TYPE_PXB_PCIE_BUS);
+ *   - hw/pci-bridge/pci_expander_bridge.c|366| <<pxb_dev_realize_common>> bus = pci_root_bus_new(ds, dev_name, NULL, NULL, 0, TYPE_PXB_CXL_BUS);
+ *   - hw/pci-bridge/pci_expander_bridge.c|370| <<pxb_dev_realize_common>> bus = pci_root_bus_new(ds, "pxb-internal", NULL, NULL, 0, TYPE_PXB_BUS);
+ *   - hw/pci-host/gt64120.c|1202| <<gt64120_realize>> phb->bus = pci_root_bus_new(dev, "pci",
+ *   - hw/pci-host/i440fx.c|281| <<i440fx_pcihost_realize>> b = pci_root_bus_new(dev, NULL, s->pci_address_space,
+ *   - hw/pci-host/q35.c|65| <<q35_host_realize>> pci->bus = pci_root_bus_new(DEVICE(s), "pcie.0",
+ *   - hw/pci-host/remote.c|44| <<remote_pcihost_realize>> pci->bus = pci_root_bus_new(DEVICE(s), "remote-pci",
+ *   - hw/pci/pci.c|772| <<pci_register_root_bus>> bus = pci_root_bus_new(parent, name, mem, io, devfn_min, typename);
+ */
 PCIBus *pci_root_bus_new(DeviceState *parent, const char *name,
                          MemoryRegion *mem, MemoryRegion *io,
                          uint8_t devfn_min, const char *typename)
@@ -2816,9 +2911,25 @@ static char *pcibus_get_dev_path(DeviceState *dev)
     return path;
 }
 
+/*
+ * 在以下使用pci_qdev_find_recursive():
+ *   - hw/pci/pci.c|2841| <<pci_qdev_find_device>> int tmp = pci_qdev_find_recursive(host_bridge->bus, id, pdev);
+ */
 static int pci_qdev_find_recursive(PCIBus *bus,
                                    const char *id, PCIDevice **pdev)
 {
+    /*
+     * 在以下使用qdev_find_recursive():
+     *   - hw/core/qdev.c|389| <<qdev_find_recursive>> ret = qdev_find_recursive(child, id);
+     *   - hw/core/sysbus-fdt.c|205| <<platform_bus_add_all_fdt_nodes>> dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
+     *   - hw/pci/pci.c|2822| <<pci_qdev_find_recursive>> DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
+     *   - hw/remote/remote-obj.c|99| <<remote_object_machine_done>> dev = qdev_find_recursive(sysbus_get_default(), o->devid);
+     *   - hw/remote/vfio-user-obj.c|760| <<vfu_object_init_ctx>> dev = qdev_find_recursive(sysbus_get_default(), o->device);
+     *   - ui/console.c|1241| <<qemu_console_lookup_by_device_name>> dev = qdev_find_recursive(sysbus_get_default(), device_id);
+     *
+     * struct PCIBus {
+     *     BusState qbus;
+     */
     DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
     if (!qdev) {
         return -ENODEV;
@@ -2832,11 +2943,44 @@ static int pci_qdev_find_recursive(PCIBus *bus,
     return -EINVAL;
 }
 
+/*
+ * pci_host_bridges上是PCIHostState.
+ *
+ * PCIHostState:
+ * -> PCIBus *bus;
+ *    -> BusState qbus;
+ *       -> BusChildHead children; (BusChild)
+ *
+ * typedef struct BusChild {
+ *     struct rcu_head rcu;
+ *     DeviceState *child;
+ *     int index;
+ *     QTAILQ_ENTRY(BusChild) sibling;
+ * } BusChild;
+ *
+ * DeviceState *child:
+ * -> BusStateHead child_bus; (BusState)
+ *
+ *
+ * 在以下使用pci_qdev_find_device():
+ *   - hw/i386/amd_iommu.c|2686| <<amdvi_sysbus_realize>> int ret = pci_qdev_find_device(s->pci_id, &pdev);
+ *   - hw/pci/pci-hmp-cmds.c|185| <<hmp_pcie_aer_inject_error>> ret = pci_qdev_find_device(id, &dev);
+ *   - hw/pci/pcie_sriov.c|404| <<pcie_sriov_register_device>> if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
+ *   - hw/smbios/smbios.c|784| <<smbios_build_type_9_table>> int rc = pci_qdev_find_device(t9->pcidev, &pdev);
+ *   - hw/smbios/smbios.c|971| <<smbios_build_type_41_table>> int rc = pci_qdev_find_device(t41->pcidev, &pdev);
+ */
 int pci_qdev_find_device(const char *id, PCIDevice **pdev)
 {
     PCIHostState *host_bridge;
     int rc = -ENODEV;
 
+    /*
+     * 在以下使用pci_host_bridges:
+     *   - hw/pci/pci.c|361| <<global>> PCIHostStateList pci_host_bridges;
+     *   - hw/pci/pci-qmp-cmds.c|192| <<qmp_query_pci>> QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
+     *   - hw/pci/pci.c|629| <<pci_host_bus_register>> QLIST_INSERT_HEAD(&pci_host_bridges, host_bridge, next);
+     *   - hw/pci/pci.c|2840| <<pci_qdev_find_device>> QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
+     */
     QLIST_FOREACH(host_bridge, &pci_host_bridges, next) {
         int tmp = pci_qdev_find_recursive(host_bridge->bus, id, pdev);
         if (!tmp) {
diff --git a/hw/pci/pci_bridge.c b/hw/pci/pci_bridge.c
index e85932e41..1e68162e4 100644
--- a/hw/pci/pci_bridge.c
+++ b/hw/pci/pci_bridge.c
@@ -71,6 +71,10 @@ PCIDevice *pci_bridge_get_device(PCIBus *bus)
 /* Accessor function to get secondary bus from pci-to-pci bridge device */
 PCIBus *pci_bridge_get_sec_bus(PCIBridge *br)
 {
+    /*
+     * PCIBridge *br:
+     * -> PCIBus sec_bus;
+     */
     return &br->sec_bus;
 }
 
diff --git a/hw/pci/pci_host.c b/hw/pci/pci_host.c
index 2a7fdfa56..132ca8953 100644
--- a/hw/pci/pci_host.c
+++ b/hw/pci/pci_host.c
@@ -69,6 +69,25 @@ static bool is_pci_dev_ejected(PCIDevice *pci_dev)
      * so we stop responding config accesses even if the
      * device is not deleted (failover flow)
      */
+    /*
+     * 在以下使用PCIDevice->partially_hotplugged:
+     *   - hw/acpi/pcihp.c|207| <<acpi_pcihp_eject_slot>> if (dev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3759| <<failover_unplug_primary>> pci_dev->partially_hotplugged = true;
+     *   - hw/net/virtio-net.c|3779| <<failover_replug_primary>> if (!pdev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3797| <<failover_replug_primary>> pdev->partially_hotplugged = false;
+     *   - hw/pci/pci_host.c|72| <<is_pci_dev_ejected>> return pci_dev && pci_dev->partially_hotplugged &&
+     *   - hw/pci/pcie.c|584| <<pcie_unplug_device>> if (dev->partially_hotplugged) {
+     *
+     * 不是"普通热插拔了一半"的通用状态, 它主要是给 virtio-net failover
+     * 用的特殊标志.
+     * 这个 PCI 设备已经被 guest 通过 hot-unplug 流程"看起来拔掉了", 但 QEMU
+     * 还没有真正把对象删掉, 因为后面可能还要把它重新插回去.
+     * virtio-net failover 做迁移时, 会把 primary 网卡先从 guest 里隐藏/拔掉,
+     * 让 guest 使用 standby virtio-net. 这里不能真的 object_unparent() 删除
+     * primary 设备, 因为如果迁移失败, 还要 replug 回来.
+     * 一句话总结: partially_hotplugged 表示 guest 侧已经完成热拔, 但 QEMU
+     * 故意保留设备对象, 等待 failover 流程决定是彻底消失还是重新插回.
+     */
     return pci_dev && pci_dev->partially_hotplugged &&
            !pci_dev->qdev.pending_deleted_event;
 }
diff --git a/hw/pci/pcie.c b/hw/pci/pcie.c
index 4622c75e4..47da3a5a9 100644
--- a/hw/pci/pcie.c
+++ b/hw/pci/pcie.c
@@ -407,6 +407,25 @@ static void hotplug_event_update_event_status(PCIDevice *dev)
         (sltsta & sltctl & PCI_EXP_HP_EV_SUPPORTED);
 }
 
+/*
+ * 在以下使用hotplug_event_notify():
+ *   - hw/pci/pcie.c|493| <<pcie_cap_slot_event>> hotplug_event_notify(dev);
+ *   - hw/pci/pcie.c|679| <<pcie_cap_slot_unplug_request_cb>> hotplug_event_notify(hotplug_pdev);
+ *   - hw/pci/pcie.c|916| <<pcie_cap_slot_write_config>> hotplug_event_notify(dev);
+ *
+ * 当前调用点都在 PCIe native hotplug 路径里:
+ *
+ * - PCIe slot plug/unplug event
+ * - PCIe slot powered-off immediate unplug path
+ * - 新增的 PCIe native force unplug path
+ * - PCIe slot write control register触发的 hotplug event
+ *
+ * 它不是 ACPI 用的. ACPI PCI hotplug 用的是:
+ * acpi_send_event(DEVICE(hotplug_dev), ACPI_PCI_HOTPLUG_STATUS);
+ *
+ * SHPC 用的是:
+ * shpc_interrupt_update(pci_hotplug_dev);
+ */
 static void hotplug_event_notify(PCIDevice *dev)
 {
     bool prev = dev->exp.hpev_notified;
@@ -490,6 +509,25 @@ static void pcie_cap_slot_event(PCIDevice *dev, PCIExpressHotPlugEvent event)
                                    PCI_EXP_SLTSTA, event) == event) {
         return;
     }
+    /*
+     * 在以下使用hotplug_event_notify():
+     *   - hw/pci/pcie.c|493| <<pcie_cap_slot_event>> hotplug_event_notify(dev);
+     *   - hw/pci/pcie.c|679| <<pcie_cap_slot_unplug_request_cb>> hotplug_event_notify(hotplug_pdev);
+     *   - hw/pci/pcie.c|916| <<pcie_cap_slot_write_config>> hotplug_event_notify(dev);
+     *
+     * 当前调用点都在 PCIe native hotplug 路径里:
+     *
+     * - PCIe slot plug/unplug event
+     * - PCIe slot powered-off immediate unplug path
+     * - 新增的 PCIe native force unplug path
+     * - PCIe slot write control register触发的 hotplug event
+     *
+     * 它不是 ACPI 用的. ACPI PCI hotplug 用的是:
+     * acpi_send_event(DEVICE(hotplug_dev), ACPI_PCI_HOTPLUG_STATUS);
+     *
+     * SHPC 用的是:
+     * shpc_interrupt_update(pci_hotplug_dev);
+     */
     hotplug_event_notify(dev);
 }
 
@@ -581,6 +619,25 @@ static void pcie_unplug_device(PCIBus *bus, PCIDevice *dev, void *opaque)
 {
     HotplugHandler *hotplug_ctrl = qdev_get_hotplug_handler(DEVICE(dev));
 
+    /*
+     * 在以下使用PCIDevice->partially_hotplugged:
+     *   - hw/acpi/pcihp.c|207| <<acpi_pcihp_eject_slot>> if (dev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3759| <<failover_unplug_primary>> pci_dev->partially_hotplugged = true;
+     *   - hw/net/virtio-net.c|3779| <<failover_replug_primary>> if (!pdev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3797| <<failover_replug_primary>> pdev->partially_hotplugged = false;
+     *   - hw/pci/pci_host.c|72| <<is_pci_dev_ejected>> return pci_dev && pci_dev->partially_hotplugged &&
+     *   - hw/pci/pcie.c|584| <<pcie_unplug_device>> if (dev->partially_hotplugged) {
+     *
+     * 不是"普通热插拔了一半"的通用状态, 它主要是给 virtio-net failover
+     * 用的特殊标志.
+     * 这个 PCI 设备已经被 guest 通过 hot-unplug 流程"看起来拔掉了", 但 QEMU
+     * 还没有真正把对象删掉, 因为后面可能还要把它重新插回去.
+     * virtio-net failover 做迁移时, 会把 primary 网卡先从 guest 里隐藏/拔掉,
+     * 让 guest 使用 standby virtio-net. 这里不能真的 object_unparent() 删除
+     * primary 设备, 因为如果迁移失败, 还要 replug 回来.
+     * 一句话总结: partially_hotplugged 表示 guest 侧已经完成热拔, 但 QEMU
+     * 故意保留设备对象, 等待 failover 流程决定是彻底消失还是重新插回.
+     */
     if (dev->partially_hotplugged) {
         dev->qdev.pending_deleted_event = false;
         return;
@@ -657,6 +714,25 @@ void pcie_cap_slot_unplug_request_cb(HotplugHandler *hotplug_dev,
     if (pcie_sltctl_powered_off(sltctl)) {
         /* slot is powered off -> unplug without round-trip to the guest */
         pcie_cap_slot_do_unplug(hotplug_pdev);
+        /*
+	 * 在以下使用hotplug_event_notify():
+         *   - hw/pci/pcie.c|493| <<pcie_cap_slot_event>> hotplug_event_notify(dev);
+         *   - hw/pci/pcie.c|679| <<pcie_cap_slot_unplug_request_cb>> hotplug_event_notify(hotplug_pdev);
+         *   - hw/pci/pcie.c|916| <<pcie_cap_slot_write_config>> hotplug_event_notify(dev);
+         *
+         * 当前调用点都在 PCIe native hotplug 路径里:
+         *
+         * - PCIe slot plug/unplug event
+         * - PCIe slot powered-off immediate unplug path
+         * - 新增的 PCIe native force unplug path
+         * - PCIe slot write control register触发的 hotplug event
+         *
+         * 它不是 ACPI 用的. ACPI PCI hotplug 用的是:
+         * acpi_send_event(DEVICE(hotplug_dev), ACPI_PCI_HOTPLUG_STATUS);
+         *
+         * SHPC 用的是:
+         * shpc_interrupt_update(pci_hotplug_dev);
+	 */
         hotplug_event_notify(hotplug_pdev);
         pci_word_test_and_clear_mask(exp_cap + PCI_EXP_SLTSTA,
                                      PCI_EXP_SLTSTA_ABP);
@@ -894,6 +970,25 @@ void pcie_cap_slot_write_config(PCIDevice *dev,
     }
     pcie_cap_update_power(dev);
 
+    /*
+     * 在以下使用hotplug_event_notify():
+     *   - hw/pci/pcie.c|493| <<pcie_cap_slot_event>> hotplug_event_notify(dev);
+     *   - hw/pci/pcie.c|679| <<pcie_cap_slot_unplug_request_cb>> hotplug_event_notify(hotplug_pdev);
+     *   - hw/pci/pcie.c|916| <<pcie_cap_slot_write_config>> hotplug_event_notify(dev);
+     *
+     * 当前调用点都在 PCIe native hotplug 路径里:
+     *
+     * - PCIe slot plug/unplug event
+     * - PCIe slot powered-off immediate unplug path
+     * - 新增的 PCIe native force unplug path
+     * - PCIe slot write control register触发的 hotplug event
+     *
+     * 它不是 ACPI 用的. ACPI PCI hotplug 用的是:
+     * acpi_send_event(DEVICE(hotplug_dev), ACPI_PCI_HOTPLUG_STATUS);
+     *
+     * SHPC 用的是:
+     * shpc_interrupt_update(pci_hotplug_dev);
+     */
     hotplug_event_notify(dev);
 
     /* 
diff --git a/hw/pci/pcie_sriov.c b/hw/pci/pcie_sriov.c
index c41ac95be..d7abc2f68 100644
--- a/hw/pci/pcie_sriov.c
+++ b/hw/pci/pcie_sriov.c
@@ -32,6 +32,10 @@ static void unparent_vfs(PCIDevice *dev, uint16_t total_vfs)
     dev->exp.sriov_pf.vf = NULL;
 }
 
+/*
+ * 在以下使用register_vfs():
+ *   - hw/pci/pcie_sriov.c|72| <<consume_config>> register_vfs(dev);
+ */
 static void register_vfs(PCIDevice *dev)
 {
     uint16_t num_vfs;
@@ -85,6 +89,11 @@ static void consume_config(PCIDevice *dev)
     }
 }
 
+/*
+ * 在以下使用pcie_sriov_pf_init_common():
+ *   - hw/pci/pcie_sriov.c|166| <<pcie_sriov_pf_init>> if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, init_vfs,
+ *   - hw/pci/pcie_sriov.c|325| <<pcie_sriov_pf_init_from_user_created_vfs>> if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, pf->len,
+ */
 static bool pcie_sriov_pf_init_common(PCIDevice *dev, uint16_t offset,
                                       uint16_t vf_dev_id, uint16_t init_vfs,
                                       uint16_t total_vfs, uint16_t vf_offset,
@@ -149,6 +158,11 @@ static bool pcie_sriov_pf_init_common(PCIDevice *dev, uint16_t offset,
     return true;
 }
 
+/*
+ * 在以下使用pcie_sriov_pf_init():
+ *   - hw/net/igb.c|448| <<igb_pci_realize>> if (!pcie_sriov_pf_init(pci_dev, IGB_CAP_SRIOV_OFFSET, TYPE_IGBVF,
+ *   - hw/nvme/ctrl.c|9059| <<nvme_init_sriov>> if (!pcie_sriov_pf_init(pci_dev, offset, "nvme", vf_dev_id,
+ */
 bool pcie_sriov_pf_init(PCIDevice *dev, uint16_t offset,
                         const char *vfname, uint16_t vf_dev_id,
                         uint16_t init_vfs, uint16_t total_vfs,
@@ -163,6 +177,11 @@ bool pcie_sriov_pf_init(PCIDevice *dev, uint16_t offset,
         return false;
     }
 
+    /*
+     * 在以下使用pcie_sriov_pf_init_common():
+     *   - hw/pci/pcie_sriov.c|166| <<pcie_sriov_pf_init>> if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, init_vfs,
+     *   - hw/pci/pcie_sriov.c|325| <<pcie_sriov_pf_init_from_user_created_vfs>> if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, pf->len,
+     */
     if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, init_vfs,
                                    total_vfs, vf_offset, vf_stride, errp)) {
         return false;
@@ -322,6 +341,11 @@ int16_t pcie_sriov_pf_init_from_user_created_vfs(PCIDevice *dev,
         }
     }
 
+    /*
+     * 在以下使用pcie_sriov_pf_init_common():
+     *   - hw/pci/pcie_sriov.c|166| <<pcie_sriov_pf_init>> if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, init_vfs,
+     *   - hw/pci/pcie_sriov.c|325| <<pcie_sriov_pf_init_from_user_created_vfs>> if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, pf->len,
+     */
     if (!pcie_sriov_pf_init_common(dev, offset, vf_dev_id, pf->len,
                                    pf->len, vf_offset, vf_stride, errp)) {
         return -1;
@@ -377,6 +401,14 @@ bool pcie_sriov_register_device(PCIDevice *dev, Error **errp)
             return false;
         }
 
+        /*
+	 * 在以下使用pci_qdev_find_device():
+         *   - hw/i386/amd_iommu.c|2686| <<amdvi_sysbus_realize>> int ret = pci_qdev_find_device(s->pci_id, &pdev);
+         *   - hw/pci/pci-hmp-cmds.c|185| <<hmp_pcie_aer_inject_error>> ret = pci_qdev_find_device(id, &dev);
+         *   - hw/pci/pcie_sriov.c|404| <<pcie_sriov_register_device>> if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
+         *   - hw/smbios/smbios.c|784| <<smbios_build_type_9_table>> int rc = pci_qdev_find_device(t9->pcidev, &pdev);
+         *   - hw/smbios/smbios.c|971| <<smbios_build_type_41_table>> int rc = pci_qdev_find_device(t41->pcidev, &pdev);
+	 */
         if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
             error_setg(errp, "PCI device specified as SR-IOV PF already exists");
             return false;
diff --git a/hw/remote/remote-obj.c b/hw/remote/remote-obj.c
index 4e74ae761..22ed7a40a 100644
--- a/hw/remote/remote-obj.c
+++ b/hw/remote/remote-obj.c
@@ -96,6 +96,15 @@ static void remote_object_machine_done(Notifier *notifier, void *data)
     RemoteCommDev *comdev = NULL;
     Error *err = NULL;
 
+    /*
+     * 在以下使用qdev_find_recursive():
+     *   - hw/core/qdev.c|389| <<qdev_find_recursive>> ret = qdev_find_recursive(child, id);
+     *   - hw/core/sysbus-fdt.c|205| <<platform_bus_add_all_fdt_nodes>> dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
+     *   - hw/pci/pci.c|2822| <<pci_qdev_find_recursive>> DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
+     *   - hw/remote/remote-obj.c|99| <<remote_object_machine_done>> dev = qdev_find_recursive(sysbus_get_default(), o->devid);
+     *   - hw/remote/vfio-user-obj.c|760| <<vfu_object_init_ctx>> dev = qdev_find_recursive(sysbus_get_default(), o->device);
+     *   - ui/console.c|1241| <<qemu_console_lookup_by_device_name>> dev = qdev_find_recursive(sysbus_get_default(), device_id);
+     */
     dev = qdev_find_recursive(sysbus_get_default(), o->devid);
     if (!dev || !object_dynamic_cast(OBJECT(dev), TYPE_PCI_DEVICE)) {
         error_report("%s is not a PCI device", o->devid);
diff --git a/hw/remote/vfio-user-obj.c b/hw/remote/vfio-user-obj.c
index 87fa7b657..1d8f68776 100644
--- a/hw/remote/vfio-user-obj.c
+++ b/hw/remote/vfio-user-obj.c
@@ -757,6 +757,15 @@ static void vfu_object_init_ctx(VfuObject *o, Error **errp)
         return;
     }
 
+    /*
+     * 在以下使用qdev_find_recursive():
+     *   - hw/core/qdev.c|389| <<qdev_find_recursive>> ret = qdev_find_recursive(child, id);
+     *   - hw/core/sysbus-fdt.c|205| <<platform_bus_add_all_fdt_nodes>> dev = qdev_find_recursive(sysbus_get_default(), TYPE_PLATFORM_BUS_DEVICE);
+     *   - hw/pci/pci.c|2822| <<pci_qdev_find_recursive>> DeviceState *qdev = qdev_find_recursive(&bus->qbus, id);
+     *   - hw/remote/remote-obj.c|99| <<remote_object_machine_done>> dev = qdev_find_recursive(sysbus_get_default(), o->devid);
+     *   - hw/remote/vfio-user-obj.c|760| <<vfu_object_init_ctx>> dev = qdev_find_recursive(sysbus_get_default(), o->device);
+     *   - ui/console.c|1241| <<qemu_console_lookup_by_device_name>> dev = qdev_find_recursive(sysbus_get_default(), device_id);
+     */
     dev = qdev_find_recursive(sysbus_get_default(), o->device);
     if (dev == NULL) {
         error_setg(errp, "vfu: Device %s not found", o->device);
diff --git a/hw/smbios/smbios.c b/hw/smbios/smbios.c
index 7d7141851..467202093 100644
--- a/hw/smbios/smbios.c
+++ b/hw/smbios/smbios.c
@@ -781,6 +781,14 @@ static void smbios_build_type_9_table(Error **errp)
 
         if (t9->pcidev) {
             PCIDevice *pdev = NULL;
+            /*
+	     * 在以下使用pci_qdev_find_device():
+             *   - hw/i386/amd_iommu.c|2686| <<amdvi_sysbus_realize>> int ret = pci_qdev_find_device(s->pci_id, &pdev);
+             *   - hw/pci/pci-hmp-cmds.c|185| <<hmp_pcie_aer_inject_error>> ret = pci_qdev_find_device(id, &dev);
+             *   - hw/pci/pcie_sriov.c|404| <<pcie_sriov_register_device>> if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
+             *   - hw/smbios/smbios.c|784| <<smbios_build_type_9_table>> int rc = pci_qdev_find_device(t9->pcidev, &pdev);
+             *   - hw/smbios/smbios.c|971| <<smbios_build_type_41_table>> int rc = pci_qdev_find_device(t41->pcidev, &pdev);
+	     */
             int rc = pci_qdev_find_device(t9->pcidev, &pdev);
             if (rc != 0) {
                 error_setg(errp,
@@ -968,6 +976,14 @@ static void smbios_build_type_41_table(Error **errp)
 
         if (t41->pcidev) {
             PCIDevice *pdev = NULL;
+	    /*
+	     * 在以下使用pci_qdev_find_device():
+	     *   - hw/i386/amd_iommu.c|2686| <<amdvi_sysbus_realize>> int ret = pci_qdev_find_device(s->pci_id, &pdev);
+	     *   - hw/pci/pci-hmp-cmds.c|185| <<hmp_pcie_aer_inject_error>> ret = pci_qdev_find_device(id, &dev);
+	     *   - hw/pci/pcie_sriov.c|404| <<pcie_sriov_register_device>> if (!pci_qdev_find_device(dev->sriov_pf, &pci_pf)) {
+             *   - hw/smbios/smbios.c|784| <<smbios_build_type_9_table>> int rc = pci_qdev_find_device(t9->pcidev, &pdev);
+             *   - hw/smbios/smbios.c|971| <<smbios_build_type_41_table>> int rc = pci_qdev_find_device(t41->pcidev, &pdev);
+	     */
             int rc = pci_qdev_find_device(t41->pcidev, &pdev);
             if (rc != 0) {
                 error_setg(errp,
diff --git a/hw/vfio/cpr-legacy.c b/hw/vfio/cpr-legacy.c
index 2d40d8bae..f6a3beb3b 100644
--- a/hw/vfio/cpr-legacy.c
+++ b/hw/vfio/cpr-legacy.c
@@ -38,6 +38,11 @@ static bool vfio_dma_unmap_vaddr_all(VFIOLegacyContainer *container,
  * Set the new @vaddr for any mappings registered during cpr load.
  * The incoming state is cleared thereafter.
  */
+/*
+ * 在以下使用vfio_legacy_cpr_dma_map():
+ *   - hw/vfio/cpr-legacy.c|107| <<vfio_container_post_load>> vioc->dma_map = vfio_legacy_cpr_dma_map;
+ *   - hw/vfio/cpr-legacy.c|153| <<vfio_cpr_fail_notifier>> vioc->dma_map = vfio_legacy_cpr_dma_map;
+ */
 static int vfio_legacy_cpr_dma_map(const VFIOContainer *bcontainer,
                                    hwaddr iova, uint64_t size, void *vaddr,
                                    bool readonly, MemoryRegion *mr)
@@ -262,6 +267,19 @@ int vfio_cpr_group_get_device_fd(int d, const char *name)
     if (fd < 0) {
         fd = ioctl(d, VFIO_GROUP_GET_DEVICE_FD, name);
         if (fd >= 0) {
+            /*
+	     * 在以下使用cpr_save_fd():
+             *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+             *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+             *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+             *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+             *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+             *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+             *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+             *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+	     */
             cpr_save_fd(name, id, fd);
         }
     }
@@ -291,6 +309,19 @@ bool vfio_cpr_container_match(VFIOLegacyContainer *container, VFIOGroup *group,
      */
     cpr_delete_fd("vfio_container_for_group", group->groupid);
     close(fd);
+    /*
+     * 在以下使用cpr_save_fd():
+     *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+     *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+     *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+     *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+     *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+     *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+     *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+     *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+     */
     cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
     return true;
 }
diff --git a/hw/vfio/cpr.c b/hw/vfio/cpr.c
index ffa4f8e09..6b954151e 100644
--- a/hw/vfio/cpr.c
+++ b/hw/vfio/cpr.c
@@ -15,6 +15,28 @@
 #include "qapi/error.h"
 #include "system/runstate.h"
 
+/*
+ * 意思是:
+ *
+ * - 只关心 MIG_EVENT_SETUP
+ * - 如果 migration mode 是 cpr-reboot
+ * - 并且 VM 没有处于 suspended runstate
+ * - 就拒绝迁移, 报错: VFIO device only supports cpr-reboot for runstate suspended
+ *
+ * 为什么需要这个限制: cpr-reboot 是停掉 QEMU, 保存状态, 之后重新启动 QEMU
+ * -incoming 恢复. QAPI 文档里也写了 VFIO 设备支持该模式的前提是用户先把 guest
+ * 放到 suspended runstate, 比如通过 guest agent 执行 guest-suspend-ram.
+ *
+ * 所以它不是做 VFIO 状态保存/恢复的主体逻辑, 只是一个准入检查: 有 VFIO 设备时,
+ * cpr-reboot 不能在 guest 还正常运行时直接开始, 必须先 suspend.
+ *
+ *
+ * 在以下使用vfio_cpr_reboot_notifier():
+ *   - hw/vfio/cpr-iommufd.c|183| <<vfio_iommufd_cpr_register_container>> migration_add_notifier_mode(&bcontainer->cpr_reboot_notifier,
+ *                                  vfio_cpr_reboot_notifier, MIG_MODE_CPR_REBOOT);
+ *   - hw/vfio/cpr-legacy.c|175| <<vfio_legacy_cpr_register_container>> migration_add_notifier_mode(&bcontainer->cpr_reboot_notifier,
+ *                                  vfio_cpr_reboot_notifier, MIG_MODE_CPR_REBOOT);
+ */
 int vfio_cpr_reboot_notifier(NotifierWithReturn *notifier,
                              MigrationEvent *e, Error **errp)
 {
@@ -32,13 +54,42 @@ int vfio_cpr_reboot_notifier(NotifierWithReturn *notifier,
 #define STRDUP_VECTOR_FD_NAME(vdev, name)   \
     g_strdup_printf("%s_%s", (vdev)->vbasedev.name, (name))
 
+/*
+ * 在以下使用vfio_cpr_save_vector_fd():
+ *   - hw/vfio/pci.c|79| <<vfio_notifier_init>> vfio_cpr_save_vector_fd(vdev, name, nr, fd);
+ */
 void vfio_cpr_save_vector_fd(VFIOPCIDevice *vdev, const char *name, int nr,
                              int fd)
 {
     g_autofree char *fdname = STRDUP_VECTOR_FD_NAME(vdev, name);
+    /*
+     * 在以下使用cpr_save_fd():
+     *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+     *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+     *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+     *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+     *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+     *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+     *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+     *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+     *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+     */
     cpr_save_fd(fdname, nr, fd);
 }
 
+/*
+ * 在以下使用vfio_cpr_load_vector_fd():
+ *   - hw/vfio/cpr.c|70| <<vfio_cpr_claim_vectors>> fd = vfio_cpr_load_vector_fd(vdev, "interrupt", i);
+ *   - hw/vfio/cpr.c|76| <<vfio_cpr_claim_vectors>> if (vfio_cpr_load_vector_fd(vdev, "kvm_interrupt", i) >= 0) {
+ *   - hw/vfio/pci.c|66| <<vfio_notifier_init>> fd = vfio_cpr_load_vector_fd(vdev, name, nr);
+ *
+ * 不是"所有 VFIO eventfd 都 preserve". 更准确地说:
+ *
+ * vfio_cpr_save_vector_fd() 说明 VFIO PCI 里通过 vfio_notifier_init()
+ * 创建/复用的 EventNotifier fd 会被放进 CPR fd 表, 从而在 cpr-transfer /
+ * cpr-exec 新 QEMU 里复用.
+ */
 int vfio_cpr_load_vector_fd(VFIOPCIDevice *vdev, const char *name, int nr)
 {
     g_autofree char *fdname = STRDUP_VECTOR_FD_NAME(vdev, name);
@@ -51,6 +102,11 @@ void vfio_cpr_delete_vector_fd(VFIOPCIDevice *vdev, const char *name, int nr)
     cpr_delete_fd(fdname, nr);
 }
 
+/*
+ * 在以下使用vfio_cpr_claim_vectors():
+ *   - hw/vfio/cpr.c|124| <<vfio_cpr_pci_post_load>> vfio_cpr_claim_vectors(vdev, nr_vectors, true);
+ *   - hw/vfio/cpr.c|128| <<vfio_cpr_pci_post_load>> vfio_cpr_claim_vectors(vdev, nr_vectors, false);
+ */
 static void vfio_cpr_claim_vectors(VFIOPCIDevice *vdev, int nr_vectors,
                                    bool msix)
 {
@@ -67,6 +123,18 @@ static void vfio_cpr_claim_vectors(VFIOPCIDevice *vdev, int nr_vectors,
     for (i = 0; i < nr_vectors; i++) {
         VFIOMSIVector *vector = &vdev->msi_vectors[i];
 
+        /*
+	 * 在以下使用vfio_cpr_load_vector_fd():
+         *   - hw/vfio/cpr.c|70| <<vfio_cpr_claim_vectors>> fd = vfio_cpr_load_vector_fd(vdev, "interrupt", i);
+         *   - hw/vfio/cpr.c|76| <<vfio_cpr_claim_vectors>> if (vfio_cpr_load_vector_fd(vdev, "kvm_interrupt", i) >= 0) {
+         *   - hw/vfio/pci.c|66| <<vfio_notifier_init>> fd = vfio_cpr_load_vector_fd(vdev, name, nr);
+         *
+         * 不是"所有 VFIO eventfd 都 preserve". 更准确地说:
+         *
+         * vfio_cpr_save_vector_fd() 说明 VFIO PCI 里通过 vfio_notifier_init()
+         * 创建/复用的 EventNotifier fd 会被放进 CPR fd 表, 从而在 cpr-transfer /
+         * cpr-exec 新 QEMU 里复用.
+	 */
         fd = vfio_cpr_load_vector_fd(vdev, "interrupt", i);
         if (fd >= 0) {
             vfio_pci_vector_init(vdev, i);
@@ -280,6 +348,10 @@ static int vfio_cpr_pci_notifier(NotifierWithReturn *notifier,
     return 0;
 }
 
+/*
+ * 在以下使用vfio_cpr_pci_register_device():
+ *   - hw/vfio/pci.c|3706| <<vfio_pci_realize>> vfio_cpr_pci_register_device(vdev);
+ */
 void vfio_cpr_pci_register_device(VFIOPCIDevice *vdev)
 {
     migration_add_notifier_modes(&vdev->cpr.transfer_notifier,
diff --git a/hw/vfio/migration.c b/hw/vfio/migration.c
index 73c49d8c2..9928dbeb3 100644
--- a/hw/vfio/migration.c
+++ b/hw/vfio/migration.c
@@ -34,11 +34,22 @@
 #include "hw/core/hw-error.h"
 #include "vfio-migration-internal.h"
 
+/*
+ * VFIO_MIG_DEFAULT_DATA_BUFFER_SIZE 是 VFIO 设备迁移时从 kernel VFIO migration
+ * data fd 读取设备状态的默认缓冲区大小.
+ *
+ * 所以它不是 VFIO 设备状态总大小, 也不是 guest RAM buffer. 它只是 QEMU
+ * 每次搬运 VFIO device migration data 的 chunk size.
+ */
 /*
  * This is an arbitrary size based on migration of mlx5 devices, where typically
  * total device migration size is on the order of 100s of MB. Testing with
  * larger values, e.g. 128MB and 1GB, did not show a performance improvement.
  */
+/*
+ * VFIO_MIG_DEFAULT_DATA_BUFFER_SIZE = 每次搬运多大 chunk, 默认 1 MiB
+ * VFIO_MIG_STOP_COPY_SIZE          = 不知道真实剩余量时的保守估计, 默认 100 GiB
+ */
 #define VFIO_MIG_DEFAULT_DATA_BUFFER_SIZE (1 * MiB)
 
 /*
@@ -426,11 +437,34 @@ static int vfio_query_precopy_size(VFIOMigration *migration)
     return ret;
 }
 
+/*
+ * 在以下使用vfio_save_block():
+ *   - hw/vfio/migration.c|743| <<vfio_save_iterate>> data_size = vfio_save_block(f, migration);
+ *   - hw/vfio/migration.c|783| <<vfio_save_complete_precopy>> data_size = vfio_save_block(f, vbasedev->migration);
+ */
 /* Returns the size of saved data on success and -errno on error */
 static ssize_t vfio_save_block(QEMUFile *f, VFIOMigration *migration)
 {
     ssize_t data_size;
 
+    /*
+     * data_fd 里的 data 对 QEMU 来说是 opaque device migration stream. QEMU
+     * 不解析它, 只负责:
+     *
+     * read(data_fd) -> 写进 migration stream -> 目标端 write(data_fd)
+     *
+     * VFIO uAPI 也明确要求 userspace 把这个 stream 当 opaque,
+     * 保持字节顺序即可; 不要求保持原来的 read/write 分段.
+     *
+     * 概念上，这些状态包括这个 VF/vHCA 在 NIC firmware 里的运行状态, 比如:
+     *
+     * - VF 的硬件上下文
+     * - RX/TX queue 相关状态
+     * - CQ/EQ/interrupt delivery 相关的设备侧状态
+     * - flow steering/filtering 相关状态
+     * - firmware 管理的对象、进度、计数器或 pending 状态
+     * - precopy 阶段跟踪到的增量设备状态
+     */
     data_size = read(migration->data_fd, migration->data_buffer,
                      migration->data_buffer_size);
     if (data_size < 0) {
@@ -723,6 +757,44 @@ static bool vfio_is_active_iterate(void *opaque)
     return vfio_device_state_is_precopy(vbasedev);
 }
 
+/*
+ * 959 static const SaveVMHandlers savevm_vfio_handlers = {
+ * 960     .save_prepare = vfio_save_prepare,
+ * 961     .save_setup = vfio_save_setup,
+ * 962     .save_cleanup = vfio_save_cleanup,
+ * 963     .save_query_pending = vfio_state_pending,
+ * 964     .is_active_iterate = vfio_is_active_iterate,
+ * 965     .save_live_iterate = vfio_save_iterate,
+ * 966     .save_complete = vfio_save_complete_precopy,
+ * 967     .save_state = vfio_save_state,
+ * 968     .load_setup = vfio_load_setup,
+ * 969     .load_cleanup = vfio_load_cleanup,
+ * 970     .load_state = vfio_load_state,
+ * 971     //
+ * 972     // Multifd support
+ * 973     //
+ * 974     .load_state_buffer = vfio_multifd_load_state_buffer,
+ * 975     .switchover_start = vfio_switchover_start,
+ * 976     .save_complete_precopy_thread = vfio_multifd_save_complete_precopy_thread,
+ * 977 };
+ *
+ * 1. 从 VFIO migration data_fd 读一块设备状态数据
+ *    通过 vfio_save_block(), 每次最多 migration->data_buffer_size, 默认 1 MiB.
+ * 2. 把这块数据写到 migration stream
+ *    vfio_save_block() 会写:
+ *    - VFIO_MIG_FLAG_DEV_DATA_STATE
+ *    - data_size
+ *    - 实际数据 buffer
+ * 3. 更新 pending data 估计
+ *    vfio_update_estimated_pending_data() 会根据这次读走了多少, 更新:
+ *    - precopy_init_size
+ *    - precopy_dirty_size
+ * 4. 写一个阶段结束标记
+ *    如果不需要继续发送 init data flag, 就写:
+ *    VFIO_MIG_FLAG_END_OF_STATE
+ * 5. 返回这轮 VFIO precopy 是否完成
+ *    return !precopy_init_size && !precopy_dirty_size;
+ */
 /*
  * Note about migration rate limiting: VFIO migration buffer size is currently
  * limited to 1MB, so there is no need to check if migration rate exceeded (as
@@ -740,6 +812,11 @@ static int vfio_save_iterate(QEMUFile *f, void *opaque)
         migration->event_save_iterate_started = true;
     }
 
+    /*
+     * 在以下使用vfio_save_block():
+     *   - hw/vfio/migration.c|743| <<vfio_save_iterate>> data_size = vfio_save_block(f, migration);
+     *   - hw/vfio/migration.c|783| <<vfio_save_complete_precopy>> data_size = vfio_save_block(f, vbasedev->migration);
+     */
     data_size = vfio_save_block(f, migration);
     if (data_size < 0) {
         return data_size;
@@ -780,6 +857,11 @@ static int vfio_save_complete_precopy(QEMUFile *f, void *opaque)
     }
 
     do {
+        /*
+	 * 在以下使用vfio_save_block():
+         *   - hw/vfio/migration.c|743| <<vfio_save_iterate>> data_size = vfio_save_block(f, migration);
+         *   - hw/vfio/migration.c|783| <<vfio_save_complete_precopy>> data_size = vfio_save_block(f, vbasedev->migration);
+	 */
         data_size = vfio_save_block(f, vbasedev->migration);
         if (data_size < 0) {
             return data_size;
@@ -1140,6 +1222,10 @@ static bool vfio_dma_logging_supported(VFIODevice *vbasedev)
     return !ioctl(vbasedev->fd, VFIO_DEVICE_FEATURE, feature);
 }
 
+/*
+ * 在以下使用vfio_migration_init():
+ *   - hw/vfio/migration.c|1391| <<vfio_migration_realize>> if (!vfio_migration_init(vbasedev, &err)) {
+ */
 static bool vfio_migration_init(VFIODevice *vbasedev, Error **errp)
 {
     int ret;
@@ -1377,6 +1463,10 @@ static bool vfio_dirty_tracking_exceed_limit(VFIODevice *vbasedev)
  * Currently only return false when adding blocker fails which will
  * de-register vfio device.
  */
+/*
+ * 在以下使用vfio_migration_realize():
+ *   - hw/vfio/pci.c|3698| <<vfio_pci_realize>> if (!vfio_migration_realize(vbasedev, errp)) {
+ */
 bool vfio_migration_realize(VFIODevice *vbasedev, Error **errp)
 {
     Error *err = NULL;
diff --git a/hw/vfio/pci.c b/hw/vfio/pci.c
index 428ab2f06..c53e73df1 100644
--- a/hw/vfio/pci.c
+++ b/hw/vfio/pci.c
@@ -63,6 +63,18 @@ static bool vfio_notifier_init(VFIOPCIDevice *vdev, EventNotifier *e,
 {
     int fd, ret;
 
+    /*
+     * 在以下使用vfio_cpr_load_vector_fd():
+     *   - hw/vfio/cpr.c|70| <<vfio_cpr_claim_vectors>> fd = vfio_cpr_load_vector_fd(vdev, "interrupt", i);
+     *   - hw/vfio/cpr.c|76| <<vfio_cpr_claim_vectors>> if (vfio_cpr_load_vector_fd(vdev, "kvm_interrupt", i) >= 0) {
+     *   - hw/vfio/pci.c|66| <<vfio_notifier_init>> fd = vfio_cpr_load_vector_fd(vdev, name, nr);
+     *
+     * 不是"所有 VFIO eventfd 都 preserve". 更准确地说:
+     *
+     * vfio_cpr_save_vector_fd() 说明 VFIO PCI 里通过 vfio_notifier_init()
+     * 创建/复用的 EventNotifier fd 会被放进 CPR fd 表, 从而在 cpr-transfer /
+     * cpr-exec 新 QEMU 里复用.
+     */
     fd = vfio_cpr_load_vector_fd(vdev, name, nr);
     if (fd >= 0) {
         event_notifier_init_fd(e, fd);
diff --git a/include/hw/core/boards.h b/include/hw/core/boards.h
index 29c68931d..c272e1609 100644
--- a/include/hw/core/boards.h
+++ b/include/hw/core/boards.h
@@ -322,6 +322,26 @@ struct MachineClass {
     SMPCompatProps smp_props;
     const char *default_ram_id;
 
+    /*
+     * 在以下设置MachineClass->get_hotplug_handler:
+     *   - hw/arm/virt.c|4191| <<virt_machine_class_init>> mc->get_hotplug_handler = virt_machine_get_hotplug_handler;
+     *   - hw/i386/microvm.c|670| <<microvm_class_init>> mc->get_hotplug_handler = microvm_get_hotplug_handler;
+     *   - hw/i386/pc.c|1700| <<pc_machine_class_init>> mc->get_hotplug_handler = pc_get_hotplug_handler;
+     *   - hw/loongarch/virt.c|1480| <<virt_class_init>> mc->get_hotplug_handler = virt_get_hotplug_handler;
+     *   - hw/ppc/e500plat.c|78| <<e500plat_machine_class_init>> mc->get_hotplug_handler = e500plat_machine_get_hotpug_handler;
+     *   - hw/ppc/spapr.c|4641| <<spapr_machine_class_init>> mc->get_hotplug_handler = spapr_get_hotplug_handler;
+     *   - hw/riscv/virt.c|1737| <<virt_machine_class_init>> mc->get_hotplug_handler = virt_machine_get_hotplug_handler;
+     *   - hw/s390x/s390-virtio-ccw.c|826| <<ccw_machine_class_init>> mc->get_hotplug_handler = s390_get_hotplug_handler;
+     * 在以下使用MachineClass->get_hotplug_handler:
+     *   - hw/arm/virt.c|4190| <<virt_machine_class_init>> assert(!mc->get_hotplug_handler);
+     *   - hw/core/qdev-hotplug.c|26| <<qdev_get_machine_hotplug_handler>> if (mc->get_hotplug_handler) {
+     *   - hw/core/qdev-hotplug.c|27| <<qdev_get_machine_hotplug_handler>> return mc->get_hotplug_handler(machine, dev);
+     *   - hw/i386/pc.c|1699| <<pc_machine_class_init>> assert(!mc->get_hotplug_handler);
+     *   - hw/ppc/e500plat.c|77| <<e500plat_machine_class_init>> assert(!mc->get_hotplug_handler);
+     *   - hw/ppc/spapr.c|4640| <<spapr_machine_class_init>> assert(!mc->get_hotplug_handler);
+     *   - hw/riscv/virt.c|1736| <<virt_machine_class_init>> assert(!mc->get_hotplug_handler);
+     *   - hw/s390x/s390-virtio-ccw.c|825| <<ccw_machine_class_init>> assert(!mc->get_hotplug_handler);
+     */
     HotplugHandler *(*get_hotplug_handler)(MachineState *machine,
                                            DeviceState *dev);
     bool (*hotplug_allowed)(MachineState *state, DeviceState *dev,
diff --git a/include/hw/pci/pci_bus.h b/include/hw/pci/pci_bus.h
index c73844678..8b9f750d4 100644
--- a/include/hw/pci/pci_bus.h
+++ b/include/hw/pci/pci_bus.h
@@ -30,6 +30,28 @@ enum PCIBusFlags {
 
 #define PCI_NO_PASID UINT32_MAX
 
+/*
+ * qdev framework view:
+ * BusState.children
+ *
+ * PCI protocol view:
+ * PCIBus.devices[devfn]
+ *
+ * 为什么不只用一个?
+ *
+ * 因为 qbus.children 是链表, 只知道 “这条 bus 上有哪些 qdev child”. 但 PCI
+ * 经常需要:
+ *
+ * bus->devices[devfn]
+ *
+ * O(1) 找到 00:1f.2 这种设备. 如果只用 qbus.children, 每次 PCI config access
+ * 都要遍历链表, 不合适.
+ *
+ * 所以可以理解成:
+ * qbus.children 管拓扑和生命周期, PCIBus.devices[] 管 PCI BDF/devfn 快速查找.
+ * 它们应当保持一致, 但不是同一个层次的抽象.
+ */
+
 struct PCIBus {
     BusState qbus;
     enum PCIBusFlags flags;
diff --git a/include/hw/pci/pci_device.h b/include/hw/pci/pci_device.h
index 5cac6e168..5c8fdccda 100644
--- a/include/hw/pci/pci_device.h
+++ b/include/hw/pci/pci_device.h
@@ -59,6 +59,25 @@ typedef struct PCIReqIDCache PCIReqIDCache;
 
 struct PCIDevice {
     DeviceState qdev;
+    /*
+     * 在以下使用PCIDevice->partially_hotplugged:
+     *   - hw/acpi/pcihp.c|207| <<acpi_pcihp_eject_slot>> if (dev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3759| <<failover_unplug_primary>> pci_dev->partially_hotplugged = true;
+     *   - hw/net/virtio-net.c|3779| <<failover_replug_primary>> if (!pdev->partially_hotplugged) {
+     *   - hw/net/virtio-net.c|3797| <<failover_replug_primary>> pdev->partially_hotplugged = false;
+     *   - hw/pci/pci_host.c|72| <<is_pci_dev_ejected>> return pci_dev && pci_dev->partially_hotplugged &&
+     *   - hw/pci/pcie.c|584| <<pcie_unplug_device>> if (dev->partially_hotplugged) {
+     *
+     * 不是"普通热插拔了一半"的通用状态, 它主要是给 virtio-net failover
+     * 用的特殊标志.
+     * 这个 PCI 设备已经被 guest 通过 hot-unplug 流程"看起来拔掉了", 但 QEMU
+     * 还没有真正把对象删掉, 因为后面可能还要把它重新插回去.
+     * virtio-net failover 做迁移时, 会把 primary 网卡先从 guest 里隐藏/拔掉,
+     * 让 guest 使用 standby virtio-net. 这里不能真的 object_unparent() 删除
+     * primary 设备, 因为如果迁移失败, 还要 replug 回来.
+     * 一句话总结: partially_hotplugged 表示 guest 侧已经完成热拔, 但 QEMU
+     * 故意保留设备对象, 等待 failover 流程决定是彻底消失还是重新插回.
+     */
     bool partially_hotplugged;
     bool enabled;
 
diff --git a/include/qemu/rcu.h b/include/qemu/rcu.h
index 020dbe4d8..af2ac5779 100644
--- a/include/qemu/rcu.h
+++ b/include/qemu/rcu.h
@@ -178,6 +178,27 @@ static inline void rcu_read_auto_unlock(RCUReadAuto *r)
 
 G_DEFINE_AUTOPTR_CLEANUP_FUNC(RCUReadAuto, rcu_read_auto_unlock)
 
+/*
+ * WITH_RCU_READ_LOCK_GUARD()核心技巧是用 for 循环制造一个作用域:
+ *
+ * 1. 初始化时调用 rcu_read_auto_lock(), 里面执行 rcu_read_lock()
+ * 2. 循环体执行你的代码
+ * 3. 离开循环时调用 rcu_read_auto_unlock(), 里面执行 rcu_read_unlock()
+ *
+ * QTAILQ_FOREACH_RCU 本身不是"加锁", 也不是完整的 RCU 机制; 它主要是 用
+ * RCU/atomic 读法遍历链表指针.
+ * 也就是说, 它和普通 QTAILQ_FOREACH 的区别是读链表指针时用:
+ * qatomic_rcu_read(...)
+ *
+ * qatomic_rcu_read() 的作用主要是:
+ * - 用 atomic load 读指针;
+ * - 带合适的内存顺序/屏障语义;
+ * - 保证 reader 看到的节点内容和 writer 发布节点时的初始化顺序一致.
+ *
+ * 两者分工是:
+ * - WITH_RCU_READ_LOCK_GUARD(): 告诉 RCU 系统"我正在读, 旧节点别 free"
+ * - QTAILQ_FOREACH_RCU(): 用 RCU 安全的 atomic load 读链表 next 指针
+ */
 #define WITH_RCU_READ_LOCK_GUARD() \
     WITH_RCU_READ_LOCK_GUARD_(glue(_rcu_read_auto, __COUNTER__))
 
diff --git a/io/channel.c b/io/channel.c
index 620256f31..f8852f9dc 100644
--- a/io/channel.c
+++ b/io/channel.c
@@ -646,6 +646,32 @@ int coroutine_mixed_fn qio_channel_pread_all(QIOChannel *ioc,
     return qio_channel_preadv_all(ioc, &iov, 1, offset, errp);
 }
 
+/*
+ * 在以下使用qio_channel_shutdown():
+ *   - backends/tpm/tpm_emulator.c|206| <<tpm_emulator_unix_tx_bufs>> if (qio_channel_shutdown(tpm_emu->data_ioc, QIO_CHANNEL_SHUTDOWN_BOTH,
+ *   - block/nbd.c|175| <<nbd_channel_error_locked>> qio_channel_shutdown(s->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - block/nbd.c|233| <<nbd_teardown_connection>> qio_channel_shutdown(s->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - block/nbd.c|1503| <<nbd_yank>> qio_channel_shutdown(s->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - blockdev-nbd.c|121| <<nbd_server_free>> qio_channel_shutdown(QIO_CHANNEL(conn->cioc), QIO_CHANNEL_SHUTDOWN_BOTH,
+ *   - chardev/char-socket.c|352| <<char_socket_yank_iochannel>> qio_channel_shutdown(ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - hw/remote/remote-obj.c|162| <<remote_object_finalize>> qio_channel_shutdown(o->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - hw/vfio-user/proxy.c|50| <<vfio_user_shutdown>> qio_channel_shutdown(proxy->ioc, QIO_CHANNEL_SHUTDOWN_READ, NULL);
+ *   - io/channel-tls.c|507| <<qio_channel_tls_shutdown>> return qio_channel_shutdown(tioc->master, how, errp);
+ *   - io/channel-websock.c|705| <<qio_channel_websock_write_close>> qio_channel_shutdown(ioc->master, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - io/channel-websock.c|877| <<qio_channel_websock_decode_payload>> qio_channel_shutdown(ioc->master, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - io/channel-websock.c|1234| <<qio_channel_websock_shutdown>> return qio_channel_shutdown(tioc->master, how, errp);
+ *   - migration/cpr.c|298| <<cpr_state_save>> qio_channel_shutdown(qemu_file_get_ioc(f), QIO_CHANNEL_SHUTDOWN_WRITE,
+ *   - migration/multifd.c|469| <<migration_ioc_shutdown_gracefully>> qio_channel_shutdown(ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - migration/multifd.c|1107| <<multifd_recv_terminate_threads>> qio_channel_shutdown(p->c, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - migration/qemu-file.c|103| <<qemu_file_shutdown>> if (qio_channel_shutdown(f->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, &err) < 0) {
+ *   - migration/yank_functions.c|25| <<migration_yank_iochannel>> qio_channel_shutdown(ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - nbd/client-connection.c|267| <<nbd_client_connection_release>> qio_channel_shutdown(QIO_CHANNEL(conn->sioc),
+ *   - nbd/client.c|1265| <<nbd_receive_export_list>> qio_channel_shutdown(ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - nbd/server.c|1631| <<client_close>> qio_channel_shutdown(client->ioc, QIO_CHANNEL_SHUTDOWN_BOTH,
+ *   - nbd/server.c|3227| <<nbd_handshake_timer_cb>> qio_channel_shutdown(ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - util/vhost-user-server.c|277| <<kick_handler>> qio_channel_shutdown(server->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ *   - util/vhost-user-server.c|404| <<vhost_user_server_stop>> qio_channel_shutdown(server->ioc, QIO_CHANNEL_SHUTDOWN_BOTH, NULL);
+ */
 int qio_channel_shutdown(QIOChannel *ioc,
                          QIOChannelShutdown how,
                          Error **errp)
diff --git a/migration/block-active.c b/migration/block-active.c
index 40e986aad..9cdc935a8 100644
--- a/migration/block-active.c
+++ b/migration/block-active.c
@@ -12,6 +12,15 @@
 #include "qemu/error-report.h"
 #include "trace.h"
 
+/*
+ * 在以下使用migration_block_activate():
+ *   - migration/colo.c|849| <<colo_process_incoming_thread>> migration_block_activate(&local_err);
+ *   - migration/cpr-exec.c|169| <<cpr_exec_cb>> if (!migration_block_activate(&err)) {
+ *   - migration/migration.c|711| <<process_incoming_migration_bh>> if (migration_block_activate(NULL)) {
+ *   - migration/migration.c|3482| <<migration_iteration_finish>> if (!migration_block_activate(&local_err)) {
+ *   - migration/savevm.c|2230| <<loadvm_postcopy_handle_run_bh>> bool success = migration_block_activate(NULL);
+ *   - monitor/qmp-cmds.c|111| <<qmp_cont>> if (!migration_block_activate(&local_err)) {
+ */
 bool migration_block_activate(Error **errp)
 {
     ERRP_GUARD();
@@ -29,6 +38,11 @@ bool migration_block_activate(Error **errp)
     return true;
 }
 
+/*
+ * 在以下使用migration_block_inactivate():
+ *   - migration/migration.c|2845| <<migration_switchover_start>> if (!migration_block_inactivate()) {
+ *   - migration/savevm.c|3414| <<qmp_xen_save_devices_state>> migration_block_inactivate();
+ */
 bool migration_block_inactivate(void)
 {
     int ret;
diff --git a/migration/block-dirty-bitmap.c b/migration/block-dirty-bitmap.c
index cba54e25c..7da841cb7 100644
--- a/migration/block-dirty-bitmap.c
+++ b/migration/block-dirty-bitmap.c
@@ -1250,6 +1250,51 @@ static bool dirty_bitmap_has_postcopy(void *opaque)
     return true;
 }
 
+/*
+ * 是 block 层的 named dirty bitmap，也就是 QMP 可见的 dirty bitmap:
+ *
+ * BlockDriverState
+ * -> dirty bitmap "backup0"
+ * -> dirty bitmap "incremental-2026..."
+ *
+ * 这些 bitmap 常用于 增量备份 / incremental backup / NBD dirty bitmap export.
+ * 它记录"磁盘哪些区域从某个时间点后被写脏了".
+ *
+ * 目的:
+ *
+ * VM live migration 后, 目标 QEMU 还能继续保留这些 block dirty bitmap,
+ * 这样迁移不会打断后续增量备份链.
+ *
+ * 举例:
+ *
+ * 源端 QEMU:
+ *   disk0 有 dirty bitmap "backup0"
+ *   bitmap 标记了哪些扇区从上次备份后变脏
+ *
+ * live migrate 到目标 QEMU:
+ *   如果不迁移 bitmap:
+ *     目标端不知道哪些块已经脏了
+ *     增量备份状态丢失
+ *     后续只能全量备份, 或者 bitmap 语义错误
+ *
+ *   有 savevm_dirty_bitmap_handlers:
+ *     源端把 bitmap 名字, 粒度、enabled/persistent 标志, bits 数据发过去
+ *     目标端创建同名 bitmap
+ *     反序列化 dirty bits
+ *     VM 启动前恢复 bitmap 状态
+ *
+ *
+ * QEMU 运行时的 block dirty bitmap 主要存在内存里.
+ *
+ * 结构大概是:
+ *
+ * BlockDriverState
+ * -> BdrvDirtyBitmap
+ *    -> HBitmap   // 内存里的位图
+ *
+ * 每个 bit 表示一段磁盘范围是否 dirty, 粒度由 bitmap granularity 决定, 比如 64
+ * KiB 一个 bit.
+ */
 static SaveVMHandlers savevm_dirty_bitmap_handlers = {
     .save_setup = dirty_bitmap_save_setup,
     .save_complete = dirty_bitmap_save_complete,
diff --git a/migration/channel-block.c b/migration/channel-block.c
index 9cf383d4b..0fb1f79b3 100644
--- a/migration/channel-block.c
+++ b/migration/channel-block.c
@@ -24,6 +24,33 @@
 #include "block/block.h"
 #include "trace.h"
 
+/*
+ * 用 channel-block 是为了让 savevm 复用 QEMUFile 序列化逻辑, 同时让最终写入走
+ * block driver 的 vmstate 接口, 而不是绕过 qcow2/raw/rbd.
+ * 等格式层直接写普通文件.
+ *
+ *
+ * 迁移框架 savevm/loadvm
+ *           |
+ *           v
+ *        QEMUFile
+ *           |
+ *           v
+ *        QIOChannel   <--- 抽象 I/O 接口
+ *           |
+ *           +-- socket channel
+ *           +-- fd channel
+ *           +-- file channel
+ *           +-- block channel
+ *
+ * migration/channel.c 管的是: 迁移时要用哪个 QIOChannel, 怎么连上对端.
+ */
+
+/*
+ * 在以下使用qio_channel_block_new():
+ *   - migration/savevm.c|172| <<qemu_fopen_bdrv>> return qemu_file_new_output(QIO_CHANNEL(qio_channel_block_new(bs)));
+ *   - migration/savevm.c|174| <<qemu_fopen_bdrv>> return qemu_file_new_input(QIO_CHANNEL(qio_channel_block_new(bs)));
+ */
 QIOChannelBlock *
 qio_channel_block_new(BlockDriverState *bs)
 {
diff --git a/migration/channel.c b/migration/channel.c
index 1e2935f92..08700a11c 100644
--- a/migration/channel.c
+++ b/migration/channel.c
@@ -34,6 +34,11 @@
 #include "trace.h"
 #include "yank_functions.h"
 
+/*
+ * 在以下使用migration_connect_outgoing():
+ *   - migration/migration.c|2085| <<migration_connect_outgoing_cb>> migration_connect_outgoing(s, opaque, &local_err);
+ *   - migration/migration.c|2148| <<qmp_migrate>> migration_connect_outgoing(s, main_ch->addr, &local_err);
+ */
 void migration_connect_outgoing(MigrationState *s, MigrationAddress *addr,
                                 Error **errp)
 {
@@ -67,12 +72,25 @@ void migration_connect_outgoing(MigrationState *s, MigrationAddress *addr,
     }
 
     if (ioc) {
+        /*
+	 * 在以下使用migration_channel_connect_outgoing():
+         *   - migration/channel.c|70| <<migration_connect_outgoing>> migration_channel_connect_outgoing(s, ioc);
+         *   - migration/socket.c|76| <<socket_outgoing_migration>> migration_channel_connect_outgoing(data->s, sioc);
+	 *   - migration/tls.c|117| <<migration_tls_outgoing_handshake>> migration_channel_connect_outgoing(s, ioc);
+         *
+         * migration_channel_connect_outgoing() 是 outgoing migration
+         * 从"连接建立完成"切到"开始发送迁移流"的桥.
+	 */
         migration_channel_connect_outgoing(s, ioc);
     }
 
     return;
 }
 
+/*
+ * 在以下使用migration_connect_incoming():
+ *   - migration/migration.c|673| <<qemu_setup_incoming_migration>> migration_connect_incoming(main_ch->addr, errp);
+ */
 void migration_connect_incoming(MigrationAddress *addr, Error **errp)
 {
     if (addr->transport == MIGRATION_ADDRESS_TYPE_SOCKET) {
@@ -237,6 +255,15 @@ out:
     }
 }
 
+/*
+ * 在以下使用migration_channel_connect_outgoing():
+ *   - migration/channel.c|70| <<migration_connect_outgoing>> migration_channel_connect_outgoing(s, ioc);
+ *   - migration/socket.c|76| <<socket_outgoing_migration>> migration_channel_connect_outgoing(data->s, sioc);
+ *   - migration/tls.c|117| <<migration_tls_outgoing_handshake>> migration_channel_connect_outgoing(s, ioc);
+ *
+ * migration_channel_connect_outgoing() 是 outgoing migration
+ * 从"连接建立完成"切到"开始发送迁移流"的桥.
+ */
 void migration_channel_connect_outgoing(MigrationState *s, QIOChannel *ioc)
 {
     trace_migration_set_outgoing_channel(ioc, object_get_typename(OBJECT(ioc)));
@@ -405,6 +432,11 @@ bool migrate_uri_parse(const char *uri, MigrationChannel **channel,
     return true;
 }
 
+/*
+ * 在以下使用migration_channel_parse_input():
+ *   - migration/migration.c|660| <<qemu_setup_incoming_migration>> if (!migration_channel_parse_input(uri, channels, &main_ch, NULL, errp)) {
+ *   - migration/migration.c|2106| <<qmp_migrate>> if (!migration_channel_parse_input(uri, channels, &main_ch, &cpr_ch,
+ */
 bool migration_channel_parse_input(const char *uri,
                                    MigrationChannelList *channels,
                                    MigrationChannel **main_channelp,
diff --git a/migration/colo.c b/migration/colo.c
index 0f30a5926..802dece9f 100644
--- a/migration/colo.c
+++ b/migration/colo.c
@@ -35,6 +35,21 @@
 #include "net/filter.h"
 #include "options.h"
 
+/*
+ * 它和普通 live migration 不同:
+ *
+ * 普通 live migration:
+ *   源 VM -> 目标 VM
+ *   迁完以后源端停，目标端继续跑
+ *   是一次性的状态搬迁
+ *
+ * COLO:
+ *   Primary 和 Secondary 长期并行运行
+ *   周期性/按需 checkpoint
+ *   不断把 Primary 状态同步到 Secondary
+ *   出故障时 failover
+ */
+
 static bool vmstate_loading;
 static Notifier packets_compare_notifier;
 
diff --git a/migration/cpr-transfer.c b/migration/cpr-transfer.c
index 9defe7bad..af9f6c42a 100644
--- a/migration/cpr-transfer.c
+++ b/migration/cpr-transfer.c
@@ -45,6 +45,16 @@ QEMUFile *cpr_transfer_output(MigrationChannel *channel, Error **errp)
     }
 }
 
+/*
+ * 在以下使用cpr_transfer_input():
+ *   - migration/cpr.c|319| <<cpr_state_load>> f = cpr_transfer_input(channel, errp);
+ *
+ *  1. 检查 CPR channel 必须是 UNIX socket 或 fd socket
+ *  2. 在这个 socket 上 listen
+ *  3. 阻塞等待 old QEMU 连接
+ *  4. accept 成 QIOChannelSocket
+ *  5. 包成 QEMUFile input 返回
+ */
 QEMUFile *cpr_transfer_input(MigrationChannel *channel, Error **errp)
 {
     MigrationAddress *addr = channel->addr;
diff --git a/migration/cpr.c b/migration/cpr.c
index bca43e9bf..944df074d 100644
--- a/migration/cpr.c
+++ b/migration/cpr.c
@@ -127,6 +127,19 @@ static int cpr_fd_post_load(void *opaque, int version_id)
     return 0;
 }
 
+/*
+ * 在以下使用cpr_save_fd():
+ *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+ *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+ *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+ *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+ *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+ *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+ *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+ *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+ *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+ *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+ */
 void cpr_save_fd(const char *name, int id, int fd)
 {
     CprFd *elem = g_new0(CprFd, 1);
@@ -176,6 +189,19 @@ void cpr_resave_fd(const char *name, int id, int fd)
     int old_fd = elem ? elem->fd : -1;
 
     if (old_fd < 0) {
+        /*
+	 * 在以下使用cpr_save_fd():
+         *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+         *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+         *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+         *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+         *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+         *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+         *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+         *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+         *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+         *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+	 */
         cpr_save_fd(name, id, fd);
     } else if (old_fd != fd) {
         error_report("internal error: cpr fd '%s' id %d value %d "
@@ -193,6 +219,19 @@ int cpr_open_fd(const char *path, int flags, const char *name, int id,
     if (fd < 0) {
         fd = qemu_open(path, flags, errp);
         if (fd >= 0) {
+            /*
+	     * 在以下使用cpr_save_fd():
+	     *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+             *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+             *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+             *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+             *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+             *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+             *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+             *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+	     */
             cpr_save_fd(name, id, fd);
         }
     }
@@ -401,6 +440,19 @@ int cpr_get_fd_param(const char *name, const char *fdname, int index,
     } else {
         fd = monitor_fd_param(monitor_cur(), fdname, errp);
         if (fd >= 0) {
+            /*
+	     * 在以下使用cpr_save_fd():
+             *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+             *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+             *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+             *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+             *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+             *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+             *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+             *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+             *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+	     */
             cpr_save_fd(name, index, fd);
         } else {
             error_prepend(errp, "Could not parse object fd %s:", fdname);
diff --git a/migration/exec.c b/migration/exec.c
index a1a7ede3b..ac2c2cc6a 100644
--- a/migration/exec.c
+++ b/migration/exec.c
@@ -27,6 +27,11 @@
 #include "trace.h"
 #include "qemu/cutils.h"
 
+/*
+ *  exec 是一种 migration transport: 把迁移流接到一个外部命令进程的
+ *  stdin/stdout 上.
+ */
+
 #ifdef WIN32
 const char *exec_get_cmd_path(void)
 {
diff --git a/migration/fd.c b/migration/fd.c
index f05f95992..0a02ab6b4 100644
--- a/migration/fd.c
+++ b/migration/fd.c
@@ -86,6 +86,10 @@ static gboolean fd_accept_incoming_migration(QIOChannel *ioc,
     return G_SOURCE_REMOVE;
 }
 
+/*
+ * 在以下使用fd_connect_incoming():
+ *   - migration/channel.c|85| <<migration_connect_incoming>> fd_connect_incoming(saddr->u.fd.str, errp);
+ */
 void fd_connect_incoming(const char *fdname, Error **errp)
 {
     QIOChannel *ioc;
diff --git a/migration/file.c b/migration/file.c
index 8ba1de200..87cdfa3e1 100644
--- a/migration/file.c
+++ b/migration/file.c
@@ -19,6 +19,18 @@
 #include "options.h"
 #include "trace.h"
 
+/*
+ * fd:
+ *   复用外部传进来的 fd
+ *   fd 必须是 socket 或 pipe
+ *   适合管理层已经建立好连接/管道的场景
+ *   
+ * file:
+ *   QEMU 自己 open 一个文件路径
+ *   fd 是普通文件
+ *   适合把 migration stream 保存到文件, 或者从文件恢复
+ */
+
 #define OFFSET_OPTION ",offset="
 
 static struct FileOutgoingArgs {
diff --git a/migration/global_state.c b/migration/global_state.c
index 91fefdf52..598fe0d59 100644
--- a/migration/global_state.c
+++ b/migration/global_state.c
@@ -20,6 +20,32 @@
 #include "migration/vmstate.h"
 #include "trace.h"
 
+/*
+ * globalstate 是迁移流里的一个很小的 VMState section, 用来传
+ * 全局运行状态, 主要是 runstate
+ *
+ * 所以例子:
+ *
+ * 源 VM 迁移前是 running
+ *   -> globalstate 保存 "running"
+ *   -> 目标端 load 完后可以 vm_start()
+ *
+ * 源 VM 迁移前是 paused
+ *   -> globalstate 保存 "paused"
+ *   -> 目标端 load 完后保持 paused
+ *
+ * 源 VM 迁移前是 suspended
+ *   -> globalstate 保存 "suspended"
+ *   -> 目标端设置 suspended 语义
+ *
+ * globalstate 的重点不是 CPU/device/RAM 内容, 那些由各设备 VMState 和 RAM
+ * migration 负责.
+ *
+ * 它只是在告诉目标端:
+ *
+ * 迁移完成后, VM 应该回到什么全局状态?
+ */
+
 typedef struct {
     uint32_t size;
 
diff --git a/migration/migration.c b/migration/migration.c
index 9df6da131..9ec332373 100644
--- a/migration/migration.c
+++ b/migration/migration.c
@@ -493,6 +493,14 @@ void migration_incoming_state_destroy(void)
     }
 
     cpr_set_incoming_mode(MIG_MODE_NONE);
+    /*
+     * 在以下使用yank_unregister_instance():
+     *   - block/nbd.c|120| <<nbd_clear_bdrvstate>> yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
+     *   - chardev/char-socket.c|1113| <<char_socket_finalize>> yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
+     *   - migration/migration.c|496| <<migration_incoming_state_destroy>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     *   - migration/migration.c|1418| <<migration_cleanup>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     *   - migration/migration.c|1877| <<qmp_migrate_incoming>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     */
     yank_unregister_instance(MIGRATION_YANK_INSTANCE);
 }
 
@@ -650,6 +658,30 @@ migration_incoming_state_setup(MigrationIncomingState *mis, Error **errp)
     return true;
 }
 
+/*
+ * qemu_setup_incoming_migration() 是 目标端 incoming migration 的启动准备函数.
+ *
+ * 它对应 source 侧的这条路径:
+ *
+ * source:
+ *   migration_channel_connect_outgoing()
+ *     -> QIOChannel 已连上
+ *     -> 包成 QEMUFile
+ *     -> 开始发送迁移流
+ *
+ * target 侧则是:
+ *
+ * target:
+ *   qemu_setup_incoming_migration()
+ *     -> 解析 incoming 地址
+ *     -> 设置 incoming 状态
+ *     -> 开始监听/接收连接
+ *
+ *
+ * 在以下使用qemu_setup_incoming_migration():
+ *   - migration/migration.c|1845| <<qmp_migrate_incoming>> qemu_setup_incoming_migration(uri, has_channels, channels, &local_err);
+ *   - migration/migration.c|1892| <<qmp_migrate_recover>> qemu_setup_incoming_migration(uri, false, NULL, errp);
+ */
 static void qemu_setup_incoming_migration(const char *uri, bool has_channels,
                                           MigrationChannelList *channels,
                                           Error **errp)
@@ -657,6 +689,11 @@ static void qemu_setup_incoming_migration(const char *uri, bool has_channels,
     g_autoptr(MigrationChannel) main_ch = NULL;
     MigrationIncomingState *mis = migration_incoming_get_current();
 
+    /*
+     * 在以下使用migration_channel_parse_input():
+     *   - migration/migration.c|660| <<qemu_setup_incoming_migration>> if (!migration_channel_parse_input(uri, channels, &main_ch, NULL, errp)) {
+     *   - migration/migration.c|2106| <<qmp_migrate>> if (!migration_channel_parse_input(uri, channels, &main_ch, &cpr_ch,
+     */
     if (!migration_channel_parse_input(uri, channels, &main_ch, NULL, errp)) {
         return;
     }
@@ -1386,6 +1423,14 @@ static void migration_cleanup(MigrationState *s)
         migration_call_notifiers(MIG_EVENT_DONE, NULL);
     }
 
+    /*
+     * 在以下使用yank_unregister_instance():
+     *   - block/nbd.c|120| <<nbd_clear_bdrvstate>> yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
+     *   - chardev/char-socket.c|1113| <<char_socket_finalize>> yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
+     *   - migration/migration.c|496| <<migration_incoming_state_destroy>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     *   - migration/migration.c|1418| <<migration_cleanup>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     *   - migration/migration.c|1877| <<qmp_migrate_incoming>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+     */
     yank_unregister_instance(MIGRATION_YANK_INSTANCE);
 }
 
@@ -1835,6 +1880,14 @@ void qmp_migrate_incoming(const char *uri, bool has_channels,
         return;
     }
 
+    /*
+     * 在以下使用yank_register_instance():
+     *   - block/nbd.c|1920| <<nbd_open>> if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
+     *   - chardev/char-socket.c|1409| <<tcp_chr_open>> if (!yank_register_instance(CHARDEV_YANK_INSTANCE(chr->label), errp)) {
+     *   - migration/migration.c|1867| <<qmp_migrate_incoming>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     *   - migration/migration.c|2097| <<migrate_prepare>> yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
+     *   - migration/savevm.c|3527| <<load_snapshot>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     */
     if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
         return;
     }
@@ -1845,6 +1898,14 @@ void qmp_migrate_incoming(const char *uri, bool has_channels,
     qemu_setup_incoming_migration(uri, has_channels, channels, &local_err);
 
     if (local_err) {
+        /*
+	 * 在以下使用yank_unregister_instance():
+         *   - block/nbd.c|120| <<nbd_clear_bdrvstate>> yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
+         *   - chardev/char-socket.c|1113| <<char_socket_finalize>> yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
+         *   - migration/migration.c|496| <<migration_incoming_state_destroy>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+         *   - migration/migration.c|1418| <<migration_cleanup>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+         *   - migration/migration.c|1877| <<qmp_migrate_incoming>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+	 */
         yank_unregister_instance(MIGRATION_YANK_INSTANCE);
         error_propagate(errp, local_err);
         return;
@@ -2065,6 +2126,14 @@ static bool migrate_prepare(MigrationState *s, bool resume, Error **errp)
         return false;
     }
 
+    /*
+     * 在以下使用yank_register_instance():
+     *   - block/nbd.c|1920| <<nbd_open>> if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
+     *   - chardev/char-socket.c|1409| <<tcp_chr_open>> if (!yank_register_instance(CHARDEV_YANK_INSTANCE(chr->label), errp)) {
+     *   - migration/migration.c|1867| <<qmp_migrate_incoming>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     *   - migration/migration.c|2097| <<migrate_prepare>> yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
+     *   - migration/savevm.c|3527| <<load_snapshot>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     */
     yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
 
     return true;
@@ -2082,6 +2151,11 @@ static gboolean migration_connect_outgoing_cb(QIOChannel *channel,
      */
     cpr_transfer_source_destroy(s);
 
+    /*
+     * 在以下使用migration_connect_outgoing():
+     *   - migration/migration.c|2085| <<migration_connect_outgoing_cb>> migration_connect_outgoing(s, opaque, &local_err);
+     *   - migration/migration.c|2148| <<qmp_migrate>> migration_connect_outgoing(s, main_ch->addr, &local_err);
+     */
     migration_connect_outgoing(s, opaque, &local_err);
 
     if (local_err) {
@@ -2103,6 +2177,11 @@ void qmp_migrate(const char *uri, bool has_channels,
     g_autoptr(MigrationChannel) main_ch = NULL;
     g_autoptr(MigrationChannel) cpr_ch = NULL;
 
+    /*
+     * 在以下使用migration_channel_parse_input():
+     *   - migration/migration.c|660| <<qemu_setup_incoming_migration>> if (!migration_channel_parse_input(uri, channels, &main_ch, NULL, errp)) {
+     *   - migration/migration.c|2106| <<qmp_migrate>> if (!migration_channel_parse_input(uri, channels, &main_ch, &cpr_ch,
+     */
     if (!migration_channel_parse_input(uri, channels, &main_ch, &cpr_ch,
                                        errp)) {
         return;
@@ -2145,6 +2224,11 @@ void qmp_migrate(const char *uri, bool has_channels,
                                    main_ch->addr);
 
     } else {
+        /*
+	 * 在以下使用migration_connect_outgoing():
+         *   - migration/migration.c|2085| <<migration_connect_outgoing_cb>> migration_connect_outgoing(s, opaque, &local_err);
+         *   - migration/migration.c|2148| <<qmp_migrate>> migration_connect_outgoing(s, main_ch->addr, &local_err);
+	 */
         migration_connect_outgoing(s, main_ch->addr, &local_err);
     }
 
diff --git a/migration/savevm.c b/migration/savevm.c
index 34dd06f9f..38e084bb5 100644
--- a/migration/savevm.c
+++ b/migration/savevm.c
@@ -166,6 +166,11 @@ static bool qemu_loadvm_thread_pool_wait(MigrationState *s,
 /***********************************************************/
 /* savevm/loadvm support */
 
+/*
+ * 在以下使用qemu_fopen_bdrv():
+ *   - migration/savevm.c|3342| <<save_snapshot>> f = qemu_fopen_bdrv(bs, 1);
+ *   - migration/savevm.c|3513| <<load_snapshot>> f = qemu_fopen_bdrv(bs_vm_state, 0);
+ */
 static QEMUFile *qemu_fopen_bdrv(BlockDriverState *bs, int is_writable)
 {
     if (is_writable) {
@@ -3519,6 +3524,14 @@ bool load_snapshot(const char *name, const char *vmstate,
     qemu_system_reset(SHUTDOWN_CAUSE_SNAPSHOT_LOAD);
     mis->from_src_file = f;
 
+    /*
+     * 在以下使用yank_register_instance():
+     *   - block/nbd.c|1920| <<nbd_open>> if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
+     *   - chardev/char-socket.c|1409| <<tcp_chr_open>> if (!yank_register_instance(CHARDEV_YANK_INSTANCE(chr->label), errp)) {
+     *   - migration/migration.c|1867| <<qmp_migrate_incoming>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     *   - migration/migration.c|2097| <<migrate_prepare>> yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
+     *   - migration/savevm.c|3527| <<load_snapshot>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+     */
     if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
         ret = -EINVAL;
         goto err_drain;
diff --git a/migration/socket.c b/migration/socket.c
index f57f1a8e7..edf8af10e 100644
--- a/migration/socket.c
+++ b/migration/socket.c
@@ -73,6 +73,15 @@ static void socket_outgoing_migration(QIOTask *task,
     }
 
     trace_migration_socket_outgoing_connected();
+    /*
+     * 在以下使用migration_channel_connect_outgoing():
+     *   - migration/channel.c|70| <<migration_connect_outgoing>> migration_channel_connect_outgoing(s, ioc);
+     *   - migration/socket.c|76| <<socket_outgoing_migration>> migration_channel_connect_outgoing(data->s, sioc);
+     *   - migration/tls.c|117| <<migration_tls_outgoing_handshake>> migration_channel_connect_outgoing(s, ioc);
+     *
+     * migration_channel_connect_outgoing() 是 outgoing migration
+     * 从"连接建立完成"切到"开始发送迁移流"的桥.
+     */
     migration_channel_connect_outgoing(data->s, sioc);
     return;
 fail:
@@ -139,6 +148,10 @@ socket_incoming_migration_end(void *opaque)
     object_unref(OBJECT(listener));
 }
 
+/*
+ * 在以下使用socket_connect_incoming():
+ *   - migration/channel.c|101| <<migration_connect_incoming>> socket_connect_incoming(saddr, errp);
+ */
 void socket_connect_incoming(SocketAddress *saddr, Error **errp)
 {
     QIONetListener *listener = qio_net_listener_new();
diff --git a/migration/tls.c b/migration/tls.c
index 89ba52de0..90d5dbd20 100644
--- a/migration/tls.c
+++ b/migration/tls.c
@@ -114,9 +114,24 @@ static void migration_tls_outgoing_handshake(QIOTask *task,
     }
 
     trace_migration_tls_outgoing_handshake_complete();
+    /*
+     * 在以下使用migration_channel_connect_outgoing():
+     *   - migration/channel.c|70| <<migration_connect_outgoing>> migration_channel_connect_outgoing(s, ioc);
+     *   - migration/socket.c|76| <<socket_outgoing_migration>> migration_channel_connect_outgoing(data->s, sioc);
+     *   - migration/tls.c|117| <<migration_tls_outgoing_handshake>> migration_channel_connect_outgoing(s, ioc);
+     *
+     * migration_channel_connect_outgoing() 是 outgoing migration
+     * 从"连接建立完成"切到"开始发送迁移流"的桥.
+     */
     migration_channel_connect_outgoing(s, ioc);
 }
 
+/*
+ * 在以下使用migration_tls_client_create():
+ *   - migration/multifd.c|818| <<multifd_tls_channel_connect>> tioc = migration_tls_client_create(ioc, errp);
+ *   - migration/postcopy-ram.c|1967| <<postcopy_preempt_send_channel_new>> tioc = migration_tls_client_create(ioc, &local_err);
+ *   - migration/tls.c|147| <<migration_tls_channel_connect>> tioc = migration_tls_client_create(ioc, errp);
+ */
 QIOChannelTLS *migration_tls_client_create(QIOChannel *ioc,
                                            Error **errp)
 {
diff --git a/migration/vfio.c b/migration/vfio.c
index af6ae2c1e..6957e22ea 100644
--- a/migration/vfio.c
+++ b/migration/vfio.c
@@ -10,6 +10,11 @@
 #include "migration.h"
 #include "hw/vfio/vfio-migration.h"
 
+/*
+ * 在以下使用migration_populate_vfio_info():
+ *   - migration/migration.c|1234| <<fill_source_migration_info>> migration_populate_vfio_info(info);
+ *   - migration/migration.c|1243| <<fill_source_migration_info>> migration_populate_vfio_info(info);
+ */
 void migration_populate_vfio_info(MigrationInfo *info)
 {
     if (vfio_migration_active()) {
@@ -18,6 +23,10 @@ void migration_populate_vfio_info(MigrationInfo *info)
     }
 }
 
+/*
+ * 在以下使用migration_reset_vfio_bytes_transferred():
+ *   - migration/migration.c|1758| <<migrate_init>> migration_reset_vfio_bytes_transferred();
+ */
 void migration_reset_vfio_bytes_transferred(void)
 {
     vfio_migration_reset_bytes_transferred();
diff --git a/migration/yank_functions.c b/migration/yank_functions.c
index 979e60c76..74b9efb80 100644
--- a/migration/yank_functions.c
+++ b/migration/yank_functions.c
@@ -13,6 +13,11 @@
 #include "qemu/yank.h"
 #include "qemu-file.h"
 
+/*
+ * 迁移过程卡住或需要中断时, 它负责把 migration 用到的 IO channel 强制 shutdown
+ * 掉, 让阻塞的网络/IO 操作醒过来并退出.
+ */
+
 void migration_yank_iochannel(void *opaque)
 {
     QIOChannel *ioc = QIO_CHANNEL(opaque);
@@ -26,9 +31,25 @@ static bool migration_ioc_yank_supported(QIOChannel *ioc)
     return qio_channel_has_feature(ioc, QIO_CHANNEL_FEATURE_SHUTDOWN);
 }
 
+/*
+ * 在以下使用migration_ioc_register_yank():
+ *   - migration/channel.c|238| <<migration_channel_process_incoming>> migration_ioc_register_yank(ioc);
+ *   - migration/channel.c|286| <<migration_channel_connect_outgoing>> migration_ioc_register_yank(ioc);
+ *   - migration/multifd.c|846| <<multifd_channel_connect>> migration_ioc_register_yank(ioc);
+ *   - migration/postcopy-ram.c|1931| <<postcopy_preempt_send_channel_done>> migration_ioc_register_yank(ioc);
+ */
 void migration_ioc_register_yank(QIOChannel *ioc)
 {
     if (migration_ioc_yank_supported(ioc)) {
+        /*
+	 * 在以下使用yank_register_function():
+         *   - block/nbd.c|341| <<nbd_co_do_establish_connection>> yank_register_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+         *               nbd_yank, bs);
+         *   - chardev/char-socket.c|909| <<tcp_chr_new_client>> yank_register_function(CHARDEV_YANK_INSTANCE(chr->label),
+         *               char_socket_yank_iochannel, QIO_CHANNEL(sioc));
+         *   - migration/yank_functions.c|44| <<migration_ioc_register_yank>> yank_register_function(MIGRATION_YANK_INSTANCE,
+         *               migration_yank_iochannel, ioc);
+	 */
         yank_register_function(MIGRATION_YANK_INSTANCE,
                                migration_yank_iochannel,
                                ioc);
@@ -38,6 +59,21 @@ void migration_ioc_register_yank(QIOChannel *ioc)
 void migration_ioc_unregister_yank(QIOChannel *ioc)
 {
     if (migration_ioc_yank_supported(ioc)) {
+        /*
+         * 在以下使用yank_unregister_function():
+         *   - block/nbd.c|226| <<nbd_teardown_connection>> yank_unregister_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+         *                nbd_yank, s->bs);
+         *   - block/nbd.c|346| <<nbd_co_do_establish_connection>> yank_unregister_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+         *                nbd_yank, bs);
+         *   - block/nbd.c|402| <<nbd_reconnect_attempt>> yank_unregister_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+         *                nbd_yank, s->bs);
+         *   - chardev/char-socket.c|377| <<tcp_chr_free_connection>> yank_unregister_function(CHARDEV_YANK_INSTANCE(chr->label),
+         *                char_socket_yank_iochannel, QIO_CHANNEL(s->sioc));
+         *   - chardev/char-socket.c|1132| <<qemu_chr_socket_connected>> yank_unregister_function(CHARDEV_YANK_INSTANCE(chr->label),
+         *                char_socket_yank_iochannel, QIO_CHANNEL(sioc));
+         *   - migration/yank_functions.c|41| <<migration_ioc_unregister_yank>> yank_unregister_function(MIGRATION_YANK_INSTANCE,
+         *                migration_yank_iochannel, ioc);
+	 */
         yank_unregister_function(MIGRATION_YANK_INSTANCE,
                                  migration_yank_iochannel,
                                  ioc);
diff --git a/system/physmem.c b/system/physmem.c
index c21ea9291..f8d1f8966 100644
--- a/system/physmem.c
+++ b/system/physmem.c
@@ -2448,6 +2448,19 @@ static int qemu_ram_get_shared_fd(const char *name, bool *reused, Error **errp)
     }
 
     if (fd >= 0) {
+        /*
+	 * 在以下使用cpr_save_fd():
+         *   - backends/hostmem-memfd.c|57| <<memfd_backend_memory_alloc>> cpr_save_fd(name, 0, fd);
+         *   - backends/hostmem-shm.c|51| <<shm_backend_memory_alloc>> cpr_save_fd(backend_name, 0, fd);
+         *   - backends/iommufd.c|88| <<iommufd_backend_complete>> cpr_save_fd(name, 0, be->fd);
+         *   - hw/vfio/cpr-legacy.c|265| <<vfio_cpr_group_get_device_fd>> cpr_save_fd(name, id, fd);
+         *   - hw/vfio/cpr-legacy.c|294| <<vfio_cpr_container_match>> cpr_save_fd("vfio_container_for_group", group->groupid, container->fd);
+         *   - hw/vfio/cpr.c|39| <<vfio_cpr_save_vector_fd>> cpr_save_fd(fdname, nr, fd);
+         *   - migration/cpr.c|179| <<cpr_resave_fd>> cpr_save_fd(name, id, fd);
+         *   - migration/cpr.c|196| <<cpr_open_fd>> cpr_save_fd(name, id, fd);
+         *   - migration/cpr.c|404| <<cpr_get_fd_param>> cpr_save_fd(name, index, fd);
+         *   - system/physmem.c|2451| <<qemu_ram_get_shared_fd>> cpr_save_fd(name, 0, fd);
+	 */
         cpr_save_fd(name, 0, fd);
     }
     *reused = false;
diff --git a/util/yank.c b/util/yank.c
index eaac50539..cb07b6091 100644
--- a/util/yank.c
+++ b/util/yank.c
@@ -17,6 +17,17 @@
 #include "qapi/clone-visitor.h"
 #include "qemu/yank.h"
 
+/*
+ * migrate_cancel:
+ *   请 migration 自己停下来
+ *
+ * yank migration:
+ *   直接把 migration 正在用的连接拔掉, 让它报错失败
+ *
+ * 如果 migration 状态机还能正常跑, 优先用 migrate_cancel. 只有怀疑它卡在
+ * I/O, 普通取消无法及时返回时, 才用 yank migration.
+ */
+
 struct YankFuncAndParam {
     YankFn *func;
     void *opaque;
@@ -40,6 +51,14 @@ typedef struct YankInstanceEntry YankInstanceEntry;
  */
 static QemuMutex yank_lock;
 
+/*
+ * 在以下使用yank_instance_list():
+ *   - util/yank.c|43| <<QLIST_HEAD>> static QLIST_HEAD(, YankInstanceEntry) yank_instance_list
+ *   - util/yank.c|44| <<QLIST_HEAD>> = QLIST_HEAD_INITIALIZER(yank_instance_list);
+ *   - util/yank.c|72| <<yank_find_entry>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+ *   - util/yank.c|94| <<yank_register_instance>> QLIST_INSERT_HEAD(&yank_instance_list, entry, next);
+ *   - util/yank.c|185| <<qmp_query_yank>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+ */
 static QLIST_HEAD(, YankInstanceEntry) yank_instance_list
     = QLIST_HEAD_INITIALIZER(yank_instance_list);
 
@@ -69,6 +88,14 @@ static YankInstanceEntry *yank_find_entry(const YankInstance *instance)
 {
     YankInstanceEntry *entry;
 
+    /*
+     * 在以下使用yank_instance_list():
+     *   - util/yank.c|43| <<QLIST_HEAD>> static QLIST_HEAD(, YankInstanceEntry) yank_instance_list
+     *   - util/yank.c|44| <<QLIST_HEAD>> = QLIST_HEAD_INITIALIZER(yank_instance_list);
+     *   - util/yank.c|72| <<yank_find_entry>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+     *   - util/yank.c|94| <<yank_register_instance>> QLIST_INSERT_HEAD(&yank_instance_list, entry, next);
+     *   - util/yank.c|185| <<qmp_query_yank>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+     */
     QLIST_FOREACH(entry, &yank_instance_list, next) {
         if (yank_instance_equal(entry->instance, instance)) {
             return entry;
@@ -77,6 +104,14 @@ static YankInstanceEntry *yank_find_entry(const YankInstance *instance)
     return NULL;
 }
 
+/*
+ * 在以下使用yank_register_instance():
+ *   - block/nbd.c|1920| <<nbd_open>> if (!yank_register_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name), errp)) {
+ *   - chardev/char-socket.c|1409| <<tcp_chr_open>> if (!yank_register_instance(CHARDEV_YANK_INSTANCE(chr->label), errp)) {
+ *   - migration/migration.c|1867| <<qmp_migrate_incoming>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+ *   - migration/migration.c|2097| <<migrate_prepare>> yank_register_instance(MIGRATION_YANK_INSTANCE, &error_abort);
+ *   - migration/savevm.c|3527| <<load_snapshot>> if (!yank_register_instance(MIGRATION_YANK_INSTANCE, errp)) {
+ */
 bool yank_register_instance(const YankInstance *instance, Error **errp)
 {
     YankInstanceEntry *entry;
@@ -91,11 +126,27 @@ bool yank_register_instance(const YankInstance *instance, Error **errp)
     entry = g_new0(YankInstanceEntry, 1);
     entry->instance = QAPI_CLONE(YankInstance, instance);
     QLIST_INIT(&entry->yankfns);
+    /*
+     * 在以下使用yank_instance_list():
+     *   - util/yank.c|43| <<QLIST_HEAD>> static QLIST_HEAD(, YankInstanceEntry) yank_instance_list
+     *   - util/yank.c|44| <<QLIST_HEAD>> = QLIST_HEAD_INITIALIZER(yank_instance_list);
+     *   - util/yank.c|72| <<yank_find_entry>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+     *   - util/yank.c|94| <<yank_register_instance>> QLIST_INSERT_HEAD(&yank_instance_list, entry, next);
+     *   - util/yank.c|185| <<qmp_query_yank>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+     */
     QLIST_INSERT_HEAD(&yank_instance_list, entry, next);
 
     return true;
 }
 
+/*
+ * 在以下使用yank_unregister_instance():
+ *   - block/nbd.c|120| <<nbd_clear_bdrvstate>> yank_unregister_instance(BLOCKDEV_YANK_INSTANCE(bs->node_name));
+ *   - chardev/char-socket.c|1113| <<char_socket_finalize>> yank_unregister_instance(CHARDEV_YANK_INSTANCE(chr->label));
+ *   - migration/migration.c|496| <<migration_incoming_state_destroy>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+ *   - migration/migration.c|1418| <<migration_cleanup>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+ *   - migration/migration.c|1877| <<qmp_migrate_incoming>> yank_unregister_instance(MIGRATION_YANK_INSTANCE);
+ */
 void yank_unregister_instance(const YankInstance *instance)
 {
     YankInstanceEntry *entry;
@@ -110,6 +161,15 @@ void yank_unregister_instance(const YankInstance *instance)
     g_free(entry);
 }
 
+/*
+ * 在以下使用yank_register_function():
+ *   - block/nbd.c|341| <<nbd_co_do_establish_connection>> yank_register_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+ *               nbd_yank, bs);
+ *   - chardev/char-socket.c|909| <<tcp_chr_new_client>> yank_register_function(CHARDEV_YANK_INSTANCE(chr->label),
+ *               char_socket_yank_iochannel, QIO_CHANNEL(sioc));
+ *   - migration/yank_functions.c|44| <<migration_ioc_register_yank>> yank_register_function(MIGRATION_YANK_INSTANCE,
+ *               migration_yank_iochannel, ioc);
+ */
 void yank_register_function(const YankInstance *instance,
                             YankFn *func,
                             void *opaque)
@@ -128,6 +188,21 @@ void yank_register_function(const YankInstance *instance,
     QLIST_INSERT_HEAD(&entry->yankfns, func_entry, next);
 }
 
+/*
+ * 在以下使用yank_unregister_function():
+ *   - block/nbd.c|226| <<nbd_teardown_connection>> yank_unregister_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+ *                nbd_yank, s->bs);
+ *   - block/nbd.c|346| <<nbd_co_do_establish_connection>> yank_unregister_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+ *                nbd_yank, bs);
+ *   - block/nbd.c|402| <<nbd_reconnect_attempt>> yank_unregister_function(BLOCKDEV_YANK_INSTANCE(s->bs->node_name),
+ *                nbd_yank, s->bs);
+ *   - chardev/char-socket.c|377| <<tcp_chr_free_connection>> yank_unregister_function(CHARDEV_YANK_INSTANCE(chr->label),
+ *                char_socket_yank_iochannel, QIO_CHANNEL(s->sioc));
+ *   - chardev/char-socket.c|1132| <<qemu_chr_socket_connected>> yank_unregister_function(CHARDEV_YANK_INSTANCE(chr->label),
+ *                char_socket_yank_iochannel, QIO_CHANNEL(sioc));
+ *   - migration/yank_functions.c|41| <<migration_ioc_unregister_yank>> yank_unregister_function(MIGRATION_YANK_INSTANCE,
+ *                migration_yank_iochannel, ioc);
+ */
 void yank_unregister_function(const YankInstance *instance,
                               YankFn *func,
                               void *opaque)
@@ -150,6 +225,9 @@ void yank_unregister_function(const YankInstance *instance,
     abort();
 }
 
+/*
+ * 只在这里
+ */
 void qmp_yank(YankInstanceList *instances,
               Error **errp)
 {
@@ -174,6 +252,10 @@ void qmp_yank(YankInstanceList *instances,
     }
 }
 
+/*
+ * 在以下使用qmp_query_yank():
+ *   - tests/unit/test-yank.c|43| <<is_yank_instance_registered>> list = qmp_query_yank(&error_abort);
+ */
 YankInstanceList *qmp_query_yank(Error **errp)
 {
     YankInstanceEntry *entry;
@@ -182,6 +264,14 @@ YankInstanceList *qmp_query_yank(Error **errp)
     ret = NULL;
 
     QEMU_LOCK_GUARD(&yank_lock);
+    /*
+     * 在以下使用yank_instance_list():
+     *   - util/yank.c|43| <<QLIST_HEAD>> static QLIST_HEAD(, YankInstanceEntry) yank_instance_list
+     *   - util/yank.c|44| <<QLIST_HEAD>> = QLIST_HEAD_INITIALIZER(yank_instance_list);
+     *   - util/yank.c|72| <<yank_find_entry>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+     *   - util/yank.c|94| <<yank_register_instance>> QLIST_INSERT_HEAD(&yank_instance_list, entry, next);
+     *   - util/yank.c|185| <<qmp_query_yank>> QLIST_FOREACH(entry, &yank_instance_list, next) {
+     */
     QLIST_FOREACH(entry, &yank_instance_list, next) {
         YankInstanceList *new_entry;
         new_entry = g_new0(YankInstanceList, 1);
-- 
2.50.1 (Apple Git-155)

