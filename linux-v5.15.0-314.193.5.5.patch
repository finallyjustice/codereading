From e8d1aeb9b5ac7e8e711942dbb1b2bd822fe923c0 Mon Sep 17 00:00:00 2001
From: Dongli Zhang <dongli.zhang0129@gmail.com>
Date: Mon, 25 May 2026 11:32:01 -0700
Subject: [PATCH 1/1] linux v5.15.0-314.193.5.5

Signed-off-by: Dongli Zhang <dongli.zhang0129@gmail.com>
---
 arch/x86/include/asm/kvm_host.h               |  24 ++
 arch/x86/kvm/lapic.c                          | 226 ++++++++++++
 arch/x86/kvm/lapic.h                          |   9 +
 arch/x86/kvm/svm/avic.c                       |  25 ++
 arch/x86/kvm/vmx/vmx.c                        |   7 +
 arch/x86/kvm/x86.c                            | 110 ++++++
 drivers/iommu/amd/amd_iommu_types.h           |  36 ++
 drivers/iommu/amd/init.c                      | 204 +++++++++++
 drivers/iommu/amd/io_pgtable.c                | 123 +++++++
 drivers/iommu/amd/iommu.c                     |  42 +++
 drivers/iommu/amd/iommu_v2.c                  |   5 +
 drivers/iommu/dma-iommu.c                     |  35 ++
 drivers/iommu/iommu.c                         |  63 ++++
 .../net/ethernet/mellanox/mlx5/core/en/ptp.c  |   7 +
 .../net/ethernet/mellanox/mlx5/core/en/trap.c |   7 +
 .../net/ethernet/mellanox/mlx5/core/en/xdp.c  |  15 +
 .../ethernet/mellanox/mlx5/core/en_ethtool.c  |  12 +
 .../net/ethernet/mellanox/mlx5/core/en_main.c | 103 ++++++
 .../net/ethernet/mellanox/mlx5/core/en_rep.c  |   6 +
 .../net/ethernet/mellanox/mlx5/core/en_rx.c   | 341 ++++++++++++++++++
 .../net/ethernet/mellanox/mlx5/core/en_tx.c   |  21 ++
 .../net/ethernet/mellanox/mlx5/core/en_txrx.c |  21 ++
 .../mellanox/mlx5/core/ipoib/ethtool.c        |   6 +
 drivers/net/ethernet/mellanox/mlx5/core/wq.c  |  18 +
 drivers/net/ethernet/mellanox/mlx5/core/wq.h  |  75 ++++
 drivers/vfio/pci/mlx5/cmd.c                   |   5 +
 drivers/vfio/pci/mlx5/main.c                  |   4 +
 drivers/vfio/pci/vfio_pci_core.c              |  24 ++
 drivers/vfio/vfio_main.c                      | 104 ++++++
 drivers/virtio/virtio_ring.c                  |  23 ++
 include/linux/mlx5/device.h                   |   6 +
 virt/kvm/kvm_main.c                           |  13 +
 32 files changed, 1720 insertions(+)

diff --git a/arch/x86/include/asm/kvm_host.h b/arch/x86/include/asm/kvm_host.h
index 8b6149faa33f..8e7c4d224341 100644
--- a/arch/x86/include/asm/kvm_host.h
+++ b/arch/x86/include/asm/kvm_host.h
@@ -637,6 +637,19 @@ struct kvm_vcpu_arch {
 	bool apicv_active;
 	bool load_eoi_exitmap_pending;
 	DECLARE_BITMAP(ioapic_handled_vectors, 256);
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 */
 	unsigned long apic_attention;
 	int32_t apic_arb_prio;
 	int mp_state;
@@ -1157,6 +1170,17 @@ struct kvm_arch {
 	struct kvm_pit *vpit;
 	atomic_t vapics_in_nmi_mode;
 	struct mutex apic_map_lock;
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	struct kvm_apic_map __rcu *apic_map;
 	atomic_t apic_map_dirty;
 
diff --git a/arch/x86/kvm/lapic.c b/arch/x86/kvm/lapic.c
index 76e5ff10dc81..e67e2bb92ceb 100644
--- a/arch/x86/kvm/lapic.c
+++ b/arch/x86/kvm/lapic.c
@@ -325,6 +325,17 @@ void kvm_recalculate_apic_map(struct kvm *kvm)
 			kvm_clear_apicv_inhibit(kvm, APICV_INHIBIT_REASON_APIC_ID_MODIFIED);
 	}
 
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	old = rcu_dereference_protected(kvm->arch.apic_map,
 			lockdep_is_held(&kvm->arch.apic_map_lock));
 	rcu_assign_pointer(kvm->arch.apic_map, new);
@@ -710,6 +721,17 @@ int kvm_pv_send_ipi(struct kvm *kvm, unsigned long ipi_bitmap_low,
 	irq.trig_mode = icr & APIC_INT_LEVELTRIG;
 
 	rcu_read_lock();
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	map = rcu_dereference(kvm->arch.apic_map);
 
 	count = -EOPNOTSUPP;
@@ -756,6 +778,19 @@ static void pv_eoi_set_pending(struct kvm_vcpu *vcpu)
 	if (pv_eoi_put_user(vcpu, KVM_PV_EOI_ENABLED) < 0)
 		return;
 
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 */
 	__set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
 }
 
@@ -764,6 +799,19 @@ static void pv_eoi_clr_pending(struct kvm_vcpu *vcpu)
 	if (pv_eoi_put_user(vcpu, KVM_PV_EOI_DISABLED) < 0)
 		return;
 
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 */
 	__clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
 }
 
@@ -1065,6 +1113,17 @@ bool kvm_irq_delivery_to_apic_fast(struct kvm *kvm, struct kvm_lapic *src,
 	}
 
 	rcu_read_lock();
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	map = rcu_dereference(kvm->arch.apic_map);
 
 	ret = kvm_apic_map_get_dest_lapic(kvm, &src, irq, map, &dst, &bitmap);
@@ -1107,6 +1166,17 @@ bool kvm_intr_is_single_vcpu_fast(struct kvm *kvm, struct kvm_lapic_irq *irq,
 		return false;
 
 	rcu_read_lock();
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	map = rcu_dereference(kvm->arch.apic_map);
 
 	if (kvm_apic_map_get_dest_lapic(kvm, NULL, irq, map, &dst, &bitmap) &&
@@ -1164,6 +1234,9 @@ static int __apic_accept_irq(struct kvm_lapic *apic, int delivery_mode,
 						       apic->regs + APIC_TMR);
 		}
 
+		/*
+		 * svm_deliver_interrupt()
+		 */
 		static_call(kvm_x86_deliver_interrupt)(apic, delivery_mode,
 						       trig_mode, vector);
 		break;
@@ -1241,6 +1314,17 @@ void kvm_bitmap_or_dest_vcpus(struct kvm *kvm, struct kvm_lapic_irq *irq,
 	bool ret;
 
 	rcu_read_lock();
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	map = rcu_dereference(kvm->arch.apic_map);
 
 	ret = kvm_apic_map_get_dest_lapic(kvm, &src, irq, map, &dest_vcpu,
@@ -1300,10 +1384,20 @@ static void kvm_ioapic_send_eoi(struct kvm_lapic *apic, int vector)
 	kvm_ioapic_update_eoi(apic->vcpu, vector, trigger_mode);
 }
 
+/*
+ * 在以下使用apic_set_eoi():
+ *   - arch/x86/kvm/lapic.c|2165| <<kvm_lapic_reg_write(APIC_EOI)>> apic_set_eoi(apic);
+ *   - arch/x86/kvm/lapic.c|2905| <<apic_sync_pv_eoi_from_guest>> vector = apic_set_eoi(apic);
+ */
 static int apic_set_eoi(struct kvm_lapic *apic)
 {
 	int vector = apic_find_highest_isr(apic);
 
+	/*
+	 * 在以下使用trace_kvm_eoi():
+	 *   - arch/x86/kvm/lapic.c|1310| <<apic_set_eoi>> trace_kvm_eoi(apic, vector);
+	 *   - arch/x86/kvm/lapic.c|1339| <<kvm_apic_set_eoi_accelerated>> trace_kvm_eoi(apic, vector);
+	 */
 	trace_kvm_eoi(apic, vector);
 
 	/*
@@ -1329,10 +1423,19 @@ static int apic_set_eoi(struct kvm_lapic *apic)
  * this interface assumes a trap-like exit, which has already finished
  * desired side effect including vISR and vPPR update.
  */
+/*
+ * 在以下使用kvm_apic_set_eoi_accelerated():
+ *   - arch/x86/kvm/vmx/vmx.c|5374| <<handle_apic_eoi_induced>> kvm_apic_set_eoi_accelerated(vcpu, vector);
+ */
 void kvm_apic_set_eoi_accelerated(struct kvm_vcpu *vcpu, int vector)
 {
 	struct kvm_lapic *apic = vcpu->arch.apic;
 
+	/*
+	 * 在以下使用trace_kvm_eoi():
+	 *   - arch/x86/kvm/lapic.c|1310| <<apic_set_eoi>> trace_kvm_eoi(apic, vector);
+	 *   - arch/x86/kvm/lapic.c|1339| <<kvm_apic_set_eoi_accelerated>> trace_kvm_eoi(apic, vector);
+	 */
 	trace_kvm_eoi(apic, vector);
 
 	kvm_ioapic_send_eoi(apic, vector);
@@ -1679,6 +1782,15 @@ static void __kvm_wait_lapic_expire(struct kvm_vcpu *vcpu)
 	struct kvm_lapic *apic = vcpu->arch.apic;
 	u64 guest_tsc, tsc_deadline;
 
+	/*
+	 * 在以下使用kvm_timer->expired_tscdeadline:
+	 *   - arch/x86/kvm/lapic.c|1685| <<__kvm_wait_lapic_expire>> tsc_deadline = apic->lapic_timer.expired_tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1686| <<__kvm_wait_lapic_expire>> apic->lapic_timer.expired_tscdeadline = 0;
+	 *   - arch/x86/kvm/lapic.c|1708| <<kvm_wait_lapic_expire>> vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|1737| <<apic_timer_expired>> ktimer->expired_tscdeadline = ktimer->tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1753| <<apic_timer_expired>> if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|2791| <<kvm_apic_set_state>> apic->lapic_timer.expired_tscdeadline = 0;
+	 */
 	tsc_deadline = apic->lapic_timer.expired_tscdeadline;
 	apic->lapic_timer.expired_tscdeadline = 0;
 	guest_tsc = kvm_read_l1_tsc(vcpu, rdtsc());
@@ -1701,6 +1813,15 @@ static void __kvm_wait_lapic_expire(struct kvm_vcpu *vcpu)
 
 void kvm_wait_lapic_expire(struct kvm_vcpu *vcpu)
 {
+	/*
+	 * 在以下使用kvm_timer->expired_tscdeadline:
+	 *   - arch/x86/kvm/lapic.c|1685| <<__kvm_wait_lapic_expire>> tsc_deadline = apic->lapic_timer.expired_tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1686| <<__kvm_wait_lapic_expire>> apic->lapic_timer.expired_tscdeadline = 0;
+	 *   - arch/x86/kvm/lapic.c|1708| <<kvm_wait_lapic_expire>> vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|1737| <<apic_timer_expired>> ktimer->expired_tscdeadline = ktimer->tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1753| <<apic_timer_expired>> if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|2791| <<kvm_apic_set_state>> apic->lapic_timer.expired_tscdeadline = 0;
+	 */
 	if (lapic_in_kernel(vcpu) &&
 	    vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
 	    vcpu->arch.apic->lapic_timer.timer_advance_ns &&
@@ -1730,6 +1851,15 @@ static void apic_timer_expired(struct kvm_lapic *apic, bool from_timer_fn)
 	if (atomic_read(&apic->lapic_timer.pending))
 		return;
 
+	/*
+	 * 在以下使用kvm_timer->expired_tscdeadline:
+	 *   - arch/x86/kvm/lapic.c|1685| <<__kvm_wait_lapic_expire>> tsc_deadline = apic->lapic_timer.expired_tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1686| <<__kvm_wait_lapic_expire>> apic->lapic_timer.expired_tscdeadline = 0;
+	 *   - arch/x86/kvm/lapic.c|1708| <<kvm_wait_lapic_expire>> vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|1737| <<apic_timer_expired>> ktimer->expired_tscdeadline = ktimer->tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1753| <<apic_timer_expired>> if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|2791| <<kvm_apic_set_state>> apic->lapic_timer.expired_tscdeadline = 0;
+	 */
 	if (apic_lvtt_tscdeadline(apic) || ktimer->hv_timer_in_use)
 		ktimer->expired_tscdeadline = ktimer->tscdeadline;
 
@@ -1747,6 +1877,15 @@ static void apic_timer_expired(struct kvm_lapic *apic, bool from_timer_fn)
 		 * interrupt isn't yet injected.  Waiting until after injecting
 		 * is not an option since that won't help a posted interrupt.
 		 */
+		/*
+		 * 在以下使用kvm_timer->expired_tscdeadline:
+		 *   - arch/x86/kvm/lapic.c|1685| <<__kvm_wait_lapic_expire>> tsc_deadline = apic->lapic_timer.expired_tscdeadline;
+		 *   - arch/x86/kvm/lapic.c|1686| <<__kvm_wait_lapic_expire>> apic->lapic_timer.expired_tscdeadline = 0;
+		 *   - arch/x86/kvm/lapic.c|1708| <<kvm_wait_lapic_expire>> vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+		 *   - arch/x86/kvm/lapic.c|1737| <<apic_timer_expired>> ktimer->expired_tscdeadline = ktimer->tscdeadline;
+		 *   - arch/x86/kvm/lapic.c|1753| <<apic_timer_expired>> if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+		 *   - arch/x86/kvm/lapic.c|2791| <<kvm_apic_set_state>> apic->lapic_timer.expired_tscdeadline = 0;
+		 */
 		if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
 		    vcpu->arch.apic->lapic_timer.timer_advance_ns)
 			__kvm_wait_lapic_expire(vcpu);
@@ -2123,6 +2262,11 @@ int kvm_lapic_reg_write(struct kvm_lapic *apic, u32 reg, u32 val)
 		break;
 
 	case APIC_EOI:
+		/*
+		 * 在以下使用apic_set_eoi():
+		 *   - arch/x86/kvm/lapic.c|2165| <<kvm_lapic_reg_write(APIC_EOI)>> apic_set_eoi(apic);
+		 *   - arch/x86/kvm/lapic.c|2905| <<apic_sync_pv_eoi_from_guest>> vector = apic_set_eoi(apic);
+		 */
 		apic_set_eoi(apic);
 		break;
 
@@ -2512,6 +2656,19 @@ void kvm_lapic_reset(struct kvm_vcpu *vcpu, bool init_event)
 	}
 
 	vcpu->arch.apic_arb_prio = 0;
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 */
 	vcpu->arch.apic_attention = 0;
 
 	kvm_recalculate_apic_map(vcpu->kvm);
@@ -2785,6 +2942,15 @@ int kvm_apic_set_state(struct kvm_vcpu *vcpu, struct kvm_lapic_state *s)
 
 	apic_update_ppr(apic);
 	cancel_apic_timer(apic);
+	/*
+	 * 在以下使用kvm_timer->expired_tscdeadline:
+	 *   - arch/x86/kvm/lapic.c|1685| <<__kvm_wait_lapic_expire>> tsc_deadline = apic->lapic_timer.expired_tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1686| <<__kvm_wait_lapic_expire>> apic->lapic_timer.expired_tscdeadline = 0;
+	 *   - arch/x86/kvm/lapic.c|1708| <<kvm_wait_lapic_expire>> vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|1737| <<apic_timer_expired>> ktimer->expired_tscdeadline = ktimer->tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1753| <<apic_timer_expired>> if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|2791| <<kvm_apic_set_state>> apic->lapic_timer.expired_tscdeadline = 0;
+	 */
 	apic->lapic_timer.expired_tscdeadline = 0;
 	apic_update_lvtt(apic);
 	apic_manage_nmi_watchdog(apic, kvm_lapic_get_reg(apic, APIC_LVT0));
@@ -2828,6 +2994,10 @@ void __kvm_migrate_apic_timer(struct kvm_vcpu *vcpu)
  * last entry. If yes, set EOI on guests's behalf.
  * Clear PV EOI in guest memory in any case.
  */
+/*
+ * 在以下使用apic_sync_pv_eoi_from_guest():
+ *   - arch/x86/kvm/lapic.c|2914| <<kvm_lapic_sync_from_vapic>> apic_sync_pv_eoi_from_guest(vcpu, vcpu->arch.apic);
+ */
 static void apic_sync_pv_eoi_from_guest(struct kvm_vcpu *vcpu,
 					struct kvm_lapic *apic)
 {
@@ -2854,14 +3024,37 @@ static void apic_sync_pv_eoi_from_guest(struct kvm_vcpu *vcpu,
 	pv_eoi_clr_pending(vcpu);
 	if (pending)
 		return;
+	/*
+	 * 在以下使用apic_set_eoi():
+	 *   - arch/x86/kvm/lapic.c|2165| <<kvm_lapic_reg_write(APIC_EOI)>> apic_set_eoi(apic);
+	 *   - arch/x86/kvm/lapic.c|2905| <<apic_sync_pv_eoi_from_guest>> vector = apic_set_eoi(apic);
+	 */
 	vector = apic_set_eoi(apic);
 	trace_kvm_pv_eoi(apic, vector);
 }
 
+/*
+ * 在以下使用kvm_lapic_sync_from_vapic():
+ *   - arch/x86/kvm/x86.c|10306| <<vcpu_enter_guest>> kvm_lapic_sync_from_vapic(vcpu);
+ *   - arch/x86/kvm/x86.c|10316| <<vcpu_enter_guest>> kvm_lapic_sync_from_vapic(vcpu);
+ */
 void kvm_lapic_sync_from_vapic(struct kvm_vcpu *vcpu)
 {
 	u32 data;
 
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 */
 	if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
 		apic_sync_pv_eoi_from_guest(vcpu, vcpu->arch.apic);
 
@@ -2909,6 +3102,19 @@ void kvm_lapic_sync_to_vapic(struct kvm_vcpu *vcpu)
 
 	apic_sync_pv_eoi_to_guest(vcpu, apic);
 
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 */
 	if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
 		return;
 
@@ -2932,6 +3138,19 @@ int kvm_lapic_set_vapic_addr(struct kvm_vcpu *vcpu, gpa_t vapic_addr)
 					&vcpu->arch.apic->vapic_cache,
 					vapic_addr, sizeof(u32)))
 			return -EINVAL;
+		/*
+		 * 在以下使用kvm_vcpu_arch->apic_attention:
+		 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+		 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+		 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+		 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+		 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+		 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+		 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+		 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+		 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+		 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+		 */
 		__set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
 	} else {
 		__clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
@@ -3047,6 +3266,13 @@ int kvm_lapic_enable_pv_eoi(struct kvm_vcpu *vcpu, u64 data, unsigned long len)
 	return kvm_gfn_to_hva_cache_init(vcpu->kvm, ghc, addr, new_len);
 }
 
+/*
+ * 在以下使用kvm_apic_accept_events():
+ *   - arch/x86/kvm/x86.c|10072| <<vcpu_enter_guest>> r = kvm_apic_accept_events(vcpu);
+ *   - arch/x86/kvm/x86.c|10345| <<vcpu_block>> if (kvm_apic_accept_events(vcpu) < 0)
+ *   - arch/x86/kvm/x86.c|10545| <<kvm_arch_vcpu_ioctl_run>> if (kvm_apic_accept_events(vcpu) < 0) {
+ *   - arch/x86/kvm/x86.c|10781| <<kvm_arch_vcpu_ioctl_get_mpstate>> r = kvm_apic_accept_events(vcpu);
+ */
 int kvm_apic_accept_events(struct kvm_vcpu *vcpu)
 {
 	struct kvm_lapic *apic = vcpu->arch.apic;
diff --git a/arch/x86/kvm/lapic.h b/arch/x86/kvm/lapic.h
index ee42eef92f32..f26124fc57e0 100644
--- a/arch/x86/kvm/lapic.h
+++ b/arch/x86/kvm/lapic.h
@@ -36,6 +36,15 @@ struct kvm_timer {
 	u32 timer_mode;
 	u32 timer_mode_mask;
 	u64 tscdeadline;
+	/*
+	 * 在以下使用kvm_timer->expired_tscdeadline:
+	 *   - arch/x86/kvm/lapic.c|1685| <<__kvm_wait_lapic_expire>> tsc_deadline = apic->lapic_timer.expired_tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1686| <<__kvm_wait_lapic_expire>> apic->lapic_timer.expired_tscdeadline = 0;
+	 *   - arch/x86/kvm/lapic.c|1708| <<kvm_wait_lapic_expire>> vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|1737| <<apic_timer_expired>> ktimer->expired_tscdeadline = ktimer->tscdeadline;
+	 *   - arch/x86/kvm/lapic.c|1753| <<apic_timer_expired>> if (vcpu->arch.apic->lapic_timer.expired_tscdeadline &&
+	 *   - arch/x86/kvm/lapic.c|2791| <<kvm_apic_set_state>> apic->lapic_timer.expired_tscdeadline = 0;
+	 */
 	u64 expired_tscdeadline;
 	u32 timer_advance_ns;
 	s64 advance_expire_delta;
diff --git a/arch/x86/kvm/svm/avic.c b/arch/x86/kvm/svm/avic.c
index 8830605bc5ec..06ab09ff96bc 100644
--- a/arch/x86/kvm/svm/avic.c
+++ b/arch/x86/kvm/svm/avic.c
@@ -51,6 +51,16 @@ module_param_unsafe(force_avic, bool, 0444);
 static DEFINE_HASHTABLE(svm_vm_data_hash, SVM_VM_DATA_HASH_BITS);
 static u32 next_vm_id = 0;
 static bool next_vm_id_wrapped = 0;
+/*
+ * 在以下使用svm_vm_data_hash_lock:
+ *   - arch/x86/kvm/svm/avic.c|54| <<global>> static DEFINE_SPINLOCK(svm_vm_data_hash_lock);
+ *   - arch/x86/kvm/svm/avic.c|127| <<avic_ga_log_notifier>> spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
+ *   - arch/x86/kvm/svm/avic.c|134| <<avic_ga_log_notifier>> spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
+ *   - arch/x86/kvm/svm/avic.c|160| <<avic_vm_destroy>> spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
+ *   - arch/x86/kvm/svm/avic.c|162| <<avic_vm_destroy>> spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
+ *   - arch/x86/kvm/svm/avic.c|192| <<avic_vm_init>> spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
+ *   - arch/x86/kvm/svm/avic.c|208| <<avic_vm_init>> spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
+ */
 static DEFINE_SPINLOCK(svm_vm_data_hash_lock);
 enum avic_modes avic_mode;
 
@@ -124,6 +134,16 @@ int avic_ga_log_notifier(u32 ga_tag)
 	pr_debug("SVM: %s: vm_id=%#x, vcpu_id=%#x\n", __func__, vm_id, vcpu_id);
 	trace_kvm_avic_ga_log(vm_id, vcpu_id);
 
+	/*
+	 * 在以下使用svm_vm_data_hash_lock:
+	 *   - arch/x86/kvm/svm/avic.c|54| <<global>> static DEFINE_SPINLOCK(svm_vm_data_hash_lock);
+	 *   - arch/x86/kvm/svm/avic.c|127| <<avic_ga_log_notifier>> spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
+	 *   - arch/x86/kvm/svm/avic.c|134| <<avic_ga_log_notifier>> spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
+	 *   - arch/x86/kvm/svm/avic.c|160| <<avic_vm_destroy>> spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
+	 *   - arch/x86/kvm/svm/avic.c|162| <<avic_vm_destroy>> spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
+	 *   - arch/x86/kvm/svm/avic.c|192| <<avic_vm_init>> spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
+	 *   - arch/x86/kvm/svm/avic.c|208| <<avic_vm_init>> spin_unlock_irqrestore(&svm_vm_data_hash_lock, flags);
+	 */
 	spin_lock_irqsave(&svm_vm_data_hash_lock, flags);
 	hash_for_each_possible(svm_vm_data_hash, kvm_svm, hnode, vm_id) {
 		if (kvm_svm->avic_vm_id != vm_id)
@@ -1098,6 +1118,11 @@ void __avic_vcpu_load(struct kvm_vcpu *vcpu, int cpu)
 	spin_unlock_irqrestore(&svm->ir_list_lock, flags);
 }
 
+/*
+ * 在以下使用__avic_vcpu_put():
+ *   - arch/x86/kvm/svm/avic.c|1156| <<avic_vcpu_put>> __avic_vcpu_put(vcpu);
+ *   - arch/x86/kvm/svm/svm.c|1632| <<svm_vcpu_put>> __avic_vcpu_put(vcpu);
+ */
 void __avic_vcpu_put(struct kvm_vcpu *vcpu)
 {
 	u64 entry;
diff --git a/arch/x86/kvm/vmx/vmx.c b/arch/x86/kvm/vmx/vmx.c
index 6fbb5f43c86a..864a07e60096 100644
--- a/arch/x86/kvm/vmx/vmx.c
+++ b/arch/x86/kvm/vmx/vmx.c
@@ -5365,11 +5365,18 @@ static int handle_apic_access(struct kvm_vcpu *vcpu)
 	return kvm_emulate_instruction(vcpu, 0);
 }
 
+/*
+ * 在以下使用handle_apic_eoi_induced():
+ *   - arch/x86/kvm/vmx/vmx.c|5785| <<global>> [EXIT_REASON_EOI_INDUCED] = handle_apic_eoi_induced,
+ */
 static int handle_apic_eoi_induced(struct kvm_vcpu *vcpu)
 {
 	unsigned long exit_qualification = vmx_get_exit_qual(vcpu);
 	int vector = exit_qualification & 0xff;
 
+	/*
+	 * 只在这里使用
+	 */
 	/* EOI-induced VM exit is trap-like and thus no need to adjust IP */
 	kvm_apic_set_eoi_accelerated(vcpu, vector);
 	return 1;
diff --git a/arch/x86/kvm/x86.c b/arch/x86/kvm/x86.c
index 0c336b6680b1..dca6e86c77fe 100644
--- a/arch/x86/kvm/x86.c
+++ b/arch/x86/kvm/x86.c
@@ -9053,6 +9053,17 @@ static void kvm_sched_yield(struct kvm_vcpu *vcpu, unsigned long dest_id)
 		goto no_yield;
 
 	rcu_read_lock();
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	map = rcu_dereference(vcpu->kvm->arch.apic_map);
 
 	if (likely(map) && dest_id <= map->max_apic_id) {
@@ -10069,6 +10080,13 @@ static int vcpu_enter_guest(struct kvm_vcpu *vcpu)
 	if (kvm_check_request(KVM_REQ_EVENT, vcpu) || req_int_win ||
 	    kvm_xen_has_interrupt(vcpu)) {
 		++vcpu->stat.req_event;
+		/*
+		 * 在以下使用kvm_apic_accept_events():
+		 *   - arch/x86/kvm/x86.c|10072| <<vcpu_enter_guest>> r = kvm_apic_accept_events(vcpu);
+		 *   - arch/x86/kvm/x86.c|10345| <<vcpu_block>> if (kvm_apic_accept_events(vcpu) < 0)
+		 *   - arch/x86/kvm/x86.c|10545| <<kvm_arch_vcpu_ioctl_run>> if (kvm_apic_accept_events(vcpu) < 0) {
+		 *   - arch/x86/kvm/x86.c|10781| <<kvm_arch_vcpu_ioctl_get_mpstate>> r = kvm_apic_accept_events(vcpu);
+		 */
 		r = kvm_apic_accept_events(vcpu);
 		if (r < 0) {
 			r = 0;
@@ -10295,6 +10313,23 @@ static int vcpu_enter_guest(struct kvm_vcpu *vcpu)
 	if (unlikely(vcpu->arch.tsc_always_catchup))
 		kvm_make_request(KVM_REQ_CLOCK_UPDATE, vcpu);
 
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 *
+	 * 在以下使用kvm_lapic_sync_from_vapic():
+	 *   - arch/x86/kvm/x86.c|10306| <<vcpu_enter_guest>> kvm_lapic_sync_from_vapic(vcpu);
+	 *   - arch/x86/kvm/x86.c|10316| <<vcpu_enter_guest>> kvm_lapic_sync_from_vapic(vcpu);
+	 */
 	if (vcpu->arch.apic_attention)
 		kvm_lapic_sync_from_vapic(vcpu);
 
@@ -10305,6 +10340,23 @@ static int vcpu_enter_guest(struct kvm_vcpu *vcpu)
 	if (req_immediate_exit)
 		kvm_make_request(KVM_REQ_EVENT, vcpu);
 	static_call(kvm_x86_cancel_injection)(vcpu);
+	/*
+	 * 在以下使用kvm_vcpu_arch->apic_attention:
+	 *   - arch/x86/kvm/lapic.c|759| <<pv_eoi_set_pending>> __set_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|767| <<pv_eoi_clr_pending>> __clear_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2554| <<kvm_lapic_reset>> vcpu->arch.apic_attention = 0;
+	 *   - arch/x86/kvm/lapic.c|2913| <<kvm_lapic_sync_from_vapic>> if (test_bit(KVM_APIC_PV_EOI_PENDING, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2916| <<kvm_lapic_sync_from_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2960| <<kvm_lapic_sync_to_vapic>> if (!test_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention))
+	 *   - arch/x86/kvm/lapic.c|2983| <<kvm_lapic_set_vapic_addr>> __set_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/lapic.c|2985| <<kvm_lapic_set_vapic_addr>> __clear_bit(KVM_APIC_CHECK_VAPIC, &vcpu->arch.apic_attention);
+	 *   - arch/x86/kvm/x86.c|10305| <<vcpu_enter_guest>> if (vcpu->arch.apic_attention)
+	 *   - arch/x86/kvm/x86.c|10315| <<vcpu_enter_guest>> if (unlikely(vcpu->arch.apic_attention))
+	 *
+	 * 在以下使用kvm_lapic_sync_from_vapic():
+	 *   - arch/x86/kvm/x86.c|10306| <<vcpu_enter_guest>> kvm_lapic_sync_from_vapic(vcpu);
+	 *   - arch/x86/kvm/x86.c|10316| <<vcpu_enter_guest>> kvm_lapic_sync_from_vapic(vcpu);
+	 */
 	if (unlikely(vcpu->arch.apic_attention))
 		kvm_lapic_sync_from_vapic(vcpu);
 out:
@@ -10329,6 +10381,19 @@ static inline int vcpu_block(struct kvm *kvm, struct kvm_vcpu *vcpu)
 			kvm_lapic_switch_to_sw_timer(vcpu);
 
 		srcu_read_unlock(&kvm->srcu, vcpu->srcu_idx);
+		/*
+		 * 在以下使用kvm_vcpu_block():
+		 *   - arch/arm64/kvm/handle_exit.c|98| <<kvm_handle_wfx>> kvm_vcpu_block(vcpu);
+		 *   - arch/arm64/kvm/psci.c|49| <<kvm_psci_vcpu_suspend>> kvm_vcpu_block(vcpu);
+		 *   - arch/mips/kvm/emulate.c|955| <<kvm_mips_emul_wait>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/book3s_pr.c|495| <<kvmppc_set_msr_pr>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/book3s_pr_papr.c|379| <<kvmppc_h_pr>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/booke.c|721| <<kvmppc_core_prepare_to_enter>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/powerpc.c|239| <<kvmppc_kvm_pv>> kvm_vcpu_block(vcpu);
+		 *   - arch/s390/kvm/interrupt.c|1342| <<kvm_s390_handle_wait>> kvm_vcpu_block(vcpu);
+		 *   - arch/x86/kvm/x86.c|10332| <<vcpu_block>> kvm_vcpu_block(vcpu);
+		 *   - arch/x86/kvm/x86.c|10544| <<kvm_arch_vcpu_ioctl_run>> kvm_vcpu_block(vcpu);
+		 */
 		kvm_vcpu_block(vcpu);
 		vcpu->srcu_idx = srcu_read_lock(&kvm->srcu);
 
@@ -10342,6 +10407,13 @@ static inline int vcpu_block(struct kvm *kvm, struct kvm_vcpu *vcpu)
 			return 1;
 	}
 
+	/*
+	 * 在以下使用kvm_apic_accept_events():
+	 *   - arch/x86/kvm/x86.c|10072| <<vcpu_enter_guest>> r = kvm_apic_accept_events(vcpu);
+	 *   - arch/x86/kvm/x86.c|10345| <<vcpu_block>> if (kvm_apic_accept_events(vcpu) < 0)
+	 *   - arch/x86/kvm/x86.c|10545| <<kvm_arch_vcpu_ioctl_run>> if (kvm_apic_accept_events(vcpu) < 0) {
+	 *   - arch/x86/kvm/x86.c|10781| <<kvm_arch_vcpu_ioctl_get_mpstate>> r = kvm_apic_accept_events(vcpu);
+	 */
 	if (kvm_apic_accept_events(vcpu) < 0)
 		return 0;
 	switch(vcpu->arch.mp_state) {
@@ -10541,7 +10613,27 @@ int kvm_arch_vcpu_ioctl_run(struct kvm_vcpu *vcpu)
 		 * use before KVM has ever run the vCPU.
 		 */
 		WARN_ON_ONCE(kvm_lapic_hv_timer_in_use(vcpu));
+		/*
+		 * 在以下使用kvm_vcpu_block():
+		 *   - arch/arm64/kvm/handle_exit.c|98| <<kvm_handle_wfx>> kvm_vcpu_block(vcpu);
+		 *   - arch/arm64/kvm/psci.c|49| <<kvm_psci_vcpu_suspend>> kvm_vcpu_block(vcpu);
+		 *   - arch/mips/kvm/emulate.c|955| <<kvm_mips_emul_wait>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/book3s_pr.c|495| <<kvmppc_set_msr_pr>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/book3s_pr_papr.c|379| <<kvmppc_h_pr>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/booke.c|721| <<kvmppc_core_prepare_to_enter>> kvm_vcpu_block(vcpu);
+		 *   - arch/powerpc/kvm/powerpc.c|239| <<kvmppc_kvm_pv>> kvm_vcpu_block(vcpu);
+		 *   - arch/s390/kvm/interrupt.c|1342| <<kvm_s390_handle_wait>> kvm_vcpu_block(vcpu);
+		 *   - arch/x86/kvm/x86.c|10332| <<vcpu_block>> kvm_vcpu_block(vcpu);
+		 *   - arch/x86/kvm/x86.c|10544| <<kvm_arch_vcpu_ioctl_run>> kvm_vcpu_block(vcpu);
+		 */
 		kvm_vcpu_block(vcpu);
+		/*
+		 * 在以下使用kvm_apic_accept_events():
+		 *   - arch/x86/kvm/x86.c|10072| <<vcpu_enter_guest>> r = kvm_apic_accept_events(vcpu);
+		 *   - arch/x86/kvm/x86.c|10345| <<vcpu_block>> if (kvm_apic_accept_events(vcpu) < 0)
+		 *   - arch/x86/kvm/x86.c|10545| <<kvm_arch_vcpu_ioctl_run>> if (kvm_apic_accept_events(vcpu) < 0) {
+		 *   - arch/x86/kvm/x86.c|10781| <<kvm_arch_vcpu_ioctl_get_mpstate>> r = kvm_apic_accept_events(vcpu);
+		 */
 		if (kvm_apic_accept_events(vcpu) < 0) {
 			r = 0;
 			goto out;
@@ -10778,6 +10870,13 @@ int kvm_arch_vcpu_ioctl_get_mpstate(struct kvm_vcpu *vcpu,
 	if (kvm_mpx_supported())
 		kvm_load_guest_fpu(vcpu);
 
+	/*
+	 * 在以下使用kvm_apic_accept_events():
+	 *   - arch/x86/kvm/x86.c|10072| <<vcpu_enter_guest>> r = kvm_apic_accept_events(vcpu);
+	 *   - arch/x86/kvm/x86.c|10345| <<vcpu_block>> if (kvm_apic_accept_events(vcpu) < 0)
+	 *   - arch/x86/kvm/x86.c|10545| <<kvm_arch_vcpu_ioctl_run>> if (kvm_apic_accept_events(vcpu) < 0) {
+	 *   - arch/x86/kvm/x86.c|10781| <<kvm_arch_vcpu_ioctl_get_mpstate>> r = kvm_apic_accept_events(vcpu);
+	 */
 	r = kvm_apic_accept_events(vcpu);
 	if (r < 0)
 		goto out;
@@ -11920,6 +12019,17 @@ void kvm_arch_destroy_vm(struct kvm *kvm)
 	kvm_pic_destroy(kvm);
 	kvm_ioapic_destroy(kvm);
 	kvm_free_vcpus(kvm);
+	/*
+	 * 在以下使用apic_map:
+	 *   - arch/x86/kvm/lapic.c|328| <<kvm_recalculate_apic_map>> old = rcu_dereference_protected(kvm->arch.apic_map,
+	 *   - arch/x86/kvm/lapic.c|330| <<kvm_recalculate_apic_map>> rcu_assign_pointer(kvm->arch.apic_map, new);
+	 *   - arch/x86/kvm/lapic.c|713| <<kvm_pv_send_ipi>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1068| <<kvm_irq_delivery_to_apic_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1110| <<kvm_intr_is_single_vcpu_fast>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/lapic.c|1247| <<kvm_bitmap_or_dest_vcpus>> map = rcu_dereference(kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|9056| <<kvm_sched_yield>> map = rcu_dereference(vcpu->kvm->arch.apic_map);
+	 *   - arch/x86/kvm/x86.c|11977| <<kvm_arch_destroy_vm>> kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
+	 */
 	kvfree(rcu_dereference_check(kvm->arch.apic_map, 1));
 	kfree(srcu_dereference_check(kvm->arch.pmu_event_filter, &kvm->srcu, 1));
 	kvm_mmu_uninit_vm(kvm);
diff --git a/drivers/iommu/amd/amd_iommu_types.h b/drivers/iommu/amd/amd_iommu_types.h
index c3b655fe8254..cee19ed40559 100644
--- a/drivers/iommu/amd/amd_iommu_types.h
+++ b/drivers/iommu/amd/amd_iommu_types.h
@@ -167,7 +167,23 @@
 #define CONTROL_GT_EN           0x10ULL
 #define CONTROL_GA_EN           0x11ULL
 #define CONTROL_GAM_EN          0x19ULL
+/*
+ * 在以下使用CONTROL_GALOG_EN:
+ *   - drivers/iommu/amd/init.c|572| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+ *   - drivers/iommu/amd/init.c|780| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+ *   - drivers/iommu/amd/init.c|787| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+ *   - drivers/iommu/amd/init.c|957| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+ *   - drivers/iommu/amd/init.c|2768| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+ */
 #define CONTROL_GALOG_EN        0x1CULL
+/*
+ * 在以下使用CONTROL_GAINT_EN:
+ *   - drivers/iommu/amd/init.c|573| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+ *   - drivers/iommu/amd/init.c|781| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+ *   - drivers/iommu/amd/init.c|786| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+ *   - drivers/iommu/amd/init.c|956| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+ *   - drivers/iommu/amd/init.c|2769| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+ */
 #define CONTROL_GAINT_EN        0x1DULL
 #define CONTROL_XT_EN           0x32ULL
 #define CONTROL_INTCAPXT_EN     0x33ULL
@@ -481,6 +497,16 @@ extern struct kmem_cache *amd_iommu_irq_cache;
 /*
  * Make iterating over all IOMMUs easier
  */
+/*
+ * 在以下使用amd_iommu_list:
+ *   - drivers/iommu/amd/init.c|181| <<global>> LIST_HEAD(amd_iommu_list);
+ *   - drivers/iommu/amd/amd_iommu_types.h|485| <<for_each_iommu>> list_for_each_entry((iommu), &amd_iommu_list, list)
+ *   - drivers/iommu/amd/amd_iommu_types.h|487| <<for_each_iommu_safe>> list_for_each_entry_safe((iommu), (next), &amd_iommu_list, list)
+ *   - drivers/iommu/amd/init.c|358| <<get_global_efr>> if (list_is_first(&iommu->list, &amd_iommu_list)) {
+ *   - drivers/iommu/amd/init.c|1701| <<init_iommu_one>> list_add_tail(&iommu->list, &amd_iommu_list);
+ *   - drivers/iommu/amd/init.c|3309| <<amd_iommu_init>> if (ret && list_empty(&amd_iommu_list)) {
+ */
+
 #define for_each_iommu(iommu) \
 	list_for_each_entry((iommu), &amd_iommu_list, list)
 #define for_each_iommu_safe(iommu, next) \
@@ -636,6 +662,16 @@ struct amd_iommu {
 	/* Base of the PPR log, if present */
 	u8 *ppr_log;
 
+	/*
+	 * 在以下使用amd_iommu->ga_log:
+	 *   - drivers/iommu/amd/init.c|931| <<free_ga_log>> free_pages((unsigned long )iommu->ga_log, get_order(GA_LOG_SIZE));
+	 *   - drivers/iommu/amd/init.c|942| <<iommu_ga_log_enable>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/init.c|945| <<iommu_ga_log_enable>> entry = iommu_virt_to_phys(iommu->ga_log) | GA_LOG_SIZE_512;
+	 *   - drivers/iommu/amd/init.c|978| <<iommu_init_ga_log>> iommu->ga_log = (u8 *)__get_free_pages(GFP_KERNEL | __GFP_ZERO,
+	 *   - drivers/iommu/amd/init.c|980| <<iommu_init_ga_log>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/iommu.c|704| <<iommu_poll_ga_log>> if (iommu->ga_log == NULL)
+	 *   - drivers/iommu/amd/iommu.c|714| <<iommu_poll_ga_log>> raw = (u64 *)(iommu->ga_log + head);
+	 */
 	/* Base of the GA log, if present */
 	u8 *ga_log;
 
diff --git a/drivers/iommu/amd/init.c b/drivers/iommu/amd/init.c
index 0fbe492becfb..84eb78ceaca9 100644
--- a/drivers/iommu/amd/init.c
+++ b/drivers/iommu/amd/init.c
@@ -178,6 +178,15 @@ bool amd_iommu_snp_en;
 EXPORT_SYMBOL(amd_iommu_snp_en);
 
 LIST_HEAD(amd_iommu_pci_seg_list);	/* list of all PCI segments */
+/*
+ * 在以下使用amd_iommu_list:
+ *   - drivers/iommu/amd/init.c|181| <<global>> LIST_HEAD(amd_iommu_list);
+ *   - drivers/iommu/amd/amd_iommu_types.h|485| <<for_each_iommu>> list_for_each_entry((iommu), &amd_iommu_list, list)
+ *   - drivers/iommu/amd/amd_iommu_types.h|487| <<for_each_iommu_safe>> list_for_each_entry_safe((iommu), (next), &amd_iommu_list, list)
+ *   - drivers/iommu/amd/init.c|358| <<get_global_efr>> if (list_is_first(&iommu->list, &amd_iommu_list)) {
+ *   - drivers/iommu/amd/init.c|1701| <<init_iommu_one>> list_add_tail(&iommu->list, &amd_iommu_list);
+ *   - drivers/iommu/amd/init.c|3309| <<amd_iommu_init>> if (ret && list_empty(&amd_iommu_list)) {
+ */
 LIST_HEAD(amd_iommu_list);		/* list of all AMD IOMMUs in the
 					   system */
 
@@ -198,6 +207,36 @@ static bool amd_iommu_pc_present __read_mostly;
 
 bool amd_iommu_force_isolation __read_mostly;
 
+/*
+ * 在以下使用amd_iommu_dev_table:
+ *   - drivers/iommu/amd/init.c|465| <<iommu_set_device_table>> entry = iommu_virt_to_phys(amd_iommu_dev_table);
+ *   - drivers/iommu/amd/init.c|984| <<set_dev_entry_bit>> amd_iommu_dev_table[devid].data[i] |= (1UL << _bit);
+ *   - drivers/iommu/amd/init.c|992| <<get_dev_entry_bit>> return (amd_iommu_dev_table[devid].data[i] & (1UL << _bit)) >> _bit;
+ *   - drivers/iommu/amd/init.c|2479| <<uninit_device_table_dma>> amd_iommu_dev_table[devid].data[0] = 0ULL;
+ *   - drivers/iommu/amd/init.c|2480| <<uninit_device_table_dma>> amd_iommu_dev_table[devid].data[1] = 0ULL;
+ *   - drivers/iommu/amd/init.c|2654| <<early_enable_iommus>> free_pages((unsigned long )amd_iommu_dev_table,
+ *   - drivers/iommu/amd/init.c|2656| <<early_enable_iommus>> amd_iommu_dev_table = old_dev_tbl_cpy;
+ *   - drivers/iommu/amd/init.c|2813| <<free_iommu_resources>> free_pages((unsigned long )amd_iommu_dev_table,
+ *   - drivers/iommu/amd/init.c|2815| <<free_iommu_resources>> amd_iommu_dev_table = NULL;
+ *   - drivers/iommu/amd/init.c|2960| <<early_amd_iommu_init>> amd_iommu_dev_table = (void *)__get_free_pages(
+ *   - drivers/iommu/amd/init.c|2963| <<early_amd_iommu_init>> if (amd_iommu_dev_table == NULL)
+ *   - drivers/iommu/amd/iommu.c|185| <<clone_alias>> memcpy(amd_iommu_dev_table[alias].data,
+ *   - drivers/iommu/amd/iommu.c|186| <<clone_alias>> amd_iommu_dev_table[devid].data,
+ *   - drivers/iommu/amd/iommu.c|187| <<clone_alias>> sizeof(amd_iommu_dev_table[alias].data));
+ *   - drivers/iommu/amd/iommu.c|365| <<iommu_ignore_device>> memset(&amd_iommu_dev_table[devid], 0, sizeof(struct dev_table_entry));
+ *   - drivers/iommu/amd/iommu.c|401| <<dump_dte_entry>> amd_iommu_dev_table[devid].data[i]);
+ *   - drivers/iommu/amd/iommu.c|1485| <<set_dte_entry>> flags = amd_iommu_dev_table[devid].data[1];
+ *   - drivers/iommu/amd/iommu.c|1529| <<set_dte_entry>> old_domid = amd_iommu_dev_table[devid].data[1] & DEV_DOMID_MASK;
+ *   - drivers/iommu/amd/iommu.c|1530| <<set_dte_entry>> amd_iommu_dev_table[devid].data[1] = flags;
+ *   - drivers/iommu/amd/iommu.c|1531| <<set_dte_entry>> amd_iommu_dev_table[devid].data[0] = pte_root;
+ *   - drivers/iommu/amd/iommu.c|1548| <<clear_dte_entry>> amd_iommu_dev_table[devid].data[0] = DTE_FLAG_V;
+ *   - drivers/iommu/amd/iommu.c|1551| <<clear_dte_entry>> amd_iommu_dev_table[devid].data[0] |= DTE_FLAG_TV;
+ *   - drivers/iommu/amd/iommu.c|1553| <<clear_dte_entry>> amd_iommu_dev_table[devid].data[1] &= DTE_FLAG_MASK;
+ *   - drivers/iommu/amd/iommu.c|2217| <<amd_iommu_set_dirty_tracking>> pte_root = amd_iommu_dev_table[dev_data->devid].data[0];
+ *   - drivers/iommu/amd/iommu.c|2223| <<amd_iommu_set_dirty_tracking>> amd_iommu_dev_table[dev_data->devid].data[0] = pte_root;
+ *   - drivers/iommu/amd/iommu.c|2760| <<set_dte_irq_entry>> dte = amd_iommu_dev_table[devid].data[2];
+ *   - drivers/iommu/amd/iommu.c|2767| <<set_dte_irq_entry>> amd_iommu_dev_table[devid].data[2] = dte;
+ */
 /*
  * Pointer to the device table which is shared by all AMD IOMMUs
  * it is indexed by the PCI device id or the HT unit id and contains
@@ -325,6 +364,15 @@ static void get_global_efr(void)
 		u64 tmp = iommu->features;
 		u64 tmp2 = iommu->features2;
 
+		/*
+		 * 在以下使用amd_iommu_list:
+		 *   - drivers/iommu/amd/init.c|181| <<global>> LIST_HEAD(amd_iommu_list);
+		 *   - drivers/iommu/amd/amd_iommu_types.h|485| <<for_each_iommu>> list_for_each_entry((iommu), &amd_iommu_list, list)
+		 *   - drivers/iommu/amd/amd_iommu_types.h|487| <<for_each_iommu_safe>> list_for_each_entry_safe((iommu), (next), &amd_iommu_list, list)
+		 *   - drivers/iommu/amd/init.c|358| <<get_global_efr>> if (list_is_first(&iommu->list, &amd_iommu_list)) {
+		 *   - drivers/iommu/amd/init.c|1701| <<init_iommu_one>> list_add_tail(&iommu->list, &amd_iommu_list);
+		 *   - drivers/iommu/amd/init.c|3309| <<amd_iommu_init>> if (ret && list_empty(&amd_iommu_list)) {
+		 */
 		if (list_is_first(&iommu->list, &amd_iommu_list)) {
 			amd_iommu_efr = tmp;
 			amd_iommu_efr2 = tmp2;
@@ -455,6 +503,11 @@ static void iommu_set_cwwb_range(struct amd_iommu *iommu)
 		    &entry, sizeof(entry));
 }
 
+/*
+ * 在以下使用iommu_set_device_table():
+ *   - drivers/iommu/amd/init.c|2612| <<early_enable_iommu>> iommu_set_device_table(iommu);
+ *   - drivers/iommu/amd/init.c|2666| <<early_enable_iommus>> iommu_set_device_table(iommu);
+ */
 /* Programs the physical address of the device table into the IOMMU hardware */
 static void iommu_set_device_table(struct amd_iommu *iommu)
 {
@@ -516,7 +569,23 @@ static void iommu_disable(struct amd_iommu *iommu)
 	iommu_feature_disable(iommu, CONTROL_EVT_LOG_EN);
 
 	/* Disable IOMMU GA_LOG */
+	/*
+	 * 在以下使用CONTROL_GALOG_EN:
+	 *   - drivers/iommu/amd/init.c|572| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|780| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|787| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|957| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|2768| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 */
 	iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	/*
+	 * 在以下使用CONTROL_GAINT_EN:
+	 *   - drivers/iommu/amd/init.c|573| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|781| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|786| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|956| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|2769| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 */
 	iommu_feature_disable(iommu, CONTROL_GAINT_EN);
 
 	/* Disable IOMMU hardware itself */
@@ -724,13 +793,45 @@ void amd_iommu_restart_ga_log(struct amd_iommu *iommu)
 
 	pr_info_ratelimited("IOMMU GA Log restarting\n");
 
+	/*
+	 * 在以下使用CONTROL_GALOG_EN:
+	 *   - drivers/iommu/amd/init.c|572| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|780| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|787| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|957| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|2768| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 */
 	iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	/*
+	 * 在以下使用CONTROL_GAINT_EN:
+	 *   - drivers/iommu/amd/init.c|573| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|781| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|786| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|956| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|2769| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 */
 	iommu_feature_disable(iommu, CONTROL_GAINT_EN);
 
 	writel(MMIO_STATUS_GALOG_OVERFLOW_MASK,
 	       iommu->mmio_base + MMIO_STATUS_OFFSET);
 
+	/*
+	 * 在以下使用CONTROL_GAINT_EN:
+	 *   - drivers/iommu/amd/init.c|573| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|781| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|786| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|956| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|2769| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 */
 	iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	/*
+	 * 在以下使用CONTROL_GALOG_EN:
+	 *   - drivers/iommu/amd/init.c|572| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|780| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|787| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|957| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|2768| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 */
 	iommu_feature_enable(iommu, CONTROL_GALOG_EN);
 }
 
@@ -875,17 +976,41 @@ static void __init free_ppr_log(struct amd_iommu *iommu)
 static void free_ga_log(struct amd_iommu *iommu)
 {
 #ifdef CONFIG_IRQ_REMAP
+	/*
+	 * 在以下使用amd_iommu->ga_log:
+	 *   - drivers/iommu/amd/init.c|931| <<free_ga_log>> free_pages((unsigned long )iommu->ga_log, get_order(GA_LOG_SIZE));
+	 *   - drivers/iommu/amd/init.c|942| <<iommu_ga_log_enable>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/init.c|945| <<iommu_ga_log_enable>> entry = iommu_virt_to_phys(iommu->ga_log) | GA_LOG_SIZE_512;
+	 *   - drivers/iommu/amd/init.c|978| <<iommu_init_ga_log>> iommu->ga_log = (u8 *)__get_free_pages(GFP_KERNEL | __GFP_ZERO,
+	 *   - drivers/iommu/amd/init.c|980| <<iommu_init_ga_log>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/iommu.c|704| <<iommu_poll_ga_log>> if (iommu->ga_log == NULL)
+	 *   - drivers/iommu/amd/iommu.c|714| <<iommu_poll_ga_log>> raw = (u64 *)(iommu->ga_log + head);
+	 */
 	free_pages((unsigned long)iommu->ga_log, get_order(GA_LOG_SIZE));
 	free_pages((unsigned long)iommu->ga_log_tail, get_order(8));
 #endif
 }
 
+/*
+ * 在以下使用iommu_ga_log_enable():
+ *   - drivers/iommu/amd/init.c|2802| <<enable_iommus_vapic>> iommu_ga_log_enable(iommu))
+ */
 static int iommu_ga_log_enable(struct amd_iommu *iommu)
 {
 #ifdef CONFIG_IRQ_REMAP
 	u32 status, i;
 	u64 entry;
 
+	/*
+	 * 在以下使用amd_iommu->ga_log:
+	 *   - drivers/iommu/amd/init.c|931| <<free_ga_log>> free_pages((unsigned long )iommu->ga_log, get_order(GA_LOG_SIZE));
+	 *   - drivers/iommu/amd/init.c|942| <<iommu_ga_log_enable>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/init.c|945| <<iommu_ga_log_enable>> entry = iommu_virt_to_phys(iommu->ga_log) | GA_LOG_SIZE_512;
+	 *   - drivers/iommu/amd/init.c|978| <<iommu_init_ga_log>> iommu->ga_log = (u8 *)__get_free_pages(GFP_KERNEL | __GFP_ZERO,
+	 *   - drivers/iommu/amd/init.c|980| <<iommu_init_ga_log>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/iommu.c|704| <<iommu_poll_ga_log>> if (iommu->ga_log == NULL)
+	 *   - drivers/iommu/amd/iommu.c|714| <<iommu_poll_ga_log>> raw = (u64 *)(iommu->ga_log + head);
+	 */
 	if (!iommu->ga_log)
 		return -EINVAL;
 
@@ -900,7 +1025,23 @@ static int iommu_ga_log_enable(struct amd_iommu *iommu)
 	writel(0x00, iommu->mmio_base + MMIO_GA_TAIL_OFFSET);
 
 
+	/*
+	 * 在以下使用CONTROL_GAINT_EN:
+	 *   - drivers/iommu/amd/init.c|573| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|781| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|786| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|956| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	 *   - drivers/iommu/amd/init.c|2769| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+	 */
 	iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+	/*
+	 * 在以下使用CONTROL_GALOG_EN:
+	 *   - drivers/iommu/amd/init.c|572| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|780| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|787| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|957| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+	 *   - drivers/iommu/amd/init.c|2768| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+	 */
 	iommu_feature_enable(iommu, CONTROL_GALOG_EN);
 
 	for (i = 0; i < LOOP_TIMEOUT; ++i) {
@@ -922,6 +1063,16 @@ static int iommu_init_ga_log(struct amd_iommu *iommu)
 	if (!AMD_IOMMU_GUEST_IR_VAPIC(amd_iommu_guest_ir))
 		return 0;
 
+	/*
+	 * 在以下使用amd_iommu->ga_log:
+	 *   - drivers/iommu/amd/init.c|931| <<free_ga_log>> free_pages((unsigned long )iommu->ga_log, get_order(GA_LOG_SIZE));
+	 *   - drivers/iommu/amd/init.c|942| <<iommu_ga_log_enable>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/init.c|945| <<iommu_ga_log_enable>> entry = iommu_virt_to_phys(iommu->ga_log) | GA_LOG_SIZE_512;
+	 *   - drivers/iommu/amd/init.c|978| <<iommu_init_ga_log>> iommu->ga_log = (u8 *)__get_free_pages(GFP_KERNEL | __GFP_ZERO,
+	 *   - drivers/iommu/amd/init.c|980| <<iommu_init_ga_log>> if (!iommu->ga_log)
+	 *   - drivers/iommu/amd/iommu.c|704| <<iommu_poll_ga_log>> if (iommu->ga_log == NULL)
+	 *   - drivers/iommu/amd/iommu.c|714| <<iommu_poll_ga_log>> raw = (u64 *)(iommu->ga_log + head);
+	 */
 	iommu->ga_log = (u8 *)__get_free_pages(GFP_KERNEL | __GFP_ZERO,
 					get_order(GA_LOG_SIZE));
 	if (!iommu->ga_log)
@@ -1650,6 +1801,10 @@ static void amd_iommu_ats_write_check_workaround(struct amd_iommu *iommu)
  * together and also allocates the command buffer and programs the
  * hardware. It does NOT enable the IOMMU. This is done afterwards.
  */
+/*
+ * 在以下使用init_iommu_one():
+ *   - drivers/iommu/amd/init.c|1898| <<init_iommu_all>> ret = init_iommu_one(iommu, h);
+ */
 static int __init init_iommu_one(struct amd_iommu *iommu, struct ivhd_header *h)
 {
 	struct amd_iommu_pci_seg *pci_seg;
@@ -1663,6 +1818,15 @@ static int __init init_iommu_one(struct amd_iommu *iommu, struct ivhd_header *h)
 	iommu->cmd_sem_val = 0;
 
 	/* Add IOMMU to internal data structures */
+	/*
+	 * 在以下使用amd_iommu_list:
+	 *   - drivers/iommu/amd/init.c|181| <<global>> LIST_HEAD(amd_iommu_list);
+	 *   - drivers/iommu/amd/amd_iommu_types.h|485| <<for_each_iommu>> list_for_each_entry((iommu), &amd_iommu_list, list)
+	 *   - drivers/iommu/amd/amd_iommu_types.h|487| <<for_each_iommu_safe>> list_for_each_entry_safe((iommu), (next), &amd_iommu_list, list)
+	 *   - drivers/iommu/amd/init.c|358| <<get_global_efr>> if (list_is_first(&iommu->list, &amd_iommu_list)) {
+	 *   - drivers/iommu/amd/init.c|1701| <<init_iommu_one>> list_add_tail(&iommu->list, &amd_iommu_list);
+	 *   - drivers/iommu/amd/init.c|3309| <<amd_iommu_init>> if (ret && list_empty(&amd_iommu_list)) {
+	 */
 	list_add_tail(&iommu->list, &amd_iommu_list);
 	iommu->index = amd_iommus_present++;
 
@@ -2628,6 +2792,11 @@ static void early_enable_iommu(struct amd_iommu *iommu)
  * the old content of device table entries. Not this case or copy failed,
  * just continue as normal kernel does.
  */
+/*
+ * 在以下使用early_enable_iommus():
+ *   - drivers/iommu/amd/init.c|2781| <<enable_iommus>> early_enable_iommus();
+ *   - drivers/iommu/amd/init.c|3172| <<state_next>> early_enable_iommus();
+ */
 static void early_enable_iommus(void)
 {
 	struct amd_iommu *iommu;
@@ -2694,7 +2863,23 @@ static void enable_iommus_vapic(void)
 		if (!(status & MMIO_STATUS_GALOG_RUN_MASK))
 			continue;
 
+		/*
+		 * 在以下使用CONTROL_GALOG_EN:
+		 *   - drivers/iommu/amd/init.c|572| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+		 *   - drivers/iommu/amd/init.c|780| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+		 *   - drivers/iommu/amd/init.c|787| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+		 *   - drivers/iommu/amd/init.c|957| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GALOG_EN);
+		 *   - drivers/iommu/amd/init.c|2768| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+		 */
 		iommu_feature_disable(iommu, CONTROL_GALOG_EN);
+		/*
+		 * 在以下使用CONTROL_GAINT_EN:
+		 *   - drivers/iommu/amd/init.c|573| <<iommu_disable>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+		 *   - drivers/iommu/amd/init.c|781| <<amd_iommu_restart_ga_log>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+		 *   - drivers/iommu/amd/init.c|786| <<amd_iommu_restart_ga_log>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+		 *   - drivers/iommu/amd/init.c|956| <<iommu_ga_log_enable>> iommu_feature_enable(iommu, CONTROL_GAINT_EN);
+		 *   - drivers/iommu/amd/init.c|2769| <<enable_iommus_vapic>> iommu_feature_disable(iommu, CONTROL_GAINT_EN);
+		 */
 		iommu_feature_disable(iommu, CONTROL_GAINT_EN);
 
 		/*
@@ -2743,6 +2928,11 @@ static void enable_iommus_vapic(void)
 
 static void enable_iommus(void)
 {
+	/*
+	 * 在以下使用early_enable_iommus():
+	 *   - drivers/iommu/amd/init.c|2781| <<enable_iommus>> early_enable_iommus();
+	 *   - drivers/iommu/amd/init.c|3172| <<state_next>> early_enable_iommus();
+	 */
 	early_enable_iommus();
 	enable_iommus_vapic();
 	enable_iommus_v2();
@@ -3134,6 +3324,11 @@ static int __init state_next(void)
 		}
 		break;
 	case IOMMU_ACPI_FINISHED:
+		/*
+		 * 在以下使用early_enable_iommus():
+		 *   - drivers/iommu/amd/init.c|2781| <<enable_iommus>> early_enable_iommus();
+		 *   - drivers/iommu/amd/init.c|3172| <<state_next>> early_enable_iommus();
+		 */
 		early_enable_iommus();
 		x86_platform.iommu_shutdown = disable_iommus;
 		init_state = IOMMU_ENABLED;
@@ -3256,6 +3451,15 @@ static int __init amd_iommu_init(void)
 
 	ret = iommu_go_to_state(IOMMU_INITIALIZED);
 #ifdef CONFIG_GART_IOMMU
+	/*
+	 * 在以下使用amd_iommu_list:
+	 *   - drivers/iommu/amd/init.c|181| <<global>> LIST_HEAD(amd_iommu_list);
+	 *   - drivers/iommu/amd/amd_iommu_types.h|485| <<for_each_iommu>> list_for_each_entry((iommu), &amd_iommu_list, list)
+	 *   - drivers/iommu/amd/amd_iommu_types.h|487| <<for_each_iommu_safe>> list_for_each_entry_safe((iommu), (next), &amd_iommu_list, list)
+	 *   - drivers/iommu/amd/init.c|358| <<get_global_efr>> if (list_is_first(&iommu->list, &amd_iommu_list)) {
+	 *   - drivers/iommu/amd/init.c|1701| <<init_iommu_one>> list_add_tail(&iommu->list, &amd_iommu_list);
+	 *   - drivers/iommu/amd/init.c|3309| <<amd_iommu_init>> if (ret && list_empty(&amd_iommu_list)) {
+	 */
 	if (ret && list_empty(&amd_iommu_list)) {
 		/*
 		 * We failed to initialize the AMD IOMMU - try fallback
diff --git a/drivers/iommu/amd/io_pgtable.c b/drivers/iommu/amd/io_pgtable.c
index 1039e8f73060..8c5f9b162007 100644
--- a/drivers/iommu/amd/io_pgtable.c
+++ b/drivers/iommu/amd/io_pgtable.c
@@ -148,6 +148,10 @@ void amd_iommu_domain_set_pgtable(struct protection_domain *domain,
  * another level increases the size of the address space by 9 bits to a size up
  * to 64 bits.
  */
+/*
+ * 在以下使用increase_address_space():
+ *   - drivers/iommu/amd/io_pgtable.c|212| <<alloc_pte>> if (!increase_address_space(domain, address, gfp))
+ */
 static bool increase_address_space(struct protection_domain *domain,
 				   unsigned long address,
 				   gfp_t gfp)
@@ -215,6 +219,11 @@ static u64 *alloc_pte(struct protection_domain *domain,
 
 
 	level   = domain->iop.mode - 1;
+	/*
+	 * struct protection_domain *domain:
+	 * -> struct amd_io_pgtable iop;
+	 *    -> u64 *root;
+	 */
 	pte     = &domain->iop.root[PM_LEVEL_INDEX(level, address)];
 	address = PAGE_SIZE_ALIGN(address, page_size);
 	end_lvl = PAGE_SIZE_LEVEL(page_size);
@@ -355,6 +364,66 @@ static void free_clear_pte(u64 *pte, u64 pteval, struct list_head *freelist)
 	free_sub_pt(pt, mode, freelist);
 }
 
+/*
+ * /usr/share/bcc/tools/stackcount -K  -T 'iommu_v1_unmap_pages'
+ *
+ * amd_iommu_map_pages
+  __iommu_map
+  iommu_map_atomic
+  __iommu_dma_map
+  iommu_dma_map_page
+  virtqueue_add_packed
+  virtqueue_add_inbuf_ctx
+  try_fill_recv
+  virtnet_receive
+  virtnet_poll
+  __napi_poll
+  net_rx_action
+  handle_softirqs
+  do_softirq
+  __local_bh_enable_ip
+  ip_finish_output2
+  __ip_queue_xmit
+  __tcp_transmit_skb
+  tcp_write_xmit
+  tcp_sendmsg_locked
+  tcp_sendmsg
+  __sock_sendmsg
+  sock_write_iter
+  new_sync_write
+  vfs_write
+  ksys_write
+  do_syscall_64
+  entry_SYSCALL_64_after_hwframe
+
+  amd_iommu_map_pages
+  __iommu_map
+  iommu_map_atomic
+  __iommu_dma_map
+  iommu_dma_map_page
+  virtqueue_add_indirect_packed
+  virtqueue_add_packed
+  virtqueue_add_outbuf
+  start_xmit
+  xmit_one.constprop.0
+  dev_hard_start_xmit
+  sch_direct_xmit
+  __dev_xmit_skb
+  __dev_queue_xmit
+  ip_finish_output2
+  __ip_queue_xmit
+  __tcp_transmit_skb
+  tcp_write_xmit
+  tcp_sendmsg_locked
+  tcp_sendmsg
+  __sock_sendmsg
+  sock_write_iter
+  new_sync_write
+  vfs_write
+  ksys_write
+  do_syscall_64
+  entry_SYSCALL_64_after_hwframe
+ */
 /*
  * Generic mapping functions. It maps a physical address into a DMA
  * address space. It allocates the page table pages if necessary.
@@ -437,6 +506,60 @@ static int iommu_v1_map_pages(struct io_pgtable_ops *ops, unsigned long iova,
 	return ret;
 }
 
+/*
+ * /usr/share/bcc/tools/stackcount -K  -T  'iommu_v1_unmap_pages'
+ *
+ * iommu_v1_unmap_pages
+ * amd_iommu_unmap_pages
+ * __iommu_unmap
+ * __iommu_dma_unmap
+ * iommu_dma_unmap_page
+ * detach_buf_packed
+ * virtqueue_get_buf_ctx_packed
+ * free_old_xmit_skbs
+ * virtnet_poll_tx
+ * __napi_poll
+ * net_rx_action
+ * handle_softirqs
+ * __irq_exit_rcu
+ * common_interrupt
+ * asm_common_interrupt
+ * native_safe_halt
+  default_idle
+  default_idle_call
+  cpuidle_idle_call
+  do_idle
+  cpu_startup_entry
+  secondary_startup_64_no_verify
+ *
+ * iommu_v1_unmap_pages
+  amd_iommu_unmap_pages
+  __iommu_unmap
+  __iommu_dma_unmap
+  iommu_dma_unmap_page
+  detach_buf_packed
+  virtqueue_get_buf_ctx_packed
+  virtnet_receive
+  virtnet_poll
+  __napi_poll
+  net_rx_action
+  handle_softirqs
+  do_softirq
+  __local_bh_enable_ip
+  ip_finish_output2
+  __ip_queue_xmit
+  __tcp_transmit_skb
+  tcp_write_xmit
+  tcp_sendmsg_locked
+  tcp_sendmsg
+  __sock_sendmsg
+  sock_write_iter
+  new_sync_write
+  vfs_write
+  ksys_write
+  do_syscall_64
+  entry_SYSCALL_64_after_hwframe
+ */
 static unsigned long iommu_v1_unmap_pages(struct io_pgtable_ops *ops,
 					  unsigned long iova,
 					  size_t pgsize, size_t pgcount,
diff --git a/drivers/iommu/amd/iommu.c b/drivers/iommu/amd/iommu.c
index 097e8163f4cf..a2b03766c593 100644
--- a/drivers/iommu/amd/iommu.c
+++ b/drivers/iommu/amd/iommu.c
@@ -693,6 +693,10 @@ int amd_iommu_register_ga_log_notifier(int (*notifier)(u32))
 }
 EXPORT_SYMBOL(amd_iommu_register_ga_log_notifier);
 
+/*
+ * 在以下使用iommu_poll_ga_log():
+ *   - drivers/iommu/amd/iommu.c|785| <<amd_iommu_int_thread>> iommu_poll_ga_log(iommu);
+ */
 static void iommu_poll_ga_log(struct amd_iommu *iommu)
 {
 	u32 head, tail, cnt = 0;
@@ -707,6 +711,16 @@ static void iommu_poll_ga_log(struct amd_iommu *iommu)
 		volatile u64 *raw;
 		u64 log_entry;
 
+		/*
+		 * 在以下使用amd_iommu->ga_log:
+		 *   - drivers/iommu/amd/init.c|931| <<free_ga_log>> free_pages((unsigned long )iommu->ga_log, get_order(GA_LOG_SIZE));
+		 *   - drivers/iommu/amd/init.c|942| <<iommu_ga_log_enable>> if (!iommu->ga_log)
+		 *   - drivers/iommu/amd/init.c|945| <<iommu_ga_log_enable>> entry = iommu_virt_to_phys(iommu->ga_log) | GA_LOG_SIZE_512;
+		 *   - drivers/iommu/amd/init.c|978| <<iommu_init_ga_log>> iommu->ga_log = (u8 *)__get_free_pages(GFP_KERNEL | __GFP_ZERO,
+		 *   - drivers/iommu/amd/init.c|980| <<iommu_init_ga_log>> if (!iommu->ga_log)
+		 *   - drivers/iommu/amd/iommu.c|704| <<iommu_poll_ga_log>> if (iommu->ga_log == NULL)
+		 *   - drivers/iommu/amd/iommu.c|714| <<iommu_poll_ga_log>> raw = (u64 *)(iommu->ga_log + head);
+		 */
 		raw = (u64 *)(iommu->ga_log + head);
 		cnt++;
 
@@ -1460,6 +1474,11 @@ static void free_gcr3_table(struct protection_domain *domain)
 	free_page((unsigned long)domain->gcr3_tbl);
 }
 
+/*
+ * 在以下使用set_dte_entry():
+ *   - drivers/iommu/amd/iommu.c|1576| <<do_attach>> set_dte_entry(dev_data->devid, domain,
+ *   - drivers/iommu/amd/iommu.c|1835| <<update_device_table>> set_dte_entry(dev_data->devid, domain,
+ */
 static void set_dte_entry(u16 devid, struct protection_domain *domain,
 			  bool ats, bool ppr)
 {
@@ -1572,6 +1591,11 @@ static void do_attach(struct iommu_dev_data *dev_data,
 	domain->dev_iommu[iommu->index] += 1;
 	domain->dev_cnt                 += 1;
 
+	/*
+	 * 在以下使用set_dte_entry():
+	 *   - drivers/iommu/amd/iommu.c|1576| <<do_attach>> set_dte_entry(dev_data->devid, domain,
+	 *   - drivers/iommu/amd/iommu.c|1835| <<update_device_table>> set_dte_entry(dev_data->devid, domain,
+	 */
 	/* Update device table */
 	set_dte_entry(dev_data->devid, domain,
 		      ats, dev_data->iommu_v2);
@@ -1832,6 +1856,11 @@ static void update_device_table(struct protection_domain *domain)
 	struct iommu_dev_data *dev_data;
 
 	list_for_each_entry(dev_data, &domain->dev_list, list) {
+		/*
+		 * 在以下使用set_dte_entry():
+		 *   - drivers/iommu/amd/iommu.c|1576| <<do_attach>> set_dte_entry(dev_data->devid, domain,
+		 *   - drivers/iommu/amd/iommu.c|1835| <<update_device_table>> set_dte_entry(dev_data->devid, domain,
+		 */
 		set_dte_entry(dev_data->devid, domain,
 			      dev_data->ats.enabled, dev_data->iommu_v2);
 		clone_aliases(dev_data->pdev);
@@ -2052,6 +2081,15 @@ static void amd_iommu_detach_device(struct iommu_domain *dom,
 	iommu_completion_wait(iommu);
 }
 
+/*
+ * 在以下调用:
+ *   - drivers/iommu/iommu.c|1993| <<__iommu_attach_device>> if (unlikely(domain->ops->attach_dev == NULL))
+ *   - drivers/iommu/iommu.c|1996| <<__iommu_attach_device>> ret = domain->ops->attach_dev(domain, dev);
+ *
+ *
+ * 在以下使用amd_iommu_attach_device():
+ *   - drivers/iommu/amd/iommu.c|2397| <<global>> .attach_dev = amd_iommu_attach_device,
+ */
 static int amd_iommu_attach_device(struct iommu_domain *dom,
 				   struct device *dev)
 {
@@ -2409,6 +2447,10 @@ const struct iommu_ops amd_iommu_ops = {
  *****************************************************************************/
 
 /* IOMMUv2 specific functions */
+/*
+ * 在以下使用amd_iommu_register_ppr_notifier():
+ *   - drivers/iommu/amd/iommu_v2.c|947| <<amd_iommu_v2_init>> amd_iommu_register_ppr_notifier(&ppr_nb);
+ */
 int amd_iommu_register_ppr_notifier(struct notifier_block *nb)
 {
 	return atomic_notifier_chain_register(&ppr_notifier, nb);
diff --git a/drivers/iommu/amd/iommu_v2.c b/drivers/iommu/amd/iommu_v2.c
index cb2632521e7b..ee44f03c0ab7 100644
--- a/drivers/iommu/amd/iommu_v2.c
+++ b/drivers/iommu/amd/iommu_v2.c
@@ -593,6 +593,11 @@ static int ppr_notifier(struct notifier_block *nb, unsigned long e, void *data)
 	return ret;
 }
 
+/*
+ * 在以下使用ppr_nb:
+ *   - drivers/iommu/amd/iommu_v2.c|947| <<amd_iommu_v2_init>> amd_iommu_register_ppr_notifier(&ppr_nb);
+ *   - drivers/iommu/amd/iommu_v2.c|965| <<amd_iommu_v2_exit>> amd_iommu_unregister_ppr_notifier(&ppr_nb);
+ */
 static struct notifier_block ppr_nb = {
 	.notifier_call = ppr_notifier,
 };
diff --git a/drivers/iommu/dma-iommu.c b/drivers/iommu/dma-iommu.c
index aa47d955de6b..7e3b61f0d7bb 100644
--- a/drivers/iommu/dma-iommu.c
+++ b/drivers/iommu/dma-iommu.c
@@ -436,6 +436,13 @@ static int dma_info_to_prot(enum dma_data_direction dir, bool coherent,
 	}
 }
 
+/*
+ * 在以下使用iommu_dma_alloc_iova():
+ *   - drivers/iommu/dma-iommu.c|534| <<__iommu_dma_map>> iova = iommu_dma_alloc_iova(domain, size, dma_mask, dev);
+ *   - drivers/iommu/dma-iommu.c|646| <<__iommu_dma_alloc_noncontiguous>> iova = iommu_dma_alloc_iova(domain, size, dev->coherent_dma_mask, dev);
+ *   - drivers/iommu/dma-iommu.c|1050| <<iommu_dma_map_sg>> iova = iommu_dma_alloc_iova(domain, iova_len, dma_get_mask(dev), dev);
+ *   - drivers/iommu/dma-iommu.c|1379| <<iommu_dma_get_msi_page>> iova = iommu_dma_alloc_iova(domain, size, dma_get_mask(dev), dev);
+ */
 static dma_addr_t iommu_dma_alloc_iova(struct iommu_domain *domain,
 		size_t size, u64 dma_limit, struct device *dev)
 {
@@ -531,6 +538,13 @@ static dma_addr_t __iommu_dma_map(struct device *dev, phys_addr_t phys,
 
 	size = iova_align(iovad, size + iova_off);
 
+	/*
+	 * 在以下使用iommu_dma_alloc_iova():
+	 *   - drivers/iommu/dma-iommu.c|534| <<__iommu_dma_map>> iova = iommu_dma_alloc_iova(domain, size, dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|646| <<__iommu_dma_alloc_noncontiguous>> iova = iommu_dma_alloc_iova(domain, size, dev->coherent_dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|1050| <<iommu_dma_map_sg>> iova = iommu_dma_alloc_iova(domain, iova_len, dma_get_mask(dev), dev);
+	 *   - drivers/iommu/dma-iommu.c|1379| <<iommu_dma_get_msi_page>> iova = iommu_dma_alloc_iova(domain, size, dma_get_mask(dev), dev);
+	 */
 	iova = iommu_dma_alloc_iova(domain, size, dma_mask, dev);
 	if (!iova)
 		return DMA_MAPPING_ERROR;
@@ -643,6 +657,13 @@ static struct page **__iommu_dma_alloc_noncontiguous(struct device *dev,
 		return NULL;
 
 	size = iova_align(iovad, size);
+	/*
+	 * 在以下使用iommu_dma_alloc_iova():
+	 *   - drivers/iommu/dma-iommu.c|534| <<__iommu_dma_map>> iova = iommu_dma_alloc_iova(domain, size, dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|646| <<__iommu_dma_alloc_noncontiguous>> iova = iommu_dma_alloc_iova(domain, size, dev->coherent_dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|1050| <<iommu_dma_map_sg>> iova = iommu_dma_alloc_iova(domain, iova_len, dma_get_mask(dev), dev);
+	 *   - drivers/iommu/dma-iommu.c|1379| <<iommu_dma_get_msi_page>> iova = iommu_dma_alloc_iova(domain, size, dma_get_mask(dev), dev);
+	 */
 	iova = iommu_dma_alloc_iova(domain, size, dev->coherent_dma_mask, dev);
 	if (!iova)
 		goto out_free_pages;
@@ -1047,6 +1068,13 @@ static int iommu_dma_map_sg(struct device *dev, struct scatterlist *sg,
 		prev = s;
 	}
 
+	/*
+	 * 在以下使用iommu_dma_alloc_iova():
+	 *   - drivers/iommu/dma-iommu.c|534| <<__iommu_dma_map>> iova = iommu_dma_alloc_iova(domain, size, dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|646| <<__iommu_dma_alloc_noncontiguous>> iova = iommu_dma_alloc_iova(domain, size, dev->coherent_dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|1050| <<iommu_dma_map_sg>> iova = iommu_dma_alloc_iova(domain, iova_len, dma_get_mask(dev), dev);
+	 *   - drivers/iommu/dma-iommu.c|1379| <<iommu_dma_get_msi_page>> iova = iommu_dma_alloc_iova(domain, size, dma_get_mask(dev), dev);
+	 */
 	iova = iommu_dma_alloc_iova(domain, iova_len, dma_get_mask(dev), dev);
 	if (!iova) {
 		ret = -ENOMEM;
@@ -1376,6 +1404,13 @@ static struct iommu_dma_msi_page *iommu_dma_get_msi_page(struct device *dev,
 	if (!msi_page)
 		return NULL;
 
+	/*
+	 * 在以下使用iommu_dma_alloc_iova():
+	 *   - drivers/iommu/dma-iommu.c|534| <<__iommu_dma_map>> iova = iommu_dma_alloc_iova(domain, size, dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|646| <<__iommu_dma_alloc_noncontiguous>> iova = iommu_dma_alloc_iova(domain, size, dev->coherent_dma_mask, dev);
+	 *   - drivers/iommu/dma-iommu.c|1050| <<iommu_dma_map_sg>> iova = iommu_dma_alloc_iova(domain, iova_len, dma_get_mask(dev), dev);
+	 *   - drivers/iommu/dma-iommu.c|1379| <<iommu_dma_get_msi_page>> iova = iommu_dma_alloc_iova(domain, size, dma_get_mask(dev), dev);
+	 */
 	iova = iommu_dma_alloc_iova(domain, size, dma_get_mask(dev), dev);
 	if (!iova)
 		goto out_free_page;
diff --git a/drivers/iommu/iommu.c b/drivers/iommu/iommu.c
index 4b0bf124eb25..2bad875c1d08 100644
--- a/drivers/iommu/iommu.c
+++ b/drivers/iommu/iommu.c
@@ -295,6 +295,15 @@ int iommu_probe_device(struct device *dev)
 	iommu_alloc_default_domain(group, dev);
 
 	if (group->default_domain) {
+		/*
+		 * 在以下使用__iommu_attach_device():
+		 *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+		 *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+		 *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+		 *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+		 *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+		 *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+		 */
 		ret = __iommu_attach_device(group->default_domain, dev);
 		if (ret) {
 			mutex_unlock(&group->mutex);
@@ -898,6 +907,15 @@ int iommu_group_add_device(struct iommu_group *group, struct device *dev)
 
 	mutex_lock(&group->mutex);
 	list_add_tail(&device->list, &group->devices);
+	/*
+	 * 在以下使用__iommu_attach_device():
+	 *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+	 *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+	 *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+	 */
 	if (group->domain  && !iommu_is_attach_deferred(group->domain, dev))
 		ret = __iommu_attach_device(group->domain, dev);
 	mutex_unlock(&group->mutex);
@@ -1753,6 +1771,15 @@ static int iommu_group_do_dma_attach(struct device *dev, void *data)
 	struct iommu_domain *domain = data;
 	int ret = 0;
 
+	/*
+	 * 在以下使用__iommu_attach_device():
+	 *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+	 *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+	 *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+	 */
 	if (!iommu_is_attach_deferred(domain, dev))
 		ret = __iommu_attach_device(domain, dev);
 
@@ -1985,6 +2012,15 @@ void iommu_domain_free(struct iommu_domain *domain)
 }
 EXPORT_SYMBOL_GPL(iommu_domain_free);
 
+/*
+ * 在以下使用__iommu_attach_device():
+ *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+ *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+ *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+ *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+ *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+ *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+ */
 static int __iommu_attach_device(struct iommu_domain *domain,
 				 struct device *dev)
 {
@@ -2031,6 +2067,15 @@ int iommu_deferred_attach(struct device *dev, struct iommu_domain *domain)
 {
 	const struct iommu_ops *ops = domain->ops;
 
+	/*
+	 * 在以下使用__iommu_attach_device():
+	 *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+	 *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+	 *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+	 */
 	if (ops->is_attach_deferred && ops->is_attach_deferred(domain, dev))
 		return __iommu_attach_device(domain, dev);
 
@@ -2322,6 +2367,15 @@ static int iommu_group_do_attach_device(struct device *dev, void *data)
 {
 	struct iommu_domain *domain = data;
 
+	/*
+	 * 在以下使用__iommu_attach_device():
+	 *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+	 *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+	 *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+	 */
 	return __iommu_attach_device(domain, dev);
 }
 
@@ -3238,6 +3292,15 @@ static int iommu_change_dev_def_domain(struct iommu_group *group,
 	if (ret)
 		goto free_new_domain;
 
+	/*
+	 * 在以下使用__iommu_attach_device():
+	 *   - drivers/iommu/iommu.c|298| <<iommu_probe_device>> ret = __iommu_attach_device(group->default_domain, dev);
+	 *   - drivers/iommu/iommu.c|902| <<iommu_group_add_device>> ret = __iommu_attach_device(group->domain, dev);
+	 *   - drivers/iommu/iommu.c|1757| <<iommu_group_do_dma_attach>> ret = __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2035| <<iommu_deferred_attach>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|2325| <<iommu_group_do_attach_device>> return __iommu_attach_device(domain, dev);
+	 *   - drivers/iommu/iommu.c|3241| <<iommu_change_dev_def_domain>> ret = __iommu_attach_device(group->default_domain, dev);
+	 */
 	ret = __iommu_attach_device(group->default_domain, dev);
 	if (ret)
 		goto free_new_domain;
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c b/drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c
index 7afada186d6d..44e818123a22 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c
@@ -279,6 +279,13 @@ static int mlx5e_ptp_napi_poll(struct napi_struct *napi, int budget)
 		}
 	}
 	if (test_bit(MLX5E_PTP_STATE_RX, c->state) && likely(budget)) {
+		/*
+		 * 在以下使用mlx5e_poll_rx_cq():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|282| <<mlx5e_ptp_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/trap.c|21| <<mlx5e_trap_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|174| <<mlx5e_napi_poll>> work_done = mlx5e_poll_rx_cq(&xskrq->cq, budget);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|177| <<mlx5e_napi_poll>> work_done += mlx5e_poll_rx_cq(&rq->cq, budget - work_done);
+		 */
 		work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
 		busy |= work_done == budget;
 		busy |= INDIRECT_CALL_2(rq->post_wqes,
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en/trap.c b/drivers/net/ethernet/mellanox/mlx5/core/en/trap.c
index 56dd2ad0a249..0ed2965da9b6 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en/trap.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en/trap.c
@@ -18,6 +18,13 @@ static int mlx5e_trap_napi_poll(struct napi_struct *napi, int budget)
 
 	ch_stats->poll++;
 
+	/*
+	 * 在以下使用mlx5e_poll_rx_cq():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|282| <<mlx5e_ptp_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en/trap.c|21| <<mlx5e_trap_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|174| <<mlx5e_napi_poll>> work_done = mlx5e_poll_rx_cq(&xskrq->cq, budget);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|177| <<mlx5e_napi_poll>> work_done += mlx5e_poll_rx_cq(&rq->cq, budget - work_done);
+	 */
 	work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
 	busy |= work_done == budget;
 	busy |= rq->post_wqes(rq);
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c b/drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c
index d0fc265cfefa..8b9e78be73fb 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c
@@ -710,6 +710,21 @@ bool mlx5e_poll_xdpsq_cq(struct mlx5e_cq *cq)
 		u16 wqe_counter, ci;
 		bool last_wqe;
 
+		/*
+		 * 在以下使用mlx5_cqwq_pop():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+		 */
 		mlx5_cqwq_pop(&cq->wq);
 
 		wqe_counter = be16_to_cpu(cqe->wqe_counter);
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c b/drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
index 255cf9f31ca8..7c0b5206fcfc 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
@@ -343,6 +343,12 @@ static void mlx5e_get_ringparam(struct net_device *dev,
 	mlx5e_ethtool_get_ringparam(priv, param, kernel_param);
 }
 
+/*
+ * 在以下使用mlx5e_ethtool_set_ringparam():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c|411| <<mlx5e_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rep.c|330| <<mlx5e_rep_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c|77| <<mlx5i_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+ */
 int mlx5e_ethtool_set_ringparam(struct mlx5e_priv *priv,
 				struct ethtool_ringparam *param)
 {
@@ -408,6 +414,12 @@ static int mlx5e_set_ringparam(struct net_device *dev,
 {
 	struct mlx5e_priv *priv = netdev_priv(dev);
 
+	/*
+	 * 在以下使用mlx5e_ethtool_set_ringparam():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c|411| <<mlx5e_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rep.c|330| <<mlx5e_rep_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c|77| <<mlx5i_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 */
 	return mlx5e_ethtool_set_ringparam(priv, param);
 }
 
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en_main.c b/drivers/net/ethernet/mellanox/mlx5/core/en_main.c
index 66ebc31b7a2d..6b799ab63648 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_main.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_main.c
@@ -664,6 +664,10 @@ static void mlx5e_init_xsk_buffs(struct mlx5e_rq *rq)
 	}
 }
 
+/*
+ * 在以下使用mlx5e_init_wqe_alloc_info():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|916| <<mlx5e_alloc_rq>> err = mlx5e_init_wqe_alloc_info(rq, node);
+ */
 static int mlx5e_init_wqe_alloc_info(struct mlx5e_rq *rq, int node)
 {
 	int wq_sz = mlx5_wq_cyc_get_size(&rq->wqe.wq);
@@ -821,6 +825,10 @@ static void mlx5e_rq_free_shampo(struct mlx5e_rq *rq)
 	mlx5e_rq_shampo_hd_free(rq);
 }
 
+/*
+ * 在以下使用mlx5e_alloc_rq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1330| <<mlx5e_open_rq>> err = mlx5e_alloc_rq(params, xsk, param, node, rq);
+ */
 static int mlx5e_alloc_rq(struct mlx5e_params *params,
 			  struct mlx5e_xsk_param *xsk,
 			  struct mlx5e_rq_param *rqp,
@@ -954,6 +962,16 @@ static int mlx5e_alloc_rq(struct mlx5e_params *params,
 
 	for (i = 0; i < wq_sz; i++) {
 		if (rq->wq_type == MLX5_WQ_TYPE_LINKED_LIST_STRIDING_RQ) {
+			/*
+			 * 在以下使用mlx5_wq_ll_get_wqe():
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+			 */
 			struct mlx5e_rx_wqe_ll *wqe =
 				mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
 			u32 byte_count =
@@ -1052,6 +1070,11 @@ static void mlx5e_free_rq(struct mlx5e_rq *rq)
 	mlx5_wq_destroy(&rq->wq_ctrl);
 }
 
+/*
+ * 在以下使用mlx5e_create_rq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1334| <<mlx5e_open_rq>> err = mlx5e_create_rq(rq, param, q_counter);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|3410| <<mlx5e_open_drop_rq>> err = mlx5e_create_rq(drop_rq, &rq_param, priv->drop_rq_q_counter);
+ */
 int mlx5e_create_rq(struct mlx5e_rq *rq, struct mlx5e_rq_param *param, u16 q_counter)
 {
 	struct mlx5_core_dev *mdev = rq->mdev;
@@ -1134,6 +1157,21 @@ static void mlx5e_flush_rq_cq(struct mlx5e_rq *rq)
 	struct mlx5_cqwq *cqwq = &rq->cq.wq;
 	struct mlx5_cqe64 *cqe;
 
+	/*
+	 * 在以下使用mlx5_cqwq_pop():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+	 */
 	if (test_bit(MLX5E_RQ_STATE_MINI_CQE_ENHANCED, &rq->state)) {
 		while ((cqe = mlx5_cqwq_get_cqe_enahnced_comp(cqwq)))
 			mlx5_cqwq_pop(cqwq);
@@ -1257,6 +1295,11 @@ void mlx5e_free_rx_missing_descs(struct mlx5e_rq *rq)
 	rq->mpwqe.umr_completed = 0;
 }
 
+/*
+ * 在以下使用mlx5e_free_rx_descs():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1174| <<mlx5e_flush_rq>> mlx5e_free_rx_descs(rq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1395| <<mlx5e_close_rq>> mlx5e_free_rx_descs(rq);
+ */
 void mlx5e_free_rx_descs(struct mlx5e_rq *rq)
 {
 	__be16 wqe_ix_be;
@@ -1272,8 +1315,25 @@ void mlx5e_free_rx_descs(struct mlx5e_rq *rq)
 
 			wqe_ix_be = *wq->tail_next;
 			wqe_ix    = be16_to_cpu(wqe_ix_be);
+			/*
+			 * 在以下使用mlx5_wq_ll_get_wqe():
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+			 */
 			wqe       = mlx5_wq_ll_get_wqe(wq, wqe_ix);
 			rq->dealloc_wqe(rq, wqe_ix);
+			/*
+			 * 在以下使用mlx5_wq_ll_pop():
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1292| <<mlx5e_free_rx_descs>> mlx5_wq_ll_pop(wq, wqe_ix_be,
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2104| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2541| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+			 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2628| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+			 */
 			mlx5_wq_ll_pop(wq, wqe_ix_be,
 				       &wqe->next.next_wqe_index);
 		}
@@ -1302,6 +1362,13 @@ void mlx5e_free_rx_descs(struct mlx5e_rq *rq)
 
 }
 
+/*
+ * 在以下使用mlx5e_open_rq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|732| <<mlx5e_ptp_open_rq>> return mlx5e_open_rq(params, rq_param, NULL, node, q_counter, &c->rq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/trap.c|92| <<mlx5e_open_trap_rq>> err = mlx5e_open_rq(&t->params, rq_param, NULL, node, q_counter, rq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/xsk/setup.c|103| <<mlx5e_open_xsk_rq>> err = mlx5e_open_rq(params, rq_params, xsk, cpu_to_node(c->cpu), q_counter, xskrq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|2387| <<mlx5e_open_rxq_rq>> return mlx5e_open_rq(params, rq_params, NULL, cpu_to_node(c->cpu), q_counter, &c->rq);
+ */
 int mlx5e_open_rq(struct mlx5e_params *params, struct mlx5e_rq_param *param,
 		  struct mlx5e_xsk_param *xsk, int node, u16 q_counter,
 		  struct mlx5e_rq *rq)
@@ -1499,6 +1566,10 @@ static void mlx5e_free_icosq_db(struct mlx5e_icosq *sq)
 	kvfree(sq->db.wqe_info);
 }
 
+/*
+ * 在以下使用mlx5e_alloc_icosq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1916| <<mlx5e_open_icosq>> err = mlx5e_alloc_icosq(c, param, sq, recover_work_func);
+ */
 static int mlx5e_alloc_icosq_db(struct mlx5e_icosq *sq, int numa)
 {
 	int wq_sz = mlx5_wq_cyc_get_size(&sq->wq);
@@ -2033,6 +2104,14 @@ static int mlx5e_alloc_cq_common(struct mlx5_core_dev *mdev,
 	int err;
 	u32 i;
 
+	/*
+	 * 在以下使用mlx5_cqwq_create():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|2051| <<mlx5e_alloc_cq_common>> err = mlx5_cqwq_create(mdev, &param->wq, param->cqc, &cq->wq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|430| <<mlx5_fpga_conn_create_cq>> err = mlx5_cqwq_create(mdev, &wqp, temp_cqc, &conn->cq.wq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|56| <<mlx5_aso_alloc_cq>> err = mlx5_cqwq_create(mdev, &param, cqc_data, &cq->wq, &cq->wq_ctrl);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|772| <<hws_send_ring_alloc_cq>> err = mlx5_cqwq_create(mdev, &param, cqc_data, &cq->wq, &cq->wq_ctrl);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|1083| <<dr_create_cq>> err = mlx5_cqwq_create(mdev, &wqp, temp_cqc, &cq->wq,
+	 */
 	err = mlx5_cqwq_create(mdev, &param->wq, param->cqc, &cq->wq,
 			       &cq->wq_ctrl);
 	if (err)
@@ -5613,6 +5692,12 @@ int mlx5e_update_nic_rx(struct mlx5e_priv *priv)
 	return mlx5e_refresh_tirs(priv, false, false);
 }
 
+/*
+ * 在以下使用mlx5e_nic_profile:
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|5697| <<mlx5e_get_pf_num_tirs>> + mlx5e_profile_max_num_channels(mdev, &mlx5e_nic_profile);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6064| <<mlx5e_netdev_attach_nic_profile>> mlx5e_netdev_change_profile(priv, &mlx5e_nic_profile, NULL);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6165| <<_mlx5e_probe>> const struct mlx5e_profile *profile = &mlx5e_nic_profile;
+ */
 static const struct mlx5e_profile mlx5e_nic_profile = {
 	.init		   = mlx5e_nic_init,
 	.cleanup	   = mlx5e_nic_cleanup,
@@ -5678,6 +5763,12 @@ int mlx5e_get_pf_num_tirs(struct mlx5_core_dev *mdev)
 	/* Indirect TIRS: 2 sets of TTCs (inner + outer steering)
 	 * and 1 set of direct TIRS
 	 */
+	/*
+	 * 在以下使用mlx5e_nic_profile:
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|5697| <<mlx5e_get_pf_num_tirs>> + mlx5e_profile_max_num_channels(mdev, &mlx5e_nic_profile);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6064| <<mlx5e_netdev_attach_nic_profile>> mlx5e_netdev_change_profile(priv, &mlx5e_nic_profile, NULL);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6165| <<_mlx5e_probe>> const struct mlx5e_profile *profile = &mlx5e_nic_profile;
+	 */
 	return 2 * MLX5E_NUM_INDIR_TIRS
 		+ mlx5e_profile_max_num_channels(mdev, &mlx5e_nic_profile);
 }
@@ -6046,6 +6137,12 @@ int mlx5e_netdev_change_profile(struct mlx5e_priv *priv,
 
 void mlx5e_netdev_attach_nic_profile(struct mlx5e_priv *priv)
 {
+	/*
+	 * 在以下使用mlx5e_nic_profile:
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|5697| <<mlx5e_get_pf_num_tirs>> + mlx5e_profile_max_num_channels(mdev, &mlx5e_nic_profile);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6064| <<mlx5e_netdev_attach_nic_profile>> mlx5e_netdev_change_profile(priv, &mlx5e_nic_profile, NULL);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6165| <<_mlx5e_probe>> const struct mlx5e_profile *profile = &mlx5e_nic_profile;
+	 */
 	mlx5e_netdev_change_profile(priv, &mlx5e_nic_profile, NULL);
 }
 
@@ -6147,6 +6244,12 @@ static int mlx5e_suspend(struct auxiliary_device *adev, pm_message_t state)
 static int _mlx5e_probe(struct auxiliary_device *adev)
 {
 	struct mlx5_adev *edev = container_of(adev, struct mlx5_adev, adev);
+	/*
+	 * 在以下使用mlx5e_nic_profile:
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|5697| <<mlx5e_get_pf_num_tirs>> + mlx5e_profile_max_num_channels(mdev, &mlx5e_nic_profile);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6064| <<mlx5e_netdev_attach_nic_profile>> mlx5e_netdev_change_profile(priv, &mlx5e_nic_profile, NULL);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|6165| <<_mlx5e_probe>> const struct mlx5e_profile *profile = &mlx5e_nic_profile;
+	 */
 	const struct mlx5e_profile *profile = &mlx5e_nic_profile;
 	struct mlx5_core_dev *mdev = edev->mdev;
 	struct mlx5e_dev *mlx5e_dev;
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en_rep.c b/drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
index 79e61c761d8e..bfdaef37b102 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
@@ -327,6 +327,12 @@ mlx5e_rep_set_ringparam(struct net_device *dev,
 {
 	struct mlx5e_priv *priv = netdev_priv(dev);
 
+	/*
+	 * 在以下使用mlx5e_ethtool_set_ringparam():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c|411| <<mlx5e_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rep.c|330| <<mlx5e_rep_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c|77| <<mlx5i_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 */
 	return mlx5e_ethtool_set_ringparam(priv, param);
 }
 
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c b/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
index 362d8ce451a0..233e1abfd599 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
@@ -72,6 +72,16 @@ static void mlx5e_handle_rx_cqe(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe);
 static void mlx5e_handle_rx_cqe_mpwrq(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe);
 static void mlx5e_handle_rx_cqe_mpwrq_shampo(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe);
 
+/*
+ * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ */
+
 const struct mlx5e_rx_handlers mlx5e_rx_handlers_nic = {
 	.handle_rx_cqe       = mlx5e_handle_rx_cqe,
 	.handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
@@ -97,6 +107,12 @@ static void mlx5e_read_enhanced_title_slot(struct mlx5e_rq *rq,
 	if (likely(test_bit(MLX5E_RQ_STATE_MINI_CQE_HW_STRIDX, &rq->state)))
 		return;
 
+	/*
+	 * 在以下使用mpwrq_get_cqe_stride_index():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|111| <<mlx5e_read_enhanced_title_slot>> cqd->wqe_counter = mpwrq_get_cqe_stride_index(title) +
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2053| <<mlx5e_handle_rx_cqe_mpwrq_rep>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2569| <<mlx5e_handle_rx_cqe_mpwrq>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+	 */
 	if (rq->wq_type == MLX5_WQ_TYPE_LINKED_LIST_STRIDING_RQ)
 		cqd->wqe_counter = mpwrq_get_cqe_stride_index(title) +
 			mpwrq_get_cqe_consumed_strides(title);
@@ -213,6 +229,15 @@ static u32 mlx5e_decompress_enhanced_cqe(struct mlx5e_rq *rq,
 	memcpy(cqd->mini_arr, cqe, sizeof(struct mlx5_cqe64));
 	for (i = 0; i < left; i++, cqd->mini_arr_idx++, cqcc++) {
 		mlx5e_decompress_cqe_no_hash(rq, wq, cqcc);
+		/*
+		 * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 */
 		INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
 				mlx5e_handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq_shampo,
 				rq, &cqd->title);
@@ -241,6 +266,15 @@ static inline u32 mlx5e_decompress_cqes_cont(struct mlx5e_rq *rq,
 			mlx5e_read_mini_arr_slot(wq, cqd, cqcc);
 
 		mlx5e_decompress_cqe_no_hash(rq, wq, cqcc);
+		/*
+		 * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 */
 		INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
 				mlx5e_handle_rx_cqe_mpwrq_shampo, mlx5e_handle_rx_cqe,
 				rq, &cqd->title);
@@ -263,6 +297,15 @@ static inline u32 mlx5e_decompress_cqes_start(struct mlx5e_rq *rq,
 	mlx5e_read_title_slot(rq, wq, cc);
 	mlx5e_read_mini_arr_slot(wq, cqd, cc + 1);
 	mlx5e_decompress_cqe(rq, wq, cc);
+	/*
+	 * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+	 */
 	INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
 			mlx5e_handle_rx_cqe_mpwrq_shampo, mlx5e_handle_rx_cqe,
 			rq, &cqd->title);
@@ -1007,6 +1050,11 @@ static void mlx5e_handle_shampo_hd_umr(struct mlx5e_shampo_umr umr,
 	shampo->ci = (shampo->ci + umr.len) & (shampo->hd_per_wq - 1);
 }
 
+/*
+ * 在以下使用mlx5e_poll_ico_cq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|181| <<mlx5e_napi_poll>> mlx5e_poll_ico_cq(&c->icosq.cq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|182| <<mlx5e_napi_poll>> if (mlx5e_poll_ico_cq(&c->async_icosq.cq))
+ */
 int mlx5e_poll_ico_cq(struct mlx5e_cq *cq)
 {
 	struct mlx5e_icosq *sq = container_of(cq, struct mlx5e_icosq, cq);
@@ -1031,6 +1079,21 @@ int mlx5e_poll_ico_cq(struct mlx5e_cq *cq)
 		u16 wqe_counter;
 		bool last_wqe;
 
+		/*
+		 * 在以下使用mlx5_cqwq_pop():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+		 */
 		mlx5_cqwq_pop(&cq->wq);
 
 		wqe_counter = be16_to_cpu(cqe->wqe_counter);
@@ -1562,6 +1625,11 @@ static inline void mlx5e_handle_csum(struct net_device *netdev,
 
 #define MLX5E_CE_BIT_MASK 0x80
 
+/*
+ * 在以下使用mlx5e_build_rx_skb():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1681| <<mlx5e_shampo_complete_rx_cqe>> mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1706| <<mlx5e_complete_rx_cqe>> mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
+ */
 static inline void mlx5e_build_rx_skb(struct mlx5_cqe64 *cqe,
 				      u32 cqe_bcnt,
 				      struct mlx5e_rq *rq,
@@ -1636,6 +1704,11 @@ static void mlx5e_shampo_complete_rx_cqe(struct mlx5e_rq *rq,
 	stats->gro_bytes += cqe_bcnt;
 	if (NAPI_GRO_CB(skb)->count != 1)
 		return;
+	/*
+	 * 在以下使用mlx5e_build_rx_skb():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1681| <<mlx5e_shampo_complete_rx_cqe>> mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1706| <<mlx5e_complete_rx_cqe>> mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
+	 */
 	mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
 	skb_reset_network_header(skb);
 	if (!skb_flow_dissect_flow_keys(skb, &rq->hw_gro_data->fk, 0)) {
@@ -1644,6 +1717,25 @@ static void mlx5e_shampo_complete_rx_cqe(struct mlx5e_rq *rq,
 	}
 }
 
+/*
+ * mlx5e_handle_rx_cqe_mpwrq
+ * mlx5e_rx_cq_process_basic_cqe_comp
+ * mlx5e_poll_rx_cq
+ * mlx5e_napi_poll
+ * __napi_poll
+ * net_rx_action
+ * handle_softirqs
+ * __irq_exit_rcu
+ * common_interrupt
+ * asm_common_interrupt
+ *
+ * 在以下使用mlx5e_complete_rx_cqe():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1883| <<mlx5e_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1930| <<mlx5e_handle_rx_cqe_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1979| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2469| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2802| <<mlx5e_trap_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+ */
 static inline void mlx5e_complete_rx_cqe(struct mlx5e_rq *rq,
 					 struct mlx5_cqe64 *cqe,
 					 u32 cqe_bcnt,
@@ -1653,6 +1745,11 @@ static inline void mlx5e_complete_rx_cqe(struct mlx5e_rq *rq,
 
 	stats->packets++;
 	stats->bytes += cqe_bcnt;
+	/*
+	 * 在以下使用mlx5e_build_rx_skb():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1681| <<mlx5e_shampo_complete_rx_cqe>> mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1706| <<mlx5e_complete_rx_cqe>> mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
+	 */
 	mlx5e_build_rx_skb(cqe, cqe_bcnt, rq, skb);
 }
 
@@ -1880,6 +1977,14 @@ static void mlx5e_handle_rx_cqe(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe)
 		goto wq_cyc_pop;
 	}
 
+	/*
+	 * 在以下使用mlx5e_complete_rx_cqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1883| <<mlx5e_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1930| <<mlx5e_handle_rx_cqe_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1979| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2469| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2802| <<mlx5e_trap_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 */
 	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
 
 	if (mlx5e_cqe_regb_chain(cqe))
@@ -1927,6 +2032,14 @@ static void mlx5e_handle_rx_cqe_rep(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe)
 		goto wq_cyc_pop;
 	}
 
+	/*
+	 * 在以下使用mlx5e_complete_rx_cqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1883| <<mlx5e_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1930| <<mlx5e_handle_rx_cqe_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1979| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2469| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2802| <<mlx5e_trap_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 */
 	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
 
 	if (rep->vlan && skb_vlan_tag_present(skb))
@@ -1938,11 +2051,23 @@ static void mlx5e_handle_rx_cqe_rep(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe)
 	mlx5_wq_cyc_pop(wq);
 }
 
+/*
+ * const struct mlx5e_rx_handlers mlx5e_rx_handlers_rep = {
+ *     .handle_rx_cqe       = mlx5e_handle_rx_cqe_rep,
+ *     .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq_rep,
+ * };
+ */
 static void mlx5e_handle_rx_cqe_mpwrq_rep(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe)
 {
 	u16 cstrides       = mpwrq_get_cqe_consumed_strides(cqe);
 	u16 wqe_id         = be16_to_cpu(cqe->wqe_id);
 	struct mlx5e_mpw_info *wi = mlx5e_get_mpw_info(rq, wqe_id);
+	/*
+	 * 在以下使用mpwrq_get_cqe_stride_index():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|111| <<mlx5e_read_enhanced_title_slot>> cqd->wqe_counter = mpwrq_get_cqe_stride_index(title) +
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2053| <<mlx5e_handle_rx_cqe_mpwrq_rep>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2569| <<mlx5e_handle_rx_cqe_mpwrq>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+	 */
 	u16 stride_ix      = mpwrq_get_cqe_stride_index(cqe);
 	u32 wqe_offset     = stride_ix << rq->mpwqe.log_stride_sz;
 	u32 head_offset    = wqe_offset & ((1 << rq->mpwqe.page_shift) - 1);
@@ -1976,6 +2101,14 @@ static void mlx5e_handle_rx_cqe_mpwrq_rep(struct mlx5e_rq *rq, struct mlx5_cqe64
 	if (!skb)
 		goto mpwrq_cqe_out;
 
+	/*
+	 * 在以下使用mlx5e_complete_rx_cqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1883| <<mlx5e_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1930| <<mlx5e_handle_rx_cqe_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1979| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2469| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2802| <<mlx5e_trap_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 */
 	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
 
 	mlx5e_rep_tc_receive(cqe, rq, skb);
@@ -1985,7 +2118,24 @@ static void mlx5e_handle_rx_cqe_mpwrq_rep(struct mlx5e_rq *rq, struct mlx5_cqe64
 		return;
 
 	wq  = &rq->mpwqe.wq;
+	/*
+	 * 在以下使用mlx5_wq_ll_get_wqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+	 */
 	wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	/*
+	 * 在以下使用mlx5_wq_ll_pop():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1292| <<mlx5e_free_rx_descs>> mlx5_wq_ll_pop(wq, wqe_ix_be,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2104| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2541| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2628| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 */
 	mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
 }
 
@@ -2178,6 +2328,26 @@ mlx5e_skb_from_cqe_mpwrq_nonlinear(struct mlx5e_rq *rq, struct mlx5e_mpw_info *w
 	return skb;
 }
 
+/*
+ * mlx5e_skb_from_cqe_mpwrq_linear
+ * mlx5e_handle_rx_cqe_mpwrq
+ * mlx5e_rx_cq_process_basic_cqe_comp
+ * mlx5e_poll_rx_cq
+ * mlx5e_napi_poll
+ * __napi_poll
+ * net_rx_action
+ * handle_softirqs
+ * __irq_exit_rcu
+ * common_interrupt
+ * asm_common_interrupt
+ * native_safe_halt
+ * default_idle
+ * default_idle_call
+ * cpuidle_idle_call
+ * do_idle
+ * cpu_startup_entry
+ * secondary_startup_64_no_verify
+ */
 static struct sk_buff *
 mlx5e_skb_from_cqe_mpwrq_linear(struct mlx5e_rq *rq, struct mlx5e_mpw_info *wi,
 				struct mlx5_cqe64 *cqe, u16 cqe_bcnt, u32 head_offset,
@@ -2423,14 +2593,69 @@ static void mlx5e_handle_rx_cqe_mpwrq_shampo(struct mlx5e_rq *rq, struct mlx5_cq
 
 	wq  = &rq->mpwqe.wq;
 	wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	/*
+	 * 在以下使用mlx5_wq_ll_pop():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1292| <<mlx5e_free_rx_descs>> mlx5_wq_ll_pop(wq, wqe_ix_be,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2104| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2541| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2628| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 */
 	mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
 }
 
+/*
+ * const struct mlx5e_rx_handlers mlx5e_rx_handlers_nic = {
+ *     .handle_rx_cqe       = mlx5e_handle_rx_cqe,
+ *     .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+ *     .handle_rx_cqe_mpwqe_shampo = mlx5e_handle_rx_cqe_mpwrq_shampo,
+ * };
+ *
+ *
+ * mlx5e_handle_rx_cqe_mpwrq
+ * mlx5e_rx_cq_process_basic_cqe_comp
+ * mlx5e_poll_rx_cq
+ * mlx5e_napi_poll
+ * __napi_poll
+ * net_rx_action
+ * handle_softirqs
+ * __irq_exit_rcu
+ * common_interrupt
+ * asm_common_interrupt
+ *
+ * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+ */
 static void mlx5e_handle_rx_cqe_mpwrq(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe)
 {
+	/*
+	 * mpwrq_get_cqe_consumed_strides(cqe) 取出的 cstrides 表示"这个 CQE
+	 * 一次性消费了多少个 stride". 在 MPWQE 模式下, 一个 CQE 不是必然只对应
+	 * 1 个 stride:
+	 * - 如果硬件用的是普通(非 mini-CQE)路径, cstrides 就是 1.
+	 * - 但开启 enhanced mini-CQE/压缩时, 硬件会把同一条 MPWQE 上连着的多个
+	 * stride 合成一个 CQE 回给驱动, 并在 CQE 里注明
+	 * consumed_strides(驱动就是用 mpwrq_get_cqe_consumed_strides()
+	 * 取的这一字段). 之后驱动会循环展开这些
+	 * mini-CQE，拆成多份包数据交给协议栈。
+	 *
+	 * 因此 mlx5e_handle_rx_cqe_mpwrq() 里先读出 cstrides, 后面再去更新
+	 * wi->consumed_strides, 并根据那个数量逐个处理. 换句话说, 这个 CQE
+	 * 可能一次"吃掉"多个 stride.
+	 */
 	u16 cstrides       = mpwrq_get_cqe_consumed_strides(cqe);
 	u16 wqe_id         = be16_to_cpu(cqe->wqe_id);
 	struct mlx5e_mpw_info *wi = mlx5e_get_mpw_info(rq, wqe_id);
+	/*
+	 * 在以下使用mpwrq_get_cqe_stride_index():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|111| <<mlx5e_read_enhanced_title_slot>> cqd->wqe_counter = mpwrq_get_cqe_stride_index(title) +
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2053| <<mlx5e_handle_rx_cqe_mpwrq_rep>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2569| <<mlx5e_handle_rx_cqe_mpwrq>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+	 */
 	u16 stride_ix      = mpwrq_get_cqe_stride_index(cqe);
 	u32 wqe_offset     = stride_ix << rq->mpwqe.log_stride_sz;
 	u32 head_offset    = wqe_offset & ((1 << rq->mpwqe.page_shift) - 1);
@@ -2457,6 +2682,9 @@ static void mlx5e_handle_rx_cqe_mpwrq(struct mlx5e_rq *rq, struct mlx5_cqe64 *cq
 
 	cqe_bcnt = mpwrq_get_cqe_byte_cnt(cqe);
 
+	/*
+	 * Ethernet是mlx5e_skb_from_cqe_mpwrq_linear()
+	 */
 	skb = INDIRECT_CALL_3(rq->mpwqe.skb_from_cqe_mpwrq,
 			      mlx5e_skb_from_cqe_mpwrq_linear,
 			      mlx5e_skb_from_cqe_mpwrq_nonlinear,
@@ -2466,6 +2694,14 @@ static void mlx5e_handle_rx_cqe_mpwrq(struct mlx5e_rq *rq, struct mlx5_cqe64 *cq
 	if (!skb)
 		goto mpwrq_cqe_out;
 
+	/*
+	 * 在以下使用mlx5e_complete_rx_cqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1883| <<mlx5e_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1930| <<mlx5e_handle_rx_cqe_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1979| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2469| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2802| <<mlx5e_trap_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 */
 	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
 
 	if (mlx5e_cqe_regb_chain(cqe))
@@ -2481,10 +2717,31 @@ static void mlx5e_handle_rx_cqe_mpwrq(struct mlx5e_rq *rq, struct mlx5_cqe64 *cq
 		return;
 
 	wq  = &rq->mpwqe.wq;
+	/*
+	 * 在以下使用mlx5_wq_ll_get_wqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+	 */
 	wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	/*
+	 * 在以下使用mlx5_wq_ll_pop():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1292| <<mlx5e_free_rx_descs>> mlx5_wq_ll_pop(wq, wqe_ix_be,
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2104| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2541| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2628| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+	 */
 	mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
 }
 
+/*
+ * 在以下使用mlx5e_rx_cq_process_enhanced_cqe_comp():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2881| <<mlx5e_poll_rx_cq>> work_done = mlx5e_rx_cq_process_enhanced_cqe_comp(rq, cqwq,
+ */
 static int mlx5e_rx_cq_process_enhanced_cqe_comp(struct mlx5e_rq *rq,
 						 struct mlx5_cqwq *cqwq,
 						 int budget_rem)
@@ -2516,8 +2773,32 @@ static int mlx5e_rx_cq_process_enhanced_cqe_comp(struct mlx5e_rq *rq,
 			continue;
 		}
 		title_cqe = cqe;
+		/*
+		 * 在以下使用mlx5_cqwq_pop():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+		 */
 		mlx5_cqwq_pop(cqwq);
 
+		/*
+		 * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 */
 		INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
 				mlx5e_handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq_shampo,
 				rq, cqe);
@@ -2534,6 +2815,27 @@ static int mlx5e_rx_cq_process_enhanced_cqe_comp(struct mlx5e_rq *rq,
 	return work_done;
 }
 
+/*
+ * mlx5e_rx_cq_process_basic_cqe_comp
+ * mlx5e_poll_rx_cq
+ * mlx5e_napi_poll
+ * __napi_poll
+ * net_rx_action
+ * handle_softirqs
+ * __irq_exit_rcu
+ * common_interrupt
+ * asm_common_interrupt
+ * native_safe_halt
+ * default_idle
+ * default_idle_call
+ * cpuidle_idle_call
+ * do_idle
+ * cpu_startup_entry
+ * secondary_startup_64_no_verify
+ *
+ * 在以下使用mlx5e_rx_cq_process_basic_cqe_comp():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2578| <<mlx5e_poll_rx_cq>> work_done = mlx5e_rx_cq_process_basic_cqe_comp(rq, cqwq,
+ */
 static int mlx5e_rx_cq_process_basic_cqe_comp(struct mlx5e_rq *rq,
 					      struct mlx5_cqwq *cqwq,
 					      int budget_rem)
@@ -2552,7 +2854,31 @@ static int mlx5e_rx_cq_process_basic_cqe_comp(struct mlx5e_rq *rq,
 			continue;
 		}
 
+		/*
+		 * 在以下使用mlx5_cqwq_pop():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+		 */
 		mlx5_cqwq_pop(cqwq);
+		/*
+		 * 在以下使用mlx5e_handle_rx_cqe_mpwrq():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|77| <<global>> .handle_rx_cqe_mpwqe = mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|216| <<mlx5e_decompress_enhanced_cqe>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|244| <<mlx5e_decompress_cqes_cont>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|266| <<mlx5e_decompress_cqes_start>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2521| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2556| <<mlx5e_rx_cq_process_basic_cqe_comp>> INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
+		 */
 		INDIRECT_CALL_3(rq->handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq,
 				mlx5e_handle_rx_cqe, mlx5e_handle_rx_cqe_mpwrq_shampo,
 				rq, cqe);
@@ -2562,6 +2888,13 @@ static int mlx5e_rx_cq_process_basic_cqe_comp(struct mlx5e_rq *rq,
 	return work_done;
 }
 
+/*
+ * 在以下使用mlx5e_poll_rx_cq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|282| <<mlx5e_ptp_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/trap.c|21| <<mlx5e_trap_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|174| <<mlx5e_napi_poll>> work_done = mlx5e_poll_rx_cq(&xskrq->cq, budget);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|177| <<mlx5e_napi_poll>> work_done += mlx5e_poll_rx_cq(&rq->cq, budget - work_done);
+ */
 int mlx5e_poll_rx_cq(struct mlx5e_cq *cq, int budget)
 {
 	struct mlx5e_rq *rq = container_of(cq, struct mlx5e_rq, cq);
@@ -2799,6 +3132,14 @@ static void mlx5e_trap_handle_rx_cqe(struct mlx5e_rq *rq, struct mlx5_cqe64 *cqe
 	if (!skb)
 		goto wq_cyc_pop;
 
+	/*
+	 * 在以下使用mlx5e_complete_rx_cqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1883| <<mlx5e_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1930| <<mlx5e_handle_rx_cqe_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1979| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2469| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2802| <<mlx5e_trap_handle_rx_cqe>> mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
+	 */
 	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
 	skb_push(skb, ETH_HLEN);
 
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en_tx.c b/drivers/net/ethernet/mellanox/mlx5/core/en_tx.c
index 33936ef45ac1..3d32f84b7618 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_tx.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_tx.c
@@ -796,6 +796,12 @@ void mlx5e_txqsq_wake(struct mlx5e_txqsq *sq)
 	}
 }
 
+/*
+ * 在以下使用mlx5e_poll_tx_cq():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|277| <<mlx5e_ptp_napi_poll>> busy |= mlx5e_poll_tx_cq(&c->ptpsq[i].txqsq.cq, budget);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|150| <<mlx5e_napi_poll>> busy |= mlx5e_poll_tx_cq(&c->sq[i].cq, budget);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|160| <<mlx5e_napi_poll>> busy |= mlx5e_poll_tx_cq(&sq->cq, budget);
+ */
 bool mlx5e_poll_tx_cq(struct mlx5e_cq *cq, int napi_budget)
 {
 	struct mlx5e_sq_stats *stats;
@@ -836,6 +842,21 @@ bool mlx5e_poll_tx_cq(struct mlx5e_cq *cq, int napi_budget)
 		bool last_wqe;
 		u16 ci;
 
+		/*
+		 * 在以下使用mlx5_cqwq_pop():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+		 */
 		mlx5_cqwq_pop(&cq->wq);
 
 		wqe_counter = be16_to_cpu(cqe->wqe_counter);
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c b/drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c
index a7d9b7cb4297..c0ba520845c6 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c
@@ -121,6 +121,15 @@ static bool mlx5e_napi_xsk_post(struct mlx5e_xdpsq *xsksq, struct mlx5e_rq *xskr
 	return busy_xsk;
 }
 
+/*
+ * mlx5e_poll_rx_cq
+ * mlx5e_napi_poll
+ * __napi_poll
+ * net_rx_action
+ * handle_softirqs
+ * __irq_exit_rcu
+ * common_interrupt
+ */
 int mlx5e_napi_poll(struct napi_struct *napi, int budget)
 {
 	struct mlx5e_channel *c = container_of(napi, struct mlx5e_channel,
@@ -173,11 +182,23 @@ int mlx5e_napi_poll(struct napi_struct *napi, int budget)
 	if (xsk_open)
 		work_done = mlx5e_poll_rx_cq(&xskrq->cq, budget);
 
+	/*
+	 * 在以下使用mlx5e_poll_rx_cq():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|282| <<mlx5e_ptp_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en/trap.c|21| <<mlx5e_trap_napi_poll>> work_done = mlx5e_poll_rx_cq(&rq->cq, budget);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|174| <<mlx5e_napi_poll>> work_done = mlx5e_poll_rx_cq(&xskrq->cq, budget);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|177| <<mlx5e_napi_poll>> work_done += mlx5e_poll_rx_cq(&rq->cq, budget - work_done);
+	 */
 	if (likely(budget - work_done))
 		work_done += mlx5e_poll_rx_cq(&rq->cq, budget - work_done);
 
 	busy |= work_done == budget;
 
+	/*
+	 * 在以下使用mlx5e_poll_ico_cq():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|181| <<mlx5e_napi_poll>> mlx5e_poll_ico_cq(&c->icosq.cq);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c|182| <<mlx5e_napi_poll>> if (mlx5e_poll_ico_cq(&c->async_icosq.cq))
+	 */
 	mlx5e_poll_ico_cq(&c->icosq.cq);
 	if (mlx5e_poll_ico_cq(&c->async_icosq.cq))
 		/* Don't clear the flag if nothing was polled to prevent
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c b/drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c
index 3d0b28f2f0b6..b535c41aa8bf 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c
@@ -74,6 +74,12 @@ static int mlx5i_set_ringparam(struct net_device *dev,
 {
 	struct mlx5e_priv *priv = mlx5i_epriv(dev);
 
+	/*
+	 * 在以下使用mlx5e_ethtool_set_ringparam():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c|411| <<mlx5e_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rep.c|330| <<mlx5e_rep_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c|77| <<mlx5i_set_ringparam>> return mlx5e_ethtool_set_ringparam(priv, param);
+	 */
 	return mlx5e_ethtool_set_ringparam(priv, param);
 }
 
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/wq.c b/drivers/net/ethernet/mellanox/mlx5/core/wq.c
index 3091dd014650..c4f98f16152b 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/wq.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/wq.c
@@ -156,6 +156,14 @@ int mlx5_wq_qp_create(struct mlx5_core_dev *mdev, struct mlx5_wq_param *param,
 	return err;
 }
 
+/*
+ * 在以下使用mlx5_cqwq_create():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|2051| <<mlx5e_alloc_cq_common>> err = mlx5_cqwq_create(mdev, &param->wq, param->cqc, &cq->wq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|430| <<mlx5_fpga_conn_create_cq>> err = mlx5_cqwq_create(mdev, &wqp, temp_cqc, &conn->cq.wq,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|56| <<mlx5_aso_alloc_cq>> err = mlx5_cqwq_create(mdev, &param, cqc_data, &cq->wq, &cq->wq_ctrl);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|772| <<hws_send_ring_alloc_cq>> err = mlx5_cqwq_create(mdev, &param, cqc_data, &cq->wq, &cq->wq_ctrl);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|1083| <<dr_create_cq>> err = mlx5_cqwq_create(mdev, &wqp, temp_cqc, &cq->wq,
+ */
 int mlx5_cqwq_create(struct mlx5_core_dev *mdev, struct mlx5_wq_param *param,
 		     void *cqc, struct mlx5_cqwq *wq,
 		     struct mlx5_wq_ctrl *wq_ctrl)
@@ -200,6 +208,16 @@ static void mlx5_wq_ll_init_list(struct mlx5_wq_ll *wq)
 	int i;
 
 	for (i = 0; i < wq->fbc.sz_m1; i++) {
+		/*
+		 * 在以下使用mlx5_wq_ll_get_wqe():
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+		 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+		 */
 		next_seg = mlx5_wq_ll_get_wqe(wq, i);
 		next_seg->next_wqe_index = cpu_to_be16(i + 1);
 	}
diff --git a/drivers/net/ethernet/mellanox/mlx5/core/wq.h b/drivers/net/ethernet/mellanox/mlx5/core/wq.h
index e4ef1d24a3ad..9485b291e834 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/wq.h
+++ b/drivers/net/ethernet/mellanox/mlx5/core/wq.h
@@ -192,6 +192,12 @@ static inline u32 mlx5_cqwq_ctr2ix(struct mlx5_cqwq *wq, u32 ctr)
 	return ctr & wq->fbc.sz_m1;
 }
 
+/*
+ * 在以下使用mlx5_cqwq_get_ci():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/health.c|33| <<mlx5e_health_cq_diag_fmsg>> devlink_fmsg_u32_pair_put(fmsg, "ci", mlx5_cqwq_get_ci(&cq->wq));
+ *   - drivers/net/ethernet/mellanox/mlx5/core/wq.h|232| <<mlx5_cqwq_get_cqe>> u32 ci = mlx5_cqwq_get_ci(wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/wq.h|250| <<mlx5_cqwq_get_cqe_enahnced_comp>> u32 ci = mlx5_cqwq_get_ci(wq);
+ */
 static inline u32 mlx5_cqwq_get_ci(struct mlx5_cqwq *wq)
 {
 	return mlx5_cqwq_ctr2ix(wq, wq->cc);
@@ -217,6 +223,21 @@ static inline u32 mlx5_cqwq_get_wrap_cnt(struct mlx5_cqwq *wq)
 	return mlx5_cqwq_get_ctr_wrap_cnt(wq, wq->cc);
 }
 
+/*
+ * 在以下使用mlx5_cqwq_pop():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|242| <<mlx5e_ptp_poll_ts_cq>> mlx5_cqwq_pop(cqwq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|713| <<mlx5e_poll_xdpsq_cq>> mlx5_cqwq_pop(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1139| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1142| <<mlx5e_flush_rq_cq>> mlx5_cqwq_pop(cqwq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1076| <<mlx5e_poll_ico_cq>> mlx5_cqwq_pop(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2647| <<mlx5e_rx_cq_process_enhanced_cqe_comp>> mlx5_cqwq_pop(cqwq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2713| <<mlx5e_rx_cq_process_basic_cqe_comp>> mlx5_cqwq_pop(cqwq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|845| <<mlx5e_poll_tx_cq>> mlx5_cqwq_pop(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|376| <<mlx5_fpga_conn_cqes>> mlx5_cqwq_pop(&conn->cq.wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|403| <<mlx5_aso_poll_cq>> mlx5_cqwq_pop(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|466| <<mlx5hws_cq_poll_one>> mlx5_cqwq_pop(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|228| <<dr_cq_poll_one>> mlx5_cqwq_pop(&dr_cq->wq);
+ */
 static inline void mlx5_cqwq_pop(struct mlx5_cqwq *wq)
 {
 	wq->cc++;
@@ -227,6 +248,24 @@ static inline void mlx5_cqwq_update_db_record(struct mlx5_cqwq *wq)
 	*wq->db = cpu_to_be32(wq->cc & 0xffffff);
 }
 
+/*
+ * 在以下使用mlx5_cqwq_get_cqe():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|237| <<mlx5e_ptp_poll_ts_cq>> cqe = mlx5_cqwq_get_cqe(cqwq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/ptp.c|246| <<mlx5e_ptp_poll_ts_cq>> } while ((++work_done < budget) && (cqe = mlx5_cqwq_get_cqe(cqwq)));
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|698| <<mlx5e_poll_xdpsq_cq>> cqe = mlx5_cqwq_get_cqe(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en/xdp.c|735| <<mlx5e_poll_xdpsq_cq>> } while ((++i < MLX5E_TX_CQ_POLL_BUDGET) && (cqe = mlx5_cqwq_get_cqe(&cq->wq)));
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1141| <<mlx5e_flush_rq_cq>> while ((cqe = mlx5_cqwq_get_cqe(cqwq)))
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1020| <<mlx5e_poll_ico_cq>> cqe = mlx5_cqwq_get_cqe(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|1085| <<mlx5e_poll_ico_cq>> } while ((++i < MLX5E_TX_CQ_POLL_BUDGET) && (cqe = mlx5_cqwq_get_cqe(&cq->wq)));
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2547| <<mlx5e_rx_cq_process_basic_cqe_comp>> while (work_done < budget_rem && (cqe = mlx5_cqwq_get_cqe(cqwq))) {
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|815| <<mlx5e_poll_tx_cq>> cqe = mlx5_cqwq_get_cqe(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_tx.c|884| <<mlx5e_poll_tx_cq>> } while ((++i < MLX5E_TX_CQ_POLL_BUDGET) && (cqe = mlx5_cqwq_get_cqe(&cq->wq)));
+ *   - drivers/net/ethernet/mellanox/mlx5/core/fpga/conn.c|371| <<mlx5_fpga_conn_cqes>> cqe = mlx5_cqwq_get_cqe(&conn->cq.wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/lib/aso.c|396| <<mlx5_aso_poll_cq>> cqe = mlx5_cqwq_get_cqe(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|455| <<mlx5hws_cq_poll_one>> cqe64 = mlx5_cqwq_get_cqe(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/hws/mlx5hws_send.c|486| <<hws_send_engine_poll_cq>> cqe = mlx5_cqwq_get_cqe(&cq->wq);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/steering/sws/dr_send.c|217| <<dr_cq_poll_one>> cqe64 = mlx5_cqwq_get_cqe(&dr_cq->wq);
+ */
 static inline struct mlx5_cqe64 *mlx5_cqwq_get_cqe(struct mlx5_cqwq *wq)
 {
 	u32 ci = mlx5_cqwq_get_ci(wq);
@@ -280,6 +319,16 @@ static inline int mlx5_wq_ll_missing(struct mlx5_wq_ll *wq)
 	return wq->fbc.sz_m1 - wq->cur_sz;
 }
 
+/*
+ * 在以下使用mlx5_wq_ll_get_wqe():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+ */
 static inline void *mlx5_wq_ll_get_wqe(struct mlx5_wq_ll *wq, u16 ix)
 {
 	return mlx5_frag_buf_get_wqe(&wq->fbc, ix);
@@ -287,6 +336,16 @@ static inline void *mlx5_wq_ll_get_wqe(struct mlx5_wq_ll *wq, u16 ix)
 
 static inline u16 mlx5_wq_ll_get_wqe_next_ix(struct mlx5_wq_ll *wq, u16 ix)
 {
+	/*
+	 * 在以下使用mlx5_wq_ll_get_wqe():
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|958| <<mlx5e_alloc_rq>> mlx5_wq_ll_get_wqe(&rq->mpwqe.wq, i);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1290| <<mlx5e_free_rx_descs>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_ix);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2103| <<mlx5e_handle_rx_cqe_mpwrq_rep>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2540| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2627| <<mlx5e_handle_rx_cqe_mpwrq>> wqe = mlx5_wq_ll_get_wqe(wq, wqe_id);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|203| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+	 *   - drivers/net/ethernet/mellanox/mlx5/core/wq.c|206| <<mlx5_wq_ll_init_list>> next_seg = mlx5_wq_ll_get_wqe(wq, i);
+	 */
 	struct mlx5_wqe_srq_next_seg *wqe = mlx5_wq_ll_get_wqe(wq, ix);
 
 	return be16_to_cpu(wqe->next_wqe_index);
@@ -299,6 +358,22 @@ static inline void mlx5_wq_ll_push(struct mlx5_wq_ll *wq, u16 head_next)
 	wq->cur_sz++;
 }
 
+/*
+ * struct mlx5_wq_ll {
+ *     struct mlx5_frag_buf_ctrl fbc;
+ *     __be32                  *db;
+ *     __be16                  *tail_next;
+ *     u16                     head;
+ *     u16                     wqe_ctr; 
+ *     u16                     cur_sz;
+ * };
+ *
+ * 在以下使用mlx5_wq_ll_pop():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_main.c|1292| <<mlx5e_free_rx_descs>> mlx5_wq_ll_pop(wq, wqe_ix_be,
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2104| <<mlx5e_handle_rx_cqe_mpwrq_rep>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2541| <<mlx5e_handle_rx_cqe_mpwrq_shampo>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2628| <<mlx5e_handle_rx_cqe_mpwrq>> mlx5_wq_ll_pop(wq, cqe->wqe_id, &wqe->next.next_wqe_index);
+ */
 static inline void mlx5_wq_ll_pop(struct mlx5_wq_ll *wq, __be16 ix,
 				  __be16 *next_tail_next)
 {
diff --git a/drivers/vfio/pci/mlx5/cmd.c b/drivers/vfio/pci/mlx5/cmd.c
index ef1ec864f252..95255147b090 100644
--- a/drivers/vfio/pci/mlx5/cmd.c
+++ b/drivers/vfio/pci/mlx5/cmd.c
@@ -225,6 +225,11 @@ void mlx5vf_cmd_remove_migratable(struct mlx5vf_pci_core_device *mvdev)
 	destroy_workqueue(mvdev->cb_wq);
 }
 
+/*
+ * 在以下使用mlx5vf_cmd_set_migratable():
+ *   - drivers/vfio/pci/mlx5/main.c|1437| <<mlx5vf_pci_init_dev>> mlx5vf_cmd_set_migratable(mvdev,
+ *          &mlx5vf_pci_mig_ops, &mlx5vf_pci_log_ops);
+ */
 void mlx5vf_cmd_set_migratable(struct mlx5vf_pci_core_device *mvdev,
 			       const struct vfio_migration_ops *mig_ops,
 			       const struct vfio_log_ops *log_ops)
diff --git a/drivers/vfio/pci/mlx5/main.c b/drivers/vfio/pci/mlx5/main.c
index 47511cdfc707..8c5217f1a366 100644
--- a/drivers/vfio/pci/mlx5/main.c
+++ b/drivers/vfio/pci/mlx5/main.c
@@ -1232,6 +1232,10 @@ mlx5vf_pci_step_device_state_locked(struct mlx5vf_pci_core_device *mvdev,
 	if (cur == VFIO_DEVICE_STATE_STOP && new == VFIO_DEVICE_STATE_RESUMING) {
 		struct mlx5_vf_migration_file *migf;
 
+		/*
+		 * 在以下使用mlx5vf_pci_resume_device_data():
+		 *   - drivers/vfio/pci/mlx5/main.c|1235| <<mlx5vf_pci_step_device_state_locked>> migf = mlx5vf_pci_resume_device_data(mvdev);
+		 */
 		migf = mlx5vf_pci_resume_device_data(mvdev);
 		if (IS_ERR(migf))
 			return ERR_CAST(migf);
diff --git a/drivers/vfio/pci/vfio_pci_core.c b/drivers/vfio/pci/vfio_pci_core.c
index f57ae4180471..7a5415f2e0e6 100644
--- a/drivers/vfio/pci/vfio_pci_core.c
+++ b/drivers/vfio/pci/vfio_pci_core.c
@@ -1976,6 +1976,18 @@ pci_ers_result_t vfio_pci_core_aer_err_detected(struct pci_dev *pdev,
 
 	mutex_unlock(&vdev->igate);
 
+	/*
+	 * 在以下使用vfio_device_put():
+	 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+	 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+	 */
 	vfio_device_put(device);
 
 	return PCI_ERS_RESULT_CAN_RECOVER;
@@ -2025,6 +2037,18 @@ int vfio_pci_core_sriov_configure(struct pci_dev *pdev, int nr_virtfn)
 out_unlock:
 	mutex_unlock(&vfio_pci_sriov_pfs_mutex);
 out_put:
+	/*
+	 * 在以下使用vfio_device_put():
+	 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+	 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+	 */
 	vfio_device_put(device);
 	return ret;
 }
diff --git a/drivers/vfio/vfio_main.c b/drivers/vfio/vfio_main.c
index c7d2cdc5b0c0..6fc827c33c5f 100644
--- a/drivers/vfio/vfio_main.c
+++ b/drivers/vfio/vfio_main.c
@@ -597,6 +597,18 @@ static struct vfio_group *vfio_group_get_from_dev(struct device *dev)
 /**
  * Device objects - create, release, get, put, search
  */
+/*
+ * 在以下使用vfio_device_put():
+ *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+ *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+ *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+ *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+ *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+ *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+ *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+ *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+ *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+ */
 /* Device reference always implies a group reference */
 void vfio_device_put(struct vfio_device *device)
 {
@@ -694,6 +706,18 @@ static int vfio_dev_viable(struct device *dev, void *data)
 
 	device = vfio_group_get_device(group, dev);
 	if (device) {
+		/*
+		 * 在以下使用vfio_device_put():
+		 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+		 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+		 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+		 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+		 */
 		vfio_device_put(device);
 		return 0;
 	}
@@ -711,6 +735,18 @@ static int vfio_group_nb_add_dev(struct vfio_group *group, struct device *dev)
 	/* Do we already know about it?  We shouldn't */
 	device = vfio_group_get_device(group, dev);
 	if (WARN_ON_ONCE(device)) {
+		/*
+		 * 在以下使用vfio_device_put():
+		 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+		 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+		 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+		 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+		 */
 		vfio_device_put(device);
 		return 0;
 	}
@@ -958,6 +994,18 @@ int vfio_register_group_dev(struct vfio_device *device)
 	if (existing_device) {
 		dev_WARN(device->dev, "Device already exists on group %d\n",
 			 iommu_group_id(iommu_group));
+		/*
+		 * 在以下使用vfio_device_put():
+		 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+		 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+		 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+		 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+		 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+		 */
 		vfio_device_put(existing_device);
 		vfio_group_put(group);
 		return -EBUSY;
@@ -1030,6 +1078,23 @@ static struct vfio_device *vfio_device_get_from_name(struct vfio_group *group,
 	return device;
 }
 
+/*
+ * vfio_unregister_group_dev
+ * vfio_pci_core_unregister_device
+ * mlx5vf_pci_remove
+ * pci_device_remove
+ * __device_release_driver
+ * device_driver_detach
+ * unbind_store
+ * kernfs_fop_write_iter
+ * new_sync_write
+ * vfs_write
+ * ksys_write
+ * bpf_trampoline_6442491955_1
+ * __x64_sys_write
+ * do_syscall_64
+ * entry_SYSCALL_64_after_hwframe
+ */
 /*
  * Decrement the device reference count and wait for the device to be
  * removed.  Open file descriptors for the device... */
@@ -1059,9 +1124,24 @@ void vfio_unregister_group_dev(struct vfio_device *device)
 	}
 	WARN_ON(!unbound);
 
+	/*
+	 * 在以下使用vfio_device_put():
+	 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+	 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+	 */
 	vfio_device_put(device);
 	rc = try_wait_for_completion(&device->comp);
 	while (rc <= 0) {
+		/*
+		 * vfio_pci_core_request()
+		 */
 		if (device->ops->request)
 			device->ops->request(device, i++);
 
@@ -1606,6 +1686,18 @@ static int vfio_group_get_device_fd(struct vfio_group *group, char *buf)
 	mutex_unlock(&device->dev_set->lock);
 	module_put(device->dev->driver->owner);
 err_device_put:
+	/*
+	 * 在以下使用vfio_device_put():
+	 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+	 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+	 */
 	vfio_device_put(device);
 	return ret;
 }
@@ -1753,6 +1845,18 @@ static int vfio_device_fops_release(struct inode *inode, struct file *filep)
 
 	vfio_group_try_dissolve_container(device->group);
 
+	/*
+	 * 在以下使用vfio_device_put():
+	 *   - drivers/gpu/drm/i915/gvt/kvmgt.c|787| <<kvmgt_put_vfio_device>> vfio_device_put(vdev->vfio_device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|1979| <<vfio_pci_core_aer_err_detected>> vfio_device_put(device);
+	 *   - drivers/vfio/pci/vfio_pci_core.c|2028| <<vfio_pci_core_sriov_configure>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|697| <<vfio_dev_viable>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|714| <<vfio_group_nb_add_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|961| <<vfio_register_group_dev>> vfio_device_put(existing_device);
+	 *   - drivers/vfio/vfio_main.c|1062| <<vfio_unregister_group_dev>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1609| <<vfio_group_get_device_fd>> vfio_device_put(device);
+	 *   - drivers/vfio/vfio_main.c|1756| <<vfio_device_fops_release>> vfio_device_put(device);
+	 */
 	vfio_device_put(device);
 
 	return 0;
diff --git a/drivers/virtio/virtio_ring.c b/drivers/virtio/virtio_ring.c
index 3e5b14e97a43..edad0e9b2cd2 100644
--- a/drivers/virtio/virtio_ring.c
+++ b/drivers/virtio/virtio_ring.c
@@ -1065,6 +1065,10 @@ static struct vring_packed_desc *alloc_indirect_packed(unsigned int total_sg,
 	return desc;
 }
 
+/*
+ * 在以下使用virtqueue_add_indirect_packed():
+ *   - drivers/virtio/virtio_ring.c|1214| <<virtqueue_add_packed>> err = virtqueue_add_indirect_packed(vq, sgs, total_sg, out_sgs,
+ */
 static int virtqueue_add_indirect_packed(struct vring_virtqueue *vq,
 				       struct scatterlist *sgs[],
 				       unsigned int total_sg,
@@ -1179,6 +1183,10 @@ static int virtqueue_add_indirect_packed(struct vring_virtqueue *vq,
 	return -ENOMEM;
 }
 
+/*
+ * 在以下使用virtqueue_add_packed():
+ *   - drivers/virtio/virtio_ring.c|1834| <<virtqueue_add>> return vq->packed_ring ? virtqueue_add_packed(_vq, sgs, total_sg,
+ */
 static inline int virtqueue_add_packed(struct virtqueue *_vq,
 				       struct scatterlist *sgs[],
 				       unsigned int total_sg,
@@ -1380,6 +1388,21 @@ static bool virtqueue_kick_prepare_packed(struct virtqueue *_vq)
 	return needs_kick;
 }
 
+/*
+ * [0] detach_buf_packed
+ * [0] virtqueue_get_buf_ctx_packed
+ * [0] free_old_xmit_skbs
+ * [0] virtnet_poll_tx
+ * [0] __napi_poll
+ * [0] net_rx_action
+ * [0] handle_softirqs
+ * [0] irq_exit_rcu
+ * [0] common_interrupt
+ *
+ * 在以下使用detach_buf_packed():
+ *   - drivers/virtio/virtio_ring.c|1487| <<virtqueue_get_buf_ctx_packed>> detach_buf_packed(vq, id, ctx);
+ *   - drivers/virtio/virtio_ring.c|1652| <<virtqueue_detach_unused_buf_packed>> detach_buf_packed(vq, i, NULL);
+ */
 static void detach_buf_packed(struct vring_virtqueue *vq,
 			      unsigned int id, void **ctx)
 {
diff --git a/include/linux/mlx5/device.h b/include/linux/mlx5/device.h
index 14a608e3a948..6fe079272b19 100644
--- a/include/linux/mlx5/device.h
+++ b/include/linux/mlx5/device.h
@@ -976,6 +976,12 @@ static inline bool mpwrq_is_filler_cqe(struct mlx5_cqe64 *cqe)
 	return 0x8000 & be16_to_cpu(bc->filler_consumed_strides);
 }
 
+/*
+ * 在以下使用mpwrq_get_cqe_stride_index():
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|111| <<mlx5e_read_enhanced_title_slot>> cqd->wqe_counter = mpwrq_get_cqe_stride_index(title) +
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2053| <<mlx5e_handle_rx_cqe_mpwrq_rep>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+ *   - drivers/net/ethernet/mellanox/mlx5/core/en_rx.c|2569| <<mlx5e_handle_rx_cqe_mpwrq>> u16 stride_ix = mpwrq_get_cqe_stride_index(cqe);
+ */
 static inline u16 mpwrq_get_cqe_stride_index(struct mlx5_cqe64 *cqe)
 {
 	return be16_to_cpu(cqe->wqe_counter);
diff --git a/virt/kvm/kvm_main.c b/virt/kvm/kvm_main.c
index 04f8fcaa82c7..1aef2f7bf4eb 100644
--- a/virt/kvm/kvm_main.c
+++ b/virt/kvm/kvm_main.c
@@ -3466,6 +3466,19 @@ update_halt_poll_stats(struct kvm_vcpu *vcpu, u64 poll_ns, bool waited)
 /*
  * The vCPU has executed a HLT instruction with in-kernel mode enabled.
  */
+/*
+ * 在以下使用kvm_vcpu_block():
+ *   - arch/arm64/kvm/handle_exit.c|98| <<kvm_handle_wfx>> kvm_vcpu_block(vcpu);
+ *   - arch/arm64/kvm/psci.c|49| <<kvm_psci_vcpu_suspend>> kvm_vcpu_block(vcpu);
+ *   - arch/mips/kvm/emulate.c|955| <<kvm_mips_emul_wait>> kvm_vcpu_block(vcpu);
+ *   - arch/powerpc/kvm/book3s_pr.c|495| <<kvmppc_set_msr_pr>> kvm_vcpu_block(vcpu);
+ *   - arch/powerpc/kvm/book3s_pr_papr.c|379| <<kvmppc_h_pr>> kvm_vcpu_block(vcpu);
+ *   - arch/powerpc/kvm/booke.c|721| <<kvmppc_core_prepare_to_enter>> kvm_vcpu_block(vcpu);
+ *   - arch/powerpc/kvm/powerpc.c|239| <<kvmppc_kvm_pv>> kvm_vcpu_block(vcpu);
+ *   - arch/s390/kvm/interrupt.c|1342| <<kvm_s390_handle_wait>> kvm_vcpu_block(vcpu);
+ *   - arch/x86/kvm/x86.c|10332| <<vcpu_block>> kvm_vcpu_block(vcpu);
+ *   - arch/x86/kvm/x86.c|10544| <<kvm_arch_vcpu_ioctl_run>> kvm_vcpu_block(vcpu);
+ */
 void kvm_vcpu_block(struct kvm_vcpu *vcpu)
 {
 	bool halt_poll_allowed = !kvm_arch_no_poll(vcpu);
-- 
2.50.1 (Apple Git-155)

