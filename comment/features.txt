

- The QEMU should not crash on MCE pages (Intel, AMD and aarch64)
- The QEMU migration should not crash on MCE pages (Intel, AMD and aarch64)
- VFIO migration support (Intel, AMD and aarch64)
- Xen on KVM (Intel, AMD and aarch64)
- haltpoll on x86 and aarch64, and KVM
- usage of PMUs (e.g., LBR, PEBS, PT)
- state remains after snapshot/migration


Performance:

- TLB flush overhead
- IPI overhead
